import type { Format, SeaOptions } from '../types/index.js';
import type { ExePlatform } from '../types/index.js';
export declare const buildExecutable: ({ builderBinaryPath, executablePath, executableBinaryPath, mainFile, format, root, seaOptions, targetPlatform, }: {
    builderBinaryPath: string;
    executablePath: string;
    executableBinaryPath: string;
    mainFile: string;
    format: Extract<Format, 'esm' | 'cjs'>;
    root: string;
    seaOptions: SeaOptions;
    targetPlatform: ExePlatform;
}) => Promise<void>;
