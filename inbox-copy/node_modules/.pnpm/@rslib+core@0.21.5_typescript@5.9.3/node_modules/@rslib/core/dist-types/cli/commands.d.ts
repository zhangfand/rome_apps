import type { LogLevel, RsbuildMode } from '@rsbuild/core';
import type { ConfigLoader } from '../loadConfig.js';
import type { Format, Syntax } from '../types/index.js';
export type CommonOptions = {
    root?: string;
    config?: string;
    configLoader?: ConfigLoader;
    env?: boolean;
    envDir?: string;
    envMode?: string;
    lib?: string[];
    logLevel?: LogLevel;
};
export type BuildOptions = CommonOptions & {
    watch?: boolean;
    format?: Format;
    entry?: string[];
    distPath?: string;
    bundle?: boolean;
    syntax?: Syntax;
    target?: string;
    dts?: boolean;
    externals?: string[];
    minify?: boolean;
    clean?: boolean;
    autoExtension?: boolean;
    autoExternal?: boolean;
    tsconfig?: string;
};
export type InspectOptions = CommonOptions & {
    mode?: RsbuildMode;
    output?: string;
    verbose?: boolean;
};
export type MfDevOptions = CommonOptions;
export declare function setupCommands(): void;
