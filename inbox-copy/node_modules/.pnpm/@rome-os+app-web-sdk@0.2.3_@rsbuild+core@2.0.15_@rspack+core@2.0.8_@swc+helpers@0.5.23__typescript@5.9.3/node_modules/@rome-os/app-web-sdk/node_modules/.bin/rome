#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
basedir_win="$basedir"
exe=""
msys=""

case `uname -a` in
  *CYGWIN*|*MINGW*|*MSYS*)
    if command -v cygpath > /dev/null 2>&1; then
      basedir_win=`cygpath -w "$basedir"`
    fi
    exe=".exe"
    msys="true"
  ;;
  *WSL2*)
    if command -v wslpath > /dev/null 2>&1; then
      basedir_win="$(wslpath -w "$basedir" 2> /dev/null)"
      if [ $? -ne 0 ] || [ -z "$basedir_win" ]; then
        basedir_win="$basedir"
      else
        exe=".exe"
      fi
    fi
  ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/home/rome/.rome/default/projects/apps/inbox-copy/node_modules/.pnpm/@rome-os+app-web-sdk@0.2.3_@rsbuild+core@2.0.15_@rspack+core@2.0.8_@swc+helpers@0.5.23__typescript@5.9.3/node_modules/@rome-os/app-web-sdk/node_modules:/home/rome/.rome/default/projects/apps/inbox-copy/node_modules/.pnpm/@rome-os+app-web-sdk@0.2.3_@rsbuild+core@2.0.15_@rspack+core@2.0.8_@swc+helpers@0.5.23__typescript@5.9.3/node_modules:/home/rome/.rome/default/projects/apps/inbox-copy/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/home/rome/.rome/default/projects/apps/inbox-copy/node_modules/.pnpm/@rome-os+app-web-sdk@0.2.3_@rsbuild+core@2.0.15_@rspack+core@2.0.8_@swc+helpers@0.5.23__typescript@5.9.3/node_modules/@rome-os/app-web-sdk/node_modules:/home/rome/.rome/default/projects/apps/inbox-copy/node_modules/.pnpm/@rome-os+app-web-sdk@0.2.3_@rsbuild+core@2.0.15_@rspack+core@2.0.8_@swc+helpers@0.5.23__typescript@5.9.3/node_modules:/home/rome/.rome/default/projects/apps/inbox-copy/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -n "$exe" ] && [ -x "$basedir/node.exe" ]; then
  exec "$basedir/node.exe"  "$basedir_win/../../bin/rome.js" "$@"
elif [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../bin/rome.js" "$@"
elif command -v node >/dev/null 2>&1; then
  exec node  "$basedir/../../bin/rome.js" "$@"
elif [ -n "$exe" ] && command -v node.exe >/dev/null 2>&1; then
  exec node.exe  "$basedir_win/../../bin/rome.js" "$@"
else
  exec node  "$basedir/../../bin/rome.js" "$@"
fi
# cmd-shim-target=/home/rome/.rome/default/projects/apps/inbox-copy/node_modules/.pnpm/@rome-os+app-web-sdk@0.2.3_@rsbuild+core@2.0.15_@rspack+core@2.0.8_@swc+helpers@0.5.23__typescript@5.9.3/node_modules/@rome-os/app-web-sdk/bin/rome.js
