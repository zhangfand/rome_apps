8 1033892
×aœ)•iŸimport{markerOffsets as e,createCommonLayoutRenderer as t,log as n,chunk_Y2CYZVJY_name as r}from"__RSPACK_ESM_CHUNK_1307";function o(e){let t=[];for(let n=0;n<e.length-1;n++)t.push({a:e[n],b:e[n+1]});return t}function l(e,t,n,r){let o=t.x-e.x,l=t.y-e.y,i=r.x-n.x,a=r.y-n.y,s=o*a-l*i;if(0===s)return null;let f=n.x-e.x,d=n.y-e.y,u=(f*a-d*i)/s,h=(f*l-d*o)/s;return u<=1e-6||u>=.999999||h<=1e-6||h>=.999999?null:{point:{x:e.x+u*o,y:e.y+u*l},tA:u,tB:h}}function i(e){return Math.abs(e.b.x-e.a.x)>=Math.abs(e.b.y-e.a.y)}function a(e){let t=[];for(let n=0;n<e.length;n++){let r=e[n],a=o(r.points);for(let s=n+1;s<e.length;s++){let n=e[s],f=o(n.points);for(let[e,o]of a.entries())for(let[a,s]of f.entries()){let f=l(o.a,o.b,s.a,s.b);if(!f)continue;let d=i(o);d!==i(s)&&d?t.push({jumpEdgeId:r.id,otherEdgeId:n.id,segIndex:e,t:f.tA,point:f.point}):t.push({jumpEdgeId:n.id,otherEdgeId:r.id,segIndex:a,t:f.tB,point:f.point})}}}return t}function s(e){let t=Math.round(1e3*e)/1e3;return Number.isInteger(t),`${t}`}function f(e){return`${s(e.x)},${s(e.y)}`}function d(e){let t=e.b.x-e.a.x,n=e.b.y-e.a.y;return Math.abs(t)>=Math.abs(n)?+(t>=0):+(n>=0)}function u(t,n){if(t.length<2)return t.map(e=>({...e}));let r=t.map(e=>({...e})),o=n.arrowTypeStart&&e[n.arrowTypeStart];if(o){let e=t[0],n=t[1],l=Math.atan2(n.y-e.y,n.x-e.x);r[0].x=e.x+o*Math.cos(l),r[0].y=e.y+o*Math.sin(l)}let l=n.arrowTypeEnd&&e[n.arrowTypeEnd];if(l){let e=t.length,n=t[e-2],o=t[e-1],i=Math.atan2(o.y-n.y,o.x-n.x);r[e-1].x=o.x-l*Math.cos(i),r[e-1].y=o.y-l*Math.sin(i)}return r}function h(e,t,n,r,o){let l=e.point.x,i=e.point.y,a={x:l-t*e.r,y:i-n*e.r},d={x:l+t*e.r,y:i+n*e.r},u=[`L${f(a)}`];return"arc"===o?u.push(`A${s(e.r)},${s(e.r)} 0 0 ${r} ${f(d)}`):u.push(`M${f(d)}`),u}function g(e,t,n,r){let o=t.x-e.x,l=t.y-e.y,i=n.x-t.x,a=n.y-t.y,s=Math.hypot(o,l),f=Math.hypot(i,a);if(s<1e-5||f<1e-5)return null;let d=o/s,u=l/s,h=i/f,g=a/f,c=Math.acos(Math.max(-1,Math.min(1,d*h+u*g)));if(c<1e-5||1e-5>Math.abs(Math.PI-c))return null;let p=Math.min(r/Math.sin(c/2),s/2,f/2);return{startX:t.x-d*p,startY:t.y-u*p,endX:t.x+h*p,endY:t.y+g*p,ctrlX:t.x,ctrlY:t.y,cutLen:p}}function c(e,t,n){let r=e.points;if(r.length<2)return"";let l=u(r,e),i="rounded"===e.curve,a=o(l),c=new Map;for(let e of t){let t=a[e.segIndex];if(!t)continue;let r=Math.hypot(t.b.x-t.a.x,t.b.y-t.a.y),o=c.get(e.segIndex)??[];o.push({t:e.t,point:e.point,d:e.t*r,r:n.jumpRadius}),c.set(e.segIndex,o)}let p=[`M${f(l[0])}`];for(let e=0;e<a.length;e++){let t=a[e],r=Math.hypot(t.b.x-t.a.x,t.b.y-t.a.y),o=0===r?0:(t.b.x-t.a.x)/r,u=0===r?0:(t.b.y-t.a.y)/r,x=d(t),m=0;if(i&&e>0){let t=g(l[e-1],l[e],l[e+1]??l[e],5);t&&(m=t.cutLen)}let y=r,b=null;i&&e<a.length-1&&(b=g(l[e],l[e+1],l[e+2]??l[e+1],5))&&(y=r-b.cutLen);let M=[...c.get(e)??[]].sort((e,t)=>e.t-t.t);for(let e of M)e.r=Math.min(e.r,e.d-m,y-e.d);for(let e=0;e<M.length-1;e++){let t=M[e+1].d-M[e].d;if(M[e].r+M[e+1].r>t){let n=t/2;M[e].r=Math.min(M[e].r,n),M[e+1].r=Math.min(M[e+1].r,n)}}for(let e of M)e.r<.001||p.push(...h(e,o,u,x,n.jumpStyle));i&&b?(p.push(`L${s(b.startX)},${s(b.startY)}`),p.push(`Q${s(b.ctrlX)},${s(b.ctrlY)} ${s(b.endX)},${s(b.endY)}`)):p.push(`L${f(t.b)}`)}return p.join(" ")}function p(e){return/^[\d\s+,.LMelm-]*$/.test(e)}function x(e){return!e||"linear"===e||"rounded"===e||"step"===e||"stepBefore"===e||"stepAfter"===e}function m(e){if(!e)return null;try{let t="function"==typeof atob?atob(e):Buffer.from(e,"base64").toString(),n=JSON.parse(t);if(!Array.isArray(n))return null;let r=[];for(let e of n)e&&"number"==typeof e.x&&"number"==typeof e.y&&r.push({x:e.x,y:e.y});return r.length>=2?r:null}catch{return null}}function y(e,t,n){if(!n.enabled)return;let r=e.node();if(!r)return;let o=new Map;for(let e of t)o.set(e.id,e);let l=[],i=new Map;for(let e of t){let t="u">typeof CSS&&CSS.escape?CSS.escape(e.id):e.id,n=r.querySelector(`path[data-id="${t}"]`);if(!n)continue;i.set(e.id,n);let o=m(n.getAttribute("data-points"))??e.points;l.push({...e,points:o})}let s=a(l);if(0===s.length)return;let f=new Map;for(let e of s){let t=f.get(e.jumpEdgeId)??[];t.push(e),f.set(e.jumpEdgeId,t)}for(let e of l){let t=f.get(e.id);if(!t||0===t.length)continue;let r=o.get(e.id),l=r?.curve;if(void 0!==l&&!x(l))continue;let a=i.get(e.id);if(!a||void 0===l&&!p(a.getAttribute("d")??""))continue;let s=a.getAttribute("style")??"",d=/stroke-dasharray\s*:\s*0\s+([\d.]+)\s+[\d.]+\s+([\d.]+)/.exec(s),u=d?Number.parseFloat(d[1]):null,h=d?Number.parseFloat(d[2]):null,g=c(e,t,n);if(a.setAttribute("d",g),null!==u&&null!==h&&"function"==typeof a.getTotalLength){let e=Math.max(0,a.getTotalLength()-u-h),t=`0 ${u} ${e} ${h}`,n=s.replace(/stroke-dasharray\s*:[^;]*;?/g,`stroke-dasharray: ${t};`).replace(/;\s*;+/g,";");a.setAttribute("style",n)}}}function b(e,{measure:t}){let n=e.config?.swimlane?.lineHops;if(!1===n)return;let r=e.edges.filter(e=>Array.isArray(e.points)&&e.points.length>=2).map(e=>({id:e.id,points:e.points,curve:e.curve,arrowTypeStart:e.arrowTypeStart,arrowTypeEnd:e.arrowTypeEnd}));y(t.groups.edgePaths,r,{enabled:!0,jumpRadius:6,jumpStyle:"gap"===n?"gap":"arc"})}r(o,"buildSegmentList"),r(l,"segmentIntersection"),r(i,"isHorizontalSeg"),r(a,"findEdgeIntersections"),r(s,"fmt"),r(f,"pointToString"),r(d,"getArcSweepFlag"),r(u,"applyMarkerOffsets"),r(h,"emitJump"),r(g,"computeRoundedCorner"),r(c,"rewriteEdgePath"),r(p,"isStraightPath"),r(x,"curveSupportsLineHops"),r(m,"decodeDataPoints"),r(y,"applyLineJumpsToSvg"),r(b,"applySwimlaneLineJumps");var M="__swimlane_default__";function I(e){return Math.max(e.padding??20,20)}function S(e){let{x:t,y:n,width:r,height:o}=e,l=e.swimlaneContentTop;if("number"!=typeof t||"number"!=typeof n||"number"!=typeof r||"number"!=typeof o||"number"!=typeof l||!Number.isFinite(t)||!Number.isFinite(n)||!Number.isFinite(r)||!Number.isFinite(o)||!Number.isFinite(l)||r<=0||o<=0)return void delete e.groupTitleRect;let i=n-o/2,a=Math.min(21,Math.max(0,Math.min(l,n+o/2)-i)),s=i+a;s<=i?delete e.groupTitleRect:e.groupTitleRect={left:t-r/2,right:t+r/2,top:i,bottom:s}}function w(e){let t=e.direction,n=e.nodes??=[];for(let n of e.nodes??[])n.isGroup&&!n.parentId&&(n.shape="swimlane",t&&(n.direction=t));let r=n.filter(e=>!e.isGroup&&!e.parentId);if(0===r.length)return;let o=n.find(e=>e.id===M);for(let e of(o?o.isGroup&&(o.shape="swimlane",t&&(o.direction=t)):(o={id:M,label:"",isGroup:!0,shape:"swimlane",padding:20,...t?{direction:t}:{}},n.push(o)),r))e.parentId=M}function v(e){let t=new Map;for(let n of e.nodes??[])t.set(n.id,n);let n=[];for(let t of e.edges??[]){let e="string"==typeof t.start?t.start:void 0,r="string"==typeof t.end?t.end:void 0;e&&r&&(t.labelNodeId||n.push({id:t.id,src:e,dst:r,ref:t}))}let r=e.nodes??[],o=r.filter(e=>e.isGroup),l=r.filter(e=>!e.isGroup);return{nodes:[...[...o].reverse(),...l].map(e=>e.id),edges:n,layout:e,nodeById:t}}function C(e,t,n,r){let{layout:o}=e,l=e.nodeById,i=r?.layerGap??100,a=r?.nodeGap??40,s=0;for(let e of t.layers){let t=0;for(let r of e){let e=l.get(r);if(!e){t++;continue}e.layer=s,e.order=t;let o=n.x[r]??t*a,f=n.y[r]??s*i;e.x=o,e.y=f,t++}s++}let f=o.nodes??[],d=new Map,u=[];for(let e of f){if(!e?.isGroup)continue;e.parentId||u.push(e);let t=f.filter(t=>t.parentId===e.id),r=1/0,o=-1/0,l=1/0,i=-1/0;for(let e of t){let t=e.x??n.x[e.id],a=e.y??n.y[e.id],s=e.width??0,f=e.height??0;null!=t&&null!=a&&(r=Math.min(r,t-s/2),o=Math.max(o,t+s/2),l=Math.min(l,a-f/2),i=Math.max(i,a+f/2))}if(r===1/0||l===1/0)e.x=e.x??0,e.y=e.y??0,e.width=e.width??0,e.height=e.height??0;else{let t=e.padding??20,n=Math.max(0,o-r)+(e.parentId?t:2*I(e)),a=Math.max(0,i-l)+t,s=(l+i)/2;e.x=(r+o)/2,e.y=s,e.width=n,e.height=a,d.set(e.id,{minX:r,maxX:o,minY:l,maxY:i})}}if(u.length>0&&d.size>0){let e=1/0,t=-1/0,n=0;for(let r of u){let o=r.padding??20;o>n&&(n=o);let l=d.get(r.id);l&&(e=Math.min(e,l.minY),t=Math.max(t,l.maxY))}if(e!==1/0&&t!==-1/0){let r=Math.max(0,t-e)+2*Math.max(n,36),o=(e+t)/2;for(let t of u)t.y=o,t.height=r,t.swimlaneContentTop=e;let l=[...u].sort((e,t)=>(e.x??0)-(t.x??0)),i=[],a=[],s=[];for(let e of l){let t=d.get(e.id);if(!t)continue;let n=Math.max(0,t.maxX-t.minX)+2*I(e),r=(t.minX+t.maxX)/2;i.push(e.id),a.push(r),s.push(n)}let f=i.length;if(f>0){let e=new Map;if(1===f)e.set(i[0],s[0]);else{let t=[];for(let e=0;e<f-1;e++)t.push(a[e+1]-a[e]);let n=Array(f);n[0]=0;for(let e=0;e<f-1;e++)n[e+1]=2*t[e]-n[e];let r=0,o=1/0;for(let e=0;e<f;e++){let t=s[e];e%2==0?r=Math.max(r,t-n[e]):o=Math.min(o,n[e]-t)}let l=r;l=r<=o?(r+o)/2:r;for(let t=0;t<f;t++){let r=n[t]+(t%2==0?l:-l),o=Math.max(s[t],r);e.set(i[t],o)}}for(let t of u){let n=e.get(t.id);null!=n&&(t.width=n),S(t)}}}}}function L(e){let t=[],r=[],o=new Map;for(let t of e.nodes)o.set(t.id,t);for(let l of e.edges){if(!l.label||0===l.label.length||l.isLayoutOnly||l.labelNodeId)continue;let e=l.start?o.get(l.start):void 0,i=l.end?o.get(l.end):void 0;if(!e||!i){n.warn("[EdgeLabelNodes]",`Edge ${l.id} has missing source or target node`);continue}let a=`edge-label-${l.start}-${l.end}-${l.id}`,s=e.parentId!==i.parentId?i.parentId:e.parentId,f={id:a,label:l.label,edgeStart:l.start??"",edgeEnd:l.end??"",shape:"labelRect",width:0,height:0,isEdgeLabel:!0,isDummy:!0,parentId:s,isGroup:!1,labelStyle:Array.isArray(l.labelStyle)?l.labelStyle[0]:l.labelStyle??"",...e.dir?{dir:e.dir}:{}};t.push(f),l.labelNodeId=a,l.label=void 0,l.text=void 0;let d={id:`${l.id}-to-label`,start:l.start,end:a,type:"normal",isLayoutOnly:!0},u={id:`${l.id}-from-label`,start:a,end:l.end,type:"normal",isLayoutOnly:!0};r.push(d,u)}let l=[...e.nodes,...t],i=[...e.edges,...r];return{...e,nodes:l,edges:i}}function k(e){let t=e.x??0,n=e.y??0,r=e.width??0,o=e.height??0;return r>0&&o>0?{cx:t,cy:n,rect:_(t,n,r,o)}:void 0}function $(e){if(e.isGroup)return;let t=k(e);if(t)return{id:String(e.id??""),cx:t.cx,cy:t.cy,rect:t.rect}}function R(e,t,n=.001){return Math.abs(e.x-t.x)<n&&Math.abs(e.y-t.y)<n}function O(e,t,n=.001){return Math.abs(e.x-t.x)<n}function T(e,t,n=.001){return Math.abs(e.y-t.y)<n}function F(e,t,n=.001){return T(e,t,n)&&Math.abs(e.x-t.x)>n}function E(e,t,n=.001){return O(e,t,n)&&Math.abs(e.y-t.y)>n}function B(e,t,n,r){return Math.max(0,Math.min(Math.max(e,t),Math.max(n,r))-Math.max(Math.min(e,t),Math.min(n,r)))}function N(e,t,n=.001){return e.horizontal&&t.horizontal&&T(e.a,t.a,n)?B(e.a.x,e.b.x,t.a.x,t.b.x):e.vertical&&t.vertical&&O(e.a,t.a,n)?B(e.a.y,e.b.y,t.a.y,t.b.y):0}function X(e,t=.001){let n=[];for(let r=0;r<e.length-1;r++){let o=e[r],l=e[r+1],i=F(o,l,t),a=E(o,l,t);(i||a)&&n.push({index:r,a:o,b:l,horizontal:i,vertical:a})}return n}function z(e,t=.001){let n=X(e,t),r=0;for(let e=1;e<n.length;e++)n[e-1].horizontal!==n[e].horizontal&&r++;return r}function Y(e,t=.001){let n=[];for(let r of e){let e=n.length>0?n[n.length-1]:void 0;e&&R(e,r,t)||n.push({x:r.x,y:r.y})}return n}function A(e,t=.001){if(!e||4!==e.length)return;let[n,r,o,l]=e;return F(n,r,t)&&E(r,o,t)&&F(o,l,t)?{kind:"HVH",p0:n,p1:r,p2:o,p3:l}:E(n,r,t)&&F(r,o,t)&&E(o,l,t)?{kind:"VHV",p0:n,p1:r,p2:o,p3:l}:void 0}function G(e,t,n,r=0){let o=Math.min(e.x,t.x),l=Math.max(e.x,t.x),i=Math.min(e.y,t.y),a=Math.max(e.y,t.y);return l>n.left-r&&o<n.right+r&&a>n.top-r&&i<n.bottom+r}function P(e,t,n=0){return e.x>t.left+n&&e.x<t.right-n&&e.y>t.top+n&&e.y<t.bottom-n}function D(e,t){return e.left<=t.left&&e.right>=t.right&&e.top<=t.top&&e.bottom>=t.bottom}function H(e,t){return e.left<t.right&&e.right>t.left&&e.top<t.bottom&&e.bottom>t.top}function j(e,t){return{left:e.left-t,right:e.right+t,top:e.top-t,bottom:e.bottom+t}}function _(e,t,n,r){return{left:e-n/2,right:e+n/2,top:t-r/2,bottom:t+r/2}}function V(e){return k(e)?.rect}function W(e,t){switch(t){case"top":return{x:e.cx,y:e.rect.top};case"bottom":return{x:e.cx,y:e.rect.bottom};case"left":return{x:e.rect.left,y:e.cy};case"right":return{x:e.rect.right,y:e.cy}}}function K(e,t,n,r,o,l=.001){let i="left"===t||"right"===t,a="left"===r||"right"===r;if(i&&a){if("right"===t&&"left"===r&&e.x<n.x||"left"===t&&"right"===r&&e.x>n.x){if(T(e,n,l))return[e,n];let t=(e.x+n.x)/2;return[e,{x:t,y:e.y},{x:t,y:n.y},n]}if(t===r){if(T(e,n,l))return;let r="left"===t?Math.min(e.x,n.x)-o:Math.max(e.x,n.x)+o;return[e,{x:r,y:e.y},{x:r,y:n.y},n]}return}if(!i&&!a){if(t===r){if(O(e,n,l))return;let r="top"===t?Math.min(e.y,n.y)-o:Math.max(e.y,n.y)+o;return[e,{x:e.x,y:r},{x:n.x,y:r},n]}if(!("bottom"===t&&"top"===r&&e.y<n.y||"top"===t&&"bottom"===r&&e.y>n.y))return;if(O(e,n,l))return[e,n];let i=(e.y+n.y)/2;return[e,{x:e.x,y:i},{x:n.x,y:i},n]}if(i&&!a){let o="right"===t&&n.x>e.x||"left"===t&&n.x<e.x,l="top"===r&&e.y<n.y||"bottom"===r&&e.y>n.y;return o&&l?[e,{x:n.x,y:e.y},n]:void 0}let s="bottom"===t&&n.y>e.y||"top"===t&&n.y<e.y,f="left"===r&&e.x<n.x||"right"===r&&e.x>n.x;return s&&f?[e,{x:e.x,y:n.y},n]:void 0}function J(e,t,n,r){return"left"===t||"right"===t?[e,{x:r,y:e.y},{x:r,y:n.y},n]:[e,{x:e.x,y:r},{x:n.x,y:r},n]}function q(e){let t=new Map,n=[];for(let r of e){if(r.isEdgeLabel)continue;let e=$(r);e&&(t.set(e.id,e),n.push({id:e.id,rect:e.rect}))}return{nodeInfoById:t,realNodeRects:n}}function U(e){let t=[],n=[];for(let r of e){let e=$(r);if(!e)continue;let o={id:e.id,rect:e.rect};r.isEdgeLabel?n.push(o):t.push(o)}return{realNodeRects:t,labelNodeRects:n}}function Q(e,{includeEdgeLabels:t=!0}={}){let n=[];for(let r of e){if(r.isGroup||!t&&r.isEdgeLabel)continue;let e=r.x??0,o=r.y??0,l=r.width??0,i=r.height??0;n.push({nodeId:r.id,..._(e,o,l,i)})}return n}function Z(e,t,n=.001){let r=e.start,o=e.end;if(!r||!o)return;let l=t.get(r),i=t.get(o);if(l&&i)return{srcId:r,dstId:o,srcInfo:l,dstInfo:i,collinearX:Math.abs(l.cx-i.cx)<n,collinearY:Math.abs(l.cy-i.cy)<n}}function ee(e,t,n,r=[],o=0){for(let l of n)if(!r.includes(l.id)&&G(e,t,l.rect,-o))return!0;return!1}function et(e,t,n,r,o=.001,l=1e-6){let i=T(e,t,o),a=O(e,t,o),s=T(n,r,o),f=O(n,r,o);if(i&&s||a&&f||!(i||a)||!(s||f))return!1;let d=i?{a:e,b:t}:{a:n,b:r},u=a?{a:e,b:t}:{a:n,b:r},h=d.a.y,g=Math.min(d.a.x,d.b.x),c=Math.max(d.a.x,d.b.x),p=u.a.x,x=Math.min(u.a.y,u.b.y),m=Math.max(u.a.y,u.b.y);if(p<g||p>c||h<x||h>m)return!1;let y=Math.abs(p-d.a.x)<l&&Math.abs(h-d.a.y)<l||Math.abs(p-d.b.x)<l&&Math.abs(h-d.b.y)<l,b=Math.abs(p-u.a.x)<l&&Math.abs(h-u.a.y)<l||Math.abs(p-u.b.x)<l&&Math.abs(h-u.b.y)<l;return!(y&&b)}function en(e,t,n,r,o=.001){let l=T(e,t,o),i=O(e,t,o),a=T(n,r,o),s=O(n,r,o);return i&&s&&O(e,n,o)?B(e.y,t.y,n.y,r.y)>o:!!(l&&a&&T(e,n,o))&&B(e.x,t.x,n.x,r.x)>o}function er(e,t,n,r,{epsilon:o=.001,skipDegenerateOther:l=!1}={}){for(let i of n){if(i===r||i.isLayoutOnly)continue;let n=i.points;if(n&&!(n.length<2))for(let r=0;r<n.length-1;r++){let i=n[r],a=n[r+1];if(!(l&&R(i,a,o))&&(et(e,t,i,a,o)||en(e,t,i,a,o)))return!0}}return!1}function eo(e,t,n,r,o=.001){let l=T(e,t,o),i=O(e,t,o),a=T(n,r,o),s=O(n,r,o);if(!(l&&s||i&&a))return!1;let f=l?{a:e,b:t}:{a:n,b:r},d=l?{a:n,b:r}:{a:e,b:t},u=f.a.y,h=Math.min(f.a.x,f.b.x),g=Math.max(f.a.x,f.b.x),c=d.a.x,p=Math.min(d.a.y,d.b.y),x=Math.max(d.a.y,d.b.y);return c>h+o&&c<g-o&&u>p+o&&u<x-o}function el(e,t,n){let r=Math.min(t,n),o=Math.max(t,n);return e>r+.001&&e<o-.001}function ei(e,t,n){return O(e,t)&&O(t,n)?el(t.y,e.y,n.y):!!(T(e,t)&&T(t,n))&&el(t.x,e.x,n.x)}function ea(e){let t=!1,n=[];for(let r=0;r<e.length;r++){let o=n[n.length-1],l=e[r],i=r+1<e.length?e[r+1]:void 0;if(o&&i){if(R(o,i)){r++,t=!0;continue}if(ei(o,l,i)){t=!0;continue}}n.push(l)}return{points:n,changed:t}}function es(e){let t=[e[0]];for(let n=1;n<e.length;n++){let r=t[t.length-1],o=e[n];if(!O(r,o)&&!T(r,o)){let e=t.length>=2?t[t.length-2]:void 0,n=e&&O(e,r)?{x:r.x,y:o.y}:{x:o.x,y:r.y};t.push(n)}t.push(o)}let n=[];for(let e of t){let t=n[n.length-1];t&&R(t,e)||n.push(e)}return n}function ef(e){if(e.length<3)return e;let t=[...e];for(let e=0;e<32;e++){let e=ea(t);if(t=e.points,!e.changed)break}return t}function ed(e,t,n){if(e.isLayoutOnly||!e.points||e.points.length<n)return;let r=e.start?t.get(e.start):void 0,o=e.end?t.get(e.end):void 0;return{edge:e,points:e.points,srcRect:r?V(r):void 0,dstRect:o?V(o):void 0}}function eu(e,t,n){if(T(e,t,.001))return{x:e.x<n.left?n.left:n.right,y:e.y};if(O(e,t,.001)){let t=e.y<n.top?n.top:n.bottom;return{x:e.x,y:t}}return{x:Math.min(n.right,Math.max(n.left,e.x)),y:Math.min(n.bottom,Math.max(n.top,e.y))}}function eh(e,t,n){let r=n?1:-1,o=n?0:e.length-1;for(;o>=0&&o<e.length&&P(e[o],t,.5);)o+=r;if(o<0||o>=e.length)return e;let l=o-r;if(l<0||l>=e.length)return e;let i=eu(e[o],e[l],t);return n?[i,...e.slice(o)]:[...e.slice(0,o+1),i]}function eg(e,t){for(let n of e){let e=ed(n,t,2);if(!e)continue;let r=[...e.points];e.srcRect&&(r=eh(r,e.srcRect,!0)),e.dstRect&&(r=eh(r,e.dstRect,!1)),r=ev(r=ef(es(r)),e.srcRect,e.dstRect),e.edge.points=ef(es(r))}}function ec(e,t,n,r=!1){if(T(e,t,.001)){if(t.y<n.top-.001||t.y>n.bottom+.001)return t;if(r){if(e.x<n.left-.001)return{x:n.left,y:e.y};if(e.x>n.right+.001)return{x:n.right,y:e.y}}return{x:Math.abs(t.x-n.left)<=Math.abs(t.x-n.right)?n.left:n.right,y:e.y}}if(O(e,t,.001)){if(t.x<n.left-.001||t.x>n.right+.001)return t;if(r){if(e.y<n.top-.001)return{x:e.x,y:n.top};if(e.y>n.bottom+.001)return{x:e.x,y:n.bottom}}let o=Math.abs(t.y-n.top)<=Math.abs(t.y-n.bottom);return{x:e.x,y:o?n.top:n.bottom}}return t}function ep(e,t,n){let r=e[t];for(let o=t+n;o>=0&&o<e.length;o+=n){let t=e[o];if(!R(t,r,.001))return t}return e[t+n]}function ex(e,t){let n=e+4,r=t-4;return n<=r?{lo:n,hi:r}:{lo:(e+t)/2,hi:(e+t)/2}}function em(e,t,n){let{lo:r,hi:o}=ex(t,n);return Math.min(o,Math.max(r,e))}function ey(e){let t=Math.max(...e.map(e=>e.lo)),n=Math.min(...e.map(e=>e.hi));if(!(t>n))return{lo:t,hi:n}}function eb(e,t){return"left"===t||"right"===t?ex(e.top,e.bottom):ex(e.left,e.right)}function eM(e,t,n){let r=e.y>=n.top-.001&&e.y<=n.bottom+.001,o=e.x>=n.left-.001&&e.x<=n.right+.001;if(T(e,t,.001)&&r){if(.001>Math.abs(e.x-n.left))return"left";if(.001>Math.abs(e.x-n.right))return"right"}if(O(e,t,.001)&&o){if(.001>Math.abs(e.y-n.top))return"top";if(.001>Math.abs(e.y-n.bottom))return"bottom"}}function eI(e){return"left"===e||"right"===e}function eS(e,t,n,r,o){let l=[],i=n?eM(e,t,n):void 0,a=r?eM(t,e,r):void 0;return n&&i&&eI(i)===o&&l.push(eb(n,i)),r&&a&&eI(a)===o&&l.push(eb(r,a)),l.length>0?ey(l):void 0}function ew(e,t,n,r,o){let l=eS(e,t,n,r,o);if(!l)return;let i=o?e.y:e.x,a=Math.min(l.hi,Math.max(l.lo,i));if(!(.001>Math.abs(a-i)))return o?[{x:e.x,y:a},{x:t.x,y:a}]:[{x:a,y:e.y},{x:a,y:t.y}]}function ev(e,t,n){if(2!==e.length)return e;let[r,o]=e;return T(r,o,.001)?ew(r,o,t,n,!0)??e:O(r,o,.001)?ew(r,o,t,n,!1)??e:e}function eC(e,t,n){return eI(n)?{x:e.x,y:em(e.y,t.top,t.bottom)}:{x:em(e.x,t.left,t.right),y:e.y}}function eL(e,t,n,r,o,l){let i=e.map(e=>({...e}));for(let a=t;a>=0&&a<e.length;a+=n){let t=e[a];if(l&&!T(t,r,.001)||!l&&!O(t,r,.001))break;l?i[a].y=o.y:i[a].x=o.x}return i}function ek(e,t,n){if(e.length<2)return e;let r=n?0:e.length-1,o=n?1:-1,l=e[r],i=ep(e,r,o);if(!i)return e;let a=eM(l,i,t);if(!a)return e;let s=eI(a),f=eC(l,t,a);return R(l,f,.001)?e:eL(e,r,o,l,f,s)}function e$(e,t,n){let r=Math.min(e.x,t.x)>=n.left-.001&&Math.max(e.x,t.x)<=n.right+.001,o=Math.min(e.y,t.y)>=n.top-.001&&Math.max(e.y,t.y)<=n.bottom+.001;return .001>Math.abs(e.y-n.top)&&.001>Math.abs(t.y-n.top)&&r?"top":.001>Math.abs(e.y-n.bottom)&&.001>Math.abs(t.y-n.bottom)&&r?"bottom":.001>Math.abs(e.x-n.left)&&.001>Math.abs(t.x-n.left)&&o?"left":.001>Math.abs(e.x-n.right)&&.001>Math.abs(t.x-n.right)&&o?"right":void 0}function eR(e,t,n,r){switch(e){case"top":return O(t,n,.001)&&n.y<r.top-.001;case"bottom":return O(t,n,.001)&&n.y>r.bottom+.001;case"left":return T(t,n,.001)&&n.x<r.left-.001;case"right":return T(t,n,.001)&&n.x>r.right+.001}}function eO(e,t,n){if(e.length<3)return e;if(n){let n=e$(e[0],e[1],t);return n&&eR(n,e[1],e[2],t)?e.slice(1):e}let r=e.length-1,o=e$(e[r-1],e[r],t);return o&&eR(o,e[r-1],e[r-2],t)?e.slice(0,r):e}function eT(e,t,n){let r=e;if(t){let e=ep(r,0,1);if(e){let n=ec(e,r[0],t);n!==r[0]&&(r=[n,...r.slice(1)])}r=eO(r,t,!0)}if(n){let e=r.length-1,t=ep(r,e,-1);if(t){let o=ec(t,r[e],n,!0);o!==r[e]&&(r=[...r.slice(0,e),o])}r=eO(r,n,!1)}let o=ev(r,t,n);return o!==r||2===r.length?o:(t&&(r=ek(r,t,!0)),n&&(r=ek(r,n,!1)),r)}function eF(e,t){for(let n of e){let e=ed(n,t,2);if(!e)continue;let r=eT(Y(e.points,.001),e.srcRect,e.dstRect);if(r.length<3){e.edge.points=r;continue}let o=[r[0],{...r[0]},...r.slice(1,-1),r[r.length-1],{...r[r.length-1]}];e.edge.points=o}}function eE(e){return new Map(e.map(e=>[e.id,e]))}function eB(e,t){let n=e.parentId,r=null;for(;n;){let e=t.get(n);if(!e?.isGroup)break;r=e.id,n=e.parentId}return r}function eN(e,t){let n=0,r=e.parentId;for(;r;){let e=t.get(r);if(!e?.isGroup)break;n++,r=e.parentId}return n}function eX(e){let t=1/0,n=-1/0,r=1/0,o=-1/0;for(let l of e){let e=l.x,i=l.y;if("number"!=typeof e||"number"!=typeof i)continue;let a=l.width??0,s=l.height??0;t=Math.min(t,e-a/2),n=Math.max(n,e+a/2),r=Math.min(r,i-s/2),o=Math.max(o,i+s/2)}return t===1/0||r===1/0?null:{minX:t,maxX:n,minY:r,maxY:o}}function ez(e,t){let n=e.padding??20;e.x=(t.minX+t.maxX)/2,e.y=(t.minY+t.maxY)/2,e.width=Math.max(0,t.maxX-t.minX)+n,e.height=Math.max(0,t.maxY-t.minY)+n}function eY(e){let t=eE(e);for(let n of e.filter(e=>e.isGroup&&e.parentId).sort((e,n)=>eN(n,t)-eN(e,t))){let t=eX(e.filter(e=>e.parentId===n.id));t&&ez(n,t)}}function eA(e,t){let n=e.nodes??[],o=e.edges??[],l=n.filter(e=>!e.isGroup),i=1/0,a=-1/0;for(let e of l){let n=e[t];"number"==typeof n&&(i=Math.min(i,n),a=Math.max(a,n))}if(!Number.isFinite(i)||!Number.isFinite(a))return!1;let s=r(e=>i+a-e,"mirror");for(let e of n){let n=e[t];"number"==typeof n&&(e[t]=s(n));let r=e.groupTitleRect;r&&(e.groupTitleRect="x"===t?{...r,left:s(r.right),right:s(r.left)}:{...r,top:s(r.bottom),bottom:s(r.top)})}for(let e of o)for(let n of e.points??[])n[t]=s(n[t]);return!0}function eG(e){return!(e.nodes??[]).some(e=>!e.isGroup)||eA(e,"y")}function eP(e,t="LR"){let n=e.nodes??[],r=e.edges??[],o=n.filter(e=>!e.isGroup),l=1/0,i=1/0;for(let e of o){let t=e.x??0,n=e.y??0;t<l&&(l=t),n<i&&(i=n)}if(!Number.isFinite(l)||!Number.isFinite(i))return!1;let a=0,s=0;for(let e of o)a+=e.width??0,s+=e.height??0;let f=a/o.length,d=s/o.length,u=d>0?Math.max(1,f/d):1;for(let e of o){let t=e.x??0,n=((e.y??0)-i)*u+36,r=t-l;e.x=n,e.y=r}for(let e of r)if(e.points)for(let t of e.points){let e=t.x,n=(t.y-i)*u+36,r=e-l;t.x=n,t.y=r}eY(n);let h=n.filter(e=>e.isGroup&&!e.parentId);if(0===h.length)return"RL"===t&&eA(e,"x"),!0;let g=eE(n),c=new Map;for(let e of n){if(e.isGroup)continue;let t=eB(e,g);if(!t)continue;let n=c.get(t)??[];n.push(e),c.set(t,n)}let p=0;for(let e of h){let t=e.padding??0;t>p&&(p=t)}let x=[],m=1/0,y=-1/0;for(let e of h){let t=eX(c.get(e.id)??[]);t&&(m=Math.min(m,t.minX),y=Math.max(y,t.maxX),x.push({lane:e,contentTop:t.minY,contentBottom:t.maxY,centerY:(t.minY+t.maxY)/2}))}if(m===1/0||y===-1/0)return!0;let b=Math.max(0,y-m)+2*Math.max(p,10),M=36+b,I=(m+y)/2-b/2-36,S=I+M/2,w=Math.max(p,36);x.sort((e,t)=>e.centerY-t.centerY);for(let e=0;e<x.length;e++){let t,n,r=x[e];if(t=0===e?r.contentTop-w:(x[e-1].contentBottom+r.contentTop)/2,e===x.length-1)n=r.contentBottom+w;else{let t=x[e+1];n=(r.contentBottom+t.contentTop)/2}let o=Math.max(0,n-t),l=(t+n)/2;r.lane.x=S,r.lane.y=l,r.lane.width=M,r.lane.height=o,r.lane.swimlaneContentTop=r.contentTop,r.lane.groupTitleRect={left:I,right:I+36,top:t,bottom:n}}return"RL"===t&&eA(e,"x"),!0}r(I,"topLaneHorizontalPadding"),r(S,"assignTopLaneTitleRect"),r(w,"prepareLayoutForSwimlanes"),r(v,"toGraphView"),r(C,"writeBackToLayoutData"),r(L,"createEdgeLabelNodes"),r(k,"measuredNodeRect"),r($,"nodeBoundsInfoFor"),r(R,"samePoint"),r(O,"sameX"),r(T,"sameY"),r(F,"isHorizontalSegment"),r(E,"isVerticalSegment"),r(B,"overlapLength"),r(N,"sameAxisSegmentOverlapLength"),r(X,"orthogonalSegmentsForPoints"),r(z,"countOrthogonalBends"),r(Y,"dedupeConsecutivePoints"),r(A,"classifyThreeSegmentRoute"),r(G,"segmentBoundsOverlapRect"),r(P,"pointInsideRect"),r(D,"rectContainsRect"),r(H,"rectsOverlap"),r(j,"inflateRect"),r(_,"rectFromCenterSize"),r(V,"rectOfNodeBounds"),r(W,"portForRectSide"),r(K,"buildOrthogonalPortPath"),r(J,"buildSameSideTrackPath"),r(q,"collectRealNodeBounds"),r(U,"collectNodeRectEntries"),r(Q,"collectLayoutNodeRects"),r(Z,"getNodePairGeometry"),r(ee,"segmentHitsAnyRect"),r(et,"orthogonalSegmentsCross"),r(en,"sameAxisSegmentsOverlap"),r(er,"segmentConflictsWithAnyEdge"),r(eo,"orthogonalSegmentsStrictlyCross"),r(el,"strictlyBetween"),r(ei,"isCollinearIntermediate"),r(ea,"simplifyPolylineOnce"),r(es,"orthogonalizePolyline"),r(ef,"simplifyPolyline"),r(ed,"endpointContextFor"),r(eu,"segmentEnterPoint"),r(eh,"clipEndpoint"),r(eg,"clipEdgeEndpointsToNodeBoundaries"),r(ec,"snapEndpointToBoundary"),r(ep,"firstDistinctAdjacent"),r(ex,"cornerClearanceRange"),r(em,"clampToCornerClearance"),r(ey,"intersectRanges"),r(eb,"clearanceRangeForSide"),r(eM,"terminalSideForSegment"),r(eI,"isHorizontalSide"),r(eS,"straightClearanceRange"),r(ew,"clearStraightEndpointCornerAxis"),r(ev,"clearStraightEndpointCornerConnections"),r(eC,"cornerClearedEndpoint"),r(eL,"moveCollinearEndpointRun"),r(ek,"clearEndpointCornerConnection"),r(e$,"borderSideForSegment"),r(eR,"leavesOutward"),r(eO,"collapseOwnBorderStub"),r(eT,"snapAndCollapseEndpoints"),r(eF,"prepareEdgeEndpointsForRenderer"),r(eE,"buildNodeMap"),r(eB,"resolveTopLevelGroupId"),r(eN,"groupDepth"),r(eX,"boundsForChildren"),r(ez,"applyGroupBounds"),r(eY,"recomputeNestedGroupBounds"),r(eA,"mirrorAxis"),r(eG,"applyBtDirectionTransform"),r(eP,"applyLrDirectionTransform");var eD=[0,8,-8,16,-16];function eH(e,t){let{nodeInfoById:n,realNodeRects:r}=q(t);for(let t of e){let o;if(t.isLayoutOnly)continue;let l=t.points;if(!l||l.length<4)continue;let i=A(Y(l,1e-6),1e-6);if(!i)continue;let{p3:a}=i,s="HVH"===i.kind,f=Z(t,n,1e-6);if(!f)continue;let{srcId:d,dstId:u,srcInfo:h,dstInfo:g,collinearX:c,collinearY:p}=f;if(c||p)continue;let x=h.rect;for(let n of eD){let l,i,f;if(s){let e=g.cy>h.cy?x.bottom:x.top,t=h.cx+n;if(t<=x.left+1e-6||t>=x.right-1e-6)continue;l={x:t,y:e},i={x:t,y:a.y},f={x:a.x,y:a.y}}else{let e=g.cx>h.cx?x.right:x.left,t=h.cy+n;if(t<=x.top+1e-6||t>=x.bottom-1e-6)continue;l={x:e,y:t},i={x:a.x,y:t},f={x:a.x,y:a.y}}let c=R(l,i,1e-6),p=R(i,f,1e-6);if(c&&p||!c&&ee(l,i,r,[d],1)||!p&&ee(i,f,r,[u],1))continue;let m=!c&&er(l,i,e,t,{epsilon:1e-6,skipDegenerateOther:!0}),y=!p&&er(i,f,e,t,{epsilon:1e-6,skipDegenerateOther:!0});if(!m&&!y){o=c?[i,f]:p?[l,i]:[l,i,f];break}}o&&(t.points=o)}}function ej(e,t){let{realNodeRects:n,labelNodeRects:o}=U(t.values());for(let l of e){let i,a;if(l.isLayoutOnly)continue;let s=l.points;if(!s||s.length<4)continue;let f=Y(s,.001);if(f.length<4)continue;let d=f.length-1,u=f[d],h=f[d-1],g=f[d-2],c=Math.hypot(u.x-h.x,u.y-h.y);if(c>=10||c<.001)continue;let p=h.x-g.x,x=h.y-g.y;if(.001>Math.hypot(p,x))continue;let m=F(h,u,.001),y=E(h,u,.001),b=F(g,h,.001),M=E(g,h,.001);if(!(m&&M||y&&b))continue;let I=l.end,S=l.start,w=I?t.get(I):void 0;if(!w)continue;let v=w.x??0,C=w.y??0,L=V(w);if(!L)continue;if(M){let e=x<0;i={x:v,y:g.y},a={x:v,y:e?L.bottom:L.top}}else{let e=p>0;i={x:g.x,y:C},a={x:e?L.right:L.left,y:C}}if(ee(i,a,n,I?[I]:[],-2)||ee(i,a,o,[],-2))continue;if(S){let e=t.get(S),n=e?V(e):void 0;if(n&&P(i,n,2))continue}let k=r((e,t)=>`${e.x.toFixed(3)},${e.y.toFixed(3)}|${t.x.toFixed(3)},${t.y.toFixed(3)}`,"ownSegmentKey"),$=new Set;for(let e=0;e<f.length-1;e++)$.add(k(f[e],f[e+1]));let R=r((t,n)=>{for(let r of e){if(r===l||r.isLayoutOnly)continue;let e=r.points;if(e&&!(e.length<2))for(let r=0;r<e.length-1;r++){let o=e[r],l=e[r+1];if(!$.has(k(o,l))&&eo(t,n,o,l,.001))return!0}}return!1},"segmentCrossesOtherEdge");if(R(i,a))continue;if(d-3>=0){let e=f[d-3];if(ee(e,i,n,[S,I].filter(e=>!!e),-2)||R(e,i))continue}let B=[...f.slice(0,d-2),i,a];l.points=B;let N=l.labelNodeId;if(N){let e=t.get(N);if(e){let t=e.width??0,n=e.height??0;if(t>0&&n>0){let r,o,l=-1;for(let e=0;e<B.length-1;e++){let i=B[e],a=B[e+1],s=Math.hypot(a.x-i.x,a.y-i.y),f=T(i,a,.001),d=O(i,a,.001);(f&&s>=t+2||d&&s>=n+2)&&s>l&&(l=s,r=(i.x+a.x)/2,o=(i.y+a.y)/2)}void 0!==r&&void 0!==o&&(e.x=r,e.y=o)}}}}}r(eH,"portSwapToLShape"),r(ej,"collapseShortTerminalStub");var e_=r((e,t)=>O(e,t,.001)||T(e,t,.001),"orthogonallyAligned");function eV(e,t){let n=r((e,t)=>{let n=e.x??0,r=e.y??0,o=t.x-n,l=t.y-r,i=(e.width??0)/2,a=(e.height??0)/2;return Math.abs(l)*i>Math.abs(o)*a?(l<0&&(a=-a),{x:n+(0===l?0:a*o/l),y:r+a}):(o<0&&(i=-i),{x:n+i,y:r+(0===o?0:i*l/o)})},"rectIntersect"),o=r((e,r)=>{let o=Y(e.points??[]);if(o.length<2)return;let l=r?e.start:e.end,i=l?t.get(l):void 0,a=i?V(i):void 0;if(!i||!l||!a)return;let s=r?o[0]:o[o.length-1],f=r?o[1]:o[o.length-2],d=n(i,s),u=s;return(e_(f,d)&&(u=f),O(d,u,.001))?{edge:e,edgeId:String(e.id??""),nodeId:l,atStart:r,orientation:"V",coord:d.x,min:Math.min(d.y,u.y),max:Math.max(d.y,u.y),boundary:d,railEnd:u,rect:a}:T(d,u,.001)?{edge:e,edgeId:String(e.id??""),nodeId:l,atStart:r,orientation:"H",coord:d.y,min:Math.min(d.x,u.x),max:Math.max(d.x,u.x),boundary:d,railEnd:u,rect:a}:void 0},"terminalLaneFor"),l=r((e,t)=>Math.max(0,Math.min(e.max,t.max)-Math.max(e.min,t.min)),"projectedOverlapLength"),i=r((e,t)=>e.nodeId===t.nodeId&&e.orientation===t.orientation&&("H"===e.orientation?(1>Math.abs(e.boundary.x-e.rect.left)||1>Math.abs(e.boundary.x-e.rect.right))&&O(e.boundary,t.boundary,1):(1>Math.abs(e.boundary.y-e.rect.top)||1>Math.abs(e.boundary.y-e.rect.bottom))&&T(e.boundary,t.boundary,1)),"sameTerminalFace"),a=r((e,t)=>e.nodeId===t.nodeId&&e.orientation===t.orientation&&l(e,t)>=8&&.5>Math.abs(e.coord-t.coord),"exactTerminalLaneConflict"),s=r((e,t)=>{if(e.nodeId!==t.nodeId||e.orientation!==t.orientation||"H"!==e.orientation||e.atStart===t.atStart)return!1;let n=l(e,t);if(n<8)return!1;let r=e.rect.bottom-e.rect.top;return!(n<r)&&!(n>2*r)&&i(e,t)&&16>Math.abs(e.coord-t.coord)},"nearTerminalLaneConflict"),f=r((e,t)=>{let n=Y(e.edge.points??[]);if(n.length<2)return;let o="V"===e.orientation?{x:e.boundary.x+t,y:e.boundary.y}:{x:e.boundary.x,y:e.boundary.y+t},l="V"===e.orientation?{x:e.railEnd.x+t,y:e.railEnd.y}:{x:e.railEnd.x,y:e.railEnd.y+t};if(!r(()=>1>Math.abs(e.boundary.y-e.rect.top)||1>Math.abs(e.boundary.y-e.rect.bottom)?T(o,e.boundary,.001)&&o.x>=e.rect.left+1&&o.x<=e.rect.right-1:!!(1>Math.abs(e.boundary.x-e.rect.left)||1>Math.abs(e.boundary.x-e.rect.right))&&O(o,e.boundary,.001)&&o.y>=e.rect.top+1&&o.y<=e.rect.bottom-1,"boundaryStaysOnSameFace")())return;if(e.atStart){let t=n.length>1&&R(n[1],e.railEnd,.001),r=n.slice(t?2:1),i=r[0];if(i&&!e_(i,l))return;return[o,l,...r]}let i=n.length>1&&R(n[n.length-2],e.railEnd,.001),a=n.slice(0,i?-2:-1),s=a[a.length-1];if(!s||e_(s,l))return[...a,l,o]},"shiftedCandidate"),d=r(e=>{let n=e.edge,r=Y(n.points??[]);if(2!==r.length)return!1;let o=n.start,l=n.end,i=o?t.get(o):void 0,a=l?t.get(l):void 0;if(!i||!a)return!1;let s=i.x??0,f=i.y??0,d=a.x??0,u=a.y??0,[h,g]=r;return T(h,g,.001)&&1>Math.abs(f-u)&&Math.abs(s-d)>1||O(h,g,.001)&&1>Math.abs(s-d)&&Math.abs(f-u)>1},"laneIsStraightCollinearConnector"),u=[-7,7,-14,14,-21,21];for(let t=0;t<8;t++){let t=e.filter(e=>!e.isLayoutOnly).flatMap(e=>[o(e,!0),o(e,!1)]).filter(e=>!!e),n=!1;for(let e=0;e<t.length&&!n;e++)for(let r=e+1;r<t.length&&!n;r++){let l=t[e],i=t[r];if(l.edge===i.edge||!(a(l,i)||s(l,i)))continue;let h=!a(l,i);for(let e of[l,i].sort((e,t)=>{let n=d(e),r=d(t);return n!==r?Number(n)-Number(r):Number(!t.atStart)-Number(!e.atStart)})){for(let r of u){let l=f(e,r);if(!l)continue;let i=o({...e.edge,points:l},e.atStart);if(!(!i||t.some(t=>t.edge!==e.edge&&(a(i,t)||h&&s(i,t))))){e.edge.points=l,n=!0;break}}if(n)break}}if(!n)return}}function eW(e,t){let{realNodeRects:n,labelNodeRects:o}=U(t.values()),l=r((t,r)=>{let l=t.start,i=t.end,a=X(r);if(a.length!==r.length-1)return!1;let s=[l,i].filter(e=>!!e);for(let e of a)if(ee(e.a,e.b,n,s,-2)||ee(e.a,e.b,o,[],-2))return!1;for(let n of e){if(n===t||n.isLayoutOnly)continue;let e=n.points;if(e&&!(e.length<2)){for(let t of a)for(let n of X(Y(e)))if(N(t,n,.5)>=8||eo(t.a,t.b,n.a,n.b,.001))return!1}}return!0},"candidateIsSafe"),i=r((e,t)=>{if(t+4>=e.length)return;let n=e[t],r=e[t+1],o=e[t+2],l=e[t+3],i=e[t+4],a=F(n,r)&&E(r,o)&&F(o,l)&&E(l,i)&&O(n,l,.001)&&O(n,i,.001)&&O(r,o,.001)&&(r.x-n.x)*(l.x-o.x)<0,s=E(n,r)&&F(r,o)&&E(o,l)&&F(l,i)&&T(n,l,.001)&&T(n,i,.001)&&T(r,o,.001)&&(r.y-n.y)*(l.y-o.y)<0;if(a||s)return Y([...e.slice(0,t+1),i,...e.slice(t+5)]);if(t+5>=e.length)return;let f=e[t+5],d=E(n,r)&&F(r,o)&&E(o,l)&&F(l,i)&&E(i,f)&&O(n,i,.001)&&O(n,f,.001)&&O(o,l,.001)&&(o.x-r.x)*(i.x-l.x)<0,u=F(n,r)&&E(r,o)&&F(o,l)&&E(l,i)&&F(i,f)&&T(n,i,.001)&&T(n,f,.001)&&T(o,l,.001)&&(o.y-r.y)*(i.y-l.y)<0;if(d||u)return Y([...e.slice(0,t+1),f,...e.slice(t+6)])},"withoutDogleg");for(let t=0;t<8;t++){let t=!1;for(let n of e){if(n.isLayoutOnly)continue;let e=Y(n.points??[]);for(let r=0;r<=e.length-5;r++){let o=i(e,r);if(o&&l(n,o)){n.points=o,t=!0;break}}if(t)break}if(!t)return}}function eK(e,t){let{realNodeRects:n,labelNodeRects:o}=U(t.values()),l=e.filter(e=>!e.isLayoutOnly),i=r((e,t,n)=>Y(e===t?n??[]:e.points??[]),"pointsFor"),a=r((e,t)=>{let n=0;for(let r=0;r<l.length;r++){let o=X(i(l[r],e,t));for(let a=r+1;a<l.length;a++){let r=X(i(l[a],e,t));for(let e of o)for(let t of r)eo(e.a,e.b,t.a,t.b,.001)&&n++}}return n},"strictCrossingCount"),s=r(e=>{let t=X(e);if(3!==t.length)return;let n=t[1];if(t[0].horizontal!==n.horizontal&&t[2].horizontal!==n.horizontal)return{index:n.index,horizontal:n.horizontal,vertical:n.vertical,segment:n}},"middleRail"),f=r((e,t)=>{let r=[e.start,e.end].filter(e=>!!e);return n.filter(e=>{if(r.includes(e.id))return!1;let n=e.rect;return t.horizontal?B(t.a.x,t.b.x,n.left,n.right)>=8&&t.a.y>=n.top-2&&t.a.y<=n.bottom+2:B(t.a.y,t.b.y,n.top,n.bottom)>=8&&t.a.x>=n.left-2&&t.a.x<=n.right+2})},"blockingRectsFor"),d=r((e,t,n)=>{let r=e.map(e=>({...e}));if(t.horizontal)r[t.index].y=n,r[t.index+1].y=n;else{if(!t.vertical)return;r[t.index].x=n,r[t.index+1].x=n}let o=ef(Y(r));return X(o).length===o.length-1?o:void 0},"candidateByMovingRail"),u=r((e,t,r)=>{let s=[e.start,e.end].filter(e=>!!e),f=X(t);if(f.length!==t.length-1)return!1;for(let e of f)if(ee(e.a,e.b,n,s,-2)||ee(e.a,e.b,o,[],-2))return!1;for(let t of l)if(t!==e){for(let e of f)for(let n of X(i(t)))if(N(e,n,.5)>=8)return!1}return a(e,t)<=r},"candidateIsSafe");for(let e=0;e<8;e++){let e=a(),t=!1;for(let n of l){let r=i(n),o=s(r);if(!o)continue;let l=f(n,o.segment);if(0!==l.length){for(let i of o.horizontal?[Math.min(...l.map(e=>e.rect.top))-20,Math.max(...l.map(e=>e.rect.bottom))+20]:[Math.min(...l.map(e=>e.rect.left))-20,Math.max(...l.map(e=>e.rect.right))+20]){let l=d(r,o.segment,i);if(l&&u(n,l,e)){n.points=l,t=!0;break}}if(t)break}}if(!t)return}}function eJ(e,t){let n=r(e=>{let t=e.groupTitleRect;if(t&&"number"==typeof t.left&&"number"==typeof t.right&&"number"==typeof t.top&&"number"==typeof t.bottom&&Number.isFinite(t.left)&&Number.isFinite(t.right)&&Number.isFinite(t.top)&&Number.isFinite(t.bottom)&&!(t.right<=t.left)&&!(t.bottom<=t.top))return{left:t.left,right:t.right,top:t.top,bottom:t.bottom}},"validTitleRect"),o=r(e=>{if(!e.isGroup||e.parentId)return;let t=e.direction,r="string"==typeof t?t.toUpperCase():"";if("LR"===r||"RL"===r||"BT"===r)return;let o=n(e),l=e.y,i=e.height;if(!o||"number"!=typeof l||"number"!=typeof i||!Number.isFinite(l)||!Number.isFinite(i)||i<=0)return;let a=o.right-o.left,s=o.bottom-o.top;if(!(s<=0)&&!(a<s))return{node:e,rect:o}},"topLaneTitleFor"),l=r((e,t)=>{if(!e.horizontal)return!1;let n=e.a.y;return!(n<=t.top+.001)&&!(n>=t.bottom-.001)&&B(e.a.x,e.b.x,t.left,t.right)>=8},"horizontalSegmentIntersectsTitle"),i=[...t.values()].map(o).filter(e=>!!e);if(0===i.length)return;let a=0;for(let t of e)if(!t.isLayoutOnly)for(let e of X(Y(t.points??[])))for(let t of i)l(e,t.rect)&&(a=Math.max(a,t.rect.bottom-e.a.y+4));if(!(a<=.001))for(let e of i){let t=e.node.y,n=e.node.height;"number"==typeof t&&"number"==typeof n&&Number.isFinite(t)&&Number.isFinite(n)&&!(n<=0)&&(e.node.y=t-a/2,e.node.height=n+a,e.node.groupTitleRect={...e.rect,top:e.rect.top-a,bottom:e.rect.bottom-a})}}function eq(e,t){let n=r(e=>{let t=e.groupTitleRect;if(t&&"number"==typeof t.left&&"number"==typeof t.right&&"number"==typeof t.top&&"number"==typeof t.bottom&&Number.isFinite(t.left)&&Number.isFinite(t.right)&&Number.isFinite(t.top)&&Number.isFinite(t.bottom)&&!(t.right<=t.left)&&!(t.bottom<=t.top))return{left:t.left,right:t.right,top:t.top,bottom:t.bottom}},"validTitleRect"),o=r(e=>{if(!e.isGroup||e.parentId||"LR"!==e.direction)return;let t=n(e),r=e.x,o=e.width;if(!t||"number"!=typeof r||"number"!=typeof o||!Number.isFinite(r)||!Number.isFinite(o)||o<=0)return;let l=t.right-t.left,i=t.bottom-t.top;if(!(l<=0)&&!(i<l))return{node:e,rect:t}},"leftLaneTitleFor"),l=r((e,t)=>{if(!e.vertical)return!1;let n=e.a.x;return!(n<=t.left+.001)&&!(n>=t.right-.001)&&B(e.a.y,e.b.y,t.top,t.bottom)>=8},"verticalSegmentIntersectsTitle"),i=r((e,t)=>{if(!e.horizontal)return!1;let n=e.a.y;return!(n<=t.top+.001)&&!(n>=t.bottom-.001)&&B(e.a.x,e.b.x,t.left,t.right)>=8},"horizontalSegmentIntersectsTitle"),a=[...t.values()].map(o).filter(e=>!!e);if(0===a.length)return;let s=0;for(let t of e)if(!t.isLayoutOnly){for(let e of X(Y(t.points??[])))for(let t of a)if(l(e,t.rect))s=Math.max(s,t.rect.right-e.a.x+4);else if(i(e,t.rect)){let n=Math.min(e.a.x,e.b.x);s=Math.max(s,t.rect.right-n+4)}}if(!(s<=.001))for(let e of a){let t=e.node.x,n=e.node.width;"number"==typeof t&&"number"==typeof n&&Number.isFinite(t)&&Number.isFinite(n)&&!(n<=0)&&(e.node.x=t-s/2,e.node.width=n+s,e.node.groupTitleRect={...e.rect,left:e.rect.left-s,right:e.rect.right-s})}}function eU(e,t){let{realNodeRects:n}=U(t.values()),o=e.filter(e=>!e.isLayoutOnly),l=r((e,t=new Map)=>Y(t.get(e)??e.points??[]),"replacementPointsFor"),i=r((e=new Map)=>{let t=0;for(let n=0;n<o.length;n++){let r=X(l(o[n],e));for(let i=n+1;i<o.length;i++){let n=X(l(o[i],e));for(let e of r)for(let r of n)eo(e.a,e.b,r.a,r.b,.001)&&t++}}return t},"crossingCount"),a=r((e=new Map)=>o.reduce((t,n)=>t+z(l(n,e)),0),"totalBends"),s=r(e=>{let t=l(e);if(t.length<4)return;let n=t[t.length-2],r=t[t.length-1];if(F(n,r,.001)||E(n,r,.001))return{tailStart:n,terminal:r}},"terminalTailFor"),f=r((e,t)=>{let n,r=l(e);if(r.length<3)return;let o=r[0],i=r[1];if(F(o,i,.001))n={x:i.x,y:t.tailStart.y};else{if(!E(o,i,.001))return;n={x:t.tailStart.x,y:i.y}}let a=ef(Y([o,i,n,t.tailStart,t.terminal]));return X(a).length===a.length-1?a:void 0},"candidateWithDestinationTail"),d=r((e,t)=>{let r=[e.start,e.end].filter(e=>!!e);for(let e of X(t))if(ee(e.a,e.b,n,r,-2))return!0;return!1},"pathHasNodeHit"),u=r((e,t,n)=>{for(let r of o)if(r!==e){for(let e of X(t))for(let t of X(l(r,n)))if(N(e,t,.5)>=8)return!0}return!1},"pathHasSharedTrack"),h=r((e,t,n)=>!d(e,t)&&!u(e,t,n),"candidateIsSafe"),g=r(()=>{let e=new Map;for(let n of o){let r=n.end;if(!r||!t.has(r)||l(n).length<4)continue;let o=e.get(r)??[];o.push(n),e.set(r,o)}return e},"edgesByDestination");for(let e=0;e<4;e++){let e,t=i();if(0===t)return;let n=a(),r=t,o=n;for(let n of g().values())for(let l=0;l<n.length;l++)for(let d=l+1;d<n.length;d++){let u=n[l],g=n[d],c=s(u),p=s(g);if(!c||!p)continue;let x=f(u,p),m=f(g,c);if(!x||!m)continue;let y=new Map([[u,x],[g,m]]);if(!h(u,x,y)||!h(g,m,y))continue;let b=i(y),M=a(y);!(b>=t)&&(b>r||b===r&&M>=o||(e=y,r=b,o=M))}if(!e)return;for(let[t,n]of e)t.points=n}}function eQ(e,t){let{realNodeRects:n,labelNodeRects:o}=U(t.values()),l=e.filter(e=>!e.isLayoutOnly),i=r((e,t=new Map)=>Y(t.get(e)??e.points??[]),"replacementPointsFor"),a=r((e=new Map)=>{let t=0;for(let n=0;n<l.length;n++){let r=X(i(l[n],e));for(let o=n+1;o<l.length;o++){let n=X(i(l[o],e));for(let e of r)for(let r of n)eo(e.a,e.b,r.a,r.b,.001)&&t++}}return t},"strictCrossingCount"),s=r((e=new Map)=>l.reduce((t,n)=>t+z(i(n,e)),0),"totalBends"),f=r(e=>{let n=e.start,r=e.end,o=n?t.get(n):void 0,l=r?t.get(r):void 0,i=o?V(o):void 0,a=l?V(l):void 0;return i&&a?{src:i,dst:a}:void 0},"endpointRectsFor"),d=r((e,t,n)=>{if(n.index<=0||n.index+1>=t.length-1)return;let r=f(e);if(r){if(n.vertical){let o=n.a.x,l=Math.min(r.src.left,r.dst.left),i=Math.max(r.src.right,r.dst.right),a=o<l-.001?"left":o>i+.001?"right":void 0;if(!a)return;return{edge:e,points:t,segmentIndex:n.index,axis:"vertical",side:a,coord:o,min:Math.min(n.a.y,n.b.y),max:Math.max(n.a.y,n.b.y)}}if(n.horizontal){let o=n.a.y,l=Math.min(r.src.top,r.dst.top),i=Math.max(r.src.bottom,r.dst.bottom),a=o<l-.001?"top":o>i+.001?"bottom":void 0;if(!a)return;return{edge:e,points:t,segmentIndex:n.index,axis:"horizontal",side:a,coord:o,min:Math.min(n.a.x,n.b.x),max:Math.max(n.a.x,n.b.x)}}}},"externalRailForSegment"),u=r(()=>{let e=[];for(let t of l){let n=i(t);for(let r of X(n)){let o=d(t,n,r);o&&e.push(o)}}return e},"collectExternalRails"),h=r((e,t)=>e.edge!==t.edge&&e.axis===t.axis&&e.side===t.side&&B(e.min,e.max,t.min,t.max)>=8,"railsInteract"),g=r(e=>{let t=[],n=new Set;for(let r of e){if(n.has(r))continue;let o=[r],l=[];for(n.add(r);o.length>0;){let t=o.pop();for(let r of(l.push(t),e))!n.has(r)&&h(t,r)&&(n.add(r),o.push(r))}l.length>1&&t.push(l)}return t},"connectedComponents"),c=r(e=>{let t=[];for(let n of e)t.some(e=>.001>Math.abs(e-n.coord))||t.push(n.coord);for(;t.length<e.length;){let n=Math.min(...t),r=Math.max(...t),o=e[0].side;t.push("left"===o||"top"===o?n-12*(e.length-t.length):r+12*(e.length-t.length))}return t},"uniqueCoordsFor"),p=r(e=>{let t=e.map(e=>e.coord),n=c(e),o=[];if(e.length<=6){let l=Array(n.length).fill(!1),i=[],a=r(()=>{if(i.length===e.length){i.some((e,n)=>Math.abs(e-t[n])>=.001)&&o.push([...i]);return}for(let[e,t]of n.entries())l[e]||(l[e]=!0,i.push(t),a(),i.pop(),l[e]=!1)},"visit");return a(),o}for(let e=0;e<t.length;e++)for(let n=e+1;n<t.length;n++){let r=[...t];[r[e],r[n]]=[r[n],r[e]],o.push(r)}return o},"coordinateAssignmentsFor"),x=r((e,t)=>{let n=new Map;for(let[r,o]of e.entries()){let e=t[r],l=n.get(o.edge)??o.points.map(e=>({x:e.x,y:e.y}));"vertical"===o.axis?(l[o.segmentIndex].x=e,l[o.segmentIndex+1].x=e):(l[o.segmentIndex].y=e,l[o.segmentIndex+1].y=e),n.set(o.edge,l)}let r=new Map;for(let[e,t]of n){let n=ef(Y(t));if(X(n).length!==n.length-1)return;r.set(e,n)}return r},"replacementsForAssignment"),m=r(e=>{for(let[t,r]of e){let e=[t.start,t.end].filter(e=>!!e);for(let t of X(r))if(ee(t.a,t.b,n,e,-2)||ee(t.a,t.b,o,[],-2))return!1}for(let t=0;t<l.length;t++){let n=l[t],r=e.has(n),o=X(i(n,e));for(let n=t+1;n<l.length;n++){let t=l[n];if(!r&&!e.has(t))continue;let a=X(i(t,e));for(let e of o)for(let t of a)if(N(e,t,.5)>=8)return!1}}return!0},"candidateIsSafe");for(let e=0;e<4;e++){let e,t=a();if(0===t)return;let n=t,r=s(),o=1/0;for(let l of g(u()))for(let i of p(l)){let f=x(l,i);if(!f||!m(f))continue;let d=a(f);if(d>=t)continue;let u=s(f),h=l.reduce((e,t,n)=>e+Math.abs(i[n]-t.coord),0);d>n||d===n&&(u>r||u===r&&h>=o)||(e=f,n=d,r=u,o=h)}if(!e)return;for(let[t,n]of e)t.points=n}}function eZ(e,t){let{realNodeRects:n,labelNodeRects:o}=U(t.values()),l=e.filter(e=>!e.isLayoutOnly),i=r((e,t,n)=>Y(e===t?n??[]:e.points??[]),"pointsFor"),a=r(e=>X(e).reduce((e,t)=>e+Math.hypot(t.a.x-t.b.x,t.a.y-t.b.y),0),"pathLength"),s=r((e,t)=>{let n=0;for(let r=0;r<l.length;r++){let o=X(i(l[r],e,t));for(let a=r+1;a<l.length;a++){let r=X(i(l[a],e,t));for(let e of o)for(let t of r)eo(e.a,e.b,t.a,t.b,.001)&&n++}}return n},"strictCrossingCount"),f=r((e,t)=>{if(e.horizontal){let n=e.a.y;return(1>Math.abs(n-t.top)||1>Math.abs(n-t.bottom))&&B(e.a.x,e.b.x,t.left,t.right)>=8}if(e.vertical){let n=e.a.x;return(1>Math.abs(n-t.left)||1>Math.abs(n-t.right))&&B(e.a.y,e.b.y,t.top,t.bottom)>=8}return!1},"segmentRunsAlongRectBorder"),d=r(e=>{let n=[e.start,e.end].filter(e=>!!e),r=[];for(let e of n){let n=t.get(e),o=n?V(n):void 0;o&&r.push(o)}return r},"endpointRectsFor"),u=r((e,t)=>{if(t+3>=e.length)return[];let n=e[t],r=e[t+1],o=e[t+2],l=e[t+3],i=F(n,r,.001)&&E(r,o,.001)&&F(o,l,.001),a=E(n,r,.001)&&F(r,o,.001)&&E(o,l,.001);if(!i&&!a||!(i?Math.sign(r.x-n.x)!==Math.sign(l.x-o.x):Math.sign(r.y-n.y)!==Math.sign(l.y-o.y)))return[];let s=O(n,l,.001)||T(n,l,.001)?[]:[{x:n.x,y:l.y},{x:l.x,y:n.y}],f=0===s.length?[[...e.slice(0,t+1),...e.slice(t+3)]]:s.map(n=>[...e.slice(0,t+1),n,...e.slice(t+3)]),d=new Set;return f.map(e=>ef(Y(e))).filter(e=>{if(X(e).length!==e.length-1||!e.some(e=>R(e,l,.001)))return!1;let t=e.map(e=>`${e.x.toFixed(3)},${e.y.toFixed(3)}`).join("|");return!d.has(t)&&(d.add(t),!0)})},"shortcutCandidatesAt"),h=r((e,t,r)=>{let a=[e.start,e.end].filter(e=>!!e),u=d(e);for(let e of X(t))if(ee(e.a,e.b,n,a,-2)||ee(e.a,e.b,o,[],-2)||u.some(t=>f(e,t)))return!1;for(let n of l)if(n!==e){for(let e of X(t))for(let t of X(i(n)))if(N(e,t,.5)>=8)return!1}return s(e,t)<=r},"candidateIsSafe");for(let e=0;e<8;e++){let e,t,n=s(),r=n,o=1/0,f=1/0;for(let d of l){let l=i(d),g=z(l,.001),c=a(l);for(let i=0;i<=l.length-4;i++)for(let p of u(l,i)){let l=z(p,.001),i=a(p);if(!(l<g||l===g&&i<c-.001)||!h(d,p,n))continue;let u=s(d,p);u>r||u===r&&(l>o||l===o&&i>=f)||(e=d,t=p,r=u,o=l,f=i)}}if(!e||!t)return;e.points=t}}function e0(e,t){let n=[];for(let e of t.values()){if(e.isGroup||e.isEdgeLabel)continue;let t=e.x??0,r=e.y??0,o=V(e);o&&n.push({id:String(e.id??""),cx:t,cy:r,rect:o})}if(0===n.length)return;let o=new Map(n.map(e=>[e.id,e])),l=n.map(e=>({id:e.id,rect:e.rect})),i=["top","bottom","left","right"],a={top:Math.min(...n.map(e=>e.rect.top))-20,bottom:Math.max(...n.map(e=>e.rect.bottom))+20,left:Math.min(...n.map(e=>e.rect.left))-20,right:Math.max(...n.map(e=>e.rect.right))+20},s=e.filter(e=>!e.isLayoutOnly),f=new Map(s.map((e,t)=>[e,t])),d=r(e=>{let t="left"===e||"top"===e?-1:1,n=[];for(let r=0;r<=2;r++)n.push(a[e]+20*t*r);return n},"outwardTracksForSide"),u=r((e,t=new Map)=>Y(t.get(e)??e.points??[]),"replacementPointsFor"),h=r((e,t)=>{let n=0;for(let r of e)for(let e of t)eo(r.a,r.b,e.a,e.b,.001)&&n++;return n},"crossingCountBetweenSegments"),g=r((e,t)=>h(X(e),X(t)),"crossingCountBetweenPaths"),c=r((e=new Map)=>{let t=0,n=[],o=new Set,l=[],i=r(e=>{o.has(e)||(o.add(e),l.push(e))},"addEdge");for(let r=0;r<s.length;r++){let o=s[r],l=u(o,e);for(let a=r+1;a<s.length;a++){let r=s[a],f=g(l,u(r,e));f>0&&(t+=f,n.push({first:o,second:r,count:f}),i(o),i(r))}}return l.sort((e,t)=>(f.get(e)??0)-(f.get(t)??0)),{count:t,pairs:n,edgeSet:o,edges:l}},"crossingSnapshot"),p=r((e,t)=>{let n=new Set(t.keys());if(0===n.size)return e.count;let r=0;for(let t of e.pairs)(n.has(t.first)||n.has(t.second))&&(r+=t.count);let o=0;for(let e=0;e<s.length;e++){let r=s[e],l=n.has(r),i=u(r,t);for(let r=e+1;r<s.length;r++){let e=s[r];(l||n.has(e))&&(o+=g(i,u(e,t)))}}return e.count-r+o},"crossingCountWithReplacements"),x=r(e=>{let t=new Map;for(let n of e.pairs){let e=t.get(n.first)??new Set;e.add(n.second),t.set(n.first,e);let r=t.get(n.second)??new Set;r.add(n.first),t.set(n.second,r)}let n=[],r=new Set;for(let o of e.edges){if(r.has(o))continue;let e=[o],l=[];for(r.add(o);e.length>0;){let n=e.pop();for(let o of(l.push(n),t.get(n)??[]))r.has(o)||(r.add(o),e.push(o))}l.sort((e,t)=>(f.get(e)??0)-(f.get(t)??0)),l.length>1&&n.push(l)}return n},"crossingComponents"),m=r(e=>[e.start,e.end].filter(e=>!!e),"endpointIdsFor"),y=r(e=>{let t=[];for(let n of x(e)){let e=new Set(n),r=new Set(n.flatMap(e=>m(e))),o=[...n];for(let t of s)!e.has(t)&&m(t).some(e=>r.has(e))&&o.push(t);o.sort((e,t)=>(f.get(e)??0)-(f.get(t)??0)),t.push(o)}return t},"pairSearchGroups"),b=r((e,t,n)=>p(e,new Map([[t,n]])),"crossingCountWithSingleReplacement"),M=r(e=>{let t=new Map;for(let n of e.pairs)t.set(n.first,(t.get(n.first)??0)+n.count),t.set(n.second,(t.get(n.second)??0)+n.count);return t},"currentCrossingsByEdge"),I=r(e=>e.slice(1).reduce((t,n,r)=>{let o=e[r];return t+Math.abs(n.x-o.x)+Math.abs(n.y-o.y)},0),"pathLength"),S=r((e=new Map)=>s.reduce((t,n)=>t+z(u(n,e)),0),"totalBends"),w=r((e=new Map)=>s.reduce((t,n)=>t+I(u(n,e)),0),"totalLength"),v=r((e,t,n=new Map)=>{let r=X(t);for(let t of s)if(t!==e){for(let e of r)for(let r of X(u(t,n)))if(N(e,r,.5)>=8)return!0}return!1},"pathHasSegmentConflict"),C=r((e,t)=>{let n=[e.start,e.end].filter(e=>!!e);for(let e of X(t))if(ee(e.a,e.b,l,n,-2))return!0;return!1},"pathHitsNode"),L=r((e,t)=>{let n=ef(Y(t));X(n).length===n.length-1&&e.push(n)},"pushOrthogonalCandidate"),k=r(e=>"left"===e||"right"===e,"sideIsHorizontal"),$=r((e,t,n)=>{switch(t){case"left":return Math.min(e.x,n.x)-20;case"right":return Math.max(e.x,n.x)+20;case"top":return Math.min(e.y,n.y)-20;case"bottom":return Math.max(e.y,n.y)+20}},"localTrackForSameSide"),R=r((e,t,n,r)=>{let o="left"===n||"top"===n?-1:1;for(let l of[$(t,n,r),a[n]])for(let i=0;i<=2;i++)L(e,J(t,n,r,l+20*o*i))},"addSameSideCandidates"),O=r((e,t,n,r,o)=>{for(let l of d(n))for(let n of d(o))L(e,[t,{x:l,y:t.y},{x:l,y:n},{x:r.x,y:n},r])},"addHorizontalToVerticalCandidates"),T=r((e,t,n,r,o)=>{for(let l of d(n))for(let n of d(o))L(e,[t,{x:t.x,y:l},{x:n,y:l},{x:n,y:r.y},r])},"addVerticalToHorizontalCandidates"),B=r((e,t,n,r,o)=>{let l=[...d("top"),...d("bottom")];for(let i of d(n))for(let n of d(o))for(let o of l)L(e,[t,{x:i,y:t.y},{x:i,y:o},{x:n,y:o},{x:n,y:r.y},r])},"addHorizontalPairCandidates"),A=r((e,t,n,r,o)=>{let l=[...d("left"),...d("right")];for(let i of d(n))for(let n of d(o))for(let o of l)L(e,[t,{x:t.x,y:i},{x:o,y:i},{x:o,y:n},{x:r.x,y:n},r])},"addVerticalPairCandidates"),G=r(e=>{let t=new Set;return e.map(e=>Y(e)).filter(e=>{let n=e.map(e=>`${e.x.toFixed(3)},${e.y.toFixed(3)}`).join("|");return!t.has(n)&&!(e.length<2)&&(t.add(n),!0)})},"dedupeCandidatePaths"),P=r((e,t,n,r)=>{let o=[],l=K(e,t,n,r,20,.001);l&&L(o,l),t===r&&R(o,e,t,n);let i=k(t),a=k(r);return i&&!a?O(o,e,t,n,r):!i&&a?T(o,e,t,n,r):i?B(o,e,t,n,r):A(o,e,t,n,r),G(o)},"buildCandidatesForSides"),D=r((e,t,n,r)=>{let o=[...d("left"),...d("right")],l=[...d("top"),...d("bottom")];for(let a of i){let i=W(r,a),s="top"===a||"bottom"===a?d(a):l;for(let r of o)for(let o of(L(e,[t,n,{x:r,y:n.y},{x:r,y:i.y},i]),s))L(e,[t,n,{x:r,y:n.y},{x:r,y:o},{x:i.x,y:o},i])}},"addVerticalDepartureOuterTrackCandidates"),H=r((e,t,n,r)=>{let o=[...d("left"),...d("right")],l=[...d("top"),...d("bottom")];for(let a of i){let i=W(r,a),s="left"===a||"right"===a?d(a):o;for(let r of l)for(let o of(L(e,[t,n,{x:n.x,y:r},{x:i.x,y:r},i]),s))L(e,[t,n,{x:n.x,y:r},{x:o,y:r},{x:o,y:i.y},i])}},"addHorizontalDepartureOuterTrackCandidates"),j=r(e=>{let t=e.start,n=e.end,r=n?o.get(n):void 0;if(!t||!r)return[];let l=Y(e.points??[]);if(l.length<4)return[];let i=l[0],a=l[1],s=[];return E(i,a,.001)?D(s,i,a,r):F(i,a,.001)&&H(s,i,a,r),s},"terminalPreservingOuterTrackCandidates"),_=r(e=>{let t=e.start,n=e.end,r=t?o.get(t):void 0,l=n?o.get(n):void 0;if(!r||!l)return[];let a=[];for(let e of i){let t=W(r,e);for(let n of i)a.push(...P(t,e,W(l,n),n))}return a.push(...j(e)),a},"candidatePathsFor"),q=r(()=>new Map(s.map(e=>[e,X(u(e))])),"currentSegmentsByEdge"),U=r((e,t,n)=>{let r=new Set;for(let o of s){if(o===e)continue;let l=n.get(o)??X(u(o));t.some(e=>l.some(t=>N(e,t,.5)>=8))&&r.add(o)}return r},"sharedTrackConflictsFor"),Q=r((e,t,n,r)=>{let o=new Set;return _(e).map(e=>ef(Y(e))).filter(t=>{if(C(e,t))return!1;let n=t.map(e=>`${e.x.toFixed(3)},${e.y.toFixed(3)}`).join("|");return!o.has(n)&&!(t.length<2)&&(o.add(n),!0)}).map(o=>{let l=X(o),i=0;for(let t of s)t!==e&&(i+=h(l,n.get(t)??X(u(t))));return{candidate:o,candidateSegments:l,crossings:t.count-(r.get(e)??0)+i,bends:z(o,.001),totalBends:z(o),length:I(o)}}).filter(({crossings:e})=>e<=t.count).sort((e,t)=>e.crossings-t.crossings||e.bends-t.bends||e.length-t.length).slice(0,48).map(t=>({path:t.candidate,segments:t.candidateSegments,sharedTrackConflicts:U(e,t.candidateSegments,n),totalBends:t.totalBends,length:t.length}))},"pairCandidatesFor"),Z=r((e,t,n,r,o,l)=>{let i=0;for(let n of e.pairs)(n.first===t||n.second===t||n.first===r||n.second===r)&&(i+=n.count);let a=h(n.segments,o.segments);for(let e of s){if(e===t||e===r)continue;let i=l.get(e)??X(u(e));a+=h(n.segments,i)+h(o.segments,i)}return e.count-i+a},"pairCrossingCount"),et=r((e,t)=>{for(let n of e.sharedTrackConflicts)if(n!==t)return!1;return!0},"conflictsOnlyWith"),en=r((e,t)=>e.segments.some(e=>t.segments.some(t=>N(e,t,.5)>=8)),"candidatesShareTrack"),er=r((e,t,n,r)=>et(t,n.edge)&&et(r,e.edge)&&!en(t,r),"pairCandidatesAreCompatible"),el=r((e,t,n,r,o)=>{let l=Z(e.current,t.edge,n,r.edge,o,e.baseSegments);if(!(l>=e.current.count))return{replacements:new Map([[t.edge,n.path],[r.edge,o.path]]),crossings:l,bends:e.currentBends-(e.baseBendsByEdge.get(t.edge)??0)-(e.baseBendsByEdge.get(r.edge)??0)+n.totalBends+o.totalBends,length:e.currentLength-(e.baseLengthByEdge.get(t.edge)??0)-(e.baseLengthByEdge.get(r.edge)??0)+n.length+o.length}},"scorePairReplacement"),ei=r((e,t)=>e.crossings<t.crossings||e.crossings===t.crossings&&(e.bends<t.bends||e.bends===t.bends&&e.length<t.length),"pairScoreIsBetter"),ea=r((e,t,n,r)=>{let o=r;for(let r of t.candidates)for(let l of n.candidates){if(!er(t,r,n,l))continue;let i=el(e,t,r,n,l);i&&ei(i,o)&&(o=i)}return o},"bestScoreForOptionPair"),es=r(e=>{let t=S(),n=w(),r=q(),o=M(e),l=new Map(s.map(e=>[e,z(u(e))])),i=new Map(s.map(e=>[e,I(u(e))])),a=new Map,f=y(e);for(let t of f)for(let n of t){if(a.has(n))continue;let t=Q(n,e,r,o);t.length>0&&a.set(n,{edge:n,candidates:t})}let d={replacements:new Map,crossings:e.count,bends:t,length:n},h={current:e,currentBends:t,currentLength:n,baseBendsByEdge:l,baseLengthByEdge:i,baseSegments:r};for(let t of f){let n=new Set(t.filter(t=>e.edgeSet.has(t))),r=t.map(e=>a.get(e)).filter(e=>!!e);for(let e=0;e<r.length;e++){let t=r[e];for(let o=e+1;o<r.length;o++){let e=r[o];(n.has(t.edge)||n.has(e.edge))&&(d=ea(h,t,e,d))}}}return d.replacements.size>0?d.replacements:void 0},"bestPairedReplacement");for(let e=0;e<4;e++){let e,t,n=c(),r=n.count;if(0===r)return;let o=r,l=1/0;for(let i of n.edges){let a=z(u(i),.001);for(let s of _(i)){let f=C(i,s),d=!f&&v(i,s),u=b(n,i,s),h=z(s,.001);!f&&!d&&(u<r||u===r&&h<a)&&(u>o||u===o&&h>=l||(e=i,t=s,o=u,l=h))}}if(e&&t){e.points=t;continue}let i=es(n);if(!i)return;for(let[e,t]of i)e.points=t}}function e1(e,t){let{nodeInfoById:n,realNodeRects:o}=q(t),l=["top","bottom","left","right"],i={top:Math.min(...o.map(e=>e.rect.top))-20,bottom:Math.max(...o.map(e=>e.rect.bottom))+20,left:Math.min(...o.map(e=>e.rect.left))-20,right:Math.max(...o.map(e=>e.rect.right))+20},a=r((e,t,n,r)=>{let o=[],l=K(e,t,n,r,20,.001);return l&&o.push(l),t===r&&o.push(J(e,t,n,i[t])),o},"buildOrthogonalPathCandidates"),s=r((e,t)=>{for(let n=0;n<e.length-1;n++)if(ee(e[n],e[n+1],o,t,1))return!0;return!1},"pathHitsNode"),f=r((t,n,r=!1)=>{let o=0,l=X(t,.001),i=n.start,a=n.end;for(let t of e){if(t===n||t.isLayoutOnly)continue;let e=t.start,s=t.end;if(!r&&i&&a&&(e===i||e===a||s===i||s===a))continue;let f=t.points;if(f&&!(f.length<2))for(let e of l)for(let t of X(f,.001)){if(et(e.a,e.b,t.a,t.b,.001,.001)){o++;continue}N(e,t,.001)>=8&&o++}}return o},"pathConflictCount"),d=r((e,t)=>{let n=Math.abs(e.y-t.rect.top),r=Math.abs(e.y-t.rect.bottom),o=Math.abs(e.x-t.rect.left),l=Math.abs(e.x-t.rect.right),i="top",a=n;return r<a&&(i="bottom",a=r),o<a&&(i="left",a=o),l<a&&(i="right",a=l),i},"nearestSideOfRect"),u=new Map,h=r((e,t,n)=>{let r=u.get(e)??[];r.push({side:t,edgeId:n}),u.set(e,r)},"addFaceClaim");for(let t of e){if(t.isLayoutOnly)continue;let e=t.points??[];if(e.length<1)continue;let r=t.id??"",o=t.start,l=t.end;if(o){let t=n.get(o);t&&h(o,d(e[0],t),r)}if(l){let t=n.get(l);t&&h(l,d(e[e.length-1],t),r)}}let g=r((e,t,n)=>u.get(e)?.some(e=>e.edgeId!==n&&e.side===t)??!1,"faceIsClaimed");for(let t of e){let e;if(t.isLayoutOnly)continue;let r=t.points;if(!r||r.length<2)continue;let o=z(r,.001);if(o<4)continue;let i=t.start,c=t.end;if(!i||!c)continue;let p=n.get(i),x=n.get(c);if(!p||!x)continue;let m=t.id??"",y=f(r,t,!0),b=f(r,t),M=y,I=o;for(let n of l){if(g(i,n,m))continue;let r=W(p,n);for(let o of l)if(!g(c,o,m))for(let l of a(r,n,W(x,o),o)){if(s(l,[i,c]))continue;let n=z(l,.001);if(y>0){let r=f(l,t,!0);if(r>M||r===M&&n>=I)continue;M=r,I=n,e=l;continue}!(f(l,t)>b)&&n<I&&(I=n,e=l)}}if(e){t.points=e;let n=u.get(i);n&&u.set(i,n.filter(e=>e.edgeId!==m));let r=u.get(c);r&&u.set(c,r.filter(e=>e.edgeId!==m)),h(i,d(e[0],p),m),h(c,d(e[e.length-1],x),m)}}}function e2(e,t){let n=t?0:e.length-1,r=e[n],o=e[n+(t?1:-1)];if(!r||!o)return;let l=o.x-r.x,i=o.y-r.y;if(!(Math.abs(l)+Math.abs(i)<.001)){if(.001>=Math.abs(i)){let e=r.x+10*Math.sign(l);return{left:Math.min(r.x,e),right:Math.max(r.x,e),top:r.y-7,bottom:r.y+7}}if(.001>=Math.abs(l)){let e=r.y+10*Math.sign(i);return{left:r.x-7,right:r.x+7,top:Math.min(r.y,e),bottom:Math.max(r.y,e)}}return{left:Math.min(r.x,o.x),right:Math.max(r.x,o.x),top:Math.min(r.y,o.y),bottom:Math.max(r.y,o.y)}}}function e6(e){return{left:Math.min(e.left,e.right),right:Math.max(e.left,e.right),top:Math.min(e.top,e.bottom),bottom:Math.max(e.top,e.bottom)}}function e5(e,t){let n=Y(t);return[e2(n,!0),e2(n,!1)].some(t=>t&&H(e,e6(t)))}function e3(e,t){let n=[];for(let t of e){if(t.isLayoutOnly)continue;let e=t.points;if(e&&!(e.length<2))for(let r=0;r<e.length-1;r++)n.push({edgeId:t.id,p1:e[r],p2:e[r+1]})}let o=[],l=[];for(let e of t.values()){let t=e.isGroup,n=e.parentId;if(t&&!n){let t=V(e);t&&l.push({id:e.id,rect:t});continue}if(t||e.isEdgeLabel)continue;let r=V(e);r&&o.push({nodeId:e.id,rect:r})}let i=r((e,t)=>{let n=j(t,3);for(let{nodeId:t,rect:r}of o)if(t!==e&&H(n,r))return!0;return!1},"labelOverlapsForeignNode"),a=r((e,t)=>{let r=j(t,3);for(let t of n)if(t.edgeId!==e&&G(t.p1,t.p2,r))return!0;return!1},"labelOverlapsForeignEdge"),s=r((e,t,n)=>i(e,n)||a(t,n),"labelOverlapsAnything"),f=[],d=r(e=>{for(let{id:t,rect:n}of l)if(D(n,e))return t},"findContainingLane"),u=r((e,t)=>f.some(n=>n.labelId!==e&&H(t,n.rect)),"overlapsPlacedLabel");for(let n of e){if(n.isLayoutOnly)continue;let e=n.labelNodeId;if(!e)continue;let o=t.get(e);if(!o)continue;let h=n.points;if(!h||h.length<2)continue;let g=o.width??0,c=o.height??0;if(g<=0||c<=0)continue;let p=[];for(let e=0;e<h.length-1;e++){let t=h[e],n=h[e+1],r=Math.abs(t.x-n.x),o=Math.abs(t.y-n.y);(!(r<.001)||!(o<.001))&&(r>=.001&&o>=.001||p.push({idx:e,length:r+o,orientation:r>=.001?"horizontal":"vertical",midX:(t.x+n.x)/2,midY:(t.y+n.y)/2}))}if(0===p.length)continue;let x=p.length>=3?p.filter(e=>e.idx>0&&e.idx<p.length-1):p,m=x.length>0?x:p,y=g>=c?"horizontal":"vertical",b=r(e=>[...e].sort((e,t)=>{let n=e.orientation===y;if(n!==(t.orientation===y))return n?-1:1;let r=e.length>=("horizontal"===e.orientation?g:c)+2;return r!==t.length>=("horizontal"===t.orientation?g:c)+2?r?-1:1:t.length-e.length}),"rankSegments"),M=p[0],I=p[p.length-1],S=[.5,.25,.75,.05,.95,.15,.85,.1,.9],w=r((e,t)=>{let n=h[e.idx],r=h[e.idx+1];return{midX:n.x+(r.x-n.x)*t,midY:n.y+(r.y-n.y)*t}},"anchorAtT"),v=r((e,t,n)=>Math.min(n,Math.max(t,e)),"clamp"),C=r((e,t)=>e.midX>=t.left-.001&&e.midX<=t.right+.001&&e.midY>=t.top-.001&&e.midY<=t.bottom+.001,"pointInsideRectInclusive"),L=r(e=>{let t=_(e.midX,e.midY,g,c),n=d(t);if(n)return{laneId:n,anchor:e,rect:t};let r=l.find(({rect:t})=>C(e,t));if(!r)return;let o=r.rect.left+g/2+1,i=r.rect.right-g/2-1,a=r.rect.top+c/2+1,s=r.rect.bottom-c/2-1;if(o>i||a>s)return;let f={midX:v(e.midX,o,i),midY:v(e.midY,a,s)},u=_(f.midX,f.midY,g,c);return C(e,u)?{laneId:r.id,anchor:f,rect:u}:void 0},"placementForAnchor"),k=r((e,t,n)=>"horizontal"===e.orientation?Math.abs(t.midX-n.x):Math.abs(t.midY-n.y),"distanceAlongSegment"),$=r((e,t)=>{let n=("horizontal"===e.orientation?g/2:c/2)+12;if(e===M){let r=h[e.idx];if(k(e,t,r)+.001<n)return!1}if(e===I){let r=h[e.idx+1];if(k(e,t,r)+.001<n)return!1}return!0},"labelClearsTerminalEndpoints"),R=r(t=>{for(let r of b(t))for(let t of S){let o=w(r,t);if(!$(r,o))continue;let l=L(o);if(!(!l||e5(l.rect,h))&&!u(e,l.rect)&&!s(e,n.id,l.rect))return{laneId:l.laneId,anchor:l.anchor}}},"tryPool"),O=r((t,r,o=!1)=>{for(let l of b(t)){let t={midX:l.midX,midY:l.midY};if(r&&!$(l,t))continue;let s=L(t);if(s&&!e5(s.rect,h)&&!u(e,s.rect)&&!i(e,s.rect)&&(o||!a(n.id,s.rect)))return{laneId:s.laneId,anchor:s.anchor}}},"findLaneContainingFallback"),T=R(m)??(m.length<p.length?R(p):void 0)??O(p,!0)??O(p,!1)??O(p,!1,!0);if(T){o.x=T.anchor.midX,o.y=T.anchor.midY,o.parentId=T.laneId;let t=_(T.anchor.midX,T.anchor.midY,g,c),n=f.findIndex(t=>t.labelId===e);n>=0?f[n]={labelId:e,rect:t}:f.push({labelId:e,rect:t})}}}r(eV,"separateSharedRenderedTerminalLanes"),r(eW,"collapseRedundantRectangularDoglegs"),r(eK,"liftObstacleHuggingSameSideRails"),r(eJ,"liftTopLaneTitleBandsAboveRails"),r(eq,"shiftLeftLaneTitleBandsLeftOfRails"),r(eU,"swapDestinationTerminalTailsToReduceCrossings"),r(eQ,"reassignCrossingExternalRailChannels"),r(eZ,"shortcutRedundantOrthogonalJogs"),r(e0,"resolveRenderedOrthogonalCrossings"),r(e1,"simplifyDetouredEdges"),r(e2,"markerClearanceRectFor"),r(e6,"normalizeRect"),r(e5,"labelOverlapsOwnMarker"),r(e3,"anchorLabelsToPolyline");function e8(e,t){return e<t?`${e}::${t}`:`${t}::${e}`}function e4(e,t){let{nodeInfoById:n,realNodeRects:o}=q(t),l=new Map;for(let e of t){let t=e.id;if(!e.isGroup&&e.isEdgeLabel){l.set(t,{w:e.width??0,h:e.height??0});continue}}let i=r((t,n,o,i)=>{let a=e8(n,o),s=0,f=r(e=>{if(!e)return;let t=l.get(e);if(!t)return;let n="x"===i?t.w/2:t.h/2;n>s&&(s=n)},"consider");for(let n of(f(t.labelNodeId),e)){if(n===t||n.isLayoutOnly)continue;let e=n.start,r=n.end;e&&r&&e8(e,r)===a&&f(n.labelNodeId)}return s>0?s+3:0},"labelClearanceFor");for(let t of e){let r,l;if(t.isLayoutOnly||!A(t.points,1e-6))continue;let a=Z(t,n,1e-6);if(!a)continue;let{srcId:s,dstId:f,srcInfo:d,dstInfo:u,collinearX:h,collinearY:g}=a;if(h===g)continue;if(h){let e=u.cy>d.cy;r={x:d.cx,y:e?d.rect.bottom:d.rect.top},l={x:u.cx,y:e?u.rect.top:u.rect.bottom}}else{let e=u.cx>d.cx;r={x:e?d.rect.right:d.rect.left,y:d.cy},l={x:e?u.rect.left:u.rect.right,y:u.cy}}if(ee(r,l,o,[s,f],1))continue;let c=i(t,s,f,h?"x":"y"),p=c>4?c:4;for(let n of[0,p,-p]){let i={...r},a={...l};if(h){if(i.x+=n,a.x+=n,i.x<=d.rect.left||i.x>=d.rect.right||a.x<=u.rect.left||a.x>=u.rect.right)continue}else if(i.y+=n,a.y+=n,i.y<=d.rect.top||i.y>=d.rect.bottom||a.y<=u.rect.top||a.y>=u.rect.bottom)continue;if(!ee(i,a,o,[s,f],1)&&!er(i,a,e,t,{epsilon:1e-6})){t.points=[i,a];break}}}}function e9(e,t){let{realNodeRects:n,labelNodeRects:o}=U(t.values()),l=r((e,t)=>X(t,.001).map(n=>({...n,edge:e,interior:n.index>=1&&n.index<=t.length-3})),"segmentsFor"),i=r(()=>{let t=[];for(let n of e){if(n.isLayoutOnly)continue;let e=n.points;e&&!(e.length<2)&&t.push(...l(n,Y(e)))}return t},"allSegments"),a=r((e,t)=>e.horizontal&&t.horizontal?B(e.a.x,e.b.x,t.a.x,t.b.x)>=8&&7>Math.abs(e.a.y-t.a.y):!!e.vertical&&!!t.vertical&&B(e.a.y,e.b.y,t.a.y,t.b.y)>=8&&7>Math.abs(e.a.x-t.a.x),"hasCrowdedParallelTrack"),s=r((t,r)=>{let i=t.start,s=t.end,f=l(t,r);if(f.length!==r.length-1)return!1;let d=[i,s].filter(e=>!!e),u=t.labelNodeId?[t.labelNodeId]:[];for(let e of f)if(ee(e.a,e.b,n,d,-2)||ee(e.a,e.b,o,u,-2))return!1;for(let n of e){if(n===t||n.isLayoutOnly)continue;let e=n.points;if(e&&!(e.length<2)){for(let t of f)for(let r of l(n,Y(e)))if(a(t,r)||eo(t.a,t.b,r.a,r.b,.001))return!1}}return!0},"candidateIsSafe"),f=r((e,t)=>{let n=Y(e.edge.points??[]);if(n.length<4||e.index>=n.length-1)return;let r=n.map(e=>({...e}));if(e.horizontal)r[e.index].y+=t,r[e.index+1].y+=t;else{if(!e.vertical)return;r[e.index].x+=t,r[e.index+1].x+=t}return l(e.edge,r).length===r.length-1?r:void 0},"shiftedCandidate"),d=r((e,t)=>({x:e.x??(t.left+t.right)/2,y:e.y??(t.top+t.bottom)/2}),"nodeCenter"),u=r(e=>{let n=e.edge,r=Y(n.points??[]);if(4!==r.length||1!==e.index)return;let o=n.start?t.get(n.start):void 0,l=n.end?t.get(n.end):void 0,i=o?V(o):void 0,a=l?V(l):void 0,s=r.slice(e.index+2);if(o&&l&&i&&a&&0!==s.length)return{sourceCenter:d(o,i),targetCenter:d(l,a),sourceRect:i,tail:s}},"sourceDetourContextFor"),h=r((e,t,n,r,o,l)=>{let i=r.y>=n.y,a=i?o.bottom:o.top,s=a+(i?20:-20);if(i&&e.b.y<=s+.001||!i&&e.b.y>=s-.001)return;let f=e.a.x+t;return Y([{x:n.x,y:a},{x:n.x,y:s},{x:f,y:s},{x:f,y:e.b.y},...l],.001)},"verticalSourceDetour"),g=r((e,t,n,r,o,l)=>{let i=r.x>=n.x,a=i?o.right:o.left,s=a+(i?20:-20);if(i&&e.b.x<=s+.001||!i&&e.b.x>=s-.001)return;let f=e.a.y+t;return Y([{x:a,y:n.y},{x:s,y:n.y},{x:s,y:f},{x:e.b.x,y:f},...l],.001)},"horizontalSourceDetour"),c=r((e,t)=>{let n=u(e);if(n){if(e.vertical)return h(e,t,n.sourceCenter,n.targetCenter,n.sourceRect,n.tail);if(e.horizontal)return g(e,t,n.sourceCenter,n.targetCenter,n.sourceRect,n.tail)}},"sourceDetourCandidate"),p=[-7,7,-14,14,-21,21];for(let e=0;e<12;e++){let e=i(),t=!1;for(let n=0;n<e.length&&!t;n++)for(let r=n+1;r<e.length&&!t;r++){let o=e[n],l=e[r];if(o.edge!==l.edge&&a(o,l))for(let e of[o,l].filter(e=>e.interior)){for(let n of p){let r=f(e,n);if(r&&s(e.edge,r)){e.edge.points=r,t=!0;break}let o=c(e,n);if(o&&s(e.edge,o)){e.edge.points=o,t=!0;break}}if(t)break}}if(!t)return}}function e7(e,t,n,r){let o=t.x-e.x,l=t.y-e.y,i=r.x-n.x,a=r.y-n.y,s=o*a-l*i;if(1e-10>Math.abs(s))return!1;let f=n.x-e.x,d=n.y-e.y,u=(f*a-d*i)/s,h=(f*l-d*o)/s;return u>.01&&u<.99&&h>.01&&h<.99}function te(e){let t=e.nodes??[],r=e.edges??[],o=[];if(!r.length||!t.length)return o;let l=Q(t),i=[];for(let e of r){if(e.isLayoutOnly)continue;let t=e.points;if(!t||t.length<2)continue;let n=e.start,r=e.end,a=e.labelNodeId,s=e.id??`${n}->${r}`;for(let e of l)if(e.nodeId!==n&&e.nodeId!==r&&(!a||e.nodeId!==a)){for(let n=0;n<t.length-1;n++)if(G(t[n],t[n+1],e,-1)){o.push({type:"edge-node-overlap",edgeId:s,targetId:e.nodeId,detail:`segment ${n} passes through node "${e.nodeId}"`});break}}for(let e=0;e<t.length-1;e++)i.push({edgeId:s,start:n,end:r,p1:t[e],p2:t[e+1]})}let a=new Set;for(let e=0;e<i.length;e++)for(let t=e+1;t<i.length;t++){let n=i[e],r=i[t];if(n.edgeId!==r.edgeId&&n.start!==r.start&&n.start!==r.end&&n.end!==r.start&&n.end!==r.end&&e7(n.p1,n.p2,r.p1,r.p2)){let e=n.edgeId<r.edgeId?`${n.edgeId}|${r.edgeId}`:`${r.edgeId}|${n.edgeId}`;a.has(e)||(a.add(e),o.push({type:"edge-edge-crossing",edgeId:n.edgeId,targetId:r.edgeId,detail:`edges "${n.edgeId}" and "${r.edgeId}" cross`}))}}if(o.length>0){let e=o.filter(e=>"edge-node-overlap"===e.type).length,t=o.filter(e=>"edge-edge-crossing"===e.type).length;for(let r of(n.warn(`[SWIMLANE_VALIDATE] ${o.length} issue(s) detected: ${e} edge-node overlap(s), ${t} edge crossing(s)`),o))n.warn(`[SWIMLANE_VALIDATE]   ${r.type}: ${r.detail}`)}return o}function tt(e,t){let n=e.nodes??[],o=e.edges??[],l=n.filter(e=>!e.isGroup);if(("LR"===t||"RL"===t)&&l.length>0&&!eP(e,t)||"BT"===t&&l.length>0&&!eG(e))return;for(let e of o){if(e.isLayoutOnly)continue;let t=e.points;t&&!(t.length<2)&&(e.points=ef(es(t)))}e1(o,n),e4(o,n),eH(o,n);let i=new Map;for(let e of n)i.set(String(e.id),e);e3(o,i),eg(o,i),ej(o,i),e9(o,i),eV(o,i),eW(o,i),eK(o,i),eU(o,i);let a=r(()=>{e0(o,i),eQ(o,i),eZ(o,i),e3(o,i),eF(o,i),eK(o,i),e3(o,i),eF(o,i)},"finalizeRenderedEdges");a(),e9(o,i),a(),eJ(o,i),eq(o,i),eJ(o,i),eq(o,i)}function tn(e){let t=new Map(e.nodeById),n=new Set,r=[];for(let o of e.edges){if(!t.has(o.src)||!t.has(o.dst))continue;let e=`${o.id}:${o.src}->${o.dst}`;n.has(e)||(n.add(e),r.push(o))}return{nodes:[...t.keys()],edges:r,layout:e.layout,nodeById:t}}function tr(e,t){return e.edges.filter(e=>e.dst===t)}function to(e){let t=new Map;for(let n of e.nodes)t.set(n,[]);for(let n of e.edges)t.get(n.src).push(n.dst);return t}function tl(e){let t=to(e);for(let e of t.values())e.sort((e,t)=>e.localeCompare(t));return t}function ti(e){let t=new Map;for(let n of e.nodes)t.set(n,0);for(let n of e.edges)t.set(n.dst,(t.get(n.dst)??0)+1);return t}function ta(e){return[...e.entries()].filter(([,e])=>0===e).map(([e])=>e).sort((e,t)=>e.localeCompare(t))}function ts(e,t=()=>!0){let n=new Map,r=new Map;for(let t of e.nodes)n.set(t,[]),r.set(t,[]);for(let o of e.edges)t(o)&&(r.get(o.src).push(o.dst),n.get(o.dst).push(o.src));return{preds:n,succs:r}}function tf(e,t,n,r){let o=0;for(let t of e.nodes)r?.skipGroups&&e.nodeById.get(t)?.isGroup||(o=Math.max(o,n[t]??0));let l=Array.from({length:o+1},()=>[]);for(let o of t)r?.skipGroups&&e.nodeById.get(o)?.isGroup||l[Math.max(0,n[o]??0)].push(o);return l}function td(e){let t=ti(e),n=ta(t),r=[],o=tl(e);for(;n.length;){let e=n.shift();for(let l of(r.push(e),o.get(e)??[]))if(t.set(l,(t.get(l)??0)-1),(t.get(l)??0)===0){let e=0;for(;e<n.length&&n[e]<l;)e++;n.splice(e,0,l)}}return r.length===e.nodes.length?r:null}function tu(e){let t=new Map,n=0;for(let r of e)t.set(r,n),n++;return t}function th(e){let t=Array(e.length),n=r((r,o)=>{if(o-r<=1)return 0;let l=r+o>>1,i=n(r,l)+n(l,o),a=r,s=l,f=r;for(;a<l||s<o;)s>=o||a<l&&e[a]<=e[s]?t[f++]=e[a++]:(t[f++]=e[s++],i+=l-a);for(let n=r;n<o;n++)e[n]=t[n];return i},"count");return n(0,e.length)}function tg(e){let t=tn(e),n=new Map;for(let e of t.nodes)n.set(e,[]);for(let e of t.edges)n.get(e.src).push(e);for(let e of n.values())e.sort((e,t)=>e.dst===t.dst?e.id.localeCompare(t.id):e.dst.localeCompare(t.dst));let o=Object.create(null);for(let e of t.nodes)o[e]=0;let l=[],i=r(e=>{for(let t of(o[e]=1,n.get(e)??[])){let e=t.dst;0===o[e]?i(e):1===o[e]&&l.push(t)}o[e]=2},"dfs");for(let e of[...t.nodes].sort((e,t)=>e.localeCompare(t)))0===o[e]&&i(e);let a=new Set(l.map(e=>`${e.id}:${e.src}->${e.dst}`)),s=t.edges.map(e=>a.has(`${e.id}:${e.src}->${e.dst}`)?{id:e.id,src:e.dst,dst:e.src,weight:e.weight,ref:e.ref}:e);return{acyclic:{nodes:[...t.nodes],edges:s,layout:t.layout,nodeById:new Map(t.nodeById)},reversed:l}}function tc(e){let t=new Map,n=r(r=>{if(t.has(r))return t.get(r);let o=e.nodeById.get(r);if(!o)return t.set(r,null),null;let l=o.parentId;if(!l)return t.set(r,null),null;let i=n(l)??l;return t.set(r,i),i},"resolve");for(let t of e.nodes)n(t);return t}function tp(e){let t=tc(e);return e=>t.get(e)??null}function tx(e){let t=[];for(let n of e.layout.nodes??[])n.isGroup&&!n.parentId&&t.push(n.id);return[...new Set(t)].reverse()}function tm(e,t){let n=tx(e);if(!t||0===t.length)return n;let r=new Set(n),o=new Set,l=[];for(let e of t)!r.has(e)||o.has(e)||(o.add(e),l.push(e));for(let e of n)o.has(e)||l.push(e);return l}r(e8,"pairKey"),r(e4,"straightenCollinearSiblingDetours"),r(e9,"nudgeSharedInteriorSubpaths"),r(e7,"segmentsIntersect"),r(te,"validateSwimlanesLayout"),r(tt,"postProcessSwimlaneLayout"),r(tn,"normalizeGraph"),r(tr,"incoming"),r(to,"buildSuccessorMap"),r(tl,"buildSortedSuccessorMap"),r(ti,"buildInDegreeMap"),r(ta,"sortedZeroInDegreeNodes"),r(ts,"buildPredecessorSuccessorMaps"),r(tf,"buildLayersFromRanks"),r(td,"topoSortIfAcyclic"),r(tu,"buildLayerIndex"),r(th,"countInversions"),r(tg,"removeCycles_DFS"),r(tc,"buildTopLaneMap"),r(tp,"createTopLaneResolver"),r(tx,"buildTopLaneOrder"),r(tm,"resolveTopLaneOrder");function ty(e,t){let n=tn(e),o=t?.laneOf??(()=>null),l=t?.rankHint,{preds:i}=ts(n);for(let e of i.values())e.sort((e,t)=>e.localeCompare(t));let a=td(n)??[...n.nodes].sort((e,t)=>e.localeCompare(t)),s=new Map;for(let[e,t]of a.entries())s.set(t,e);let f=new Map,d=new Map;for(let e of n.nodes)d.set(e,[]);for(let e of a){let t=(i.get(e)??[]).filter(e=>f.has(e));if(t.length>0){let n=tb(e,t,{laneOf:o,rankHint:l,topoIndex:s});f.set(e,n),d.get(n).push(e)}else f.has(e)||f.set(e,null)}for(let e of n.nodes)f.has(e)||f.set(e,null);let u=new Set;for(let e of n.nodes)(f.get(e)??null)===null&&u.add(e);let h=[...u].sort((e,t)=>{let n=s.get(e)??0,r=s.get(t)??0;return n===r?e.localeCompare(t):n-r}),g=tM(n),c=new Map;for(let[e,t]of g.entries())c.set(e,[...t].sort((e,t)=>e.localeCompare(t)));let p=tI(c),x=tS(c),m=new Map;for(let e of n.nodes)m.set(e,[]);for(let e of x)for(let t of e.nodes){let n=m.get(t);n?n.push(e.id):m.set(t,[e.id])}let y=[],b=[],M=new Set,I=r(e=>{if(!M.has(e)){for(let t of(M.add(e),y.push(e),d.get(e)??[]))I(t);b.push(e)}},"walk");for(let e of h)I(e);for(let e of a)I(e);return{parent:f,children:d,roots:h,componentOf:p,blocks:x,nodeBlocks:m,adjacency:c,preorder:y,postorder:b,topologicalOrder:a}}function tb(e,t,n){let r=n.laneOf(e);return[...t].sort((e,t)=>{let o=n.laneOf(e),l=n.laneOf(t),i=null!=o&&o===r;if(i!==(null!=l&&l===r))return i?-1:1;let a=n.rankHint?.[e],s=n.rankHint?.[t];if(null!=a&&null!=s&&a!==s)return s-a;let f=n.topoIndex.get(e)??0,d=n.topoIndex.get(t)??0;return f!==d?f-d:e.localeCompare(t)})[0]}function tM(e){let t=new Map;for(let n of e.nodes)t.set(n,new Set);for(let n of e.edges)t.get(n.src).add(n.dst),t.get(n.dst).add(n.src);return t}function tI(e){let t=new Map,n=0;for(let r of e.keys()){if(t.has(r))continue;let o=[r];for(;o.length>0;){let r=o.pop();if(!t.has(r))for(let l of(t.set(r,n),e.get(r)??[]))t.has(l)||o.push(l)}n++}return t}function tS(e){let t=new Map,n=new Map,o=[],l=[],i=0,a=r((r,s)=>{for(let f of(t.set(r,++i),n.set(r,i),e.get(r)??[]))f!==s&&(t.has(f)?(t.get(f)??0)<(t.get(r)??0)&&(o.push([r,f]),n.set(r,Math.min(n.get(r)??i,t.get(f)??i))):(o.push([r,f]),a(f,r),n.set(r,Math.min(n.get(r)??i,n.get(f)??i)),(n.get(f)??0)>=(t.get(r)??0)&&l.push(tw(r,f,o,l.length))))},"visit");for(let n of e.keys())t.has(n)||a(n,null);return l}function tw(e,t,n,r){let o=[],l=new Set;for(;n.length>0;){let r=n.pop();if(o.push(r),l.add(r[0]),l.add(r[1]),r[0]===e&&r[1]===t||r[0]===t&&r[1]===e)break}return{id:r,edges:o,nodes:[...l]}}function tv(e,t,n){let o=[...e.nodes],l=new Map;for(let[e,t]of o.entries())l.set(t,e);let i=o.length,a=Array(i).fill(-1),s=Array(i).fill(0),f=[],d=new Set;for(let e of o){let t=n.parent.get(e)??null,r=l.get(e);null!=r&&null==t&&(a[r]=-1,s[r]=0,d.has(e)||(d.add(e),f.push(e)))}for(;f.length>0;){let e=f.shift(),t=l.get(e);if(null!=t)for(let r of n.children.get(e)??[]){if(d.has(r))continue;let e=l.get(r);null!=e&&(a[e]=t,s[e]=s[t]+1,d.add(r),f.push(r))}}for(let e of o){if(d.has(e))continue;let t=l.get(e);null!=t&&(a[t]=-1,s[t]=0,d.add(e))}let u=Math.max(1,Math.ceil(Math.log2(Math.max(1,i)))+1),h=Array.from({length:u},()=>Array(i).fill(-1));for(let e=0;e<i;e++)h[0][e]=a[e];for(let e=1;e<u;e++)for(let t=0;t<i;t++){let n=h[e-1][t];h[e][t]=-1===n?-1:h[e-1][n]}let g=r((e,t)=>{if(-1===e||-1===t)return -1;s[e]<s[t]&&([e,t]=[t,e]);let n=s[e]-s[t];for(let t=0;t<u;t++)if(n>>t&1&&-1===(e=h[t][e]))return -1;if(e===t)return e;for(let n=u-1;n>=0;n--){let r=h[n][e],o=h[n][t];-1!==r&&-1!==o&&r!==o&&(e=r,t=o)}return h[0][e]},"lcaIndex"),c=Array.from({length:i},()=>new Map);for(let n of e.edges){let e=n.src,r=n.dst,o=t[e],i=t[r];if(null==o||null==i||(o>i&&([e,r]=[r,e],[o,i]=[i,o]),null==o||null==i||o===i))continue;let a=l.get(e),s=l.get(r);if(null==a||null==s)continue;let f=g(a,s);if(-1===f)continue;let d=c[f];for(let e=o;e<i;e++)d.set(e,(d.get(e)??0)+1)}let p=new Map,x=r((e,t)=>{if(0!==t.size)for(let[n,r]of t)e.set(n,(e.get(n)??0)+r)},"mergeInto"),m=new Set,y=r(e=>{let r=l.get(e);m.add(e);let o=null==r?void 0:c[r],i=o?new Map(o):new Map;for(let r of n.children.get(e)??[]){let n=y(r),o=t[e];if(null!=o){let l=p.get(e);l||(l=new Map,p.set(e,l));let i=n.get(o)??0,a=t[r];null!=a&&a>o&&(i+=1),l.set(r,i)}x(i,n)}return i},"dfs");for(let e of n.roots)m.has(e)||y(e);for(let e of o)m.has(e)||y(e);return p}function tC(e,t,n){let o=new Map,l=r(e=>{let r=n[e]??0,i=[...t.get(e)??[]];for(let e of(i.sort(tL(n)),i)){l(e);let t=o.get(e);null!=t&&(r=Math.min(r,t))}o.set(e,r)},"annotate");for(let t of e)l(t);return o}function tL(e){return(t,n)=>{let r=e[t]??0,o=e[n]??0;return r===o?t.localeCompare(n):r-o}}function tk(e,t,n,o){let l=0;for(let e of t){let t=n[e]??0;t>l&&(l=t)}let i=Array.from({length:l+1},()=>[]),a=new Set,s=r(e=>{if(a.has(e))return;a.add(e);let t=n[e]??0;for(let n of(i[t]||(i[t]=[]),i[t].push(e),o(e)))s(n)},"emit");for(let t of e)s(t);for(let e of t)if(!a.has(e)){let t=n[e]??0;i[t]||(i[t]=[]),i[t].push(e),a.add(e)}return i}function t$(e){let t=[];for(let n of e){let e=new Set,r=[];for(let t of n)e.has(t)||(e.add(t),r.push(t));t.push(r)}return t}function tR(e,t,n,r){return o=>{let l=e.get(o)??[];if(0===l.length)return[];let i=t[o]??0,a=[],s=[],f=n.get(o);for(let e of l){let t=r.get(e)??i;t>i?a.push({child:e,min:t}):s.push(e)}return a.sort((e,t)=>e.min===t.min?e.child.localeCompare(t.child):e.min-t.min),s.sort((e,t)=>{let n=f?.get(e)??0,o=f?.get(t)??0;if(n!==o)return n-o;let l=r.get(e)??i,a=r.get(t)??i;return l!==a?l-a:e.localeCompare(t)}),[...a.map(e=>e.child),...s]}}function tO(e,t,n){let r=ty(e,{rankHint:t,laneOf:n}),{children:o,roots:l}=r;for(let t of e.nodes)o.has(t)||o.set(t,[]);let i=tv(e,t,r),a=[...l].sort(tL(t)),s=tC(a,o,t),f=tR(o,t,i,s),d=tk(a,e.nodes,t,f);return t$(d)}function tT(e,t,n){let r=new Set(e),o=new Set(t),l=tu(t),i=[];for(let e of n)r.has(e.src)&&o.has(e.dst)&&i.push(l.get(e.dst));return th(i)}function tF(e,t,n){let r=[];for(let e of t){let t=n[e.src],o=n[e.dst];if(null==t||null==o||t===o)continue;let l=e.src,i=e.dst,a=t,s=o;t>o&&(l=e.dst,i=e.src,a=o,s=t);for(let t=a;t<s;t++)r.push({id:`${e.id}@${t}`,src:l,dst:i,ref:e.ref})}let o=0;for(let t=0;t+1<e.length;t++)o+=tT(e[t],e[t+1],r);return o}function tE(e,t){let n={...t},{preds:r}=ts(e),o=tp(e),l=tF(tO(e,n,o),e.edges,n);for(let t=0;t<4;t++){let t=!1;for(let i of[...e.nodes].sort((e,t)=>(n[t]??0)-(n[e]??0))){let a=n[i]??0;if(0===a)continue;let s=0;for(let e of r.get(i)??[])s=Math.max(s,(n[e]??0)+1);if(s>=a)continue;n[i]=s;let f=tF(tO(e,n,o),e.edges,n);f<l?(l=f,t=!0):n[i]=a}if(!t)break}return n}function tB(e,t){let n=tp(e);for(let r of[...e.nodes].sort((e,n)=>(t[e]??0)-(t[n]??0)||e.localeCompare(n))){let o=n(r);if(!o)continue;let l=e.edges.filter(e=>e.src===r);if(0===l.length)continue;let i=!1,a=0;for(let e of l){let t=n(e.dst);null==t||t===o?i=!0:a++}if(0===a||i)continue;let s=0,f=!1;for(let t of e.edges){if(t.dst!==r)continue;let e=n(t.src);e&&(e===o?f=!0:s++)}if(s>0||!f)continue;let d=t[r]??0,u=d+a,h=0;for(let n of e.edges)n.dst===r&&(h=Math.max(h,(t[n.src]??0)+1));let g=Math.max(d,h,u);g!==d&&(t[r]=g)}}function tN(e,t){let n=tn(e),r=td(n)??[...n.nodes].sort(),o=t?.compactSingleInput??!1,l=tp(n),i=Object.create(null);for(let e of r){let r=tr(n,e),a=t?.ignoreCrossLaneEdges?r.filter(t=>{let n=l(t.src),r=l(e);return!n||!r||n===r}):r;if(0===a.length)i[e]=0;else if(o&&1===a.length){let t=a[0].src;l(t)!==l(e)?i[e]=i[t]??0:i[e]=(i[t]??0)+1}else{let t=-1/0;for(let e of a)t=Math.max(t,(i[e.src]??0)+1);i[e]=t===-1/0?0:t}}return t?.optimizeRanksByCrossings&&(i=tE(n,i)),t?.ignoreCrossLaneEdges&&tB(n,i),{layers:tO(n,i,l),rankOf:i,dummy:new Set}}function tX(e,t){let n=tn(e),o={...tN(n,{compactSingleInput:t?.compactSingleInput,ignoreCrossLaneEdges:t?.ignoreCrossLaneEdges,optimizeRanksByCrossings:t?.optimizeRanksByCrossings}).rankOf},l=tp(n),{preds:i,succs:a}=ts(n,e=>{if(t?.ignoreCrossLaneEdges){let t=l(e.src),n=l(e.dst);if(t&&n&&t!==n)return!1}return!0}),s=td(n)??[...n.nodes],f=[...s].reverse(),d=r((e,t)=>{let n=0;for(let t of i.get(e)??[])n=Math.max(n,(o[t]??0)+1);let r=1/0,l=a.get(e)??[];return l.length>0&&(r=Math.min(...l.map(e=>(o[e]??0)-1))),Number.isFinite(r)||(r=Math.max(n,t)),Math.min(Math.max(t,n),r)},"clampFeasible"),u=r(e=>{let t=!1;for(let n of e){let e=i.get(n)??[],r=a.get(n)??[];if(0===e.length&&0===r.length)continue;let l=Math.round(((e.length>0?e.reduce((e,t)=>e+(o[t]??0)+1,0)/e.length:o[n]??0)+(r.length>0?r.reduce((e,t)=>e+(o[t]??0)-1,0)/r.length:o[n]??0))/2),s=d(n,l);s!==o[n]&&(o[n]=s,t=!0)}return t},"relaxOrder");for(let e=0;e<8;e++){let e=u(s),t=u(f);if(!e&&!t)break}for(let e of s){let t=0;for(let n of i.get(e)??[])t=Math.max(t,(o[n]??0)+1);(o[e]??0)<t&&(o[e]=t)}for(let e of f){let t=a.get(e)??[];if(t.length>0){let n=Math.min(...t.map(e=>(o[e]??0)-1));(o[e]??0)>n&&(o[e]=n)}}return{layers:tf(n,s,o),rankOf:o,dummy:new Set}}function tz(e){let t=ti(e),n=tl(e),r=ta(t),o=[];for(;r.length>0;){let e=[];for(let l of r)for(let r of(o.push(l),n.get(l)??[]))t.set(r,(t.get(r)??0)-1),(t.get(r)??0)===0&&e.push(r);r=e.sort((e,t)=>e.localeCompare(t))}return o.length===e.nodes.length?o:null}function tY(e,t){let n=tn(e),o=t?.direction==="LR"?tz(n)??[...n.nodes].sort():td(n)??[...n.nodes].sort(),l=tp(n),i=r(e=>l(e)??e,"laneOf"),a=Object.create(null),s=new Map,f=r((e,n)=>t?.ignoreCrossLaneEdges??!0?+(i(e)===i(n)):1,"edgeWeight");for(let e of o){let t=n.nodeById.get(e);if(t?.isGroup)continue;let r=tr(n,e),o=0;if(r.length>0)for(let t of r){let n=t.src;o=Math.max(o,(a[n]??0)+f(n,e))}let l=i(e),d=Math.max(o,s.get(l)??0);a[e]=d,s.set(l,d+1)}return{layers:tf(n,o,a,{skipGroups:!0}),rankOf:a,dummy:new Set}}function tA(e,t){let n=tn(t),{rankOf:o}=e,l=e.layers.map(e=>[...e]),i=new Set(e.dummy?[...e.dummy]:[]),a=0,s=new Map(n.nodeById),f=r(e=>{let t=`placeholder-${a++}`;for(s.set(t,{id:t,isGroup:!1,isDummy:!0,width:0,height:0}),i.add(t);l.length<=e;)l.push([]);return l[e].push(t),o[t]=e,t},"addDummyAt"),d=[...n.edges].sort((e,t)=>e.id===t.id?e.src===t.src?e.dst.localeCompare(t.dst):e.src.localeCompare(t.src):e.id.localeCompare(t.id)),u=[];for(let e of d){let t=o[e.src]??0,n=o[e.dst]??0;if(n-t<=1){u.push(e);continue}let r=e.src;for(let o=t+1,l=0;o<n;o++,l++){let t=f(o);u.push({id:`${e.id}#${l}`,src:r,dst:t,weight:e.weight,ref:e.ref}),r=t}let l=n-t-2;u.push({id:`${e.id}#${Math.max(l+1,0)}`,src:r,dst:e.dst,weight:e.weight,ref:e.ref})}let h={nodes:[...n.nodes,...[...i].filter(e=>!n.nodes.includes(e))],edges:u,layout:n.layout,nodeById:s};return{layering:{layers:l,rankOf:o,dummy:i},graphWithDummies:h}}function tG(e){let t=e.length;if(0===t)return 1/0;let n=[...e].sort((e,t)=>e-t);return t%2==1?n[(t-1)/2]:.5*(n[t/2-1]+n[t/2])}function tP(e){return 0===e.length?1/0:e.reduce((e,t)=>e+t,0)/e.length}function tD(e,t,n,r){let o=new Map;for(let t of e)o.set(t,[]);for(let e of n)"down"===r?t.has(e.src)&&o.has(e.dst)&&o.get(e.dst).push(t.get(e.src)):t.has(e.dst)&&o.has(e.src)&&o.get(e.src).push(t.get(e.dst));return o}function tH(e,t,n){let r=n.get(e)??0,o=n.get(t)??0;return r!==o?r-o:e.localeCompare(t)}function tj(e,t,n){let r=new Set(e),o=new Set(t),l=tu(e),i=tu(t),a=[];for(let e of n)r.has(e.src)&&o.has(e.dst)&&a.push({u:l.get(e.src),v:i.get(e.dst)});return a.sort((e,t)=>e.u===t.u?e.v-t.v:e.u-t.u),th(a.map(e=>e.v))}function t_(e,t,n){return[...e].sort((e,r)=>{let o=tG(t.get(e)??[]),l=tG(t.get(r)??[]);return o===l?tH(e,r,n):isFinite(o)?isFinite(l)?o-l:-1:1})}function tV(e,t,n,r,o,l){let i=tu(e),a=tu(t),s=tD(t,i,n,r);if(!o||!l||0===l.length)return t_(t,s,a);let f=new Map;for(let e of t){let t=o(e),n=f.get(t)??[];n.push(e),f.set(t,n)}let d=[];for(let e of l){let t=f.get(e);if(!t||0===t.length)continue;let n=t_(t,s,a);d.push(...n)}let u=f.get(null);if(u&&u.length>0)for(let e of t_(u,s,a)){let t=tP(s.get(e)??[]),n=d.length;if(isFinite(t)){for(let[e,r]of d.entries())if(t<tP(s.get(r)??[])){n=e;break}}d.splice(n,0,e)}return d}function tW(e,t,n,o,l){let i=[...t],a=new Set(e),s=new Set(t),f=o?new Set(o):null,d=n.filter(e=>a.has(e.src)&&s.has(e.dst)),u=f?n.filter(e=>s.has(e.src)&&f.has(e.dst)):void 0,h=r(t=>{let n=tj(e,t,d);return u&&o&&(n+=tj(t,o,u)),n},"crossingScore"),g=l?new Map:null;if(l&&g)for(let e of t)g.set(e,l(e));let c=!0,p=h(i);for(;c;){c=!1;for(let e=0;e+1<i.length;e++){if(g&&g.get(i[e])!==g.get(i[e+1]))continue;let t=p;[i[e],i[e+1]]=[i[e+1],i[e]];let n=h(i);n<t?(p=n,c=!0):[i[e],i[e+1]]=[i[e+1],i[e]]}}return i}function tK(e,t,n){let r=e.layers.map(e=>[...e]),o=t.edges,l=tp(t),i=tm(t,n?.laneOrder);for(let e=0;e<3;e++){for(let e=1;e<r.length;e++)r[e]=tV(r[e-1],r[e],o,"down",l,i),r[e]=tW(r[e-1],r[e],o,r[e+1],l);for(let e=r.length-2;e>=0;e--)r[e]=tV(r[e+1],r[e],o,"up",l,i),r[e]=tW(r[e+1],r[e],o,r[e-1],l)}return{layers:r}}function tJ(e,t,n){let o=n?.layerGap??100,l=n?.nodeGap??40,i=n?.laneGap??2*l,a=n?.direction??"TB",s=e.layers,f=Object.create(null),d=Object.create(null),u=r(e=>t.nodeById.get(e),"getNode"),h=r(e=>u(e)?.width??0,"getWidth"),g=r(e=>u(e)?.height??0,"getHeight"),c=tp(t),p=tm(t,n?.laneOrder),x=s.map(e=>e.reduce((e,t)=>Math.max(e,g(t)),0)),m=[];if("LR"===a||"RL"===a)for(let e=0;e+1<s.length;e++){let t=Math.max(0,(s[e].reduce((e,t)=>Math.max(e,h(t)),0)+s[e+1].reduce((e,t)=>Math.max(e,h(t)),0))/2-(x[e]/2+x[e+1]/2)-o);m.push(t)}let y=new Set;for(let e of s)for(let t of e)y.add(c(t));let b=y.has(null),M=p.filter(e=>y.has(e)),I=[...b?[null]:[],...M],S=Object.create(null);for(let e of M)S[e]=0;for(let e of(b&&(S.null=0),s)){let t=Object.create(null),n=[];for(let r of e){let e=c(r);null===e?n.push(r):(t[e]||=[]).push(r)}for(let[e,n]of Object.entries(t)){let t=n.reduce((e,t)=>e+h(t),0)+l*Math.max(0,n.length-1);S[e]=Math.max(S[e]??0,t)}if(b&&n.length){let e=n.reduce((e,t)=>e+h(t),0)+l*Math.max(0,n.length-1);S.null=Math.max(S.null??0,e)}}let w=new Map;{let e=I.map(e=>(null===e?S.null:S[e])??0),t=-(e.reduce((e,t)=>e+t,0)+i*Math.max(0,I.length-1))/2;for(let n=0;n<I.length;n++){let r=I[n],o=e[n]??0,l=t+o/2;w.set(r,l),t+=o,n<I.length-1&&(t+=i)}}let v=0;for(let[e,t]of s.entries()){let n=x[e]??0,r=new Map;for(let e of t){let t=c(e),n=r.get(t)??[];n.push(e),r.set(t,n)}for(let e of I){let t=r.get(e)??[];if(0===t.length)continue;let o=w.get(e);if(1===t.length){let e=t[0];f[e]=o,d[e]=v+n/2}else{let e=t.map(e=>h(e)),r=o-(e.reduce((e,t)=>e+t,0)+l*(t.length-1))/2;for(let[o,i]of t.entries()){let t=e[o];f[i]=r+t/2,d[i]=v+n/2,r+=t+l}}}v+=n+o+(m[e]??0)}let C=new Map;for(let e of t.edges){let t=e.ref.id;C.has(t)||C.set(t,[]),C.get(t).push(e)}for(let[,e]of C){if(0===e.length)continue;let n=e[0].ref,r=n.start,o=n.end;if(null==r||null==o)continue;let l=Math.round(((f[r]??0)+(f[o]??0))/2),i=new Set;for(let t of e)i.add(t.src),i.add(t.dst);for(let e of i){if(e===r||e===o)continue;let n=t.nodeById.get(e);n?.isDummy&&(f[e]=l)}}return{x:f,y:d}}function tq(e){let t=0x811c9dc5;for(let n=0;n<e.length;n++)t^=e.charCodeAt(n),t=Math.imul(t,0x1000193);return t>>>0}function tU(e){let t=e>>>0;return()=>{let e=t+=0x6d2b79f5;return e=Math.imul(e^e>>>15,1|e),(((e^=e+Math.imul(e^e>>>7,61|e))^e>>>14)>>>0)/0x100000000}}function tQ(e,t){let n=[...e],r=tU(t);for(let e=n.length-1;e>0;e--){let t=Math.floor(r()*(e+1));[n[e],n[t]]=[n[t],n[e]]}return n}function tZ(e,t){let n=0;for(let[r,o]of e.entries())n+=Math.abs(r-(t.get(o)??r));return n}function t0(e,t){let n=new Map;for(let[t,r]of e.entries())n.set(r,t);let r=0;for(let{a:e,b:o,weight:l}of t){let t=n.get(e),i=n.get(o);null!=t&&null!=i&&(r+=l*Math.abs(t-i))}return r}function t1(e){let t=tx(e);if(t.length<2)return[];let n=new Map(t.map((e,t)=>[e,t])),r=tp(e),o=new Map;for(let t of e.layout.edges??[]){if(t.isLayoutOnly)continue;let l="string"==typeof t.start?t.start:void 0,i="string"==typeof t.end?t.end:void 0;if(!l||!i||!e.nodeById.has(l)||!e.nodeById.has(i))continue;let a=r(l),s=r(i);if(!a||!s||a===s)continue;let f=n.get(a),d=n.get(s);if(null==f||null==d)continue;let[u,h]=f<=d?[a,s]:[s,a],g=`${u}\0${h}`,c=o.get(g);c?c.weight++:o.set(g,{a:u,b:h,weight:1})}return[...o.values()]}function t2(e,t,n){let r=[...e],o=t0(r,t),l=!0,i=0,a=Math.max(1,r.length);for(;l&&i<a;){l=!1,i++;for(let e=0;e+1<r.length;e++){[r[e],r[e+1]]=[r[e+1],r[e]];let n=t0(r,t);n<o?(o=n,l=!0):[r[e],r[e+1]]=[r[e+1],r[e]]}}return{order:r,cost:o,sourceDistance:tZ(r,n)}}function t6(e,t){return e.cost!==t.cost?e.cost<t.cost:e.sourceDistance<t.sourceDistance}function t5(e,t,n){let r=[...t].sort((e,t)=>e.a===t.a?e.b.localeCompare(t.b):e.a.localeCompare(t.a)).map(({a:e,b:t,weight:n})=>`${e}:${t}:${n}`).join("|");return tq(`${e.join("|")}#${r}#${n}`)}function t3(e,t={}){let n=tx(e);if(n.length<2)return n;let r=t1(e);if(0===r.length)return n;let o=new Map(n.map((e,t)=>[e,t])),l=t2(n,r,o),i=Math.max(0,t.restarts??8);for(let e=0;e<i;e++){let t=t5(n,r,e),i=t2(tQ(n,t),r,o);t6(i,l)&&(l=i)}return l.order}function t8(e,t){let n=t?.ignoreCrossLaneEdges??!0,r=t?.optimizeRanksByCrossings??!0,o=tn(e),l=t?.automaticLaneOrdering?t3(o,{restarts:8}):void 0,i=tg(o),a=i.acyclic,{layering:s,graphWithDummies:f}=tA(n?tY(a,{compactSingleInput:t?.compactSingleInput??!0,ignoreCrossLaneEdges:!0,direction:t?.direction}):tX(a,{compactSingleInput:t?.compactSingleInput??!0,ignoreCrossLaneEdges:!1,optimizeRanksByCrossings:r}),a),d=tK(s,f,{laneOrder:l}),u=tJ(d,f,{layerGap:t?.layerGap,nodeGap:t?.nodeGap,direction:t?.direction,laneOrder:l});return{acyclic:a,reversed:i.reversed,layering:s,ordered:d,coordinates:u}}function t4(e,t,n){let r=e.x??0,o=e.y??0,l=t.x-r,i=t.y-o,a=Math.abs(l),s=Math.abs(i);return a<1e-6&&s<1e-6?n:s>1e-6&&3*s>=a?i>0?"bottom":"top":a>1e-6?l>0?"right":"left":n}function t9(e,t){return 1e-6>Math.abs(e.to-t.from)||1e-6>Math.abs(e.to-t.to)?e.to:e.from}function t7(e,t){return"vertical"===e.orient?{x:e.coord,y:t}:{x:t,y:e.coord}}function ne(e,t){let n=e.nodes??[],o=e.edges??[],l=[];for(let e of o)e.isLayoutOnly||l.push({...e,__originalEdge:e});let i=new Map,a=new Map,s=[],f="LR"===t;for(let e of n)i.set(e.id,e);for(let e of n.filter(e=>e.isGroup&&!e.parentId)){let t={id:e.id},o=r(e=>{a.set(e.id,t),n.filter(t=>t.parentId===e.id).forEach(o)},"assignLane");o(e)}let d=n.filter(e=>!e.isGroup&&!e.isEdgeLabel).map(e=>{let t=e.width??10,n=e.height??10,r=e.x??0,o=e.y??0;return{nodeId:e.id,minX:r-t/2-8,maxX:r+t/2+8,minY:o-n/2-8,maxY:o+n/2+8,visualXHalfExtent:f?n/2+8:t/2+8}}),u=r((e,t,n,r)=>{let o=s.find(n=>n.orientation===e&&1>Math.abs(n.coord-t));return o||(o={id:`pipe-${e}-${t.toFixed(0)}`,orientation:e,coord:t,spanMin:n,spanMax:r,tracks:[]},s.push(o)),o.spanMin=Math.min(o.spanMin,n),o.spanMax=Math.max(o.spanMax,r),o},"getOrAddPipe"),h=r((e,t)=>{let n=e.width??10,r=e.height??10,o=e.x??0,l=e.y??0;switch(t){case"top":return{x:o,y:l-r/2};case"bottom":return{x:o,y:l+r/2};case"left":return{x:o-n/2,y:l};case"right":return{x:o+n/2,y:l}}},"portForSide"),g=r((e,t,n)=>h(e,t4(e,t,n?"bottom":"top")),"getOrthogonalPort"),c=[],p=[],x=new Set,m=r((e,t,n)=>{if(0===c.length)return 0;let r=1e-6>Math.abs(t.y-n.y),o=1e-6>Math.abs(t.x-n.x);if(!r&&!o)return 0;let l=0;if(r){let r=t.y,o=Math.min(t.x,n.x)-1e-6,i=Math.max(t.x,n.x)+1e-6;if(i<=o)return 0;for(let t of c)t.edgeIndex!==e&&"vertical"===t.orientation&&!(t.pipe.coord<o)&&!(t.pipe.coord>i)&&t.from-1e-6<=r&&t.to+1e-6>=r&&(l+=1e3)}else if(o){let r=t.x,o=Math.min(t.y,n.y)-1e-6,i=Math.max(t.y,n.y)+1e-6;if(i<=o)return 0;for(let t of c)t.edgeIndex!==e&&"horizontal"===t.orientation&&!(t.pipe.coord<o)&&!(t.pipe.coord>i)&&t.from-1e-6<=r&&t.to+1e-6>=r&&(l+=1e3)}return l},"crossingPenalty"),y=l.map((e,t)=>{if(!e.start||!e.end)return{idx:t,crossLane:0,dx:0,dy:0};let n=i.get(e.start),r=i.get(e.end),o=a.get(e.start),l=a.get(e.end),s=o&&l&&o.id!==l.id?1:0;return{idx:t,crossLane:s,dx:n&&r?Math.abs((r.x??0)-(n.x??0)):0,dy:n&&r?Math.abs((r.y??0)-(n.y??0)):0}}).sort((e,t)=>{if(e.crossLane!==t.crossLane)return t.crossLane-e.crossLane;let n=e.dx+e.dy,r=t.dx+t.dy;return Math.abs(n-r)>1?n-r:e.idx-t.idx}).map(e=>e.idx),b=r((e,t,n,r)=>{let o=Math.min(e.x,t.x),l=Math.max(e.x,t.x),i=Math.min(e.y,t.y),a=Math.max(e.y,t.y);return!!d.find(s=>(!n||s.nodeId!==n)&&(!r||s.nodeId!==r)&&(Math.abs(e.x-t.x)>1e-6?s.minY<e.y&&s.maxY>e.y&&s.maxX>o&&s.minX<l:s.minX<e.x&&s.maxX>e.x&&s.maxY>i&&s.minY<a))},"isSegmentBlocked"),M=new Map,I=new Map;for(let e of l)e.start&&e.end&&e.start!==e.end&&(I.set(e.start,(I.get(e.start)??0)+1),I.set(e.end,(I.get(e.end)??0)+1));let S=r((e,t)=>t4(e,t,"bottom"),"determineSide"),w=new Map;for(let[e,t]of l.entries()){if(!t.start||!t.end||t.start===t.end||t.points&&t.points.length>0)continue;let n=i.get(t.start),r=i.get(t.end);if(!n||!r)continue;let o=(r.x??0)-(n.x??0),l=(r.y??0)-(n.y??0);w.set(e,{edgeIdx:e,srcId:t.start,dstId:t.end,srcSide:S(n,{x:r.x??0,y:r.y??0}),dstSide:S(r,{x:n.x??0,y:n.y??0}),absDx:Math.abs(o),absDy:Math.abs(l),dxSign:Math.sign(o),dySign:Math.sign(l)})}let v=r(e=>"top"===e.srcSide||"bottom"===e.srcSide?0===e.absDx?1/0:e.absDy/e.absDx:0===e.absDy?1/0:e.absDx/e.absDy,"preferenceStrength"),C=r(e=>"top"===e.srcSide||"bottom"===e.srcSide?e.dxSign>=0?"right":"left":e.dySign>=0?"bottom":"top","secondarySide"),L=new Map;for(let e of w.values()){let t=`${e.srcId}:${e.srcSide}`;L.has(t)||L.set(t,[]),L.get(t).push(e)}let k=new Map,$=r((e,t)=>`${e}:${t}`,"loadKey");for(let e of w.values())k.set($(e.srcId,e.srcSide),(k.get($(e.srcId,e.srcSide))??0)+1),k.set($(e.dstId,e.dstSide),(k.get($(e.dstId,e.dstSide))??0)+1);for(let e of L.values())if(!(e.length<2)){e.sort((e,t)=>{let n=v(e),r=v(t);return Math.abs(n-r)>1e-9?r-n:e.edgeIdx-t.edgeIdx});for(let t=1;t<e.length;t++){let n=e[t],r=C(n),o=k.get($(n.srcId,n.srcSide))??0,l=k.get($(n.srcId,r))??0;l>=o||(k.set($(n.srcId,n.srcSide),o-1),k.set($(n.srcId,r),l+1),n.srcSide=r)}}let R=r(e=>{let t=e?.shape;return"question"===t||"diamond"===t},"isDiamondNode"),O=new Map;for(let e of w.values())O.has(e.dstId)||O.set(e.dstId,new Set),O.get(e.dstId).add(e.dstSide);for(let e of w.values()){if(!R(i.get(e.srcId)))continue;let t=O.get(e.srcId);if(!t?.has(e.srcSide))continue;let n=C(e);if(t.has(n)||(k.get($(e.srcId,n))??0)>0)continue;let r=k.get($(e.srcId,e.srcSide))??0;k.set($(e.srcId,e.srcSide),Math.max(0,r-1)),k.set($(e.srcId,n),1),e.srcSide=n}for(let e of w.values()){let{edgeIdx:t,srcId:n,dstId:r,srcSide:o,dstSide:l}=e,a=i.get(n),s=i.get(r),f=`${n}:${o}:src`,d="top"===o||"bottom"===o?s.x??0:s.y??0;M.has(f)||M.set(f,[]),M.get(f).push({edgeIdx:t,oppositeCoord:d});let u=`${r}:${l}:dst`,h="top"===l||"bottom"===l?a.x??0:a.y??0;M.has(u)||M.set(u,[]),M.get(u).push({edgeIdx:t,oppositeCoord:h})}let T=new Map;for(let[e,t]of M){if(t.length<2)continue;t.sort((e,t)=>e.oppositeCoord-t.oppositeCoord);let n=e.split(":"),r=n.slice(0,-2).join(":"),o=n[n.length-2],l=n[n.length-1],a=i.get(r);if(!a)continue;let s="left"===o||"right"===o?a.height??10:a.width??10,f=a.shape,d=Math.min(20,Math.max(8,("question"===f||"diamond"===f?.3*s:s)/(t.length+1))),u=-(d*(t.length-1))/2;for(let[e,n]of t.entries()){let t=u+e*d,r=`${n.edgeIdx}:${l}`;T.set(r,t)}}let F=r(e=>!!l[e]?.labelNodeId,"edgeHasLabelNode"),E=r((e,t)=>!!e&&((M.get(`${e}:${t}:src`)??[]).some(({edgeIdx:e})=>F(e))||(M.get(`${e}:${t}:dst`)??[]).some(({edgeIdx:e})=>F(e))),"faceHasLabelNode"),B=r((e,t,n)=>"top"===t||"bottom"===t?{x:e.x+n,y:e.y}:{x:e.x,y:e.y+n},"applyPortOffset"),N=r((e,t,n)=>{let r=w.get(e),o={x:n.x??0,y:n.y??0},l={x:t.x??0,y:t.y??0},i=r?.srcSide??S(t,o),a=r?.dstSide??S(n,l),s=r?h(t,r.srcSide):g(t,o,!0),f=r?h(n,r.dstSide):g(n,l,!1),d=T.get(`${e}:src`),u=T.get(`${e}:dst`);return void 0!==d&&(s=B(s,i,d)),void 0!==u&&(f=B(f,a,u)),{pSrcPort:s,pDstPort:f,srcSide:i,dstSide:a}},"portsForEdge");for(let e of y){let t=l[e];if(p[e]=[],!t.start||!t.end||t.points&&t.points.length>0||t.start===t.end)continue;let n=i.get(t.start),o=i.get(t.end);if(!n||!o)continue;let{pSrcPort:a,pDstPort:h,srcSide:g,dstSide:y}=N(e,n,o),S={...a},w={...h},v="top"===g||"bottom"===g,C="top"===y||"bottom"===y;v?S.y=a.y>(n.y??0)?a.y+20:a.y-20:S.x=a.x>(n.x??0)?a.x+20:a.x-20,C?w.y=h.y>(o.y??0)?h.y+20:h.y-20:w.x=h.x>(o.x??0)?h.x+20:h.x-20;let L=r((e,t)=>{for(let n of d)if(!t.includes(n.nodeId)&&e.x>n.minX&&e.x<n.maxX&&e.y>n.minY&&e.y<n.maxY)return{inside:!0,obstacle:n};return{inside:!1}},"isPointInObstacle"),k=r((e,t,n,r,o)=>{if(o){let o=e.y>(t.y??0);return{x:(n.x??0)>=e.x?r.maxX+15:r.minX-15,y:o?r.maxY+15:r.minY-15,leavesPositiveSide:o}}let l=e.x>(t.x??0),i=(n.y??0)>=e.y;return{x:l?r.maxX+15:r.minX-15,y:i?r.maxY+15:r.minY-15,leavesPositiveSide:l}},"obstacleDetour"),$=[],R=[t.start,t.end],O=L(S,R);if(O.inside&&O.obstacle){let e=O.obstacle;if(v){let t=k(a,n,o,e,!0);S.x=t.x,S.y=t.y;let r=t.leavesPositiveSide?Math.min(e.minY-2,a.y+20):Math.max(e.maxY+2,a.y-20);$=[{x:a.x,y:r},{x:t.x,y:r},{x:t.x,y:t.y}]}else{let t=k(a,n,o,e,!1),r=t.leavesPositiveSide?Math.min(e.minX-2,a.x+20):Math.max(e.maxX+2,a.x-20);S.x=t.x,S.y=t.y,$=[{x:r,y:a.y},{x:r,y:t.y},{x:t.x,y:t.y}]}}let F=[],B=L(w,R);if(B.inside&&B.obstacle){let e=B.obstacle;if(C){let t=k(h,o,n,e,!0);w.x=t.x,w.y=t.y,F=[{x:t.x,y:t.y},{x:h.x,y:t.y}]}else{let t=k(h,o,n,e,!1);w.x=t.x,w.y=t.y,F=[{x:t.x,y:t.y},{x:t.x,y:h.y}]}}if(0===$.length&&0===F.length){let n=15>Math.abs(S.x-w.x),r=15>Math.abs(S.y-w.y),o=void 0!==T.get(`${e}:src`)||void 0!==T.get(`${e}:dst`),l=(M.get(`${t.start??""}:${g}:src`)?.length??0)+(M.get(`${t.start??""}:${g}:dst`)?.length??0),i=(M.get(`${t.end??""}:${y}:src`)?.length??0)+(M.get(`${t.end??""}:${y}:dst`)?.length??0),s=l>1||i>1,f=I.get(t.start??"")??0,d=I.get(t.end??"")??0,u=l>1&&E(t.start,g)||i>1&&E(t.end,y),p=l<=1||f<=2,m=i<=1||d<=2,v=s&&!u&&p&&m;if((n||r)&&!o&&(!s||v)&&!b(a,h,t.start,t.end)){t.points=[{...a},{...S},{...w},{...h}],x.add(e);let n=r?"horizontal":"vertical",o=r?a.y:a.x,l=r?Math.min(a.x,h.x):Math.min(a.y,h.y),i=r?Math.max(a.x,h.x):Math.max(a.y,h.y),s={id:`fast-path-${n}-${o.toFixed(0)}-${e}`,orientation:n,coord:o,spanMin:l,spanMax:i,tracks:[]};c.push({edgeIndex:e,segmentIndex:0,orientation:n,pipe:s,trackIndex:0,from:l,to:i});continue}}let X=u("vertical",S.x,S.y,S.y);S.x=X.coord;let z=u("vertical",w.x,w.y,w.y);w.x=z.coord;let Y=Math.min(S.x,w.x)-50,A=Math.max(S.x,w.x)+50,G=Math.min(S.y,w.y)-50,P=Math.max(S.y,w.y)+50;for(let e of d){let t=Math.min(S.x,w.x),n=Math.max(S.x,w.x),r=Math.min(S.y,w.y),o=Math.max(S.y,w.y);e.minX<n&&e.maxX>t&&e.minY<o&&e.maxY>r&&(Y=Math.min(Y,e.minX-25),A=Math.max(A,e.maxX+25),G=Math.min(G,e.minY-25),P=Math.max(P,e.maxY+25))}for(let e of d)e.maxX<Y||e.minX>A||e.maxY<G||e.minY>P||(u("horizontal",e.minY-15,Y,A),u("horizontal",e.maxY+15,Y,A),u("vertical",e.minX-15,G,P),u("vertical",e.maxX+15,G,P));u("horizontal",S.y,Y,A),u("horizontal",w.y,Y,A);let D=s.filter(e=>"horizontal"===e.orientation&&e.coord>=G&&e.coord<=P),H=s.filter(e=>"vertical"===e.orientation&&e.coord>=Y&&e.coord<=A),j=r((e,t)=>`${e.toFixed(1)},${t.toFixed(1)}`,"getKey"),_=j(S.x,S.y),V=j(w.x,w.y),W=new Map,K=new Map,J=new Map,q=new Set,U=[];W.set(_,0),J.set(_,"n"),U.push({key:_,f:Math.hypot(w.x-S.x,w.y-S.y),pt:S}),q.add(_);let Q=[],Z=r((e,n)=>b(e,n,t.start,t.end),"checkSegmentBlocked"),ee={x:w.x,y:S.y},et=Z(S,ee),en=Z(ee,w),er=et||en,eo={x:S.x,y:w.y},el=Z(S,eo),ei=Z(eo,w),ea=el||ei;if(er?ea||(Q=1e-6>Math.abs(S.x-w.x)?[S,w]:[S,eo,w]):Q=1e-6>Math.abs(S.y-w.y)||1e-6>Math.abs(S.x-w.x)?[S,w]:[S,ee,w],0===Q.length)for(;U.length>0;){U.sort((e,t)=>e.f-t.f);let n=U.shift();if(q.delete(n.key),n.key===V){let e=V,t=w;for(Q=[t];K.has(e);){let n=K.get(e);Q.unshift(n),t=n,e=j(n.x,n.y)}break}let r=n.pt.x,o=n.pt.y,l=H.sort((e,t)=>e.coord-t.coord),i=l.findIndex(e=>1>Math.abs(e.coord-r)),a=D.sort((e,t)=>e.coord-t.coord),s=a.findIndex(e=>1>Math.abs(e.coord-o)),f=[];for(let u of(i>0&&f.push({x:l[i-1].coord,y:o}),i>=0&&i<l.length-1&&f.push({x:l[i+1].coord,y:o}),s>0&&f.push({x:r,y:a[s-1].coord}),s>=0&&s<a.length-1&&f.push({x:r,y:a[s+1].coord}),f)){let l=Math.min(r,u.x),i=Math.max(r,u.x),a=Math.min(o,u.y),s=Math.max(o,u.y);if(d.some(e=>e.nodeId!==t.start&&e.nodeId!==t.end&&(l!==i?e.minY<o&&e.maxY>o&&e.maxX>l&&e.minX<i:e.minX<r&&e.maxX>r&&e.maxY>a&&e.minY<s)))continue;let f=j(u.x,u.y),h=Math.abs(u.x-r)+Math.abs(u.y-o),g=m(e,n.pt,u),c=0,p=w.x-S.x,x=w.y-S.y,y=u.x-r,b=u.y-o;(x>10&&b<-5||x<-10&&b>5)&&(c=100*Math.abs(b)),(p>10&&y<-5||p<-10&&y>5)&&(c+=50*Math.abs(y));let M=0,I=J.get(n.key)??"n",v=Math.abs(y)>1e-6?"h":"v";"n"!==I&&I!==v&&(M=50);let C=h+g+c+M,L=(W.get(n.key)??1/0)+C,k=Math.abs(w.x-u.x)+Math.abs(w.y-u.y);if(L<(W.get(f)??1/0))if(K.set(f,n.pt),W.set(f,L),J.set(f,v),q.has(f)){let e=U.findIndex(e=>e.key===f);-1!==e&&(U[e].f=L+k)}else U.push({key:f,f:L+k,pt:u}),q.add(f)}}if(0===Q.length&&(Q=[S,{x:S.x,y:w.y},w]),Q.length>4){let e=Q[0],t=Q[Q.length-1],n=Math.min(e.x,t.x),o=Math.max(e.x,t.x),l=Math.min(e.y,t.y),i=Math.max(e.y,t.y);for(let e of Q)n=Math.min(n,e.x),o=Math.max(o,e.x),l=Math.min(l,e.y),i=Math.max(i,e.y);let a=o>Math.max(e.x,t.x),s=n<Math.min(e.x,t.x);if(f){if(a){let n=Math.max(e.x,t.x),r=Math.min(e.y,t.y),l=Math.max(e.y,t.y),i=d.filter(e=>e.minX<n&&e.maxX>n&&e.minY<l&&e.maxY>r);if(i.length>0){let n=Math.max(e.x,t.x);for(let e of i){let t=(e.minX+e.maxX)/2;void 0===e.visualXHalfExtent||isNaN(e.visualXHalfExtent)||(n=Math.max(n,t+e.visualXHalfExtent+15))}isNaN(n)||(o=n)}}if(s){let r=d.filter(n=>n.minX<Math.min(e.x,t.x)+15&&n.minY<Math.max(e.y,t.y)&&n.maxY>Math.min(e.y,t.y));if(r.length>0){let o=Math.min(e.x,t.x);for(let e of r)o=Math.min(o,(e.minX+e.maxX)/2-e.visualXHalfExtent-15);n=o}}}let u=r(n=>{let r=t.y>e.y,o=d.filter(n=>{let r=Math.min(e.x,t.x)<n.maxX&&Math.max(e.x,t.x)>n.minX,o=Math.min(e.y,t.y)<n.maxY&&Math.max(e.y,t.y)>n.minY;return r&&o}),l=o;if(f&&o.length>0){let e=o.filter(e=>e.minX<n&&e.maxX>n);e.length>0&&(l=e)}if(0===l.length)return t.y;if(r){let e=Math.max(...l.map(e=>e.maxY))+15;if(e<t.y-1e-6)return e}else{let e=Math.min(...l.map(e=>e.minY))-15;if(e>t.y+1e-6)return e}return t.y},"findBestReturnY"),h=r(n=>{let r=u(n),o={x:n,y:e.y},l={x:n,y:r},i={x:t.x,y:r},a=Z(e,o),s=Z(o,l),f=Z(l,i),d=r!==t.y&&Z(i,t);return a||s||f||d?null:1e-6>Math.abs(r-t.y)?[e,o,l,t]:[e,o,l,i,t]},"trySimplifyWithDetourX"),g=a&&!s?h(o):s&&!a?h(n):null;g&&(Q=g)}let es=[a,...$,...Q,...F.reverse(),h];if(es.length>=3){let e=es[es.length-1],t=es[es.length-2],n=es[es.length-3],r=1e-6>Math.abs(n.y-t.y)&&1e-6>Math.abs(t.y-e.y),o=1e-6>Math.abs(n.x-t.x)&&1e-6>Math.abs(t.x-e.x);if(r){let r=Math.sign(t.x-n.x),o=Math.sign(e.x-n.x);0!==r&&r===o&&Math.abs(t.x-n.x)>Math.abs(e.x-n.x)&&es.splice(-2,1)}else if(o){let r=Math.sign(t.y-n.y),o=Math.sign(e.y-n.y);0!==r&&r===o&&Math.abs(t.y-n.y)>Math.abs(e.y-n.y)&&es.splice(-2,1)}}let ef=[es[0]];for(let e=1;e<es.length-1;e++){if(1===e){ef.push(es[e]);continue}let t=ef[ef.length-1],n=es[e],r=es[e+1];if(1e-6>Math.abs(t.y-n.y)&&1e-6>Math.abs(n.y-r.y)){n.x>t.x!=r.x>n.x&&ef.push(n);continue}if(1e-6>Math.abs(t.x-n.x)&&1e-6>Math.abs(n.x-r.x)){n.y>t.y!=r.y>n.y&&ef.push(n);continue}ef.push(n)}ef.push(es[es.length-1]);for(let t=0;t<ef.length-1;t++){let n=ef[t],r=ef[t+1],o=1e-6>Math.abs(n.x-r.x)?"vertical":"horizontal",l="vertical"===o?n.x:n.y,i="vertical"===o?Math.min(n.y,r.y):Math.min(n.x,r.x),a="vertical"===o?Math.max(n.y,r.y):Math.max(n.x,r.x),s=u(o,l,i,a),f={edgeIndex:e,segmentIndex:t,orientation:o,pipe:s,trackIndex:0,from:i,to:a};c.push(f),p[e].push(c.length-1),s.tracks[0]||(s.tracks[0]={index:0,coord:s.coord,segments:[]}),s.tracks[0].segments.push({edgeIndex:e,segmentIndex:t,from:i,to:a})}}let X=r((e,t)=>e.from<t.to&&t.from<e.to,"segmentsOverlap"),z=r((e,t,n,r)=>{let o=!r.segments.some(n=>(n.edgeIndex!==t.edgeIndex||n.segmentIndex!==t.segmentIndex)&&X(n,e)),l=!n.segments.some(n=>(n.edgeIndex!==e.edgeIndex||n.segmentIndex!==e.segmentIndex)&&X(n,t));return!!o&&!!l&&(e.trackIndex=r.index,t.trackIndex=n.index,n.segments=[...n.segments.filter(t=>t.edgeIndex!==e.edgeIndex||t.segmentIndex!==e.segmentIndex),{edgeIndex:t.edgeIndex,segmentIndex:t.segmentIndex,from:t.from,to:t.to}],r.segments=[...r.segments.filter(e=>e.edgeIndex!==t.edgeIndex||e.segmentIndex!==t.segmentIndex),{edgeIndex:e.edgeIndex,segmentIndex:e.segmentIndex,from:e.from,to:e.to}],!0)},"trySwapSegmentsAcrossTracks"),Y=r(e=>{let t=e.tracks.length;return e.tracks[t]={index:t,coord:e.coord,segments:[]},t},"createNewTrack"),A=r((e,t)=>{let n=e.pipe.tracks[e.trackIndex];n.segments=n.segments.filter(t=>t.edgeIndex!==e.edgeIndex||t.segmentIndex!==e.segmentIndex),e.trackIndex=t,e.pipe.tracks[t].segments.push({edgeIndex:e.edgeIndex,segmentIndex:e.segmentIndex,from:e.from,to:e.to})},"moveSegmentToTrack"),G=r((e,t)=>{for(let n of p[e.edgeIndex]){let r=c[n];r.pipe===e.pipe&&A(r,t)}},"moveSegmentChainToTrack"),P=r(e=>{let t=p[e.edgeIndex],n=t.indexOf(c.indexOf(e)),r=[];return n>0&&r.push(c[t[n-1]]),n<t.length-1&&r.push(c[t[n+1]]),r},"getAdjacentSegmentsAlongEdge"),D=r((e,t)=>{if(e.orientation===t.orientation)return!1;let n="horizontal"===e.orientation?e:t,r="horizontal"===e.orientation?t:e;return r.pipe.coord>n.from&&r.pipe.coord<n.to&&n.pipe.coord>r.from&&n.pipe.coord<r.to},"haveAnyCrossing"),H=r((e,t)=>{for(let n of e.tracks)if(!n.segments.some(e=>(e.edgeIndex!==t.edgeIndex||e.segmentIndex!==t.segmentIndex)&&X(e,t)))return n.index;return -1},"findAvailableTrack"),j=r((e,t)=>{if(e.trackIndex===t.trackIndex)return X(e,t);let n=P(e),r=P(t);return n.some(e=>r.some(t=>D(e,t)))},"segmentsConflict"),_=r((e,t,n)=>{if(z(e,t,e.pipe.tracks[e.trackIndex],t.pipe.tracks[t.trackIndex]))return;let r=H(e.pipe,t);n(t,-1!==r?r:Y(e.pipe))},"resolveTrackConflict"),V=r(e=>{let t=0;for(let n=0;n<e.length;n++)for(let r=n+1;r<e.length;r++){let o=e[n],l=e[r];o.pipe===l.pipe&&j(o,l)&&(t++,_(o,l,G))}return t},"resolveHandleConflicts"),W=new Map,K=r(e=>{if(W.has(e))return W.get(e);let t=p[e];if(0===t.length){let t={dest:0,deviation:0,base:0,delta:0};return W.set(e,t),t}let n=c[t[0]].pipe.coord,r=n;for(let e=1;e<t.length;e++){let o=c[t[e]];if("horizontal"===o.orientation){let e=o.from,t=o.to;r=Math.abs(e-n)>Math.abs(t-n)?e:t;break}}let o=Math.abs(r-n),l={dest:r,deviation:o,base:n,delta:r-n};return W.set(e,l),l},"getDestInfo"),J=r(()=>{let e=0,t=new Map;for(let[e,n]of l.entries())0!==p[e].length&&n.start&&(t.has(n.start)||t.set(n.start,[]),t.get(n.start).push(e));let n=r(e=>{let t=l[e];if(!t.start||!t.end)return 0;let n=i.get(t.start),r=i.get(t.end);return n&&r?Math.abs((r.x??0)-(n.x??0))+Math.abs((r.y??0)-(n.y??0)):0},"getEdgeDistance");for(let r of t.values())r.sort((e,t)=>{let r=K(e),o=K(t);if(Math.abs(r.deviation-o.deviation)>1)return r.deviation-o.deviation;if(Math.abs(r.dest-o.dest)>1)return r.dest-o.dest;let l=n(e),i=n(t);if(Math.abs(l-i)>1)return i-l;let a=p[e].length,s=p[t].length;if(a!==s)return a-s;if(1===a){let n=p[e][0],r=p[t][0];if(c[n]&&c[r]){let e=c[n],t=c[r],o=Math.abs(e.to-e.from),l=Math.abs(t.to-t.from);if(Math.abs(o-l)>1)return o-l}}return 0}),e+=V(r.map(e=>c[p[e][0]]));return e},"fixSourceHandleCrossings"),q=r(()=>{let e=0,t=new Map;for(let[e,n]of l.entries())0!==p[e].length&&n.end&&(t.has(n.end)||t.set(n.end,[]),t.get(n.end).push(e));for(let n of t.values())n.sort((e,t)=>{let n=r(e=>{let t=p[e];if(t.length<2)return 0;let n=c[t[t.length-2]];return Math.abs(n.to-n.from)},"getDist"),o=n(e),l=n(t);return Math.abs(o-l)>.1?o-l:e-t}),e+=V(n.map(e=>c[p[e][p[e].length-1]]));return e},"fixTargetHandleCrossings"),U=r(()=>{let e=0;for(let t of s){let n=[];for(let e of t.tracks)for(let t of e.segments){let e=p[t.edgeIndex].find(e=>c[e].segmentIndex===t.segmentIndex);void 0!==e&&n.push(c[e])}n.sort((e,t)=>e.edgeIndex-t.edgeIndex||e.segmentIndex-t.segmentIndex);for(let t=0;t<n.length;t++)for(let r=t+1;r<n.length;r++){let o=n[t],l=n[r];j(o,l)&&(e++,_(o,l,A))}}return e},"fixPipeCrossings"),Q=0;for(;Q<10&&0!==0+J()+q()+U();){;Q++}let Z=new Map;for(let e of s){let t=[];e.tracks.forEach(e=>{e.segments.forEach(n=>{t.push({edgeIndex:n.edgeIndex,segmentIndex:n.segmentIndex,trackIndex:e.index,from:n.from,to:n.to})})}),t.sort((e,t)=>e.from-t.from);let n=[];if(t.length>0){let e=[t[0]],r=t[0].to;for(let o=1;o<t.length;o++){let l=t[o];l.from<r?(e.push(l),r=Math.max(r,l.to)):(n.push(e),e=[l],r=l.to)}n.push(e)}for(let t of n){let n=new Set;t.forEach(e=>n.add(e.trackIndex));let o=new Map;t.forEach(e=>{let t=K(e.edgeIndex);o.set(e.trackIndex,(o.get(e.trackIndex)??0)+t.delta)});let l=[...n].filter(e=>(o.get(e)??0)<-1),i=[...n].filter(e=>(o.get(e)??0)>1),a=[...n].filter(e=>1>=Math.abs(o.get(e)??0));l.sort((e,t)=>(o.get(t)??0)-(o.get(e)??0)),i.sort((e,t)=>(o.get(e)??0)-(o.get(t)??0));let s=r((n,r)=>{t.filter(e=>e.trackIndex===n).forEach(t=>{let n=x.has(t.edgeIndex)?e.coord:r;Z.set(`${t.edgeIndex}-${t.segmentIndex}`,n)})},"assignCoord"),f=0;for(let t of l)f++,s(t,e.coord-10*f);if(0===a.length&&n.size>0){let e=[...n].sort((e,t)=>Math.abs(o.get(e)??0)-Math.abs(o.get(t)??0))[0],t=l.indexOf(e);-1!==t&&l.splice(t,1);let r=i.indexOf(e);-1!==r&&i.splice(r,1),a.push(e)}let d=0;for(let t of a){if(0===d)s(t,e.coord);else{let n=d%2==1?1:-1,r=Math.ceil(d/2);s(t,e.coord+n*r*5)}d++}let u=0;for(let t of i)u++,s(t,e.coord+10*u)}}for(let[e,t]of l.entries()){let n=p[e]??[];if(0===n.length)continue;let r=[],{pSrcPort:o,pDstPort:l}=N(e,i.get(t.start),i.get(t.end)),a=n.map(e=>{let t=c[e],n=Z.get(`${t.edgeIndex}-${t.segmentIndex}`)??t.pipe.coord;return{orient:t.orientation,coord:n,from:t.from,to:t.to}});r.push(o);for(let e=0;e<a.length;e++){let t=a[e],n=r[r.length-1],o="vertical"===t.orient?n.y:n.x,l="vertical"===t.orient?n.x:n.y,i=a[e+1],s=e<a.length-1;if(Math.abs(l-t.coord)>1e-6&&r.push(t7(t,o)),s&&i.orient===t.orient)if(Math.abs(t.coord-i.coord)>1e-6){let e="vertical"===t.orient?(o+i.from)/2:t9(t,i);r.push(t7(t,e),t7(i,e))}else(0===e||e===a.length-2)&&r.push(t7(t,t9(t,i)));else if(s)r.push(t7(t,i.coord));else{let e=Math.abs(t.from-o)<Math.abs(t.to-o)?t.to:t.from;r.push(t7(t,e))}}let s=r[r.length-1];(Math.abs(s.x-l.x)>1e-6||Math.abs(s.y-l.y)>1e-6)&&r.push(l);let f=[];r.length>0&&f.push(r[0]);for(let e=1;e<r.length;e++){let t=r[e],n=f[f.length-1];(Math.abs(t.x-n.x)>1e-6||Math.abs(t.y-n.y)>1e-6)&&f.push(t)}t.points=f}for(let e of l){let t=e.__originalEdge;t&&e.points&&(t.points=e.points)}e.edges=(e.edges??[]).filter(e=>!e.isLayoutOnly);let ee=r((e,t)=>{let n=t.x??0,r=t.y??0,o=t.width??0,l=t.height??0;if(o<=0||l<=0)return e;let i=n-o/2,a=n+o/2,s=r-l/2,f=r+l/2;if(e.x<i||e.x>a||e.y<s||e.y>f)return e;let d=e.x-i,u=a-e.x,h=e.y-s,g=Math.min(d,u,h,f-e.y);return g===d?{x:i,y:e.y}:g===u?{x:a,y:e.y}:g===h?{x:e.x,y:s}:{x:e.x,y:f}},"nodeBoundaryClamp");for(let t of e.edges){let e=t.points;if(!e||e.length<2)continue;let n=t.start,r=t.end,o=n?i.get(n):void 0,l=r?i.get(r):void 0;o&&(e[0]=ee(e[0],o)),l&&(e[e.length-1]=ee(e[e.length-1],l))}return e}function nt(e){return e.direction??"TB"}function nn(e){let t=v(e),n=e.config.flowchart?.nodeSpacing??40,r=e.config.flowchart?.rankSpacing??100,o=e.config.swimlane?.ignoreCrossLaneEdges??!0,l=e.config.swimlane?.optimizeRanksByCrossings??!0,i=e.config.swimlane?.automaticLaneOrdering??!1,a=nt(e),{ordered:s,coordinates:f}=t8(t,{nodeGap:n,layerGap:r,ignoreCrossLaneEdges:o,optimizeRanksByCrossings:l,automaticLaneOrdering:i,direction:a});for(let o of(C(t,s,f,{nodeGap:n,layerGap:r}),e.edges??[]))delete o.points;for(let t of(ne(e,a),e.edges??[]))t.curve&&"basis"!==t.curve||(t.curve="rounded");return tt(e,a),te(e),a}function nr(e){w(e);let t=L(e);e.nodes=t.nodes,e.edges=t.edges}r(ty,"buildDrivingTree"),r(tb,"chooseParent"),r(tM,"buildAdjacency"),r(tI,"assignComponents"),r(tS,"computeBlocks"),r(tw,"popBlock"),r(tv,"computeSubtreeCrossCounts"),r(tC,"annotateMinimumLayers"),r(tL,"compareByRankThenId"),r(tk,"emitNodesInTreeOrder"),r(t$,"deduplicateLayers"),r(tR,"createChildOrderer"),r(tO,"buildMultitreeLayerOrder"),r(tT,"countCrossingsBetweenAdjacent"),r(tF,"totalCrossings"),r(tE,"optimizeRanksByCrossings"),r(tB,"adjustCrossLaneSources"),r(tN,"assignLayers_LongestPath"),r(tX,"assignLayers_Gravity"),r(tz,"topoSortByGenerationIfAcyclic"),r(tY,"assignLayers_LaneAwareCompact"),r(tA,"makeProperLayering"),r(tG,"median"),r(tP,"barycenter"),r(tD,"neighborPositionsFor"),r(tH,"currentOrderTieBreak"),r(tj,"countCrossingsBetweenAdjacent"),r(t_,"sortByHeuristic"),r(tV,"reorderLayer"),r(tW,"transposeImprove"),r(tK,"orderLayers"),r(tJ,"assignCoordinates"),r(tq,"hashString"),r(tU,"mulberry32"),r(tQ,"deterministicShuffle"),r(tZ,"sourceDistance"),r(t0,"laneArrangementCost"),r(t1,"buildWeightedLaneEdges"),r(t2,"greedySwitch"),r(t6,"isBetterCandidate"),r(t5,"seedForRestart"),r(t3,"optimizeTopLaneOrder"),r(t8,"sugiyamaLayout"),r(t4,"chooseOrthogonalSide"),r(t9,"sharedLineEndpointCoord"),r(t7,"pointOnLine"),r(ne,"routeEdgesOrthogonal"),r(nt,"getSwimlaneDirection"),r(nn,"runSwimlaneLayoutCore"),r(nr,"prepareSwimlaneLayout");var no=t({prepareLayout:nr,runLayoutCore:nn,afterPaint:b});export{no as render};0~swimlanes-XN3QIQJK.js{"version":3,"sources":["0~swimlanes-XN3QIQJK.js"],"sourcesContent":["import { markerOffsets, createCommonLayoutRenderer, log, chunk_Y2CYZVJY_name } from \"__RSPACK_ESM_CHUNK_1307\";\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n// src/rendering-util/rendering-elements/lineJump.ts\nvar ROUNDED_CORNER_RADIUS = 5;\nvar CORNER_EPSILON = 1e-5;\nvar ENDPOINT_EPSILON = 1e-6;\nfunction buildSegmentList(points) {\n  const segments = [];\n  for (let i = 0; i < points.length - 1; i++) {\n    segments.push({ a: points[i], b: points[i + 1] });\n  }\n  return segments;\n}\nchunk_Y2CYZVJY_name(buildSegmentList, \"buildSegmentList\");\nfunction segmentIntersection(a1, a2, b1, b2) {\n  const dxA = a2.x - a1.x;\n  const dyA = a2.y - a1.y;\n  const dxB = b2.x - b1.x;\n  const dyB = b2.y - b1.y;\n  const denom = dxA * dyB - dyA * dxB;\n  if (denom === 0) {\n    return null;\n  }\n  const dx = b1.x - a1.x;\n  const dy = b1.y - a1.y;\n  const tA = (dx * dyB - dy * dxB) / denom;\n  const tB = (dx * dyA - dy * dxA) / denom;\n  if (tA <= ENDPOINT_EPSILON || tA >= 1 - ENDPOINT_EPSILON || tB <= ENDPOINT_EPSILON || tB >= 1 - ENDPOINT_EPSILON) {\n    return null;\n  }\n  return {\n    point: { x: a1.x + tA * dxA, y: a1.y + tA * dyA },\n    tA,\n    tB\n  };\n}\nchunk_Y2CYZVJY_name(segmentIntersection, \"segmentIntersection\");\nfunction isHorizontalSeg(seg) {\n  return Math.abs(seg.b.x - seg.a.x) >= Math.abs(seg.b.y - seg.a.y);\n}\nchunk_Y2CYZVJY_name(isHorizontalSeg, \"isHorizontalSeg\");\nfunction findEdgeIntersections(edges) {\n  const crossings = [];\n  for (let i = 0; i < edges.length; i++) {\n    const edgeA = edges[i];\n    const segmentsA = buildSegmentList(edgeA.points);\n    for (let j = i + 1; j < edges.length; j++) {\n      const edgeB = edges[j];\n      const segmentsB = buildSegmentList(edgeB.points);\n      for (const [si, segA] of segmentsA.entries()) {\n        for (const [sj, segB] of segmentsB.entries()) {\n          const hit = segmentIntersection(segA.a, segA.b, segB.a, segB.b);\n          if (!hit) {\n            continue;\n          }\n          const aHoriz = isHorizontalSeg(segA);\n          const bHoriz = isHorizontalSeg(segB);\n          const orthogonalPair = aHoriz !== bHoriz;\n          const jumpOnA = orthogonalPair ? aHoriz : false;\n          if (jumpOnA) {\n            crossings.push({\n              jumpEdgeId: edgeA.id,\n              otherEdgeId: edgeB.id,\n              segIndex: si,\n              t: hit.tA,\n              point: hit.point\n            });\n          } else {\n            crossings.push({\n              jumpEdgeId: edgeB.id,\n              otherEdgeId: edgeA.id,\n              segIndex: sj,\n              t: hit.tB,\n              point: hit.point\n            });\n          }\n        }\n      }\n    }\n  }\n  return crossings;\n}\nchunk_Y2CYZVJY_name(findEdgeIntersections, \"findEdgeIntersections\");\nfunction fmt(n) {\n  const rounded = Math.round(n * 1e3) / 1e3;\n  return Number.isInteger(rounded) ? `${rounded}` : `${rounded}`;\n}\nchunk_Y2CYZVJY_name(fmt, \"fmt\");\nfunction pointToString(p) {\n  return `${fmt(p.x)},${fmt(p.y)}`;\n}\nchunk_Y2CYZVJY_name(pointToString, \"pointToString\");\nfunction getArcSweepFlag(seg) {\n  const dx = seg.b.x - seg.a.x;\n  const dy = seg.b.y - seg.a.y;\n  if (Math.abs(dx) >= Math.abs(dy)) {\n    return dx >= 0 ? 1 : 0;\n  }\n  return dy >= 0 ? 1 : 0;\n}\nchunk_Y2CYZVJY_name(getArcSweepFlag, \"getArcSweepFlag\");\nvar MIN_JUMP_RADIUS = 1e-3;\nfunction applyMarkerOffsets(points, edge) {\n  if (points.length < 2) {\n    return points.map((p) => ({ ...p }));\n  }\n  const out = points.map((p) => ({ ...p }));\n  const startOff = edge.arrowTypeStart && markerOffsets[edge.arrowTypeStart];\n  if (startOff) {\n    const a = points[0];\n    const b = points[1];\n    const ang = Math.atan2(b.y - a.y, b.x - a.x);\n    out[0].x = a.x + startOff * Math.cos(ang);\n    out[0].y = a.y + startOff * Math.sin(ang);\n  }\n  const endOff = edge.arrowTypeEnd && markerOffsets[edge.arrowTypeEnd];\n  if (endOff) {\n    const n = points.length;\n    const a = points[n - 2];\n    const b = points[n - 1];\n    const ang = Math.atan2(b.y - a.y, b.x - a.x);\n    out[n - 1].x = b.x - endOff * Math.cos(ang);\n    out[n - 1].y = b.y - endOff * Math.sin(ang);\n  }\n  return out;\n}\nchunk_Y2CYZVJY_name(applyMarkerOffsets, \"applyMarkerOffsets\");\nfunction emitJump(jump, ux, uy, sweep, style) {\n  const cx = jump.point.x;\n  const cy = jump.point.y;\n  const pre = { x: cx - ux * jump.r, y: cy - uy * jump.r };\n  const post = { x: cx + ux * jump.r, y: cy + uy * jump.r };\n  const out = [`L${pointToString(pre)}`];\n  if (style === \"arc\") {\n    out.push(`A${fmt(jump.r)},${fmt(jump.r)} 0 0 ${sweep} ${pointToString(post)}`);\n  } else {\n    out.push(`M${pointToString(post)}`);\n  }\n  return out;\n}\nchunk_Y2CYZVJY_name(emitJump, \"emitJump\");\nfunction computeRoundedCorner(prev, curr, next, radius) {\n  const dx1 = curr.x - prev.x;\n  const dy1 = curr.y - prev.y;\n  const dx2 = next.x - curr.x;\n  const dy2 = next.y - curr.y;\n  const len1 = Math.hypot(dx1, dy1);\n  const len2 = Math.hypot(dx2, dy2);\n  if (len1 < CORNER_EPSILON || len2 < CORNER_EPSILON) {\n    return null;\n  }\n  const nx1 = dx1 / len1;\n  const ny1 = dy1 / len1;\n  const nx2 = dx2 / len2;\n  const ny2 = dy2 / len2;\n  const dot = nx1 * nx2 + ny1 * ny2;\n  const clamped = Math.max(-1, Math.min(1, dot));\n  const angle = Math.acos(clamped);\n  if (angle < CORNER_EPSILON || Math.abs(Math.PI - angle) < CORNER_EPSILON) {\n    return null;\n  }\n  const cutLen = Math.min(radius / Math.sin(angle / 2), len1 / 2, len2 / 2);\n  return {\n    startX: curr.x - nx1 * cutLen,\n    startY: curr.y - ny1 * cutLen,\n    endX: curr.x + nx2 * cutLen,\n    endY: curr.y + ny2 * cutLen,\n    ctrlX: curr.x,\n    ctrlY: curr.y,\n    cutLen\n  };\n}\nchunk_Y2CYZVJY_name(computeRoundedCorner, \"computeRoundedCorner\");\nfunction rewriteEdgePath(edge, jumps, config) {\n  const rawPoints = edge.points;\n  if (rawPoints.length < 2) {\n    return \"\";\n  }\n  const points = applyMarkerOffsets(rawPoints, edge);\n  const rounded = edge.curve === \"rounded\";\n  const segments = buildSegmentList(points);\n  const bySeg = /* @__PURE__ */ new Map();\n  for (const j of jumps) {\n    const seg = segments[j.segIndex];\n    if (!seg) {\n      continue;\n    }\n    const segLen = Math.hypot(seg.b.x - seg.a.x, seg.b.y - seg.a.y);\n    const list = bySeg.get(j.segIndex) ?? [];\n    list.push({\n      t: j.t,\n      point: j.point,\n      d: j.t * segLen,\n      r: config.jumpRadius\n    });\n    bySeg.set(j.segIndex, list);\n  }\n  const parts = [`M${pointToString(points[0])}`];\n  for (let i = 0; i < segments.length; i++) {\n    const seg = segments[i];\n    const segLen = Math.hypot(seg.b.x - seg.a.x, seg.b.y - seg.a.y);\n    const ux = segLen === 0 ? 0 : (seg.b.x - seg.a.x) / segLen;\n    const uy = segLen === 0 ? 0 : (seg.b.y - seg.a.y) / segLen;\n    const sweep = getArcSweepFlag(seg);\n    let segStartConsumed = 0;\n    if (rounded && i > 0) {\n      const corner = computeRoundedCorner(\n        points[i - 1],\n        points[i],\n        points[i + 1] ?? points[i],\n        ROUNDED_CORNER_RADIUS\n      );\n      if (corner) {\n        segStartConsumed = corner.cutLen;\n      }\n    }\n    let segEndStop = segLen;\n    let upcomingCorner = null;\n    if (rounded && i < segments.length - 1) {\n      upcomingCorner = computeRoundedCorner(\n        points[i],\n        points[i + 1],\n        points[i + 2] ?? points[i + 1],\n        ROUNDED_CORNER_RADIUS\n      );\n      if (upcomingCorner) {\n        segEndStop = segLen - upcomingCorner.cutLen;\n      }\n    }\n    const segJumps = [...bySeg.get(i) ?? []].sort((a, b) => a.t - b.t);\n    for (const j of segJumps) {\n      j.r = Math.min(j.r, j.d - segStartConsumed, segEndStop - j.d);\n    }\n    for (let k = 0; k < segJumps.length - 1; k++) {\n      const gap = segJumps[k + 1].d - segJumps[k].d;\n      if (segJumps[k].r + segJumps[k + 1].r > gap) {\n        const half = gap / 2;\n        segJumps[k].r = Math.min(segJumps[k].r, half);\n        segJumps[k + 1].r = Math.min(segJumps[k + 1].r, half);\n      }\n    }\n    for (const j of segJumps) {\n      if (j.r < MIN_JUMP_RADIUS) {\n        continue;\n      }\n      parts.push(...emitJump(j, ux, uy, sweep, config.jumpStyle));\n    }\n    if (rounded && upcomingCorner) {\n      parts.push(`L${fmt(upcomingCorner.startX)},${fmt(upcomingCorner.startY)}`);\n      parts.push(\n        `Q${fmt(upcomingCorner.ctrlX)},${fmt(upcomingCorner.ctrlY)} ${fmt(upcomingCorner.endX)},${fmt(upcomingCorner.endY)}`\n      );\n    } else {\n      parts.push(`L${pointToString(seg.b)}`);\n    }\n  }\n  return parts.join(\" \");\n}\nchunk_Y2CYZVJY_name(rewriteEdgePath, \"rewriteEdgePath\");\nfunction isStraightPath(d) {\n  return /^[\\d\\s+,.LMelm-]*$/.test(d);\n}\nchunk_Y2CYZVJY_name(isStraightPath, \"isStraightPath\");\nfunction curveSupportsLineHops(curve) {\n  if (!curve) {\n    return true;\n  }\n  return curve === \"linear\" || curve === \"rounded\" || curve === \"step\" || curve === \"stepBefore\" || curve === \"stepAfter\";\n}\nchunk_Y2CYZVJY_name(curveSupportsLineHops, \"curveSupportsLineHops\");\nfunction decodeDataPoints(raw) {\n  if (!raw) {\n    return null;\n  }\n  try {\n    const json = typeof atob === \"function\" ? atob(raw) : Buffer.from(raw, \"base64\").toString();\n    const parsed = JSON.parse(json);\n    if (!Array.isArray(parsed)) {\n      return null;\n    }\n    const pts = [];\n    for (const p of parsed) {\n      if (p && typeof p.x === \"number\" && typeof p.y === \"number\") {\n        pts.push({ x: p.x, y: p.y });\n      }\n    }\n    return pts.length >= 2 ? pts : null;\n  } catch {\n    return null;\n  }\n}\nchunk_Y2CYZVJY_name(decodeDataPoints, \"decodeDataPoints\");\nfunction applyLineJumpsToSvg(edgePathsGroup, edges, config) {\n  if (!config.enabled) {\n    return;\n  }\n  const groupNode = edgePathsGroup.node();\n  if (!groupNode) {\n    return;\n  }\n  const edgeMeta = /* @__PURE__ */ new Map();\n  for (const e of edges) {\n    edgeMeta.set(e.id, e);\n  }\n  const renderedEdges = [];\n  const pathById = /* @__PURE__ */ new Map();\n  for (const e of edges) {\n    const escapedId = typeof CSS !== \"undefined\" && CSS.escape ? CSS.escape(e.id) : e.id;\n    const pathEl = groupNode.querySelector(`path[data-id=\"${escapedId}\"]`);\n    if (!pathEl) {\n      continue;\n    }\n    pathById.set(e.id, pathEl);\n    const decoded = decodeDataPoints(pathEl.getAttribute(\"data-points\"));\n    const points = decoded ?? e.points;\n    renderedEdges.push({ ...e, points });\n  }\n  const crossings = findEdgeIntersections(renderedEdges);\n  if (crossings.length === 0) {\n    return;\n  }\n  const jumpsByEdge = /* @__PURE__ */ new Map();\n  for (const c of crossings) {\n    const list = jumpsByEdge.get(c.jumpEdgeId) ?? [];\n    list.push(c);\n    jumpsByEdge.set(c.jumpEdgeId, list);\n  }\n  for (const renderedEdge of renderedEdges) {\n    const jumps = jumpsByEdge.get(renderedEdge.id);\n    if (!jumps || jumps.length === 0) {\n      continue;\n    }\n    const meta = edgeMeta.get(renderedEdge.id);\n    const curveHint = meta?.curve;\n    if (curveHint !== void 0 && !curveSupportsLineHops(curveHint)) {\n      continue;\n    }\n    const pathEl = pathById.get(renderedEdge.id);\n    if (!pathEl) {\n      continue;\n    }\n    if (curveHint === void 0) {\n      const currentD = pathEl.getAttribute(\"d\") ?? \"\";\n      if (!isStraightPath(currentD)) {\n        continue;\n      }\n    }\n    const originalStyle = pathEl.getAttribute(\"style\") ?? \"\";\n    const dasharrayMatch = /stroke-dasharray\\s*:\\s*0\\s+([\\d.]+)\\s+[\\d.]+\\s+([\\d.]+)/.exec(\n      originalStyle\n    );\n    const preservedOValueS = dasharrayMatch ? Number.parseFloat(dasharrayMatch[1]) : null;\n    const preservedOValueE = dasharrayMatch ? Number.parseFloat(dasharrayMatch[2]) : null;\n    const newD = rewriteEdgePath(renderedEdge, jumps, config);\n    pathEl.setAttribute(\"d\", newD);\n    if (preservedOValueS !== null && preservedOValueE !== null && typeof pathEl.getTotalLength === \"function\") {\n      const newLen = pathEl.getTotalLength();\n      const onLen = Math.max(0, newLen - preservedOValueS - preservedOValueE);\n      const newDasharray = `0 ${preservedOValueS} ${onLen} ${preservedOValueE}`;\n      const cleaned = originalStyle.replace(/stroke-dasharray\\s*:[^;]*;?/g, `stroke-dasharray: ${newDasharray};`).replace(/;\\s*;+/g, \";\");\n      pathEl.setAttribute(\"style\", cleaned);\n    }\n  }\n}\nchunk_Y2CYZVJY_name(applyLineJumpsToSvg, \"applyLineJumpsToSvg\");\n\n// src/rendering-util/layout-algorithms/swimlanes/adjustLayout.ts\nfunction applySwimlaneLineJumps(data4Layout, { measure }) {\n  const lineHopsConfig = data4Layout.config?.swimlane?.lineHops;\n  if (lineHopsConfig === false) {\n    return;\n  }\n  const jumpStyle = lineHopsConfig === \"gap\" ? \"gap\" : \"arc\";\n  const edgeGeometries = data4Layout.edges.filter((edge) => Array.isArray(edge.points) && edge.points.length >= 2).map((edge) => ({\n    id: edge.id,\n    points: edge.points,\n    curve: edge.curve,\n    arrowTypeStart: edge.arrowTypeStart,\n    arrowTypeEnd: edge.arrowTypeEnd\n  }));\n  applyLineJumpsToSvg(measure.groups.edgePaths, edgeGeometries, {\n    enabled: true,\n    jumpRadius: 6,\n    jumpStyle\n  });\n}\nchunk_Y2CYZVJY_name(applySwimlaneLineJumps, \"applySwimlaneLineJumps\");\n\n// src/rendering-util/layout-algorithms/swimlanes/helpers.ts\nvar DEFAULT_SWIMLANE_ID = \"__swimlane_default__\";\nvar TOP_LANE_TITLE_BAND_HEIGHT = 21;\nvar MIN_TOP_LANE_HORIZONTAL_PADDING = 20;\nfunction topLaneHorizontalPadding(lane) {\n  return Math.max(lane.padding ?? MIN_TOP_LANE_HORIZONTAL_PADDING, MIN_TOP_LANE_HORIZONTAL_PADDING);\n}\nchunk_Y2CYZVJY_name(topLaneHorizontalPadding, \"topLaneHorizontalPadding\");\nfunction assignTopLaneTitleRect(lane) {\n  const { x, y, width, height } = lane;\n  const contentTop = lane.swimlaneContentTop;\n  if (typeof x !== \"number\" || typeof y !== \"number\" || typeof width !== \"number\" || typeof height !== \"number\" || typeof contentTop !== \"number\" || !Number.isFinite(x) || !Number.isFinite(y) || !Number.isFinite(width) || !Number.isFinite(height) || !Number.isFinite(contentTop) || width <= 0 || height <= 0) {\n    delete lane.groupTitleRect;\n    return;\n  }\n  const top = y - height / 2;\n  const headerBottom = Math.min(contentTop, y + height / 2);\n  const titleHeight = Math.min(TOP_LANE_TITLE_BAND_HEIGHT, Math.max(0, headerBottom - top));\n  const bottom = top + titleHeight;\n  if (bottom <= top) {\n    delete lane.groupTitleRect;\n    return;\n  }\n  lane.groupTitleRect = {\n    left: x - width / 2,\n    right: x + width / 2,\n    top,\n    bottom\n  };\n}\nchunk_Y2CYZVJY_name(assignTopLaneTitleRect, \"assignTopLaneTitleRect\");\nfunction prepareLayoutForSwimlanes(layout) {\n  const direction = layout.direction;\n  const nodes = layout.nodes ??= [];\n  for (const node of layout.nodes ?? []) {\n    if (node.isGroup && !node.parentId) {\n      node.shape = \"swimlane\";\n      if (direction) {\n        node.direction = direction;\n      }\n    }\n  }\n  const looseNodes = nodes.filter((node) => !node.isGroup && !node.parentId);\n  if (looseNodes.length === 0) {\n    return;\n  }\n  let defaultLane = nodes.find((node) => node.id === DEFAULT_SWIMLANE_ID);\n  if (!defaultLane) {\n    defaultLane = {\n      id: DEFAULT_SWIMLANE_ID,\n      label: \"\",\n      isGroup: true,\n      shape: \"swimlane\",\n      padding: 20,\n      ...direction ? { direction } : {}\n    };\n    nodes.push(defaultLane);\n  } else if (defaultLane.isGroup) {\n    defaultLane.shape = \"swimlane\";\n    if (direction) {\n      defaultLane.direction = direction;\n    }\n  }\n  for (const node of looseNodes) {\n    node.parentId = DEFAULT_SWIMLANE_ID;\n  }\n}\nchunk_Y2CYZVJY_name(prepareLayoutForSwimlanes, \"prepareLayoutForSwimlanes\");\nfunction toGraphView(layout) {\n  const nodeById = /* @__PURE__ */ new Map();\n  for (const n of layout.nodes ?? []) {\n    nodeById.set(n.id, n);\n  }\n  const edges = [];\n  for (const e of layout.edges ?? []) {\n    const src = typeof e.start === \"string\" ? e.start : void 0;\n    const dst = typeof e.end === \"string\" ? e.end : void 0;\n    if (!src || !dst) {\n      continue;\n    }\n    if (e.labelNodeId) {\n      continue;\n    }\n    edges.push({ id: e.id, src, dst, ref: e });\n  }\n  const allNodes = layout.nodes ?? [];\n  const groupNodes = allNodes.filter((n) => n.isGroup);\n  const nonGroupNodes = allNodes.filter((n) => !n.isGroup);\n  const nodesInGroupOrder = [...groupNodes].reverse();\n  const nodes = [...nodesInGroupOrder, ...nonGroupNodes].map((n) => n.id);\n  return { nodes, edges, layout, nodeById };\n}\nchunk_Y2CYZVJY_name(toGraphView, \"toGraphView\");\nfunction writeBackToLayoutData(g, ordered, coords, opts) {\n  const { layout } = g;\n  const nodeMap = g.nodeById;\n  const layerGap = opts?.layerGap ?? 100;\n  const nodeGap = opts?.nodeGap ?? 40;\n  let layerIndex = 0;\n  for (const layer of ordered.layers) {\n    let orderIndex = 0;\n    for (const id of layer) {\n      const node = nodeMap.get(id);\n      if (!node) {\n        orderIndex++;\n        continue;\n      }\n      node.layer = layerIndex;\n      node.order = orderIndex;\n      const x = coords.x[id] ?? orderIndex * nodeGap;\n      const y = coords.y[id] ?? layerIndex * layerGap;\n      node.x = x;\n      node.y = y;\n      orderIndex++;\n    }\n    layerIndex++;\n  }\n  const allNodes = layout.nodes ?? [];\n  const groupBounds = /* @__PURE__ */ new Map();\n  const topLevelGroups = [];\n  for (const group of allNodes) {\n    if (!group?.isGroup) {\n      continue;\n    }\n    if (!group.parentId) {\n      topLevelGroups.push(group);\n    }\n    const children = allNodes.filter((n) => n.parentId === group.id);\n    let minX = Infinity;\n    let maxX = -Infinity;\n    let minY = Infinity;\n    let maxY = -Infinity;\n    for (const child of children) {\n      const cx = child.x ?? coords.x[child.id];\n      const cy = child.y ?? coords.y[child.id];\n      const cw = child.width ?? 0;\n      const ch = child.height ?? 0;\n      if (cx != null && cy != null) {\n        minX = Math.min(minX, cx - cw / 2);\n        maxX = Math.max(maxX, cx + cw / 2);\n        minY = Math.min(minY, cy - ch / 2);\n        maxY = Math.max(maxY, cy + ch / 2);\n      }\n    }\n    if (minX === Infinity || minY === Infinity) {\n      group.x = group.x ?? 0;\n      group.y = group.y ?? 0;\n      group.width = group.width ?? 0;\n      group.height = group.height ?? 0;\n    } else {\n      const pad = group.padding ?? 20;\n      const horizontalPad = group.parentId ? pad : 2 * topLaneHorizontalPadding(group);\n      const verticalPad = pad;\n      const w = Math.max(0, maxX - minX) + horizontalPad;\n      const h = Math.max(0, maxY - minY) + verticalPad;\n      const cx = (minX + maxX) / 2;\n      const cy = (minY + maxY) / 2;\n      group.x = cx;\n      group.y = cy;\n      group.width = w;\n      group.height = h;\n      groupBounds.set(group.id, { minX, maxX, minY, maxY });\n    }\n  }\n  if (topLevelGroups.length > 0 && groupBounds.size > 0) {\n    let globalMinY = Infinity;\n    let globalMaxY = -Infinity;\n    let maxPad = 0;\n    for (const lane of topLevelGroups) {\n      const pad = lane.padding ?? 20;\n      if (pad > maxPad) {\n        maxPad = pad;\n      }\n      const b = groupBounds.get(lane.id);\n      if (!b) {\n        continue;\n      }\n      globalMinY = Math.min(globalMinY, b.minY);\n      globalMaxY = Math.max(globalMaxY, b.maxY);\n    }\n    if (globalMinY !== Infinity && globalMaxY !== -Infinity) {\n      const contentHeight = Math.max(0, globalMaxY - globalMinY);\n      const minHeaderMargin = 36;\n      const verticalMargin = Math.max(maxPad, minHeaderMargin);\n      const laneHeight = contentHeight + 2 * verticalMargin;\n      const centerY = (globalMinY + globalMaxY) / 2;\n      for (const lane of topLevelGroups) {\n        lane.y = centerY;\n        lane.height = laneHeight;\n        lane.swimlaneContentTop = globalMinY;\n      }\n      const sortedLanes = [...topLevelGroups].sort((a, b) => {\n        const ax = a.x ?? 0;\n        const bx = b.x ?? 0;\n        return ax - bx;\n      });\n      const laneIds = [];\n      const centers = [];\n      const baseWidths = [];\n      for (const lane of sortedLanes) {\n        const b = groupBounds.get(lane.id);\n        if (!b) {\n          continue;\n        }\n        const contentWidth = Math.max(0, b.maxX - b.minX) + 2 * topLaneHorizontalPadding(lane);\n        const cx = (b.minX + b.maxX) / 2;\n        laneIds.push(lane.id);\n        centers.push(cx);\n        baseWidths.push(contentWidth);\n      }\n      const count = laneIds.length;\n      if (count > 0) {\n        const laneWidths = /* @__PURE__ */ new Map();\n        if (count === 1) {\n          laneWidths.set(laneIds[0], baseWidths[0]);\n        } else {\n          const d = [];\n          for (let i = 0; i < count - 1; i++) {\n            d.push(centers[i + 1] - centers[i]);\n          }\n          const u = new Array(count);\n          u[0] = 0;\n          for (let i = 0; i < count - 1; i++) {\n            u[i + 1] = 2 * d[i] - u[i];\n          }\n          let lowerBound = 0;\n          let upperBound = Number.POSITIVE_INFINITY;\n          for (let i = 0; i < count; i++) {\n            const baseW = baseWidths[i];\n            if (i % 2 === 0) {\n              lowerBound = Math.max(lowerBound, baseW - u[i]);\n            } else {\n              upperBound = Math.min(upperBound, u[i] - baseW);\n            }\n          }\n          let x = lowerBound;\n          if (lowerBound <= upperBound) {\n            x = (lowerBound + upperBound) / 2;\n          } else {\n            x = lowerBound;\n          }\n          for (let i = 0; i < count; i++) {\n            const w = u[i] + (i % 2 === 0 ? x : -x);\n            const finalWidth = Math.max(baseWidths[i], w);\n            laneWidths.set(laneIds[i], finalWidth);\n          }\n        }\n        for (const lane of topLevelGroups) {\n          const w = laneWidths.get(lane.id);\n          if (w != null) {\n            lane.width = w;\n          }\n          assignTopLaneTitleRect(lane);\n        }\n      }\n    }\n  }\n}\nchunk_Y2CYZVJY_name(writeBackToLayoutData, \"writeBackToLayoutData\");\n\n// src/rendering-util/layout-algorithms/swimlanes/edgeLabelNodes.ts\nvar EDGE_LABEL_LOG_PREFIX = \"[EdgeLabelNodes]\";\nfunction createEdgeLabelNodes(data) {\n  const nodesToAdd = [];\n  const layoutOnlyEdges = [];\n  const nodeById = /* @__PURE__ */ new Map();\n  for (const node of data.nodes) {\n    nodeById.set(node.id, node);\n  }\n  for (const edge of data.edges) {\n    if (!edge.label || edge.label.length === 0) {\n      continue;\n    }\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    if (edge.labelNodeId) {\n      continue;\n    }\n    const sourceNode = edge.start ? nodeById.get(edge.start) : void 0;\n    const targetNode = edge.end ? nodeById.get(edge.end) : void 0;\n    if (!sourceNode || !targetNode) {\n      log.warn(EDGE_LABEL_LOG_PREFIX, `Edge ${edge.id} has missing source or target node`);\n      continue;\n    }\n    const labelNodeId = `edge-label-${edge.start}-${edge.end}-${edge.id}`;\n    const isCrossLane = sourceNode.parentId !== targetNode.parentId;\n    const labelLane = isCrossLane ? targetNode.parentId : sourceNode.parentId;\n    const labelNode = {\n      id: labelNodeId,\n      label: edge.label,\n      edgeStart: edge.start ?? \"\",\n      edgeEnd: edge.end ?? \"\",\n      shape: \"labelRect\",\n      width: 0,\n      // populated when rendered / applied from fixture\n      height: 0,\n      isEdgeLabel: true,\n      isDummy: true,\n      parentId: labelLane,\n      isGroup: false,\n      labelStyle: Array.isArray(edge.labelStyle) ? edge.labelStyle[0] : edge.labelStyle ?? \"\",\n      ...sourceNode.dir ? { dir: sourceNode.dir } : {}\n    };\n    nodesToAdd.push(labelNode);\n    edge.labelNodeId = labelNodeId;\n    edge.label = void 0;\n    edge.text = void 0;\n    const toLabelVirtual = {\n      id: `${edge.id}-to-label`,\n      start: edge.start,\n      end: labelNodeId,\n      type: \"normal\",\n      isLayoutOnly: true\n    };\n    const fromLabelVirtual = {\n      id: `${edge.id}-from-label`,\n      start: labelNodeId,\n      end: edge.end,\n      type: \"normal\",\n      isLayoutOnly: true\n    };\n    layoutOnlyEdges.push(toLabelVirtual, fromLabelVirtual);\n  }\n  const newNodes = [...data.nodes, ...nodesToAdd];\n  const newEdges = [...data.edges, ...layoutOnlyEdges];\n  return {\n    ...data,\n    nodes: newNodes,\n    edges: newEdges\n  };\n}\nchunk_Y2CYZVJY_name(createEdgeLabelNodes, \"createEdgeLabelNodes\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/geometry.ts\nvar EPS = 1e-3;\nfunction measuredNodeRect(node) {\n  const cx = node.x ?? 0;\n  const cy = node.y ?? 0;\n  const width = node.width ?? 0;\n  const height = node.height ?? 0;\n  return width > 0 && height > 0 ? { cx, cy, rect: rectFromCenterSize(cx, cy, width, height) } : void 0;\n}\nchunk_Y2CYZVJY_name(measuredNodeRect, \"measuredNodeRect\");\nfunction nodeBoundsInfoFor(node) {\n  if (node.isGroup) {\n    return void 0;\n  }\n  const measured = measuredNodeRect(node);\n  if (!measured) {\n    return void 0;\n  }\n  const id = String(node.id ?? \"\");\n  return {\n    id,\n    cx: measured.cx,\n    cy: measured.cy,\n    rect: measured.rect\n  };\n}\nchunk_Y2CYZVJY_name(nodeBoundsInfoFor, \"nodeBoundsInfoFor\");\nfunction samePoint(a, b, epsilon = EPS) {\n  return Math.abs(a.x - b.x) < epsilon && Math.abs(a.y - b.y) < epsilon;\n}\nchunk_Y2CYZVJY_name(samePoint, \"samePoint\");\nfunction sameX(a, b, epsilon = EPS) {\n  return Math.abs(a.x - b.x) < epsilon;\n}\nchunk_Y2CYZVJY_name(sameX, \"sameX\");\nfunction sameY(a, b, epsilon = EPS) {\n  return Math.abs(a.y - b.y) < epsilon;\n}\nchunk_Y2CYZVJY_name(sameY, \"sameY\");\nfunction isHorizontalSegment(a, b, epsilon = EPS) {\n  return sameY(a, b, epsilon) && Math.abs(a.x - b.x) > epsilon;\n}\nchunk_Y2CYZVJY_name(isHorizontalSegment, \"isHorizontalSegment\");\nfunction isVerticalSegment(a, b, epsilon = EPS) {\n  return sameX(a, b, epsilon) && Math.abs(a.y - b.y) > epsilon;\n}\nchunk_Y2CYZVJY_name(isVerticalSegment, \"isVerticalSegment\");\nfunction overlapLength(a1, a2, b1, b2) {\n  return Math.max(\n    0,\n    Math.min(Math.max(a1, a2), Math.max(b1, b2)) - Math.max(Math.min(a1, a2), Math.min(b1, b2))\n  );\n}\nchunk_Y2CYZVJY_name(overlapLength, \"overlapLength\");\nfunction sameAxisSegmentOverlapLength(a, b, epsilon = EPS) {\n  if (a.horizontal && b.horizontal && sameY(a.a, b.a, epsilon)) {\n    return overlapLength(a.a.x, a.b.x, b.a.x, b.b.x);\n  }\n  if (a.vertical && b.vertical && sameX(a.a, b.a, epsilon)) {\n    return overlapLength(a.a.y, a.b.y, b.a.y, b.b.y);\n  }\n  return 0;\n}\nchunk_Y2CYZVJY_name(sameAxisSegmentOverlapLength, \"sameAxisSegmentOverlapLength\");\nfunction orthogonalSegmentsForPoints(points, epsilon = EPS) {\n  const result = [];\n  for (let i = 0; i < points.length - 1; i++) {\n    const a = points[i];\n    const b = points[i + 1];\n    const horizontal = isHorizontalSegment(a, b, epsilon);\n    const vertical = isVerticalSegment(a, b, epsilon);\n    if (horizontal || vertical) {\n      result.push({ index: i, a, b, horizontal, vertical });\n    }\n  }\n  return result;\n}\nchunk_Y2CYZVJY_name(orthogonalSegmentsForPoints, \"orthogonalSegmentsForPoints\");\nfunction countOrthogonalBends(points, epsilon = EPS) {\n  const segments = orthogonalSegmentsForPoints(points, epsilon);\n  let bends = 0;\n  for (let i = 1; i < segments.length; i++) {\n    if (segments[i - 1].horizontal !== segments[i].horizontal) {\n      bends++;\n    }\n  }\n  return bends;\n}\nchunk_Y2CYZVJY_name(countOrthogonalBends, \"countOrthogonalBends\");\nfunction dedupeConsecutivePoints(points, epsilon = EPS) {\n  const result = [];\n  for (const point of points) {\n    const last = result.length > 0 ? result[result.length - 1] : void 0;\n    if (!last || !samePoint(last, point, epsilon)) {\n      result.push({ x: point.x, y: point.y });\n    }\n  }\n  return result;\n}\nchunk_Y2CYZVJY_name(dedupeConsecutivePoints, \"dedupeConsecutivePoints\");\nfunction classifyThreeSegmentRoute(points, epsilon = EPS) {\n  if (!points || points.length !== 4) {\n    return void 0;\n  }\n  const [p0, p1, p2, p3] = points;\n  const isHVH = isHorizontalSegment(p0, p1, epsilon) && isVerticalSegment(p1, p2, epsilon) && isHorizontalSegment(p2, p3, epsilon);\n  if (isHVH) {\n    return { kind: \"HVH\", p0, p1, p2, p3 };\n  }\n  const isVHV = isVerticalSegment(p0, p1, epsilon) && isHorizontalSegment(p1, p2, epsilon) && isVerticalSegment(p2, p3, epsilon);\n  return isVHV ? { kind: \"VHV\", p0, p1, p2, p3 } : void 0;\n}\nchunk_Y2CYZVJY_name(classifyThreeSegmentRoute, \"classifyThreeSegmentRoute\");\nfunction segmentBoundsOverlapRect(a, b, rect, buffer = 0) {\n  const segMinX = Math.min(a.x, b.x);\n  const segMaxX = Math.max(a.x, b.x);\n  const segMinY = Math.min(a.y, b.y);\n  const segMaxY = Math.max(a.y, b.y);\n  return segMaxX > rect.left - buffer && segMinX < rect.right + buffer && segMaxY > rect.top - buffer && segMinY < rect.bottom + buffer;\n}\nchunk_Y2CYZVJY_name(segmentBoundsOverlapRect, \"segmentBoundsOverlapRect\");\nfunction pointInsideRect(point, rect, buffer = 0) {\n  return point.x > rect.left + buffer && point.x < rect.right - buffer && point.y > rect.top + buffer && point.y < rect.bottom - buffer;\n}\nchunk_Y2CYZVJY_name(pointInsideRect, \"pointInsideRect\");\nfunction rectContainsRect(outer, inner) {\n  return outer.left <= inner.left && outer.right >= inner.right && outer.top <= inner.top && outer.bottom >= inner.bottom;\n}\nchunk_Y2CYZVJY_name(rectContainsRect, \"rectContainsRect\");\nfunction rectsOverlap(a, b) {\n  return a.left < b.right && a.right > b.left && a.top < b.bottom && a.bottom > b.top;\n}\nchunk_Y2CYZVJY_name(rectsOverlap, \"rectsOverlap\");\nfunction inflateRect(rect, margin) {\n  return {\n    left: rect.left - margin,\n    right: rect.right + margin,\n    top: rect.top - margin,\n    bottom: rect.bottom + margin\n  };\n}\nchunk_Y2CYZVJY_name(inflateRect, \"inflateRect\");\nfunction rectFromCenterSize(cx, cy, width, height) {\n  return {\n    left: cx - width / 2,\n    right: cx + width / 2,\n    top: cy - height / 2,\n    bottom: cy + height / 2\n  };\n}\nchunk_Y2CYZVJY_name(rectFromCenterSize, \"rectFromCenterSize\");\nfunction rectOfNodeBounds(node) {\n  return measuredNodeRect(node)?.rect;\n}\nchunk_Y2CYZVJY_name(rectOfNodeBounds, \"rectOfNodeBounds\");\nfunction portForRectSide(node, side) {\n  switch (side) {\n    case \"top\":\n      return { x: node.cx, y: node.rect.top };\n    case \"bottom\":\n      return { x: node.cx, y: node.rect.bottom };\n    case \"left\":\n      return { x: node.rect.left, y: node.cy };\n    case \"right\":\n      return { x: node.rect.right, y: node.cy };\n  }\n}\nchunk_Y2CYZVJY_name(portForRectSide, \"portForRectSide\");\nfunction buildOrthogonalPortPath(src, srcSide, dst, dstSide, anchor, epsilon = EPS) {\n  const srcH = srcSide === \"left\" || srcSide === \"right\";\n  const dstH = dstSide === \"left\" || dstSide === \"right\";\n  if (srcH && dstH) {\n    const opposingDir = srcSide === \"right\" && dstSide === \"left\" && src.x < dst.x || srcSide === \"left\" && dstSide === \"right\" && src.x > dst.x;\n    if (opposingDir) {\n      if (sameY(src, dst, epsilon)) {\n        return [src, dst];\n      }\n      const midX = (src.x + dst.x) / 2;\n      return [src, { x: midX, y: src.y }, { x: midX, y: dst.y }, dst];\n    }\n    if (srcSide === dstSide) {\n      if (sameY(src, dst, epsilon)) {\n        return void 0;\n      }\n      const intX = srcSide === \"left\" ? Math.min(src.x, dst.x) - anchor : Math.max(src.x, dst.x) + anchor;\n      return [src, { x: intX, y: src.y }, { x: intX, y: dst.y }, dst];\n    }\n    return void 0;\n  }\n  if (!srcH && !dstH) {\n    if (srcSide === dstSide) {\n      if (sameX(src, dst, epsilon)) {\n        return void 0;\n      }\n      const intY = srcSide === \"top\" ? Math.min(src.y, dst.y) - anchor : Math.max(src.y, dst.y) + anchor;\n      return [src, { x: src.x, y: intY }, { x: dst.x, y: intY }, dst];\n    }\n    const sameDir = srcSide === \"bottom\" && dstSide === \"top\" && src.y < dst.y || srcSide === \"top\" && dstSide === \"bottom\" && src.y > dst.y;\n    if (!sameDir) {\n      return void 0;\n    }\n    if (sameX(src, dst, epsilon)) {\n      return [src, dst];\n    }\n    const midY = (src.y + dst.y) / 2;\n    return [src, { x: src.x, y: midY }, { x: dst.x, y: midY }, dst];\n  }\n  if (srcH && !dstH) {\n    const sameDirSrc2 = srcSide === \"right\" && dst.x > src.x || srcSide === \"left\" && dst.x < src.x;\n    const sameDirDst2 = dstSide === \"top\" && src.y < dst.y || dstSide === \"bottom\" && src.y > dst.y;\n    return sameDirSrc2 && sameDirDst2 ? [src, { x: dst.x, y: src.y }, dst] : void 0;\n  }\n  const sameDirSrc = srcSide === \"bottom\" && dst.y > src.y || srcSide === \"top\" && dst.y < src.y;\n  const sameDirDst = dstSide === \"left\" && src.x < dst.x || dstSide === \"right\" && src.x > dst.x;\n  return sameDirSrc && sameDirDst ? [src, { x: src.x, y: dst.y }, dst] : void 0;\n}\nchunk_Y2CYZVJY_name(buildOrthogonalPortPath, \"buildOrthogonalPortPath\");\nfunction buildSameSideTrackPath(src, side, dst, track) {\n  return side === \"left\" || side === \"right\" ? [src, { x: track, y: src.y }, { x: track, y: dst.y }, dst] : [src, { x: src.x, y: track }, { x: dst.x, y: track }, dst];\n}\nchunk_Y2CYZVJY_name(buildSameSideTrackPath, \"buildSameSideTrackPath\");\nfunction collectRealNodeBounds(nodes) {\n  const nodeInfoById = /* @__PURE__ */ new Map();\n  const realNodeRects = [];\n  for (const node of nodes) {\n    if (node.isEdgeLabel) {\n      continue;\n    }\n    const info = nodeBoundsInfoFor(node);\n    if (!info) {\n      continue;\n    }\n    nodeInfoById.set(info.id, info);\n    realNodeRects.push({ id: info.id, rect: info.rect });\n  }\n  return { nodeInfoById, realNodeRects };\n}\nchunk_Y2CYZVJY_name(collectRealNodeBounds, \"collectRealNodeBounds\");\nfunction collectNodeRectEntries(nodes) {\n  const realNodeRects = [];\n  const labelNodeRects = [];\n  for (const node of nodes) {\n    const info = nodeBoundsInfoFor(node);\n    if (!info) {\n      continue;\n    }\n    const entry = { id: info.id, rect: info.rect };\n    if (node.isEdgeLabel) {\n      labelNodeRects.push(entry);\n    } else {\n      realNodeRects.push(entry);\n    }\n  }\n  return { realNodeRects, labelNodeRects };\n}\nchunk_Y2CYZVJY_name(collectNodeRectEntries, \"collectNodeRectEntries\");\nfunction collectLayoutNodeRects(nodes, { includeEdgeLabels = true } = {}) {\n  const result = [];\n  for (const node of nodes) {\n    if (node.isGroup || !includeEdgeLabels && node.isEdgeLabel) {\n      continue;\n    }\n    const cx = node.x ?? 0;\n    const cy = node.y ?? 0;\n    const width = node.width ?? 0;\n    const height = node.height ?? 0;\n    result.push({\n      nodeId: node.id,\n      ...rectFromCenterSize(cx, cy, width, height)\n    });\n  }\n  return result;\n}\nchunk_Y2CYZVJY_name(collectLayoutNodeRects, \"collectLayoutNodeRects\");\nfunction getNodePairGeometry(edge, nodeInfoById, epsilon = EPS) {\n  const srcId = edge.start;\n  const dstId = edge.end;\n  if (!srcId || !dstId) {\n    return void 0;\n  }\n  const srcInfo = nodeInfoById.get(srcId);\n  const dstInfo = nodeInfoById.get(dstId);\n  if (!srcInfo || !dstInfo) {\n    return void 0;\n  }\n  return {\n    srcId,\n    dstId,\n    srcInfo,\n    dstInfo,\n    collinearX: Math.abs(srcInfo.cx - dstInfo.cx) < epsilon,\n    collinearY: Math.abs(srcInfo.cy - dstInfo.cy) < epsilon\n  };\n}\nchunk_Y2CYZVJY_name(getNodePairGeometry, \"getNodePairGeometry\");\nfunction segmentHitsAnyRect(a, b, rects, excludeIds = [], shrink = 0) {\n  for (const entry of rects) {\n    if (excludeIds.includes(entry.id)) {\n      continue;\n    }\n    if (segmentBoundsOverlapRect(a, b, entry.rect, -shrink)) {\n      return true;\n    }\n  }\n  return false;\n}\nchunk_Y2CYZVJY_name(segmentHitsAnyRect, \"segmentHitsAnyRect\");\nfunction orthogonalSegmentsCross(a1, b1, a2, b2, epsilon = EPS, endpointTolerance = 1e-6) {\n  const s1H = sameY(a1, b1, epsilon);\n  const s1V = sameX(a1, b1, epsilon);\n  const s2H = sameY(a2, b2, epsilon);\n  const s2V = sameX(a2, b2, epsilon);\n  if (s1H && s2H || s1V && s2V) {\n    return false;\n  }\n  if (!(s1H || s1V) || !(s2H || s2V)) {\n    return false;\n  }\n  const horiz = s1H ? { a: a1, b: b1 } : { a: a2, b: b2 };\n  const vert = s1V ? { a: a1, b: b1 } : { a: a2, b: b2 };\n  const hY = horiz.a.y;\n  const hX1 = Math.min(horiz.a.x, horiz.b.x);\n  const hX2 = Math.max(horiz.a.x, horiz.b.x);\n  const vX = vert.a.x;\n  const vY1 = Math.min(vert.a.y, vert.b.y);\n  const vY2 = Math.max(vert.a.y, vert.b.y);\n  if (vX < hX1 || vX > hX2 || hY < vY1 || hY > vY2) {\n    return false;\n  }\n  const matchesHorizEndpoint = Math.abs(vX - horiz.a.x) < endpointTolerance && Math.abs(hY - horiz.a.y) < endpointTolerance || Math.abs(vX - horiz.b.x) < endpointTolerance && Math.abs(hY - horiz.b.y) < endpointTolerance;\n  const matchesVertEndpoint = Math.abs(vX - vert.a.x) < endpointTolerance && Math.abs(hY - vert.a.y) < endpointTolerance || Math.abs(vX - vert.b.x) < endpointTolerance && Math.abs(hY - vert.b.y) < endpointTolerance;\n  return !(matchesHorizEndpoint && matchesVertEndpoint);\n}\nchunk_Y2CYZVJY_name(orthogonalSegmentsCross, \"orthogonalSegmentsCross\");\nfunction sameAxisSegmentsOverlap(a1, b1, a2, b2, epsilon = EPS) {\n  const s1H = sameY(a1, b1, epsilon);\n  const s1V = sameX(a1, b1, epsilon);\n  const s2H = sameY(a2, b2, epsilon);\n  const s2V = sameX(a2, b2, epsilon);\n  if (s1V && s2V && sameX(a1, a2, epsilon)) {\n    return overlapLength(a1.y, b1.y, a2.y, b2.y) > epsilon;\n  }\n  if (s1H && s2H && sameY(a1, a2, epsilon)) {\n    return overlapLength(a1.x, b1.x, a2.x, b2.x) > epsilon;\n  }\n  return false;\n}\nchunk_Y2CYZVJY_name(sameAxisSegmentsOverlap, \"sameAxisSegmentsOverlap\");\nfunction segmentConflictsWithAnyEdge(a, b, edges, excludeEdge, {\n  epsilon = EPS,\n  skipDegenerateOther = false\n} = {}) {\n  for (const other of edges) {\n    if (other === excludeEdge || other.isLayoutOnly) {\n      continue;\n    }\n    const points = other.points;\n    if (!points || points.length < 2) {\n      continue;\n    }\n    for (let i = 0; i < points.length - 1; i++) {\n      const oa = points[i];\n      const ob = points[i + 1];\n      if (skipDegenerateOther && samePoint(oa, ob, epsilon)) {\n        continue;\n      }\n      if (orthogonalSegmentsCross(a, b, oa, ob, epsilon) || sameAxisSegmentsOverlap(a, b, oa, ob, epsilon)) {\n        return true;\n      }\n    }\n  }\n  return false;\n}\nchunk_Y2CYZVJY_name(segmentConflictsWithAnyEdge, \"segmentConflictsWithAnyEdge\");\nfunction orthogonalSegmentsStrictlyCross(a1, b1, a2, b2, epsilon = EPS) {\n  const aHoriz = sameY(a1, b1, epsilon);\n  const aVert = sameX(a1, b1, epsilon);\n  const bHoriz = sameY(a2, b2, epsilon);\n  const bVert = sameX(a2, b2, epsilon);\n  if (!(aHoriz && bVert || aVert && bHoriz)) {\n    return false;\n  }\n  const horiz = aHoriz ? { a: a1, b: b1 } : { a: a2, b: b2 };\n  const vert = aHoriz ? { a: a2, b: b2 } : { a: a1, b: b1 };\n  const hY = horiz.a.y;\n  const hXmin = Math.min(horiz.a.x, horiz.b.x);\n  const hXmax = Math.max(horiz.a.x, horiz.b.x);\n  const vX = vert.a.x;\n  const vYmin = Math.min(vert.a.y, vert.b.y);\n  const vYmax = Math.max(vert.a.y, vert.b.y);\n  return vX > hXmin + epsilon && vX < hXmax - epsilon && hY > vYmin + epsilon && hY < vYmax - epsilon;\n}\nchunk_Y2CYZVJY_name(orthogonalSegmentsStrictlyCross, \"orthogonalSegmentsStrictlyCross\");\nfunction strictlyBetween(value, a, b) {\n  const lo = Math.min(a, b);\n  const hi = Math.max(a, b);\n  return value > lo + EPS && value < hi - EPS;\n}\nchunk_Y2CYZVJY_name(strictlyBetween, \"strictlyBetween\");\nfunction isCollinearIntermediate(prev, cur, next) {\n  if (sameX(prev, cur) && sameX(cur, next)) {\n    return strictlyBetween(cur.y, prev.y, next.y);\n  }\n  if (sameY(prev, cur) && sameY(cur, next)) {\n    return strictlyBetween(cur.x, prev.x, next.x);\n  }\n  return false;\n}\nchunk_Y2CYZVJY_name(isCollinearIntermediate, \"isCollinearIntermediate\");\nfunction simplifyPolylineOnce(points) {\n  let changed = false;\n  const out = [];\n  for (let i = 0; i < points.length; i++) {\n    const prev = out[out.length - 1];\n    const cur = points[i];\n    const next = i + 1 < points.length ? points[i + 1] : void 0;\n    if (prev && next) {\n      if (samePoint(prev, next)) {\n        i++;\n        changed = true;\n        continue;\n      }\n      if (isCollinearIntermediate(prev, cur, next)) {\n        changed = true;\n        continue;\n      }\n    }\n    out.push(cur);\n  }\n  return { points: out, changed };\n}\nchunk_Y2CYZVJY_name(simplifyPolylineOnce, \"simplifyPolylineOnce\");\nfunction orthogonalizePolyline(pts) {\n  const cleaned = [pts[0]];\n  for (let i = 1; i < pts.length; i++) {\n    const prev = cleaned[cleaned.length - 1];\n    const curr = pts[i];\n    if (!sameX(prev, curr) && !sameY(prev, curr)) {\n      const prevPrev = cleaned.length >= 2 ? cleaned[cleaned.length - 2] : void 0;\n      const incomingVertical = prevPrev ? sameX(prevPrev, prev) : false;\n      const corner = incomingVertical ? { x: prev.x, y: curr.y } : { x: curr.x, y: prev.y };\n      cleaned.push(corner);\n    }\n    cleaned.push(curr);\n  }\n  const deduped = [];\n  for (const p of cleaned) {\n    const last = deduped[deduped.length - 1];\n    if (!last || !samePoint(last, p)) {\n      deduped.push(p);\n    }\n  }\n  return deduped;\n}\nchunk_Y2CYZVJY_name(orthogonalizePolyline, \"orthogonalizePolyline\");\nfunction simplifyPolyline(pts) {\n  if (pts.length < 3) {\n    return pts;\n  }\n  let work = [...pts];\n  for (let guard = 0; guard < 32; guard++) {\n    const result = simplifyPolylineOnce(work);\n    work = result.points;\n    if (!result.changed) {\n      break;\n    }\n  }\n  return work;\n}\nchunk_Y2CYZVJY_name(simplifyPolyline, \"simplifyPolyline\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/endpointClip.ts\nvar EPS2 = 1e-3;\nvar INSIDE_EPS = 0.5;\nvar CORNER_CLEARANCE = 4;\nfunction endpointContextFor(edge, nodeByIdMap, minPoints) {\n  const candidate = edge;\n  if (candidate.isLayoutOnly || !candidate.points || candidate.points.length < minPoints) {\n    return void 0;\n  }\n  const src = candidate.start ? nodeByIdMap.get(candidate.start) : void 0;\n  const dst = candidate.end ? nodeByIdMap.get(candidate.end) : void 0;\n  return {\n    edge: candidate,\n    points: candidate.points,\n    srcRect: src ? rectOfNodeBounds(src) : void 0,\n    dstRect: dst ? rectOfNodeBounds(dst) : void 0\n  };\n}\nchunk_Y2CYZVJY_name(endpointContextFor, \"endpointContextFor\");\nfunction segmentEnterPoint(outside, inside, r) {\n  if (sameY(outside, inside, EPS2)) {\n    const x = outside.x < r.left ? r.left : r.right;\n    return { x, y: outside.y };\n  }\n  if (sameX(outside, inside, EPS2)) {\n    const y = outside.y < r.top ? r.top : r.bottom;\n    return { x: outside.x, y };\n  }\n  return {\n    x: Math.min(r.right, Math.max(r.left, outside.x)),\n    y: Math.min(r.bottom, Math.max(r.top, outside.y))\n  };\n}\nchunk_Y2CYZVJY_name(segmentEnterPoint, \"segmentEnterPoint\");\nfunction clipEndpoint(points, rect, atStart) {\n  const step = atStart ? 1 : -1;\n  let outsideIndex = atStart ? 0 : points.length - 1;\n  while (outsideIndex >= 0 && outsideIndex < points.length && pointInsideRect(points[outsideIndex], rect, INSIDE_EPS)) {\n    outsideIndex += step;\n  }\n  if (outsideIndex < 0 || outsideIndex >= points.length) {\n    return points;\n  }\n  const insideIndex = outsideIndex - step;\n  if (insideIndex < 0 || insideIndex >= points.length) {\n    return points;\n  }\n  const entry = segmentEnterPoint(points[outsideIndex], points[insideIndex], rect);\n  return atStart ? [entry, ...points.slice(outsideIndex)] : [...points.slice(0, outsideIndex + 1), entry];\n}\nchunk_Y2CYZVJY_name(clipEndpoint, \"clipEndpoint\");\nfunction clipEdgeEndpointsToNodeBoundaries(edges, nodeByIdMap) {\n  for (const edge of edges) {\n    const context = endpointContextFor(edge, nodeByIdMap, 2);\n    if (!context) {\n      continue;\n    }\n    let next = [...context.points];\n    if (context.srcRect) {\n      next = clipEndpoint(next, context.srcRect, true);\n    }\n    if (context.dstRect) {\n      next = clipEndpoint(next, context.dstRect, false);\n    }\n    next = simplifyPolyline(orthogonalizePolyline(next));\n    next = clearStraightEndpointCornerConnections(next, context.srcRect, context.dstRect);\n    context.edge.points = simplifyPolyline(orthogonalizePolyline(next));\n  }\n}\nchunk_Y2CYZVJY_name(clipEdgeEndpointsToNodeBoundaries, \"clipEdgeEndpointsToNodeBoundaries\");\nfunction snapEndpointToBoundary(inner, endpoint, r, useApproachSide = false) {\n  if (sameY(inner, endpoint, EPS2)) {\n    if (endpoint.y < r.top - EPS2 || endpoint.y > r.bottom + EPS2) {\n      return endpoint;\n    }\n    if (useApproachSide) {\n      if (inner.x < r.left - EPS2) {\n        return { x: r.left, y: inner.y };\n      }\n      if (inner.x > r.right + EPS2) {\n        return { x: r.right, y: inner.y };\n      }\n    }\n    const toLeft = Math.abs(endpoint.x - r.left) <= Math.abs(endpoint.x - r.right);\n    return { x: toLeft ? r.left : r.right, y: inner.y };\n  }\n  if (sameX(inner, endpoint, EPS2)) {\n    if (endpoint.x < r.left - EPS2 || endpoint.x > r.right + EPS2) {\n      return endpoint;\n    }\n    if (useApproachSide) {\n      if (inner.y < r.top - EPS2) {\n        return { x: inner.x, y: r.top };\n      }\n      if (inner.y > r.bottom + EPS2) {\n        return { x: inner.x, y: r.bottom };\n      }\n    }\n    const toTop = Math.abs(endpoint.y - r.top) <= Math.abs(endpoint.y - r.bottom);\n    return { x: inner.x, y: toTop ? r.top : r.bottom };\n  }\n  return endpoint;\n}\nchunk_Y2CYZVJY_name(snapEndpointToBoundary, \"snapEndpointToBoundary\");\nfunction firstDistinctAdjacent(points, endpointIndex, step) {\n  const endpoint = points[endpointIndex];\n  for (let index = endpointIndex + step; index >= 0 && index < points.length; index += step) {\n    const candidate = points[index];\n    if (!samePoint(candidate, endpoint, EPS2)) {\n      return candidate;\n    }\n  }\n  return points[endpointIndex + step];\n}\nchunk_Y2CYZVJY_name(firstDistinctAdjacent, \"firstDistinctAdjacent\");\nfunction cornerClearanceRange(min, max) {\n  const lo = min + CORNER_CLEARANCE;\n  const hi = max - CORNER_CLEARANCE;\n  return lo <= hi ? { lo, hi } : { lo: (min + max) / 2, hi: (min + max) / 2 };\n}\nchunk_Y2CYZVJY_name(cornerClearanceRange, \"cornerClearanceRange\");\nfunction clampToCornerClearance(value, min, max) {\n  const { lo, hi } = cornerClearanceRange(min, max);\n  return Math.min(hi, Math.max(lo, value));\n}\nchunk_Y2CYZVJY_name(clampToCornerClearance, \"clampToCornerClearance\");\nfunction intersectRanges(ranges) {\n  const lo = Math.max(...ranges.map((range) => range.lo));\n  const hi = Math.min(...ranges.map((range) => range.hi));\n  if (lo > hi) {\n    return void 0;\n  }\n  return { lo, hi };\n}\nchunk_Y2CYZVJY_name(intersectRanges, \"intersectRanges\");\nfunction clearanceRangeForSide(r, side) {\n  return side === \"left\" || side === \"right\" ? cornerClearanceRange(r.top, r.bottom) : cornerClearanceRange(r.left, r.right);\n}\nchunk_Y2CYZVJY_name(clearanceRangeForSide, \"clearanceRangeForSide\");\nfunction terminalSideForSegment(endpoint, adjacent, r) {\n  const yWithin = endpoint.y >= r.top - EPS2 && endpoint.y <= r.bottom + EPS2;\n  const xWithin = endpoint.x >= r.left - EPS2 && endpoint.x <= r.right + EPS2;\n  if (sameY(endpoint, adjacent, EPS2) && yWithin) {\n    if (Math.abs(endpoint.x - r.left) < EPS2) {\n      return \"left\";\n    }\n    if (Math.abs(endpoint.x - r.right) < EPS2) {\n      return \"right\";\n    }\n  }\n  if (sameX(endpoint, adjacent, EPS2) && xWithin) {\n    if (Math.abs(endpoint.y - r.top) < EPS2) {\n      return \"top\";\n    }\n    if (Math.abs(endpoint.y - r.bottom) < EPS2) {\n      return \"bottom\";\n    }\n  }\n  return void 0;\n}\nchunk_Y2CYZVJY_name(terminalSideForSegment, \"terminalSideForSegment\");\nfunction isHorizontalSide(side) {\n  return side === \"left\" || side === \"right\";\n}\nchunk_Y2CYZVJY_name(isHorizontalSide, \"isHorizontalSide\");\nfunction straightClearanceRange(start, end, srcRect, dstRect, horizontal) {\n  const ranges = [];\n  const srcSide = srcRect ? terminalSideForSegment(start, end, srcRect) : void 0;\n  const dstSide = dstRect ? terminalSideForSegment(end, start, dstRect) : void 0;\n  if (srcRect && srcSide && isHorizontalSide(srcSide) === horizontal) {\n    ranges.push(clearanceRangeForSide(srcRect, srcSide));\n  }\n  if (dstRect && dstSide && isHorizontalSide(dstSide) === horizontal) {\n    ranges.push(clearanceRangeForSide(dstRect, dstSide));\n  }\n  return ranges.length > 0 ? intersectRanges(ranges) : void 0;\n}\nchunk_Y2CYZVJY_name(straightClearanceRange, \"straightClearanceRange\");\nfunction clearStraightEndpointCornerAxis(start, end, srcRect, dstRect, horizontal) {\n  const range = straightClearanceRange(start, end, srcRect, dstRect, horizontal);\n  if (!range) {\n    return void 0;\n  }\n  const current = horizontal ? start.y : start.x;\n  const next = Math.min(range.hi, Math.max(range.lo, current));\n  if (Math.abs(next - current) < EPS2) {\n    return void 0;\n  }\n  return horizontal ? [\n    { x: start.x, y: next },\n    { x: end.x, y: next }\n  ] : [\n    { x: next, y: start.y },\n    { x: next, y: end.y }\n  ];\n}\nchunk_Y2CYZVJY_name(clearStraightEndpointCornerAxis, \"clearStraightEndpointCornerAxis\");\nfunction clearStraightEndpointCornerConnections(points, srcRect, dstRect) {\n  if (points.length !== 2) {\n    return points;\n  }\n  const [start, end] = points;\n  if (sameY(start, end, EPS2)) {\n    return clearStraightEndpointCornerAxis(start, end, srcRect, dstRect, true) ?? points;\n  }\n  if (sameX(start, end, EPS2)) {\n    return clearStraightEndpointCornerAxis(start, end, srcRect, dstRect, false) ?? points;\n  }\n  return points;\n}\nchunk_Y2CYZVJY_name(clearStraightEndpointCornerConnections, \"clearStraightEndpointCornerConnections\");\nfunction cornerClearedEndpoint(endpoint, r, side) {\n  return isHorizontalSide(side) ? { x: endpoint.x, y: clampToCornerClearance(endpoint.y, r.top, r.bottom) } : { x: clampToCornerClearance(endpoint.x, r.left, r.right), y: endpoint.y };\n}\nchunk_Y2CYZVJY_name(cornerClearedEndpoint, \"cornerClearedEndpoint\");\nfunction moveCollinearEndpointRun(points, endpointIndex, step, endpoint, adjusted, horizontalTerminal) {\n  const next = points.map((point) => ({ ...point }));\n  for (let index = endpointIndex; index >= 0 && index < points.length; index += step) {\n    const point = points[index];\n    if (horizontalTerminal && !sameY(point, endpoint, EPS2)) {\n      break;\n    }\n    if (!horizontalTerminal && !sameX(point, endpoint, EPS2)) {\n      break;\n    }\n    if (horizontalTerminal) {\n      next[index].y = adjusted.y;\n    } else {\n      next[index].x = adjusted.x;\n    }\n  }\n  return next;\n}\nchunk_Y2CYZVJY_name(moveCollinearEndpointRun, \"moveCollinearEndpointRun\");\nfunction clearEndpointCornerConnection(points, r, atStart) {\n  if (points.length < 2) {\n    return points;\n  }\n  const endpointIndex = atStart ? 0 : points.length - 1;\n  const step = atStart ? 1 : -1;\n  const endpoint = points[endpointIndex];\n  const adjacent = firstDistinctAdjacent(points, endpointIndex, step);\n  if (!adjacent) {\n    return points;\n  }\n  const side = terminalSideForSegment(endpoint, adjacent, r);\n  if (!side) {\n    return points;\n  }\n  const horizontalTerminal = isHorizontalSide(side);\n  const adjusted = cornerClearedEndpoint(endpoint, r, side);\n  if (samePoint(endpoint, adjusted, EPS2)) {\n    return points;\n  }\n  return moveCollinearEndpointRun(\n    points,\n    endpointIndex,\n    step,\n    endpoint,\n    adjusted,\n    horizontalTerminal\n  );\n}\nchunk_Y2CYZVJY_name(clearEndpointCornerConnection, \"clearEndpointCornerConnection\");\nfunction borderSideForSegment(a, b, r) {\n  const xWithin = Math.min(a.x, b.x) >= r.left - EPS2 && Math.max(a.x, b.x) <= r.right + EPS2;\n  const yWithin = Math.min(a.y, b.y) >= r.top - EPS2 && Math.max(a.y, b.y) <= r.bottom + EPS2;\n  if (Math.abs(a.y - r.top) < EPS2 && Math.abs(b.y - r.top) < EPS2 && xWithin) {\n    return \"top\";\n  }\n  if (Math.abs(a.y - r.bottom) < EPS2 && Math.abs(b.y - r.bottom) < EPS2 && xWithin) {\n    return \"bottom\";\n  }\n  if (Math.abs(a.x - r.left) < EPS2 && Math.abs(b.x - r.left) < EPS2 && yWithin) {\n    return \"left\";\n  }\n  if (Math.abs(a.x - r.right) < EPS2 && Math.abs(b.x - r.right) < EPS2 && yWithin) {\n    return \"right\";\n  }\n  return void 0;\n}\nchunk_Y2CYZVJY_name(borderSideForSegment, \"borderSideForSegment\");\nfunction leavesOutward(side, from, to, r) {\n  switch (side) {\n    case \"top\":\n      return sameX(from, to, EPS2) && to.y < r.top - EPS2;\n    case \"bottom\":\n      return sameX(from, to, EPS2) && to.y > r.bottom + EPS2;\n    case \"left\":\n      return sameY(from, to, EPS2) && to.x < r.left - EPS2;\n    case \"right\":\n      return sameY(from, to, EPS2) && to.x > r.right + EPS2;\n  }\n}\nchunk_Y2CYZVJY_name(leavesOutward, \"leavesOutward\");\nfunction collapseOwnBorderStub(points, r, atStart) {\n  if (points.length < 3) {\n    return points;\n  }\n  if (atStart) {\n    const side2 = borderSideForSegment(points[0], points[1], r);\n    if (side2 && leavesOutward(side2, points[1], points[2], r)) {\n      return points.slice(1);\n    }\n    return points;\n  }\n  const last = points.length - 1;\n  const side = borderSideForSegment(points[last - 1], points[last], r);\n  if (side && leavesOutward(side, points[last - 1], points[last - 2], r)) {\n    return points.slice(0, last);\n  }\n  return points;\n}\nchunk_Y2CYZVJY_name(collapseOwnBorderStub, \"collapseOwnBorderStub\");\nfunction snapAndCollapseEndpoints(points, srcRect, dstRect) {\n  let next = points;\n  if (srcRect) {\n    const adjacent = firstDistinctAdjacent(next, 0, 1);\n    if (adjacent) {\n      const snapped = snapEndpointToBoundary(adjacent, next[0], srcRect);\n      if (snapped !== next[0]) {\n        next = [snapped, ...next.slice(1)];\n      }\n    }\n    next = collapseOwnBorderStub(next, srcRect, true);\n  }\n  if (dstRect) {\n    const last = next.length - 1;\n    const adjacent = firstDistinctAdjacent(next, last, -1);\n    if (adjacent) {\n      const snapped = snapEndpointToBoundary(adjacent, next[last], dstRect, true);\n      if (snapped !== next[last]) {\n        next = [...next.slice(0, last), snapped];\n      }\n    }\n    next = collapseOwnBorderStub(next, dstRect, false);\n  }\n  const straightCleared = clearStraightEndpointCornerConnections(next, srcRect, dstRect);\n  if (straightCleared !== next || next.length === 2) {\n    return straightCleared;\n  }\n  if (srcRect) {\n    next = clearEndpointCornerConnection(next, srcRect, true);\n  }\n  if (dstRect) {\n    next = clearEndpointCornerConnection(next, dstRect, false);\n  }\n  return next;\n}\nchunk_Y2CYZVJY_name(snapAndCollapseEndpoints, \"snapAndCollapseEndpoints\");\nfunction prepareEdgeEndpointsForRenderer(edges, nodeByIdMap) {\n  for (const edge of edges) {\n    const context = endpointContextFor(edge, nodeByIdMap, 2);\n    if (!context) {\n      continue;\n    }\n    const input = dedupeConsecutivePoints(context.points, EPS2);\n    const newPts = snapAndCollapseEndpoints(input, context.srcRect, context.dstRect);\n    if (newPts.length < 3) {\n      context.edge.points = newPts;\n      continue;\n    }\n    const duplicated = [\n      newPts[0],\n      { ...newPts[0] },\n      ...newPts.slice(1, -1),\n      newPts[newPts.length - 1],\n      { ...newPts[newPts.length - 1] }\n    ];\n    context.edge.points = duplicated;\n  }\n}\nchunk_Y2CYZVJY_name(prepareEdgeEndpointsForRenderer, \"prepareEdgeEndpointsForRenderer\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/lrTransform.ts\nfunction buildNodeMap(nodes) {\n  return new Map(nodes.map((node) => [node.id, node]));\n}\nchunk_Y2CYZVJY_name(buildNodeMap, \"buildNodeMap\");\nfunction resolveTopLevelGroupId(node, nodeById) {\n  let parentId = node.parentId;\n  let topLevelGroupId = null;\n  while (parentId) {\n    const parent = nodeById.get(parentId);\n    if (!parent?.isGroup) {\n      break;\n    }\n    topLevelGroupId = parent.id;\n    parentId = parent.parentId;\n  }\n  return topLevelGroupId;\n}\nchunk_Y2CYZVJY_name(resolveTopLevelGroupId, \"resolveTopLevelGroupId\");\nfunction groupDepth(group, nodeById) {\n  let depth = 0;\n  let parentId = group.parentId;\n  while (parentId) {\n    const parent = nodeById.get(parentId);\n    if (!parent?.isGroup) {\n      break;\n    }\n    depth++;\n    parentId = parent.parentId;\n  }\n  return depth;\n}\nchunk_Y2CYZVJY_name(groupDepth, \"groupDepth\");\nfunction boundsForChildren(children) {\n  let minX = Infinity;\n  let maxX = -Infinity;\n  let minY = Infinity;\n  let maxY = -Infinity;\n  for (const child of children) {\n    const cx = child.x;\n    const cy = child.y;\n    if (typeof cx !== \"number\" || typeof cy !== \"number\") {\n      continue;\n    }\n    const w = child.width ?? 0;\n    const h = child.height ?? 0;\n    minX = Math.min(minX, cx - w / 2);\n    maxX = Math.max(maxX, cx + w / 2);\n    minY = Math.min(minY, cy - h / 2);\n    maxY = Math.max(maxY, cy + h / 2);\n  }\n  if (minX === Infinity || minY === Infinity) {\n    return null;\n  }\n  return { minX, maxX, minY, maxY };\n}\nchunk_Y2CYZVJY_name(boundsForChildren, \"boundsForChildren\");\nfunction applyGroupBounds(group, bounds) {\n  const pad = group.padding ?? 20;\n  group.x = (bounds.minX + bounds.maxX) / 2;\n  group.y = (bounds.minY + bounds.maxY) / 2;\n  group.width = Math.max(0, bounds.maxX - bounds.minX) + pad;\n  group.height = Math.max(0, bounds.maxY - bounds.minY) + pad;\n}\nchunk_Y2CYZVJY_name(applyGroupBounds, \"applyGroupBounds\");\nfunction recomputeNestedGroupBounds(nodes) {\n  const nodeById = buildNodeMap(nodes);\n  const groupsByDepth = nodes.filter((node) => node.isGroup && node.parentId).sort((a, b) => groupDepth(b, nodeById) - groupDepth(a, nodeById));\n  for (const group of groupsByDepth) {\n    const children = nodes.filter((node) => node.parentId === group.id);\n    const bounds = boundsForChildren(children);\n    if (bounds) {\n      applyGroupBounds(group, bounds);\n    }\n  }\n}\nchunk_Y2CYZVJY_name(recomputeNestedGroupBounds, \"recomputeNestedGroupBounds\");\nfunction mirrorAxis(layout, axis) {\n  const nodes = layout.nodes ?? [];\n  const edges = layout.edges ?? [];\n  const contentNodes = nodes.filter((node) => !node.isGroup);\n  let min = Infinity;\n  let max = -Infinity;\n  for (const node of contentNodes) {\n    const value = node[axis];\n    if (typeof value !== \"number\") {\n      continue;\n    }\n    min = Math.min(min, value);\n    max = Math.max(max, value);\n  }\n  if (!Number.isFinite(min) || !Number.isFinite(max)) {\n    return false;\n  }\n  const mirror = /* @__PURE__ */ chunk_Y2CYZVJY_name((value) => min + max - value, \"mirror\");\n  for (const node of nodes) {\n    const value = node[axis];\n    if (typeof value === \"number\") {\n      node[axis] = mirror(value);\n    }\n    const titleRect = node.groupTitleRect;\n    if (titleRect) {\n      node.groupTitleRect = axis === \"x\" ? {\n        ...titleRect,\n        left: mirror(titleRect.right),\n        right: mirror(titleRect.left)\n      } : {\n        ...titleRect,\n        top: mirror(titleRect.bottom),\n        bottom: mirror(titleRect.top)\n      };\n    }\n  }\n  for (const edge of edges) {\n    for (const point of edge.points ?? []) {\n      point[axis] = mirror(point[axis]);\n    }\n  }\n  return true;\n}\nchunk_Y2CYZVJY_name(mirrorAxis, \"mirrorAxis\");\nfunction applyBtDirectionTransform(layout) {\n  const nodes = layout.nodes ?? [];\n  if (!nodes.some((node) => !node.isGroup)) {\n    return true;\n  }\n  return mirrorAxis(layout, \"y\");\n}\nchunk_Y2CYZVJY_name(applyBtDirectionTransform, \"applyBtDirectionTransform\");\nfunction applyLrDirectionTransform(layout, direction = \"LR\") {\n  const nodes = layout.nodes ?? [];\n  const edges = layout.edges ?? [];\n  const contentNodes = nodes.filter((n) => !n.isGroup);\n  let minX = Infinity;\n  let minY = Infinity;\n  for (const n of contentNodes) {\n    const x0 = n.x ?? 0;\n    const y0 = n.y ?? 0;\n    if (x0 < minX) {\n      minX = x0;\n    }\n    if (y0 < minY) {\n      minY = y0;\n    }\n  }\n  if (!Number.isFinite(minX) || !Number.isFinite(minY)) {\n    return false;\n  }\n  const titleBandSize = 36;\n  let totalWidth = 0;\n  let totalHeight = 0;\n  for (const n of contentNodes) {\n    totalWidth += n.width ?? 0;\n    totalHeight += n.height ?? 0;\n  }\n  const avgWidth = totalWidth / contentNodes.length;\n  const avgHeight = totalHeight / contentNodes.length;\n  const horizontalScaleFactor = avgHeight > 0 ? Math.max(1, avgWidth / avgHeight) : 1;\n  for (const n of contentNodes) {\n    const x0 = n.x ?? 0;\n    const y0 = n.y ?? 0;\n    const newX = (y0 - minY) * horizontalScaleFactor + titleBandSize;\n    const newY = x0 - minX;\n    n.x = newX;\n    n.y = newY;\n  }\n  for (const e of edges) {\n    if (!e.points) {\n      continue;\n    }\n    for (const p of e.points) {\n      const x0 = p.x;\n      const y0 = p.y;\n      const newX = (y0 - minY) * horizontalScaleFactor + titleBandSize;\n      const newY = x0 - minX;\n      p.x = newX;\n      p.y = newY;\n    }\n  }\n  recomputeNestedGroupBounds(nodes);\n  const laneNodes = nodes.filter((n) => n.isGroup && !n.parentId);\n  if (laneNodes.length === 0) {\n    if (direction === \"RL\") {\n      mirrorAxis(layout, \"x\");\n    }\n    return true;\n  }\n  const nodeById = buildNodeMap(nodes);\n  const childrenByLane = /* @__PURE__ */ new Map();\n  for (const n of nodes) {\n    if (n.isGroup) {\n      continue;\n    }\n    const laneId = resolveTopLevelGroupId(n, nodeById);\n    if (!laneId) {\n      continue;\n    }\n    const bucket = childrenByLane.get(laneId) ?? [];\n    bucket.push(n);\n    childrenByLane.set(laneId, bucket);\n  }\n  let maxPad = 0;\n  for (const lane of laneNodes) {\n    const pad = lane.padding ?? 0;\n    if (pad > maxPad) {\n      maxPad = pad;\n    }\n  }\n  const laneBounds = [];\n  let globalMinXChild = Infinity;\n  let globalMaxXChild = -Infinity;\n  for (const lane of laneNodes) {\n    const children = childrenByLane.get(lane.id) ?? [];\n    const bounds = boundsForChildren(children);\n    if (!bounds) {\n      continue;\n    }\n    globalMinXChild = Math.min(globalMinXChild, bounds.minX);\n    globalMaxXChild = Math.max(globalMaxXChild, bounds.maxX);\n    laneBounds.push({\n      lane,\n      contentTop: bounds.minY,\n      contentBottom: bounds.maxY,\n      centerY: (bounds.minY + bounds.maxY) / 2\n    });\n  }\n  if (globalMinXChild === Infinity || globalMaxXChild === -Infinity) {\n    return true;\n  }\n  const fullContentWidth = Math.max(0, globalMaxXChild - globalMinXChild);\n  const horizontalMargin = Math.max(maxPad, 10);\n  const bodyWidth = fullContentWidth + 2 * horizontalMargin;\n  const laneWidth = titleBandSize + bodyWidth;\n  const bodyCenter = (globalMinXChild + globalMaxXChild) / 2;\n  const bodyLeft = bodyCenter - bodyWidth / 2;\n  const laneLeft = bodyLeft - titleBandSize;\n  const centerX = laneLeft + laneWidth / 2;\n  const verticalMargin = Math.max(maxPad, titleBandSize);\n  laneBounds.sort((a, b) => a.centerY - b.centerY);\n  for (let i = 0; i < laneBounds.length; i++) {\n    const curr = laneBounds[i];\n    let laneTop;\n    let laneBottom;\n    if (i === 0) {\n      laneTop = curr.contentTop - verticalMargin;\n    } else {\n      const prev = laneBounds[i - 1];\n      laneTop = (prev.contentBottom + curr.contentTop) / 2;\n    }\n    if (i === laneBounds.length - 1) {\n      laneBottom = curr.contentBottom + verticalMargin;\n    } else {\n      const next = laneBounds[i + 1];\n      laneBottom = (curr.contentBottom + next.contentTop) / 2;\n    }\n    const laneHeight = Math.max(0, laneBottom - laneTop);\n    const centerY = (laneTop + laneBottom) / 2;\n    curr.lane.x = centerX;\n    curr.lane.y = centerY;\n    curr.lane.width = laneWidth;\n    curr.lane.height = laneHeight;\n    curr.lane.swimlaneContentTop = curr.contentTop;\n    curr.lane.groupTitleRect = {\n      left: laneLeft,\n      right: laneLeft + titleBandSize,\n      top: laneTop,\n      bottom: laneBottom\n    };\n  }\n  if (direction === \"RL\") {\n    mirrorAxis(layout, \"x\");\n  }\n  return true;\n}\nchunk_Y2CYZVJY_name(applyLrDirectionTransform, \"applyLrDirectionTransform\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/portSwap.ts\nvar EPS3 = 1e-6;\nvar MIN_PORT_SPACING = 8;\nvar PORT_SHIFT = MIN_PORT_SPACING;\nvar TRY_DELTAS = [0, PORT_SHIFT, -PORT_SHIFT, 2 * PORT_SHIFT, -2 * PORT_SHIFT];\nfunction portSwapToLShape(edges, nodes) {\n  const { nodeInfoById, realNodeRects } = collectRealNodeBounds(nodes);\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const pts = edge.points;\n    if (!pts || pts.length < 4) {\n      continue;\n    }\n    const route = classifyThreeSegmentRoute(dedupeConsecutivePoints(pts, EPS3), EPS3);\n    if (!route) {\n      continue;\n    }\n    const { p3 } = route;\n    const isHVH = route.kind === \"HVH\";\n    const nodePair = getNodePairGeometry(edge, nodeInfoById, EPS3);\n    if (!nodePair) {\n      continue;\n    }\n    const { srcId, dstId, srcInfo, dstInfo, collinearX, collinearY } = nodePair;\n    if (collinearX || collinearY) {\n      continue;\n    }\n    let newPts;\n    const srcRect = srcInfo.rect;\n    for (const delta of TRY_DELTAS) {\n      let np0;\n      let np1;\n      let np2;\n      if (isHVH) {\n        const dstBelow = dstInfo.cy > srcInfo.cy;\n        const newSrcY = dstBelow ? srcRect.bottom : srcRect.top;\n        const newSrcX = srcInfo.cx + delta;\n        if (newSrcX <= srcRect.left + EPS3 || newSrcX >= srcRect.right - EPS3) {\n          continue;\n        }\n        np0 = { x: newSrcX, y: newSrcY };\n        np1 = { x: newSrcX, y: p3.y };\n        np2 = { x: p3.x, y: p3.y };\n      } else {\n        const dstEast = dstInfo.cx > srcInfo.cx;\n        const newSrcX = dstEast ? srcRect.right : srcRect.left;\n        const newSrcY = srcInfo.cy + delta;\n        if (newSrcY <= srcRect.top + EPS3 || newSrcY >= srcRect.bottom - EPS3) {\n          continue;\n        }\n        np0 = { x: newSrcX, y: newSrcY };\n        np1 = { x: p3.x, y: newSrcY };\n        np2 = { x: p3.x, y: p3.y };\n      }\n      const firstSegDegenerate = samePoint(np0, np1, EPS3);\n      const secondSegDegenerate = samePoint(np1, np2, EPS3);\n      if (firstSegDegenerate && secondSegDegenerate) {\n        continue;\n      }\n      if (!firstSegDegenerate && segmentHitsAnyRect(np0, np1, realNodeRects, [srcId], 1)) {\n        continue;\n      }\n      if (!secondSegDegenerate && segmentHitsAnyRect(np1, np2, realNodeRects, [dstId], 1)) {\n        continue;\n      }\n      const firstSegConflicts = !firstSegDegenerate && segmentConflictsWithAnyEdge(np0, np1, edges, edge, {\n        epsilon: EPS3,\n        skipDegenerateOther: true\n      });\n      const secondSegConflicts = !secondSegDegenerate && segmentConflictsWithAnyEdge(np1, np2, edges, edge, {\n        epsilon: EPS3,\n        skipDegenerateOther: true\n      });\n      if (firstSegConflicts || secondSegConflicts) {\n        continue;\n      }\n      if (firstSegDegenerate) {\n        newPts = [np1, np2];\n      } else if (secondSegDegenerate) {\n        newPts = [np0, np1];\n      } else {\n        newPts = [np0, np1, np2];\n      }\n      break;\n    }\n    if (newPts) {\n      edge.points = newPts;\n    }\n  }\n}\nchunk_Y2CYZVJY_name(portSwapToLShape, \"portSwapToLShape\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/terminalStub.ts\nfunction collapseShortTerminalStub(edges, nodeByIdMap) {\n  const MIN_STUB = 10;\n  const EPS_LOCAL2 = 1e-3;\n  const BUFFER = 2;\n  const { realNodeRects, labelNodeRects: labelRects } = collectNodeRectEntries(\n    nodeByIdMap.values()\n  );\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const rawPts = edge.points;\n    if (!rawPts || rawPts.length < 4) {\n      continue;\n    }\n    const pts = dedupeConsecutivePoints(rawPts, EPS_LOCAL2);\n    if (pts.length < 4) {\n      continue;\n    }\n    const nLast = pts.length - 1;\n    const endPt = pts[nLast];\n    const penultPt = pts[nLast - 1];\n    const prevPt = pts[nLast - 2];\n    const lastDx = endPt.x - penultPt.x;\n    const lastDy = endPt.y - penultPt.y;\n    const lastLen = Math.hypot(lastDx, lastDy);\n    if (lastLen >= MIN_STUB || lastLen < EPS_LOCAL2) {\n      continue;\n    }\n    const penultDx = penultPt.x - prevPt.x;\n    const penultDy = penultPt.y - prevPt.y;\n    const penultLen = Math.hypot(penultDx, penultDy);\n    if (penultLen < EPS_LOCAL2) {\n      continue;\n    }\n    const lastIsHoriz = isHorizontalSegment(penultPt, endPt, EPS_LOCAL2);\n    const lastIsVert = isVerticalSegment(penultPt, endPt, EPS_LOCAL2);\n    const penultIsHoriz = isHorizontalSegment(prevPt, penultPt, EPS_LOCAL2);\n    const penultIsVert = isVerticalSegment(prevPt, penultPt, EPS_LOCAL2);\n    if (!(lastIsHoriz && penultIsVert || lastIsVert && penultIsHoriz)) {\n      continue;\n    }\n    const dstId = edge.end;\n    const srcId = edge.start;\n    const dst = dstId ? nodeByIdMap.get(dstId) : void 0;\n    if (!dst) {\n      continue;\n    }\n    const dstCx = dst.x ?? 0;\n    const dstCy = dst.y ?? 0;\n    const dstRect = rectOfNodeBounds(dst);\n    if (!dstRect) {\n      continue;\n    }\n    let newPrev;\n    let newEnd;\n    if (penultIsVert) {\n      const approachFromBelow = penultDy < 0;\n      newPrev = { x: dstCx, y: prevPt.y };\n      newEnd = { x: dstCx, y: approachFromBelow ? dstRect.bottom : dstRect.top };\n    } else {\n      const approachFromLeft = penultDx > 0;\n      newPrev = { x: prevPt.x, y: dstCy };\n      newEnd = { x: approachFromLeft ? dstRect.right : dstRect.left, y: dstCy };\n    }\n    if (segmentHitsAnyRect(newPrev, newEnd, realNodeRects, dstId ? [dstId] : [], -BUFFER)) {\n      continue;\n    }\n    if (segmentHitsAnyRect(newPrev, newEnd, labelRects, [], -BUFFER)) {\n      continue;\n    }\n    if (srcId) {\n      const src = nodeByIdMap.get(srcId);\n      const srcRect = src ? rectOfNodeBounds(src) : void 0;\n      if (srcRect && pointInsideRect(newPrev, srcRect, BUFFER)) {\n        continue;\n      }\n    }\n    const ownSegmentKey = /* @__PURE__ */ chunk_Y2CYZVJY_name((a, b) => `${a.x.toFixed(3)},${a.y.toFixed(3)}|${b.x.toFixed(3)},${b.y.toFixed(3)}`, \"ownSegmentKey\");\n    const selfSegments = /* @__PURE__ */ new Set();\n    for (let i = 0; i < pts.length - 1; i++) {\n      selfSegments.add(ownSegmentKey(pts[i], pts[i + 1]));\n    }\n    const segmentCrossesOtherEdge = /* @__PURE__ */ chunk_Y2CYZVJY_name((from, to) => {\n      for (const other of edges) {\n        if (other === edge) {\n          continue;\n        }\n        if (other.isLayoutOnly) {\n          continue;\n        }\n        const oPts = other.points;\n        if (!oPts || oPts.length < 2) {\n          continue;\n        }\n        for (let i = 0; i < oPts.length - 1; i++) {\n          const a = oPts[i];\n          const b = oPts[i + 1];\n          if (selfSegments.has(ownSegmentKey(a, b))) {\n            continue;\n          }\n          if (orthogonalSegmentsStrictlyCross(from, to, a, b, EPS_LOCAL2)) {\n            return true;\n          }\n        }\n      }\n      return false;\n    }, \"segmentCrossesOtherEdge\");\n    if (segmentCrossesOtherEdge(newPrev, newEnd)) {\n      continue;\n    }\n    if (nLast - 3 >= 0) {\n      const beforePrev = pts[nLast - 3];\n      const endpointIds = [srcId, dstId].filter((id) => Boolean(id));\n      if (segmentHitsAnyRect(beforePrev, newPrev, realNodeRects, endpointIds, -BUFFER)) {\n        continue;\n      }\n      if (segmentCrossesOtherEdge(beforePrev, newPrev)) {\n        continue;\n      }\n    }\n    const head = pts.slice(0, nLast - 2);\n    const newPts = [...head, newPrev, newEnd];\n    edge.points = newPts;\n    const labelId = edge.labelNodeId;\n    if (labelId) {\n      const labelNode = nodeByIdMap.get(labelId);\n      if (labelNode) {\n        const lw = labelNode.width ?? 0;\n        const lh = labelNode.height ?? 0;\n        if (lw > 0 && lh > 0) {\n          let bestMidX;\n          let bestMidY;\n          let bestLen = -1;\n          for (let i = 0; i < newPts.length - 1; i++) {\n            const a = newPts[i];\n            const b = newPts[i + 1];\n            const segLen = Math.hypot(b.x - a.x, b.y - a.y);\n            const isHoriz = sameY(a, b, EPS_LOCAL2);\n            const isVert = sameX(a, b, EPS_LOCAL2);\n            const fits = isHoriz && segLen >= lw + 2 || isVert && segLen >= lh + 2;\n            if (!fits) {\n              continue;\n            }\n            if (segLen > bestLen) {\n              bestLen = segLen;\n              bestMidX = (a.x + b.x) / 2;\n              bestMidY = (a.y + b.y) / 2;\n            }\n          }\n          if (bestMidX !== void 0 && bestMidY !== void 0) {\n            labelNode.x = bestMidX;\n            labelNode.y = bestMidY;\n          }\n        }\n      }\n    }\n  }\n}\nchunk_Y2CYZVJY_name(collapseShortTerminalStub, \"collapseShortTerminalStub\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/materializedGeometry.ts\nvar EPS_LOCAL = 1e-3;\nvar MIN_SHARED = 8;\nvar segmentsFor = orthogonalSegmentsForPoints;\nvar orthogonallyAligned = /* @__PURE__ */ chunk_Y2CYZVJY_name((a, b) => sameX(a, b, EPS_LOCAL) || sameY(a, b, EPS_LOCAL), \"orthogonallyAligned\");\nfunction separateSharedRenderedTerminalLanes(edges, nodeByIdMap) {\n  const MIN_FACE_CLEARANCE = 16;\n  const TRACK_SHIFT = 7;\n  const rectIntersect = /* @__PURE__ */ chunk_Y2CYZVJY_name((node, point) => {\n    const x = node.x ?? 0;\n    const y = node.y ?? 0;\n    const dx = point.x - x;\n    const dy = point.y - y;\n    let w = (node.width ?? 0) / 2;\n    let h = (node.height ?? 0) / 2;\n    if (Math.abs(dy) * w > Math.abs(dx) * h) {\n      if (dy < 0) {\n        h = -h;\n      }\n      return { x: x + (dy === 0 ? 0 : h * dx / dy), y: y + h };\n    }\n    if (dx < 0) {\n      w = -w;\n    }\n    return { x: x + w, y: y + (dx === 0 ? 0 : w * dy / dx) };\n  }, \"rectIntersect\");\n  const terminalLaneFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, atStart) => {\n    const points = dedupeConsecutivePoints(edge.points ?? []);\n    if (points.length < 2) {\n      return void 0;\n    }\n    const nodeId = atStart ? edge.start : edge.end;\n    const node = nodeId ? nodeByIdMap.get(nodeId) : void 0;\n    const rect = node ? rectOfNodeBounds(node) : void 0;\n    if (!node || !nodeId || !rect) {\n      return void 0;\n    }\n    const endpoint = atStart ? points[0] : points[points.length - 1];\n    const adjacent = atStart ? points[1] : points[points.length - 2];\n    const boundary = rectIntersect(node, endpoint);\n    let railEnd = endpoint;\n    if (orthogonallyAligned(adjacent, boundary)) {\n      railEnd = adjacent;\n    }\n    if (sameX(boundary, railEnd, EPS_LOCAL)) {\n      return {\n        edge,\n        edgeId: String(edge.id ?? \"\"),\n        nodeId,\n        atStart,\n        orientation: \"V\",\n        coord: boundary.x,\n        min: Math.min(boundary.y, railEnd.y),\n        max: Math.max(boundary.y, railEnd.y),\n        boundary,\n        railEnd,\n        rect\n      };\n    }\n    if (sameY(boundary, railEnd, EPS_LOCAL)) {\n      return {\n        edge,\n        edgeId: String(edge.id ?? \"\"),\n        nodeId,\n        atStart,\n        orientation: \"H\",\n        coord: boundary.y,\n        min: Math.min(boundary.x, railEnd.x),\n        max: Math.max(boundary.x, railEnd.x),\n        boundary,\n        railEnd,\n        rect\n      };\n    }\n    return void 0;\n  }, \"terminalLaneFor\");\n  const projectedOverlapLength = /* @__PURE__ */ chunk_Y2CYZVJY_name((a, b) => Math.max(0, Math.min(a.max, b.max) - Math.max(a.min, b.min)), \"projectedOverlapLength\");\n  const sameTerminalFace = /* @__PURE__ */ chunk_Y2CYZVJY_name((a, b) => {\n    if (a.nodeId !== b.nodeId || a.orientation !== b.orientation) {\n      return false;\n    }\n    if (a.orientation === \"H\") {\n      const aOnHorizontalFace = Math.abs(a.boundary.x - a.rect.left) < 1 || Math.abs(a.boundary.x - a.rect.right) < 1;\n      return aOnHorizontalFace && sameX(a.boundary, b.boundary, 1);\n    }\n    const aOnVerticalFace = Math.abs(a.boundary.y - a.rect.top) < 1 || Math.abs(a.boundary.y - a.rect.bottom) < 1;\n    return aOnVerticalFace && sameY(a.boundary, b.boundary, 1);\n  }, \"sameTerminalFace\");\n  const exactTerminalLaneConflict = /* @__PURE__ */ chunk_Y2CYZVJY_name((a, b) => {\n    if (a.nodeId !== b.nodeId || a.orientation !== b.orientation) {\n      return false;\n    }\n    const shared = projectedOverlapLength(a, b);\n    return shared >= MIN_SHARED && Math.abs(a.coord - b.coord) < 0.5;\n  }, \"exactTerminalLaneConflict\");\n  const nearTerminalLaneConflict = /* @__PURE__ */ chunk_Y2CYZVJY_name((a, b) => {\n    if (a.nodeId !== b.nodeId || a.orientation !== b.orientation || a.orientation !== \"H\" || a.atStart === b.atStart) {\n      return false;\n    }\n    const shared = projectedOverlapLength(a, b);\n    if (shared < MIN_SHARED) {\n      return false;\n    }\n    const faceSpan = a.rect.bottom - a.rect.top;\n    if (shared < faceSpan || shared > 2 * faceSpan) {\n      return false;\n    }\n    return sameTerminalFace(a, b) && Math.abs(a.coord - b.coord) < MIN_FACE_CLEARANCE;\n  }, \"nearTerminalLaneConflict\");\n  const shiftedCandidate = /* @__PURE__ */ chunk_Y2CYZVJY_name((lane, shift) => {\n    const points = dedupeConsecutivePoints(lane.edge.points ?? []);\n    if (points.length < 2) {\n      return void 0;\n    }\n    const shiftedBoundary = lane.orientation === \"V\" ? { x: lane.boundary.x + shift, y: lane.boundary.y } : { x: lane.boundary.x, y: lane.boundary.y + shift };\n    const shiftedRailEnd = lane.orientation === \"V\" ? { x: lane.railEnd.x + shift, y: lane.railEnd.y } : { x: lane.railEnd.x, y: lane.railEnd.y + shift };\n    const boundaryStaysOnSameFace = /* @__PURE__ */ chunk_Y2CYZVJY_name(() => {\n      if (Math.abs(lane.boundary.y - lane.rect.top) < 1 || Math.abs(lane.boundary.y - lane.rect.bottom) < 1) {\n        return sameY(shiftedBoundary, lane.boundary, EPS_LOCAL) && shiftedBoundary.x >= lane.rect.left + 1 && shiftedBoundary.x <= lane.rect.right - 1;\n      }\n      if (Math.abs(lane.boundary.x - lane.rect.left) < 1 || Math.abs(lane.boundary.x - lane.rect.right) < 1) {\n        return sameX(shiftedBoundary, lane.boundary, EPS_LOCAL) && shiftedBoundary.y >= lane.rect.top + 1 && shiftedBoundary.y <= lane.rect.bottom - 1;\n      }\n      return false;\n    }, \"boundaryStaysOnSameFace\");\n    if (!boundaryStaysOnSameFace()) {\n      return void 0;\n    }\n    if (lane.atStart) {\n      const railEndIsAdjacent2 = points.length > 1 && samePoint(points[1], lane.railEnd, EPS_LOCAL);\n      const rest = points.slice(railEndIsAdjacent2 ? 2 : 1);\n      const next = rest[0];\n      if (next && !orthogonallyAligned(next, shiftedRailEnd)) {\n        return void 0;\n      }\n      return [shiftedBoundary, shiftedRailEnd, ...rest];\n    }\n    const railEndIsAdjacent = points.length > 1 && samePoint(points[points.length - 2], lane.railEnd, EPS_LOCAL);\n    const before = points.slice(0, railEndIsAdjacent ? -2 : -1);\n    const previous = before[before.length - 1];\n    if (previous && !orthogonallyAligned(previous, shiftedRailEnd)) {\n      return void 0;\n    }\n    return [...before, shiftedRailEnd, shiftedBoundary];\n  }, \"shiftedCandidate\");\n  const laneIsStraightCollinearConnector = /* @__PURE__ */ chunk_Y2CYZVJY_name((lane) => {\n    const edge = lane.edge;\n    const points = dedupeConsecutivePoints(edge.points ?? []);\n    if (points.length !== 2) {\n      return false;\n    }\n    const startId = edge.start;\n    const endId = edge.end;\n    const start = startId ? nodeByIdMap.get(startId) : void 0;\n    const end = endId ? nodeByIdMap.get(endId) : void 0;\n    if (!start || !end) {\n      return false;\n    }\n    const startX = start.x ?? 0;\n    const startY = start.y ?? 0;\n    const endX = end.x ?? 0;\n    const endY = end.y ?? 0;\n    const [a, b] = points;\n    return sameY(a, b, EPS_LOCAL) && Math.abs(startY - endY) < 1 && Math.abs(startX - endX) > 1 || sameX(a, b, EPS_LOCAL) && Math.abs(startX - endX) < 1 && Math.abs(startY - endY) > 1;\n  }, \"laneIsStraightCollinearConnector\");\n  const shifts = [\n    -TRACK_SHIFT,\n    TRACK_SHIFT,\n    -2 * TRACK_SHIFT,\n    2 * TRACK_SHIFT,\n    -3 * TRACK_SHIFT,\n    3 * TRACK_SHIFT\n  ];\n  for (let iteration = 0; iteration < 8; iteration++) {\n    const lanes = edges.filter((edge) => !edge.isLayoutOnly).flatMap((edge) => [terminalLaneFor(edge, true), terminalLaneFor(edge, false)]).filter((lane) => Boolean(lane));\n    let fixed = false;\n    for (let i = 0; i < lanes.length && !fixed; i++) {\n      for (let j = i + 1; j < lanes.length && !fixed; j++) {\n        const first = lanes[i];\n        const second = lanes[j];\n        if (first.edge === second.edge || !(exactTerminalLaneConflict(first, second) || nearTerminalLaneConflict(first, second))) {\n          continue;\n        }\n        const fixingNearConflict = !exactTerminalLaneConflict(first, second);\n        const candidates = [first, second].sort((a, b) => {\n          const aPreservesStraight = laneIsStraightCollinearConnector(a);\n          const bPreservesStraight = laneIsStraightCollinearConnector(b);\n          if (aPreservesStraight !== bPreservesStraight) {\n            return Number(aPreservesStraight) - Number(bPreservesStraight);\n          }\n          return Number(!b.atStart) - Number(!a.atStart);\n        });\n        for (const lane of candidates) {\n          for (const shift of shifts) {\n            const candidate = shiftedCandidate(lane, shift);\n            if (!candidate) {\n              continue;\n            }\n            const nextLane = terminalLaneFor({ ...lane.edge, points: candidate }, lane.atStart);\n            if (!nextLane || lanes.some(\n              (other) => other.edge !== lane.edge && (exactTerminalLaneConflict(nextLane, other) || fixingNearConflict && nearTerminalLaneConflict(nextLane, other))\n            )) {\n              continue;\n            }\n            lane.edge.points = candidate;\n            fixed = true;\n            break;\n          }\n          if (fixed) {\n            break;\n          }\n        }\n      }\n    }\n    if (!fixed) {\n      return;\n    }\n  }\n}\nchunk_Y2CYZVJY_name(separateSharedRenderedTerminalLanes, \"separateSharedRenderedTerminalLanes\");\nfunction collapseRedundantRectangularDoglegs(edges, nodeByIdMap) {\n  const BUFFER = 2;\n  const MAX_ITERATIONS = 8;\n  const { realNodeRects, labelNodeRects: labelRects } = collectNodeRectEntries(\n    nodeByIdMap.values()\n  );\n  const candidateIsSafe = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, candidate) => {\n    const sourceId = edge.start;\n    const targetId = edge.end;\n    const candidateSegments = segmentsFor(candidate);\n    if (candidateSegments.length !== candidate.length - 1) {\n      return false;\n    }\n    const endpointIds = [sourceId, targetId].filter((id) => Boolean(id));\n    for (const segment of candidateSegments) {\n      if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -BUFFER)) {\n        return false;\n      }\n      if (segmentHitsAnyRect(segment.a, segment.b, labelRects, [], -BUFFER)) {\n        return false;\n      }\n    }\n    for (const other of edges) {\n      if (other === edge || other.isLayoutOnly) {\n        continue;\n      }\n      const otherPoints = other.points;\n      if (!otherPoints || otherPoints.length < 2) {\n        continue;\n      }\n      for (const candidateSegment of candidateSegments) {\n        for (const otherSegment of segmentsFor(dedupeConsecutivePoints(otherPoints))) {\n          if (sameAxisSegmentOverlapLength(candidateSegment, otherSegment, 0.5) >= MIN_SHARED) {\n            return false;\n          }\n          if (orthogonalSegmentsStrictlyCross(\n            candidateSegment.a,\n            candidateSegment.b,\n            otherSegment.a,\n            otherSegment.b,\n            EPS_LOCAL\n          )) {\n            return false;\n          }\n        }\n      }\n    }\n    return true;\n  }, \"candidateIsSafe\");\n  const withoutDogleg = /* @__PURE__ */ chunk_Y2CYZVJY_name((points, i) => {\n    if (i + 4 >= points.length) {\n      return void 0;\n    }\n    const p0 = points[i];\n    const p1 = points[i + 1];\n    const p2 = points[i + 2];\n    const p3 = points[i + 3];\n    const p4 = points[i + 4];\n    const terminalVerticalDogleg = isHorizontalSegment(p0, p1) && isVerticalSegment(p1, p2) && isHorizontalSegment(p2, p3) && isVerticalSegment(p3, p4) && sameX(p0, p3, EPS_LOCAL) && sameX(p0, p4, EPS_LOCAL) && sameX(p1, p2, EPS_LOCAL) && (p1.x - p0.x) * (p3.x - p2.x) < 0;\n    const terminalHorizontalDogleg = isVerticalSegment(p0, p1) && isHorizontalSegment(p1, p2) && isVerticalSegment(p2, p3) && isHorizontalSegment(p3, p4) && sameY(p0, p3, EPS_LOCAL) && sameY(p0, p4, EPS_LOCAL) && sameY(p1, p2, EPS_LOCAL) && (p1.y - p0.y) * (p3.y - p2.y) < 0;\n    if (terminalVerticalDogleg || terminalHorizontalDogleg) {\n      return dedupeConsecutivePoints([...points.slice(0, i + 1), p4, ...points.slice(i + 5)]);\n    }\n    if (i + 5 >= points.length) {\n      return void 0;\n    }\n    const p5 = points[i + 5];\n    const verticalDogleg = isVerticalSegment(p0, p1) && isHorizontalSegment(p1, p2) && isVerticalSegment(p2, p3) && isHorizontalSegment(p3, p4) && isVerticalSegment(p4, p5) && sameX(p0, p4, EPS_LOCAL) && sameX(p0, p5, EPS_LOCAL) && sameX(p2, p3, EPS_LOCAL) && (p2.x - p1.x) * (p4.x - p3.x) < 0;\n    const horizontalDogleg = isHorizontalSegment(p0, p1) && isVerticalSegment(p1, p2) && isHorizontalSegment(p2, p3) && isVerticalSegment(p3, p4) && isHorizontalSegment(p4, p5) && sameY(p0, p4, EPS_LOCAL) && sameY(p0, p5, EPS_LOCAL) && sameY(p2, p3, EPS_LOCAL) && (p2.y - p1.y) * (p4.y - p3.y) < 0;\n    if (!verticalDogleg && !horizontalDogleg) {\n      return void 0;\n    }\n    return dedupeConsecutivePoints([...points.slice(0, i + 1), p5, ...points.slice(i + 6)]);\n  }, \"withoutDogleg\");\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    let fixed = false;\n    for (const edge of edges) {\n      if (edge.isLayoutOnly) {\n        continue;\n      }\n      const points = dedupeConsecutivePoints(edge.points ?? []);\n      for (let i = 0; i <= points.length - 5; i++) {\n        const candidate = withoutDogleg(points, i);\n        if (!candidate || !candidateIsSafe(edge, candidate)) {\n          continue;\n        }\n        edge.points = candidate;\n        fixed = true;\n        break;\n      }\n      if (fixed) {\n        break;\n      }\n    }\n    if (!fixed) {\n      return;\n    }\n  }\n}\nchunk_Y2CYZVJY_name(collapseRedundantRectangularDoglegs, \"collapseRedundantRectangularDoglegs\");\nfunction liftObstacleHuggingSameSideRails(edges, nodeByIdMap) {\n  const BUFFER = 2;\n  const CLEARANCE = 20;\n  const MAX_ITERATIONS = 8;\n  const { realNodeRects, labelNodeRects: labelRects } = collectNodeRectEntries(\n    nodeByIdMap.values()\n  );\n  const visibleEdges = edges.filter((edge) => !edge.isLayoutOnly);\n  const pointsFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, replacementEdge, replacement) => dedupeConsecutivePoints(\n    edge === replacementEdge ? replacement ?? [] : edge.points ?? []\n  ), \"pointsFor\");\n  const strictCrossingCount = /* @__PURE__ */ chunk_Y2CYZVJY_name((replacementEdge, replacement) => {\n    let count = 0;\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const firstSegments = segmentsFor(pointsFor(visibleEdges[i], replacementEdge, replacement));\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const secondSegments = segmentsFor(\n          pointsFor(visibleEdges[j], replacementEdge, replacement)\n        );\n        for (const firstSegment of firstSegments) {\n          for (const secondSegment of secondSegments) {\n            if (orthogonalSegmentsStrictlyCross(\n              firstSegment.a,\n              firstSegment.b,\n              secondSegment.a,\n              secondSegment.b,\n              EPS_LOCAL\n            )) {\n              count++;\n            }\n          }\n        }\n      }\n    }\n    return count;\n  }, \"strictCrossingCount\");\n  const middleRail = /* @__PURE__ */ chunk_Y2CYZVJY_name((points) => {\n    const segments = segmentsFor(points);\n    if (segments.length !== 3) {\n      return void 0;\n    }\n    const middle = segments[1];\n    if (segments[0].horizontal === middle.horizontal || segments[2].horizontal === middle.horizontal) {\n      return void 0;\n    }\n    return {\n      index: middle.index,\n      horizontal: middle.horizontal,\n      vertical: middle.vertical,\n      segment: middle\n    };\n  }, \"middleRail\");\n  const blockingRectsFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, rail) => {\n    const endpointIds = [edge.start, edge.end].filter(\n      (id) => Boolean(id)\n    );\n    return realNodeRects.filter((entry) => {\n      if (endpointIds.includes(entry.id)) {\n        return false;\n      }\n      const rect = entry.rect;\n      if (rail.horizontal) {\n        const xOverlap = overlapLength(rail.a.x, rail.b.x, rect.left, rect.right);\n        return xOverlap >= MIN_SHARED && rail.a.y >= rect.top - BUFFER && rail.a.y <= rect.bottom + BUFFER;\n      }\n      const yOverlap = overlapLength(rail.a.y, rail.b.y, rect.top, rect.bottom);\n      return yOverlap >= MIN_SHARED && rail.a.x >= rect.left - BUFFER && rail.a.x <= rect.right + BUFFER;\n    });\n  }, \"blockingRectsFor\");\n  const candidateByMovingRail = /* @__PURE__ */ chunk_Y2CYZVJY_name((points, rail, coord) => {\n    const candidate = points.map((point) => ({ ...point }));\n    if (rail.horizontal) {\n      candidate[rail.index].y = coord;\n      candidate[rail.index + 1].y = coord;\n    } else if (rail.vertical) {\n      candidate[rail.index].x = coord;\n      candidate[rail.index + 1].x = coord;\n    } else {\n      return void 0;\n    }\n    const simplified = simplifyPolyline(dedupeConsecutivePoints(candidate));\n    return segmentsFor(simplified).length === simplified.length - 1 ? simplified : void 0;\n  }, \"candidateByMovingRail\");\n  const candidateIsSafe = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, candidate, currentCrossings) => {\n    const endpointIds = [edge.start, edge.end].filter(\n      (id) => Boolean(id)\n    );\n    const candidateSegments = segmentsFor(candidate);\n    if (candidateSegments.length !== candidate.length - 1) {\n      return false;\n    }\n    for (const segment of candidateSegments) {\n      if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -BUFFER)) {\n        return false;\n      }\n      if (segmentHitsAnyRect(segment.a, segment.b, labelRects, [], -BUFFER)) {\n        return false;\n      }\n    }\n    for (const other of visibleEdges) {\n      if (other === edge) {\n        continue;\n      }\n      for (const candidateSegment of candidateSegments) {\n        for (const otherSegment of segmentsFor(pointsFor(other))) {\n          if (sameAxisSegmentOverlapLength(candidateSegment, otherSegment, 0.5) >= MIN_SHARED) {\n            return false;\n          }\n        }\n      }\n    }\n    return strictCrossingCount(edge, candidate) <= currentCrossings;\n  }, \"candidateIsSafe\");\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    const currentCrossings = strictCrossingCount();\n    let fixed = false;\n    for (const edge of visibleEdges) {\n      const points = pointsFor(edge);\n      const rail = middleRail(points);\n      if (!rail) {\n        continue;\n      }\n      const blockers = blockingRectsFor(edge, rail.segment);\n      if (blockers.length === 0) {\n        continue;\n      }\n      const coords = rail.horizontal ? [\n        Math.min(...blockers.map((entry) => entry.rect.top)) - CLEARANCE,\n        Math.max(...blockers.map((entry) => entry.rect.bottom)) + CLEARANCE\n      ] : [\n        Math.min(...blockers.map((entry) => entry.rect.left)) - CLEARANCE,\n        Math.max(...blockers.map((entry) => entry.rect.right)) + CLEARANCE\n      ];\n      for (const coord of coords) {\n        const candidate = candidateByMovingRail(points, rail.segment, coord);\n        if (!candidate || !candidateIsSafe(edge, candidate, currentCrossings)) {\n          continue;\n        }\n        edge.points = candidate;\n        fixed = true;\n        break;\n      }\n      if (fixed) {\n        break;\n      }\n    }\n    if (!fixed) {\n      return;\n    }\n  }\n}\nchunk_Y2CYZVJY_name(liftObstacleHuggingSameSideRails, \"liftObstacleHuggingSameSideRails\");\nfunction liftTopLaneTitleBandsAboveRails(edges, nodeByIdMap) {\n  const CLEARANCE = 4;\n  const validTitleRect = /* @__PURE__ */ chunk_Y2CYZVJY_name((node) => {\n    const rect = node.groupTitleRect;\n    if (!rect || typeof rect.left !== \"number\" || typeof rect.right !== \"number\" || typeof rect.top !== \"number\" || typeof rect.bottom !== \"number\" || !Number.isFinite(rect.left) || !Number.isFinite(rect.right) || !Number.isFinite(rect.top) || !Number.isFinite(rect.bottom) || rect.right <= rect.left || rect.bottom <= rect.top) {\n      return void 0;\n    }\n    return { left: rect.left, right: rect.right, top: rect.top, bottom: rect.bottom };\n  }, \"validTitleRect\");\n  const topLaneTitleFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((node) => {\n    if (!node.isGroup || node.parentId) {\n      return void 0;\n    }\n    const rawDirection = node.direction;\n    const direction = typeof rawDirection === \"string\" ? rawDirection.toUpperCase() : \"\";\n    if (direction === \"LR\" || direction === \"RL\" || direction === \"BT\") {\n      return void 0;\n    }\n    const rect = validTitleRect(node);\n    const y = node.y;\n    const height = node.height;\n    if (!rect || typeof y !== \"number\" || typeof height !== \"number\" || !Number.isFinite(y) || !Number.isFinite(height) || height <= 0) {\n      return void 0;\n    }\n    const titleWidth = rect.right - rect.left;\n    const titleHeight = rect.bottom - rect.top;\n    if (titleHeight <= 0 || titleWidth < titleHeight) {\n      return void 0;\n    }\n    return { node, rect };\n  }, \"topLaneTitleFor\");\n  const horizontalSegmentIntersectsTitle = /* @__PURE__ */ chunk_Y2CYZVJY_name((segment, rect) => {\n    if (!segment.horizontal) {\n      return false;\n    }\n    const y = segment.a.y;\n    if (y <= rect.top + EPS_LOCAL || y >= rect.bottom - EPS_LOCAL) {\n      return false;\n    }\n    return overlapLength(segment.a.x, segment.b.x, rect.left, rect.right) >= MIN_SHARED;\n  }, \"horizontalSegmentIntersectsTitle\");\n  const lanes = [...nodeByIdMap.values()].map(topLaneTitleFor).filter((lane) => Boolean(lane));\n  if (lanes.length === 0) {\n    return;\n  }\n  let topDelta = 0;\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const points = dedupeConsecutivePoints(edge.points ?? []);\n    for (const segment of segmentsFor(points)) {\n      for (const lane of lanes) {\n        if (!horizontalSegmentIntersectsTitle(segment, lane.rect)) {\n          continue;\n        }\n        topDelta = Math.max(topDelta, lane.rect.bottom - segment.a.y + CLEARANCE);\n      }\n    }\n  }\n  if (topDelta <= EPS_LOCAL) {\n    return;\n  }\n  for (const lane of lanes) {\n    const y = lane.node.y;\n    const height = lane.node.height;\n    if (typeof y !== \"number\" || typeof height !== \"number\" || !Number.isFinite(y) || !Number.isFinite(height) || height <= 0) {\n      continue;\n    }\n    lane.node.y = y - topDelta / 2;\n    lane.node.height = height + topDelta;\n    lane.node.groupTitleRect = {\n      ...lane.rect,\n      top: lane.rect.top - topDelta,\n      bottom: lane.rect.bottom - topDelta\n    };\n  }\n}\nchunk_Y2CYZVJY_name(liftTopLaneTitleBandsAboveRails, \"liftTopLaneTitleBandsAboveRails\");\nfunction shiftLeftLaneTitleBandsLeftOfRails(edges, nodeByIdMap) {\n  const CLEARANCE = 4;\n  const validTitleRect = /* @__PURE__ */ chunk_Y2CYZVJY_name((node) => {\n    const rect = node.groupTitleRect;\n    if (!rect || typeof rect.left !== \"number\" || typeof rect.right !== \"number\" || typeof rect.top !== \"number\" || typeof rect.bottom !== \"number\" || !Number.isFinite(rect.left) || !Number.isFinite(rect.right) || !Number.isFinite(rect.top) || !Number.isFinite(rect.bottom) || rect.right <= rect.left || rect.bottom <= rect.top) {\n      return void 0;\n    }\n    return { left: rect.left, right: rect.right, top: rect.top, bottom: rect.bottom };\n  }, \"validTitleRect\");\n  const leftLaneTitleFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((node) => {\n    if (!node.isGroup || node.parentId) {\n      return void 0;\n    }\n    const rawDirection = node.direction;\n    if (rawDirection !== \"LR\") {\n      return void 0;\n    }\n    const rect = validTitleRect(node);\n    const x = node.x;\n    const width = node.width;\n    if (!rect || typeof x !== \"number\" || typeof width !== \"number\" || !Number.isFinite(x) || !Number.isFinite(width) || width <= 0) {\n      return void 0;\n    }\n    const titleWidth = rect.right - rect.left;\n    const titleHeight = rect.bottom - rect.top;\n    if (titleWidth <= 0 || titleHeight < titleWidth) {\n      return void 0;\n    }\n    return { node, rect };\n  }, \"leftLaneTitleFor\");\n  const verticalSegmentIntersectsTitle = /* @__PURE__ */ chunk_Y2CYZVJY_name((segment, rect) => {\n    if (!segment.vertical) {\n      return false;\n    }\n    const x = segment.a.x;\n    if (x <= rect.left + EPS_LOCAL || x >= rect.right - EPS_LOCAL) {\n      return false;\n    }\n    return overlapLength(segment.a.y, segment.b.y, rect.top, rect.bottom) >= MIN_SHARED;\n  }, \"verticalSegmentIntersectsTitle\");\n  const horizontalSegmentIntersectsTitle = /* @__PURE__ */ chunk_Y2CYZVJY_name((segment, rect) => {\n    if (!segment.horizontal) {\n      return false;\n    }\n    const y = segment.a.y;\n    if (y <= rect.top + EPS_LOCAL || y >= rect.bottom - EPS_LOCAL) {\n      return false;\n    }\n    return overlapLength(segment.a.x, segment.b.x, rect.left, rect.right) >= MIN_SHARED;\n  }, \"horizontalSegmentIntersectsTitle\");\n  const lanes = [...nodeByIdMap.values()].map(leftLaneTitleFor).filter((lane) => Boolean(lane));\n  if (lanes.length === 0) {\n    return;\n  }\n  let leftDelta = 0;\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const points = dedupeConsecutivePoints(edge.points ?? []);\n    for (const segment of segmentsFor(points)) {\n      for (const lane of lanes) {\n        if (verticalSegmentIntersectsTitle(segment, lane.rect)) {\n          leftDelta = Math.max(leftDelta, lane.rect.right - segment.a.x + CLEARANCE);\n        } else if (horizontalSegmentIntersectsTitle(segment, lane.rect)) {\n          const segmentLeft = Math.min(segment.a.x, segment.b.x);\n          leftDelta = Math.max(leftDelta, lane.rect.right - segmentLeft + CLEARANCE);\n        }\n      }\n    }\n  }\n  if (leftDelta <= EPS_LOCAL) {\n    return;\n  }\n  for (const lane of lanes) {\n    const x = lane.node.x;\n    const width = lane.node.width;\n    if (typeof x !== \"number\" || typeof width !== \"number\" || !Number.isFinite(x) || !Number.isFinite(width) || width <= 0) {\n      continue;\n    }\n    lane.node.x = x - leftDelta / 2;\n    lane.node.width = width + leftDelta;\n    lane.node.groupTitleRect = {\n      ...lane.rect,\n      left: lane.rect.left - leftDelta,\n      right: lane.rect.right - leftDelta\n    };\n  }\n}\nchunk_Y2CYZVJY_name(shiftLeftLaneTitleBandsLeftOfRails, \"shiftLeftLaneTitleBandsLeftOfRails\");\nfunction swapDestinationTerminalTailsToReduceCrossings(edges, nodeByIdMap) {\n  const BUFFER = 2;\n  const MAX_ITERATIONS = 4;\n  const { realNodeRects } = collectNodeRectEntries(nodeByIdMap.values());\n  const visibleEdges = edges.filter((edge) => !edge.isLayoutOnly);\n  const replacementPointsFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, replacements = /* @__PURE__ */ new Map()) => dedupeConsecutivePoints(\n    replacements.get(edge) ?? edge.points ?? []\n  ), \"replacementPointsFor\");\n  const crossingCount = /* @__PURE__ */ chunk_Y2CYZVJY_name((replacements = /* @__PURE__ */ new Map()) => {\n    let count = 0;\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const firstSegments = segmentsFor(replacementPointsFor(visibleEdges[i], replacements));\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const secondSegments = segmentsFor(replacementPointsFor(visibleEdges[j], replacements));\n        for (const firstSegment of firstSegments) {\n          for (const secondSegment of secondSegments) {\n            if (orthogonalSegmentsStrictlyCross(\n              firstSegment.a,\n              firstSegment.b,\n              secondSegment.a,\n              secondSegment.b,\n              EPS_LOCAL\n            )) {\n              count++;\n            }\n          }\n        }\n      }\n    }\n    return count;\n  }, \"crossingCount\");\n  const totalBends = /* @__PURE__ */ chunk_Y2CYZVJY_name((replacements = /* @__PURE__ */ new Map()) => visibleEdges.reduce(\n    (sum, edge) => sum + countOrthogonalBends(replacementPointsFor(edge, replacements)),\n    0\n  ), \"totalBends\");\n  const terminalTailFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge) => {\n    const points = replacementPointsFor(edge);\n    if (points.length < 4) {\n      return void 0;\n    }\n    const tailStart = points[points.length - 2];\n    const terminal = points[points.length - 1];\n    if (!isHorizontalSegment(tailStart, terminal, EPS_LOCAL) && !isVerticalSegment(tailStart, terminal, EPS_LOCAL)) {\n      return void 0;\n    }\n    return { tailStart, terminal };\n  }, \"terminalTailFor\");\n  const candidateWithDestinationTail = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, tail) => {\n    const points = replacementPointsFor(edge);\n    if (points.length < 3) {\n      return void 0;\n    }\n    const start = points[0];\n    const firstTurn = points[1];\n    let connector;\n    if (isHorizontalSegment(start, firstTurn, EPS_LOCAL)) {\n      connector = { x: firstTurn.x, y: tail.tailStart.y };\n    } else if (isVerticalSegment(start, firstTurn, EPS_LOCAL)) {\n      connector = { x: tail.tailStart.x, y: firstTurn.y };\n    } else {\n      return void 0;\n    }\n    const candidate = simplifyPolyline(\n      dedupeConsecutivePoints([start, firstTurn, connector, tail.tailStart, tail.terminal])\n    );\n    return segmentsFor(candidate).length === candidate.length - 1 ? candidate : void 0;\n  }, \"candidateWithDestinationTail\");\n  const pathHasNodeHit = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, path) => {\n    const endpointIds = [edge.start, edge.end].filter(\n      (id) => Boolean(id)\n    );\n    for (const segment of segmentsFor(path)) {\n      if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -BUFFER)) {\n        return true;\n      }\n    }\n    return false;\n  }, \"pathHasNodeHit\");\n  const pathHasSharedTrack = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, path, replacements) => {\n    for (const other of visibleEdges) {\n      if (other === edge) {\n        continue;\n      }\n      for (const candidateSegment of segmentsFor(path)) {\n        for (const otherSegment of segmentsFor(replacementPointsFor(other, replacements))) {\n          if (sameAxisSegmentOverlapLength(candidateSegment, otherSegment, 0.5) >= MIN_SHARED) {\n            return true;\n          }\n        }\n      }\n    }\n    return false;\n  }, \"pathHasSharedTrack\");\n  const candidateIsSafe = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, path, replacements) => !pathHasNodeHit(edge, path) && !pathHasSharedTrack(edge, path, replacements), \"candidateIsSafe\");\n  const edgesByDestination = /* @__PURE__ */ chunk_Y2CYZVJY_name(() => {\n    const result = /* @__PURE__ */ new Map();\n    for (const edge of visibleEdges) {\n      const dstId = edge.end;\n      if (!dstId || !nodeByIdMap.has(dstId)) {\n        continue;\n      }\n      const points = replacementPointsFor(edge);\n      if (points.length < 4) {\n        continue;\n      }\n      const bucket = result.get(dstId) ?? [];\n      bucket.push(edge);\n      result.set(dstId, bucket);\n    }\n    return result;\n  }, \"edgesByDestination\");\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    const currentCrossings = crossingCount();\n    if (currentCrossings === 0) {\n      return;\n    }\n    const currentBends = totalBends();\n    let bestReplacements;\n    let bestCrossings = currentCrossings;\n    let bestBends = currentBends;\n    for (const destinationEdges of edgesByDestination().values()) {\n      for (let i = 0; i < destinationEdges.length; i++) {\n        for (let j = i + 1; j < destinationEdges.length; j++) {\n          const first = destinationEdges[i];\n          const second = destinationEdges[j];\n          const firstTail = terminalTailFor(first);\n          const secondTail = terminalTailFor(second);\n          if (!firstTail || !secondTail) {\n            continue;\n          }\n          const firstCandidate = candidateWithDestinationTail(first, secondTail);\n          const secondCandidate = candidateWithDestinationTail(second, firstTail);\n          if (!firstCandidate || !secondCandidate) {\n            continue;\n          }\n          const replacements = /* @__PURE__ */ new Map([\n            [first, firstCandidate],\n            [second, secondCandidate]\n          ]);\n          if (!candidateIsSafe(first, firstCandidate, replacements) || !candidateIsSafe(second, secondCandidate, replacements)) {\n            continue;\n          }\n          const candidateCrossings = crossingCount(replacements);\n          const candidateBends = totalBends(replacements);\n          if (candidateCrossings >= currentCrossings) {\n            continue;\n          }\n          if (candidateCrossings > bestCrossings || candidateCrossings === bestCrossings && candidateBends >= bestBends) {\n            continue;\n          }\n          bestReplacements = replacements;\n          bestCrossings = candidateCrossings;\n          bestBends = candidateBends;\n        }\n      }\n    }\n    if (!bestReplacements) {\n      return;\n    }\n    for (const [edge, points] of bestReplacements) {\n      edge.points = points;\n    }\n  }\n}\nchunk_Y2CYZVJY_name(swapDestinationTerminalTailsToReduceCrossings, \"swapDestinationTerminalTailsToReduceCrossings\");\nfunction reassignCrossingExternalRailChannels(edges, nodeByIdMap) {\n  const BUFFER = 2;\n  const RAIL_CHANNEL_GAP = 12;\n  const MAX_ITERATIONS = 4;\n  const MAX_EXHAUSTIVE_COMPONENT = 6;\n  const { realNodeRects, labelNodeRects: labelRects } = collectNodeRectEntries(\n    nodeByIdMap.values()\n  );\n  const visibleEdges = edges.filter((edge) => !edge.isLayoutOnly);\n  const replacementPointsFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, replacements = /* @__PURE__ */ new Map()) => dedupeConsecutivePoints(\n    replacements.get(edge) ?? edge.points ?? []\n  ), \"replacementPointsFor\");\n  const strictCrossingCount = /* @__PURE__ */ chunk_Y2CYZVJY_name((replacements = /* @__PURE__ */ new Map()) => {\n    let count = 0;\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const firstSegments = segmentsFor(replacementPointsFor(visibleEdges[i], replacements));\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const secondSegments = segmentsFor(replacementPointsFor(visibleEdges[j], replacements));\n        for (const firstSegment of firstSegments) {\n          for (const secondSegment of secondSegments) {\n            if (orthogonalSegmentsStrictlyCross(\n              firstSegment.a,\n              firstSegment.b,\n              secondSegment.a,\n              secondSegment.b,\n              EPS_LOCAL\n            )) {\n              count++;\n            }\n          }\n        }\n      }\n    }\n    return count;\n  }, \"strictCrossingCount\");\n  const totalBends = /* @__PURE__ */ chunk_Y2CYZVJY_name((replacements = /* @__PURE__ */ new Map()) => visibleEdges.reduce(\n    (sum, edge) => sum + countOrthogonalBends(replacementPointsFor(edge, replacements)),\n    0\n  ), \"totalBends\");\n  const endpointRectsFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge) => {\n    const srcId = edge.start;\n    const dstId = edge.end;\n    const srcNode = srcId ? nodeByIdMap.get(srcId) : void 0;\n    const dstNode = dstId ? nodeByIdMap.get(dstId) : void 0;\n    const src = srcNode ? rectOfNodeBounds(srcNode) : void 0;\n    const dst = dstNode ? rectOfNodeBounds(dstNode) : void 0;\n    return src && dst ? { src, dst } : void 0;\n  }, \"endpointRectsFor\");\n  const externalRailForSegment = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, points, segment) => {\n    if (segment.index <= 0 || segment.index + 1 >= points.length - 1) {\n      return void 0;\n    }\n    const endpointRects = endpointRectsFor(edge);\n    if (!endpointRects) {\n      return void 0;\n    }\n    if (segment.vertical) {\n      const coord = segment.a.x;\n      const leftBound = Math.min(endpointRects.src.left, endpointRects.dst.left);\n      const rightBound = Math.max(endpointRects.src.right, endpointRects.dst.right);\n      const side = coord < leftBound - EPS_LOCAL ? \"left\" : coord > rightBound + EPS_LOCAL ? \"right\" : void 0;\n      if (!side) {\n        return void 0;\n      }\n      return {\n        edge,\n        points,\n        segmentIndex: segment.index,\n        axis: \"vertical\",\n        side,\n        coord,\n        min: Math.min(segment.a.y, segment.b.y),\n        max: Math.max(segment.a.y, segment.b.y)\n      };\n    }\n    if (segment.horizontal) {\n      const coord = segment.a.y;\n      const topBound = Math.min(endpointRects.src.top, endpointRects.dst.top);\n      const bottomBound = Math.max(endpointRects.src.bottom, endpointRects.dst.bottom);\n      const side = coord < topBound - EPS_LOCAL ? \"top\" : coord > bottomBound + EPS_LOCAL ? \"bottom\" : void 0;\n      if (!side) {\n        return void 0;\n      }\n      return {\n        edge,\n        points,\n        segmentIndex: segment.index,\n        axis: \"horizontal\",\n        side,\n        coord,\n        min: Math.min(segment.a.x, segment.b.x),\n        max: Math.max(segment.a.x, segment.b.x)\n      };\n    }\n    return void 0;\n  }, \"externalRailForSegment\");\n  const collectExternalRails = /* @__PURE__ */ chunk_Y2CYZVJY_name(() => {\n    const rails = [];\n    for (const edge of visibleEdges) {\n      const points = replacementPointsFor(edge);\n      for (const segment of segmentsFor(points)) {\n        const rail = externalRailForSegment(edge, points, segment);\n        if (rail) {\n          rails.push(rail);\n        }\n      }\n    }\n    return rails;\n  }, \"collectExternalRails\");\n  const railsInteract = /* @__PURE__ */ chunk_Y2CYZVJY_name((a, b) => a.edge !== b.edge && a.axis === b.axis && a.side === b.side && overlapLength(a.min, a.max, b.min, b.max) >= MIN_SHARED, \"railsInteract\");\n  const connectedComponents = /* @__PURE__ */ chunk_Y2CYZVJY_name((rails) => {\n    const result = [];\n    const seen = /* @__PURE__ */ new Set();\n    for (const rail of rails) {\n      if (seen.has(rail)) {\n        continue;\n      }\n      const queue = [rail];\n      const component = [];\n      seen.add(rail);\n      while (queue.length > 0) {\n        const current = queue.pop();\n        component.push(current);\n        for (const next of rails) {\n          if (!seen.has(next) && railsInteract(current, next)) {\n            seen.add(next);\n            queue.push(next);\n          }\n        }\n      }\n      if (component.length > 1) {\n        result.push(component);\n      }\n    }\n    return result;\n  }, \"connectedComponents\");\n  const uniqueCoordsFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((component) => {\n    const coords = [];\n    for (const rail of component) {\n      if (!coords.some((coord) => Math.abs(coord - rail.coord) < EPS_LOCAL)) {\n        coords.push(rail.coord);\n      }\n    }\n    while (coords.length < component.length) {\n      const min = Math.min(...coords);\n      const max = Math.max(...coords);\n      const side = component[0].side;\n      coords.push(\n        side === \"left\" || side === \"top\" ? min - RAIL_CHANNEL_GAP * (component.length - coords.length) : max + RAIL_CHANNEL_GAP * (component.length - coords.length)\n      );\n    }\n    return coords;\n  }, \"uniqueCoordsFor\");\n  const coordinateAssignmentsFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((component) => {\n    const current = component.map((rail) => rail.coord);\n    const coords = uniqueCoordsFor(component);\n    const assignments = [];\n    if (component.length <= MAX_EXHAUSTIVE_COMPONENT) {\n      const used = new Array(coords.length).fill(false);\n      const next = [];\n      const visit = /* @__PURE__ */ chunk_Y2CYZVJY_name(() => {\n        if (next.length === component.length) {\n          if (next.some((coord, index) => Math.abs(coord - current[index]) >= EPS_LOCAL)) {\n            assignments.push([...next]);\n          }\n          return;\n        }\n        for (const [i, coord] of coords.entries()) {\n          if (used[i]) {\n            continue;\n          }\n          used[i] = true;\n          next.push(coord);\n          visit();\n          next.pop();\n          used[i] = false;\n        }\n      }, \"visit\");\n      visit();\n      return assignments;\n    }\n    for (let i = 0; i < current.length; i++) {\n      for (let j = i + 1; j < current.length; j++) {\n        const assignment = [...current];\n        [assignment[i], assignment[j]] = [assignment[j], assignment[i]];\n        assignments.push(assignment);\n      }\n    }\n    return assignments;\n  }, \"coordinateAssignmentsFor\");\n  const replacementsForAssignment = /* @__PURE__ */ chunk_Y2CYZVJY_name((component, assignment) => {\n    const draftByEdge = /* @__PURE__ */ new Map();\n    for (const [i, rail] of component.entries()) {\n      const coord = assignment[i];\n      const points = draftByEdge.get(rail.edge) ?? rail.points.map((point) => ({ x: point.x, y: point.y }));\n      if (rail.axis === \"vertical\") {\n        points[rail.segmentIndex].x = coord;\n        points[rail.segmentIndex + 1].x = coord;\n      } else {\n        points[rail.segmentIndex].y = coord;\n        points[rail.segmentIndex + 1].y = coord;\n      }\n      draftByEdge.set(rail.edge, points);\n    }\n    const replacements = /* @__PURE__ */ new Map();\n    for (const [edge, points] of draftByEdge) {\n      const simplified = simplifyPolyline(dedupeConsecutivePoints(points));\n      if (segmentsFor(simplified).length !== simplified.length - 1) {\n        return void 0;\n      }\n      replacements.set(edge, simplified);\n    }\n    return replacements;\n  }, \"replacementsForAssignment\");\n  const candidateIsSafe = /* @__PURE__ */ chunk_Y2CYZVJY_name((replacements) => {\n    for (const [edge, points] of replacements) {\n      const endpointIds = [\n        edge.start,\n        edge.end\n      ].filter((id) => Boolean(id));\n      for (const segment of segmentsFor(points)) {\n        if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -BUFFER)) {\n          return false;\n        }\n        if (segmentHitsAnyRect(segment.a, segment.b, labelRects, [], -BUFFER)) {\n          return false;\n        }\n      }\n    }\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const first = visibleEdges[i];\n      const firstChanged = replacements.has(first);\n      const firstSegments = segmentsFor(replacementPointsFor(first, replacements));\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const second = visibleEdges[j];\n        if (!firstChanged && !replacements.has(second)) {\n          continue;\n        }\n        const secondSegments = segmentsFor(replacementPointsFor(second, replacements));\n        for (const firstSegment of firstSegments) {\n          for (const secondSegment of secondSegments) {\n            if (sameAxisSegmentOverlapLength(firstSegment, secondSegment, 0.5) >= MIN_SHARED) {\n              return false;\n            }\n          }\n        }\n      }\n    }\n    return true;\n  }, \"candidateIsSafe\");\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    const currentCrossings = strictCrossingCount();\n    if (currentCrossings === 0) {\n      return;\n    }\n    let bestReplacements;\n    let bestCrossings = currentCrossings;\n    let bestBends = totalBends();\n    let bestDisplacement = Number.POSITIVE_INFINITY;\n    for (const component of connectedComponents(collectExternalRails())) {\n      for (const assignment of coordinateAssignmentsFor(component)) {\n        const replacements = replacementsForAssignment(component, assignment);\n        if (!replacements || !candidateIsSafe(replacements)) {\n          continue;\n        }\n        const candidateCrossings = strictCrossingCount(replacements);\n        if (candidateCrossings >= currentCrossings) {\n          continue;\n        }\n        const candidateBends = totalBends(replacements);\n        const candidateDisplacement = component.reduce(\n          (sum, rail, index) => sum + Math.abs(assignment[index] - rail.coord),\n          0\n        );\n        if (candidateCrossings > bestCrossings || candidateCrossings === bestCrossings && (candidateBends > bestBends || candidateBends === bestBends && candidateDisplacement >= bestDisplacement)) {\n          continue;\n        }\n        bestReplacements = replacements;\n        bestCrossings = candidateCrossings;\n        bestBends = candidateBends;\n        bestDisplacement = candidateDisplacement;\n      }\n    }\n    if (!bestReplacements) {\n      return;\n    }\n    for (const [edge, points] of bestReplacements) {\n      edge.points = points;\n    }\n  }\n}\nchunk_Y2CYZVJY_name(reassignCrossingExternalRailChannels, \"reassignCrossingExternalRailChannels\");\nfunction shortcutRedundantOrthogonalJogs(edges, nodeByIdMap) {\n  const BUFFER = 2;\n  const MAX_ITERATIONS = 8;\n  const { realNodeRects, labelNodeRects: labelRects } = collectNodeRectEntries(\n    nodeByIdMap.values()\n  );\n  const visibleEdges = edges.filter((edge) => !edge.isLayoutOnly);\n  const pointsFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, replacementEdge, replacement) => dedupeConsecutivePoints(\n    edge === replacementEdge ? replacement ?? [] : edge.points ?? []\n  ), \"pointsFor\");\n  const pathLength = /* @__PURE__ */ chunk_Y2CYZVJY_name((points) => segmentsFor(points).reduce((sum, segment) => {\n    const dx = segment.a.x - segment.b.x;\n    const dy = segment.a.y - segment.b.y;\n    return sum + Math.hypot(dx, dy);\n  }, 0), \"pathLength\");\n  const strictCrossingCount = /* @__PURE__ */ chunk_Y2CYZVJY_name((replacementEdge, replacement) => {\n    let count = 0;\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const firstSegments = segmentsFor(pointsFor(visibleEdges[i], replacementEdge, replacement));\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const secondSegments = segmentsFor(\n          pointsFor(visibleEdges[j], replacementEdge, replacement)\n        );\n        for (const firstSegment of firstSegments) {\n          for (const secondSegment of secondSegments) {\n            if (orthogonalSegmentsStrictlyCross(\n              firstSegment.a,\n              firstSegment.b,\n              secondSegment.a,\n              secondSegment.b,\n              EPS_LOCAL\n            )) {\n              count++;\n            }\n          }\n        }\n      }\n    }\n    return count;\n  }, \"strictCrossingCount\");\n  const segmentRunsAlongRectBorder = /* @__PURE__ */ chunk_Y2CYZVJY_name((segment, rect) => {\n    if (segment.horizontal) {\n      const y = segment.a.y;\n      const onBorder = Math.abs(y - rect.top) < 1 || Math.abs(y - rect.bottom) < 1;\n      return onBorder && overlapLength(segment.a.x, segment.b.x, rect.left, rect.right) >= MIN_SHARED;\n    }\n    if (segment.vertical) {\n      const x = segment.a.x;\n      const onBorder = Math.abs(x - rect.left) < 1 || Math.abs(x - rect.right) < 1;\n      return onBorder && overlapLength(segment.a.y, segment.b.y, rect.top, rect.bottom) >= MIN_SHARED;\n    }\n    return false;\n  }, \"segmentRunsAlongRectBorder\");\n  const endpointRectsFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge) => {\n    const endpointIds = [edge.start, edge.end].filter(\n      (id) => Boolean(id)\n    );\n    const rects = [];\n    for (const id of endpointIds) {\n      const node = nodeByIdMap.get(id);\n      const rect = node ? rectOfNodeBounds(node) : void 0;\n      if (rect) {\n        rects.push(rect);\n      }\n    }\n    return rects;\n  }, \"endpointRectsFor\");\n  const shortcutCandidatesAt = /* @__PURE__ */ chunk_Y2CYZVJY_name((points, index) => {\n    if (index + 3 >= points.length) {\n      return [];\n    }\n    const p0 = points[index];\n    const p1 = points[index + 1];\n    const p2 = points[index + 2];\n    const p3 = points[index + 3];\n    const isHVH = isHorizontalSegment(p0, p1, EPS_LOCAL) && isVerticalSegment(p1, p2, EPS_LOCAL) && isHorizontalSegment(p2, p3, EPS_LOCAL);\n    const isVHV = isVerticalSegment(p0, p1, EPS_LOCAL) && isHorizontalSegment(p1, p2, EPS_LOCAL) && isVerticalSegment(p2, p3, EPS_LOCAL);\n    if (!isHVH && !isVHV) {\n      return [];\n    }\n    const outerSegmentsOppose = isHVH ? Math.sign(p1.x - p0.x) !== Math.sign(p3.x - p2.x) : Math.sign(p1.y - p0.y) !== Math.sign(p3.y - p2.y);\n    if (!outerSegmentsOppose) {\n      return [];\n    }\n    const corners = sameX(p0, p3, EPS_LOCAL) || sameY(p0, p3, EPS_LOCAL) ? [] : [\n      { x: p0.x, y: p3.y },\n      { x: p3.x, y: p0.y }\n    ];\n    const rawCandidates = corners.length === 0 ? [[...points.slice(0, index + 1), ...points.slice(index + 3)]] : corners.map((corner) => [\n      ...points.slice(0, index + 1),\n      corner,\n      ...points.slice(index + 3)\n    ]);\n    const seen = /* @__PURE__ */ new Set();\n    return rawCandidates.map((candidate) => simplifyPolyline(dedupeConsecutivePoints(candidate))).filter((candidate) => {\n      if (segmentsFor(candidate).length !== candidate.length - 1) {\n        return false;\n      }\n      if (!candidate.some((point) => samePoint(point, p3, EPS_LOCAL))) {\n        return false;\n      }\n      const key = candidate.map((point) => `${point.x.toFixed(3)},${point.y.toFixed(3)}`).join(\"|\");\n      if (seen.has(key)) {\n        return false;\n      }\n      seen.add(key);\n      return true;\n    });\n  }, \"shortcutCandidatesAt\");\n  const candidateIsSafe = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, candidate, currentCrossings) => {\n    const endpointIds = [edge.start, edge.end].filter(\n      (id) => Boolean(id)\n    );\n    const endpointRects = endpointRectsFor(edge);\n    for (const segment of segmentsFor(candidate)) {\n      if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -BUFFER)) {\n        return false;\n      }\n      if (segmentHitsAnyRect(segment.a, segment.b, labelRects, [], -BUFFER)) {\n        return false;\n      }\n      if (endpointRects.some((rect) => segmentRunsAlongRectBorder(segment, rect))) {\n        return false;\n      }\n    }\n    for (const other of visibleEdges) {\n      if (other === edge) {\n        continue;\n      }\n      for (const candidateSegment of segmentsFor(candidate)) {\n        for (const otherSegment of segmentsFor(pointsFor(other))) {\n          if (sameAxisSegmentOverlapLength(candidateSegment, otherSegment, 0.5) >= MIN_SHARED) {\n            return false;\n          }\n        }\n      }\n    }\n    return strictCrossingCount(edge, candidate) <= currentCrossings;\n  }, \"candidateIsSafe\");\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    const currentCrossings = strictCrossingCount();\n    let bestEdge;\n    let bestPath;\n    let bestCrossings = currentCrossings;\n    let bestBends = Number.POSITIVE_INFINITY;\n    let bestLength = Number.POSITIVE_INFINITY;\n    for (const edge of visibleEdges) {\n      const currentPoints = pointsFor(edge);\n      const currentBends = countOrthogonalBends(currentPoints, EPS_LOCAL);\n      const currentLength = pathLength(currentPoints);\n      for (let index = 0; index <= currentPoints.length - 4; index++) {\n        for (const candidate of shortcutCandidatesAt(currentPoints, index)) {\n          const candidateBends = countOrthogonalBends(candidate, EPS_LOCAL);\n          const candidateLength = pathLength(candidate);\n          const improvesShape = candidateBends < currentBends || candidateBends === currentBends && candidateLength < currentLength - EPS_LOCAL;\n          if (!improvesShape || !candidateIsSafe(edge, candidate, currentCrossings)) {\n            continue;\n          }\n          const candidateCrossings = strictCrossingCount(edge, candidate);\n          if (candidateCrossings > bestCrossings || candidateCrossings === bestCrossings && (candidateBends > bestBends || candidateBends === bestBends && candidateLength >= bestLength)) {\n            continue;\n          }\n          bestEdge = edge;\n          bestPath = candidate;\n          bestCrossings = candidateCrossings;\n          bestBends = candidateBends;\n          bestLength = candidateLength;\n        }\n      }\n    }\n    if (!bestEdge || !bestPath) {\n      return;\n    }\n    bestEdge.points = bestPath;\n  }\n}\nchunk_Y2CYZVJY_name(shortcutRedundantOrthogonalJogs, \"shortcutRedundantOrthogonalJogs\");\nfunction resolveRenderedOrthogonalCrossings(edges, nodeByIdMap) {\n  const ANCHOR = 20;\n  const EXTRA_CHANNEL_COUNT = 2;\n  const MAX_ITERATIONS = 4;\n  const MAX_PAIR_CANDIDATES_PER_EDGE = 48;\n  const realNodes = [];\n  for (const node of nodeByIdMap.values()) {\n    if (node.isGroup || node.isEdgeLabel) {\n      continue;\n    }\n    const cx = node.x ?? 0;\n    const cy = node.y ?? 0;\n    const rect = rectOfNodeBounds(node);\n    if (!rect) {\n      continue;\n    }\n    realNodes.push({\n      id: String(node.id ?? \"\"),\n      cx,\n      cy,\n      rect\n    });\n  }\n  if (realNodes.length === 0) {\n    return;\n  }\n  const nodeInfoById = new Map(realNodes.map((node) => [node.id, node]));\n  const realNodeRects = realNodes.map((node) => ({ id: node.id, rect: node.rect }));\n  const sides = [\"top\", \"bottom\", \"left\", \"right\"];\n  const outsideTracks = {\n    top: Math.min(...realNodes.map((node) => node.rect.top)) - ANCHOR,\n    bottom: Math.max(...realNodes.map((node) => node.rect.bottom)) + ANCHOR,\n    left: Math.min(...realNodes.map((node) => node.rect.left)) - ANCHOR,\n    right: Math.max(...realNodes.map((node) => node.rect.right)) + ANCHOR\n  };\n  const visibleEdges = edges.filter((edge) => !edge.isLayoutOnly);\n  const edgeIndex = new Map(visibleEdges.map((edge, index) => [edge, index]));\n  const outwardTracksForSide = /* @__PURE__ */ chunk_Y2CYZVJY_name((side) => {\n    const outward = side === \"left\" || side === \"top\" ? -1 : 1;\n    const tracks = [];\n    for (let channel = 0; channel <= EXTRA_CHANNEL_COUNT; channel++) {\n      tracks.push(outsideTracks[side] + outward * ANCHOR * channel);\n    }\n    return tracks;\n  }, \"outwardTracksForSide\");\n  const replacementPointsFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, replacements = /* @__PURE__ */ new Map()) => dedupeConsecutivePoints(\n    replacements.get(edge) ?? edge.points ?? []\n  ), \"replacementPointsFor\");\n  const crossingCountBetweenSegments = /* @__PURE__ */ chunk_Y2CYZVJY_name((firstSegments, secondSegments) => {\n    let count = 0;\n    for (const firstSegment of firstSegments) {\n      for (const secondSegment of secondSegments) {\n        if (orthogonalSegmentsStrictlyCross(\n          firstSegment.a,\n          firstSegment.b,\n          secondSegment.a,\n          secondSegment.b,\n          EPS_LOCAL\n        )) {\n          count++;\n        }\n      }\n    }\n    return count;\n  }, \"crossingCountBetweenSegments\");\n  const crossingCountBetweenPaths = /* @__PURE__ */ chunk_Y2CYZVJY_name((first, second) => crossingCountBetweenSegments(segmentsFor(first), segmentsFor(second)), \"crossingCountBetweenPaths\");\n  const crossingSnapshot = /* @__PURE__ */ chunk_Y2CYZVJY_name((replacements = /* @__PURE__ */ new Map()) => {\n    let count = 0;\n    const pairs = [];\n    const edgeSet = /* @__PURE__ */ new Set();\n    const edgeOrder = [];\n    const addEdge = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge) => {\n      if (!edgeSet.has(edge)) {\n        edgeSet.add(edge);\n        edgeOrder.push(edge);\n      }\n    }, \"addEdge\");\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const first = visibleEdges[i];\n      const firstPoints = replacementPointsFor(first, replacements);\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const second = visibleEdges[j];\n        const pairCount = crossingCountBetweenPaths(\n          firstPoints,\n          replacementPointsFor(second, replacements)\n        );\n        if (pairCount > 0) {\n          count += pairCount;\n          pairs.push({ first, second, count: pairCount });\n          addEdge(first);\n          addEdge(second);\n        }\n      }\n    }\n    edgeOrder.sort((a, b) => (edgeIndex.get(a) ?? 0) - (edgeIndex.get(b) ?? 0));\n    return {\n      count,\n      pairs,\n      edgeSet,\n      edges: edgeOrder\n    };\n  }, \"crossingSnapshot\");\n  const crossingCountWithReplacements = /* @__PURE__ */ chunk_Y2CYZVJY_name((current, replacements) => {\n    const changed = new Set(replacements.keys());\n    if (changed.size === 0) {\n      return current.count;\n    }\n    let currentAffected = 0;\n    for (const pair of current.pairs) {\n      if (changed.has(pair.first) || changed.has(pair.second)) {\n        currentAffected += pair.count;\n      }\n    }\n    let replacementAffected = 0;\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const first = visibleEdges[i];\n      const firstChanged = changed.has(first);\n      const firstPoints = replacementPointsFor(first, replacements);\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const second = visibleEdges[j];\n        if (!firstChanged && !changed.has(second)) {\n          continue;\n        }\n        replacementAffected += crossingCountBetweenPaths(\n          firstPoints,\n          replacementPointsFor(second, replacements)\n        );\n      }\n    }\n    return current.count - currentAffected + replacementAffected;\n  }, \"crossingCountWithReplacements\");\n  const crossingComponents = /* @__PURE__ */ chunk_Y2CYZVJY_name((snapshot) => {\n    const neighbors = /* @__PURE__ */ new Map();\n    for (const pair of snapshot.pairs) {\n      const firstNeighbors = neighbors.get(pair.first) ?? /* @__PURE__ */ new Set();\n      firstNeighbors.add(pair.second);\n      neighbors.set(pair.first, firstNeighbors);\n      const secondNeighbors = neighbors.get(pair.second) ?? /* @__PURE__ */ new Set();\n      secondNeighbors.add(pair.first);\n      neighbors.set(pair.second, secondNeighbors);\n    }\n    const components = [];\n    const seen = /* @__PURE__ */ new Set();\n    for (const edge of snapshot.edges) {\n      if (seen.has(edge)) {\n        continue;\n      }\n      const queue = [edge];\n      const component = [];\n      seen.add(edge);\n      while (queue.length > 0) {\n        const current = queue.pop();\n        component.push(current);\n        for (const next of neighbors.get(current) ?? []) {\n          if (!seen.has(next)) {\n            seen.add(next);\n            queue.push(next);\n          }\n        }\n      }\n      component.sort((a, b) => (edgeIndex.get(a) ?? 0) - (edgeIndex.get(b) ?? 0));\n      if (component.length > 1) {\n        components.push(component);\n      }\n    }\n    return components;\n  }, \"crossingComponents\");\n  const endpointIdsFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge) => [edge.start, edge.end].filter(\n    (id) => Boolean(id)\n  ), \"endpointIdsFor\");\n  const pairSearchGroups = /* @__PURE__ */ chunk_Y2CYZVJY_name((snapshot) => {\n    const groups = [];\n    for (const component of crossingComponents(snapshot)) {\n      const componentSet = new Set(component);\n      const componentEndpointIds = new Set(component.flatMap((edge) => endpointIdsFor(edge)));\n      const group = [...component];\n      for (const edge of visibleEdges) {\n        if (componentSet.has(edge)) {\n          continue;\n        }\n        if (endpointIdsFor(edge).some((id) => componentEndpointIds.has(id))) {\n          group.push(edge);\n        }\n      }\n      group.sort((a, b) => (edgeIndex.get(a) ?? 0) - (edgeIndex.get(b) ?? 0));\n      groups.push(group);\n    }\n    return groups;\n  }, \"pairSearchGroups\");\n  const crossingCountWithSingleReplacement = /* @__PURE__ */ chunk_Y2CYZVJY_name((current, edge, replacement) => crossingCountWithReplacements(\n    current,\n    /* @__PURE__ */ new Map([[edge, replacement]])\n  ), \"crossingCountWithSingleReplacement\");\n  const currentCrossingsByEdge = /* @__PURE__ */ chunk_Y2CYZVJY_name((current) => {\n    const result = /* @__PURE__ */ new Map();\n    for (const pair of current.pairs) {\n      result.set(pair.first, (result.get(pair.first) ?? 0) + pair.count);\n      result.set(pair.second, (result.get(pair.second) ?? 0) + pair.count);\n    }\n    return result;\n  }, \"currentCrossingsByEdge\");\n  const pathLength = /* @__PURE__ */ chunk_Y2CYZVJY_name((points) => points.slice(1).reduce((sum, point, index) => {\n    const previous = points[index];\n    return sum + Math.abs(point.x - previous.x) + Math.abs(point.y - previous.y);\n  }, 0), \"pathLength\");\n  const totalBends = /* @__PURE__ */ chunk_Y2CYZVJY_name((replacements = /* @__PURE__ */ new Map()) => visibleEdges.reduce(\n    (sum, edge) => sum + countOrthogonalBends(replacementPointsFor(edge, replacements)),\n    0\n  ), \"totalBends\");\n  const totalLength = /* @__PURE__ */ chunk_Y2CYZVJY_name((replacements = /* @__PURE__ */ new Map()) => visibleEdges.reduce(\n    (sum, edge) => sum + pathLength(replacementPointsFor(edge, replacements)),\n    0\n  ), \"totalLength\");\n  const pathHasSegmentConflict = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, path, replacements = /* @__PURE__ */ new Map()) => {\n    const pathSegments = segmentsFor(path);\n    for (const other of visibleEdges) {\n      if (other === edge) {\n        continue;\n      }\n      for (const candidateSegment of pathSegments) {\n        for (const otherSegment of segmentsFor(replacementPointsFor(other, replacements))) {\n          if (sameAxisSegmentOverlapLength(candidateSegment, otherSegment, 0.5) >= MIN_SHARED) {\n            return true;\n          }\n        }\n      }\n    }\n    return false;\n  }, \"pathHasSegmentConflict\");\n  const pathHitsNode = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, path) => {\n    const endpointIds = [edge.start, edge.end].filter(\n      (id) => Boolean(id)\n    );\n    for (const segment of segmentsFor(path)) {\n      if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -2)) {\n        return true;\n      }\n    }\n    return false;\n  }, \"pathHitsNode\");\n  const pushOrthogonalCandidate = /* @__PURE__ */ chunk_Y2CYZVJY_name((candidates, points) => {\n    const candidate = simplifyPolyline(dedupeConsecutivePoints(points));\n    if (segmentsFor(candidate).length === candidate.length - 1) {\n      candidates.push(candidate);\n    }\n  }, \"pushOrthogonalCandidate\");\n  const sideIsHorizontal = /* @__PURE__ */ chunk_Y2CYZVJY_name((side) => side === \"left\" || side === \"right\", \"sideIsHorizontal\");\n  const localTrackForSameSide = /* @__PURE__ */ chunk_Y2CYZVJY_name((src, side, dst) => {\n    switch (side) {\n      case \"left\":\n        return Math.min(src.x, dst.x) - ANCHOR;\n      case \"right\":\n        return Math.max(src.x, dst.x) + ANCHOR;\n      case \"top\":\n        return Math.min(src.y, dst.y) - ANCHOR;\n      case \"bottom\":\n        return Math.max(src.y, dst.y) + ANCHOR;\n    }\n  }, \"localTrackForSameSide\");\n  const addSameSideCandidates = /* @__PURE__ */ chunk_Y2CYZVJY_name((candidates, src, srcSide, dst) => {\n    const outward = srcSide === \"left\" || srcSide === \"top\" ? -1 : 1;\n    const trackSeeds = [localTrackForSameSide(src, srcSide, dst), outsideTracks[srcSide]];\n    for (const seed of trackSeeds) {\n      for (let channel = 0; channel <= EXTRA_CHANNEL_COUNT; channel++) {\n        pushOrthogonalCandidate(\n          candidates,\n          buildSameSideTrackPath(src, srcSide, dst, seed + outward * ANCHOR * channel)\n        );\n      }\n    }\n  }, \"addSameSideCandidates\");\n  const addHorizontalToVerticalCandidates = /* @__PURE__ */ chunk_Y2CYZVJY_name((candidates, src, srcSide, dst, dstSide) => {\n    for (const xTrack of outwardTracksForSide(srcSide)) {\n      for (const yTrack of outwardTracksForSide(dstSide)) {\n        pushOrthogonalCandidate(candidates, [\n          src,\n          { x: xTrack, y: src.y },\n          { x: xTrack, y: yTrack },\n          { x: dst.x, y: yTrack },\n          dst\n        ]);\n      }\n    }\n  }, \"addHorizontalToVerticalCandidates\");\n  const addVerticalToHorizontalCandidates = /* @__PURE__ */ chunk_Y2CYZVJY_name((candidates, src, srcSide, dst, dstSide) => {\n    for (const yTrack of outwardTracksForSide(srcSide)) {\n      for (const xTrack of outwardTracksForSide(dstSide)) {\n        pushOrthogonalCandidate(candidates, [\n          src,\n          { x: src.x, y: yTrack },\n          { x: xTrack, y: yTrack },\n          { x: xTrack, y: dst.y },\n          dst\n        ]);\n      }\n    }\n  }, \"addVerticalToHorizontalCandidates\");\n  const addHorizontalPairCandidates = /* @__PURE__ */ chunk_Y2CYZVJY_name((candidates, src, srcSide, dst, dstSide) => {\n    const yTracks = [...outwardTracksForSide(\"top\"), ...outwardTracksForSide(\"bottom\")];\n    for (const srcTrack of outwardTracksForSide(srcSide)) {\n      for (const dstTrack of outwardTracksForSide(dstSide)) {\n        for (const yTrack of yTracks) {\n          pushOrthogonalCandidate(candidates, [\n            src,\n            { x: srcTrack, y: src.y },\n            { x: srcTrack, y: yTrack },\n            { x: dstTrack, y: yTrack },\n            { x: dstTrack, y: dst.y },\n            dst\n          ]);\n        }\n      }\n    }\n  }, \"addHorizontalPairCandidates\");\n  const addVerticalPairCandidates = /* @__PURE__ */ chunk_Y2CYZVJY_name((candidates, src, srcSide, dst, dstSide) => {\n    const xTracks = [...outwardTracksForSide(\"left\"), ...outwardTracksForSide(\"right\")];\n    for (const srcTrack of outwardTracksForSide(srcSide)) {\n      for (const dstTrack of outwardTracksForSide(dstSide)) {\n        for (const xTrack of xTracks) {\n          pushOrthogonalCandidate(candidates, [\n            src,\n            { x: src.x, y: srcTrack },\n            { x: xTrack, y: srcTrack },\n            { x: xTrack, y: dstTrack },\n            { x: dst.x, y: dstTrack },\n            dst\n          ]);\n        }\n      }\n    }\n  }, \"addVerticalPairCandidates\");\n  const dedupeCandidatePaths = /* @__PURE__ */ chunk_Y2CYZVJY_name((candidates) => {\n    const seen = /* @__PURE__ */ new Set();\n    return candidates.map((candidate) => dedupeConsecutivePoints(candidate)).filter((candidate) => {\n      const key = candidate.map((point) => `${point.x.toFixed(3)},${point.y.toFixed(3)}`).join(\"|\");\n      if (seen.has(key) || candidate.length < 2) {\n        return false;\n      }\n      seen.add(key);\n      return true;\n    });\n  }, \"dedupeCandidatePaths\");\n  const buildCandidatesForSides = /* @__PURE__ */ chunk_Y2CYZVJY_name((src, srcSide, dst, dstSide) => {\n    const candidates = [];\n    const base = buildOrthogonalPortPath(src, srcSide, dst, dstSide, ANCHOR, EPS_LOCAL);\n    if (base) {\n      pushOrthogonalCandidate(candidates, base);\n    }\n    if (srcSide === dstSide) {\n      addSameSideCandidates(candidates, src, srcSide, dst);\n    }\n    const srcHorizontal = sideIsHorizontal(srcSide);\n    const dstHorizontal = sideIsHorizontal(dstSide);\n    if (srcHorizontal && !dstHorizontal) {\n      addHorizontalToVerticalCandidates(candidates, src, srcSide, dst, dstSide);\n    } else if (!srcHorizontal && dstHorizontal) {\n      addVerticalToHorizontalCandidates(candidates, src, srcSide, dst, dstSide);\n    } else if (srcHorizontal) {\n      addHorizontalPairCandidates(candidates, src, srcSide, dst, dstSide);\n    } else {\n      addVerticalPairCandidates(candidates, src, srcSide, dst, dstSide);\n    }\n    return dedupeCandidatePaths(candidates);\n  }, \"buildCandidatesForSides\");\n  const addVerticalDepartureOuterTrackCandidates = /* @__PURE__ */ chunk_Y2CYZVJY_name((candidates, first, departure, dstNode) => {\n    const externalXTracks = [...outwardTracksForSide(\"left\"), ...outwardTracksForSide(\"right\")];\n    const externalYTracks = [...outwardTracksForSide(\"top\"), ...outwardTracksForSide(\"bottom\")];\n    for (const side of sides) {\n      const dst = portForRectSide(dstNode, side);\n      const targetYTracks = side === \"top\" || side === \"bottom\" ? outwardTracksForSide(side) : externalYTracks;\n      for (const track of externalXTracks) {\n        pushOrthogonalCandidate(candidates, [\n          first,\n          departure,\n          { x: track, y: departure.y },\n          { x: track, y: dst.y },\n          dst\n        ]);\n        for (const targetTrack of targetYTracks) {\n          pushOrthogonalCandidate(candidates, [\n            first,\n            departure,\n            { x: track, y: departure.y },\n            { x: track, y: targetTrack },\n            { x: dst.x, y: targetTrack },\n            dst\n          ]);\n        }\n      }\n    }\n  }, \"addVerticalDepartureOuterTrackCandidates\");\n  const addHorizontalDepartureOuterTrackCandidates = /* @__PURE__ */ chunk_Y2CYZVJY_name((candidates, first, departure, dstNode) => {\n    const externalXTracks = [...outwardTracksForSide(\"left\"), ...outwardTracksForSide(\"right\")];\n    const externalYTracks = [...outwardTracksForSide(\"top\"), ...outwardTracksForSide(\"bottom\")];\n    for (const side of sides) {\n      const dst = portForRectSide(dstNode, side);\n      const targetXTracks = side === \"left\" || side === \"right\" ? outwardTracksForSide(side) : externalXTracks;\n      for (const track of externalYTracks) {\n        pushOrthogonalCandidate(candidates, [\n          first,\n          departure,\n          { x: departure.x, y: track },\n          { x: dst.x, y: track },\n          dst\n        ]);\n        for (const targetTrack of targetXTracks) {\n          pushOrthogonalCandidate(candidates, [\n            first,\n            departure,\n            { x: departure.x, y: track },\n            { x: targetTrack, y: track },\n            { x: targetTrack, y: dst.y },\n            dst\n          ]);\n        }\n      }\n    }\n  }, \"addHorizontalDepartureOuterTrackCandidates\");\n  const terminalPreservingOuterTrackCandidates = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge) => {\n    const srcId = edge.start;\n    const dstId = edge.end;\n    const dstNode = dstId ? nodeInfoById.get(dstId) : void 0;\n    if (!srcId || !dstNode) {\n      return [];\n    }\n    const points = dedupeConsecutivePoints(edge.points ?? []);\n    if (points.length < 4) {\n      return [];\n    }\n    const first = points[0];\n    const departure = points[1];\n    const candidates = [];\n    if (isVerticalSegment(first, departure, EPS_LOCAL)) {\n      addVerticalDepartureOuterTrackCandidates(candidates, first, departure, dstNode);\n    } else if (isHorizontalSegment(first, departure, EPS_LOCAL)) {\n      addHorizontalDepartureOuterTrackCandidates(candidates, first, departure, dstNode);\n    }\n    return candidates;\n  }, \"terminalPreservingOuterTrackCandidates\");\n  const candidatePathsFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge) => {\n    const srcId = edge.start;\n    const dstId = edge.end;\n    const srcNode = srcId ? nodeInfoById.get(srcId) : void 0;\n    const dstNode = dstId ? nodeInfoById.get(dstId) : void 0;\n    if (!srcNode || !dstNode) {\n      return [];\n    }\n    const candidates = [];\n    for (const srcSide of sides) {\n      const srcPort = portForRectSide(srcNode, srcSide);\n      for (const dstSide of sides) {\n        candidates.push(\n          ...buildCandidatesForSides(srcPort, srcSide, portForRectSide(dstNode, dstSide), dstSide)\n        );\n      }\n    }\n    candidates.push(...terminalPreservingOuterTrackCandidates(edge));\n    return candidates;\n  }, \"candidatePathsFor\");\n  const currentSegmentsByEdge = /* @__PURE__ */ chunk_Y2CYZVJY_name(() => new Map(visibleEdges.map((edge) => [edge, segmentsFor(replacementPointsFor(edge))])), \"currentSegmentsByEdge\");\n  const sharedTrackConflictsFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, candidateSegments, baseSegments) => {\n    const conflicts = /* @__PURE__ */ new Set();\n    for (const other of visibleEdges) {\n      if (other === edge) {\n        continue;\n      }\n      const otherSegments = baseSegments.get(other) ?? segmentsFor(replacementPointsFor(other));\n      if (candidateSegments.some(\n        (candidateSegment) => otherSegments.some(\n          (otherSegment) => sameAxisSegmentOverlapLength(candidateSegment, otherSegment, 0.5) >= MIN_SHARED\n        )\n      )) {\n        conflicts.add(other);\n      }\n    }\n    return conflicts;\n  }, \"sharedTrackConflictsFor\");\n  const pairCandidatesFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, current, baseSegments, crossingCountByEdge) => {\n    const seen = /* @__PURE__ */ new Set();\n    const candidates = candidatePathsFor(edge).map((candidate) => simplifyPolyline(dedupeConsecutivePoints(candidate))).filter((candidate) => {\n      if (pathHitsNode(edge, candidate)) {\n        return false;\n      }\n      const key = candidate.map((point) => `${point.x.toFixed(3)},${point.y.toFixed(3)}`).join(\"|\");\n      if (seen.has(key) || candidate.length < 2) {\n        return false;\n      }\n      seen.add(key);\n      return true;\n    }).map((candidate) => {\n      const candidateSegments = segmentsFor(candidate);\n      let replacementAffected = 0;\n      for (const other of visibleEdges) {\n        if (other === edge) {\n          continue;\n        }\n        replacementAffected += crossingCountBetweenSegments(\n          candidateSegments,\n          baseSegments.get(other) ?? segmentsFor(replacementPointsFor(other))\n        );\n      }\n      return {\n        candidate,\n        candidateSegments,\n        crossings: current.count - (crossingCountByEdge.get(edge) ?? 0) + replacementAffected,\n        bends: countOrthogonalBends(candidate, EPS_LOCAL),\n        totalBends: countOrthogonalBends(candidate),\n        length: pathLength(candidate)\n      };\n    }).filter(({ crossings }) => crossings <= current.count).sort((a, b) => a.crossings - b.crossings || a.bends - b.bends || a.length - b.length);\n    return candidates.slice(0, MAX_PAIR_CANDIDATES_PER_EDGE).map((candidate) => {\n      return {\n        path: candidate.candidate,\n        segments: candidate.candidateSegments,\n        sharedTrackConflicts: sharedTrackConflictsFor(\n          edge,\n          candidate.candidateSegments,\n          baseSegments\n        ),\n        totalBends: candidate.totalBends,\n        length: candidate.length\n      };\n    });\n  }, \"pairCandidatesFor\");\n  const pairCrossingCount = /* @__PURE__ */ chunk_Y2CYZVJY_name((current, firstEdge, firstCandidate, secondEdge, secondCandidate, baseSegments) => {\n    let currentAffected = 0;\n    for (const pair of current.pairs) {\n      if (pair.first === firstEdge || pair.second === firstEdge || pair.first === secondEdge || pair.second === secondEdge) {\n        currentAffected += pair.count;\n      }\n    }\n    let replacementAffected = crossingCountBetweenSegments(\n      firstCandidate.segments,\n      secondCandidate.segments\n    );\n    for (const other of visibleEdges) {\n      if (other === firstEdge || other === secondEdge) {\n        continue;\n      }\n      const otherSegments = baseSegments.get(other) ?? segmentsFor(replacementPointsFor(other));\n      replacementAffected += crossingCountBetweenSegments(firstCandidate.segments, otherSegments) + crossingCountBetweenSegments(secondCandidate.segments, otherSegments);\n    }\n    return current.count - currentAffected + replacementAffected;\n  }, \"pairCrossingCount\");\n  const conflictsOnlyWith = /* @__PURE__ */ chunk_Y2CYZVJY_name((candidate, edge) => {\n    for (const conflict of candidate.sharedTrackConflicts) {\n      if (conflict !== edge) {\n        return false;\n      }\n    }\n    return true;\n  }, \"conflictsOnlyWith\");\n  const candidatesShareTrack = /* @__PURE__ */ chunk_Y2CYZVJY_name((firstCandidate, secondCandidate) => firstCandidate.segments.some(\n    (firstSegment) => secondCandidate.segments.some(\n      (secondSegment) => sameAxisSegmentOverlapLength(firstSegment, secondSegment, 0.5) >= MIN_SHARED\n    )\n  ), \"candidatesShareTrack\");\n  const pairCandidatesAreCompatible = /* @__PURE__ */ chunk_Y2CYZVJY_name((first, firstCandidate, second, secondCandidate) => conflictsOnlyWith(firstCandidate, second.edge) && conflictsOnlyWith(secondCandidate, first.edge) && !candidatesShareTrack(firstCandidate, secondCandidate), \"pairCandidatesAreCompatible\");\n  const scorePairReplacement = /* @__PURE__ */ chunk_Y2CYZVJY_name((context, first, firstCandidate, second, secondCandidate) => {\n    const crossings = pairCrossingCount(\n      context.current,\n      first.edge,\n      firstCandidate,\n      second.edge,\n      secondCandidate,\n      context.baseSegments\n    );\n    if (crossings >= context.current.count) {\n      return void 0;\n    }\n    return {\n      replacements: /* @__PURE__ */ new Map([\n        [first.edge, firstCandidate.path],\n        [second.edge, secondCandidate.path]\n      ]),\n      crossings,\n      bends: context.currentBends - (context.baseBendsByEdge.get(first.edge) ?? 0) - (context.baseBendsByEdge.get(second.edge) ?? 0) + firstCandidate.totalBends + secondCandidate.totalBends,\n      length: context.currentLength - (context.baseLengthByEdge.get(first.edge) ?? 0) - (context.baseLengthByEdge.get(second.edge) ?? 0) + firstCandidate.length + secondCandidate.length\n    };\n  }, \"scorePairReplacement\");\n  const pairScoreIsBetter = /* @__PURE__ */ chunk_Y2CYZVJY_name((candidate, best) => candidate.crossings < best.crossings || candidate.crossings === best.crossings && (candidate.bends < best.bends || candidate.bends === best.bends && candidate.length < best.length), \"pairScoreIsBetter\");\n  const bestScoreForOptionPair = /* @__PURE__ */ chunk_Y2CYZVJY_name((context, first, second, best) => {\n    let pairBest = best;\n    for (const firstCandidate of first.candidates) {\n      for (const secondCandidate of second.candidates) {\n        if (!pairCandidatesAreCompatible(first, firstCandidate, second, secondCandidate)) {\n          continue;\n        }\n        const score = scorePairReplacement(context, first, firstCandidate, second, secondCandidate);\n        if (score && pairScoreIsBetter(score, pairBest)) {\n          pairBest = score;\n        }\n      }\n    }\n    return pairBest;\n  }, \"bestScoreForOptionPair\");\n  const bestPairedReplacement = /* @__PURE__ */ chunk_Y2CYZVJY_name((current) => {\n    const currentBends = totalBends();\n    const currentLength = totalLength();\n    const baseSegments = currentSegmentsByEdge();\n    const crossingCountByEdge = currentCrossingsByEdge(current);\n    const baseBendsByEdge = new Map(\n      visibleEdges.map((edge) => [edge, countOrthogonalBends(replacementPointsFor(edge))])\n    );\n    const baseLengthByEdge = new Map(\n      visibleEdges.map((edge) => [edge, pathLength(replacementPointsFor(edge))])\n    );\n    const optionsByEdge = /* @__PURE__ */ new Map();\n    const groups = pairSearchGroups(current);\n    for (const group of groups) {\n      for (const edge of group) {\n        if (optionsByEdge.has(edge)) {\n          continue;\n        }\n        const candidates = pairCandidatesFor(edge, current, baseSegments, crossingCountByEdge);\n        if (candidates.length > 0) {\n          optionsByEdge.set(edge, { edge, candidates });\n        }\n      }\n    }\n    let best = {\n      replacements: /* @__PURE__ */ new Map(),\n      crossings: current.count,\n      bends: currentBends,\n      length: currentLength\n    };\n    const scoringContext = {\n      current,\n      currentBends,\n      currentLength,\n      baseBendsByEdge,\n      baseLengthByEdge,\n      baseSegments\n    };\n    for (const group of groups) {\n      const crossingEdgeSet = new Set(group.filter((edge) => current.edgeSet.has(edge)));\n      const options = group.map((edge) => optionsByEdge.get(edge)).filter((option) => Boolean(option));\n      for (let i = 0; i < options.length; i++) {\n        const first = options[i];\n        for (let j = i + 1; j < options.length; j++) {\n          const second = options[j];\n          if (!crossingEdgeSet.has(first.edge) && !crossingEdgeSet.has(second.edge)) {\n            continue;\n          }\n          best = bestScoreForOptionPair(scoringContext, first, second, best);\n        }\n      }\n    }\n    return best.replacements.size > 0 ? best.replacements : void 0;\n  }, \"bestPairedReplacement\");\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    const current = crossingSnapshot();\n    const currentCrossings = current.count;\n    if (currentCrossings === 0) {\n      return;\n    }\n    let bestEdge;\n    let bestPath;\n    let bestCrossings = currentCrossings;\n    let bestBends = Number.POSITIVE_INFINITY;\n    for (const edge of current.edges) {\n      const currentEdgeBends = countOrthogonalBends(replacementPointsFor(edge), EPS_LOCAL);\n      for (const candidate of candidatePathsFor(edge)) {\n        const candidateHitsNode = pathHitsNode(edge, candidate);\n        const candidateHasSegmentConflict = !candidateHitsNode && pathHasSegmentConflict(edge, candidate);\n        const candidateCrossings = crossingCountWithSingleReplacement(current, edge, candidate);\n        const candidateBends = countOrthogonalBends(candidate, EPS_LOCAL);\n        if (candidateHitsNode || candidateHasSegmentConflict) {\n          continue;\n        }\n        const improvesCurrentEdge = candidateCrossings < currentCrossings || candidateCrossings === currentCrossings && candidateBends < currentEdgeBends;\n        if (!improvesCurrentEdge) {\n          continue;\n        }\n        if (candidateCrossings > bestCrossings || candidateCrossings === bestCrossings && candidateBends >= bestBends) {\n          continue;\n        }\n        bestEdge = edge;\n        bestPath = candidate;\n        bestCrossings = candidateCrossings;\n        bestBends = candidateBends;\n      }\n    }\n    if (bestEdge && bestPath) {\n      bestEdge.points = bestPath;\n      continue;\n    }\n    const pairedReplacement = bestPairedReplacement(current);\n    if (!pairedReplacement) {\n      return;\n    }\n    for (const [edge, points] of pairedReplacement) {\n      edge.points = points;\n    }\n  }\n}\nchunk_Y2CYZVJY_name(resolveRenderedOrthogonalCrossings, \"resolveRenderedOrthogonalCrossings\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/detourSimplification.ts\nvar EPS4 = 1e-3;\nvar MIN_SHARED2 = 8;\nfunction simplifyDetouredEdges(edges, nodes) {\n  const { nodeInfoById, realNodeRects } = collectRealNodeBounds(nodes);\n  const sides = [\"top\", \"bottom\", \"left\", \"right\"];\n  const ANCHOR = 20;\n  const outsideTracks = {\n    top: Math.min(...realNodeRects.map((node) => node.rect.top)) - ANCHOR,\n    bottom: Math.max(...realNodeRects.map((node) => node.rect.bottom)) + ANCHOR,\n    left: Math.min(...realNodeRects.map((node) => node.rect.left)) - ANCHOR,\n    right: Math.max(...realNodeRects.map((node) => node.rect.right)) + ANCHOR\n  };\n  const buildOrthogonalPathCandidates = /* @__PURE__ */ chunk_Y2CYZVJY_name((src, srcSide, dst, dstSide) => {\n    const paths = [];\n    const base = buildOrthogonalPortPath(src, srcSide, dst, dstSide, ANCHOR, EPS4);\n    if (base) {\n      paths.push(base);\n    }\n    if (srcSide === dstSide) {\n      paths.push(buildSameSideTrackPath(src, srcSide, dst, outsideTracks[srcSide]));\n    }\n    return paths;\n  }, \"buildOrthogonalPathCandidates\");\n  const pathHitsNode = /* @__PURE__ */ chunk_Y2CYZVJY_name((pts, excludeIds) => {\n    for (let i = 0; i < pts.length - 1; i++) {\n      const a = pts[i];\n      const b = pts[i + 1];\n      if (segmentHitsAnyRect(a, b, realNodeRects, excludeIds, 1)) {\n        return true;\n      }\n    }\n    return false;\n  }, \"pathHitsNode\");\n  const pathConflictCount = /* @__PURE__ */ chunk_Y2CYZVJY_name((path, currentEdge, includeIncidentEdges = false) => {\n    let conflicts = 0;\n    const pathSegments = orthogonalSegmentsForPoints(path, EPS4);\n    const currentStart = currentEdge.start;\n    const currentEnd = currentEdge.end;\n    for (const other of edges) {\n      if (other === currentEdge || other.isLayoutOnly) {\n        continue;\n      }\n      const otherStart = other.start;\n      const otherEnd = other.end;\n      if (!includeIncidentEdges && currentStart && currentEnd && (otherStart === currentStart || otherStart === currentEnd || otherEnd === currentStart || otherEnd === currentEnd)) {\n        continue;\n      }\n      const otherPts = other.points;\n      if (!otherPts || otherPts.length < 2) {\n        continue;\n      }\n      for (const pathSegment of pathSegments) {\n        for (const otherSegment of orthogonalSegmentsForPoints(otherPts, EPS4)) {\n          if (orthogonalSegmentsCross(\n            pathSegment.a,\n            pathSegment.b,\n            otherSegment.a,\n            otherSegment.b,\n            EPS4,\n            EPS4\n          )) {\n            conflicts++;\n            continue;\n          }\n          if (sameAxisSegmentOverlapLength(pathSegment, otherSegment, EPS4) >= MIN_SHARED2) {\n            conflicts++;\n          }\n        }\n      }\n    }\n    return conflicts;\n  }, \"pathConflictCount\");\n  const BEND_THRESHOLD = 4;\n  const nearestSideOfRect = /* @__PURE__ */ chunk_Y2CYZVJY_name((pt, info) => {\n    const dTop = Math.abs(pt.y - info.rect.top);\n    const dBottom = Math.abs(pt.y - info.rect.bottom);\n    const dLeft = Math.abs(pt.x - info.rect.left);\n    const dRight = Math.abs(pt.x - info.rect.right);\n    let best = \"top\";\n    let bestDist = dTop;\n    if (dBottom < bestDist) {\n      best = \"bottom\";\n      bestDist = dBottom;\n    }\n    if (dLeft < bestDist) {\n      best = \"left\";\n      bestDist = dLeft;\n    }\n    if (dRight < bestDist) {\n      best = \"right\";\n      bestDist = dRight;\n    }\n    return best;\n  }, \"nearestSideOfRect\");\n  const faceClaims = /* @__PURE__ */ new Map();\n  const addFaceClaim = /* @__PURE__ */ chunk_Y2CYZVJY_name((nodeId, side, edgeId) => {\n    const claims = faceClaims.get(nodeId) ?? [];\n    claims.push({ side, edgeId });\n    faceClaims.set(nodeId, claims);\n  }, \"addFaceClaim\");\n  for (const e of edges) {\n    if (e.isLayoutOnly) {\n      continue;\n    }\n    const pts = e.points ?? [];\n    if (pts.length < 1) {\n      continue;\n    }\n    const eId = e.id ?? \"\";\n    const startId = e.start;\n    const endId = e.end;\n    if (startId) {\n      const info = nodeInfoById.get(startId);\n      if (info) {\n        addFaceClaim(startId, nearestSideOfRect(pts[0], info), eId);\n      }\n    }\n    if (endId) {\n      const info = nodeInfoById.get(endId);\n      if (info) {\n        addFaceClaim(endId, nearestSideOfRect(pts[pts.length - 1], info), eId);\n      }\n    }\n  }\n  const faceIsClaimed = /* @__PURE__ */ chunk_Y2CYZVJY_name((nodeId, side, ignoreEdgeId) => {\n    return faceClaims.get(nodeId)?.some((c) => c.edgeId !== ignoreEdgeId && c.side === side) ?? false;\n  }, \"faceIsClaimed\");\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const pts = edge.points;\n    if (!pts || pts.length < 2) {\n      continue;\n    }\n    const currentBends = countOrthogonalBends(pts, EPS4);\n    if (currentBends < BEND_THRESHOLD) {\n      continue;\n    }\n    const srcId = edge.start;\n    const dstId = edge.end;\n    if (!srcId || !dstId) {\n      continue;\n    }\n    const srcInfo = nodeInfoById.get(srcId);\n    const dstInfo = nodeInfoById.get(dstId);\n    if (!srcInfo || !dstInfo) {\n      continue;\n    }\n    const edgeId = edge.id ?? \"\";\n    const currentCrossingConflicts = pathConflictCount(pts, edge, true);\n    const currentNonIncidentConflicts = pathConflictCount(pts, edge);\n    let bestPath;\n    let bestCrossingConflicts = currentCrossingConflicts;\n    let bestBends = currentBends;\n    for (const srcSide of sides) {\n      if (faceIsClaimed(srcId, srcSide, edgeId)) {\n        continue;\n      }\n      const srcPort = portForRectSide(srcInfo, srcSide);\n      for (const dstSide of sides) {\n        if (faceIsClaimed(dstId, dstSide, edgeId)) {\n          continue;\n        }\n        const dstPort = portForRectSide(dstInfo, dstSide);\n        for (const path of buildOrthogonalPathCandidates(srcPort, srcSide, dstPort, dstSide)) {\n          if (pathHitsNode(path, [srcId, dstId])) {\n            continue;\n          }\n          const pathBends = countOrthogonalBends(path, EPS4);\n          if (currentCrossingConflicts > 0) {\n            const pathCrossingConflicts = pathConflictCount(path, edge, true);\n            if (pathCrossingConflicts > bestCrossingConflicts || pathCrossingConflicts === bestCrossingConflicts && pathBends >= bestBends) {\n              continue;\n            }\n            bestCrossingConflicts = pathCrossingConflicts;\n            bestBends = pathBends;\n            bestPath = path;\n            continue;\n          }\n          if (pathConflictCount(path, edge) > currentNonIncidentConflicts) {\n            continue;\n          }\n          if (pathBends < bestBends) {\n            bestBends = pathBends;\n            bestPath = path;\n          }\n        }\n      }\n    }\n    if (bestPath) {\n      edge.points = bestPath;\n      const refreshSrc = faceClaims.get(srcId);\n      if (refreshSrc) {\n        faceClaims.set(\n          srcId,\n          refreshSrc.filter((c) => c.edgeId !== edgeId)\n        );\n      }\n      const refreshDst = faceClaims.get(dstId);\n      if (refreshDst) {\n        faceClaims.set(\n          dstId,\n          refreshDst.filter((c) => c.edgeId !== edgeId)\n        );\n      }\n      addFaceClaim(srcId, nearestSideOfRect(bestPath[0], srcInfo), edgeId);\n      addFaceClaim(dstId, nearestSideOfRect(bestPath[bestPath.length - 1], dstInfo), edgeId);\n    }\n  }\n}\nchunk_Y2CYZVJY_name(simplifyDetouredEdges, \"simplifyDetouredEdges\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/labelAnchoring.ts\nvar EPS5 = 1e-3;\nvar MARKER_CLEARANCE_LENGTH = 10;\nvar MARKER_CLEARANCE_HALF_WIDTH = 7;\nfunction markerClearanceRectFor(pts, atStart) {\n  const terminalIndex = atStart ? 0 : pts.length - 1;\n  const step = atStart ? 1 : -1;\n  const tip = pts[terminalIndex];\n  const inner = pts[terminalIndex + step];\n  if (!tip || !inner) {\n    return void 0;\n  }\n  const dx = inner.x - tip.x;\n  const dy = inner.y - tip.y;\n  const len = Math.abs(dx) + Math.abs(dy);\n  if (len < EPS5) {\n    return void 0;\n  }\n  if (Math.abs(dy) <= EPS5) {\n    const x2 = tip.x + Math.sign(dx) * MARKER_CLEARANCE_LENGTH;\n    return {\n      left: Math.min(tip.x, x2),\n      right: Math.max(tip.x, x2),\n      top: tip.y - MARKER_CLEARANCE_HALF_WIDTH,\n      bottom: tip.y + MARKER_CLEARANCE_HALF_WIDTH\n    };\n  }\n  if (Math.abs(dx) <= EPS5) {\n    const y2 = tip.y + Math.sign(dy) * MARKER_CLEARANCE_LENGTH;\n    return {\n      left: tip.x - MARKER_CLEARANCE_HALF_WIDTH,\n      right: tip.x + MARKER_CLEARANCE_HALF_WIDTH,\n      top: Math.min(tip.y, y2),\n      bottom: Math.max(tip.y, y2)\n    };\n  }\n  return {\n    left: Math.min(tip.x, inner.x),\n    right: Math.max(tip.x, inner.x),\n    top: Math.min(tip.y, inner.y),\n    bottom: Math.max(tip.y, inner.y)\n  };\n}\nchunk_Y2CYZVJY_name(markerClearanceRectFor, \"markerClearanceRectFor\");\nfunction normalizeRect(rect) {\n  return {\n    left: Math.min(rect.left, rect.right),\n    right: Math.max(rect.left, rect.right),\n    top: Math.min(rect.top, rect.bottom),\n    bottom: Math.max(rect.top, rect.bottom)\n  };\n}\nchunk_Y2CYZVJY_name(normalizeRect, \"normalizeRect\");\nfunction labelOverlapsOwnMarker(rect, pts) {\n  const visiblePts = dedupeConsecutivePoints(pts);\n  const startMarker = markerClearanceRectFor(visiblePts, true);\n  const endMarker = markerClearanceRectFor(visiblePts, false);\n  return [startMarker, endMarker].some(\n    (marker) => marker && rectsOverlap(rect, normalizeRect(marker))\n  );\n}\nchunk_Y2CYZVJY_name(labelOverlapsOwnMarker, \"labelOverlapsOwnMarker\");\nfunction anchorLabelsToPolyline(edges, nodeByIdMap) {\n  const allEdgeSegments = [];\n  for (const other of edges) {\n    if (other.isLayoutOnly) {\n      continue;\n    }\n    const pts = other.points;\n    if (!pts || pts.length < 2) {\n      continue;\n    }\n    for (let i = 0; i < pts.length - 1; i++) {\n      allEdgeSegments.push({ edgeId: other.id, p1: pts[i], p2: pts[i + 1] });\n    }\n  }\n  const foreignNodeRects = [];\n  const laneGroups = [];\n  for (const n of nodeByIdMap.values()) {\n    const isGroup = n.isGroup;\n    const parentId = n.parentId;\n    if (isGroup && !parentId) {\n      const rect2 = rectOfNodeBounds(n);\n      if (rect2) {\n        laneGroups.push({\n          id: n.id,\n          rect: rect2\n        });\n      }\n      continue;\n    }\n    if (isGroup) {\n      continue;\n    }\n    if (n.isEdgeLabel) {\n      continue;\n    }\n    const rect = rectOfNodeBounds(n);\n    if (!rect) {\n      continue;\n    }\n    foreignNodeRects.push({\n      nodeId: n.id,\n      rect\n    });\n  }\n  const LABEL_PLACEMENT_BUFFER = 3;\n  const LABEL_LANE_MARGIN = 1;\n  const LABEL_ENDPOINT_CLEARANCE = 12;\n  const labelOverlapsForeignNode = /* @__PURE__ */ chunk_Y2CYZVJY_name((labelId, rect) => {\n    const buffered = inflateRect(rect, LABEL_PLACEMENT_BUFFER);\n    for (const { nodeId, rect: nr } of foreignNodeRects) {\n      if (nodeId === labelId) {\n        continue;\n      }\n      if (rectsOverlap(buffered, nr)) {\n        return true;\n      }\n    }\n    return false;\n  }, \"labelOverlapsForeignNode\");\n  const labelOverlapsForeignEdge = /* @__PURE__ */ chunk_Y2CYZVJY_name((edgeId, rect) => {\n    const buffered = inflateRect(rect, LABEL_PLACEMENT_BUFFER);\n    for (const s of allEdgeSegments) {\n      if (s.edgeId === edgeId) {\n        continue;\n      }\n      if (segmentBoundsOverlapRect(s.p1, s.p2, buffered)) {\n        return true;\n      }\n    }\n    return false;\n  }, \"labelOverlapsForeignEdge\");\n  const labelOverlapsAnything = /* @__PURE__ */ chunk_Y2CYZVJY_name((labelId, edgeId, rect) => labelOverlapsForeignNode(labelId, rect) || labelOverlapsForeignEdge(edgeId, rect), \"labelOverlapsAnything\");\n  const placedLabelRects = [];\n  const findContainingLane = /* @__PURE__ */ chunk_Y2CYZVJY_name((rect) => {\n    for (const { id, rect: laneRect } of laneGroups) {\n      if (rectContainsRect(laneRect, rect)) {\n        return id;\n      }\n    }\n    return void 0;\n  }, \"findContainingLane\");\n  const overlapsPlacedLabel = /* @__PURE__ */ chunk_Y2CYZVJY_name((labelId, rect) => placedLabelRects.some(\n    (placed) => placed.labelId !== labelId && rectsOverlap(rect, placed.rect)\n  ), \"overlapsPlacedLabel\");\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const labelId = edge.labelNodeId;\n    if (!labelId) {\n      continue;\n    }\n    const labelNode = nodeByIdMap.get(labelId);\n    if (!labelNode) {\n      continue;\n    }\n    const pts = edge.points;\n    if (!pts || pts.length < 2) {\n      continue;\n    }\n    const lw = labelNode.width ?? 0;\n    const lh = labelNode.height ?? 0;\n    if (lw <= 0 || lh <= 0) {\n      continue;\n    }\n    const segments = [];\n    for (let i = 0; i < pts.length - 1; i++) {\n      const a = pts[i];\n      const b = pts[i + 1];\n      const dx = Math.abs(a.x - b.x);\n      const dy = Math.abs(a.y - b.y);\n      if (dx < EPS5 && dy < EPS5) {\n        continue;\n      }\n      if (dx >= EPS5 && dy >= EPS5) {\n        continue;\n      }\n      segments.push({\n        idx: i,\n        length: dx + dy,\n        orientation: dx >= EPS5 ? \"horizontal\" : \"vertical\",\n        midX: (a.x + b.x) / 2,\n        midY: (a.y + b.y) / 2\n      });\n    }\n    if (segments.length === 0) {\n      continue;\n    }\n    const middleSegments = segments.length >= 3 ? segments.filter((s) => s.idx > 0 && s.idx < segments.length - 1) : segments;\n    const poolBase = middleSegments.length > 0 ? middleSegments : segments;\n    const labelLongAxis = lw >= lh ? \"horizontal\" : \"vertical\";\n    const rankSegments = /* @__PURE__ */ chunk_Y2CYZVJY_name((pool) => {\n      return [...pool].sort((a, b) => {\n        const aLongAxis = a.orientation === labelLongAxis;\n        const bLongAxis = b.orientation === labelLongAxis;\n        if (aLongAxis !== bLongAxis) {\n          return aLongAxis ? -1 : 1;\n        }\n        const aFits = a.length >= (a.orientation === \"horizontal\" ? lw : lh) + 2;\n        const bFits = b.length >= (b.orientation === \"horizontal\" ? lw : lh) + 2;\n        if (aFits !== bFits) {\n          return aFits ? -1 : 1;\n        }\n        return b.length - a.length;\n      });\n    }, \"rankSegments\");\n    const firstVisibleSegment = segments[0];\n    const lastVisibleSegment = segments[segments.length - 1];\n    const ALONG_SEGMENT_TS = [0.5, 0.25, 0.75, 0.05, 0.95, 0.15, 0.85, 0.1, 0.9];\n    const anchorAtT = /* @__PURE__ */ chunk_Y2CYZVJY_name((seg, t) => {\n      const a = pts[seg.idx];\n      const b = pts[seg.idx + 1];\n      return {\n        midX: a.x + (b.x - a.x) * t,\n        midY: a.y + (b.y - a.y) * t\n      };\n    }, \"anchorAtT\");\n    const clamp = /* @__PURE__ */ chunk_Y2CYZVJY_name((value, min, max) => Math.min(max, Math.max(min, value)), \"clamp\");\n    const pointInsideRectInclusive = /* @__PURE__ */ chunk_Y2CYZVJY_name((point, rect) => point.midX >= rect.left - EPS5 && point.midX <= rect.right + EPS5 && point.midY >= rect.top - EPS5 && point.midY <= rect.bottom + EPS5, \"pointInsideRectInclusive\");\n    const placementForAnchor = /* @__PURE__ */ chunk_Y2CYZVJY_name((anchor) => {\n      const centeredRect = rectFromCenterSize(anchor.midX, anchor.midY, lw, lh);\n      const centeredLane = findContainingLane(centeredRect);\n      if (centeredLane) {\n        return { laneId: centeredLane, anchor, rect: centeredRect };\n      }\n      const containingLane = laneGroups.find(({ rect }) => pointInsideRectInclusive(anchor, rect));\n      if (!containingLane) {\n        return void 0;\n      }\n      const minX = containingLane.rect.left + lw / 2 + LABEL_LANE_MARGIN;\n      const maxX = containingLane.rect.right - lw / 2 - LABEL_LANE_MARGIN;\n      const minY = containingLane.rect.top + lh / 2 + LABEL_LANE_MARGIN;\n      const maxY = containingLane.rect.bottom - lh / 2 - LABEL_LANE_MARGIN;\n      if (minX > maxX || minY > maxY) {\n        return void 0;\n      }\n      const clampedAnchor = {\n        midX: clamp(anchor.midX, minX, maxX),\n        midY: clamp(anchor.midY, minY, maxY)\n      };\n      const clampedRect = rectFromCenterSize(clampedAnchor.midX, clampedAnchor.midY, lw, lh);\n      return pointInsideRectInclusive(anchor, clampedRect) ? { laneId: containingLane.id, anchor: clampedAnchor, rect: clampedRect } : void 0;\n    }, \"placementForAnchor\");\n    const distanceAlongSegment = /* @__PURE__ */ chunk_Y2CYZVJY_name((seg, anchor, endpoint) => seg.orientation === \"horizontal\" ? Math.abs(anchor.midX - endpoint.x) : Math.abs(anchor.midY - endpoint.y), \"distanceAlongSegment\");\n    const labelClearsTerminalEndpoints = /* @__PURE__ */ chunk_Y2CYZVJY_name((seg, anchor) => {\n      const labelHalfExtent = seg.orientation === \"horizontal\" ? lw / 2 : lh / 2;\n      const requiredDistance = labelHalfExtent + LABEL_ENDPOINT_CLEARANCE;\n      if (seg === firstVisibleSegment) {\n        const start = pts[seg.idx];\n        if (distanceAlongSegment(seg, anchor, start) + EPS5 < requiredDistance) {\n          return false;\n        }\n      }\n      if (seg === lastVisibleSegment) {\n        const end = pts[seg.idx + 1];\n        if (distanceAlongSegment(seg, anchor, end) + EPS5 < requiredDistance) {\n          return false;\n        }\n      }\n      return true;\n    }, \"labelClearsTerminalEndpoints\");\n    const tryPool = /* @__PURE__ */ chunk_Y2CYZVJY_name((pool) => {\n      const rankedPool = rankSegments(pool);\n      for (const seg of rankedPool) {\n        for (const t of ALONG_SEGMENT_TS) {\n          const anchor = anchorAtT(seg, t);\n          if (!labelClearsTerminalEndpoints(seg, anchor)) {\n            continue;\n          }\n          const placement = placementForAnchor(anchor);\n          if (!placement) {\n            continue;\n          }\n          if (labelOverlapsOwnMarker(placement.rect, pts)) {\n            continue;\n          }\n          if (overlapsPlacedLabel(labelId, placement.rect)) {\n            continue;\n          }\n          if (!labelOverlapsAnything(labelId, edge.id, placement.rect)) {\n            return { laneId: placement.laneId, anchor: placement.anchor };\n          }\n        }\n      }\n      return void 0;\n    }, \"tryPool\");\n    const findLaneContainingFallback = /* @__PURE__ */ chunk_Y2CYZVJY_name((pool, requireEndpointClearance, allowForeignEdgeOverlap = false) => {\n      const rankedPool = rankSegments(pool);\n      for (const seg of rankedPool) {\n        const anchor = { midX: seg.midX, midY: seg.midY };\n        if (requireEndpointClearance && !labelClearsTerminalEndpoints(seg, anchor)) {\n          continue;\n        }\n        const placement = placementForAnchor(anchor);\n        if (placement && !labelOverlapsOwnMarker(placement.rect, pts) && !overlapsPlacedLabel(labelId, placement.rect) && !labelOverlapsForeignNode(labelId, placement.rect) && (allowForeignEdgeOverlap || !labelOverlapsForeignEdge(edge.id, placement.rect))) {\n          return { laneId: placement.laneId, anchor: placement.anchor };\n        }\n      }\n      return void 0;\n    }, \"findLaneContainingFallback\");\n    const chosen = tryPool(poolBase) ?? (poolBase.length < segments.length ? tryPool(segments) : void 0) ?? findLaneContainingFallback(segments, true) ?? findLaneContainingFallback(segments, false) ?? findLaneContainingFallback(segments, false, true);\n    if (chosen) {\n      labelNode.x = chosen.anchor.midX;\n      labelNode.y = chosen.anchor.midY;\n      labelNode.parentId = chosen.laneId;\n      const chosenRect = rectFromCenterSize(chosen.anchor.midX, chosen.anchor.midY, lw, lh);\n      const priorIdx = placedLabelRects.findIndex((placed) => placed.labelId === labelId);\n      if (priorIdx >= 0) {\n        placedLabelRects[priorIdx] = { labelId, rect: chosenRect };\n      } else {\n        placedLabelRects.push({ labelId, rect: chosenRect });\n      }\n    }\n  }\n}\nchunk_Y2CYZVJY_name(anchorLabelsToPolyline, \"anchorLabelsToPolyline\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/siblingSharedFaceRouting.ts\nvar EPS6 = 1e-6;\nvar MIN_PORT_SPACING2 = 8;\nvar PORT_SHIFT2 = MIN_PORT_SPACING2 / 2;\nvar LABEL_CLEARANCE_BUFFER = 3;\nfunction pairKey(a, b) {\n  return a < b ? `${a}::${b}` : `${b}::${a}`;\n}\nchunk_Y2CYZVJY_name(pairKey, \"pairKey\");\nfunction straightenCollinearSiblingDetours(edges, nodes) {\n  const { nodeInfoById, realNodeRects } = collectRealNodeBounds(nodes);\n  const labelDimById = /* @__PURE__ */ new Map();\n  for (const n of nodes) {\n    const id = n.id;\n    if (n.isGroup) {\n      continue;\n    }\n    if (n.isEdgeLabel) {\n      labelDimById.set(id, {\n        w: n.width ?? 0,\n        h: n.height ?? 0\n      });\n      continue;\n    }\n  }\n  const labelClearanceFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((thisEdge, thisSrcId, thisDstId, axis) => {\n    const targetPair = pairKey(thisSrcId, thisDstId);\n    let maxHalf = 0;\n    const consider = /* @__PURE__ */ chunk_Y2CYZVJY_name((labelId) => {\n      if (!labelId) {\n        return;\n      }\n      const dim = labelDimById.get(labelId);\n      if (!dim) {\n        return;\n      }\n      const half = axis === \"x\" ? dim.w / 2 : dim.h / 2;\n      if (half > maxHalf) {\n        maxHalf = half;\n      }\n    }, \"consider\");\n    consider(thisEdge.labelNodeId);\n    for (const other of edges) {\n      if (other === thisEdge) {\n        continue;\n      }\n      if (other.isLayoutOnly) {\n        continue;\n      }\n      const oSrc = other.start;\n      const oDst = other.end;\n      if (!oSrc || !oDst) {\n        continue;\n      }\n      if (pairKey(oSrc, oDst) !== targetPair) {\n        continue;\n      }\n      consider(other.labelNodeId);\n    }\n    return maxHalf > 0 ? maxHalf + LABEL_CLEARANCE_BUFFER : 0;\n  }, \"labelClearanceFor\");\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const pts = edge.points;\n    if (!classifyThreeSegmentRoute(pts, EPS6)) {\n      continue;\n    }\n    const nodePair = getNodePairGeometry(edge, nodeInfoById, EPS6);\n    if (!nodePair) {\n      continue;\n    }\n    const { srcId, dstId, srcInfo, dstInfo, collinearX, collinearY } = nodePair;\n    if (collinearX === collinearY) {\n      continue;\n    }\n    let targetSrc;\n    let targetDst;\n    if (collinearX) {\n      const dstBelow = dstInfo.cy > srcInfo.cy;\n      targetSrc = { x: srcInfo.cx, y: dstBelow ? srcInfo.rect.bottom : srcInfo.rect.top };\n      targetDst = { x: dstInfo.cx, y: dstBelow ? dstInfo.rect.top : dstInfo.rect.bottom };\n    } else {\n      const dstEast = dstInfo.cx > srcInfo.cx;\n      targetSrc = { x: dstEast ? srcInfo.rect.right : srcInfo.rect.left, y: srcInfo.cy };\n      targetDst = { x: dstEast ? dstInfo.rect.left : dstInfo.rect.right, y: dstInfo.cy };\n    }\n    if (segmentHitsAnyRect(targetSrc, targetDst, realNodeRects, [srcId, dstId], 1)) {\n      continue;\n    }\n    const shiftAxis = collinearX ? \"x\" : \"y\";\n    const labelShift = labelClearanceFor(edge, srcId, dstId, shiftAxis);\n    const effectiveShift = labelShift > PORT_SHIFT2 ? labelShift : PORT_SHIFT2;\n    const deltas = [0, effectiveShift, -effectiveShift];\n    for (const delta of deltas) {\n      const shiftedSrc = { ...targetSrc };\n      const shiftedDst = { ...targetDst };\n      if (collinearX) {\n        shiftedSrc.x += delta;\n        shiftedDst.x += delta;\n        if (shiftedSrc.x <= srcInfo.rect.left || shiftedSrc.x >= srcInfo.rect.right) {\n          continue;\n        }\n        if (shiftedDst.x <= dstInfo.rect.left || shiftedDst.x >= dstInfo.rect.right) {\n          continue;\n        }\n      } else {\n        shiftedSrc.y += delta;\n        shiftedDst.y += delta;\n        if (shiftedSrc.y <= srcInfo.rect.top || shiftedSrc.y >= srcInfo.rect.bottom) {\n          continue;\n        }\n        if (shiftedDst.y <= dstInfo.rect.top || shiftedDst.y >= dstInfo.rect.bottom) {\n          continue;\n        }\n      }\n      if (segmentHitsAnyRect(shiftedSrc, shiftedDst, realNodeRects, [srcId, dstId], 1)) {\n        continue;\n      }\n      if (segmentConflictsWithAnyEdge(shiftedSrc, shiftedDst, edges, edge, { epsilon: EPS6 })) {\n        continue;\n      }\n      edge.points = [shiftedSrc, shiftedDst];\n      break;\n    }\n  }\n}\nchunk_Y2CYZVJY_name(straightenCollinearSiblingDetours, \"straightenCollinearSiblingDetours\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/sharedTrackNudging.ts\nfunction nudgeSharedInteriorSubpaths(edges, nodeByIdMap) {\n  const EPS_LOCAL2 = 1e-3;\n  const MIN_SHARED3 = 8;\n  const TRACK_SHIFT = 7;\n  const MIN_TRACK_GAP = TRACK_SHIFT;\n  const SOURCE_DETOUR_STUB = 20;\n  const BUFFER = 2;\n  const MAX_ITERATIONS = 12;\n  const { realNodeRects, labelNodeRects: labelRects } = collectNodeRectEntries(\n    nodeByIdMap.values()\n  );\n  const segmentsFor2 = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, points) => {\n    return orthogonalSegmentsForPoints(points, EPS_LOCAL2).map((segment) => ({\n      ...segment,\n      edge,\n      interior: segment.index >= 1 && segment.index <= points.length - 3\n    }));\n  }, \"segmentsFor\");\n  const allSegments = /* @__PURE__ */ chunk_Y2CYZVJY_name(() => {\n    const result = [];\n    for (const edge of edges) {\n      if (edge.isLayoutOnly) {\n        continue;\n      }\n      const points = edge.points;\n      if (!points || points.length < 2) {\n        continue;\n      }\n      result.push(...segmentsFor2(edge, dedupeConsecutivePoints(points)));\n    }\n    return result;\n  }, \"allSegments\");\n  const hasCrowdedParallelTrack = /* @__PURE__ */ chunk_Y2CYZVJY_name((a, b) => {\n    if (a.horizontal && b.horizontal) {\n      return overlapLength(a.a.x, a.b.x, b.a.x, b.b.x) >= MIN_SHARED3 && Math.abs(a.a.y - b.a.y) < MIN_TRACK_GAP;\n    }\n    if (a.vertical && b.vertical) {\n      return overlapLength(a.a.y, a.b.y, b.a.y, b.b.y) >= MIN_SHARED3 && Math.abs(a.a.x - b.a.x) < MIN_TRACK_GAP;\n    }\n    return false;\n  }, \"hasCrowdedParallelTrack\");\n  const candidateIsSafe = /* @__PURE__ */ chunk_Y2CYZVJY_name((edge, candidate) => {\n    const sourceId = edge.start;\n    const targetId = edge.end;\n    const candidateSegments = segmentsFor2(edge, candidate);\n    if (candidateSegments.length !== candidate.length - 1) {\n      return false;\n    }\n    const endpointIds = [sourceId, targetId].filter((id) => Boolean(id));\n    const ownLabelIds = edge.labelNodeId ? [edge.labelNodeId] : [];\n    for (const segment of candidateSegments) {\n      if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -BUFFER)) {\n        return false;\n      }\n      if (segmentHitsAnyRect(segment.a, segment.b, labelRects, ownLabelIds, -BUFFER)) {\n        return false;\n      }\n    }\n    for (const other of edges) {\n      if (other === edge || other.isLayoutOnly) {\n        continue;\n      }\n      const otherPoints = other.points;\n      if (!otherPoints || otherPoints.length < 2) {\n        continue;\n      }\n      for (const candidateSegment of candidateSegments) {\n        for (const otherSegment of segmentsFor2(other, dedupeConsecutivePoints(otherPoints))) {\n          if (hasCrowdedParallelTrack(candidateSegment, otherSegment)) {\n            return false;\n          }\n          if (orthogonalSegmentsStrictlyCross(\n            candidateSegment.a,\n            candidateSegment.b,\n            otherSegment.a,\n            otherSegment.b,\n            EPS_LOCAL2\n          )) {\n            return false;\n          }\n        }\n      }\n    }\n    return true;\n  }, \"candidateIsSafe\");\n  const shiftedCandidate = /* @__PURE__ */ chunk_Y2CYZVJY_name((segment, shift) => {\n    const points = dedupeConsecutivePoints(segment.edge.points ?? []);\n    if (points.length < 4 || segment.index >= points.length - 1) {\n      return void 0;\n    }\n    const candidate = points.map((p) => ({ ...p }));\n    if (segment.horizontal) {\n      candidate[segment.index].y += shift;\n      candidate[segment.index + 1].y += shift;\n    } else if (segment.vertical) {\n      candidate[segment.index].x += shift;\n      candidate[segment.index + 1].x += shift;\n    } else {\n      return void 0;\n    }\n    return segmentsFor2(segment.edge, candidate).length === candidate.length - 1 ? candidate : void 0;\n  }, \"shiftedCandidate\");\n  const nodeCenter = /* @__PURE__ */ chunk_Y2CYZVJY_name((node, rect) => ({\n    x: node.x ?? (rect.left + rect.right) / 2,\n    y: node.y ?? (rect.top + rect.bottom) / 2\n  }), \"nodeCenter\");\n  const sourceDetourContextFor = /* @__PURE__ */ chunk_Y2CYZVJY_name((segment) => {\n    const edge = segment.edge;\n    const points = dedupeConsecutivePoints(edge.points ?? []);\n    if (points.length !== 4 || segment.index !== 1) {\n      return void 0;\n    }\n    const sourceNode = edge.start ? nodeByIdMap.get(edge.start) : void 0;\n    const targetNode = edge.end ? nodeByIdMap.get(edge.end) : void 0;\n    const sourceRect = sourceNode ? rectOfNodeBounds(sourceNode) : void 0;\n    const targetRect = targetNode ? rectOfNodeBounds(targetNode) : void 0;\n    const tail = points.slice(segment.index + 2);\n    if (!sourceNode || !targetNode || !sourceRect || !targetRect || tail.length === 0) {\n      return void 0;\n    }\n    return {\n      sourceCenter: nodeCenter(sourceNode, sourceRect),\n      targetCenter: nodeCenter(targetNode, targetRect),\n      sourceRect,\n      tail\n    };\n  }, \"sourceDetourContextFor\");\n  const verticalSourceDetour = /* @__PURE__ */ chunk_Y2CYZVJY_name((segment, shift, sourceCenter, targetCenter, sourceRect, tail) => {\n    const targetBelow = targetCenter.y >= sourceCenter.y;\n    const sourcePortY = targetBelow ? sourceRect.bottom : sourceRect.top;\n    const stubY = sourcePortY + (targetBelow ? SOURCE_DETOUR_STUB : -SOURCE_DETOUR_STUB);\n    if (targetBelow && segment.b.y <= stubY + EPS_LOCAL2 || !targetBelow && segment.b.y >= stubY - EPS_LOCAL2) {\n      return void 0;\n    }\n    const railX = segment.a.x + shift;\n    return dedupeConsecutivePoints(\n      [\n        { x: sourceCenter.x, y: sourcePortY },\n        { x: sourceCenter.x, y: stubY },\n        { x: railX, y: stubY },\n        { x: railX, y: segment.b.y },\n        ...tail\n      ],\n      EPS_LOCAL2\n    );\n  }, \"verticalSourceDetour\");\n  const horizontalSourceDetour = /* @__PURE__ */ chunk_Y2CYZVJY_name((segment, shift, sourceCenter, targetCenter, sourceRect, tail) => {\n    const targetRight = targetCenter.x >= sourceCenter.x;\n    const sourcePortX = targetRight ? sourceRect.right : sourceRect.left;\n    const stubX = sourcePortX + (targetRight ? SOURCE_DETOUR_STUB : -SOURCE_DETOUR_STUB);\n    if (targetRight && segment.b.x <= stubX + EPS_LOCAL2 || !targetRight && segment.b.x >= stubX - EPS_LOCAL2) {\n      return void 0;\n    }\n    const railY = segment.a.y + shift;\n    return dedupeConsecutivePoints(\n      [\n        { x: sourcePortX, y: sourceCenter.y },\n        { x: stubX, y: sourceCenter.y },\n        { x: stubX, y: railY },\n        { x: segment.b.x, y: railY },\n        ...tail\n      ],\n      EPS_LOCAL2\n    );\n  }, \"horizontalSourceDetour\");\n  const sourceDetourCandidate = /* @__PURE__ */ chunk_Y2CYZVJY_name((segment, shift) => {\n    const context = sourceDetourContextFor(segment);\n    if (!context) {\n      return void 0;\n    }\n    if (segment.vertical) {\n      return verticalSourceDetour(\n        segment,\n        shift,\n        context.sourceCenter,\n        context.targetCenter,\n        context.sourceRect,\n        context.tail\n      );\n    }\n    if (segment.horizontal) {\n      return horizontalSourceDetour(\n        segment,\n        shift,\n        context.sourceCenter,\n        context.targetCenter,\n        context.sourceRect,\n        context.tail\n      );\n    }\n    return void 0;\n  }, \"sourceDetourCandidate\");\n  const shifts = [\n    -TRACK_SHIFT,\n    TRACK_SHIFT,\n    -2 * TRACK_SHIFT,\n    2 * TRACK_SHIFT,\n    -3 * TRACK_SHIFT,\n    3 * TRACK_SHIFT\n  ];\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    const segments = allSegments();\n    let fixed = false;\n    for (let i = 0; i < segments.length && !fixed; i++) {\n      for (let j = i + 1; j < segments.length && !fixed; j++) {\n        const first = segments[i];\n        const second = segments[j];\n        if (first.edge === second.edge || !hasCrowdedParallelTrack(first, second)) {\n          continue;\n        }\n        const candidates = [first, second].filter((segment) => segment.interior);\n        for (const segment of candidates) {\n          for (const shift of shifts) {\n            const direct = shiftedCandidate(segment, shift);\n            if (direct && candidateIsSafe(segment.edge, direct)) {\n              segment.edge.points = direct;\n              fixed = true;\n              break;\n            }\n            const detoured = sourceDetourCandidate(segment, shift);\n            if (detoured && candidateIsSafe(segment.edge, detoured)) {\n              segment.edge.points = detoured;\n              fixed = true;\n              break;\n            }\n          }\n          if (fixed) {\n            break;\n          }\n        }\n      }\n    }\n    if (!fixed) {\n      return;\n    }\n  }\n}\nchunk_Y2CYZVJY_name(nudgeSharedInteriorSubpaths, \"nudgeSharedInteriorSubpaths\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/validation.ts\nfunction segmentsIntersect(p1, p2, p3, p4) {\n  const d1x = p2.x - p1.x;\n  const d1y = p2.y - p1.y;\n  const d2x = p4.x - p3.x;\n  const d2y = p4.y - p3.y;\n  const cross = d1x * d2y - d1y * d2x;\n  if (Math.abs(cross) < 1e-10) {\n    return false;\n  }\n  const dx = p3.x - p1.x;\n  const dy = p3.y - p1.y;\n  const t = (dx * d2y - dy * d2x) / cross;\n  const u = (dx * d1y - dy * d1x) / cross;\n  const eps = 0.01;\n  return t > eps && t < 1 - eps && u > eps && u < 1 - eps;\n}\nchunk_Y2CYZVJY_name(segmentsIntersect, \"segmentsIntersect\");\nfunction validateSwimlanesLayout(layout) {\n  const nodes = layout.nodes ?? [];\n  const edges = layout.edges ?? [];\n  const issues = [];\n  if (!edges.length || !nodes.length) {\n    return issues;\n  }\n  const nodeRects = collectLayoutNodeRects(nodes);\n  const epsilon = 1;\n  const edgeSegments = [];\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const points = edge.points;\n    if (!points || points.length < 2) {\n      continue;\n    }\n    const edgeStart = edge.start;\n    const edgeEnd = edge.end;\n    const ownLabelId = edge.labelNodeId;\n    const edgeId = edge.id ?? `${edgeStart}->${edgeEnd}`;\n    for (const rect of nodeRects) {\n      if (rect.nodeId === edgeStart || rect.nodeId === edgeEnd) {\n        continue;\n      }\n      if (ownLabelId && rect.nodeId === ownLabelId) {\n        continue;\n      }\n      for (let i = 0; i < points.length - 1; i++) {\n        if (segmentBoundsOverlapRect(points[i], points[i + 1], rect, -epsilon)) {\n          issues.push({\n            type: \"edge-node-overlap\",\n            edgeId,\n            targetId: rect.nodeId,\n            detail: `segment ${i} passes through node \"${rect.nodeId}\"`\n          });\n          break;\n        }\n      }\n    }\n    for (let i = 0; i < points.length - 1; i++) {\n      edgeSegments.push({\n        edgeId,\n        start: edgeStart,\n        end: edgeEnd,\n        p1: points[i],\n        p2: points[i + 1]\n      });\n    }\n  }\n  const crossingPairs = /* @__PURE__ */ new Set();\n  for (let i = 0; i < edgeSegments.length; i++) {\n    for (let j = i + 1; j < edgeSegments.length; j++) {\n      const a = edgeSegments[i];\n      const b = edgeSegments[j];\n      if (a.edgeId === b.edgeId) {\n        continue;\n      }\n      if (a.start === b.start || a.start === b.end || a.end === b.start || a.end === b.end) {\n        continue;\n      }\n      if (segmentsIntersect(a.p1, a.p2, b.p1, b.p2)) {\n        const pairKey2 = a.edgeId < b.edgeId ? `${a.edgeId}|${b.edgeId}` : `${b.edgeId}|${a.edgeId}`;\n        if (!crossingPairs.has(pairKey2)) {\n          crossingPairs.add(pairKey2);\n          issues.push({\n            type: \"edge-edge-crossing\",\n            edgeId: a.edgeId,\n            targetId: b.edgeId,\n            detail: `edges \"${a.edgeId}\" and \"${b.edgeId}\" cross`\n          });\n        }\n      }\n    }\n  }\n  if (issues.length > 0) {\n    const overlaps = issues.filter((i) => i.type === \"edge-node-overlap\").length;\n    const crossings = issues.filter((i) => i.type === \"edge-edge-crossing\").length;\n    log.warn(\n      `[SWIMLANE_VALIDATE] ${issues.length} issue(s) detected: ${overlaps} edge-node overlap(s), ${crossings} edge crossing(s)`\n    );\n    for (const issue of issues) {\n      log.warn(`[SWIMLANE_VALIDATE]   ${issue.type}: ${issue.detail}`);\n    }\n  }\n  return issues;\n}\nchunk_Y2CYZVJY_name(validateSwimlanesLayout, \"validateSwimlanesLayout\");\n\n// src/rendering-util/layout-algorithms/swimlanes/postProcessing.ts\nfunction postProcessSwimlaneLayout(layout, direction) {\n  const nodes = layout.nodes ?? [];\n  const edges = layout.edges ?? [];\n  const contentNodes = nodes.filter((n) => !n.isGroup);\n  if ((direction === \"LR\" || direction === \"RL\") && contentNodes.length > 0 && !applyLrDirectionTransform(layout, direction)) {\n    return;\n  }\n  if (direction === \"BT\" && contentNodes.length > 0 && !applyBtDirectionTransform(layout)) {\n    return;\n  }\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const pts = edge.points;\n    if (!pts || pts.length < 2) {\n      continue;\n    }\n    edge.points = simplifyPolyline(\n      orthogonalizePolyline(pts)\n    );\n  }\n  simplifyDetouredEdges(edges, nodes);\n  straightenCollinearSiblingDetours(edges, nodes);\n  portSwapToLShape(edges, nodes);\n  const nodeByIdMap = /* @__PURE__ */ new Map();\n  for (const n of nodes) {\n    nodeByIdMap.set(String(n.id), n);\n  }\n  anchorLabelsToPolyline(edges, nodeByIdMap);\n  clipEdgeEndpointsToNodeBoundaries(edges, nodeByIdMap);\n  collapseShortTerminalStub(edges, nodeByIdMap);\n  nudgeSharedInteriorSubpaths(edges, nodeByIdMap);\n  separateSharedRenderedTerminalLanes(edges, nodeByIdMap);\n  collapseRedundantRectangularDoglegs(edges, nodeByIdMap);\n  liftObstacleHuggingSameSideRails(edges, nodeByIdMap);\n  swapDestinationTerminalTailsToReduceCrossings(edges, nodeByIdMap);\n  const finalizeRenderedEdges = /* @__PURE__ */ chunk_Y2CYZVJY_name(() => {\n    resolveRenderedOrthogonalCrossings(edges, nodeByIdMap);\n    reassignCrossingExternalRailChannels(edges, nodeByIdMap);\n    shortcutRedundantOrthogonalJogs(edges, nodeByIdMap);\n    anchorLabelsToPolyline(edges, nodeByIdMap);\n    prepareEdgeEndpointsForRenderer(edges, nodeByIdMap);\n    liftObstacleHuggingSameSideRails(edges, nodeByIdMap);\n    anchorLabelsToPolyline(edges, nodeByIdMap);\n    prepareEdgeEndpointsForRenderer(edges, nodeByIdMap);\n  }, \"finalizeRenderedEdges\");\n  finalizeRenderedEdges();\n  nudgeSharedInteriorSubpaths(edges, nodeByIdMap);\n  finalizeRenderedEdges();\n  liftTopLaneTitleBandsAboveRails(edges, nodeByIdMap);\n  shiftLeftLaneTitleBandsLeftOfRails(edges, nodeByIdMap);\n  liftTopLaneTitleBandsAboveRails(edges, nodeByIdMap);\n  shiftLeftLaneTitleBandsLeftOfRails(edges, nodeByIdMap);\n}\nchunk_Y2CYZVJY_name(postProcessSwimlaneLayout, \"postProcessSwimlaneLayout\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase0.helpers.ts\nfunction normalizeGraph(g) {\n  const nodeById = new Map(g.nodeById);\n  const seen = /* @__PURE__ */ new Set();\n  const edges = [];\n  for (const e of g.edges) {\n    if (!nodeById.has(e.src) || !nodeById.has(e.dst)) {\n      continue;\n    }\n    const key = `${e.id}:${e.src}->${e.dst}`;\n    if (seen.has(key)) {\n      continue;\n    }\n    seen.add(key);\n    edges.push(e);\n  }\n  const nodes = [...nodeById.keys()];\n  return { nodes, edges, layout: g.layout, nodeById };\n}\nchunk_Y2CYZVJY_name(normalizeGraph, \"normalizeGraph\");\nfunction incoming(g, v) {\n  return g.edges.filter((e) => e.dst === v);\n}\nchunk_Y2CYZVJY_name(incoming, \"incoming\");\nfunction buildSuccessorMap(g) {\n  const succs = /* @__PURE__ */ new Map();\n  for (const v of g.nodes) {\n    succs.set(v, []);\n  }\n  for (const e of g.edges) {\n    succs.get(e.src).push(e.dst);\n  }\n  return succs;\n}\nchunk_Y2CYZVJY_name(buildSuccessorMap, \"buildSuccessorMap\");\nfunction buildSortedSuccessorMap(g) {\n  const succs = buildSuccessorMap(g);\n  for (const successors of succs.values()) {\n    successors.sort((a, b) => a.localeCompare(b));\n  }\n  return succs;\n}\nchunk_Y2CYZVJY_name(buildSortedSuccessorMap, \"buildSortedSuccessorMap\");\nfunction buildInDegreeMap(g) {\n  const indeg = /* @__PURE__ */ new Map();\n  for (const v of g.nodes) {\n    indeg.set(v, 0);\n  }\n  for (const e of g.edges) {\n    indeg.set(e.dst, (indeg.get(e.dst) ?? 0) + 1);\n  }\n  return indeg;\n}\nchunk_Y2CYZVJY_name(buildInDegreeMap, \"buildInDegreeMap\");\nfunction sortedZeroInDegreeNodes(indeg) {\n  return [...indeg.entries()].filter(([, degree]) => degree === 0).map(([id]) => id).sort((a, b) => a.localeCompare(b));\n}\nchunk_Y2CYZVJY_name(sortedZeroInDegreeNodes, \"sortedZeroInDegreeNodes\");\nfunction buildPredecessorSuccessorMaps(g, includeEdge = () => true) {\n  const preds = /* @__PURE__ */ new Map();\n  const succs = /* @__PURE__ */ new Map();\n  for (const v of g.nodes) {\n    preds.set(v, []);\n    succs.set(v, []);\n  }\n  for (const e of g.edges) {\n    if (!includeEdge(e)) {\n      continue;\n    }\n    succs.get(e.src).push(e.dst);\n    preds.get(e.dst).push(e.src);\n  }\n  return { preds, succs };\n}\nchunk_Y2CYZVJY_name(buildPredecessorSuccessorMaps, \"buildPredecessorSuccessorMaps\");\nfunction buildLayersFromRanks(g, order, rankOf, opts) {\n  let maxRank = 0;\n  for (const v of g.nodes) {\n    if (opts?.skipGroups && g.nodeById.get(v)?.isGroup) {\n      continue;\n    }\n    maxRank = Math.max(maxRank, rankOf[v] ?? 0);\n  }\n  const layers = Array.from({ length: maxRank + 1 }, () => []);\n  for (const v of order) {\n    if (opts?.skipGroups && g.nodeById.get(v)?.isGroup) {\n      continue;\n    }\n    layers[Math.max(0, rankOf[v] ?? 0)].push(v);\n  }\n  return layers;\n}\nchunk_Y2CYZVJY_name(buildLayersFromRanks, \"buildLayersFromRanks\");\nfunction topoSortIfAcyclic(g) {\n  const indeg = buildInDegreeMap(g);\n  const queue = sortedZeroInDegreeNodes(indeg);\n  const order = [];\n  const adj = buildSortedSuccessorMap(g);\n  while (queue.length) {\n    const u = queue.shift();\n    order.push(u);\n    for (const v of adj.get(u) ?? []) {\n      indeg.set(v, (indeg.get(v) ?? 0) - 1);\n      if ((indeg.get(v) ?? 0) === 0) {\n        let i = 0;\n        while (i < queue.length && queue[i] < v) {\n          i++;\n        }\n        queue.splice(i, 0, v);\n      }\n    }\n  }\n  return order.length === g.nodes.length ? order : null;\n}\nchunk_Y2CYZVJY_name(topoSortIfAcyclic, \"topoSortIfAcyclic\");\nfunction buildLayerIndex(layer) {\n  const m = /* @__PURE__ */ new Map();\n  let index = 0;\n  for (const id of layer) {\n    m.set(id, index);\n    index++;\n  }\n  return m;\n}\nchunk_Y2CYZVJY_name(buildLayerIndex, \"buildLayerIndex\");\nfunction countInversions(values) {\n  const tmp = new Array(values.length);\n  const count = /* @__PURE__ */ chunk_Y2CYZVJY_name((left, right) => {\n    if (right - left <= 1) {\n      return 0;\n    }\n    const mid = left + right >> 1;\n    let inversions = count(left, mid) + count(mid, right);\n    let i = left;\n    let j = mid;\n    let k = left;\n    while (i < mid || j < right) {\n      if (j >= right || i < mid && values[i] <= values[j]) {\n        tmp[k++] = values[i++];\n      } else {\n        tmp[k++] = values[j++];\n        inversions += mid - i;\n      }\n    }\n    for (let t = left; t < right; t++) {\n      values[t] = tmp[t];\n    }\n    return inversions;\n  }, \"count\");\n  return count(0, values.length);\n}\nchunk_Y2CYZVJY_name(countInversions, \"countInversions\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase1.cycles.ts\nfunction removeCycles_DFS(g) {\n  const gn = normalizeGraph(g);\n  const adj = /* @__PURE__ */ new Map();\n  for (const v of gn.nodes) {\n    adj.set(v, []);\n  }\n  for (const e of gn.edges) {\n    adj.get(e.src).push(e);\n  }\n  for (const arr of adj.values()) {\n    arr.sort((a, b) => a.dst === b.dst ? a.id.localeCompare(b.id) : a.dst.localeCompare(b.dst));\n  }\n  const color = /* @__PURE__ */ Object.create(null);\n  for (const v of gn.nodes) {\n    color[v] = 0;\n  }\n  const reversed = [];\n  const dfs = /* @__PURE__ */ chunk_Y2CYZVJY_name((u) => {\n    color[u] = 1;\n    for (const e of adj.get(u) ?? []) {\n      const v = e.dst;\n      if (color[v] === 0) {\n        dfs(v);\n      } else if (color[v] === 1) {\n        reversed.push(e);\n      }\n    }\n    color[u] = 2;\n  }, \"dfs\");\n  const nodesSorted = [...gn.nodes].sort((a, b) => a.localeCompare(b));\n  for (const v of nodesSorted) {\n    if (color[v] === 0) {\n      dfs(v);\n    }\n  }\n  const toReverse = new Set(reversed.map((e) => `${e.id}:${e.src}->${e.dst}`));\n  const acycEdges = gn.edges.map(\n    (e) => toReverse.has(`${e.id}:${e.src}->${e.dst}`) ? { id: e.id, src: e.dst, dst: e.src, weight: e.weight, ref: e.ref } : e\n  );\n  const acyclic = {\n    nodes: [...gn.nodes],\n    edges: acycEdges,\n    layout: gn.layout,\n    nodeById: new Map(gn.nodeById)\n  };\n  return { acyclic, reversed };\n}\nchunk_Y2CYZVJY_name(removeCycles_DFS, \"removeCycles_DFS\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.options.ts\nfunction buildTopLaneMap(g) {\n  const cache = /* @__PURE__ */ new Map();\n  const resolve = /* @__PURE__ */ chunk_Y2CYZVJY_name((id) => {\n    if (cache.has(id)) {\n      return cache.get(id);\n    }\n    const node = g.nodeById.get(id);\n    if (!node) {\n      cache.set(id, null);\n      return null;\n    }\n    const parentId = node.parentId;\n    if (!parentId) {\n      cache.set(id, null);\n      return null;\n    }\n    const parentLane = resolve(parentId);\n    const lane = parentLane ?? parentId;\n    cache.set(id, lane);\n    return lane;\n  }, \"resolve\");\n  for (const id of g.nodes) {\n    resolve(id);\n  }\n  return cache;\n}\nchunk_Y2CYZVJY_name(buildTopLaneMap, \"buildTopLaneMap\");\nfunction createTopLaneResolver(g) {\n  const topLaneMap = buildTopLaneMap(g);\n  return (id) => topLaneMap.get(id) ?? null;\n}\nchunk_Y2CYZVJY_name(createTopLaneResolver, \"createTopLaneResolver\");\nfunction buildTopLaneOrder(g) {\n  const lanes = [];\n  for (const node of g.layout.nodes ?? []) {\n    if (node.isGroup && !node.parentId) {\n      lanes.push(node.id);\n    }\n  }\n  return [...new Set(lanes)].reverse();\n}\nchunk_Y2CYZVJY_name(buildTopLaneOrder, \"buildTopLaneOrder\");\nfunction resolveTopLaneOrder(g, preferredOrder) {\n  const sourceOrder = buildTopLaneOrder(g);\n  if (!preferredOrder || preferredOrder.length === 0) {\n    return sourceOrder;\n  }\n  const sourceLaneIds = new Set(sourceOrder);\n  const seen = /* @__PURE__ */ new Set();\n  const resolved = [];\n  for (const laneId of preferredOrder) {\n    if (!sourceLaneIds.has(laneId) || seen.has(laneId)) {\n      continue;\n    }\n    seen.add(laneId);\n    resolved.push(laneId);\n  }\n  for (const laneId of sourceOrder) {\n    if (seen.has(laneId)) {\n      continue;\n    }\n    resolved.push(laneId);\n  }\n  return resolved;\n}\nchunk_Y2CYZVJY_name(resolveTopLaneOrder, \"resolveTopLaneOrder\");\n\n// src/rendering-util/layout-algorithms/swimlanes/config.ts\nvar PRECISION = {\n  /** Epsilon for floating-point comparisons */\n  EPSILON: 1e-6\n};\nvar LAYERING = {\n  /** Default number of iterations for gravity-based layering */\n  GRAVITY_ITERATIONS: 8,\n  /** Maximum number of passes for crossing-based rank optimization */\n  MAX_CROSSING_OPTIMIZATION_PASSES: 4,\n  /** Whether to compact single-input nodes by default */\n  DEFAULT_COMPACT_SINGLE_INPUT: true\n};\nvar COORDINATES = {\n  /** Default vertical gap between layers (px) */\n  DEFAULT_LAYER_GAP: 100,\n  /** Default horizontal gap between nodes (px) */\n  DEFAULT_NODE_GAP: 40\n};\n\n// src/rendering-util/layout-algorithms/swimlanes/driving-tree.ts\nfunction buildDrivingTree(graph, opts) {\n  const g = normalizeGraph(graph);\n  const laneOf = opts?.laneOf ?? (() => null);\n  const rankHint = opts?.rankHint;\n  const { preds } = buildPredecessorSuccessorMaps(g);\n  for (const arr of preds.values()) {\n    arr.sort((a, b) => a.localeCompare(b));\n  }\n  const topoOrder = topoSortIfAcyclic(g) ?? [...g.nodes].sort((a, b) => a.localeCompare(b));\n  const topoIndex = /* @__PURE__ */ new Map();\n  for (const [idx, id] of topoOrder.entries()) {\n    topoIndex.set(id, idx);\n  }\n  const parent = /* @__PURE__ */ new Map();\n  const children = /* @__PURE__ */ new Map();\n  for (const node of g.nodes) {\n    children.set(node, []);\n  }\n  for (const node of topoOrder) {\n    const candidates = (preds.get(node) ?? []).filter((p) => parent.has(p));\n    if (candidates.length > 0) {\n      const chosen = chooseParent(node, candidates, {\n        laneOf,\n        rankHint,\n        topoIndex\n      });\n      parent.set(node, chosen);\n      children.get(chosen).push(node);\n    } else if (!parent.has(node)) {\n      parent.set(node, null);\n    }\n  }\n  for (const node of g.nodes) {\n    if (!parent.has(node)) {\n      parent.set(node, null);\n    }\n  }\n  const rootSet = /* @__PURE__ */ new Set();\n  for (const node of g.nodes) {\n    if ((parent.get(node) ?? null) === null) {\n      rootSet.add(node);\n    }\n  }\n  const roots = [...rootSet].sort((a, b) => {\n    const ta = topoIndex.get(a) ?? 0;\n    const tb = topoIndex.get(b) ?? 0;\n    if (ta === tb) {\n      return a.localeCompare(b);\n    }\n    return ta - tb;\n  });\n  const adjacency = buildAdjacency(g);\n  const adjacencyList = /* @__PURE__ */ new Map();\n  for (const [node, set] of adjacency.entries()) {\n    adjacencyList.set(\n      node,\n      [...set].sort((a, b) => a.localeCompare(b))\n    );\n  }\n  const componentOf = assignComponents(adjacencyList);\n  const blocks = computeBlocks(adjacencyList);\n  const nodeBlocks = /* @__PURE__ */ new Map();\n  for (const node of g.nodes) {\n    nodeBlocks.set(node, []);\n  }\n  for (const block of blocks) {\n    for (const node of block.nodes) {\n      const list = nodeBlocks.get(node);\n      if (list) {\n        list.push(block.id);\n      } else {\n        nodeBlocks.set(node, [block.id]);\n      }\n    }\n  }\n  const preorder = [];\n  const postorder = [];\n  const seen = /* @__PURE__ */ new Set();\n  const walk = /* @__PURE__ */ chunk_Y2CYZVJY_name((node) => {\n    if (seen.has(node)) {\n      return;\n    }\n    seen.add(node);\n    preorder.push(node);\n    for (const child of children.get(node) ?? []) {\n      walk(child);\n    }\n    postorder.push(node);\n  }, \"walk\");\n  for (const root of roots) {\n    walk(root);\n  }\n  for (const node of topoOrder) {\n    walk(node);\n  }\n  return {\n    parent,\n    children,\n    roots,\n    componentOf,\n    blocks,\n    nodeBlocks,\n    adjacency: adjacencyList,\n    preorder,\n    postorder,\n    topologicalOrder: topoOrder\n  };\n}\nchunk_Y2CYZVJY_name(buildDrivingTree, \"buildDrivingTree\");\nfunction chooseParent(node, candidates, ctx) {\n  const laneNode = ctx.laneOf(node);\n  const sorted = [...candidates].sort((a, b) => {\n    const laneA = ctx.laneOf(a);\n    const laneB = ctx.laneOf(b);\n    const sameLaneA = laneA != null && laneA === laneNode;\n    const sameLaneB = laneB != null && laneB === laneNode;\n    if (sameLaneA !== sameLaneB) {\n      return sameLaneA ? -1 : 1;\n    }\n    const rankA = ctx.rankHint?.[a];\n    const rankB = ctx.rankHint?.[b];\n    if (rankA != null && rankB != null && rankA !== rankB) {\n      return rankB - rankA;\n    }\n    const idxA = ctx.topoIndex.get(a) ?? 0;\n    const idxB = ctx.topoIndex.get(b) ?? 0;\n    if (idxA !== idxB) {\n      return idxA - idxB;\n    }\n    return a.localeCompare(b);\n  });\n  return sorted[0];\n}\nchunk_Y2CYZVJY_name(chooseParent, \"chooseParent\");\nfunction buildAdjacency(g) {\n  const adjacency = /* @__PURE__ */ new Map();\n  for (const node of g.nodes) {\n    adjacency.set(node, /* @__PURE__ */ new Set());\n  }\n  for (const e of g.edges) {\n    adjacency.get(e.src).add(e.dst);\n    adjacency.get(e.dst).add(e.src);\n  }\n  return adjacency;\n}\nchunk_Y2CYZVJY_name(buildAdjacency, \"buildAdjacency\");\nfunction assignComponents(adjacency) {\n  const componentOf = /* @__PURE__ */ new Map();\n  let componentId = 0;\n  for (const node of adjacency.keys()) {\n    if (componentOf.has(node)) {\n      continue;\n    }\n    const stack = [node];\n    while (stack.length > 0) {\n      const cur = stack.pop();\n      if (componentOf.has(cur)) {\n        continue;\n      }\n      componentOf.set(cur, componentId);\n      for (const next of adjacency.get(cur) ?? []) {\n        if (!componentOf.has(next)) {\n          stack.push(next);\n        }\n      }\n    }\n    componentId++;\n  }\n  return componentOf;\n}\nchunk_Y2CYZVJY_name(assignComponents, \"assignComponents\");\nfunction computeBlocks(adjacency) {\n  const discovery = /* @__PURE__ */ new Map();\n  const low = /* @__PURE__ */ new Map();\n  const edgeStack = [];\n  const blocks = [];\n  let time = 0;\n  const visit = /* @__PURE__ */ chunk_Y2CYZVJY_name((node, parent) => {\n    discovery.set(node, ++time);\n    low.set(node, time);\n    for (const next of adjacency.get(node) ?? []) {\n      if (next === parent) {\n        continue;\n      }\n      if (!discovery.has(next)) {\n        edgeStack.push([node, next]);\n        visit(next, node);\n        low.set(node, Math.min(low.get(node) ?? time, low.get(next) ?? time));\n        if ((low.get(next) ?? 0) >= (discovery.get(node) ?? 0)) {\n          blocks.push(popBlock(node, next, edgeStack, blocks.length));\n        }\n      } else if ((discovery.get(next) ?? 0) < (discovery.get(node) ?? 0)) {\n        edgeStack.push([node, next]);\n        low.set(node, Math.min(low.get(node) ?? time, discovery.get(next) ?? time));\n      }\n    }\n  }, \"visit\");\n  for (const node of adjacency.keys()) {\n    if (!discovery.has(node)) {\n      visit(node, null);\n    }\n  }\n  return blocks;\n}\nchunk_Y2CYZVJY_name(computeBlocks, \"computeBlocks\");\nfunction popBlock(u, v, stack, id) {\n  const edges = [];\n  const nodes = /* @__PURE__ */ new Set();\n  while (stack.length > 0) {\n    const edge = stack.pop();\n    edges.push(edge);\n    nodes.add(edge[0]);\n    nodes.add(edge[1]);\n    if (edge[0] === u && edge[1] === v || edge[0] === v && edge[1] === u) {\n      break;\n    }\n  }\n  return { id, edges, nodes: [...nodes] };\n}\nchunk_Y2CYZVJY_name(popBlock, \"popBlock\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.crossCounts.ts\nfunction computeSubtreeCrossCounts(g, rankOf, tree) {\n  const nodes = [...g.nodes];\n  const indexOf = /* @__PURE__ */ new Map();\n  for (const [i, node] of nodes.entries()) {\n    indexOf.set(node, i);\n  }\n  const n = nodes.length;\n  const parentIdx = new Array(n).fill(-1);\n  const depth = new Array(n).fill(0);\n  const queue = [];\n  const seen = /* @__PURE__ */ new Set();\n  for (const node of nodes) {\n    const parentId = tree.parent.get(node) ?? null;\n    const idx = indexOf.get(node);\n    if (idx == null) {\n      continue;\n    }\n    if (parentId == null) {\n      parentIdx[idx] = -1;\n      depth[idx] = 0;\n      if (!seen.has(node)) {\n        seen.add(node);\n        queue.push(node);\n      }\n    }\n  }\n  while (queue.length > 0) {\n    const current = queue.shift();\n    const currentIdx = indexOf.get(current);\n    if (currentIdx == null) {\n      continue;\n    }\n    const childList = tree.children.get(current) ?? [];\n    for (const child of childList) {\n      if (seen.has(child)) {\n        continue;\n      }\n      const childIdx = indexOf.get(child);\n      if (childIdx == null) {\n        continue;\n      }\n      parentIdx[childIdx] = currentIdx;\n      depth[childIdx] = depth[currentIdx] + 1;\n      seen.add(child);\n      queue.push(child);\n    }\n  }\n  for (const node of nodes) {\n    if (seen.has(node)) {\n      continue;\n    }\n    const idx = indexOf.get(node);\n    if (idx == null) {\n      continue;\n    }\n    parentIdx[idx] = -1;\n    depth[idx] = 0;\n    seen.add(node);\n  }\n  const maxLog = Math.max(1, Math.ceil(Math.log2(Math.max(1, n))) + 1);\n  const up = Array.from({ length: maxLog }, () => new Array(n).fill(-1));\n  for (let i = 0; i < n; i++) {\n    up[0][i] = parentIdx[i];\n  }\n  for (let k = 1; k < maxLog; k++) {\n    for (let i = 0; i < n; i++) {\n      const prev = up[k - 1][i];\n      up[k][i] = prev === -1 ? -1 : up[k - 1][prev];\n    }\n  }\n  const lcaIndex = /* @__PURE__ */ chunk_Y2CYZVJY_name((aIdx, bIdx) => {\n    if (aIdx === -1 || bIdx === -1) {\n      return -1;\n    }\n    if (depth[aIdx] < depth[bIdx]) {\n      [aIdx, bIdx] = [bIdx, aIdx];\n    }\n    const diff = depth[aIdx] - depth[bIdx];\n    for (let k = 0; k < maxLog; k++) {\n      if (diff >> k & 1) {\n        aIdx = up[k][aIdx];\n        if (aIdx === -1) {\n          return -1;\n        }\n      }\n    }\n    if (aIdx === bIdx) {\n      return aIdx;\n    }\n    for (let k = maxLog - 1; k >= 0; k--) {\n      const upA = up[k][aIdx];\n      const upB = up[k][bIdx];\n      if (upA === -1 || upB === -1) {\n        continue;\n      }\n      if (upA !== upB) {\n        aIdx = upA;\n        bIdx = upB;\n      }\n    }\n    return up[0][aIdx];\n  }, \"lcaIndex\");\n  const ownCounts = Array.from({ length: n }, () => /* @__PURE__ */ new Map());\n  for (const edge of g.edges) {\n    let src = edge.src;\n    let dst = edge.dst;\n    let ru = rankOf[src];\n    let rv = rankOf[dst];\n    if (ru == null || rv == null) {\n      continue;\n    }\n    if (ru > rv) {\n      [src, dst] = [dst, src];\n      [ru, rv] = [rv, ru];\n    }\n    if (ru == null || rv == null || ru === rv) {\n      continue;\n    }\n    const upperIdx = indexOf.get(src);\n    const lowerIdx = indexOf.get(dst);\n    if (upperIdx == null || lowerIdx == null) {\n      continue;\n    }\n    const lca = lcaIndex(upperIdx, lowerIdx);\n    if (lca === -1) {\n      continue;\n    }\n    const bucket = ownCounts[lca];\n    for (let layer = ru; layer < rv; layer++) {\n      bucket.set(layer, (bucket.get(layer) ?? 0) + 1);\n    }\n  }\n  const crossCounts = /* @__PURE__ */ new Map();\n  const mergeInto = /* @__PURE__ */ chunk_Y2CYZVJY_name((target, source) => {\n    if (source.size === 0) {\n      return;\n    }\n    for (const [layer, value] of source) {\n      target.set(layer, (target.get(layer) ?? 0) + value);\n    }\n  }, \"mergeInto\");\n  const visited = /* @__PURE__ */ new Set();\n  const dfs = /* @__PURE__ */ chunk_Y2CYZVJY_name((node) => {\n    const idx = indexOf.get(node);\n    visited.add(node);\n    const base = idx == null ? void 0 : ownCounts[idx];\n    const accumulator = base ? new Map(base) : /* @__PURE__ */ new Map();\n    const childList = tree.children.get(node) ?? [];\n    for (const child of childList) {\n      const childMap = dfs(child);\n      const parentLayer = rankOf[node];\n      if (parentLayer != null) {\n        let map = crossCounts.get(node);\n        if (!map) {\n          map = /* @__PURE__ */ new Map();\n          crossCounts.set(node, map);\n        }\n        let value = childMap.get(parentLayer) ?? 0;\n        const childLayer = rankOf[child];\n        if (childLayer != null && childLayer > parentLayer) {\n          value += 1;\n        }\n        map.set(child, value);\n      }\n      mergeInto(accumulator, childMap);\n    }\n    return accumulator;\n  }, \"dfs\");\n  for (const root of tree.roots) {\n    if (!visited.has(root)) {\n      dfs(root);\n    }\n  }\n  for (const node of nodes) {\n    if (!visited.has(node)) {\n      dfs(node);\n    }\n  }\n  return crossCounts;\n}\nchunk_Y2CYZVJY_name(computeSubtreeCrossCounts, \"computeSubtreeCrossCounts\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.multitree.core.ts\nfunction annotateMinimumLayers(nodes, children, rankOf) {\n  const minLayer = /* @__PURE__ */ new Map();\n  const annotate = /* @__PURE__ */ chunk_Y2CYZVJY_name((node) => {\n    let minL = rankOf[node] ?? 0;\n    const childList = [...children.get(node) ?? []];\n    childList.sort(compareByRankThenId(rankOf));\n    for (const child of childList) {\n      annotate(child);\n      const childMin = minLayer.get(child);\n      if (childMin != null) {\n        minL = Math.min(minL, childMin);\n      }\n    }\n    minLayer.set(node, minL);\n  }, \"annotate\");\n  for (const node of nodes) {\n    annotate(node);\n  }\n  return minLayer;\n}\nchunk_Y2CYZVJY_name(annotateMinimumLayers, \"annotateMinimumLayers\");\nfunction compareByRankThenId(rankOf) {\n  return (a, b) => {\n    const ra = rankOf[a] ?? 0;\n    const rb = rankOf[b] ?? 0;\n    return ra === rb ? a.localeCompare(b) : ra - rb;\n  };\n}\nchunk_Y2CYZVJY_name(compareByRankThenId, \"compareByRankThenId\");\nfunction emitNodesInTreeOrder(roots, allNodes, rankOf, orderChildren) {\n  let maxRank = 0;\n  for (const node of allNodes) {\n    const r = rankOf[node] ?? 0;\n    if (r > maxRank) {\n      maxRank = r;\n    }\n  }\n  const layers = Array.from({ length: maxRank + 1 }, () => []);\n  const emitted = /* @__PURE__ */ new Set();\n  const emit = /* @__PURE__ */ chunk_Y2CYZVJY_name((node) => {\n    if (emitted.has(node)) {\n      return;\n    }\n    emitted.add(node);\n    const layer = rankOf[node] ?? 0;\n    if (!layers[layer]) {\n      layers[layer] = [];\n    }\n    layers[layer].push(node);\n    for (const child of orderChildren(node)) {\n      emit(child);\n    }\n  }, \"emit\");\n  for (const root of roots) {\n    emit(root);\n  }\n  for (const node of allNodes) {\n    if (!emitted.has(node)) {\n      const layer = rankOf[node] ?? 0;\n      if (!layers[layer]) {\n        layers[layer] = [];\n      }\n      layers[layer].push(node);\n      emitted.add(node);\n    }\n  }\n  return layers;\n}\nchunk_Y2CYZVJY_name(emitNodesInTreeOrder, \"emitNodesInTreeOrder\");\nfunction deduplicateLayers(layers) {\n  const result = [];\n  for (const layer of layers) {\n    const seen = /* @__PURE__ */ new Set();\n    const deduped = [];\n    for (const id of layer) {\n      if (seen.has(id)) {\n        continue;\n      }\n      seen.add(id);\n      deduped.push(id);\n    }\n    result.push(deduped);\n  }\n  return result;\n}\nchunk_Y2CYZVJY_name(deduplicateLayers, \"deduplicateLayers\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.multitree.order.ts\nfunction createChildOrderer(children, rankOf, crossCounts, minLayer) {\n  return (node) => {\n    const raw = children.get(node) ?? [];\n    if (raw.length === 0) {\n      return [];\n    }\n    const layer = rankOf[node] ?? 0;\n    const future = [];\n    const present = [];\n    const crossMap = crossCounts.get(node);\n    for (const child of raw) {\n      const minL = minLayer.get(child) ?? layer;\n      if (minL > layer) {\n        future.push({ child, min: minL });\n      } else {\n        present.push(child);\n      }\n    }\n    future.sort((a, b) => {\n      if (a.min === b.min) {\n        return a.child.localeCompare(b.child);\n      }\n      return a.min - b.min;\n    });\n    present.sort((a, b) => {\n      const ca = crossMap?.get(a) ?? 0;\n      const cb = crossMap?.get(b) ?? 0;\n      if (ca !== cb) {\n        return ca - cb;\n      }\n      const ma = minLayer.get(a) ?? layer;\n      const mb = minLayer.get(b) ?? layer;\n      if (ma !== mb) {\n        return ma - mb;\n      }\n      return a.localeCompare(b);\n    });\n    return [...future.map((item) => item.child), ...present];\n  };\n}\nchunk_Y2CYZVJY_name(createChildOrderer, \"createChildOrderer\");\nfunction buildMultitreeLayerOrder(g, rankOf, laneOf) {\n  const tree = buildDrivingTree(g, {\n    rankHint: rankOf,\n    laneOf\n  });\n  const { children, roots } = tree;\n  for (const node of g.nodes) {\n    if (!children.has(node)) {\n      children.set(node, []);\n    }\n  }\n  const crossCounts = computeSubtreeCrossCounts(g, rankOf, tree);\n  const rootsSorted = [...roots].sort(compareByRankThenId(rankOf));\n  const minLayer = annotateMinimumLayers(rootsSorted, children, rankOf);\n  const orderChildren = createChildOrderer(children, rankOf, crossCounts, minLayer);\n  let layers = emitNodesInTreeOrder(rootsSorted, g.nodes, rankOf, orderChildren);\n  layers = deduplicateLayers(layers);\n  return layers;\n}\nchunk_Y2CYZVJY_name(buildMultitreeLayerOrder, \"buildMultitreeLayerOrder\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.crossOptimization.ts\nfunction countCrossingsBetweenAdjacent(upper, lower, edges) {\n  const upperSet = new Set(upper);\n  const lowerSet = new Set(lower);\n  const li = buildLayerIndex(lower);\n  const vs = [];\n  for (const e of edges) {\n    if (upperSet.has(e.src) && lowerSet.has(e.dst)) {\n      vs.push(li.get(e.dst));\n    }\n  }\n  return countInversions(vs);\n}\nchunk_Y2CYZVJY_name(countCrossingsBetweenAdjacent, \"countCrossingsBetweenAdjacent\");\nfunction totalCrossings(layers, edges, rankOf) {\n  const expanded = [];\n  for (const e of edges) {\n    const ru = rankOf[e.src];\n    const rv = rankOf[e.dst];\n    if (ru == null || rv == null || ru === rv) {\n      continue;\n    }\n    let upper = e.src;\n    let lower = e.dst;\n    let rUpper = ru;\n    let rLower = rv;\n    if (ru > rv) {\n      upper = e.dst;\n      lower = e.src;\n      rUpper = rv;\n      rLower = ru;\n    }\n    for (let L = rUpper; L < rLower; L++) {\n      expanded.push({ id: `${e.id}@${L}`, src: upper, dst: lower, ref: e.ref });\n    }\n  }\n  let sum = 0;\n  for (let i = 0; i + 1 < layers.length; i++) {\n    sum += countCrossingsBetweenAdjacent(layers[i], layers[i + 1], expanded);\n  }\n  return sum;\n}\nchunk_Y2CYZVJY_name(totalCrossings, \"totalCrossings\");\nfunction optimizeRanksByCrossings(g, initialRank) {\n  const rankOf = { ...initialRank };\n  const { preds } = buildPredecessorSuccessorMaps(g);\n  const laneOf = createTopLaneResolver(g);\n  const layers = buildMultitreeLayerOrder(g, rankOf, laneOf);\n  let best = totalCrossings(layers, g.edges, rankOf);\n  const maxPasses = LAYERING.MAX_CROSSING_OPTIMIZATION_PASSES;\n  for (let pass = 0; pass < maxPasses; pass++) {\n    let changed = false;\n    const nodesByRank = [...g.nodes].sort((a, b) => (rankOf[b] ?? 0) - (rankOf[a] ?? 0));\n    for (const v of nodesByRank) {\n      const r = rankOf[v] ?? 0;\n      if (r === 0) {\n        continue;\n      }\n      let lb = 0;\n      for (const u of preds.get(v) ?? []) {\n        lb = Math.max(lb, (rankOf[u] ?? 0) + 1);\n      }\n      if (lb >= r) {\n        continue;\n      }\n      const old = r;\n      rankOf[v] = lb;\n      const trialLayers = buildMultitreeLayerOrder(g, rankOf, laneOf);\n      const score = totalCrossings(trialLayers, g.edges, rankOf);\n      if (score < best) {\n        best = score;\n        changed = true;\n      } else {\n        rankOf[v] = old;\n      }\n    }\n    if (!changed) {\n      break;\n    }\n  }\n  return rankOf;\n}\nchunk_Y2CYZVJY_name(optimizeRanksByCrossings, \"optimizeRanksByCrossings\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.crossLaneAdjust.ts\nfunction adjustCrossLaneSources(g, rankOf) {\n  const topLaneOf = createTopLaneResolver(g);\n  const nodesByRank = [...g.nodes].sort(\n    (a, b) => (rankOf[a] ?? 0) - (rankOf[b] ?? 0) || a.localeCompare(b)\n  );\n  for (const v of nodesByRank) {\n    const laneV = topLaneOf(v);\n    if (!laneV) {\n      continue;\n    }\n    const outEdges = g.edges.filter((e) => e.src === v);\n    if (outEdges.length === 0) {\n      continue;\n    }\n    let hasSameLaneSucc = false;\n    let crossLaneCount = 0;\n    for (const e of outEdges) {\n      const laneDst = topLaneOf(e.dst);\n      if (laneDst == null || laneDst === laneV) {\n        hasSameLaneSucc = true;\n      } else {\n        crossLaneCount++;\n      }\n    }\n    if (crossLaneCount === 0 || hasSameLaneSucc) {\n      continue;\n    }\n    let crossLaneIncoming = 0;\n    let hasSameLanePred = false;\n    for (const e of g.edges) {\n      if (e.dst !== v) {\n        continue;\n      }\n      const laneSrc = topLaneOf(e.src);\n      if (!laneSrc) {\n        continue;\n      }\n      if (laneSrc === laneV) {\n        hasSameLanePred = true;\n      } else {\n        crossLaneIncoming++;\n      }\n    }\n    if (crossLaneIncoming > 0 || !hasSameLanePred) {\n      continue;\n    }\n    const current = rankOf[v] ?? 0;\n    const target = current + crossLaneCount;\n    let lb = 0;\n    for (const e of g.edges) {\n      if (e.dst === v) {\n        lb = Math.max(lb, (rankOf[e.src] ?? 0) + 1);\n      }\n    }\n    const newRank = Math.max(current, lb, target);\n    if (newRank !== current) {\n      rankOf[v] = newRank;\n    }\n  }\n}\nchunk_Y2CYZVJY_name(adjustCrossLaneSources, \"adjustCrossLaneSources\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.longestPath.ts\nfunction assignLayers_LongestPath(gAcyclic, opts) {\n  const g = normalizeGraph(gAcyclic);\n  const order = topoSortIfAcyclic(g) ?? [...g.nodes].sort();\n  const compact = opts?.compactSingleInput ?? false;\n  const topLaneOf = createTopLaneResolver(g);\n  let rankOf = /* @__PURE__ */ Object.create(null);\n  for (const v of order) {\n    const incAll = incoming(g, v);\n    const inc = opts?.ignoreCrossLaneEdges ? incAll.filter((e) => {\n      const laneSrc = topLaneOf(e.src);\n      const laneDst = topLaneOf(v);\n      if (!laneSrc || !laneDst) {\n        return true;\n      }\n      return laneSrc === laneDst;\n    }) : incAll;\n    if (inc.length === 0) {\n      rankOf[v] = 0;\n    } else if (compact && inc.length === 1) {\n      const u = inc[0].src;\n      const laneU = topLaneOf(u);\n      const laneV = topLaneOf(v);\n      if (laneU !== laneV) {\n        rankOf[v] = rankOf[u] ?? 0;\n      } else {\n        rankOf[v] = (rankOf[u] ?? 0) + 1;\n      }\n    } else {\n      let mx = -Infinity;\n      for (const e of inc) {\n        mx = Math.max(mx, (rankOf[e.src] ?? 0) + 1);\n      }\n      rankOf[v] = mx === -Infinity ? 0 : mx;\n    }\n  }\n  if (opts?.optimizeRanksByCrossings ?? false) {\n    rankOf = optimizeRanksByCrossings(g, rankOf);\n  }\n  if (opts?.ignoreCrossLaneEdges) {\n    adjustCrossLaneSources(g, rankOf);\n  }\n  const layers = buildMultitreeLayerOrder(g, rankOf, topLaneOf);\n  return { layers, rankOf, dummy: /* @__PURE__ */ new Set() };\n}\nchunk_Y2CYZVJY_name(assignLayers_LongestPath, \"assignLayers_LongestPath\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.gravity.ts\nfunction assignLayers_Gravity(gAcyclic, opts) {\n  const g = normalizeGraph(gAcyclic);\n  const base = assignLayers_LongestPath(g, {\n    compactSingleInput: opts?.compactSingleInput,\n    ignoreCrossLaneEdges: opts?.ignoreCrossLaneEdges,\n    optimizeRanksByCrossings: opts?.optimizeRanksByCrossings\n  });\n  const rankOf = { ...base.rankOf };\n  const topLaneOf = createTopLaneResolver(g);\n  const { preds, succs } = buildPredecessorSuccessorMaps(g, (e) => {\n    if (opts?.ignoreCrossLaneEdges) {\n      const laneSrc = topLaneOf(e.src);\n      const laneDst = topLaneOf(e.dst);\n      if (laneSrc && laneDst && laneSrc !== laneDst) {\n        return false;\n      }\n    }\n    return true;\n  });\n  const order = topoSortIfAcyclic(g) ?? [...g.nodes];\n  const revOrder = [...order].reverse();\n  const clampFeasible = /* @__PURE__ */ chunk_Y2CYZVJY_name((v, desired) => {\n    let lb = 0;\n    for (const u of preds.get(v) ?? []) {\n      lb = Math.max(lb, (rankOf[u] ?? 0) + 1);\n    }\n    let ub = Number.POSITIVE_INFINITY;\n    const s = succs.get(v) ?? [];\n    if (s.length > 0) {\n      ub = Math.min(...s.map((w) => (rankOf[w] ?? 0) - 1));\n    }\n    if (!Number.isFinite(ub)) {\n      ub = Math.max(lb, desired);\n    }\n    return Math.min(Math.max(desired, lb), ub);\n  }, \"clampFeasible\");\n  const iters = LAYERING.GRAVITY_ITERATIONS;\n  const relaxOrder = /* @__PURE__ */ chunk_Y2CYZVJY_name((nodeOrder) => {\n    let changed = false;\n    for (const v of nodeOrder) {\n      const ps = preds.get(v) ?? [];\n      const ss = succs.get(v) ?? [];\n      if (ps.length === 0 && ss.length === 0) {\n        continue;\n      }\n      const predAvg = ps.length > 0 ? ps.reduce((a, u) => a + (rankOf[u] ?? 0) + 1, 0) / ps.length : rankOf[v] ?? 0;\n      const succAvg = ss.length > 0 ? ss.reduce((a, w) => a + (rankOf[w] ?? 0) - 1, 0) / ss.length : rankOf[v] ?? 0;\n      const desired = Math.round((predAvg + succAvg) / 2);\n      const clamped = clampFeasible(v, desired);\n      if (clamped !== rankOf[v]) {\n        rankOf[v] = clamped;\n        changed = true;\n      }\n    }\n    return changed;\n  }, \"relaxOrder\");\n  for (let it = 0; it < iters; it++) {\n    const forwardChanged = relaxOrder(order);\n    const backwardChanged = relaxOrder(revOrder);\n    if (!forwardChanged && !backwardChanged) {\n      break;\n    }\n  }\n  for (const v of order) {\n    let lb = 0;\n    for (const u of preds.get(v) ?? []) {\n      lb = Math.max(lb, (rankOf[u] ?? 0) + 1);\n    }\n    if ((rankOf[v] ?? 0) < lb) {\n      rankOf[v] = lb;\n    }\n  }\n  for (const v of revOrder) {\n    const s = succs.get(v) ?? [];\n    if (s.length > 0) {\n      const ub = Math.min(...s.map((w) => (rankOf[w] ?? 0) - 1));\n      if ((rankOf[v] ?? 0) > ub) {\n        rankOf[v] = ub;\n      }\n    }\n  }\n  const layers = buildLayersFromRanks(g, order, rankOf);\n  return { layers, rankOf, dummy: /* @__PURE__ */ new Set() };\n}\nchunk_Y2CYZVJY_name(assignLayers_Gravity, \"assignLayers_Gravity\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.laneAwareCompact.ts\nfunction topoSortByGenerationIfAcyclic(g) {\n  const indeg = buildInDegreeMap(g);\n  const adj = buildSortedSuccessorMap(g);\n  let frontier = sortedZeroInDegreeNodes(indeg);\n  const order = [];\n  while (frontier.length > 0) {\n    const nextFrontier = [];\n    for (const u of frontier) {\n      order.push(u);\n      for (const v of adj.get(u) ?? []) {\n        indeg.set(v, (indeg.get(v) ?? 0) - 1);\n        if ((indeg.get(v) ?? 0) === 0) {\n          nextFrontier.push(v);\n        }\n      }\n    }\n    frontier = nextFrontier.sort((a, b) => a.localeCompare(b));\n  }\n  return order.length === g.nodes.length ? order : null;\n}\nchunk_Y2CYZVJY_name(topoSortByGenerationIfAcyclic, \"topoSortByGenerationIfAcyclic\");\nfunction assignLayers_LaneAwareCompact(gAcyclic, opts) {\n  const g = normalizeGraph(gAcyclic);\n  const order = opts?.direction === \"LR\" ? topoSortByGenerationIfAcyclic(g) ?? [...g.nodes].sort() : topoSortIfAcyclic(g) ?? [...g.nodes].sort();\n  const topLaneOf = createTopLaneResolver(g);\n  const laneOf = /* @__PURE__ */ chunk_Y2CYZVJY_name((id) => topLaneOf(id) ?? id, \"laneOf\");\n  const rankOf = /* @__PURE__ */ Object.create(null);\n  const nextFree = /* @__PURE__ */ new Map();\n  const edgeWeight = /* @__PURE__ */ chunk_Y2CYZVJY_name((u, v) => {\n    const ignoreCrossLane = opts?.ignoreCrossLaneEdges ?? true;\n    if (ignoreCrossLane) {\n      return laneOf(u) === laneOf(v) ? 1 : 0;\n    }\n    return 1;\n  }, \"edgeWeight\");\n  for (const v of order) {\n    const node = g.nodeById.get(v);\n    if (node?.isGroup) {\n      continue;\n    }\n    const preds = incoming(g, v);\n    let base = 0;\n    if (preds.length > 0) {\n      for (const e of preds) {\n        const u = e.src;\n        const ru = rankOf[u] ?? 0;\n        base = Math.max(base, ru + edgeWeight(u, v));\n      }\n    }\n    const lane = laneOf(v);\n    const nf = nextFree.get(lane) ?? 0;\n    const L = Math.max(base, nf);\n    rankOf[v] = L;\n    nextFree.set(lane, L + 1);\n  }\n  const layers = buildLayersFromRanks(g, order, rankOf, { skipGroups: true });\n  return { layers, rankOf, dummy: /* @__PURE__ */ new Set() };\n}\nchunk_Y2CYZVJY_name(assignLayers_LaneAwareCompact, \"assignLayers_LaneAwareCompact\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.dummies.ts\nfunction makeProperLayering(layering, gAcyclic) {\n  const g = normalizeGraph(gAcyclic);\n  const { rankOf } = layering;\n  const layers = layering.layers.map((l) => [...l]);\n  const dummy = new Set(layering.dummy ? [...layering.dummy] : []);\n  let dummySeq = 0;\n  const nodeById = new Map(g.nodeById);\n  const addDummyAt = /* @__PURE__ */ chunk_Y2CYZVJY_name((L) => {\n    const id = `placeholder-${dummySeq++}`;\n    const dn = { id, isGroup: false, isDummy: true, width: 0, height: 0 };\n    nodeById.set(id, dn);\n    dummy.add(id);\n    while (layers.length <= L) {\n      layers.push([]);\n    }\n    layers[L].push(id);\n    rankOf[id] = L;\n    return id;\n  }, \"addDummyAt\");\n  const edgesSorted = [...g.edges].sort(\n    (a, b) => a.id === b.id ? a.src === b.src ? a.dst.localeCompare(b.dst) : a.src.localeCompare(b.src) : a.id.localeCompare(b.id)\n  );\n  const newEdges = [];\n  for (const e of edgesSorted) {\n    const rU = rankOf[e.src] ?? 0;\n    const rV = rankOf[e.dst] ?? 0;\n    if (rV - rU <= 1) {\n      newEdges.push(e);\n      continue;\n    }\n    let prev = e.src;\n    for (let L = rU + 1, k = 0; L < rV; L++, k++) {\n      const d = addDummyAt(L);\n      newEdges.push({ id: `${e.id}#${k}`, src: prev, dst: d, weight: e.weight, ref: e.ref });\n      prev = d;\n    }\n    const lastIndex = rV - rU - 2;\n    newEdges.push({\n      id: `${e.id}#${Math.max(lastIndex + 1, 0)}`,\n      src: prev,\n      dst: e.dst,\n      weight: e.weight,\n      ref: e.ref\n    });\n  }\n  const nodes = [...g.nodes, ...[...dummy].filter((id) => !g.nodes.includes(id))];\n  const graphWithDummies = { nodes, edges: newEdges, layout: g.layout, nodeById };\n  return { layering: { layers, rankOf, dummy }, graphWithDummies };\n}\nchunk_Y2CYZVJY_name(makeProperLayering, \"makeProperLayering\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase3.ordering.ts\nfunction median(values) {\n  const n = values.length;\n  if (n === 0) {\n    return Number.POSITIVE_INFINITY;\n  }\n  const a = [...values].sort((x, y) => x - y);\n  if (n % 2 === 1) {\n    return a[(n - 1) / 2];\n  }\n  return 0.5 * (a[n / 2 - 1] + a[n / 2]);\n}\nchunk_Y2CYZVJY_name(median, \"median\");\nfunction barycenter(values) {\n  if (values.length === 0) {\n    return Number.POSITIVE_INFINITY;\n  }\n  const s = values.reduce((acc, v) => acc + v, 0);\n  return s / values.length;\n}\nchunk_Y2CYZVJY_name(barycenter, \"barycenter\");\nfunction neighborPositionsFor(targetNodes, fixedIndex, edges, direction) {\n  const neighborPositions = /* @__PURE__ */ new Map();\n  for (const v of targetNodes) {\n    neighborPositions.set(v, []);\n  }\n  for (const e of edges) {\n    if (direction === \"down\") {\n      if (fixedIndex.has(e.src) && neighborPositions.has(e.dst)) {\n        neighborPositions.get(e.dst).push(fixedIndex.get(e.src));\n      }\n    } else if (fixedIndex.has(e.dst) && neighborPositions.has(e.src)) {\n      neighborPositions.get(e.src).push(fixedIndex.get(e.dst));\n    }\n  }\n  return neighborPositions;\n}\nchunk_Y2CYZVJY_name(neighborPositionsFor, \"neighborPositionsFor\");\nfunction currentOrderTieBreak(a, b, currentLayerIndex) {\n  const ia = currentLayerIndex.get(a) ?? 0;\n  const ib = currentLayerIndex.get(b) ?? 0;\n  return ia !== ib ? ia - ib : a.localeCompare(b);\n}\nchunk_Y2CYZVJY_name(currentOrderTieBreak, \"currentOrderTieBreak\");\nfunction countCrossingsBetweenAdjacent2(upper, lower, edges) {\n  const upperSet = new Set(upper);\n  const lowerSet = new Set(lower);\n  const upperIndex = buildLayerIndex(upper);\n  const lowerIndex = buildLayerIndex(lower);\n  const pairs = [];\n  for (const e of edges) {\n    if (upperSet.has(e.src) && lowerSet.has(e.dst)) {\n      pairs.push({ u: upperIndex.get(e.src), v: lowerIndex.get(e.dst) });\n    }\n  }\n  pairs.sort((a, b) => a.u === b.u ? a.v - b.v : a.u - b.u);\n  const vs = pairs.map((p) => p.v);\n  return countInversions(vs);\n}\nchunk_Y2CYZVJY_name(countCrossingsBetweenAdjacent2, \"countCrossingsBetweenAdjacent\");\nfunction sortByHeuristic(nodes, neighborPositions, currentLayerIndex) {\n  return [...nodes].sort((a, b) => {\n    const sa = median(neighborPositions.get(a) ?? []);\n    const sb = median(neighborPositions.get(b) ?? []);\n    if (sa === sb) {\n      return currentOrderTieBreak(a, b, currentLayerIndex);\n    }\n    if (!isFinite(sa)) {\n      return 1;\n    }\n    if (!isFinite(sb)) {\n      return -1;\n    }\n    return sa - sb;\n  });\n}\nchunk_Y2CYZVJY_name(sortByHeuristic, \"sortByHeuristic\");\nfunction reorderLayer(fixedLayer, targetLayer, edges, direction, topLaneOf, laneOrder) {\n  const fixedIndex = buildLayerIndex(fixedLayer);\n  const currIndex = buildLayerIndex(targetLayer);\n  const neighborPositions = neighborPositionsFor(targetLayer, fixedIndex, edges, direction);\n  if (!topLaneOf || !laneOrder || laneOrder.length === 0) {\n    return sortByHeuristic(targetLayer, neighborPositions, currIndex);\n  }\n  const byLane = /* @__PURE__ */ new Map();\n  for (const id of targetLayer) {\n    const lane = topLaneOf(id);\n    const arr = byLane.get(lane) ?? [];\n    arr.push(id);\n    byLane.set(lane, arr);\n  }\n  const result = [];\n  for (const lane of laneOrder) {\n    const nodesInLane = byLane.get(lane);\n    if (!nodesInLane || nodesInLane.length === 0) {\n      continue;\n    }\n    const sorted = sortByHeuristic(nodesInLane, neighborPositions, currIndex);\n    result.push(...sorted);\n  }\n  const nullNodes = byLane.get(null);\n  if (nullNodes && nullNodes.length > 0) {\n    const sorted = sortByHeuristic(nullNodes, neighborPositions, currIndex);\n    for (const nid of sorted) {\n      const bc = barycenter(neighborPositions.get(nid) ?? []);\n      let bestIdx = result.length;\n      if (isFinite(bc)) {\n        for (const [i, rid] of result.entries()) {\n          const rBc = barycenter(neighborPositions.get(rid) ?? []);\n          if (bc < rBc) {\n            bestIdx = i;\n            break;\n          }\n        }\n      }\n      result.splice(bestIdx, 0, nid);\n    }\n  }\n  return result;\n}\nchunk_Y2CYZVJY_name(reorderLayer, \"reorderLayer\");\nfunction transposeImprove(upper, current, edges, next, topLaneOf) {\n  const best = [...current];\n  const upperSet = new Set(upper);\n  const layerSet = new Set(current);\n  const nextSet = next ? new Set(next) : null;\n  const edgesIn = edges.filter((e) => upperSet.has(e.src) && layerSet.has(e.dst));\n  const edgesOut = nextSet ? edges.filter((e) => layerSet.has(e.src) && nextSet.has(e.dst)) : void 0;\n  const crossingScore = /* @__PURE__ */ chunk_Y2CYZVJY_name((order) => {\n    let score = countCrossingsBetweenAdjacent2(upper, order, edgesIn);\n    if (edgesOut && next) {\n      score += countCrossingsBetweenAdjacent2(order, next, edgesOut);\n    }\n    return score;\n  }, \"crossingScore\");\n  const laneOf = topLaneOf ? /* @__PURE__ */ new Map() : null;\n  if (topLaneOf && laneOf) {\n    for (const id of current) {\n      laneOf.set(id, topLaneOf(id));\n    }\n  }\n  let improved = true;\n  let bestScore = crossingScore(best);\n  while (improved) {\n    improved = false;\n    for (let i = 0; i + 1 < best.length; i++) {\n      if (laneOf) {\n        const laneA = laneOf.get(best[i]);\n        const laneB = laneOf.get(best[i + 1]);\n        if (laneA !== laneB) {\n          continue;\n        }\n      }\n      const prev = bestScore;\n      [best[i], best[i + 1]] = [best[i + 1], best[i]];\n      const nextScore = crossingScore(best);\n      if (nextScore < prev) {\n        bestScore = nextScore;\n        improved = true;\n      } else {\n        [best[i], best[i + 1]] = [best[i + 1], best[i]];\n      }\n    }\n  }\n  return best;\n}\nchunk_Y2CYZVJY_name(transposeImprove, \"transposeImprove\");\nfunction orderLayers(layering, gWithDummies, opts) {\n  const layers = layering.layers.map((l) => [...l]);\n  const edges = gWithDummies.edges;\n  const topLaneOf = createTopLaneResolver(gWithDummies);\n  const laneOrder = resolveTopLaneOrder(gWithDummies, opts?.laneOrder);\n  for (let s = 0; s < 3; s++) {\n    for (let i = 1; i < layers.length; i++) {\n      layers[i] = reorderLayer(layers[i - 1], layers[i], edges, \"down\", topLaneOf, laneOrder);\n      layers[i] = transposeImprove(layers[i - 1], layers[i], edges, layers[i + 1], topLaneOf);\n    }\n    for (let i = layers.length - 2; i >= 0; i--) {\n      layers[i] = reorderLayer(layers[i + 1], layers[i], edges, \"up\", topLaneOf, laneOrder);\n      layers[i] = transposeImprove(layers[i + 1], layers[i], edges, layers[i - 1], topLaneOf);\n    }\n  }\n  return { layers };\n}\nchunk_Y2CYZVJY_name(orderLayers, \"orderLayers\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase4.coordinates.ts\nfunction assignCoordinates(ordered, gWithDummies, opts) {\n  const layerGap = opts?.layerGap ?? COORDINATES.DEFAULT_LAYER_GAP;\n  const nodeGap = opts?.nodeGap ?? COORDINATES.DEFAULT_NODE_GAP;\n  const laneGap = opts?.laneGap ?? nodeGap * 2;\n  const direction = opts?.direction ?? \"TB\";\n  const isHorizontal = direction === \"LR\" || direction === \"RL\";\n  const layers = ordered.layers;\n  const x = /* @__PURE__ */ Object.create(null);\n  const y = /* @__PURE__ */ Object.create(null);\n  const getNode = /* @__PURE__ */ chunk_Y2CYZVJY_name((id) => gWithDummies.nodeById.get(id), \"getNode\");\n  const getWidth = /* @__PURE__ */ chunk_Y2CYZVJY_name((id) => getNode(id)?.width ?? 0, \"getWidth\");\n  const getHeight = /* @__PURE__ */ chunk_Y2CYZVJY_name((id) => getNode(id)?.height ?? 0, \"getHeight\");\n  const topLaneOf = createTopLaneResolver(gWithDummies);\n  const laneOrderGlobal = resolveTopLaneOrder(gWithDummies, opts?.laneOrder);\n  const layerHeights = layers.map(\n    (layer) => layer.reduce((m, v) => Math.max(m, getHeight(v)), 0)\n  );\n  const extraLayerGaps = [];\n  if (isHorizontal) {\n    for (let i = 0; i + 1 < layers.length; i++) {\n      const thisLayerMaxWidth = layers[i].reduce((m, v) => Math.max(m, getWidth(v)), 0);\n      const nextLayerMaxWidth = layers[i + 1].reduce((m, v) => Math.max(m, getWidth(v)), 0);\n      const thisLayerMaxHeight = layerHeights[i];\n      const nextLayerMaxHeight = layerHeights[i + 1];\n      const normalSpacing = thisLayerMaxHeight / 2 + nextLayerMaxHeight / 2;\n      const requiredSpacing = (thisLayerMaxWidth + nextLayerMaxWidth) / 2;\n      const extraNeeded = Math.max(0, requiredSpacing - normalSpacing - layerGap);\n      extraLayerGaps.push(extraNeeded);\n    }\n  }\n  const lanesUsedSet = /* @__PURE__ */ new Set();\n  for (const layer of layers) {\n    for (const id of layer) {\n      lanesUsedSet.add(topLaneOf(id));\n    }\n  }\n  const hasNullLane = lanesUsedSet.has(null);\n  const lanesUsed = laneOrderGlobal.filter((L) => lanesUsedSet.has(L));\n  const laneOrderColumns = [...hasNullLane ? [null] : [], ...lanesUsed];\n  const laneWidth = /* @__PURE__ */ Object.create(null);\n  for (const L of lanesUsed) {\n    laneWidth[L] = 0;\n  }\n  if (hasNullLane) {\n    laneWidth.null = 0;\n  }\n  for (const layer of layers) {\n    const perLane = /* @__PURE__ */ Object.create(null);\n    const nullIds = [];\n    for (const id of layer) {\n      const L = topLaneOf(id);\n      if (L === null) {\n        nullIds.push(id);\n      } else {\n        (perLane[L] ||= []).push(id);\n      }\n    }\n    for (const [L, ids] of Object.entries(perLane)) {\n      const total = ids.reduce((s, id) => s + getWidth(id), 0) + nodeGap * Math.max(0, ids.length - 1);\n      laneWidth[L] = Math.max(laneWidth[L] ?? 0, total);\n    }\n    if (hasNullLane && nullIds.length) {\n      const totalNull = nullIds.reduce((s, id) => s + getWidth(id), 0) + nodeGap * Math.max(0, nullIds.length - 1);\n      laneWidth.null = Math.max(laneWidth.null ?? 0, totalNull);\n    }\n  }\n  const centerX = /* @__PURE__ */ new Map();\n  {\n    const widths = laneOrderColumns.map(\n      (L) => (L === null ? laneWidth.null : laneWidth[L]) ?? 0\n    );\n    const totalW = widths.reduce((a, b) => a + b, 0) + laneGap * Math.max(0, laneOrderColumns.length - 1);\n    let cursor = -totalW / 2;\n    for (let i = 0; i < laneOrderColumns.length; i++) {\n      const L = laneOrderColumns[i];\n      const w = widths[i] ?? 0;\n      const cx = cursor + w / 2;\n      centerX.set(L, cx);\n      cursor += w;\n      if (i < laneOrderColumns.length - 1) {\n        cursor += laneGap;\n      }\n    }\n  }\n  let yOffset = 0;\n  for (const [li, layer] of layers.entries()) {\n    const layerH = layerHeights[li] ?? 0;\n    const byLane = /* @__PURE__ */ new Map();\n    for (const id of layer) {\n      const laneId = topLaneOf(id);\n      const arr = byLane.get(laneId) ?? [];\n      arr.push(id);\n      byLane.set(laneId, arr);\n    }\n    for (const L of laneOrderColumns) {\n      const nodesInLane = byLane.get(L) ?? [];\n      if (nodesInLane.length === 0) {\n        continue;\n      }\n      const cx = centerX.get(L);\n      if (nodesInLane.length === 1) {\n        const id = nodesInLane[0];\n        x[id] = cx;\n        y[id] = yOffset + layerH / 2;\n      } else {\n        const widths = nodesInLane.map((id) => getWidth(id));\n        const total = widths.reduce((a, b) => a + b, 0) + nodeGap * (nodesInLane.length - 1);\n        let start = cx - total / 2;\n        for (const [i, id] of nodesInLane.entries()) {\n          const w = widths[i];\n          x[id] = start + w / 2;\n          y[id] = yOffset + layerH / 2;\n          start += w + nodeGap;\n        }\n      }\n    }\n    const extraGap = extraLayerGaps[li] ?? 0;\n    yOffset += layerH + layerGap + extraGap;\n  }\n  const byRef = /* @__PURE__ */ new Map();\n  for (const e of gWithDummies.edges) {\n    const rid = e.ref.id;\n    if (!byRef.has(rid)) {\n      byRef.set(rid, []);\n    }\n    byRef.get(rid).push(e);\n  }\n  for (const [, chainEdges] of byRef) {\n    if (chainEdges.length === 0) {\n      continue;\n    }\n    const ref = chainEdges[0].ref;\n    const src = ref.start;\n    const dst = ref.end;\n    if (src == null || dst == null) {\n      continue;\n    }\n    const midX = Math.round(((x[src] ?? 0) + (x[dst] ?? 0)) / 2);\n    const involved = /* @__PURE__ */ new Set();\n    for (const e of chainEdges) {\n      involved.add(e.src);\n      involved.add(e.dst);\n    }\n    for (const vid of involved) {\n      if (vid === src || vid === dst) {\n        continue;\n      }\n      const node = gWithDummies.nodeById.get(vid);\n      if (node?.isDummy) {\n        x[vid] = midX;\n      }\n    }\n  }\n  return { x, y };\n}\nchunk_Y2CYZVJY_name(assignCoordinates, \"assignCoordinates\");\n\n// src/rendering-util/layout-algorithms/swimlanes/laneOrdering.ts\nvar AUTOMATIC_LANE_ORDERING_RESTARTS = 8;\nfunction hashString(input) {\n  let hash = 2166136261;\n  for (let i = 0; i < input.length; i++) {\n    hash ^= input.charCodeAt(i);\n    hash = Math.imul(hash, 16777619);\n  }\n  return hash >>> 0;\n}\nchunk_Y2CYZVJY_name(hashString, \"hashString\");\nfunction mulberry32(seed) {\n  let state = seed >>> 0;\n  return () => {\n    state += 1831565813;\n    let t = state;\n    t = Math.imul(t ^ t >>> 15, t | 1);\n    t ^= t + Math.imul(t ^ t >>> 7, t | 61);\n    return ((t ^ t >>> 14) >>> 0) / 4294967296;\n  };\n}\nchunk_Y2CYZVJY_name(mulberry32, \"mulberry32\");\nfunction deterministicShuffle(order, seed) {\n  const shuffled = [...order];\n  const random = mulberry32(seed);\n  for (let i = shuffled.length - 1; i > 0; i--) {\n    const j = Math.floor(random() * (i + 1));\n    [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];\n  }\n  return shuffled;\n}\nchunk_Y2CYZVJY_name(deterministicShuffle, \"deterministicShuffle\");\nfunction sourceDistance(order, sourceIndex) {\n  let distance = 0;\n  for (const [index, laneId] of order.entries()) {\n    distance += Math.abs(index - (sourceIndex.get(laneId) ?? index));\n  }\n  return distance;\n}\nchunk_Y2CYZVJY_name(sourceDistance, \"sourceDistance\");\nfunction laneArrangementCost(order, weights) {\n  const position = /* @__PURE__ */ new Map();\n  for (const [index, laneId] of order.entries()) {\n    position.set(laneId, index);\n  }\n  let cost = 0;\n  for (const { a, b, weight } of weights) {\n    const ai = position.get(a);\n    const bi = position.get(b);\n    if (ai == null || bi == null) {\n      continue;\n    }\n    cost += weight * Math.abs(ai - bi);\n  }\n  return cost;\n}\nchunk_Y2CYZVJY_name(laneArrangementCost, \"laneArrangementCost\");\nfunction buildWeightedLaneEdges(g) {\n  const sourceOrder = buildTopLaneOrder(g);\n  if (sourceOrder.length < 2) {\n    return [];\n  }\n  const sourceIndex = new Map(sourceOrder.map((laneId, index) => [laneId, index]));\n  const topLaneOf = createTopLaneResolver(g);\n  const weights = /* @__PURE__ */ new Map();\n  for (const edge of g.layout.edges ?? []) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const src = typeof edge.start === \"string\" ? edge.start : void 0;\n    const dst = typeof edge.end === \"string\" ? edge.end : void 0;\n    if (!src || !dst || !g.nodeById.has(src) || !g.nodeById.has(dst)) {\n      continue;\n    }\n    const laneA = topLaneOf(src);\n    const laneB = topLaneOf(dst);\n    if (!laneA || !laneB || laneA === laneB) {\n      continue;\n    }\n    const ia = sourceIndex.get(laneA);\n    const ib = sourceIndex.get(laneB);\n    if (ia == null || ib == null) {\n      continue;\n    }\n    const [a, b] = ia <= ib ? [laneA, laneB] : [laneB, laneA];\n    const key = `${a}\\0${b}`;\n    const existing = weights.get(key);\n    if (existing) {\n      existing.weight++;\n    } else {\n      weights.set(key, { a, b, weight: 1 });\n    }\n  }\n  return [...weights.values()];\n}\nchunk_Y2CYZVJY_name(buildWeightedLaneEdges, \"buildWeightedLaneEdges\");\nfunction greedySwitch(startOrder, weights, sourceIndex) {\n  const order = [...startOrder];\n  let cost = laneArrangementCost(order, weights);\n  let changed = true;\n  let sweeps = 0;\n  const maxSweeps = Math.max(1, order.length);\n  while (changed && sweeps < maxSweeps) {\n    changed = false;\n    sweeps++;\n    for (let i = 0; i + 1 < order.length; i++) {\n      [order[i], order[i + 1]] = [order[i + 1], order[i]];\n      const nextCost = laneArrangementCost(order, weights);\n      if (nextCost < cost) {\n        cost = nextCost;\n        changed = true;\n      } else {\n        [order[i], order[i + 1]] = [order[i + 1], order[i]];\n      }\n    }\n  }\n  return {\n    order,\n    cost,\n    sourceDistance: sourceDistance(order, sourceIndex)\n  };\n}\nchunk_Y2CYZVJY_name(greedySwitch, \"greedySwitch\");\nfunction isBetterCandidate(candidate, best) {\n  if (candidate.cost !== best.cost) {\n    return candidate.cost < best.cost;\n  }\n  return candidate.sourceDistance < best.sourceDistance;\n}\nchunk_Y2CYZVJY_name(isBetterCandidate, \"isBetterCandidate\");\nfunction seedForRestart(sourceOrder, weights, restartIndex) {\n  const weightSignature = [...weights].sort((a, b) => a.a === b.a ? a.b.localeCompare(b.b) : a.a.localeCompare(b.a)).map(({ a, b, weight }) => `${a}:${b}:${weight}`).join(\"|\");\n  return hashString(`${sourceOrder.join(\"|\")}#${weightSignature}#${restartIndex}`);\n}\nchunk_Y2CYZVJY_name(seedForRestart, \"seedForRestart\");\nfunction optimizeTopLaneOrder(g, opts = {}) {\n  const sourceOrder = buildTopLaneOrder(g);\n  if (sourceOrder.length < 2) {\n    return sourceOrder;\n  }\n  const weights = buildWeightedLaneEdges(g);\n  if (weights.length === 0) {\n    return sourceOrder;\n  }\n  const sourceIndex = new Map(sourceOrder.map((laneId, index) => [laneId, index]));\n  let best = greedySwitch(sourceOrder, weights, sourceIndex);\n  const restarts = Math.max(0, opts.restarts ?? AUTOMATIC_LANE_ORDERING_RESTARTS);\n  for (let i = 0; i < restarts; i++) {\n    const seed = seedForRestart(sourceOrder, weights, i);\n    const start = deterministicShuffle(sourceOrder, seed);\n    const candidate = greedySwitch(start, weights, sourceIndex);\n    if (isBetterCandidate(candidate, best)) {\n      best = candidate;\n    }\n  }\n  return best.order;\n}\nchunk_Y2CYZVJY_name(optimizeTopLaneOrder, \"optimizeTopLaneOrder\");\n\n// src/rendering-util/layout-algorithms/swimlanes/pipeline.ts\nfunction sugiyamaLayout(g, opts) {\n  const ignoreCrossLaneEdges = opts?.ignoreCrossLaneEdges ?? true;\n  const optimizeRanksByCrossings2 = opts?.optimizeRanksByCrossings ?? true;\n  const g0 = normalizeGraph(g);\n  const laneOrder = opts?.automaticLaneOrdering ? optimizeTopLaneOrder(g0, { restarts: AUTOMATIC_LANE_ORDERING_RESTARTS }) : void 0;\n  const cycleRes = removeCycles_DFS(g0);\n  const gAcyclic = cycleRes.acyclic;\n  const layering = ignoreCrossLaneEdges ? assignLayers_LaneAwareCompact(gAcyclic, {\n    compactSingleInput: opts?.compactSingleInput ?? LAYERING.DEFAULT_COMPACT_SINGLE_INPUT,\n    ignoreCrossLaneEdges: true,\n    direction: opts?.direction\n  }) : assignLayers_Gravity(gAcyclic, {\n    compactSingleInput: opts?.compactSingleInput ?? LAYERING.DEFAULT_COMPACT_SINGLE_INPUT,\n    ignoreCrossLaneEdges: false,\n    optimizeRanksByCrossings: optimizeRanksByCrossings2\n  });\n  const { layering: properLayering, graphWithDummies } = makeProperLayering(layering, gAcyclic);\n  const ordered = orderLayers(properLayering, graphWithDummies, { laneOrder });\n  const coordinates = assignCoordinates(ordered, graphWithDummies, {\n    layerGap: opts?.layerGap,\n    nodeGap: opts?.nodeGap,\n    direction: opts?.direction,\n    laneOrder\n  });\n  return {\n    acyclic: gAcyclic,\n    reversed: cycleRes.reversed,\n    layering: properLayering,\n    ordered,\n    coordinates\n  };\n}\nchunk_Y2CYZVJY_name(sugiyamaLayout, \"sugiyamaLayout\");\n\n// src/rendering-util/layout-algorithms/swimlanes/orthogonalRouter/router.ts\nvar EPS7 = PRECISION.EPSILON;\nvar NODE_PADDING = 8;\nvar HORIZONTAL_PIPE_MARGIN = 15;\nvar VERTICAL_PIPE_MARGIN = 15;\nvar ROUTING_MARGIN = 25;\nvar ANCHOR_OFFSET = 20;\nvar TRACK_SPACING = 10;\nfunction chooseOrthogonalSide(node, target, fallback) {\n  const cx = node.x ?? 0;\n  const cy = node.y ?? 0;\n  const dx = target.x - cx;\n  const dy = target.y - cy;\n  const absDx = Math.abs(dx);\n  const absDy = Math.abs(dy);\n  if (absDx < EPS7 && absDy < EPS7) {\n    return fallback;\n  }\n  const verticalBias = 3;\n  if (absDy > EPS7 && absDy * verticalBias >= absDx) {\n    return dy > 0 ? \"bottom\" : \"top\";\n  }\n  if (absDx > EPS7) {\n    return dx > 0 ? \"right\" : \"left\";\n  }\n  return fallback;\n}\nchunk_Y2CYZVJY_name(chooseOrthogonalSide, \"chooseOrthogonalSide\");\nfunction sharedLineEndpointCoord(line, nextLine) {\n  return Math.abs(line.to - nextLine.from) < EPS7 || Math.abs(line.to - nextLine.to) < EPS7 ? line.to : line.from;\n}\nchunk_Y2CYZVJY_name(sharedLineEndpointCoord, \"sharedLineEndpointCoord\");\nfunction pointOnLine(line, along) {\n  return line.orient === \"vertical\" ? { x: line.coord, y: along } : { x: along, y: line.coord };\n}\nchunk_Y2CYZVJY_name(pointOnLine, \"pointOnLine\");\nfunction routeEdgesOrthogonal(data, direction) {\n  const nodes = data.nodes ?? [];\n  const originalEdges = data.edges ?? [];\n  const edges = [];\n  for (const oe of originalEdges) {\n    if (oe.isLayoutOnly) {\n      continue;\n    }\n    edges.push({\n      ...oe,\n      __originalEdge: oe\n    });\n  }\n  const nodeById = /* @__PURE__ */ new Map();\n  const laneByNodeId = /* @__PURE__ */ new Map();\n  const pipes = [];\n  const isLR = direction === \"LR\";\n  for (const n of nodes) {\n    nodeById.set(n.id, n);\n  }\n  const topLevelGroups = nodes.filter((n) => n.isGroup && !n.parentId);\n  for (const group of topLevelGroups) {\n    const lane = { id: group.id };\n    const assignLane = /* @__PURE__ */ chunk_Y2CYZVJY_name((n) => {\n      laneByNodeId.set(n.id, lane);\n      nodes.filter((child) => child.parentId === n.id).forEach(assignLane);\n    }, \"assignLane\");\n    assignLane(group);\n  }\n  const obstacles = nodes.filter((n) => !n.isGroup && !n.isEdgeLabel).map((n) => {\n    const w = n.width ?? 10;\n    const h = n.height ?? 10;\n    const x = n.x ?? 0;\n    const y = n.y ?? 0;\n    const padding = NODE_PADDING;\n    return {\n      nodeId: n.id,\n      minX: x - w / 2 - padding,\n      maxX: x + w / 2 + padding,\n      minY: y - h / 2 - padding,\n      maxY: y + h / 2 + padding,\n      // For LR: TB x becomes LR y, so visual Y extent should be based on height\n      visualXHalfExtent: isLR ? h / 2 + padding : w / 2 + padding\n    };\n  });\n  const getOrAddPipe = /* @__PURE__ */ chunk_Y2CYZVJY_name((orientation, coord, spanMin, spanMax) => {\n    let pipe = pipes.find((p) => p.orientation === orientation && Math.abs(p.coord - coord) < 1);\n    if (!pipe) {\n      pipe = {\n        id: `pipe-${orientation}-${coord.toFixed(0)}`,\n        orientation,\n        coord,\n        spanMin,\n        spanMax,\n        tracks: []\n      };\n      pipes.push(pipe);\n    }\n    pipe.spanMin = Math.min(pipe.spanMin, spanMin);\n    pipe.spanMax = Math.max(pipe.spanMax, spanMax);\n    return pipe;\n  }, \"getOrAddPipe\");\n  const portForSide = /* @__PURE__ */ chunk_Y2CYZVJY_name((node, side) => {\n    const w = node.width ?? 10;\n    const h = node.height ?? 10;\n    const cx = node.x ?? 0;\n    const cy = node.y ?? 0;\n    switch (side) {\n      case \"top\":\n        return { x: cx, y: cy - h / 2 };\n      case \"bottom\":\n        return { x: cx, y: cy + h / 2 };\n      case \"left\":\n        return { x: cx - w / 2, y: cy };\n      case \"right\":\n        return { x: cx + w / 2, y: cy };\n    }\n  }, \"portForSide\");\n  const getOrthogonalPort = /* @__PURE__ */ chunk_Y2CYZVJY_name((node, target, isSource) => portForSide(node, chooseOrthogonalSide(node, target, isSource ? \"bottom\" : \"top\")), \"getOrthogonalPort\");\n  const allRoutedSegments = [];\n  const edgeSegmentIndices = [];\n  const straightIntraLaneEdges = /* @__PURE__ */ new Set();\n  const CROSSING_PENALTY = 1e3;\n  const crossingPenalty = /* @__PURE__ */ chunk_Y2CYZVJY_name((edgeIdx, from, to) => {\n    if (allRoutedSegments.length === 0) {\n      return 0;\n    }\n    const isHorizontal = Math.abs(from.y - to.y) < EPS7;\n    const isVertical = Math.abs(from.x - to.x) < EPS7;\n    if (!isHorizontal && !isVertical) {\n      return 0;\n    }\n    let penalties = 0;\n    if (isHorizontal) {\n      const y = from.y;\n      const minX = Math.min(from.x, to.x) - EPS7;\n      const maxX = Math.max(from.x, to.x) + EPS7;\n      if (maxX <= minX) {\n        return 0;\n      }\n      for (const seg of allRoutedSegments) {\n        if (seg.edgeIndex === edgeIdx || seg.orientation !== \"vertical\") {\n          continue;\n        }\n        if (seg.pipe.coord < minX || seg.pipe.coord > maxX) {\n          continue;\n        }\n        if (seg.from - EPS7 <= y && seg.to + EPS7 >= y) {\n          penalties += CROSSING_PENALTY;\n        }\n      }\n    } else if (isVertical) {\n      const x = from.x;\n      const minY = Math.min(from.y, to.y) - EPS7;\n      const maxY = Math.max(from.y, to.y) + EPS7;\n      if (maxY <= minY) {\n        return 0;\n      }\n      for (const seg of allRoutedSegments) {\n        if (seg.edgeIndex === edgeIdx || seg.orientation !== \"horizontal\") {\n          continue;\n        }\n        if (seg.pipe.coord < minY || seg.pipe.coord > maxY) {\n          continue;\n        }\n        if (seg.from - EPS7 <= x && seg.to + EPS7 >= x) {\n          penalties += CROSSING_PENALTY;\n        }\n      }\n    }\n    return penalties;\n  }, \"crossingPenalty\");\n  const routingOrder = edges.map((edge, idx) => {\n    if (!edge.start || !edge.end) {\n      return { idx, crossLane: 0, dx: 0, dy: 0 };\n    }\n    const srcNode = nodeById.get(edge.start);\n    const dstNode = nodeById.get(edge.end);\n    const srcLane = laneByNodeId.get(edge.start);\n    const dstLane = laneByNodeId.get(edge.end);\n    const crossLane = srcLane && dstLane && srcLane.id !== dstLane.id ? 1 : 0;\n    const dx = srcNode && dstNode ? Math.abs((dstNode.x ?? 0) - (srcNode.x ?? 0)) : 0;\n    const dy = srcNode && dstNode ? Math.abs((dstNode.y ?? 0) - (srcNode.y ?? 0)) : 0;\n    return { idx, crossLane, dx, dy };\n  }).sort((a, b) => {\n    if (a.crossLane !== b.crossLane) {\n      return b.crossLane - a.crossLane;\n    }\n    const aDist = a.dx + a.dy;\n    const bDist = b.dx + b.dy;\n    if (Math.abs(aDist - bDist) > 1) {\n      return aDist - bDist;\n    }\n    return a.idx - b.idx;\n  }).map((entry) => entry.idx);\n  const isSegmentBlocked = /* @__PURE__ */ chunk_Y2CYZVJY_name((p1, p2, excludeStart, excludeEnd) => {\n    const segMinX = Math.min(p1.x, p2.x);\n    const segMaxX = Math.max(p1.x, p2.x);\n    const segMinY = Math.min(p1.y, p2.y);\n    const segMaxY = Math.max(p1.y, p2.y);\n    const blockingObs = obstacles.find((obs) => {\n      if (excludeStart && obs.nodeId === excludeStart) {\n        return false;\n      }\n      if (excludeEnd && obs.nodeId === excludeEnd) {\n        return false;\n      }\n      if (Math.abs(p1.x - p2.x) > EPS7) {\n        return obs.minY < p1.y && obs.maxY > p1.y && obs.maxX > segMinX && obs.minX < segMaxX;\n      } else {\n        return obs.minX < p1.x && obs.maxX > p1.x && obs.maxY > segMinY && obs.minY < segMaxY;\n      }\n    });\n    return !!blockingObs;\n  }, \"isSegmentBlocked\");\n  const portGroups = /* @__PURE__ */ new Map();\n  const incidentEdgeTotals = /* @__PURE__ */ new Map();\n  for (const edge of edges) {\n    if (!edge.start || !edge.end || edge.start === edge.end) {\n      continue;\n    }\n    incidentEdgeTotals.set(edge.start, (incidentEdgeTotals.get(edge.start) ?? 0) + 1);\n    incidentEdgeTotals.set(edge.end, (incidentEdgeTotals.get(edge.end) ?? 0) + 1);\n  }\n  const determineSide = /* @__PURE__ */ chunk_Y2CYZVJY_name((node, target) => chooseOrthogonalSide(node, target, \"bottom\"), \"determineSide\");\n  const sideInfoByIdx = /* @__PURE__ */ new Map();\n  for (const [i, e] of edges.entries()) {\n    if (!e.start || !e.end || e.start === e.end) {\n      continue;\n    }\n    if (e.points && e.points.length > 0) {\n      continue;\n    }\n    const src = nodeById.get(e.start);\n    const dst = nodeById.get(e.end);\n    if (!src || !dst) {\n      continue;\n    }\n    const dx = (dst.x ?? 0) - (src.x ?? 0);\n    const dy = (dst.y ?? 0) - (src.y ?? 0);\n    sideInfoByIdx.set(i, {\n      edgeIdx: i,\n      srcId: e.start,\n      dstId: e.end,\n      srcSide: determineSide(src, { x: dst.x ?? 0, y: dst.y ?? 0 }),\n      dstSide: determineSide(dst, { x: src.x ?? 0, y: src.y ?? 0 }),\n      absDx: Math.abs(dx),\n      absDy: Math.abs(dy),\n      dxSign: Math.sign(dx),\n      dySign: Math.sign(dy)\n    });\n  }\n  const preferenceStrength = /* @__PURE__ */ chunk_Y2CYZVJY_name((info) => {\n    if (info.srcSide === \"top\" || info.srcSide === \"bottom\") {\n      return info.absDx === 0 ? Infinity : info.absDy / info.absDx;\n    }\n    return info.absDy === 0 ? Infinity : info.absDx / info.absDy;\n  }, \"preferenceStrength\");\n  const secondarySide = /* @__PURE__ */ chunk_Y2CYZVJY_name((info) => {\n    if (info.srcSide === \"top\" || info.srcSide === \"bottom\") {\n      return info.dxSign >= 0 ? \"right\" : \"left\";\n    }\n    return info.dySign >= 0 ? \"bottom\" : \"top\";\n  }, \"secondarySide\");\n  const sourceSideGroups = /* @__PURE__ */ new Map();\n  for (const info of sideInfoByIdx.values()) {\n    const key = `${info.srcId}:${info.srcSide}`;\n    if (!sourceSideGroups.has(key)) {\n      sourceSideGroups.set(key, []);\n    }\n    sourceSideGroups.get(key).push(info);\n  }\n  const sideLoad = /* @__PURE__ */ new Map();\n  const loadKey = /* @__PURE__ */ chunk_Y2CYZVJY_name((nodeId, side) => `${nodeId}:${side}`, \"loadKey\");\n  for (const info of sideInfoByIdx.values()) {\n    sideLoad.set(\n      loadKey(info.srcId, info.srcSide),\n      (sideLoad.get(loadKey(info.srcId, info.srcSide)) ?? 0) + 1\n    );\n    sideLoad.set(\n      loadKey(info.dstId, info.dstSide),\n      (sideLoad.get(loadKey(info.dstId, info.dstSide)) ?? 0) + 1\n    );\n  }\n  for (const group of sourceSideGroups.values()) {\n    if (group.length < 2) {\n      continue;\n    }\n    group.sort((a, b) => {\n      const sa = preferenceStrength(a);\n      const sb = preferenceStrength(b);\n      if (Math.abs(sa - sb) > 1e-9) {\n        return sb - sa;\n      }\n      return a.edgeIdx - b.edgeIdx;\n    });\n    for (let g = 1; g < group.length; g++) {\n      const info = group[g];\n      const secondary = secondarySide(info);\n      const primaryLoad = sideLoad.get(loadKey(info.srcId, info.srcSide)) ?? 0;\n      const secondaryLoad = sideLoad.get(loadKey(info.srcId, secondary)) ?? 0;\n      if (secondaryLoad >= primaryLoad) {\n        continue;\n      }\n      sideLoad.set(loadKey(info.srcId, info.srcSide), primaryLoad - 1);\n      sideLoad.set(loadKey(info.srcId, secondary), secondaryLoad + 1);\n      info.srcSide = secondary;\n    }\n  }\n  const isDiamondNode = /* @__PURE__ */ chunk_Y2CYZVJY_name((node) => {\n    const shape = node?.shape;\n    return shape === \"question\" || shape === \"diamond\";\n  }, \"isDiamondNode\");\n  const inSidesByNode = /* @__PURE__ */ new Map();\n  for (const info of sideInfoByIdx.values()) {\n    if (!inSidesByNode.has(info.dstId)) {\n      inSidesByNode.set(info.dstId, /* @__PURE__ */ new Set());\n    }\n    inSidesByNode.get(info.dstId).add(info.dstSide);\n  }\n  for (const info of sideInfoByIdx.values()) {\n    if (!isDiamondNode(nodeById.get(info.srcId))) {\n      continue;\n    }\n    const inSides = inSidesByNode.get(info.srcId);\n    if (!inSides?.has(info.srcSide)) {\n      continue;\n    }\n    const secondary = secondarySide(info);\n    if (inSides.has(secondary) || (sideLoad.get(loadKey(info.srcId, secondary)) ?? 0) > 0) {\n      continue;\n    }\n    const primaryLoad = sideLoad.get(loadKey(info.srcId, info.srcSide)) ?? 0;\n    sideLoad.set(loadKey(info.srcId, info.srcSide), Math.max(0, primaryLoad - 1));\n    sideLoad.set(loadKey(info.srcId, secondary), 1);\n    info.srcSide = secondary;\n  }\n  for (const info of sideInfoByIdx.values()) {\n    const { edgeIdx: i, srcId, dstId, srcSide, dstSide } = info;\n    const src = nodeById.get(srcId);\n    const dst = nodeById.get(dstId);\n    const srcKey = `${srcId}:${srcSide}:src`;\n    const dstCoord = srcSide === \"top\" || srcSide === \"bottom\" ? dst.x ?? 0 : dst.y ?? 0;\n    if (!portGroups.has(srcKey)) {\n      portGroups.set(srcKey, []);\n    }\n    portGroups.get(srcKey).push({ edgeIdx: i, oppositeCoord: dstCoord });\n    const dstKey = `${dstId}:${dstSide}:dst`;\n    const srcCoord = dstSide === \"top\" || dstSide === \"bottom\" ? src.x ?? 0 : src.y ?? 0;\n    if (!portGroups.has(dstKey)) {\n      portGroups.set(dstKey, []);\n    }\n    portGroups.get(dstKey).push({ edgeIdx: i, oppositeCoord: srcCoord });\n  }\n  const portOffsets = /* @__PURE__ */ new Map();\n  const MIN_PORT_SPACING3 = 8;\n  for (const [key, group] of portGroups) {\n    if (group.length < 2) {\n      continue;\n    }\n    group.sort((a, b) => a.oppositeCoord - b.oppositeCoord);\n    const parts = key.split(\":\");\n    const nodeId = parts.slice(0, -2).join(\":\");\n    const side = parts[parts.length - 2];\n    const role = parts[parts.length - 1];\n    const node = nodeById.get(nodeId);\n    if (!node) {\n      continue;\n    }\n    const isVerticalSide = side === \"left\" || side === \"right\";\n    const sideLength = isVerticalSide ? node.height ?? 10 : node.width ?? 10;\n    const shape = node.shape;\n    const isDiamond = shape === \"question\" || shape === \"diamond\";\n    const effectiveLength = isDiamond ? sideLength * 0.3 : sideLength;\n    const MAX_PORT_SPACING = 20;\n    const spacing = Math.min(\n      MAX_PORT_SPACING,\n      Math.max(MIN_PORT_SPACING3, effectiveLength / (group.length + 1))\n    );\n    const totalSpan = spacing * (group.length - 1);\n    const startOffset = -totalSpan / 2;\n    for (const [j, element] of group.entries()) {\n      const offset = startOffset + j * spacing;\n      const offsetKey = `${element.edgeIdx}:${role}`;\n      portOffsets.set(offsetKey, offset);\n    }\n  }\n  const edgeHasLabelNode = /* @__PURE__ */ chunk_Y2CYZVJY_name((edgeIdx) => Boolean(edges[edgeIdx]?.labelNodeId), \"edgeHasLabelNode\");\n  const faceHasLabelNode = /* @__PURE__ */ chunk_Y2CYZVJY_name((nodeId, side) => {\n    if (!nodeId) {\n      return false;\n    }\n    return (portGroups.get(`${nodeId}:${side}:src`) ?? []).some(\n      ({ edgeIdx }) => edgeHasLabelNode(edgeIdx)\n    ) || (portGroups.get(`${nodeId}:${side}:dst`) ?? []).some(\n      ({ edgeIdx }) => edgeHasLabelNode(edgeIdx)\n    );\n  }, \"faceHasLabelNode\");\n  const applyPortOffset = /* @__PURE__ */ chunk_Y2CYZVJY_name((basePort, side, offset) => {\n    if (side === \"top\" || side === \"bottom\") {\n      return { x: basePort.x + offset, y: basePort.y };\n    } else {\n      return { x: basePort.x, y: basePort.y + offset };\n    }\n  }, \"applyPortOffset\");\n  const portsForEdge = /* @__PURE__ */ chunk_Y2CYZVJY_name((edgeIndex, src, dst) => {\n    const sideInfo = sideInfoByIdx.get(edgeIndex);\n    const srcTarget = { x: dst.x ?? 0, y: dst.y ?? 0 };\n    const dstTarget = { x: src.x ?? 0, y: src.y ?? 0 };\n    const srcSide = sideInfo?.srcSide ?? determineSide(src, srcTarget);\n    const dstSide = sideInfo?.dstSide ?? determineSide(dst, dstTarget);\n    let pSrcPort = sideInfo ? portForSide(src, sideInfo.srcSide) : getOrthogonalPort(src, srcTarget, true);\n    let pDstPort = sideInfo ? portForSide(dst, sideInfo.dstSide) : getOrthogonalPort(dst, dstTarget, false);\n    const srcOffset = portOffsets.get(`${edgeIndex}:src`);\n    const dstOffset = portOffsets.get(`${edgeIndex}:dst`);\n    if (srcOffset !== void 0) {\n      pSrcPort = applyPortOffset(pSrcPort, srcSide, srcOffset);\n    }\n    if (dstOffset !== void 0) {\n      pDstPort = applyPortOffset(pDstPort, dstSide, dstOffset);\n    }\n    return { pSrcPort, pDstPort, srcSide, dstSide };\n  }, \"portsForEdge\");\n  for (const i of routingOrder) {\n    const e = edges[i];\n    edgeSegmentIndices[i] = [];\n    if (!e.start || !e.end) {\n      continue;\n    }\n    if (e.points && e.points.length > 0) {\n      continue;\n    }\n    if (e.start === e.end) {\n      continue;\n    }\n    const src = nodeById.get(e.start);\n    const dst = nodeById.get(e.end);\n    if (!src || !dst) {\n      continue;\n    }\n    const {\n      pSrcPort,\n      pDstPort,\n      srcSide: srcPortSide,\n      dstSide: dstPortSide\n    } = portsForEdge(i, src, dst);\n    const pSrcAnchor = { ...pSrcPort };\n    const pDstAnchor = { ...pDstPort };\n    const srcPortIsVertical = srcPortSide === \"top\" || srcPortSide === \"bottom\";\n    const dstPortIsVertical = dstPortSide === \"top\" || dstPortSide === \"bottom\";\n    if (srcPortIsVertical) {\n      const isBottom = pSrcPort.y > (src.y ?? 0);\n      pSrcAnchor.y = isBottom ? pSrcPort.y + ANCHOR_OFFSET : pSrcPort.y - ANCHOR_OFFSET;\n    } else {\n      const isRight = pSrcPort.x > (src.x ?? 0);\n      pSrcAnchor.x = isRight ? pSrcPort.x + ANCHOR_OFFSET : pSrcPort.x - ANCHOR_OFFSET;\n    }\n    if (dstPortIsVertical) {\n      const isBottom = pDstPort.y > (dst.y ?? 0);\n      pDstAnchor.y = isBottom ? pDstPort.y + ANCHOR_OFFSET : pDstPort.y - ANCHOR_OFFSET;\n    } else {\n      const isRight = pDstPort.x > (dst.x ?? 0);\n      pDstAnchor.x = isRight ? pDstPort.x + ANCHOR_OFFSET : pDstPort.x - ANCHOR_OFFSET;\n    }\n    const isPointInObstacle = /* @__PURE__ */ chunk_Y2CYZVJY_name((pt, excludeNodeIds) => {\n      for (const obs of obstacles) {\n        if (excludeNodeIds.includes(obs.nodeId)) {\n          continue;\n        }\n        if (pt.x > obs.minX && pt.x < obs.maxX && pt.y > obs.minY && pt.y < obs.maxY) {\n          return { inside: true, obstacle: obs };\n        }\n      }\n      return { inside: false };\n    }, \"isPointInObstacle\");\n    const obstacleDetour = /* @__PURE__ */ chunk_Y2CYZVJY_name((port, node, opposite, obs, portIsVertical) => {\n      if (portIsVertical) {\n        const leavesPositiveSide2 = port.y > (node.y ?? 0);\n        const goRight = (opposite.x ?? 0) >= port.x;\n        return {\n          x: goRight ? obs.maxX + HORIZONTAL_PIPE_MARGIN : obs.minX - HORIZONTAL_PIPE_MARGIN,\n          y: leavesPositiveSide2 ? obs.maxY + VERTICAL_PIPE_MARGIN : obs.minY - VERTICAL_PIPE_MARGIN,\n          leavesPositiveSide: leavesPositiveSide2\n        };\n      }\n      const leavesPositiveSide = port.x > (node.x ?? 0);\n      const goDown = (opposite.y ?? 0) >= port.y;\n      return {\n        x: leavesPositiveSide ? obs.maxX + HORIZONTAL_PIPE_MARGIN : obs.minX - HORIZONTAL_PIPE_MARGIN,\n        y: goDown ? obs.maxY + VERTICAL_PIPE_MARGIN : obs.minY - VERTICAL_PIPE_MARGIN,\n        leavesPositiveSide\n      };\n    }, \"obstacleDetour\");\n    let srcHandleWaypoints = [];\n    const endpointIds = [e.start, e.end];\n    const srcCheck = isPointInObstacle(pSrcAnchor, endpointIds);\n    if (srcCheck.inside && srcCheck.obstacle) {\n      const obs = srcCheck.obstacle;\n      if (srcPortIsVertical) {\n        const detour = obstacleDetour(pSrcPort, src, dst, obs, true);\n        pSrcAnchor.x = detour.x;\n        pSrcAnchor.y = detour.y;\n        const gapY = detour.leavesPositiveSide ? Math.min(obs.minY - 2, pSrcPort.y + ANCHOR_OFFSET) : Math.max(obs.maxY + 2, pSrcPort.y - ANCHOR_OFFSET);\n        srcHandleWaypoints = [\n          { x: pSrcPort.x, y: gapY },\n          // Orthogonal step in the gap\n          { x: detour.x, y: gapY },\n          // Horizontal detour\n          { x: detour.x, y: detour.y }\n          // Down past obstacle\n        ];\n      } else {\n        const detour = obstacleDetour(pSrcPort, src, dst, obs, false);\n        const gapX = detour.leavesPositiveSide ? Math.min(obs.minX - 2, pSrcPort.x + ANCHOR_OFFSET) : Math.max(obs.maxX + 2, pSrcPort.x - ANCHOR_OFFSET);\n        pSrcAnchor.x = detour.x;\n        pSrcAnchor.y = detour.y;\n        srcHandleWaypoints = [\n          { x: gapX, y: pSrcPort.y },\n          // Orthogonal step in the gap\n          { x: gapX, y: detour.y },\n          // Vertical detour\n          { x: detour.x, y: detour.y }\n          // Horizontal past obstacle\n        ];\n      }\n    }\n    let dstHandleWaypoints = [];\n    const dstCheck = isPointInObstacle(pDstAnchor, endpointIds);\n    if (dstCheck.inside && dstCheck.obstacle) {\n      const obs = dstCheck.obstacle;\n      if (dstPortIsVertical) {\n        const detour = obstacleDetour(pDstPort, dst, src, obs, true);\n        pDstAnchor.x = detour.x;\n        pDstAnchor.y = detour.y;\n        dstHandleWaypoints = [\n          { x: detour.x, y: detour.y },\n          // From anchor position\n          { x: pDstPort.x, y: detour.y }\n          // Go sideways to port's X\n          // Then orthogonally to port\n        ];\n      } else {\n        const detour = obstacleDetour(pDstPort, dst, src, obs, false);\n        pDstAnchor.x = detour.x;\n        pDstAnchor.y = detour.y;\n        dstHandleWaypoints = [\n          { x: detour.x, y: detour.y },\n          // From anchor position\n          { x: detour.x, y: pDstPort.y }\n          // Go vertically to port's Y\n        ];\n      }\n    }\n    if (srcHandleWaypoints.length === 0 && dstHandleWaypoints.length === 0) {\n      const hpMargin = HORIZONTAL_PIPE_MARGIN;\n      const anchorsSameX = Math.abs(pSrcAnchor.x - pDstAnchor.x) < hpMargin;\n      const anchorsSameY = Math.abs(pSrcAnchor.y - pDstAnchor.y) < hpMargin;\n      const hasPortOffset = portOffsets.get(`${i}:src`) !== void 0 || portOffsets.get(`${i}:dst`) !== void 0;\n      const srcFaceTotal = (portGroups.get(`${e.start ?? \"\"}:${srcPortSide}:src`)?.length ?? 0) + (portGroups.get(`${e.start ?? \"\"}:${srcPortSide}:dst`)?.length ?? 0);\n      const dstFaceTotal = (portGroups.get(`${e.end ?? \"\"}:${dstPortSide}:src`)?.length ?? 0) + (portGroups.get(`${e.end ?? \"\"}:${dstPortSide}:dst`)?.length ?? 0);\n      const faceContested = srcFaceTotal > 1 || dstFaceTotal > 1;\n      const srcIncidentTotal = incidentEdgeTotals.get(e.start ?? \"\") ?? 0;\n      const dstIncidentTotal = incidentEdgeTotals.get(e.end ?? \"\") ?? 0;\n      const contestedFaceHasLabel = srcFaceTotal > 1 && faceHasLabelNode(e.start, srcPortSide) || dstFaceTotal > 1 && faceHasLabelNode(e.end, dstPortSide);\n      const srcContestAllowsCenteredStraight = srcFaceTotal <= 1 || srcIncidentTotal <= 2;\n      const dstContestAllowsCenteredStraight = dstFaceTotal <= 1 || dstIncidentTotal <= 2;\n      const canPreserveSimpleContestedStraight = faceContested && !contestedFaceHasLabel && srcContestAllowsCenteredStraight && dstContestAllowsCenteredStraight;\n      if ((anchorsSameX || anchorsSameY) && !hasPortOffset && (!faceContested || canPreserveSimpleContestedStraight)) {\n        const directBlocked = isSegmentBlocked(pSrcPort, pDstPort, e.start, e.end);\n        if (!directBlocked) {\n          e.points = [{ ...pSrcPort }, { ...pSrcAnchor }, { ...pDstAnchor }, { ...pDstPort }];\n          straightIntraLaneEdges.add(i);\n          const fastPathOrientation = anchorsSameY ? \"horizontal\" : \"vertical\";\n          const fastPathCoord = anchorsSameY ? pSrcPort.y : pSrcPort.x;\n          const fastPathFrom = anchorsSameY ? Math.min(pSrcPort.x, pDstPort.x) : Math.min(pSrcPort.y, pDstPort.y);\n          const fastPathTo = anchorsSameY ? Math.max(pSrcPort.x, pDstPort.x) : Math.max(pSrcPort.y, pDstPort.y);\n          const fastPathPipe = {\n            id: `fast-path-${fastPathOrientation}-${fastPathCoord.toFixed(0)}-${i}`,\n            orientation: fastPathOrientation,\n            coord: fastPathCoord,\n            spanMin: fastPathFrom,\n            spanMax: fastPathTo,\n            tracks: []\n          };\n          allRoutedSegments.push({\n            edgeIndex: i,\n            segmentIndex: 0,\n            orientation: fastPathOrientation,\n            pipe: fastPathPipe,\n            trackIndex: 0,\n            from: fastPathFrom,\n            to: fastPathTo\n          });\n          continue;\n        }\n      }\n    }\n    const srcPipe = getOrAddPipe(\"vertical\", pSrcAnchor.x, pSrcAnchor.y, pSrcAnchor.y);\n    pSrcAnchor.x = srcPipe.coord;\n    const dstPipe = getOrAddPipe(\"vertical\", pDstAnchor.x, pDstAnchor.y, pDstAnchor.y);\n    pDstAnchor.x = dstPipe.coord;\n    let bbMinX = Math.min(pSrcAnchor.x, pDstAnchor.x) - 50;\n    let bbMaxX = Math.max(pSrcAnchor.x, pDstAnchor.x) + 50;\n    let bbMinY = Math.min(pSrcAnchor.y, pDstAnchor.y) - 50;\n    let bbMaxY = Math.max(pSrcAnchor.y, pDstAnchor.y) + 50;\n    for (const obs of obstacles) {\n      const pathMinX = Math.min(pSrcAnchor.x, pDstAnchor.x);\n      const pathMaxX = Math.max(pSrcAnchor.x, pDstAnchor.x);\n      const pathMinY = Math.min(pSrcAnchor.y, pDstAnchor.y);\n      const pathMaxY = Math.max(pSrcAnchor.y, pDstAnchor.y);\n      const obsBlocksPath = obs.minX < pathMaxX && obs.maxX > pathMinX && obs.minY < pathMaxY && obs.maxY > pathMinY;\n      if (obsBlocksPath) {\n        bbMinX = Math.min(bbMinX, obs.minX - ROUTING_MARGIN);\n        bbMaxX = Math.max(bbMaxX, obs.maxX + ROUTING_MARGIN);\n        bbMinY = Math.min(bbMinY, obs.minY - ROUTING_MARGIN);\n        bbMaxY = Math.max(bbMaxY, obs.maxY + ROUTING_MARGIN);\n      }\n    }\n    for (const obs of obstacles) {\n      if (obs.maxX < bbMinX || obs.minX > bbMaxX || obs.maxY < bbMinY || obs.minY > bbMaxY) {\n        continue;\n      }\n      const hMargin = HORIZONTAL_PIPE_MARGIN;\n      getOrAddPipe(\"horizontal\", obs.minY - hMargin, bbMinX, bbMaxX);\n      getOrAddPipe(\"horizontal\", obs.maxY + hMargin, bbMinX, bbMaxX);\n      const vMargin = VERTICAL_PIPE_MARGIN;\n      getOrAddPipe(\"vertical\", obs.minX - vMargin, bbMinY, bbMaxY);\n      getOrAddPipe(\"vertical\", obs.maxX + vMargin, bbMinY, bbMaxY);\n    }\n    getOrAddPipe(\"horizontal\", pSrcAnchor.y, bbMinX, bbMaxX);\n    getOrAddPipe(\"horizontal\", pDstAnchor.y, bbMinX, bbMaxX);\n    const hPipes = pipes.filter(\n      (p) => p.orientation === \"horizontal\" && p.coord >= bbMinY && p.coord <= bbMaxY\n    );\n    const vPipes = pipes.filter(\n      (p) => p.orientation === \"vertical\" && p.coord >= bbMinX && p.coord <= bbMaxX\n    );\n    const getKey = /* @__PURE__ */ chunk_Y2CYZVJY_name((x, y) => `${x.toFixed(1)},${y.toFixed(1)}`, \"getKey\");\n    const startKey = getKey(pSrcAnchor.x, pSrcAnchor.y);\n    const endKey = getKey(pDstAnchor.x, pDstAnchor.y);\n    const gScore = /* @__PURE__ */ new Map();\n    const cameFrom = /* @__PURE__ */ new Map();\n    const arrivalDir = /* @__PURE__ */ new Map();\n    const openSet = /* @__PURE__ */ new Set();\n    const openList = [];\n    gScore.set(startKey, 0);\n    arrivalDir.set(startKey, \"n\");\n    openList.push({\n      key: startKey,\n      f: Math.hypot(pDstAnchor.x - pSrcAnchor.x, pDstAnchor.y - pSrcAnchor.y),\n      pt: pSrcAnchor\n    });\n    openSet.add(startKey);\n    let foundPath = [];\n    const checkSegmentBlocked = /* @__PURE__ */ chunk_Y2CYZVJY_name((p1, p2) => {\n      return isSegmentBlocked(p1, p2, e.start, e.end);\n    }, \"checkSegmentBlocked\");\n    const cornerHV = { x: pDstAnchor.x, y: pSrcAnchor.y };\n    const seg1HV_blocked = checkSegmentBlocked(pSrcAnchor, cornerHV);\n    const seg2HV_blocked = checkSegmentBlocked(cornerHV, pDstAnchor);\n    const pathHV_blocked = seg1HV_blocked || seg2HV_blocked;\n    const cornerVH = { x: pSrcAnchor.x, y: pDstAnchor.y };\n    const seg1VH_blocked = checkSegmentBlocked(pSrcAnchor, cornerVH);\n    const seg2VH_blocked = checkSegmentBlocked(cornerVH, pDstAnchor);\n    const pathVH_blocked = seg1VH_blocked || seg2VH_blocked;\n    if (!pathHV_blocked) {\n      if (Math.abs(pSrcAnchor.y - pDstAnchor.y) < EPS7 || Math.abs(pSrcAnchor.x - pDstAnchor.x) < EPS7) {\n        foundPath = [pSrcAnchor, pDstAnchor];\n      } else {\n        foundPath = [pSrcAnchor, cornerHV, pDstAnchor];\n      }\n    } else if (!pathVH_blocked) {\n      if (Math.abs(pSrcAnchor.x - pDstAnchor.x) < EPS7) {\n        foundPath = [pSrcAnchor, pDstAnchor];\n      } else {\n        foundPath = [pSrcAnchor, cornerVH, pDstAnchor];\n      }\n    }\n    if (foundPath.length === 0) {\n      while (openList.length > 0) {\n        openList.sort((a, b) => a.f - b.f);\n        const current = openList.shift();\n        openSet.delete(current.key);\n        if (current.key === endKey) {\n          let currKey = endKey;\n          let currPt = pDstAnchor;\n          foundPath = [currPt];\n          while (cameFrom.has(currKey)) {\n            const prev = cameFrom.get(currKey);\n            foundPath.unshift(prev);\n            currPt = prev;\n            currKey = getKey(prev.x, prev.y);\n          }\n          break;\n        }\n        const cx = current.pt.x;\n        const cy = current.pt.y;\n        const sortedVPipes = vPipes.sort((a, b) => a.coord - b.coord);\n        const vIdx = sortedVPipes.findIndex((p) => Math.abs(p.coord - cx) < 1);\n        const hPipesSorted = hPipes.sort((a, b) => a.coord - b.coord);\n        const hIdx = hPipesSorted.findIndex((p) => Math.abs(p.coord - cy) < 1);\n        const neighbors = [];\n        if (vIdx > 0) {\n          neighbors.push({ x: sortedVPipes[vIdx - 1].coord, y: cy });\n        }\n        if (vIdx >= 0 && vIdx < sortedVPipes.length - 1) {\n          neighbors.push({ x: sortedVPipes[vIdx + 1].coord, y: cy });\n        }\n        if (hIdx > 0) {\n          neighbors.push({ x: cx, y: hPipesSorted[hIdx - 1].coord });\n        }\n        if (hIdx >= 0 && hIdx < hPipesSorted.length - 1) {\n          neighbors.push({ x: cx, y: hPipesSorted[hIdx + 1].coord });\n        }\n        for (const neighbor of neighbors) {\n          const minX = Math.min(cx, neighbor.x);\n          const maxX = Math.max(cx, neighbor.x);\n          const minY = Math.min(cy, neighbor.y);\n          const maxY = Math.max(cy, neighbor.y);\n          const blocked = obstacles.some((obs) => {\n            if (obs.nodeId === e.start || obs.nodeId === e.end) {\n              return false;\n            }\n            if (minX !== maxX) {\n              return obs.minY < cy && obs.maxY > cy && obs.maxX > minX && obs.minX < maxX;\n            } else {\n              return obs.minX < cx && obs.maxX > cx && obs.maxY > minY && obs.minY < maxY;\n            }\n          });\n          if (blocked) {\n            continue;\n          }\n          const nKey = getKey(neighbor.x, neighbor.y);\n          const dist = Math.abs(neighbor.x - cx) + Math.abs(neighbor.y - cy);\n          const penalty = crossingPenalty(i, current.pt, neighbor);\n          let dirPenalty = 0;\n          const destDx = pDstAnchor.x - pSrcAnchor.x;\n          const destDy = pDstAnchor.y - pSrcAnchor.y;\n          const moveDx = neighbor.x - cx;\n          const moveDy = neighbor.y - cy;\n          if (destDy > 10 && moveDy < -5 || destDy < -10 && moveDy > 5) {\n            dirPenalty = Math.abs(moveDy) * 100;\n          }\n          if (destDx > 10 && moveDx < -5 || destDx < -10 && moveDx > 5) {\n            dirPenalty += Math.abs(moveDx) * 50;\n          }\n          let bendPenalty = 0;\n          const currentDir = arrivalDir.get(current.key) ?? \"n\";\n          const moveDir = Math.abs(moveDx) > EPS7 ? \"h\" : \"v\";\n          if (currentDir !== \"n\" && currentDir !== moveDir) {\n            bendPenalty = 50;\n          }\n          const stepCost = dist + penalty + dirPenalty + bendPenalty;\n          const tentativeG = (gScore.get(current.key) ?? Infinity) + stepCost;\n          const h = Math.abs(pDstAnchor.x - neighbor.x) + Math.abs(pDstAnchor.y - neighbor.y);\n          if (tentativeG < (gScore.get(nKey) ?? Infinity)) {\n            cameFrom.set(nKey, current.pt);\n            gScore.set(nKey, tentativeG);\n            arrivalDir.set(nKey, moveDir);\n            if (!openSet.has(nKey)) {\n              openList.push({ key: nKey, f: tentativeG + h, pt: neighbor });\n              openSet.add(nKey);\n            } else {\n              const idx = openList.findIndex((x) => x.key === nKey);\n              if (idx !== -1) {\n                openList[idx].f = tentativeG + h;\n              }\n            }\n          }\n        }\n      }\n    }\n    if (foundPath.length === 0) {\n      foundPath = [pSrcAnchor, { x: pSrcAnchor.x, y: pDstAnchor.y }, pDstAnchor];\n    }\n    if (foundPath.length > 4) {\n      const start = foundPath[0];\n      const end = foundPath[foundPath.length - 1];\n      let minX = Math.min(start.x, end.x);\n      let maxX = Math.max(start.x, end.x);\n      let minY = Math.min(start.y, end.y);\n      let maxY = Math.max(start.y, end.y);\n      for (const pt of foundPath) {\n        minX = Math.min(minX, pt.x);\n        maxX = Math.max(maxX, pt.x);\n        minY = Math.min(minY, pt.y);\n        maxY = Math.max(maxY, pt.y);\n      }\n      const wentRight = maxX > Math.max(start.x, end.x);\n      const wentLeft = minX < Math.min(start.x, end.x);\n      if (isLR) {\n        const margin = VERTICAL_PIPE_MARGIN;\n        if (wentRight) {\n          const pathX = Math.max(start.x, end.x);\n          const pathMinY = Math.min(start.y, end.y);\n          const pathMaxY = Math.max(start.y, end.y);\n          const detourObstacles = obstacles.filter(\n            (obs) => obs.minX < pathX && obs.maxX > pathX && // obstacle's x-range contains the path x\n            obs.minY < pathMaxY && obs.maxY > pathMinY\n            // obstacle's y-range overlaps with path y-range\n          );\n          if (detourObstacles.length > 0) {\n            let visualMaxX = Math.max(start.x, end.x);\n            for (const obs of detourObstacles) {\n              const obsCenterX = (obs.minX + obs.maxX) / 2;\n              if (obs.visualXHalfExtent === void 0 || isNaN(obs.visualXHalfExtent)) {\n                continue;\n              }\n              const visualRight = obsCenterX + obs.visualXHalfExtent + margin;\n              visualMaxX = Math.max(visualMaxX, visualRight);\n            }\n            if (!isNaN(visualMaxX)) {\n              maxX = visualMaxX;\n            }\n          }\n        }\n        if (wentLeft) {\n          const detourObstacles = obstacles.filter(\n            (obs) => obs.minX < Math.min(start.x, end.x) + margin && // obstacle extends past the direct path\n            obs.minY < Math.max(start.y, end.y) && obs.maxY > Math.min(start.y, end.y)\n            // obstacle is in Y range\n          );\n          if (detourObstacles.length > 0) {\n            let visualMinX = Math.min(start.x, end.x);\n            for (const obs of detourObstacles) {\n              const obsCenterX = (obs.minX + obs.maxX) / 2;\n              const visualLeft = obsCenterX - obs.visualXHalfExtent - margin;\n              visualMinX = Math.min(visualMinX, visualLeft);\n            }\n            minX = visualMinX;\n          }\n        }\n      }\n      const findBestReturnY = /* @__PURE__ */ chunk_Y2CYZVJY_name((detourX) => {\n        const goingDown = end.y > start.y;\n        const relevantObs = obstacles.filter((obs) => {\n          const obsInXRange = Math.min(start.x, end.x) < obs.maxX && Math.max(start.x, end.x) > obs.minX;\n          const obsInYRange = Math.min(start.y, end.y) < obs.maxY && Math.max(start.y, end.y) > obs.minY;\n          return obsInXRange && obsInYRange;\n        });\n        let filteredObs = relevantObs;\n        if (isLR && relevantObs.length > 0) {\n          const obsAtDetourX = relevantObs.filter(\n            (obs) => obs.minX < detourX && obs.maxX > detourX\n          );\n          if (obsAtDetourX.length > 0) {\n            filteredObs = obsAtDetourX;\n          }\n        }\n        if (filteredObs.length === 0) {\n          return end.y;\n        }\n        const margin = HORIZONTAL_PIPE_MARGIN;\n        if (goingDown) {\n          const lowestObsBottom = Math.max(...filteredObs.map((obs) => obs.maxY));\n          const bestY = lowestObsBottom + margin;\n          if (bestY < end.y - EPS7) {\n            return bestY;\n          }\n        } else {\n          const highestObsTop = Math.min(...filteredObs.map((obs) => obs.minY));\n          const bestY = highestObsTop - margin;\n          if (bestY > end.y + EPS7) {\n            return bestY;\n          }\n        }\n        return end.y;\n      }, \"findBestReturnY\");\n      const trySimplifyWithDetourX = /* @__PURE__ */ chunk_Y2CYZVJY_name((detourX) => {\n        const bestY = findBestReturnY(detourX);\n        const corner1 = { x: detourX, y: start.y };\n        const corner2 = { x: detourX, y: bestY };\n        const corner3 = { x: end.x, y: bestY };\n        const seg1Blocked = checkSegmentBlocked(start, corner1);\n        const seg2Blocked = checkSegmentBlocked(corner1, corner2);\n        const seg3Blocked = checkSegmentBlocked(corner2, corner3);\n        const seg4Blocked = bestY !== end.y ? checkSegmentBlocked(corner3, end) : false;\n        if (!seg1Blocked && !seg2Blocked && !seg3Blocked && !seg4Blocked) {\n          if (Math.abs(bestY - end.y) < EPS7) {\n            return [start, corner1, corner2, end];\n          }\n          return [start, corner1, corner2, corner3, end];\n        }\n        return null;\n      }, \"trySimplifyWithDetourX\");\n      const simplified2 = wentRight && !wentLeft ? trySimplifyWithDetourX(maxX) : wentLeft && !wentRight ? trySimplifyWithDetourX(minX) : null;\n      if (simplified2) {\n        foundPath = simplified2;\n      }\n    }\n    const fullPoints = [\n      pSrcPort,\n      ...srcHandleWaypoints,\n      ...foundPath,\n      ...dstHandleWaypoints.reverse(),\n      // Reverse because they're stored anchor->waypoint->port\n      pDstPort\n    ];\n    if (fullPoints.length >= 3) {\n      const C = fullPoints[fullPoints.length - 1];\n      const B = fullPoints[fullPoints.length - 2];\n      const A = fullPoints[fullPoints.length - 3];\n      const isHoriz = Math.abs(A.y - B.y) < EPS7 && Math.abs(B.y - C.y) < EPS7;\n      const isVert = Math.abs(A.x - B.x) < EPS7 && Math.abs(B.x - C.x) < EPS7;\n      if (isHoriz) {\n        const signAB = Math.sign(B.x - A.x);\n        const signAC = Math.sign(C.x - A.x);\n        if (signAB !== 0 && signAB === signAC && Math.abs(B.x - A.x) > Math.abs(C.x - A.x)) {\n          fullPoints.splice(-2, 1);\n        }\n      } else if (isVert) {\n        const signAB = Math.sign(B.y - A.y);\n        const signAC = Math.sign(C.y - A.y);\n        if (signAB !== 0 && signAB === signAC && Math.abs(B.y - A.y) > Math.abs(C.y - A.y)) {\n          fullPoints.splice(-2, 1);\n        }\n      }\n    }\n    const simplified = [fullPoints[0]];\n    for (let k = 1; k < fullPoints.length - 1; k++) {\n      if (k === 1) {\n        simplified.push(fullPoints[k]);\n        continue;\n      }\n      const prev = simplified[simplified.length - 1];\n      const curr = fullPoints[k];\n      const next = fullPoints[k + 1];\n      if (Math.abs(prev.y - curr.y) < EPS7 && Math.abs(curr.y - next.y) < EPS7) {\n        const dir1 = curr.x > prev.x;\n        const dir2 = next.x > curr.x;\n        if (dir1 !== dir2) {\n          simplified.push(curr);\n          continue;\n        }\n        continue;\n      }\n      if (Math.abs(prev.x - curr.x) < EPS7 && Math.abs(curr.x - next.x) < EPS7) {\n        const dir1 = curr.y > prev.y;\n        const dir2 = next.y > curr.y;\n        if (dir1 !== dir2) {\n          simplified.push(curr);\n          continue;\n        }\n        continue;\n      }\n      simplified.push(curr);\n    }\n    simplified.push(fullPoints[fullPoints.length - 1]);\n    for (let k = 0; k < simplified.length - 1; k++) {\n      const p1 = simplified[k];\n      const p2 = simplified[k + 1];\n      const orientation = Math.abs(p1.x - p2.x) < EPS7 ? \"vertical\" : \"horizontal\";\n      const coord = orientation === \"vertical\" ? p1.x : p1.y;\n      const from = orientation === \"vertical\" ? Math.min(p1.y, p2.y) : Math.min(p1.x, p2.x);\n      const to = orientation === \"vertical\" ? Math.max(p1.y, p2.y) : Math.max(p1.x, p2.x);\n      const pipe = getOrAddPipe(orientation, coord, from, to);\n      const rSeg = {\n        edgeIndex: i,\n        segmentIndex: k,\n        orientation,\n        pipe,\n        trackIndex: 0,\n        // Initial track\n        from,\n        to\n      };\n      allRoutedSegments.push(rSeg);\n      edgeSegmentIndices[i].push(allRoutedSegments.length - 1);\n      if (!pipe.tracks[0]) {\n        pipe.tracks[0] = { index: 0, coord: pipe.coord, segments: [] };\n      }\n      pipe.tracks[0].segments.push({\n        edgeIndex: i,\n        segmentIndex: k,\n        from,\n        to\n      });\n    }\n  }\n  const segmentsOverlap = /* @__PURE__ */ chunk_Y2CYZVJY_name((s1, s2) => {\n    return s1.from < s2.to && s2.from < s1.to;\n  }, \"segmentsOverlap\");\n  const trySwapSegmentsAcrossTracks = /* @__PURE__ */ chunk_Y2CYZVJY_name((s1, s2, t1, t2) => {\n    const canS1GoT2 = !t2.segments.some(\n      (r) => (r.edgeIndex !== s2.edgeIndex || r.segmentIndex !== s2.segmentIndex) && segmentsOverlap(r, s1)\n    );\n    const canS2GoT1 = !t1.segments.some(\n      (r) => (r.edgeIndex !== s1.edgeIndex || r.segmentIndex !== s1.segmentIndex) && segmentsOverlap(r, s2)\n    );\n    if (canS1GoT2 && canS2GoT1) {\n      s1.trackIndex = t2.index;\n      s2.trackIndex = t1.index;\n      t1.segments = [\n        ...t1.segments.filter(\n          (r) => r.edgeIndex !== s1.edgeIndex || r.segmentIndex !== s1.segmentIndex\n        ),\n        {\n          edgeIndex: s2.edgeIndex,\n          segmentIndex: s2.segmentIndex,\n          from: s2.from,\n          to: s2.to\n        }\n      ];\n      t2.segments = [\n        ...t2.segments.filter(\n          (r) => r.edgeIndex !== s2.edgeIndex || r.segmentIndex !== s2.segmentIndex\n        ),\n        {\n          edgeIndex: s1.edgeIndex,\n          segmentIndex: s1.segmentIndex,\n          from: s1.from,\n          to: s1.to\n        }\n      ];\n      return true;\n    }\n    return false;\n  }, \"trySwapSegmentsAcrossTracks\");\n  const createNewTrack = /* @__PURE__ */ chunk_Y2CYZVJY_name((pipe) => {\n    const idx = pipe.tracks.length;\n    pipe.tracks[idx] = { index: idx, coord: pipe.coord, segments: [] };\n    return idx;\n  }, \"createNewTrack\");\n  const moveSegmentToTrack = /* @__PURE__ */ chunk_Y2CYZVJY_name((seg, trackIdx) => {\n    const oldTrack = seg.pipe.tracks[seg.trackIndex];\n    oldTrack.segments = oldTrack.segments.filter(\n      (r) => r.edgeIndex !== seg.edgeIndex || r.segmentIndex !== seg.segmentIndex\n    );\n    seg.trackIndex = trackIdx;\n    const newTrack = seg.pipe.tracks[trackIdx];\n    newTrack.segments.push({\n      edgeIndex: seg.edgeIndex,\n      segmentIndex: seg.segmentIndex,\n      from: seg.from,\n      to: seg.to\n    });\n  }, \"moveSegmentToTrack\");\n  const moveSegmentChainToTrack = /* @__PURE__ */ chunk_Y2CYZVJY_name((seg, trackIdx) => {\n    const indices = edgeSegmentIndices[seg.edgeIndex];\n    for (const idx of indices) {\n      const s = allRoutedSegments[idx];\n      if (s.pipe === seg.pipe) {\n        moveSegmentToTrack(s, trackIdx);\n      }\n    }\n  }, \"moveSegmentChainToTrack\");\n  const getAdjacentSegmentsAlongEdge = /* @__PURE__ */ chunk_Y2CYZVJY_name((seg) => {\n    const indices = edgeSegmentIndices[seg.edgeIndex];\n    const idxInList = indices.indexOf(allRoutedSegments.indexOf(seg));\n    const adj = [];\n    if (idxInList > 0) {\n      adj.push(allRoutedSegments[indices[idxInList - 1]]);\n    }\n    if (idxInList < indices.length - 1) {\n      adj.push(allRoutedSegments[indices[idxInList + 1]]);\n    }\n    return adj;\n  }, \"getAdjacentSegmentsAlongEdge\");\n  const haveAnyCrossing = /* @__PURE__ */ chunk_Y2CYZVJY_name((segA, segB) => {\n    if (segA.orientation === segB.orientation) {\n      return false;\n    }\n    const h = segA.orientation === \"horizontal\" ? segA : segB;\n    const v = segA.orientation === \"horizontal\" ? segB : segA;\n    return v.pipe.coord > h.from && v.pipe.coord < h.to && h.pipe.coord > v.from && h.pipe.coord < v.to;\n  }, \"haveAnyCrossing\");\n  const findAvailableTrack = /* @__PURE__ */ chunk_Y2CYZVJY_name((pipe, seg) => {\n    for (const track of pipe.tracks) {\n      const overlap = track.segments.some(\n        (r) => (r.edgeIndex !== seg.edgeIndex || r.segmentIndex !== seg.segmentIndex) && segmentsOverlap(r, seg)\n      );\n      if (!overlap) {\n        return track.index;\n      }\n    }\n    return -1;\n  }, \"findAvailableTrack\");\n  const segmentsConflict = /* @__PURE__ */ chunk_Y2CYZVJY_name((s1, s2) => {\n    if (s1.trackIndex === s2.trackIndex) {\n      return segmentsOverlap(s1, s2);\n    }\n    const adj1 = getAdjacentSegmentsAlongEdge(s1);\n    const adj2 = getAdjacentSegmentsAlongEdge(s2);\n    return adj1.some((a1) => adj2.some((a2) => haveAnyCrossing(a1, a2)));\n  }, \"segmentsConflict\");\n  const resolveTrackConflict = /* @__PURE__ */ chunk_Y2CYZVJY_name((s1, s2, move) => {\n    if (trySwapSegmentsAcrossTracks(\n      s1,\n      s2,\n      s1.pipe.tracks[s1.trackIndex],\n      s2.pipe.tracks[s2.trackIndex]\n    )) {\n      return;\n    }\n    const avail = findAvailableTrack(s1.pipe, s2);\n    move(s2, avail !== -1 ? avail : createNewTrack(s1.pipe));\n  }, \"resolveTrackConflict\");\n  const resolveHandleConflicts = /* @__PURE__ */ chunk_Y2CYZVJY_name((handles) => {\n    let crossings = 0;\n    for (let i = 0; i < handles.length; i++) {\n      for (let j = i + 1; j < handles.length; j++) {\n        const h1 = handles[i];\n        const h2 = handles[j];\n        if (h1.pipe !== h2.pipe) {\n          continue;\n        }\n        if (segmentsConflict(h1, h2)) {\n          crossings++;\n          resolveTrackConflict(h1, h2, moveSegmentChainToTrack);\n        }\n      }\n    }\n    return crossings;\n  }, \"resolveHandleConflicts\");\n  const destInfoCache = /* @__PURE__ */ new Map();\n  const getDestInfo = /* @__PURE__ */ chunk_Y2CYZVJY_name((edgeIdx) => {\n    if (destInfoCache.has(edgeIdx)) {\n      return destInfoCache.get(edgeIdx);\n    }\n    const indices = edgeSegmentIndices[edgeIdx];\n    if (indices.length === 0) {\n      const info2 = { dest: 0, deviation: 0, base: 0, delta: 0 };\n      destInfoCache.set(edgeIdx, info2);\n      return info2;\n    }\n    const firstSeg = allRoutedSegments[indices[0]];\n    const base = firstSeg.pipe.coord;\n    let dest = base;\n    for (let idx = 1; idx < indices.length; idx++) {\n      const seg = allRoutedSegments[indices[idx]];\n      if (seg.orientation === \"horizontal\") {\n        const candidateA = seg.from;\n        const candidateB = seg.to;\n        dest = Math.abs(candidateA - base) > Math.abs(candidateB - base) ? candidateA : candidateB;\n        break;\n      }\n    }\n    const deviation = Math.abs(dest - base);\n    const info = { dest, deviation, base, delta: dest - base };\n    destInfoCache.set(edgeIdx, info);\n    return info;\n  }, \"getDestInfo\");\n  const fixSourceHandleCrossings = /* @__PURE__ */ chunk_Y2CYZVJY_name(() => {\n    let crossings = 0;\n    const edgesBySource = /* @__PURE__ */ new Map();\n    for (const [i, e] of edges.entries()) {\n      if (edgeSegmentIndices[i].length === 0) {\n        continue;\n      }\n      if (!e.start) {\n        continue;\n      }\n      if (!edgesBySource.has(e.start)) {\n        edgesBySource.set(e.start, []);\n      }\n      edgesBySource.get(e.start).push(i);\n    }\n    const getEdgeDistance = /* @__PURE__ */ chunk_Y2CYZVJY_name((edgeIdx) => {\n      const edge = edges[edgeIdx];\n      if (!edge.start || !edge.end) {\n        return 0;\n      }\n      const srcNode = nodeById.get(edge.start);\n      const dstNode = nodeById.get(edge.end);\n      if (!srcNode || !dstNode) {\n        return 0;\n      }\n      const dx = (dstNode.x ?? 0) - (srcNode.x ?? 0);\n      const dy = (dstNode.y ?? 0) - (srcNode.y ?? 0);\n      return Math.abs(dx) + Math.abs(dy);\n    }, \"getEdgeDistance\");\n    for (const grp of edgesBySource.values()) {\n      grp.sort((a, b) => {\n        const infoA = getDestInfo(a);\n        const infoB = getDestInfo(b);\n        if (Math.abs(infoA.deviation - infoB.deviation) > 1) {\n          return infoA.deviation - infoB.deviation;\n        }\n        if (Math.abs(infoA.dest - infoB.dest) > 1) {\n          return infoA.dest - infoB.dest;\n        }\n        const distA = getEdgeDistance(a);\n        const distB = getEdgeDistance(b);\n        if (Math.abs(distA - distB) > 1) {\n          return distB - distA;\n        }\n        const lenA = edgeSegmentIndices[a].length;\n        const lenB = edgeSegmentIndices[b].length;\n        if (lenA !== lenB) {\n          return lenA - lenB;\n        }\n        if (lenA === 1) {\n          const idxA = edgeSegmentIndices[a][0];\n          const idxB = edgeSegmentIndices[b][0];\n          if (allRoutedSegments[idxA] && allRoutedSegments[idxB]) {\n            const segA = allRoutedSegments[idxA];\n            const segB = allRoutedSegments[idxB];\n            const distA2 = Math.abs(segA.to - segA.from);\n            const distB2 = Math.abs(segB.to - segB.from);\n            if (Math.abs(distA2 - distB2) > 1) {\n              return distA2 - distB2;\n            }\n          }\n        }\n        return 0;\n      });\n      const handles = grp.map((ei) => allRoutedSegments[edgeSegmentIndices[ei][0]]);\n      crossings += resolveHandleConflicts(handles);\n    }\n    return crossings;\n  }, \"fixSourceHandleCrossings\");\n  const fixTargetHandleCrossings = /* @__PURE__ */ chunk_Y2CYZVJY_name(() => {\n    let crossings = 0;\n    const edgesByTarget = /* @__PURE__ */ new Map();\n    for (const [i, e] of edges.entries()) {\n      const indices = edgeSegmentIndices[i];\n      if (indices.length === 0) {\n        continue;\n      }\n      if (!e.end) {\n        continue;\n      }\n      if (!edgesByTarget.has(e.end)) {\n        edgesByTarget.set(e.end, []);\n      }\n      edgesByTarget.get(e.end).push(i);\n    }\n    for (const grp of edgesByTarget.values()) {\n      grp.sort((a, b) => {\n        const getDist = /* @__PURE__ */ chunk_Y2CYZVJY_name((edgeIdx) => {\n          const indices = edgeSegmentIndices[edgeIdx];\n          if (indices.length < 2) {\n            return 0;\n          }\n          const prev = allRoutedSegments[indices[indices.length - 2]];\n          return Math.abs(prev.to - prev.from);\n        }, \"getDist\");\n        const scoreA = getDist(a);\n        const scoreB = getDist(b);\n        if (Math.abs(scoreA - scoreB) > 0.1) {\n          return scoreA - scoreB;\n        }\n        return a - b;\n      });\n      const handles = grp.map(\n        (ei) => allRoutedSegments[edgeSegmentIndices[ei][edgeSegmentIndices[ei].length - 1]]\n      );\n      crossings += resolveHandleConflicts(handles);\n    }\n    return crossings;\n  }, \"fixTargetHandleCrossings\");\n  const fixPipeCrossings = /* @__PURE__ */ chunk_Y2CYZVJY_name(() => {\n    let crossings = 0;\n    for (const pipe of pipes) {\n      const pipeSegments = [];\n      for (const t of pipe.tracks) {\n        for (const ref of t.segments) {\n          const idx = edgeSegmentIndices[ref.edgeIndex].find(\n            (ix) => allRoutedSegments[ix].segmentIndex === ref.segmentIndex\n          );\n          if (idx !== void 0) {\n            pipeSegments.push(allRoutedSegments[idx]);\n          }\n        }\n      }\n      pipeSegments.sort((a, b) => a.edgeIndex - b.edgeIndex || a.segmentIndex - b.segmentIndex);\n      for (let i = 0; i < pipeSegments.length; i++) {\n        for (let j = i + 1; j < pipeSegments.length; j++) {\n          const s1 = pipeSegments[i];\n          const s2 = pipeSegments[j];\n          if (segmentsConflict(s1, s2)) {\n            crossings++;\n            resolveTrackConflict(s1, s2, moveSegmentToTrack);\n          }\n        }\n      }\n    }\n    return crossings;\n  }, \"fixPipeCrossings\");\n  let iterations = 0;\n  const MAX_ITER = 10;\n  while (iterations < MAX_ITER) {\n    let changed = 0;\n    changed += fixSourceHandleCrossings();\n    changed += fixTargetHandleCrossings();\n    changed += fixPipeCrossings();\n    if (changed === 0) {\n      break;\n    }\n    iterations++;\n  }\n  const segmentCoords = /* @__PURE__ */ new Map();\n  for (const pipe of pipes) {\n    const segments = [];\n    pipe.tracks.forEach((t) => {\n      t.segments.forEach((s) => {\n        segments.push({\n          edgeIndex: s.edgeIndex,\n          segmentIndex: s.segmentIndex,\n          trackIndex: t.index,\n          from: s.from,\n          to: s.to\n        });\n      });\n    });\n    segments.sort((a, b) => a.from - b.from);\n    const clusters = [];\n    if (segments.length > 0) {\n      let currentCluster = [segments[0]];\n      let clusterEnd = segments[0].to;\n      for (let k = 1; k < segments.length; k++) {\n        const s = segments[k];\n        if (s.from < clusterEnd) {\n          currentCluster.push(s);\n          clusterEnd = Math.max(clusterEnd, s.to);\n        } else {\n          clusters.push(currentCluster);\n          currentCluster = [s];\n          clusterEnd = s.to;\n        }\n      }\n      clusters.push(currentCluster);\n    }\n    for (const cluster of clusters) {\n      const usedTracks = /* @__PURE__ */ new Set();\n      cluster.forEach((s) => usedTracks.add(s.trackIndex));\n      const trackScores = /* @__PURE__ */ new Map();\n      cluster.forEach((s) => {\n        const info = getDestInfo(s.edgeIndex);\n        trackScores.set(s.trackIndex, (trackScores.get(s.trackIndex) ?? 0) + info.delta);\n      });\n      const leftTracks = [...usedTracks].filter((t) => (trackScores.get(t) ?? 0) < -1);\n      const rightTracks = [...usedTracks].filter((t) => (trackScores.get(t) ?? 0) > 1);\n      const neutralTracks = [...usedTracks].filter((t) => Math.abs(trackScores.get(t) ?? 0) <= 1);\n      leftTracks.sort((a, b) => (trackScores.get(b) ?? 0) - (trackScores.get(a) ?? 0));\n      rightTracks.sort((a, b) => (trackScores.get(a) ?? 0) - (trackScores.get(b) ?? 0));\n      const assignCoord = /* @__PURE__ */ chunk_Y2CYZVJY_name((trackIndex, coord) => {\n        cluster.filter((s) => s.trackIndex === trackIndex).forEach((s) => {\n          const effectiveCoord = straightIntraLaneEdges.has(s.edgeIndex) ? pipe.coord : coord;\n          segmentCoords.set(`${s.edgeIndex}-${s.segmentIndex}`, effectiveCoord);\n        });\n      }, \"assignCoord\");\n      let leftCount = 0;\n      for (const trackIndex of leftTracks) {\n        leftCount++;\n        assignCoord(trackIndex, pipe.coord - leftCount * TRACK_SPACING);\n      }\n      if (neutralTracks.length === 0 && usedTracks.size > 0) {\n        const bestTrack = [...usedTracks].sort(\n          (a, b) => Math.abs(trackScores.get(a) ?? 0) - Math.abs(trackScores.get(b) ?? 0)\n        )[0];\n        const leftIdx = leftTracks.indexOf(bestTrack);\n        if (leftIdx !== -1) {\n          leftTracks.splice(leftIdx, 1);\n        }\n        const rightIdx = rightTracks.indexOf(bestTrack);\n        if (rightIdx !== -1) {\n          rightTracks.splice(rightIdx, 1);\n        }\n        neutralTracks.push(bestTrack);\n      }\n      let neutralAssigned = 0;\n      for (const trackIndex of neutralTracks) {\n        if (neutralAssigned === 0) {\n          assignCoord(trackIndex, pipe.coord);\n        } else {\n          const dir = neutralAssigned % 2 === 1 ? 1 : -1;\n          const magnitude = Math.ceil(neutralAssigned / 2);\n          assignCoord(trackIndex, pipe.coord + dir * magnitude * TRACK_SPACING * 0.5);\n        }\n        neutralAssigned++;\n      }\n      let rightCount = 0;\n      for (const trackIndex of rightTracks) {\n        rightCount++;\n        assignCoord(trackIndex, pipe.coord + rightCount * TRACK_SPACING);\n      }\n    }\n  }\n  for (const [i, e] of edges.entries()) {\n    const indices = edgeSegmentIndices[i] ?? [];\n    if (indices.length === 0) {\n      continue;\n    }\n    const newPoints = [];\n    const src = nodeById.get(e.start);\n    const dst = nodeById.get(e.end);\n    const { pSrcPort, pDstPort } = portsForEdge(i, src, dst);\n    const lines = indices.map((idx) => {\n      const s = allRoutedSegments[idx];\n      const coord = segmentCoords.get(`${s.edgeIndex}-${s.segmentIndex}`) ?? s.pipe.coord;\n      return {\n        orient: s.orientation,\n        coord,\n        from: s.from,\n        to: s.to\n      };\n    });\n    newPoints.push(pSrcPort);\n    for (let k = 0; k < lines.length; k++) {\n      const line = lines[k];\n      const prevPt = newPoints[newPoints.length - 1];\n      const prevAlong = line.orient === \"vertical\" ? prevPt.y : prevPt.x;\n      const prevTrackCoord = line.orient === \"vertical\" ? prevPt.x : prevPt.y;\n      const nextLine = lines[k + 1];\n      const hasNextLine = k < lines.length - 1;\n      if (Math.abs(prevTrackCoord - line.coord) > EPS7) {\n        newPoints.push(pointOnLine(line, prevAlong));\n      }\n      if (hasNextLine && nextLine.orient === line.orient) {\n        if (Math.abs(line.coord - nextLine.coord) > EPS7) {\n          const junction = line.orient === \"vertical\" ? (prevAlong + nextLine.from) / 2 : sharedLineEndpointCoord(line, nextLine);\n          newPoints.push(pointOnLine(line, junction), pointOnLine(nextLine, junction));\n        } else if (k === 0 || k === lines.length - 2) {\n          newPoints.push(pointOnLine(line, sharedLineEndpointCoord(line, nextLine)));\n        }\n      } else if (hasNextLine) {\n        newPoints.push(pointOnLine(line, nextLine.coord));\n      } else {\n        const endAlong = Math.abs(line.from - prevAlong) < Math.abs(line.to - prevAlong) ? line.to : line.from;\n        newPoints.push(pointOnLine(line, endAlong));\n      }\n    }\n    const last = newPoints[newPoints.length - 1];\n    if (Math.abs(last.x - pDstPort.x) > EPS7 || Math.abs(last.y - pDstPort.y) > EPS7) {\n      newPoints.push(pDstPort);\n    }\n    const filtered = [];\n    if (newPoints.length > 0) {\n      filtered.push(newPoints[0]);\n    }\n    for (let k = 1; k < newPoints.length; k++) {\n      const p = newPoints[k];\n      const prev = filtered[filtered.length - 1];\n      if (Math.abs(p.x - prev.x) > EPS7 || Math.abs(p.y - prev.y) > EPS7) {\n        filtered.push(p);\n      }\n    }\n    e.points = filtered;\n  }\n  for (const re of edges) {\n    const orig = re.__originalEdge;\n    if (orig && re.points) {\n      orig.points = re.points;\n    }\n  }\n  data.edges = (data.edges ?? []).filter((e) => !e.isLayoutOnly);\n  const nodeBoundaryClamp = /* @__PURE__ */ chunk_Y2CYZVJY_name((p, node) => {\n    const cx = node.x ?? 0;\n    const cy = node.y ?? 0;\n    const w = node.width ?? 0;\n    const h = node.height ?? 0;\n    if (w <= 0 || h <= 0) {\n      return p;\n    }\n    const left = cx - w / 2;\n    const right = cx + w / 2;\n    const top = cy - h / 2;\n    const bottom = cy + h / 2;\n    if (p.x < left || p.x > right || p.y < top || p.y > bottom) {\n      return p;\n    }\n    const dLeft = p.x - left;\n    const dRight = right - p.x;\n    const dTop = p.y - top;\n    const dBottom = bottom - p.y;\n    const minD = Math.min(dLeft, dRight, dTop, dBottom);\n    if (minD === dLeft) {\n      return { x: left, y: p.y };\n    }\n    if (minD === dRight) {\n      return { x: right, y: p.y };\n    }\n    if (minD === dTop) {\n      return { x: p.x, y: top };\n    }\n    return { x: p.x, y: bottom };\n  }, \"nodeBoundaryClamp\");\n  for (const edge of data.edges) {\n    const pts = edge.points;\n    if (!pts || pts.length < 2) {\n      continue;\n    }\n    const srcId = edge.start;\n    const dstId = edge.end;\n    const src = srcId ? nodeById.get(srcId) : void 0;\n    const dst = dstId ? nodeById.get(dstId) : void 0;\n    if (src) {\n      pts[0] = nodeBoundaryClamp(pts[0], src);\n    }\n    if (dst) {\n      pts[pts.length - 1] = nodeBoundaryClamp(pts[pts.length - 1], dst);\n    }\n  }\n  return data;\n}\nchunk_Y2CYZVJY_name(routeEdgesOrthogonal, \"routeEdgesOrthogonal\");\n\n// src/rendering-util/layout-algorithms/swimlanes/layoutCore.ts\nfunction getSwimlaneDirection(data4Layout) {\n  return data4Layout.direction ?? \"TB\";\n}\nchunk_Y2CYZVJY_name(getSwimlaneDirection, \"getSwimlaneDirection\");\nfunction runSwimlaneLayoutCore(data4Layout) {\n  const g = toGraphView(data4Layout);\n  const nodeGap = data4Layout.config.flowchart?.nodeSpacing ?? 40;\n  const layerGap = data4Layout.config.flowchart?.rankSpacing ?? 100;\n  const ignoreCrossLaneEdges = data4Layout.config.swimlane?.ignoreCrossLaneEdges ?? true;\n  const optimizeRanksByCrossings2 = data4Layout.config.swimlane?.optimizeRanksByCrossings ?? true;\n  const automaticLaneOrdering = data4Layout.config.swimlane?.automaticLaneOrdering ?? false;\n  const direction = getSwimlaneDirection(data4Layout);\n  const { ordered, coordinates } = sugiyamaLayout(g, {\n    nodeGap,\n    layerGap,\n    ignoreCrossLaneEdges,\n    optimizeRanksByCrossings: optimizeRanksByCrossings2,\n    automaticLaneOrdering,\n    direction\n  });\n  writeBackToLayoutData(g, ordered, coordinates, { nodeGap, layerGap });\n  for (const edge of data4Layout.edges ?? []) {\n    delete edge.points;\n  }\n  routeEdgesOrthogonal(data4Layout, direction);\n  for (const edge of data4Layout.edges ?? []) {\n    if (!edge.curve || edge.curve === \"basis\") {\n      edge.curve = \"rounded\";\n    }\n  }\n  postProcessSwimlaneLayout(data4Layout, direction);\n  validateSwimlanesLayout(data4Layout);\n  return direction;\n}\nchunk_Y2CYZVJY_name(runSwimlaneLayoutCore, \"runSwimlaneLayoutCore\");\n\n// src/rendering-util/layout-algorithms/swimlanes/index.ts\nfunction prepareSwimlaneLayout(data4Layout) {\n  prepareLayoutForSwimlanes(data4Layout);\n  const transformedData = createEdgeLabelNodes(data4Layout);\n  data4Layout.nodes = transformedData.nodes;\n  data4Layout.edges = transformedData.edges;\n}\nchunk_Y2CYZVJY_name(prepareSwimlaneLayout, \"prepareSwimlaneLayout\");\nvar render = createCommonLayoutRenderer({\n  prepareLayout: prepareSwimlaneLayout,\n  runLayoutCore: runSwimlaneLayoutCore,\n  afterPaint: applySwimlaneLineJumps\n});\n\n\nexport { render };\n"],"names":["markerOffsets","createCommonLayoutRenderer","log","chunk_Y2CYZVJY_name","o","e","t","n","l","r","i","a","s","f","d","u","h","Math","Number","g","c","p","Map","x","m","y","b","M","atob","Buffer","JSON","Array","CSS","I","S","w","v","C","L","k","_","$","String","R","O","T","F","E","B","N","X","z","Y","A","G","P","D","H","j","V","W","K","J","q","U","Q","Z","ee","et","en","er","eo","el","ei","ea","es","ef","ed","eu","eh","eg","ev","ec","ep","ex","em","ey","eb","eM","eI","eS","ew","eC","eL","ek","e$","eR","eO","eT","eF","eE","eB","eN","eX","ez","eY","eA","eG","eP","eD","eH","ej","Set","e_","eV","eW","eK","eJ","eq","eU","eQ","eZ","e0","e1","e2","e6","e5","e3","e8","e4","e9","e7","te","tt","tn","tr","to","tl","ti","ta","ts","tf","td","tu","th","tg","Object","tc","tp","tx","tm","ty","tb","tM","tI","tS","tw","tv","tC","tL","tk","t$","tR","tO","tT","tF","tE","tB","tN","tX","tz","tY","tA","tG","tP","tD","tH","tj","t_","isFinite","tV","tW","tK","tJ","tq","tU","tQ","tZ","t0","t1","t2","t6","t5","t3","t8","t4","t9","t7","ne","isNaN","nt","nn","nr","no","render"],"mappings":"AAAA,OAASA,iBAAAA,EAAeC,8BAAAA,EAA4BC,OAAAA,EAAKC,uBAAAA,MAA2B,yBAA0B,CAmB9G,SAASC,EAAiBC,CAAM,EAC9B,IAAMC,EAAW,EAAE,CACnB,IAAK,IAAIC,EAAI,EAAGA,EAAIF,EAAO,MAAM,CAAG,EAAGE,IACrCD,EAAS,IAAI,CAAC,CAAE,EAAGD,CAAM,CAACE,EAAE,CAAE,EAAGF,CAAM,CAACE,EAAI,EAAE,AAAC,GAEjD,OAAOD,CACT,CAEA,SAASE,EAAoBH,CAAE,CAAEC,CAAE,CAAEC,CAAE,CAAEE,CAAE,EACzC,IAAML,EAAME,EAAG,CAAC,CAAGD,EAAG,CAAC,CACjBG,EAAMF,EAAG,CAAC,CAAGD,EAAG,CAAC,CACjBK,EAAMD,EAAG,CAAC,CAAGF,EAAG,CAAC,CACjBI,EAAMF,EAAG,CAAC,CAAGF,EAAG,CAAC,CACjBK,EAAQR,EAAMO,EAAMH,EAAME,EAChC,GAAIE,AAAU,IAAVA,EACF,OAAO,KAET,IAAMC,EAAKN,EAAG,CAAC,CAAGF,EAAG,CAAC,CAChBS,EAAKP,EAAG,CAAC,CAAGF,EAAG,CAAC,CAChBU,EAAK,AAACF,CAAAA,EAAKF,EAAMG,EAAKJ,CAAE,EAAKE,EAC7BI,EAAK,AAACH,CAAAA,EAAKL,EAAMM,EAAKV,CAAE,EAAKQ,SACnC,AAAIG,GAtBiB,MAsBSA,GAAM,SAAwBC,GAtBvC,MAsBiEA,GAAM,QACnF,KAEF,CACL,MAAO,CAAE,EAAGX,EAAG,CAAC,CAAGU,EAAKX,EAAK,EAAGC,EAAG,CAAC,CAAGU,EAAKP,CAAI,EAChDO,GAAAA,EACAC,GAAAA,CACF,CACF,CAEA,SAASN,EAAgBL,CAAG,EAC1B,OAAOY,KAAK,GAAG,CAACZ,EAAI,CAAC,CAAC,CAAC,CAAGA,EAAI,CAAC,CAAC,CAAC,GAAKY,KAAK,GAAG,CAACZ,EAAI,CAAC,CAAC,CAAC,CAAGA,EAAI,CAAC,CAAC,CAAC,CAClE,CAEA,SAASM,EAAsBN,CAAK,EAClC,IAAMC,EAAY,EAAE,CACpB,IAAK,IAAIC,EAAI,EAAGA,EAAIF,EAAM,MAAM,CAAEE,IAAK,CACrC,IAAME,EAAQJ,CAAK,CAACE,EAAE,CAChBI,EAAYP,EAAiBK,EAAM,MAAM,EAC/C,IAAK,IAAIG,EAAIL,EAAI,EAAGK,EAAIP,EAAM,MAAM,CAAEO,IAAK,CACzC,IAAML,EAAQF,CAAK,CAACO,EAAE,CAChBC,EAAYT,EAAiBG,EAAM,MAAM,EAC/C,IAAK,GAAM,CAACF,EAAID,EAAK,GAAIO,EAAU,OAAO,GACxC,IAAK,GAAM,CAACA,EAAIC,EAAK,GAAIC,EAAU,OAAO,GAAI,CAC5C,IAAMA,EAAML,EAAoBJ,EAAK,CAAC,CAAEA,EAAK,CAAC,CAAEQ,EAAK,CAAC,CAAEA,EAAK,CAAC,EAC9D,GAAI,CAACC,EACH,SAEF,IAAMC,EAASJ,EAAgBN,EAGf,CADOU,IADRJ,EAAgBE,IAEEE,EAE/BR,EAAU,IAAI,CAAC,CACb,WAAYG,EAAM,EAAE,CACpB,YAAaF,EAAM,EAAE,CACrB,SAAUF,EACV,EAAGQ,EAAI,EAAE,CACT,MAAOA,EAAI,KAAK,AAClB,GAEAP,EAAU,IAAI,CAAC,CACb,WAAYC,EAAM,EAAE,CACpB,YAAaE,EAAM,EAAE,CACrB,SAAUE,EACV,EAAGE,EAAI,EAAE,CACT,MAAOA,EAAI,KAAK,AAClB,EAEJ,CAEJ,CACF,CACA,OAAOP,CACT,CAEA,SAASM,EAAIP,CAAC,EACZ,IAAMC,EAAUW,KAAK,KAAK,CAACZ,AAAI,IAAJA,GAAW,IACtC,OAAOa,OAAO,SAAS,CAACZ,GAAW,CAAC,EAAEA,EAAQ,CAAC,AACjD,CAEA,SAASO,EAAcR,CAAC,EACtB,MAAO,CAAC,EAAEO,EAAIP,EAAE,CAAC,EAAE,CAAC,EAAEO,EAAIP,EAAE,CAAC,EAAE,CAAC,AAClC,CAEA,SAASS,EAAgBT,CAAG,EAC1B,IAAMC,EAAKD,EAAI,CAAC,CAAC,CAAC,CAAGA,EAAI,CAAC,CAAC,CAAC,CACtBE,EAAKF,EAAI,CAAC,CAAC,CAAC,CAAGA,EAAI,CAAC,CAAC,CAAC,QAC5B,AAAIY,KAAK,GAAG,CAACX,IAAOW,KAAK,GAAG,CAACV,GACpBD,CAAAA,CAAAA,GAAM,GAERC,CAAAA,CAAAA,GAAM,EACf,CAGA,SAASQ,EAAmBT,CAAM,CAAEC,CAAI,EACtC,GAAID,EAAO,MAAM,CAAG,EAClB,OAAOA,EAAO,GAAG,CAAC,AAACD,GAAO,EAAE,GAAGA,CAAC,AAAC,IAEnC,IAAMI,EAAMH,EAAO,GAAG,CAAC,AAACD,GAAO,EAAE,GAAGA,CAAC,AAAC,IAChCD,EAAWG,EAAK,cAAc,EAAIF,CAAa,CAACE,EAAK,cAAc,CAAC,CAC1E,GAAIH,EAAU,CACZ,IAAMC,EAAIC,CAAM,CAAC,EAAE,CACbC,EAAID,CAAM,CAAC,EAAE,CACbE,EAAMS,KAAK,KAAK,CAACV,EAAE,CAAC,CAAGF,EAAE,CAAC,CAAEE,EAAE,CAAC,CAAGF,EAAE,CAAC,CAC3CI,CAAAA,CAAG,CAAC,EAAE,CAAC,CAAC,CAAGJ,EAAE,CAAC,CAAGD,EAAWa,KAAK,GAAG,CAACT,GACrCC,CAAG,CAAC,EAAE,CAAC,CAAC,CAAGJ,EAAE,CAAC,CAAGD,EAAWa,KAAK,GAAG,CAACT,EACvC,CACA,IAAMA,EAASD,EAAK,YAAY,EAAIF,CAAa,CAACE,EAAK,YAAY,CAAC,CACpE,GAAIC,EAAQ,CACV,IAAMH,EAAIC,EAAO,MAAM,CACjBC,EAAID,CAAM,CAACD,EAAI,EAAE,CACjBD,EAAIE,CAAM,CAACD,EAAI,EAAE,CACjBK,EAAMO,KAAK,KAAK,CAACb,EAAE,CAAC,CAAGG,EAAE,CAAC,CAAEH,EAAE,CAAC,CAAGG,EAAE,CAAC,CAC3CE,CAAAA,CAAG,CAACJ,EAAI,EAAE,CAAC,CAAC,CAAGD,EAAE,CAAC,CAAGI,EAASS,KAAK,GAAG,CAACP,GACvCD,CAAG,CAACJ,EAAI,EAAE,CAAC,CAAC,CAAGD,EAAE,CAAC,CAAGI,EAASS,KAAK,GAAG,CAACP,EACzC,CACA,OAAOD,CACT,CAEA,SAASO,EAASX,CAAI,CAAEC,CAAE,CAAEC,CAAE,CAAEE,CAAK,CAAEL,CAAK,EAC1C,IAAMI,EAAKH,EAAK,KAAK,CAAC,CAAC,CACjBK,EAAKL,EAAK,KAAK,CAAC,CAAC,CACjBM,EAAM,CAAE,EAAGH,EAAKF,EAAKD,EAAK,CAAC,CAAE,EAAGK,EAAKH,EAAKF,EAAK,CAAC,AAAC,EACjDS,EAAO,CAAE,EAAGN,EAAKF,EAAKD,EAAK,CAAC,CAAE,EAAGK,EAAKH,EAAKF,EAAK,CAAC,AAAC,EAClDU,EAAM,CAAC,CAAC,CAAC,EAAEF,EAAcF,GAAK,CAAC,CAAC,CAMtC,MALIP,AAAU,QAAVA,EACFW,EAAI,IAAI,CAAC,CAAC,CAAC,EAAEH,EAAIP,EAAK,CAAC,EAAE,CAAC,EAAEO,EAAIP,EAAK,CAAC,EAAE,KAAK,EAAEI,EAAM,CAAC,EAAEI,EAAcC,GAAM,CAAC,EAE7EC,EAAI,IAAI,CAAC,CAAC,CAAC,EAAEF,EAAcC,GAAM,CAAC,EAE7BC,CACT,CAEA,SAASI,EAAqBd,CAAI,CAAEC,CAAI,CAAEC,CAAI,CAAEE,CAAM,EACpD,IAAML,EAAME,EAAK,CAAC,CAAGD,EAAK,CAAC,CACrBG,EAAMF,EAAK,CAAC,CAAGD,EAAK,CAAC,CACrBK,EAAMH,EAAK,CAAC,CAAGD,EAAK,CAAC,CACrBK,EAAMJ,EAAK,CAAC,CAAGD,EAAK,CAAC,CACrBM,EAAOK,KAAK,KAAK,CAACb,EAAKI,GACvBK,EAAOI,KAAK,KAAK,CAACP,EAAKC,GAC7B,GAAIC,EAhJe,MAgJUC,EAhJV,KAiJjB,OAAO,KAET,IAAMC,EAAMV,EAAMQ,EACZG,EAAMP,EAAMI,EACZI,EAAMN,EAAMG,EACZM,EAAMR,EAAME,EAGZO,EAAQH,KAAK,IAAI,CADPA,KAAK,GAAG,CAAC,GAAIA,KAAK,GAAG,CAAC,EAD1BH,EAAME,EAAMD,EAAMI,KAG9B,GAAIC,EA1Je,MA0JWH,AA1JX,KA0JWA,KAAK,GAAG,CAACA,KAAK,EAAE,CAAGG,GAC/C,OAAO,KAET,IAAMC,EAASJ,KAAK,GAAG,CAACR,EAASQ,KAAK,GAAG,CAACG,EAAQ,GAAIR,EAAO,EAAGC,EAAO,GACvE,MAAO,CACL,OAAQP,EAAK,CAAC,CAAGQ,EAAMO,EACvB,OAAQf,EAAK,CAAC,CAAGS,EAAMM,EACvB,KAAMf,EAAK,CAAC,CAAGU,EAAMK,EACrB,KAAMf,EAAK,CAAC,CAAGa,EAAME,EACrB,MAAOf,EAAK,CAAC,CACb,MAAOA,EAAK,CAAC,CACbe,OAAAA,CACF,CACF,CAEA,SAASD,EAAgBf,CAAI,CAAEC,CAAK,CAAEC,CAAM,EAC1C,IAAME,EAAYJ,EAAK,MAAM,CAC7B,GAAII,EAAU,MAAM,CAAG,EACrB,MAAO,GAET,IAAMD,EAASO,EAAmBN,EAAWJ,GACvCK,EAAUL,AAAe,YAAfA,EAAK,KAAK,CACpBM,EAAWP,EAAiBI,GAC5BY,EAAwB,IAAIE,IAClC,IAAK,IAAMjB,KAAKC,EAAO,CACrB,IAAMA,EAAMK,CAAQ,CAACN,EAAE,QAAQ,CAAC,CAChC,GAAI,CAACC,EACH,SAEF,IAAMG,EAASQ,KAAK,KAAK,CAACX,EAAI,CAAC,CAAC,CAAC,CAAGA,EAAI,CAAC,CAAC,CAAC,CAAEA,EAAI,CAAC,CAAC,CAAC,CAAGA,EAAI,CAAC,CAAC,CAAC,EACxDF,EAAOgB,EAAM,GAAG,CAACf,EAAE,QAAQ,GAAK,EAAE,CACxCD,EAAK,IAAI,CAAC,CACR,EAAGC,EAAE,CAAC,CACN,MAAOA,EAAE,KAAK,CACd,EAAGA,EAAE,CAAC,CAAGI,EACT,EAAGF,EAAO,UAAU,AACtB,GACAa,EAAM,GAAG,CAACf,EAAE,QAAQ,CAAED,EACxB,CACA,IAAMiB,EAAQ,CAAC,CAAC,CAAC,EAAER,EAAcL,CAAM,CAAC,EAAE,EAAE,CAAC,CAAC,CAC9C,IAAK,IAAIH,EAAI,EAAGA,EAAIM,EAAS,MAAM,CAAEN,IAAK,CACxC,IAAMC,EAAMK,CAAQ,CAACN,EAAE,CACjBI,EAASQ,KAAK,KAAK,CAACX,EAAI,CAAC,CAAC,CAAC,CAAGA,EAAI,CAAC,CAAC,CAAC,CAAEA,EAAI,CAAC,CAAC,CAAC,CAAGA,EAAI,CAAC,CAAC,CAAC,EACxDF,EAAKK,AAAW,IAAXA,EAAe,EAAI,AAACH,CAAAA,EAAI,CAAC,CAAC,CAAC,CAAGA,EAAI,CAAC,CAAC,CAAC,AAAD,EAAKG,EAC9CM,EAAKN,AAAW,IAAXA,EAAe,EAAI,AAACH,CAAAA,EAAI,CAAC,CAAC,CAAC,CAAGA,EAAI,CAAC,CAAC,CAAC,AAAD,EAAKG,EAC9Cc,EAAQT,EAAgBR,GAC1BkB,EAAmB,EACvB,GAAId,GAAWL,EAAI,EAAG,CACpB,IAAMC,EAASa,EACbX,CAAM,CAACH,EAAI,EAAE,CACbG,CAAM,CAACH,EAAE,CACTG,CAAM,CAACH,EAAI,EAAE,EAAIG,CAAM,CAACH,EAAE,CA9MN,EAiNlBC,CAAAA,GACFkB,CAAAA,EAAmBlB,EAAO,MAAM,AAAD,CAEnC,CACA,IAAImB,EAAahB,EACbiB,EAAiB,KACjBhB,GAAWL,EAAIM,EAAS,MAAM,CAAG,GACnCe,CAAAA,EAAiBP,EACfX,CAAM,CAACH,EAAE,CACTG,CAAM,CAACH,EAAI,EAAE,CACbG,CAAM,CAACH,EAAI,EAAE,EAAIG,CAAM,CAACH,EAAI,EAAE,CA3NV,EA6NtB,GAEEoB,CAAAA,EAAahB,EAASiB,EAAe,MAAM,AAAD,EAG9C,IAAMC,EAAW,IAAIP,EAAM,GAAG,CAACf,IAAM,EAAE,CAAC,CAAC,IAAI,CAAC,CAACA,EAAGC,IAAMD,EAAE,CAAC,CAAGC,EAAE,CAAC,EACjE,IAAK,IAAMD,KAAKsB,EACdtB,EAAE,CAAC,CAAGY,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAEA,EAAE,CAAC,CAAGmB,EAAkBC,EAAapB,EAAE,CAAC,EAE9D,IAAK,IAAIA,EAAI,EAAGA,EAAIsB,EAAS,MAAM,CAAG,EAAGtB,IAAK,CAC5C,IAAMC,EAAMqB,CAAQ,CAACtB,EAAI,EAAE,CAAC,CAAC,CAAGsB,CAAQ,CAACtB,EAAE,CAAC,CAAC,CAC7C,GAAIsB,CAAQ,CAACtB,EAAE,CAAC,CAAC,CAAGsB,CAAQ,CAACtB,EAAI,EAAE,CAAC,CAAC,CAAGC,EAAK,CAC3C,IAAMC,EAAOD,EAAM,CACnBqB,CAAAA,CAAQ,CAACtB,EAAE,CAAC,CAAC,CAAGY,KAAK,GAAG,CAACU,CAAQ,CAACtB,EAAE,CAAC,CAAC,CAAEE,GACxCoB,CAAQ,CAACtB,EAAI,EAAE,CAAC,CAAC,CAAGY,KAAK,GAAG,CAACU,CAAQ,CAACtB,EAAI,EAAE,CAAC,CAAC,CAAEE,EAClD,CACF,CACA,IAAK,IAAMF,KAAKsB,EACVtB,EAAE,CAAC,CA7IS,MAgJhBgB,EAAM,IAAI,IAAIL,EAASX,EAAGD,EAAIW,EAAIQ,EAAOhB,EAAO,SAAS,EAEvDG,CAAAA,GAAWgB,GACbL,EAAM,IAAI,CAAC,CAAC,CAAC,EAAET,EAAIc,EAAe,MAAM,EAAE,CAAC,EAAEd,EAAIc,EAAe,MAAM,EAAE,CAAC,EACzEL,EAAM,IAAI,CACR,CAAC,CAAC,EAAET,EAAIc,EAAe,KAAK,EAAE,CAAC,EAAEd,EAAIc,EAAe,KAAK,EAAE,CAAC,EAAEd,EAAIc,EAAe,IAAI,EAAE,CAAC,EAAEd,EAAIc,EAAe,IAAI,EAAE,CAAC,GAGtHL,EAAM,IAAI,CAAC,CAAC,CAAC,EAAER,EAAcP,EAAI,CAAC,EAAE,CAAC,CAEzC,CACA,OAAOe,EAAM,IAAI,CAAC,IACpB,CAEA,SAASA,EAAehB,CAAC,EACvB,MAAO,qBAAqB,IAAI,CAACA,EACnC,CAEA,SAASkB,EAAsBlB,CAAK,QAClC,CAAKA,GAGEA,AAAU,WAAVA,GAAsBA,AAAU,YAAVA,GAAuBA,AAAU,SAAVA,GAAoBA,AAAU,eAAVA,GAA0BA,AAAU,cAAVA,CACpG,CAEA,SAASmB,EAAiBnB,CAAG,EAC3B,GAAI,CAACA,EACH,OAAO,KAET,GAAI,CACF,IAAMC,EAAO,AAAgB,YAAhB,OAAOsB,KAAsBA,KAAKvB,GAAOwB,OAAO,IAAI,CAACxB,EAAK,UAAU,QAAQ,GACnFE,EAASuB,KAAK,KAAK,CAACxB,GAC1B,GAAI,CAACyB,MAAM,OAAO,CAACxB,GACjB,OAAO,KAET,IAAME,EAAM,EAAE,CACd,IAAK,IAAMJ,KAAKE,EACVF,GAAK,AAAe,UAAf,OAAOA,EAAE,CAAC,EAAiB,AAAe,UAAf,OAAOA,EAAE,CAAC,EAC5CI,EAAI,IAAI,CAAC,CAAE,EAAGJ,EAAE,CAAC,CAAE,EAAGA,EAAE,CAAC,AAAC,GAG9B,OAAOI,EAAI,MAAM,EAAI,EAAIA,EAAM,IACjC,CAAE,KAAM,CACN,OAAO,IACT,CACF,CAEA,SAASgB,EAAoBpB,CAAc,CAAEC,CAAK,CAAEC,CAAM,EACxD,GAAI,CAACA,EAAO,OAAO,CACjB,OAEF,IAAME,EAAYJ,EAAe,IAAI,GACrC,GAAI,CAACI,EACH,OAEF,IAAML,EAA2B,IAAIkB,IACrC,IAAK,IAAMjB,KAAKC,EACdF,EAAS,GAAG,CAACC,EAAE,EAAE,CAAEA,GAErB,IAAMG,EAAgB,EAAE,CAClBE,EAA2B,IAAIY,IACrC,IAAK,IAAMjB,KAAKC,EAAO,CACrB,IAAMA,EAAY,AAAe,IAAf,OAAO0B,KAAuBA,IAAI,MAAM,CAAGA,IAAI,MAAM,CAAC3B,EAAE,EAAE,EAAIA,EAAE,EAAE,CAC9EE,EAASE,EAAU,aAAa,CAAC,CAAC,cAAc,EAAEH,EAAU,EAAE,CAAC,EACrE,GAAI,CAACC,EACH,SAEFG,EAAS,GAAG,CAACL,EAAE,EAAE,CAAEE,GAEnB,IAAMH,EAAS,AADCoB,EAAiBjB,EAAO,YAAY,CAAC,iBAC3BF,EAAE,MAAM,CAClCG,EAAc,IAAI,CAAC,CAAE,GAAGH,CAAC,CAAED,OAAAA,CAAO,EACpC,CACA,IAAMQ,EAAYD,EAAsBH,GACxC,GAAII,AAAqB,IAArBA,EAAU,MAAM,CAClB,OAEF,IAAMC,EAA8B,IAAIS,IACxC,IAAK,IAAMjB,KAAKO,EAAW,CACzB,IAAMN,EAAOO,EAAY,GAAG,CAACR,EAAE,UAAU,GAAK,EAAE,CAChDC,EAAK,IAAI,CAACD,GACVQ,EAAY,GAAG,CAACR,EAAE,UAAU,CAAEC,EAChC,CACA,IAAK,IAAMD,KAAgBG,EAAe,CACxC,IAAMF,EAAQO,EAAY,GAAG,CAACR,EAAa,EAAE,EAC7C,GAAI,CAACC,GAASA,AAAiB,IAAjBA,EAAM,MAAM,CACxB,SAEF,IAAMG,EAAOL,EAAS,GAAG,CAACC,EAAa,EAAE,EACnCG,EAAYC,GAAM,MACxB,GAAID,AAAc,KAAK,IAAnBA,GAAwB,CAACe,EAAsBf,GACjD,SAEF,IAAMG,EAASD,EAAS,GAAG,CAACL,EAAa,EAAE,EAC3C,GAAI,CAACM,GAGDH,AAAc,KAAK,IAAnBA,GAEE,CAACa,EADYV,EAAO,YAAY,CAAC,MAAQ,IAH7C,SAQF,IAAMC,EAAgBD,EAAO,YAAY,CAAC,UAAY,GAChDG,EAAiB,0DAA0D,IAAI,CACnFF,GAEIG,EAAmBD,EAAiBI,OAAO,UAAU,CAACJ,CAAc,CAAC,EAAE,EAAI,KAC3EE,EAAmBF,EAAiBI,OAAO,UAAU,CAACJ,CAAc,CAAC,EAAE,EAAI,KAC3EK,EAAOC,EAAgBf,EAAcC,EAAOC,GAElD,GADAI,EAAO,YAAY,CAAC,IAAKQ,GACrBJ,AAAqB,OAArBA,GAA6BC,AAAqB,OAArBA,GAA6B,AAAiC,YAAjC,OAAOL,EAAO,cAAc,CAAiB,CAEzG,IAAMN,EAAQY,KAAK,GAAG,CAAC,EAAG,AADXN,EAAO,cAAc,GACDI,EAAmBC,GAChDV,EAAe,CAAC,EAAE,EAAES,EAAiB,CAAC,EAAEV,EAAM,CAAC,EAAEW,EAAiB,CAAC,CACnET,EAAUK,EAAc,OAAO,CAAC,+BAAgC,CAAC,kBAAkB,EAAEN,EAAa,CAAC,CAAC,EAAE,OAAO,CAAC,UAAW,KAC/HK,EAAO,YAAY,CAAC,QAASJ,EAC/B,CACF,CACF,CAIA,SAASmB,EAAuBrB,CAAW,CAAE,CAAEC,QAAAA,CAAO,CAAE,EACtD,IAAMC,EAAiBF,EAAY,MAAM,EAAE,UAAU,SACrD,GAAIE,AAAmB,KAAnBA,EACF,OAGF,IAAME,EAAiBJ,EAAY,KAAK,CAAC,MAAM,CAAC,AAACA,GAAS0B,MAAM,OAAO,CAAC1B,EAAK,MAAM,GAAKA,EAAK,MAAM,CAAC,MAAM,EAAI,GAAG,GAAG,CAAC,AAACA,GAAU,EAC9H,GAAIA,EAAK,EAAE,CACX,OAAQA,EAAK,MAAM,CACnB,MAAOA,EAAK,KAAK,CACjB,eAAgBA,EAAK,cAAc,CACnC,aAAcA,EAAK,YAAY,AACjC,IACAoB,EAAoBnB,EAAQ,MAAM,CAAC,SAAS,CAAEG,EAAgB,CAC5D,QAAS,GACT,WAAY,EACZ,UAXgBF,AAAmB,QAAnBA,EAA2B,MAAQ,KAYrD,EACF,CApXAE,EAAoBL,EAAkB,oBAuBtCK,EAAoBD,EAAqB,uBAIzCC,EAAoBC,EAAiB,mBA0CrCD,EAAoBE,EAAuB,yBAK3CF,EAAoBG,EAAK,OAIzBH,EAAoBI,EAAe,iBASnCJ,EAAoBK,EAAiB,mBA0BrCL,EAAoBM,EAAoB,sBAcxCN,EAAoBO,EAAU,YAgC9BP,EAAoBU,EAAsB,wBAsF1CV,EAAoBW,EAAiB,mBAIrCX,EAAoBY,EAAgB,kBAOpCZ,EAAoBc,EAAuB,yBAsB3Cd,EAAoBe,EAAkB,oBAyEtCf,EAAoBgB,EAAqB,uBAsBzChB,EAAoBiB,EAAwB,0BAG5C,IAAIC,EAAsB,uBAG1B,SAASM,EAAyB5B,CAAI,EACpC,OAAOY,KAAK,GAAG,CAACZ,EAAK,OAAO,EAFQ,MAGtC,CAEA,SAAS6B,EAAuB7B,CAAI,EAClC,GAAM,CAAEC,EAAAA,CAAC,CAAEC,EAAAA,CAAC,CAAEE,MAAAA,CAAK,CAAEL,OAAAA,CAAM,CAAE,CAAGC,EAC1BG,EAAaH,EAAK,kBAAkB,CAC1C,GAAI,AAAa,UAAb,OAAOC,GAAkB,AAAa,UAAb,OAAOC,GAAkB,AAAiB,UAAjB,OAAOE,GAAsB,AAAkB,UAAlB,OAAOL,GAAuB,AAAsB,UAAtB,OAAOI,GAA2B,CAACU,OAAO,QAAQ,CAACZ,IAAM,CAACY,OAAO,QAAQ,CAACX,IAAM,CAACW,OAAO,QAAQ,CAACT,IAAU,CAACS,OAAO,QAAQ,CAACd,IAAW,CAACc,OAAO,QAAQ,CAACV,IAAeC,GAAS,GAAKL,GAAU,EAAG,YACjT,OAAOC,EAAK,cAAc,CAG5B,IAAMK,EAAMH,EAAIH,EAAS,EAEnBO,EAAcM,KAAK,GAAG,CAfG,GAe0BA,KAAK,GAAG,CAAC,EAAG,AADhDA,KAAK,GAAG,CAACT,EAAYD,EAAIH,EAAS,GAC6BM,IAC9EE,EAASF,EAAMC,CACrB,AAAIC,CAAAA,GAAUF,EACZ,OAAOL,EAAK,cAAc,CAG5BA,EAAK,cAAc,CAAG,CACpB,KAAMC,EAAIG,EAAQ,EAClB,MAAOH,EAAIG,EAAQ,EACnBC,IAAAA,EACAE,OAAAA,CACF,CACF,CAEA,SAASuB,EAA0B9B,CAAM,EACvC,IAAMC,EAAYD,EAAO,SAAS,CAC5BE,EAAQF,EAAO,KAAK,GAAK,EAAE,CACjC,IAAK,IAAME,KAAQF,EAAO,KAAK,EAAI,EAAE,CAC/BE,EAAK,OAAO,EAAI,CAACA,EAAK,QAAQ,GAChCA,EAAK,KAAK,CAAG,WACTD,GACFC,CAAAA,EAAK,SAAS,CAAGD,CAAQ,GAI/B,IAAMG,EAAaF,EAAM,MAAM,CAAC,AAACF,GAAS,CAACA,EAAK,OAAO,EAAI,CAACA,EAAK,QAAQ,EACzE,GAAII,AAAsB,IAAtBA,EAAW,MAAM,CACnB,OAEF,IAAIL,EAAcG,EAAM,IAAI,CAAC,AAACF,GAASA,EAAK,EAAE,GAAKsB,GAiBnD,IAAK,IAAMtB,KAhBND,EAUMA,EAAY,OAAO,GAC5BA,EAAY,KAAK,CAAG,WAChBE,GACFF,CAAAA,EAAY,SAAS,CAAGE,CAAQ,IAZlCF,EAAc,CACZ,GAAIuB,EACJ,MAAO,GACP,QAAS,GACT,MAAO,WACP,QAAS,GACT,GAAGrB,EAAY,CAAEA,UAAAA,CAAU,EAAI,CAAC,CAAC,AACnC,EACAC,EAAM,IAAI,CAACH,IAOMK,GACjBJ,EAAK,QAAQ,CAAGsB,CAEpB,CAEA,SAASS,EAAY/B,CAAM,EACzB,IAAMC,EAA2B,IAAIgB,IACrC,IAAK,IAAMf,KAAKF,EAAO,KAAK,EAAI,EAAE,CAChCC,EAAS,GAAG,CAACC,EAAE,EAAE,CAAEA,GAErB,IAAMA,EAAQ,EAAE,CAChB,IAAK,IAAMD,KAAKD,EAAO,KAAK,EAAI,EAAE,CAAE,CAClC,IAAMA,EAAM,AAAmB,UAAnB,OAAOC,EAAE,KAAK,CAAgBA,EAAE,KAAK,CAAG,KAAK,EACnDG,EAAM,AAAiB,UAAjB,OAAOH,EAAE,GAAG,CAAgBA,EAAE,GAAG,CAAG,KAAK,EAChDD,GAAQI,IAGTH,EAAE,WAAW,EAGjBC,EAAM,IAAI,CAAC,CAAE,GAAID,EAAE,EAAE,CAAED,IAAAA,EAAKI,IAAAA,EAAK,IAAKH,CAAE,GAC1C,CACA,IAAMG,EAAWJ,EAAO,KAAK,EAAI,EAAE,CAC7BD,EAAaK,EAAS,MAAM,CAAC,AAACJ,GAAMA,EAAE,OAAO,EAC7CG,EAAgBC,EAAS,MAAM,CAAC,AAACJ,GAAM,CAACA,EAAE,OAAO,EAGvD,MAAO,CAAE,MADK,IADY,IAAID,EAAW,CAAC,OAAO,MACTI,EAAc,CAAC,GAAG,CAAC,AAACH,GAAMA,EAAE,EAAE,EACtDE,MAAAA,EAAOF,OAAAA,EAAQC,SAAAA,CAAS,CAC1C,CAEA,SAAS+B,EAAsBhC,CAAC,CAAEC,CAAO,CAAEC,CAAM,CAAEE,CAAI,EACrD,GAAM,CAAEL,OAAAA,CAAM,CAAE,CAAGC,EACbG,EAAUH,EAAE,QAAQ,CACpBK,EAAWD,GAAM,UAAY,IAC7BE,EAAUF,GAAM,SAAW,GAC7BG,EAAa,EACjB,IAAK,IAAMP,KAASC,EAAQ,MAAM,CAAE,CAClC,IAAIA,EAAa,EACjB,IAAK,IAAMG,KAAMJ,EAAO,CACtB,IAAMA,EAAOG,EAAQ,GAAG,CAACC,GACzB,GAAI,CAACJ,EAAM,CACTC,IACA,QACF,CACAD,EAAK,KAAK,CAAGO,EACbP,EAAK,KAAK,CAAGC,EACb,IAAMF,EAAIG,EAAO,CAAC,CAACE,EAAG,EAAIH,EAAaK,EACjCE,EAAIN,EAAO,CAAC,CAACE,EAAG,EAAIG,EAAaF,CACvCL,CAAAA,EAAK,CAAC,CAAGD,EACTC,EAAK,CAAC,CAAGQ,EACTP,GACF,CACAM,GACF,CACA,IAAMC,EAAWT,EAAO,KAAK,EAAI,EAAE,CAC7BU,EAA8B,IAAIQ,IAClCP,EAAiB,EAAE,CACzB,IAAK,IAAMV,KAASQ,EAAU,CAC5B,GAAI,CAACR,GAAO,QACV,QAEE,AAACA,CAAAA,EAAM,QAAQ,EACjBU,EAAe,IAAI,CAACV,GAEtB,IAAMC,EAAWO,EAAS,MAAM,CAAC,AAACP,GAAMA,EAAE,QAAQ,GAAKD,EAAM,EAAE,EAC3DI,EAAO,IACPL,EAAO,CAAC,IACRI,EAAO,IACPE,EAAO,CAAC,IACZ,IAAK,IAAML,KAASC,EAAU,CAC5B,IAAMA,EAAKD,EAAM,CAAC,EAAIE,EAAO,CAAC,CAACF,EAAM,EAAE,CAAC,CAClCM,EAAKN,EAAM,CAAC,EAAIE,EAAO,CAAC,CAACF,EAAM,EAAE,CAAC,CAClCO,EAAKP,EAAM,KAAK,EAAI,EACpBQ,EAAKR,EAAM,MAAM,EAAI,CACjB,OAANC,GAAcK,AAAM,MAANA,IAChBF,EAAOQ,KAAK,GAAG,CAACR,EAAMH,EAAKM,EAAK,GAChCR,EAAOa,KAAK,GAAG,CAACb,EAAME,EAAKM,EAAK,GAChCJ,EAAOS,KAAK,GAAG,CAACT,EAAMG,EAAKE,EAAK,GAChCH,EAAOO,KAAK,GAAG,CAACP,EAAMC,EAAKE,EAAK,GAEpC,CACA,GAAIJ,IAAS,KAAYD,IAAS,IAChCH,EAAM,CAAC,CAAGA,EAAM,CAAC,EAAI,EACrBA,EAAM,CAAC,CAAGA,EAAM,CAAC,EAAI,EACrBA,EAAM,KAAK,CAAGA,EAAM,KAAK,EAAI,EAC7BA,EAAM,MAAM,CAAGA,EAAM,MAAM,EAAI,MAC1B,CACL,IAAMC,EAAMD,EAAM,OAAO,EAAI,GAGvBE,EAAIU,KAAK,GAAG,CAAC,EAAGb,EAAOK,GAFPJ,CAAAA,EAAM,QAAQ,CAAGC,EAAM,EAAI2B,EAAyB5B,EAAK,EAGzEM,EAAIM,KAAK,GAAG,CAAC,EAAGP,EAAOF,GAFTF,EAIdM,EAAK,AAACJ,CAAAA,EAAOE,CAAG,EAAK,CAC3BL,CAAAA,EAAM,CAAC,CAFI,AAACI,CAAAA,EAAOL,CAAG,EAAK,EAG3BC,EAAM,CAAC,CAAGO,EACVP,EAAM,KAAK,CAAGE,EACdF,EAAM,MAAM,CAAGM,EACfG,EAAY,GAAG,CAACT,EAAM,EAAE,CAAE,CAAEI,KAAAA,EAAML,KAAAA,EAAMI,KAAAA,EAAME,KAAAA,CAAK,EACrD,CACF,CACA,GAAIK,EAAe,MAAM,CAAG,GAAKD,EAAY,IAAI,CAAG,EAAG,CACrD,IAAIT,EAAa,IACbC,EAAa,CAAC,IACdC,EAAS,EACb,IAAK,IAAME,KAAQM,EAAgB,CACjC,IAAMX,EAAMK,EAAK,OAAO,EAAI,EACxBL,CAAAA,EAAMG,GACRA,CAAAA,EAASH,CAAE,EAEb,IAAMI,EAAIM,EAAY,GAAG,CAACL,EAAK,EAAE,EAC5BD,IAGLH,EAAaY,KAAK,GAAG,CAACZ,EAAYG,EAAE,IAAI,EACxCF,EAAaW,KAAK,GAAG,CAACX,EAAYE,EAAE,IAAI,EAC1C,CACA,GAAIH,IAAe,KAAYC,IAAe,CAAC,IAAU,CACvD,IAGMG,EAAa,AAHGQ,KAAK,GAAG,CAAC,EAAGX,EAAaD,GAGZ,EADZY,KAAK,GAAG,CAACV,EADR,IAGlBH,EAAU,AAACC,CAAAA,EAAaC,CAAS,EAAK,EAC5C,IAAK,IAAMA,KAAQS,EACjBT,EAAK,CAAC,CAAGF,EACTE,EAAK,MAAM,CAAGG,EACdH,EAAK,kBAAkB,CAAGD,EAE5B,IAAMG,EAAc,IAAIO,EAAe,CAAC,IAAI,CAAC,CAACV,EAAGC,IAGxC,AAFID,CAAAA,EAAE,CAAC,EAAI,GACPC,CAAAA,EAAE,CAAC,EAAI,IAGdI,EAAU,EAAE,CACZC,EAAU,EAAE,CACZC,EAAa,EAAE,CACrB,IAAK,IAAMP,KAAQG,EAAa,CAC9B,IAAMF,EAAIQ,EAAY,GAAG,CAACT,EAAK,EAAE,EACjC,GAAI,CAACC,EACH,SAEF,IAAMC,EAAeU,KAAK,GAAG,CAAC,EAAGX,EAAE,IAAI,CAAGA,EAAE,IAAI,EAAI,EAAI2B,EAAyB5B,GAC3EI,EAAK,AAACH,CAAAA,EAAE,IAAI,CAAGA,EAAE,IAAI,AAAD,EAAK,EAC/BI,EAAQ,IAAI,CAACL,EAAK,EAAE,EACpBM,EAAQ,IAAI,CAACF,GACbG,EAAW,IAAI,CAACL,EAClB,CACA,IAAMM,EAAQH,EAAQ,MAAM,CAC5B,GAAIG,EAAQ,EAAG,CACb,IAAMR,EAA6B,IAAIiB,IACvC,GAAIT,AAAU,IAAVA,EACFR,EAAW,GAAG,CAACK,CAAO,CAAC,EAAE,CAAEE,CAAU,CAAC,EAAE,MACnC,CACL,IAAMN,EAAI,EAAE,CACZ,IAAK,IAAID,EAAI,EAAGA,EAAIQ,EAAQ,EAAGR,IAC7BC,EAAE,IAAI,CAACK,CAAO,CAACN,EAAI,EAAE,CAAGM,CAAO,CAACN,EAAE,EAEpC,IAAME,EAAI,AAAIwB,MAAMlB,EACpBN,CAAAA,CAAC,CAAC,EAAE,CAAG,EACP,IAAK,IAAIF,EAAI,EAAGA,EAAIQ,EAAQ,EAAGR,IAC7BE,CAAC,CAACF,EAAI,EAAE,CAAG,EAAIC,CAAC,CAACD,EAAE,CAAGE,CAAC,CAACF,EAAE,CAE5B,IAAII,EAAa,EACbL,EAAa,IACjB,IAAK,IAAIC,EAAI,EAAGA,EAAIQ,EAAOR,IAAK,CAC9B,IAAMC,EAAQM,CAAU,CAACP,EAAE,AACvBA,CAAAA,EAAI,GAAM,EACZI,EAAaQ,KAAK,GAAG,CAACR,EAAYH,EAAQC,CAAC,CAACF,EAAE,EAE9CD,EAAaa,KAAK,GAAG,CAACb,EAAYG,CAAC,CAACF,EAAE,CAAGC,EAE7C,CACA,IAAIE,EAAIC,EAEND,EADEC,GAAcL,EACZ,AAACK,CAAAA,EAAaL,CAAS,EAAK,EAE5BK,EAEN,IAAK,IAAIH,EAAI,EAAGA,EAAIO,EAAOP,IAAK,CAC9B,IAAMG,EAAIF,CAAC,CAACD,EAAE,CAAIA,CAAAA,EAAI,GAAM,EAAIE,EAAI,CAACA,CAAAA,EAC/BJ,EAAaa,KAAK,GAAG,CAACL,CAAU,CAACN,EAAE,CAAEG,GAC3CJ,EAAW,GAAG,CAACK,CAAO,CAACJ,EAAE,CAAEF,EAC7B,CACF,CACA,IAAK,IAAME,KAAQS,EAAgB,CACjC,IAAMR,EAAIF,EAAW,GAAG,CAACC,EAAK,EAAE,CAC5BC,AAAK,OAALA,GACFD,CAAAA,EAAK,KAAK,CAAGC,CAAAA,EAEf2B,EAAuB5B,EACzB,CACF,CACF,CACF,CACF,CAKA,SAASgC,EAAqBjC,CAAI,EAChC,IAAMC,EAAa,EAAE,CACfG,EAAkB,EAAE,CACpBL,EAA2B,IAAIkB,IACrC,IAAK,IAAMhB,KAAQD,EAAK,KAAK,CAC3BD,EAAS,GAAG,CAACE,EAAK,EAAE,CAAEA,GAExB,IAAK,IAAME,KAAQH,EAAK,KAAK,CAAE,CAC7B,GAAI,CAACG,EAAK,KAAK,EAAIA,AAAsB,IAAtBA,EAAK,KAAK,CAAC,MAAM,EAGhCA,EAAK,YAAY,EAGjBA,EAAK,WAAW,CALlB,SAQF,IAAMH,EAAaG,EAAK,KAAK,CAAGJ,EAAS,GAAG,CAACI,EAAK,KAAK,EAAI,KAAK,EAC1DE,EAAaF,EAAK,GAAG,CAAGJ,EAAS,GAAG,CAACI,EAAK,GAAG,EAAI,KAAK,EAC5D,GAAI,CAACH,GAAc,CAACK,EAAY,CAC9BH,EAAI,IAAI,CArBc,mBAqBU,CAAC,KAAK,EAAEC,EAAK,EAAE,CAAC,kCAAkC,CAAC,EACnF,QACF,CACA,IAAMG,EAAc,CAAC,WAAW,EAAEH,EAAK,KAAK,CAAC,CAAC,EAAEA,EAAK,GAAG,CAAC,CAAC,EAAEA,EAAK,EAAE,CAAC,CAAC,CAE/DI,EAAY,AADEP,EAAW,QAAQ,GAAKK,EAAW,QAAQ,CAC/BA,EAAW,QAAQ,CAAGL,EAAW,QAAQ,CACnEQ,EAAY,CAChB,GAAIF,EACJ,MAAOH,EAAK,KAAK,CACjB,UAAWA,EAAK,KAAK,EAAI,GACzB,QAASA,EAAK,GAAG,EAAI,GACrB,MAAO,YACP,MAAO,EAEP,OAAQ,EACR,YAAa,GACb,QAAS,GACT,SAAUI,EACV,QAAS,GACT,WAAYmB,MAAM,OAAO,CAACvB,EAAK,UAAU,EAAIA,EAAK,UAAU,CAAC,EAAE,CAAGA,EAAK,UAAU,EAAI,GACrF,GAAGH,EAAW,GAAG,CAAG,CAAE,IAAKA,EAAW,GAAG,AAAC,EAAI,CAAC,CAAC,AAClD,EACAC,EAAW,IAAI,CAACO,GAChBL,EAAK,WAAW,CAAGG,EACnBH,EAAK,KAAK,CAAG,KAAK,EAClBA,EAAK,IAAI,CAAG,KAAK,EACjB,IAAMM,EAAiB,CACrB,GAAI,CAAC,EAAEN,EAAK,EAAE,CAAC,SAAS,CAAC,CACzB,MAAOA,EAAK,KAAK,CACjB,IAAKG,EACL,KAAM,SACN,aAAc,EAChB,EACMI,EAAmB,CACvB,GAAI,CAAC,EAAEP,EAAK,EAAE,CAAC,WAAW,CAAC,CAC3B,MAAOG,EACP,IAAKH,EAAK,GAAG,CACb,KAAM,SACN,aAAc,EAChB,EACAC,EAAgB,IAAI,CAACK,EAAgBC,EACvC,CACA,IAAMP,EAAW,IAAIH,EAAK,KAAK,IAAKC,EAAW,CACzCI,EAAW,IAAIL,EAAK,KAAK,IAAKI,EAAgB,CACpD,MAAO,CACL,GAAGJ,CAAI,CACP,MAAOG,EACP,MAAOE,CACT,CACF,CAKA,SAAS6B,EAAiBlC,CAAI,EAC5B,IAAMC,EAAKD,EAAK,CAAC,EAAI,EACfE,EAAKF,EAAK,CAAC,EAAI,EACfI,EAAQJ,EAAK,KAAK,EAAI,EACtBD,EAASC,EAAK,MAAM,EAAI,EAC9B,OAAOI,EAAQ,GAAKL,EAAS,EAAI,CAAEE,GAAAA,EAAIC,GAAAA,EAAI,KAAMiC,EAAmBlC,EAAIC,EAAIE,EAAOL,EAAQ,EAAI,KAAK,CACtG,CAEA,SAASqC,EAAkBpC,CAAI,EAC7B,GAAIA,EAAK,OAAO,CACd,OAEF,IAAMC,EAAWiC,EAAiBlC,GAClC,GAAKC,EAIL,MAAO,CACL,GAFSoC,OAAOrC,EAAK,EAAE,EAAI,IAG3B,GAAIC,EAAS,EAAE,CACf,GAAIA,EAAS,EAAE,CACf,KAAMA,EAAS,IAAI,AACrB,CACF,CAEA,SAASqC,EAAUtC,CAAC,CAAEC,CAAC,CAAEC,EA1Bf,IA0B4B,EACpC,OAAOU,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAGC,EAAE,CAAC,EAAIC,GAAWU,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAGC,EAAE,CAAC,EAAIC,CAChE,CAEA,SAASqC,EAAMvC,CAAC,CAAEC,CAAC,CAAEC,EA9BX,IA8BwB,EAChC,OAAOU,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAGC,EAAE,CAAC,EAAIC,CAC/B,CAEA,SAASsC,EAAMxC,CAAC,CAAEC,CAAC,CAAEC,EAlCX,IAkCwB,EAChC,OAAOU,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAGC,EAAE,CAAC,EAAIC,CAC/B,CAEA,SAASuC,EAAoBzC,CAAC,CAAEC,CAAC,CAAEC,EAtCzB,IAsCsC,EAC9C,OAAOsC,EAAMxC,EAAGC,EAAGC,IAAYU,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAGC,EAAE,CAAC,EAAIC,CACvD,CAEA,SAASwC,EAAkB1C,CAAC,CAAEC,CAAC,CAAEC,EA1CvB,IA0CoC,EAC5C,OAAOqC,EAAMvC,EAAGC,EAAGC,IAAYU,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAGC,EAAE,CAAC,EAAIC,CACvD,CAEA,SAASyC,EAAc3C,CAAE,CAAEC,CAAE,CAAEC,CAAE,CAAEE,CAAE,EACnC,OAAOQ,KAAK,GAAG,CACb,EACAA,KAAK,GAAG,CAACA,KAAK,GAAG,CAACZ,EAAIC,GAAKW,KAAK,GAAG,CAACV,EAAIE,IAAOQ,KAAK,GAAG,CAACA,KAAK,GAAG,CAACZ,EAAIC,GAAKW,KAAK,GAAG,CAACV,EAAIE,IAE3F,CAEA,SAASwC,EAA6B5C,CAAC,CAAEC,CAAC,CAAEC,EArDlC,IAqD+C,SACvD,AAAIF,EAAE,UAAU,EAAIC,EAAE,UAAU,EAAIuC,EAAMxC,EAAE,CAAC,CAAEC,EAAE,CAAC,CAAEC,GAC3CyC,EAAc3C,EAAE,CAAC,CAAC,CAAC,CAAEA,EAAE,CAAC,CAAC,CAAC,CAAEC,EAAE,CAAC,CAAC,CAAC,CAAEA,EAAE,CAAC,CAAC,CAAC,EAE7CD,EAAE,QAAQ,EAAIC,EAAE,QAAQ,EAAIsC,EAAMvC,EAAE,CAAC,CAAEC,EAAE,CAAC,CAAEC,GACvCyC,EAAc3C,EAAE,CAAC,CAAC,CAAC,CAAEA,EAAE,CAAC,CAAC,CAAC,CAAEC,EAAE,CAAC,CAAC,CAAC,CAAEA,EAAE,CAAC,CAAC,CAAC,EAE1C,CACT,CAEA,SAAS4C,EAA4B7C,CAAM,CAAEC,EA/DnC,IA+DgD,EACxD,IAAMC,EAAS,EAAE,CACjB,IAAK,IAAIE,EAAI,EAAGA,EAAIJ,EAAO,MAAM,CAAG,EAAGI,IAAK,CAC1C,IAAML,EAAIC,CAAM,CAACI,EAAE,CACbD,EAAIH,CAAM,CAACI,EAAI,EAAE,CACjBC,EAAaoC,EAAoB1C,EAAGI,EAAGF,GACvCK,EAAWoC,EAAkB3C,EAAGI,EAAGF,EACrCI,CAAAA,CAAAA,GAAcC,CAAO,GACvBJ,EAAO,IAAI,CAAC,CAAE,MAAOE,EAAGL,EAAAA,EAAGI,EAAAA,EAAGE,WAAAA,EAAYC,SAAAA,CAAS,EAEvD,CACA,OAAOJ,CACT,CAEA,SAAS4C,EAAqB9C,CAAM,CAAEC,EA7E5B,IA6EyC,EACjD,IAAMC,EAAW2C,EAA4B7C,EAAQC,GACjDG,EAAQ,EACZ,IAAK,IAAIJ,EAAI,EAAGA,EAAIE,EAAS,MAAM,CAAEF,IAC/BE,CAAQ,CAACF,EAAI,EAAE,CAAC,UAAU,GAAKE,CAAQ,CAACF,EAAE,CAAC,UAAU,EACvDI,IAGJ,OAAOA,CACT,CAEA,SAAS2C,EAAwB/C,CAAM,CAAEC,EAxF/B,IAwF4C,EACpD,IAAMC,EAAS,EAAE,CACjB,IAAK,IAAME,KAASJ,EAAQ,CAC1B,IAAMA,EAAOE,EAAO,MAAM,CAAG,EAAIA,CAAM,CAACA,EAAO,MAAM,CAAG,EAAE,CAAG,KAAK,CAC9D,CAACF,GAASsC,EAAUtC,EAAMI,EAAOH,IACnCC,EAAO,IAAI,CAAC,CAAE,EAAGE,EAAM,CAAC,CAAE,EAAGA,EAAM,CAAC,AAAC,EAEzC,CACA,OAAOF,CACT,CAEA,SAAS8C,EAA0BhD,CAAM,CAAEC,EAnGjC,IAmG8C,EACtD,GAAI,CAACD,GAAUA,AAAkB,IAAlBA,EAAO,MAAM,CAC1B,OAEF,GAAM,CAACE,EAAIE,EAAIL,EAAII,EAAG,CAAGH,SAEzB,AADcyC,EAAoBvC,EAAIE,EAAIH,IAAYyC,EAAkBtC,EAAIL,EAAIE,IAAYwC,EAAoB1C,EAAII,EAAIF,GAE/G,CAAE,KAAM,MAAOC,GAAAA,EAAIE,GAAAA,EAAIL,GAAAA,EAAII,GAAAA,CAAG,EAGhC,AADOuC,EAAkBxC,EAAIE,EAAIH,IAAYwC,EAAoBrC,EAAIL,EAAIE,IAAYyC,EAAkB3C,EAAII,EAAIF,GACvG,CAAE,KAAM,MAAOC,GAAAA,EAAIE,GAAAA,EAAIL,GAAAA,EAAII,GAAAA,CAAG,EAAI,KAAK,CACxD,CAEA,SAAS8C,EAAyBjD,CAAC,CAAEC,CAAC,CAAEC,CAAI,CAAEE,EAAS,CAAC,EACtD,IAAML,EAAUa,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAEC,EAAE,CAAC,EAC3BE,EAAUS,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAEC,EAAE,CAAC,EAC3BI,EAAUO,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAEC,EAAE,CAAC,EAC3BK,EAAUM,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAEC,EAAE,CAAC,EACjC,OAAOE,EAAUD,EAAK,IAAI,CAAGE,GAAUL,EAAUG,EAAK,KAAK,CAAGE,GAAUE,EAAUJ,EAAK,GAAG,CAAGE,GAAUC,EAAUH,EAAK,MAAM,CAAGE,CACjI,CAEA,SAAS8C,EAAgBlD,CAAK,CAAEC,CAAI,CAAEC,EAAS,CAAC,EAC9C,OAAOF,EAAM,CAAC,CAAGC,EAAK,IAAI,CAAGC,GAAUF,EAAM,CAAC,CAAGC,EAAK,KAAK,CAAGC,GAAUF,EAAM,CAAC,CAAGC,EAAK,GAAG,CAAGC,GAAUF,EAAM,CAAC,CAAGC,EAAK,MAAM,CAAGC,CACjI,CAEA,SAASiD,EAAiBnD,CAAK,CAAEC,CAAK,EACpC,OAAOD,EAAM,IAAI,EAAIC,EAAM,IAAI,EAAID,EAAM,KAAK,EAAIC,EAAM,KAAK,EAAID,EAAM,GAAG,EAAIC,EAAM,GAAG,EAAID,EAAM,MAAM,EAAIC,EAAM,MAAM,AACzH,CAEA,SAASmD,EAAapD,CAAC,CAAEC,CAAC,EACxB,OAAOD,EAAE,IAAI,CAAGC,EAAE,KAAK,EAAID,EAAE,KAAK,CAAGC,EAAE,IAAI,EAAID,EAAE,GAAG,CAAGC,EAAE,MAAM,EAAID,EAAE,MAAM,CAAGC,EAAE,GAAG,AACrF,CAEA,SAASoD,EAAYrD,CAAI,CAAEC,CAAM,EAC/B,MAAO,CACL,KAAMD,EAAK,IAAI,CAAGC,EAClB,MAAOD,EAAK,KAAK,CAAGC,EACpB,IAAKD,EAAK,GAAG,CAAGC,EAChB,OAAQD,EAAK,MAAM,CAAGC,CACxB,CACF,CAEA,SAASkC,EAAmBnC,CAAE,CAAEC,CAAE,CAAEC,CAAK,CAAEE,CAAM,EAC/C,MAAO,CACL,KAAMJ,EAAKE,EAAQ,EACnB,MAAOF,EAAKE,EAAQ,EACpB,IAAKD,EAAKG,EAAS,EACnB,OAAQH,EAAKG,EAAS,CACxB,CACF,CAEA,SAASkD,EAAiBtD,CAAI,EAC5B,OAAOkC,EAAiBlC,IAAO,IACjC,CAEA,SAASuD,EAAgBvD,CAAI,CAAEC,CAAI,EACjC,OAAQA,GACN,IAAK,MACH,MAAO,CAAE,EAAGD,EAAK,EAAE,CAAE,EAAGA,EAAK,IAAI,CAAC,GAAG,AAAC,CACxC,KAAK,SACH,MAAO,CAAE,EAAGA,EAAK,EAAE,CAAE,EAAGA,EAAK,IAAI,CAAC,MAAM,AAAC,CAC3C,KAAK,OACH,MAAO,CAAE,EAAGA,EAAK,IAAI,CAAC,IAAI,CAAE,EAAGA,EAAK,EAAE,AAAC,CACzC,KAAK,QACH,MAAO,CAAE,EAAGA,EAAK,IAAI,CAAC,KAAK,CAAE,EAAGA,EAAK,EAAE,AAAC,CAC5C,CACF,CAEA,SAASwD,EAAwBxD,CAAG,CAAEC,CAAO,CAAEC,CAAG,CAAEE,CAAO,CAAEL,CAAM,CAAEI,EAvK3D,IAuKwE,EAChF,IAAME,EAAOJ,AAAY,SAAZA,GAAsBA,AAAY,UAAZA,EAC7BK,EAAOF,AAAY,SAAZA,GAAsBA,AAAY,UAAZA,EACnC,GAAIC,GAAQC,EAAM,CAEhB,GADoBL,AAAY,UAAZA,GAAuBG,AAAY,SAAZA,GAAsBJ,EAAI,CAAC,CAAGE,EAAI,CAAC,EAAID,AAAY,SAAZA,GAAsBG,AAAY,UAAZA,GAAuBJ,EAAI,CAAC,CAAGE,EAAI,CAAC,CAC3H,CACf,GAAIsC,EAAMxC,EAAKE,EAAKC,GAClB,MAAO,CAACH,EAAKE,EAAI,CAEnB,IAAMD,EAAO,AAACD,CAAAA,EAAI,CAAC,CAAGE,EAAI,CAAC,AAAD,EAAK,EAC/B,MAAO,CAACF,EAAK,CAAE,EAAGC,EAAM,EAAGD,EAAI,CAAC,AAAC,EAAG,CAAE,EAAGC,EAAM,EAAGC,EAAI,CAAC,AAAC,EAAGA,EAAI,AACjE,CACA,GAAID,IAAYG,EAAS,CACvB,GAAIoC,EAAMxC,EAAKE,EAAKC,GAClB,OAEF,IAAMC,EAAOH,AAAY,SAAZA,EAAqBW,KAAK,GAAG,CAACZ,EAAI,CAAC,CAAEE,EAAI,CAAC,EAAIH,EAASa,KAAK,GAAG,CAACZ,EAAI,CAAC,CAAEE,EAAI,CAAC,EAAIH,EAC7F,MAAO,CAACC,EAAK,CAAE,EAAGI,EAAM,EAAGJ,EAAI,CAAC,AAAC,EAAG,CAAE,EAAGI,EAAM,EAAGF,EAAI,CAAC,AAAC,EAAGA,EAAI,AACjE,CACA,MACF,CACA,GAAI,CAACG,GAAQ,CAACC,EAAM,CAClB,GAAIL,IAAYG,EAAS,CACvB,GAAImC,EAAMvC,EAAKE,EAAKC,GAClB,OAEF,IAAMC,EAAOH,AAAY,QAAZA,EAAoBW,KAAK,GAAG,CAACZ,EAAI,CAAC,CAAEE,EAAI,CAAC,EAAIH,EAASa,KAAK,GAAG,CAACZ,EAAI,CAAC,CAAEE,EAAI,CAAC,EAAIH,EAC5F,MAAO,CAACC,EAAK,CAAE,EAAGA,EAAI,CAAC,CAAE,EAAGI,CAAK,EAAG,CAAE,EAAGF,EAAI,CAAC,CAAE,EAAGE,CAAK,EAAGF,EAAI,AACjE,CAEA,GAAI,CADYD,CAAAA,AAAY,WAAZA,GAAwBG,AAAY,QAAZA,GAAqBJ,EAAI,CAAC,CAAGE,EAAI,CAAC,EAAID,AAAY,QAAZA,GAAqBG,AAAY,WAAZA,GAAwBJ,EAAI,CAAC,CAAGE,EAAI,CAAC,AAAD,EAErI,OAEF,GAAIqC,EAAMvC,EAAKE,EAAKC,GAClB,MAAO,CAACH,EAAKE,EAAI,CAEnB,IAAMG,EAAO,AAACL,CAAAA,EAAI,CAAC,CAAGE,EAAI,CAAC,AAAD,EAAK,EAC/B,MAAO,CAACF,EAAK,CAAE,EAAGA,EAAI,CAAC,CAAE,EAAGK,CAAK,EAAG,CAAE,EAAGH,EAAI,CAAC,CAAE,EAAGG,CAAK,EAAGH,EAAI,AACjE,CACA,GAAIG,GAAQ,CAACC,EAAM,CACjB,IAAMP,EAAcE,AAAY,UAAZA,GAAuBC,EAAI,CAAC,CAAGF,EAAI,CAAC,EAAIC,AAAY,SAAZA,GAAsBC,EAAI,CAAC,CAAGF,EAAI,CAAC,CACzFG,EAAcC,AAAY,QAAZA,GAAqBJ,EAAI,CAAC,CAAGE,EAAI,CAAC,EAAIE,AAAY,WAAZA,GAAwBJ,EAAI,CAAC,CAAGE,EAAI,CAAC,CAC/F,OAAOH,GAAeI,EAAc,CAACH,EAAK,CAAE,EAAGE,EAAI,CAAC,CAAE,EAAGF,EAAI,CAAC,AAAC,EAAGE,EAAI,CAAG,KAAK,CAChF,CACA,IAAMK,EAAaN,AAAY,WAAZA,GAAwBC,EAAI,CAAC,CAAGF,EAAI,CAAC,EAAIC,AAAY,QAAZA,GAAqBC,EAAI,CAAC,CAAGF,EAAI,CAAC,CACxFQ,EAAaJ,AAAY,SAAZA,GAAsBJ,EAAI,CAAC,CAAGE,EAAI,CAAC,EAAIE,AAAY,UAAZA,GAAuBJ,EAAI,CAAC,CAAGE,EAAI,CAAC,CAC9F,OAAOK,GAAcC,EAAa,CAACR,EAAK,CAAE,EAAGA,EAAI,CAAC,CAAE,EAAGE,EAAI,CAAC,AAAC,EAAGA,EAAI,CAAG,KAAK,CAC9E,CAEA,SAASuD,EAAuBzD,CAAG,CAAEC,CAAI,CAAEC,CAAG,CAAEE,CAAK,EACnD,MAAOH,AAAS,SAATA,GAAmBA,AAAS,UAATA,EAAmB,CAACD,EAAK,CAAE,EAAGI,EAAO,EAAGJ,EAAI,CAAC,AAAC,EAAG,CAAE,EAAGI,EAAO,EAAGF,EAAI,CAAC,AAAC,EAAGA,EAAI,CAAG,CAACF,EAAK,CAAE,EAAGA,EAAI,CAAC,CAAE,EAAGI,CAAM,EAAG,CAAE,EAAGF,EAAI,CAAC,CAAE,EAAGE,CAAM,EAAGF,EAAI,AACtK,CAEA,SAASwD,EAAsB1D,CAAK,EAClC,IAAMC,EAA+B,IAAIgB,IACnCf,EAAgB,EAAE,CACxB,IAAK,IAAME,KAAQJ,EAAO,CACxB,GAAII,EAAK,WAAW,CAClB,SAEF,IAAMJ,EAAOoC,EAAkBhC,GAC1BJ,IAGLC,EAAa,GAAG,CAACD,EAAK,EAAE,CAAEA,GAC1BE,EAAc,IAAI,CAAC,CAAE,GAAIF,EAAK,EAAE,CAAE,KAAMA,EAAK,IAAI,AAAC,GACpD,CACA,MAAO,CAAEC,aAAAA,EAAcC,cAAAA,CAAc,CACvC,CAEA,SAASyD,EAAuB3D,CAAK,EACnC,IAAMC,EAAgB,EAAE,CAClBC,EAAiB,EAAE,CACzB,IAAK,IAAME,KAAQJ,EAAO,CACxB,IAAMA,EAAOoC,EAAkBhC,GAC/B,GAAI,CAACJ,EACH,SAEF,IAAMD,EAAQ,CAAE,GAAIC,EAAK,EAAE,CAAE,KAAMA,EAAK,IAAI,AAAC,CACzCI,CAAAA,EAAK,WAAW,CAClBF,EAAe,IAAI,CAACH,GAEpBE,EAAc,IAAI,CAACF,EAEvB,CACA,MAAO,CAAEE,cAAAA,EAAeC,eAAAA,CAAe,CACzC,CAEA,SAAS0D,EAAuB5D,CAAK,CAAE,CAAEC,kBAAAA,EAAoB,EAAI,CAAE,CAAG,CAAC,CAAC,EACtE,IAAMC,EAAS,EAAE,CACjB,IAAK,IAAME,KAAQJ,EAAO,CACxB,GAAII,EAAK,OAAO,EAAI,CAACH,GAAqBG,EAAK,WAAW,CACxD,SAEF,IAAMJ,EAAKI,EAAK,CAAC,EAAI,EACfL,EAAKK,EAAK,CAAC,EAAI,EACfD,EAAQC,EAAK,KAAK,EAAI,EACtBC,EAASD,EAAK,MAAM,EAAI,EAC9BF,EAAO,IAAI,CAAC,CACV,OAAQE,EAAK,EAAE,CACf,GAAG+B,EAAmBnC,EAAID,EAAII,EAAOE,EAAO,AAC9C,EACF,CACA,OAAOH,CACT,CAEA,SAAS2D,EAAoB7D,CAAI,CAAEC,CAAY,CAAEC,EAjRvC,IAiRoD,EAC5D,IAAME,EAAQJ,EAAK,KAAK,CAClBD,EAAQC,EAAK,GAAG,CACtB,GAAI,CAACI,GAAS,CAACL,EACb,OAEF,IAAMI,EAAUF,EAAa,GAAG,CAACG,GAC3BC,EAAUJ,EAAa,GAAG,CAACF,GACjC,GAAI,AAACI,GAAYE,EAGjB,MAAO,CACLD,MAAAA,EACAL,MAAAA,EACAI,QAAAA,EACAE,QAAAA,EACA,WAAYO,KAAK,GAAG,CAACT,EAAQ,EAAE,CAAGE,EAAQ,EAAE,EAAIH,EAChD,WAAYU,KAAK,GAAG,CAACT,EAAQ,EAAE,CAAGE,EAAQ,EAAE,EAAIH,CAClD,CACF,CAEA,SAAS4D,GAAmB9D,CAAC,CAAEC,CAAC,CAAEC,CAAK,CAAEE,EAAa,EAAE,CAAEL,EAAS,CAAC,EAClE,IAAK,IAAMI,KAASD,EAClB,IAAIE,EAAW,QAAQ,CAACD,EAAM,EAAE,GAG5B8C,EAAyBjD,EAAGC,EAAGE,EAAM,IAAI,CAAE,CAACJ,GAC9C,MAAO,GAGX,MAAO,EACT,CAEA,SAASgE,GAAwB/D,CAAE,CAAEC,CAAE,CAAEC,CAAE,CAAEE,CAAE,CAAEL,EAlTvC,IAkToD,CAAEI,EAAoB,IAAI,EACtF,IAAME,EAAMmC,EAAMxC,EAAIC,EAAIF,GACpBO,EAAMiC,EAAMvC,EAAIC,EAAIF,GACpBQ,EAAMiC,EAAMtC,EAAIE,EAAIL,GACpBS,EAAM+B,EAAMrC,EAAIE,EAAIL,GAC1B,GAAIM,GAAOE,GAAOD,GAAOE,GAGrB,CAAEH,CAAAA,GAAOC,CAAE,GAAM,CAAEC,CAAAA,GAAOC,CAAE,EAF9B,MAAO,GAKT,IAAMC,EAAQJ,EAAM,CAAE,EAAGL,EAAI,EAAGC,CAAG,EAAI,CAAE,EAAGC,EAAI,EAAGE,CAAG,EAChDM,EAAOJ,EAAM,CAAE,EAAGN,EAAI,EAAGC,CAAG,EAAI,CAAE,EAAGC,EAAI,EAAGE,CAAG,EAC/CO,EAAKF,EAAM,CAAC,CAAC,CAAC,CACdK,EAAMF,KAAK,GAAG,CAACH,EAAM,CAAC,CAAC,CAAC,CAAEA,EAAM,CAAC,CAAC,CAAC,EACnCM,EAAMH,KAAK,GAAG,CAACH,EAAM,CAAC,CAAC,CAAC,CAAEA,EAAM,CAAC,CAAC,CAAC,EACnCO,EAAKN,EAAK,CAAC,CAAC,CAAC,CACbQ,EAAMN,KAAK,GAAG,CAACF,EAAK,CAAC,CAAC,CAAC,CAAEA,EAAK,CAAC,CAAC,CAAC,EACjCS,EAAMP,KAAK,GAAG,CAACF,EAAK,CAAC,CAAC,CAAC,CAAEA,EAAK,CAAC,CAAC,CAAC,EACvC,GAAIM,EAAKF,GAAOE,EAAKD,GAAOJ,EAAKO,GAAOP,EAAKQ,EAC3C,MAAO,GAET,IAAMC,EAAuBR,KAAK,GAAG,CAACI,EAAKP,EAAM,CAAC,CAAC,CAAC,EAAIN,GAAqBS,KAAK,GAAG,CAACD,EAAKF,EAAM,CAAC,CAAC,CAAC,EAAIN,GAAqBS,KAAK,GAAG,CAACI,EAAKP,EAAM,CAAC,CAAC,CAAC,EAAIN,GAAqBS,KAAK,GAAG,CAACD,EAAKF,EAAM,CAAC,CAAC,CAAC,EAAIN,EAClMkB,EAAsBT,KAAK,GAAG,CAACI,EAAKN,EAAK,CAAC,CAAC,CAAC,EAAIP,GAAqBS,KAAK,GAAG,CAACD,EAAKD,EAAK,CAAC,CAAC,CAAC,EAAIP,GAAqBS,KAAK,GAAG,CAACI,EAAKN,EAAK,CAAC,CAAC,CAAC,EAAIP,GAAqBS,KAAK,GAAG,CAACD,EAAKD,EAAK,CAAC,CAAC,CAAC,EAAIP,EACnM,MAAO,CAAEiB,CAAAA,GAAwBC,CAAkB,CACrD,CAEA,SAAS2C,GAAwBhE,CAAE,CAAEC,CAAE,CAAEC,CAAE,CAAEE,CAAE,CAAEL,EA7UvC,IA6UoD,EAC5D,IAAMI,EAAMqC,EAAMxC,EAAIC,EAAIF,GACpBM,EAAMkC,EAAMvC,EAAIC,EAAIF,GACpBO,EAAMkC,EAAMtC,EAAIE,EAAIL,GACpBQ,EAAMgC,EAAMrC,EAAIE,EAAIL,UAC1B,AAAIM,GAAOE,GAAOgC,EAAMvC,EAAIE,EAAIH,GACvB4C,EAAc3C,EAAG,CAAC,CAAEC,EAAG,CAAC,CAAEC,EAAG,CAAC,CAAEE,EAAG,CAAC,EAAIL,IAE7CI,CAAAA,GAAOG,GAAOkC,EAAMxC,EAAIE,EAAIH,EAAO,GAC9B4C,EAAc3C,EAAG,CAAC,CAAEC,EAAG,CAAC,CAAEC,EAAG,CAAC,CAAEE,EAAG,CAAC,EAAIL,CAGnD,CAEA,SAASkE,GAA4BjE,CAAC,CAAEC,CAAC,CAAEC,CAAK,CAAEE,CAAW,CAAE,CAC7DL,QAAAA,EA5VQ,IA4VK,CACbI,oBAAAA,EAAsB,EAAK,CAC5B,CAAG,CAAC,CAAC,EACJ,IAAK,IAAME,KAASH,EAAO,CACzB,GAAIG,IAAUD,GAAeC,EAAM,YAAY,CAC7C,SAEF,IAAMH,EAASG,EAAM,MAAM,CAC3B,GAAI,AAACH,IAAUA,CAAAA,EAAO,MAAM,CAAG,GAG/B,IAAK,IAAIE,EAAI,EAAGA,EAAIF,EAAO,MAAM,CAAG,EAAGE,IAAK,CAC1C,IAAMC,EAAKH,CAAM,CAACE,EAAE,CACdE,EAAKJ,CAAM,CAACE,EAAI,EAAE,CACxB,IAAID,CAAAA,GAAuBmC,EAAUjC,EAAIC,EAAIP,EAAO,GAGhDgE,CAAAA,GAAwB/D,EAAGC,EAAGI,EAAIC,EAAIP,IAAYiE,GAAwBhE,EAAGC,EAAGI,EAAIC,EAAIP,EAAO,EACjG,MAAO,EAEX,CACF,CACA,MAAO,EACT,CAEA,SAASmE,GAAgClE,CAAE,CAAEC,CAAE,CAAEC,CAAE,CAAEE,CAAE,CAAEL,EArX/C,IAqX4D,EACpE,IAAMI,EAASqC,EAAMxC,EAAIC,EAAIF,GACvBM,EAAQkC,EAAMvC,EAAIC,EAAIF,GACtBO,EAASkC,EAAMtC,EAAIE,EAAIL,GACvBQ,EAAQgC,EAAMrC,EAAIE,EAAIL,GAC5B,GAAI,CAAEI,CAAAA,GAAUI,GAASF,GAASC,CAAK,EACrC,MAAO,GAET,IAAME,EAAQL,EAAS,CAAE,EAAGH,EAAI,EAAGC,CAAG,EAAI,CAAE,EAAGC,EAAI,EAAGE,CAAG,EACnDK,EAAON,EAAS,CAAE,EAAGD,EAAI,EAAGE,CAAG,EAAI,CAAE,EAAGJ,EAAI,EAAGC,CAAG,EAClDS,EAAKF,EAAM,CAAC,CAAC,CAAC,CACdG,EAAQC,KAAK,GAAG,CAACJ,EAAM,CAAC,CAAC,CAAC,CAAEA,EAAM,CAAC,CAAC,CAAC,EACrCM,EAAQF,KAAK,GAAG,CAACJ,EAAM,CAAC,CAAC,CAAC,CAAEA,EAAM,CAAC,CAAC,CAAC,EACrCO,EAAKN,EAAK,CAAC,CAAC,CAAC,CACbO,EAAQJ,KAAK,GAAG,CAACH,EAAK,CAAC,CAAC,CAAC,CAAEA,EAAK,CAAC,CAAC,CAAC,EACnCS,EAAQN,KAAK,GAAG,CAACH,EAAK,CAAC,CAAC,CAAC,CAAEA,EAAK,CAAC,CAAC,CAAC,EACzC,OAAOM,EAAKJ,EAAQZ,GAAWgB,EAAKD,EAAQf,GAAWW,EAAKM,EAAQjB,GAAWW,EAAKQ,EAAQnB,CAC9F,CAEA,SAASoE,GAAgBnE,CAAK,CAAEC,CAAC,CAAEC,CAAC,EAClC,IAAME,EAAKQ,KAAK,GAAG,CAACX,EAAGC,GACjBH,EAAKa,KAAK,GAAG,CAACX,EAAGC,GACvB,OAAOF,EAAQI,EA3YP,MA2YmBJ,EAAQD,EA3Y3B,IA4YV,CAEA,SAASqE,GAAwBpE,CAAI,CAAEC,CAAG,CAAEC,CAAI,SAC9C,AAAIqC,EAAMvC,EAAMC,IAAQsC,EAAMtC,EAAKC,GAC1BiE,GAAgBlE,EAAI,CAAC,CAAED,EAAK,CAAC,CAAEE,EAAK,CAAC,IAE1CsC,CAAAA,EAAMxC,EAAMC,IAAQuC,EAAMvC,EAAKC,EAAI,GAC9BiE,GAAgBlE,EAAI,CAAC,CAAED,EAAK,CAAC,CAAEE,EAAK,CAAC,CAGhD,CAEA,SAASmE,GAAqBrE,CAAM,EAClC,IAAIC,EAAU,GACRC,EAAM,EAAE,CACd,IAAK,IAAIE,EAAI,EAAGA,EAAIJ,EAAO,MAAM,CAAEI,IAAK,CACtC,IAAML,EAAOG,CAAG,CAACA,EAAI,MAAM,CAAG,EAAE,CAC1BC,EAAMH,CAAM,CAACI,EAAE,CACfC,EAAOD,EAAI,EAAIJ,EAAO,MAAM,CAAGA,CAAM,CAACI,EAAI,EAAE,CAAG,KAAK,EAC1D,GAAIL,GAAQM,EAAM,CAChB,GAAIiC,EAAUvC,EAAMM,GAAO,CACzBD,IACAH,EAAU,GACV,QACF,CACA,GAAImE,GAAwBrE,EAAMI,EAAKE,GAAO,CAC5CJ,EAAU,GACV,QACF,CACF,CACAC,EAAI,IAAI,CAACC,EACX,CACA,MAAO,CAAE,OAAQD,EAAKD,QAAAA,CAAQ,CAChC,CAEA,SAASqE,GAAsBtE,CAAG,EAChC,IAAMC,EAAU,CAACD,CAAG,CAAC,EAAE,CAAC,CACxB,IAAK,IAAIE,EAAI,EAAGA,EAAIF,EAAI,MAAM,CAAEE,IAAK,CACnC,IAAME,EAAOH,CAAO,CAACA,EAAQ,MAAM,CAAG,EAAE,CAClCF,EAAOC,CAAG,CAACE,EAAE,CACnB,GAAI,CAACqC,EAAMnC,EAAML,IAAS,CAACyC,EAAMpC,EAAML,GAAO,CAC5C,IAAMC,EAAWC,EAAQ,MAAM,EAAI,EAAIA,CAAO,CAACA,EAAQ,MAAM,CAAG,EAAE,CAAG,KAAK,EAEpEC,EAAS,AADUF,GAAWuC,EAAMvC,EAAUI,GAClB,CAAE,EAAGA,EAAK,CAAC,CAAE,EAAGL,EAAK,CAAC,AAAC,EAAI,CAAE,EAAGA,EAAK,CAAC,CAAE,EAAGK,EAAK,CAAC,AAAC,EACpFH,EAAQ,IAAI,CAACC,EACf,CACAD,EAAQ,IAAI,CAACF,EACf,CACA,IAAMG,EAAU,EAAE,CAClB,IAAK,IAAMF,KAAKC,EAAS,CACvB,IAAMA,EAAOC,CAAO,CAACA,EAAQ,MAAM,CAAG,EAAE,AACpC,CAACD,GAASqC,EAAUrC,EAAMD,IAC5BE,EAAQ,IAAI,CAACF,EAEjB,CACA,OAAOE,CACT,CAEA,SAASqE,GAAiBvE,CAAG,EAC3B,GAAIA,EAAI,MAAM,CAAG,EACf,OAAOA,EAET,IAAIC,EAAO,IAAID,EAAI,CACnB,IAAK,IAAIA,EAAQ,EAAGA,EAAQ,GAAIA,IAAS,CACvC,IAAMA,EAASqE,GAAqBpE,GAEpC,GADAA,EAAOD,EAAO,MAAM,CAChB,CAACA,EAAO,OAAO,CACjB,KAEJ,CACA,OAAOC,CACT,CAOA,SAASuE,GAAmBxE,CAAI,CAAEC,CAAW,CAAEC,CAAS,EAEtD,GAAI,AADcF,EACJ,YAAY,EAAI,CAAC,AADbA,EACuB,MAAM,EAAI,AADjCA,EAC2C,MAAM,CAAC,MAAM,CAAGE,EAC3E,OAEF,IAAME,EAAM,AAJMJ,EAII,KAAK,CAAGC,EAAY,GAAG,CAAC,AAJ5BD,EAIsC,KAAK,EAAI,KAAK,EAChED,EAAM,AALMC,EAKI,GAAG,CAAGC,EAAY,GAAG,CAAC,AAL1BD,EAKoC,GAAG,EAAI,KAAK,EAClE,MAAO,CACL,KAPgBA,EAQhB,OAAQ,AARQA,EAQE,MAAM,CACxB,QAASI,EAAMkD,EAAiBlD,GAAO,KAAK,EAC5C,QAASL,EAAMuD,EAAiBvD,GAAO,KAAK,CAC9C,CACF,CAEA,SAAS0E,GAAkBzE,CAAO,CAAEC,CAAM,CAAEC,CAAC,EAC3C,GAAIsC,EAAMxC,EAASC,EAnBV,MAqBP,MAAO,CAAE,EADCD,EAAQ,CAAC,CAAGE,EAAE,IAAI,CAAGA,EAAE,IAAI,CAAGA,EAAE,KAAK,CACnC,EAAGF,EAAQ,CAAC,AAAC,EAE3B,GAAIuC,EAAMvC,EAASC,EAvBV,MAuByB,CAChC,IAAMA,EAAID,EAAQ,CAAC,CAAGE,EAAE,GAAG,CAAGA,EAAE,GAAG,CAAGA,EAAE,MAAM,CAC9C,MAAO,CAAE,EAAGF,EAAQ,CAAC,CAAEC,EAAAA,CAAE,CAC3B,CACA,MAAO,CACL,EAAGW,KAAK,GAAG,CAACV,EAAE,KAAK,CAAEU,KAAK,GAAG,CAACV,EAAE,IAAI,CAAEF,EAAQ,CAAC,GAC/C,EAAGY,KAAK,GAAG,CAACV,EAAE,MAAM,CAAEU,KAAK,GAAG,CAACV,EAAE,GAAG,CAAEF,EAAQ,CAAC,EACjD,CACF,CAEA,SAAS0E,GAAa1E,CAAM,CAAEC,CAAI,CAAEC,CAAO,EACzC,IAAME,EAAOF,EAAU,EAAI,GACvBH,EAAeG,EAAU,EAAIF,EAAO,MAAM,CAAG,EACjD,KAAOD,GAAgB,GAAKA,EAAeC,EAAO,MAAM,EAAIkD,EAAgBlD,CAAM,CAACD,EAAa,CAAEE,EAnCnF,KAoCbF,GAAgBK,EAElB,GAAIL,EAAe,GAAKA,GAAgBC,EAAO,MAAM,CACnD,OAAOA,EAET,IAAMG,EAAcJ,EAAeK,EACnC,GAAID,EAAc,GAAKA,GAAeH,EAAO,MAAM,CACjD,OAAOA,EAET,IAAMK,EAAQoE,GAAkBzE,CAAM,CAACD,EAAa,CAAEC,CAAM,CAACG,EAAY,CAAEF,GAC3E,OAAOC,EAAU,CAACG,KAAUL,EAAO,KAAK,CAACD,GAAc,CAAG,IAAIC,EAAO,KAAK,CAAC,EAAGD,EAAe,GAAIM,EAAM,AACzG,CAEA,SAASsE,GAAkC3E,CAAK,CAAEC,CAAW,EAC3D,IAAK,IAAMC,KAAQF,EAAO,CACxB,IAAMA,EAAUwE,GAAmBtE,EAAMD,EAAa,GACtD,GAAI,CAACD,EACH,SAEF,IAAII,EAAO,IAAIJ,EAAQ,MAAM,CAAC,AAC1BA,CAAAA,EAAQ,OAAO,EACjBI,CAAAA,EAAOsE,GAAatE,EAAMJ,EAAQ,OAAO,CAAE,GAAI,EAE7CA,EAAQ,OAAO,EACjBI,CAAAA,EAAOsE,GAAatE,EAAMJ,EAAQ,OAAO,CAAE,GAAK,EAGlDI,EAAOwE,GADPxE,EAAOmE,GAAiBD,GAAsBlE,IACMJ,EAAQ,OAAO,CAAEA,EAAQ,OAAO,EACpFA,EAAQ,IAAI,CAAC,MAAM,CAAGuE,GAAiBD,GAAsBlE,GAC/D,CACF,CAEA,SAASyE,GAAuB7E,CAAK,CAAEC,CAAQ,CAAEC,CAAC,CAAEE,EAAkB,EAAK,EACzE,GAAIoC,EAAMxC,EAAOC,EAtER,MAsEyB,CAChC,GAAIA,EAAS,CAAC,CAAGC,EAAE,GAAG,CAvEf,MAuE0BD,EAAS,CAAC,CAAGC,EAAE,MAAM,CAvE/C,KAwEL,OAAOD,EAET,GAAIG,EAAiB,CACnB,GAAIJ,EAAM,CAAC,CAAGE,EAAE,IAAI,CA3Ef,KA4EH,MAAO,CAAE,EAAGA,EAAE,IAAI,CAAE,EAAGF,EAAM,CAAC,AAAC,EAEjC,GAAIA,EAAM,CAAC,CAAGE,EAAE,KAAK,CA9EhB,KA+EH,MAAO,CAAE,EAAGA,EAAE,KAAK,CAAE,EAAGF,EAAM,CAAC,AAAC,CAEpC,CAEA,MAAO,CAAE,EAAG,AADGY,KAAK,GAAG,CAACX,EAAS,CAAC,CAAGC,EAAE,IAAI,GAAKU,KAAK,GAAG,CAACX,EAAS,CAAC,CAAGC,EAAE,KAAK,EACxDA,EAAE,IAAI,CAAGA,EAAE,KAAK,CAAE,EAAGF,EAAM,CAAC,AAAC,CACpD,CACA,GAAIuC,EAAMvC,EAAOC,EArFR,MAqFyB,CAChC,GAAIA,EAAS,CAAC,CAAGC,EAAE,IAAI,CAtFhB,MAsF2BD,EAAS,CAAC,CAAGC,EAAE,KAAK,CAtF/C,KAuFL,OAAOD,EAET,GAAIG,EAAiB,CACnB,GAAIJ,EAAM,CAAC,CAAGE,EAAE,GAAG,CA1Fd,KA2FH,MAAO,CAAE,EAAGF,EAAM,CAAC,CAAE,EAAGE,EAAE,GAAG,AAAC,EAEhC,GAAIF,EAAM,CAAC,CAAGE,EAAE,MAAM,CA7FjB,KA8FH,MAAO,CAAE,EAAGF,EAAM,CAAC,CAAE,EAAGE,EAAE,MAAM,AAAC,CAErC,CACA,IAAMH,EAAQa,KAAK,GAAG,CAACX,EAAS,CAAC,CAAGC,EAAE,GAAG,GAAKU,KAAK,GAAG,CAACX,EAAS,CAAC,CAAGC,EAAE,MAAM,EAC5E,MAAO,CAAE,EAAGF,EAAM,CAAC,CAAE,EAAGD,EAAQG,EAAE,GAAG,CAAGA,EAAE,MAAM,AAAC,CACnD,CACA,OAAOD,CACT,CAEA,SAAS6E,GAAsB9E,CAAM,CAAEC,CAAa,CAAEC,CAAI,EACxD,IAAME,EAAWJ,CAAM,CAACC,EAAc,CACtC,IAAK,IAAIF,EAAQE,EAAgBC,EAAMH,GAAS,GAAKA,EAAQC,EAAO,MAAM,CAAED,GAASG,EAAM,CACzF,IAAMD,EAAYD,CAAM,CAACD,EAAM,CAC/B,GAAI,CAACuC,EAAUrC,EAAWG,EA3GnB,MA4GL,OAAOH,CAEX,CACA,OAAOD,CAAM,CAACC,EAAgBC,EAAK,AACrC,CAEA,SAAS6E,GAAqB/E,CAAG,CAAEC,CAAG,EACpC,IAAMC,EAAKF,EAjHU,EAkHfI,EAAKH,EAlHU,EAmHrB,OAAOC,GAAME,EAAK,CAAEF,GAAAA,EAAIE,GAAAA,CAAG,EAAI,CAAE,GAAI,AAACJ,CAAAA,EAAMC,CAAE,EAAK,EAAG,GAAI,AAACD,CAAAA,EAAMC,CAAE,EAAK,CAAE,CAC5E,CAEA,SAAS+E,GAAuBhF,CAAK,CAAEC,CAAG,CAAEC,CAAG,EAC7C,GAAM,CAAEE,GAAAA,CAAE,CAAEL,GAAAA,CAAE,CAAE,CAAGgF,GAAqB9E,EAAKC,GAC7C,OAAOU,KAAK,GAAG,CAACb,EAAIa,KAAK,GAAG,CAACR,EAAIJ,GACnC,CAEA,SAASiF,GAAgBjF,CAAM,EAC7B,IAAMC,EAAKW,KAAK,GAAG,IAAIZ,EAAO,GAAG,CAAC,AAACA,GAAUA,EAAM,EAAE,GAC/CE,EAAKU,KAAK,GAAG,IAAIZ,EAAO,GAAG,CAAC,AAACA,GAAUA,EAAM,EAAE,GACrD,IAAIC,CAAAA,EAAKC,CAAC,EAGV,MAAO,CAAED,GAAAA,EAAIC,GAAAA,CAAG,CAClB,CAEA,SAASgF,GAAsBlF,CAAC,CAAEC,CAAI,EACpC,MAAOA,AAAS,SAATA,GAAmBA,AAAS,UAATA,EAAmB8E,GAAqB/E,EAAE,GAAG,CAAEA,EAAE,MAAM,EAAI+E,GAAqB/E,EAAE,IAAI,CAAEA,EAAE,KAAK,CAC3H,CAEA,SAASmF,GAAuBnF,CAAQ,CAAEC,CAAQ,CAAEC,CAAC,EACnD,IAAME,EAAUJ,EAAS,CAAC,EAAIE,EAAE,GAAG,CA3I1B,MA2IqCF,EAAS,CAAC,EAAIE,EAAE,MAAM,CA3I3D,KA4IHH,EAAUC,EAAS,CAAC,EAAIE,EAAE,IAAI,CA5I3B,MA4IsCF,EAAS,CAAC,EAAIE,EAAE,KAAK,CA5I3D,KA6IT,GAAIsC,EAAMxC,EAAUC,EA7IX,OA6I8BG,EAAS,CAC9C,GAAIQ,AA9IG,KA8IHA,KAAK,GAAG,CAACZ,EAAS,CAAC,CAAGE,EAAE,IAAI,EAC9B,MAAO,OAET,GAAIU,AAjJG,KAiJHA,KAAK,GAAG,CAACZ,EAAS,CAAC,CAAGE,EAAE,KAAK,EAC/B,MAAO,OAEX,CACA,GAAIqC,EAAMvC,EAAUC,EArJX,OAqJ8BF,EAAS,CAC9C,GAAIa,AAtJG,KAsJHA,KAAK,GAAG,CAACZ,EAAS,CAAC,CAAGE,EAAE,GAAG,EAC7B,MAAO,MAET,GAAIU,AAzJG,KAyJHA,KAAK,GAAG,CAACZ,EAAS,CAAC,CAAGE,EAAE,MAAM,EAChC,MAAO,QAEX,CAEF,CAEA,SAASkF,GAAiBpF,CAAI,EAC5B,MAAOA,AAAS,SAATA,GAAmBA,AAAS,UAATA,CAC5B,CAEA,SAASqF,GAAuBrF,CAAK,CAAEC,CAAG,CAAEC,CAAO,CAAEE,CAAO,CAAEL,CAAU,EACtE,IAAMI,EAAS,EAAE,CACXE,EAAUH,EAAUiF,GAAuBnF,EAAOC,EAAKC,GAAW,KAAK,EACvEI,EAAUF,EAAU+E,GAAuBlF,EAAKD,EAAOI,GAAW,KAAK,EAO7E,OANIF,GAAWG,GAAW+E,GAAiB/E,KAAaN,GACtDI,EAAO,IAAI,CAAC+E,GAAsBhF,EAASG,IAEzCD,GAAWE,GAAW8E,GAAiB9E,KAAaP,GACtDI,EAAO,IAAI,CAAC+E,GAAsB9E,EAASE,IAEtCH,EAAO,MAAM,CAAG,EAAI8E,GAAgB9E,GAAU,KAAK,CAC5D,CAEA,SAASmF,GAAgCtF,CAAK,CAAEC,CAAG,CAAEC,CAAO,CAAEE,CAAO,CAAEL,CAAU,EAC/E,IAAMI,EAAQkF,GAAuBrF,EAAOC,EAAKC,EAASE,EAASL,GACnE,GAAI,CAACI,EACH,OAEF,IAAME,EAAUN,EAAaC,EAAM,CAAC,CAAGA,EAAM,CAAC,CACxCM,EAAOM,KAAK,GAAG,CAACT,EAAM,EAAE,CAAES,KAAK,GAAG,CAACT,EAAM,EAAE,CAAEE,IACnD,IAAIO,CAAAA,AAxLK,KAwLLA,KAAK,GAAG,CAACN,EAAOD,EAAc,EAGlC,OAAON,EAAa,CAClB,CAAE,EAAGC,EAAM,CAAC,CAAE,EAAGM,CAAK,EACtB,CAAE,EAAGL,EAAI,CAAC,CAAE,EAAGK,CAAK,EACrB,CAAG,CACF,CAAE,EAAGA,EAAM,EAAGN,EAAM,CAAC,AAAC,EACtB,CAAE,EAAGM,EAAM,EAAGL,EAAI,CAAC,AAAC,EACrB,AACH,CAEA,SAAS2E,GAAuC5E,CAAM,CAAEC,CAAO,CAAEC,CAAO,EACtE,GAAIF,AAAkB,IAAlBA,EAAO,MAAM,CACf,OAAOA,EAET,GAAM,CAACI,EAAOL,EAAI,CAAGC,SACrB,AAAIwC,EAAMpC,EAAOL,EAzMR,MA0MAuF,GAAgClF,EAAOL,EAAKE,EAASC,EAAS,KAASF,EAE5EuC,EAAMnC,EAAOL,EA5MR,MA6MAuF,GAAgClF,EAAOL,EAAKE,EAASC,EAAS,KAAUF,EAE1EA,CACT,CAEA,SAASuF,GAAsBvF,CAAQ,CAAEC,CAAC,CAAEC,CAAI,EAC9C,OAAOkF,GAAiBlF,GAAQ,CAAE,EAAGF,EAAS,CAAC,CAAE,EAAGgF,GAAuBhF,EAAS,CAAC,CAAEC,EAAE,GAAG,CAAEA,EAAE,MAAM,CAAE,EAAI,CAAE,EAAG+E,GAAuBhF,EAAS,CAAC,CAAEC,EAAE,IAAI,CAAEA,EAAE,KAAK,EAAG,EAAGD,EAAS,CAAC,AAAC,CACtL,CAEA,SAASwF,GAAyBxF,CAAM,CAAEC,CAAa,CAAEC,CAAI,CAAEE,CAAQ,CAAEL,CAAQ,CAAEI,CAAkB,EACnG,IAAME,EAAOL,EAAO,GAAG,CAAC,AAACA,GAAW,EAAE,GAAGA,CAAK,AAAC,IAC/C,IAAK,IAAIM,EAAQL,EAAeK,GAAS,GAAKA,EAAQN,EAAO,MAAM,CAAEM,GAASJ,EAAM,CAClF,IAAMD,EAAQD,CAAM,CAACM,EAAM,CAC3B,GAAIH,GAAsB,CAACqC,EAAMvC,EAAOG,EA1NjC,OA6NH,CAACD,GAAsB,CAACoC,EAAMtC,EAAOG,EA7NlC,MA2NL,MAKED,EACFE,CAAI,CAACC,EAAM,CAAC,CAAC,CAAGP,EAAS,CAAC,CAE1BM,CAAI,CAACC,EAAM,CAAC,CAAC,CAAGP,EAAS,CAAC,AAE9B,CACA,OAAOM,CACT,CAEA,SAASoF,GAA8BzF,CAAM,CAAEC,CAAC,CAAEC,CAAO,EACvD,GAAIF,EAAO,MAAM,CAAG,EAClB,OAAOA,EAET,IAAMI,EAAgBF,EAAU,EAAIF,EAAO,MAAM,CAAG,EAC9CD,EAAOG,EAAU,EAAI,GACrBC,EAAWH,CAAM,CAACI,EAAc,CAChCC,EAAWyE,GAAsB9E,EAAQI,EAAeL,GAC9D,GAAI,CAACM,EACH,OAAOL,EAET,IAAMM,EAAO6E,GAAuBhF,EAAUE,EAAUJ,GACxD,GAAI,CAACK,EACH,OAAON,EAET,IAAMO,EAAqB6E,GAAiB9E,GACtCE,EAAW+E,GAAsBpF,EAAUF,EAAGK,UACpD,AAAIgC,EAAUnC,EAAUK,EA1Pf,MA2PAR,EAEFwF,GACLxF,EACAI,EACAL,EACAI,EACAK,EACAD,EAEJ,CAEA,SAASmF,GAAqB1F,CAAC,CAAEC,CAAC,CAAEC,CAAC,EACnC,IAAME,EAAUQ,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAEC,EAAE,CAAC,GAAKC,EAAE,IAAI,CAxQnC,MAwQ8CU,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAEC,EAAE,CAAC,GAAKC,EAAE,KAAK,CAxQ3E,KAyQHH,EAAUa,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAEC,EAAE,CAAC,GAAKC,EAAE,GAAG,CAzQlC,MAyQ6CU,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAEC,EAAE,CAAC,GAAKC,EAAE,MAAM,CAzQ3E,YA0QT,AAAIU,AA1QK,KA0QLA,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAGE,EAAE,GAAG,GAAYU,AA1Q3B,KA0Q2BA,KAAK,GAAG,CAACX,EAAE,CAAC,CAAGC,EAAE,GAAG,GAAYE,EAC3D,MAELQ,AA7QK,KA6QLA,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAGE,EAAE,MAAM,GAAYU,AA7Q9B,KA6Q8BA,KAAK,GAAG,CAACX,EAAE,CAAC,CAAGC,EAAE,MAAM,GAAYE,EACjE,SAELQ,AAhRK,KAgRLA,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAGE,EAAE,IAAI,GAAYU,AAhR5B,KAgR4BA,KAAK,GAAG,CAACX,EAAE,CAAC,CAAGC,EAAE,IAAI,GAAYH,EAC7D,OAELa,AAnRK,KAmRLA,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAGE,EAAE,KAAK,GAAYU,AAnR7B,KAmR6BA,KAAK,GAAG,CAACX,EAAE,CAAC,CAAGC,EAAE,KAAK,GAAYH,EAC/D,cAGX,CAEA,SAAS4F,GAAc3F,CAAI,CAAEC,CAAI,CAAEC,CAAE,CAAEE,CAAC,EACtC,OAAQJ,GACN,IAAK,MACH,OAAOuC,EAAMtC,EAAMC,EA5Rd,OA4R2BA,EAAG,CAAC,CAAGE,EAAE,GAAG,CA5RvC,IA6RP,KAAK,SACH,OAAOmC,EAAMtC,EAAMC,EA9Rd,OA8R2BA,EAAG,CAAC,CAAGE,EAAE,MAAM,CA9R1C,IA+RP,KAAK,OACH,OAAOoC,EAAMvC,EAAMC,EAhSd,OAgS2BA,EAAG,CAAC,CAAGE,EAAE,IAAI,CAhSxC,IAiSP,KAAK,QACH,OAAOoC,EAAMvC,EAAMC,EAlSd,OAkS2BA,EAAG,CAAC,CAAGE,EAAE,KAAK,CAlSzC,IAmST,CACF,CAEA,SAASwF,GAAsB5F,CAAM,CAAEC,CAAC,CAAEC,CAAO,EAC/C,GAAIF,EAAO,MAAM,CAAG,EAClB,OAAOA,EAET,GAAIE,EAAS,CACX,IAAMA,EAAQwF,GAAqB1F,CAAM,CAAC,EAAE,CAAEA,CAAM,CAAC,EAAE,CAAEC,UACzD,AAAIC,GAASyF,GAAczF,EAAOF,CAAM,CAAC,EAAE,CAAEA,CAAM,CAAC,EAAE,CAAEC,GAC/CD,EAAO,KAAK,CAAC,GAEfA,CACT,CACA,IAAMI,EAAOJ,EAAO,MAAM,CAAG,EACvBD,EAAO2F,GAAqB1F,CAAM,CAACI,EAAO,EAAE,CAAEJ,CAAM,CAACI,EAAK,CAAEH,UAClE,AAAIF,GAAQ4F,GAAc5F,EAAMC,CAAM,CAACI,EAAO,EAAE,CAAEJ,CAAM,CAACI,EAAO,EAAE,CAAEH,GAC3DD,EAAO,KAAK,CAAC,EAAGI,GAElBJ,CACT,CAEA,SAAS6F,GAAyB7F,CAAM,CAAEC,CAAO,CAAEC,CAAO,EACxD,IAAIE,EAAOJ,EACX,GAAIC,EAAS,CACX,IAAMD,EAAW8E,GAAsB1E,EAAM,EAAG,GAChD,GAAIJ,EAAU,CACZ,IAAME,EAAU2E,GAAuB7E,EAAUI,CAAI,CAAC,EAAE,CAAEH,EACtDC,CAAAA,IAAYE,CAAI,CAAC,EAAE,EACrBA,CAAAA,EAAO,CAACF,KAAYE,EAAK,KAAK,CAAC,GAAG,AAAD,CAErC,CACAA,EAAOwF,GAAsBxF,EAAMH,EAAS,GAC9C,CACA,GAAIC,EAAS,CACX,IAAMF,EAAOI,EAAK,MAAM,CAAG,EACrBH,EAAW6E,GAAsB1E,EAAMJ,EAAM,IACnD,GAAIC,EAAU,CACZ,IAAMF,EAAU8E,GAAuB5E,EAAUG,CAAI,CAACJ,EAAK,CAAEE,EAAS,GAClEH,CAAAA,IAAYK,CAAI,CAACJ,EAAK,EACxBI,CAAAA,EAAO,IAAIA,EAAK,KAAK,CAAC,EAAGJ,GAAOD,EAAQ,AAAD,CAE3C,CACAK,EAAOwF,GAAsBxF,EAAMF,EAAS,GAC9C,CACA,IAAMH,EAAkB6E,GAAuCxE,EAAMH,EAASC,UAC9E,AAAIH,IAAoBK,GAAQA,AAAgB,IAAhBA,EAAK,MAAM,CAClCL,GAELE,GACFG,CAAAA,EAAOqF,GAA8BrF,EAAMH,EAAS,GAAI,EAEtDC,GACFE,CAAAA,EAAOqF,GAA8BrF,EAAMF,EAAS,GAAK,EAEpDE,EACT,CAEA,SAAS0F,GAAgC9F,CAAK,CAAEC,CAAW,EACzD,IAAK,IAAMC,KAAQF,EAAO,CACxB,IAAMA,EAAUwE,GAAmBtE,EAAMD,EAAa,GACtD,GAAI,CAACD,EACH,SAGF,IAAMI,EAASyF,GADD9C,EAAwB/C,EAAQ,MAAM,CAnW7C,MAoWwCA,EAAQ,OAAO,CAAEA,EAAQ,OAAO,EAC/E,GAAII,EAAO,MAAM,CAAG,EAAG,CACrBJ,EAAQ,IAAI,CAAC,MAAM,CAAGI,EACtB,QACF,CACA,IAAML,EAAa,CACjBK,CAAM,CAAC,EAAE,CACT,CAAE,GAAGA,CAAM,CAAC,EAAE,AAAC,KACZA,EAAO,KAAK,CAAC,EAAG,IACnBA,CAAM,CAACA,EAAO,MAAM,CAAG,EAAE,CACzB,CAAE,GAAGA,CAAM,CAACA,EAAO,MAAM,CAAG,EAAE,AAAC,EAChC,AACDJ,CAAAA,EAAQ,IAAI,CAAC,MAAM,CAAGD,CACxB,CACF,CAIA,SAASgG,GAAa/F,CAAK,EACzB,OAAO,IAAIiB,IAAIjB,EAAM,GAAG,CAAC,AAACA,GAAS,CAACA,EAAK,EAAE,CAAEA,EAAK,EACpD,CAEA,SAASgG,GAAuBhG,CAAI,CAAEC,CAAQ,EAC5C,IAAIC,EAAWF,EAAK,QAAQ,CACxBI,EAAkB,KACtB,KAAOF,GAAU,CACf,IAAMF,EAASC,EAAS,GAAG,CAACC,GAC5B,GAAI,CAACF,GAAQ,QACX,MAEFI,EAAkBJ,EAAO,EAAE,CAC3BE,EAAWF,EAAO,QAAQ,AAC5B,CACA,OAAOI,CACT,CAEA,SAAS6F,GAAWjG,CAAK,CAAEC,CAAQ,EACjC,IAAIC,EAAQ,EACRE,EAAWJ,EAAM,QAAQ,CAC7B,KAAOI,GAAU,CACf,IAAMJ,EAASC,EAAS,GAAG,CAACG,GAC5B,GAAI,CAACJ,GAAQ,QACX,KAEFE,CAAAA,IACAE,EAAWJ,EAAO,QAAQ,AAC5B,CACA,OAAOE,CACT,CAEA,SAASgG,GAAkBlG,CAAQ,EACjC,IAAIC,EAAO,IACPC,EAAO,CAAC,IACRE,EAAO,IACPL,EAAO,CAAC,IACZ,IAAK,IAAMI,KAASH,EAAU,CAC5B,IAAMA,EAAKG,EAAM,CAAC,CACZE,EAAKF,EAAM,CAAC,CAClB,GAAI,AAAc,UAAd,OAAOH,GAAmB,AAAc,UAAd,OAAOK,EACnC,SAEF,IAAMC,EAAIH,EAAM,KAAK,EAAI,EACnBI,EAAIJ,EAAM,MAAM,EAAI,EAC1BF,EAAOW,KAAK,GAAG,CAACX,EAAMD,EAAKM,EAAI,GAC/BJ,EAAOU,KAAK,GAAG,CAACV,EAAMF,EAAKM,EAAI,GAC/BF,EAAOQ,KAAK,GAAG,CAACR,EAAMC,EAAKE,EAAI,GAC/BR,EAAOa,KAAK,GAAG,CAACb,EAAMM,EAAKE,EAAI,EACjC,QACA,AAAIN,IAAS,KAAYG,IAAS,IACzB,KAEF,CAAEH,KAAAA,EAAMC,KAAAA,EAAME,KAAAA,EAAML,KAAAA,CAAK,CAClC,CAEA,SAASoG,GAAiBnG,CAAK,CAAEC,CAAM,EACrC,IAAMC,EAAMF,EAAM,OAAO,EAAI,EAC7BA,CAAAA,EAAM,CAAC,CAAG,AAACC,CAAAA,EAAO,IAAI,CAAGA,EAAO,IAAI,AAAD,EAAK,EACxCD,EAAM,CAAC,CAAG,AAACC,CAAAA,EAAO,IAAI,CAAGA,EAAO,IAAI,AAAD,EAAK,EACxCD,EAAM,KAAK,CAAGY,KAAK,GAAG,CAAC,EAAGX,EAAO,IAAI,CAAGA,EAAO,IAAI,EAAIC,EACvDF,EAAM,MAAM,CAAGY,KAAK,GAAG,CAAC,EAAGX,EAAO,IAAI,CAAGA,EAAO,IAAI,EAAIC,CAC1D,CAEA,SAASkG,GAA2BpG,CAAK,EACvC,IAAMC,EAAW8F,GAAa/F,GAE9B,IAAK,IAAME,KADWF,EAAM,MAAM,CAAC,AAACA,GAASA,EAAK,OAAO,EAAIA,EAAK,QAAQ,EAAE,IAAI,CAAC,CAACA,EAAGE,IAAM+F,GAAW/F,EAAGD,GAAYgG,GAAWjG,EAAGC,IAChG,CAEjC,IAAMA,EAASiG,GADElG,EAAM,MAAM,CAAC,AAACA,GAASA,EAAK,QAAQ,GAAKE,EAAM,EAAE,EAE9DD,CAAAA,GACFkG,GAAiBjG,EAAOD,EAE5B,CACF,CAEA,SAASoG,GAAWrG,CAAM,CAAEC,CAAI,EAC9B,IAAMC,EAAQF,EAAO,KAAK,EAAI,EAAE,CAC1BD,EAAQC,EAAO,KAAK,EAAI,EAAE,CAC1BG,EAAeD,EAAM,MAAM,CAAC,AAACF,GAAS,CAACA,EAAK,OAAO,EACrDK,EAAM,IACNC,EAAM,CAAC,IACX,IAAK,IAAMN,KAAQG,EAAc,CAC/B,IAAMD,EAAQF,CAAI,CAACC,EAAK,AACH,WAAjB,OAAOC,IAGXG,EAAMO,KAAK,GAAG,CAACP,EAAKH,GACpBI,EAAMM,KAAK,GAAG,CAACN,EAAKJ,GACtB,CACA,GAAI,CAACW,OAAO,QAAQ,CAACR,IAAQ,CAACQ,OAAO,QAAQ,CAACP,GAC5C,MAAO,GAET,IAAMC,EAAyBH,EAAoB,AAACJ,GAAUK,EAAMC,EAAMN,EAAO,UACjF,IAAK,IAAMA,KAAQE,EAAO,CACxB,IAAMA,EAAQF,CAAI,CAACC,EAAK,AACpB,AAAiB,WAAjB,OAAOC,GACTF,CAAAA,CAAI,CAACC,EAAK,CAAGM,EAAOL,EAAK,EAE3B,IAAME,EAAYJ,EAAK,cAAc,AACjCI,CAAAA,GACFJ,CAAAA,EAAK,cAAc,CAAGC,AAAS,MAATA,EAAe,CACnC,GAAGG,CAAS,CACZ,KAAMG,EAAOH,EAAU,KAAK,EAC5B,MAAOG,EAAOH,EAAU,IAAI,CAC9B,EAAI,CACF,GAAGA,CAAS,CACZ,IAAKG,EAAOH,EAAU,MAAM,EAC5B,OAAQG,EAAOH,EAAU,GAAG,CAC9B,EAEJ,CACA,IAAK,IAAMJ,KAAQD,EACjB,IAAK,IAAMG,KAASF,EAAK,MAAM,EAAI,EAAE,CACnCE,CAAK,CAACD,EAAK,CAAGM,EAAOL,CAAK,CAACD,EAAK,EAGpC,MAAO,EACT,CAEA,SAASqG,GAA0BtG,CAAM,QAEvC,CAAK,AADSA,CAAAA,EAAO,KAAK,EAAI,EAAE,AAAD,EACpB,IAAI,CAAC,AAACA,GAAS,CAACA,EAAK,OAAO,GAGhCqG,GAAWrG,EAAQ,IAC5B,CAEA,SAASuG,GAA0BvG,CAAM,CAAEC,EAAY,IAAI,EACzD,IAAMC,EAAQF,EAAO,KAAK,EAAI,EAAE,CAC1BI,EAAQJ,EAAO,KAAK,EAAI,EAAE,CAC1BD,EAAeG,EAAM,MAAM,CAAC,AAACF,GAAM,CAACA,EAAE,OAAO,EAC/CG,EAAO,IACPE,EAAO,IACX,IAAK,IAAML,KAAKD,EAAc,CAC5B,IAAME,EAAKD,EAAE,CAAC,EAAI,EACZE,EAAKF,EAAE,CAAC,EAAI,CACdC,CAAAA,EAAKE,GACPA,CAAAA,EAAOF,CAAC,EAENC,EAAKG,GACPA,CAAAA,EAAOH,CAAC,CAEZ,CACA,GAAI,CAACW,OAAO,QAAQ,CAACV,IAAS,CAACU,OAAO,QAAQ,CAACR,GAC7C,MAAO,GAGT,IAAIC,EAAa,EACbC,EAAc,EAClB,IAAK,IAAMP,KAAKD,EACdO,GAAcN,EAAE,KAAK,EAAI,EACzBO,GAAeP,EAAE,MAAM,EAAI,EAE7B,IAAMQ,EAAWF,EAAaP,EAAa,MAAM,CAC3CU,EAAYF,EAAcR,EAAa,MAAM,CAC7CW,EAAwBD,EAAY,EAAIG,KAAK,GAAG,CAAC,EAAGJ,EAAWC,GAAa,EAClF,IAAK,IAAMT,KAAKD,EAAc,CAC5B,IAAME,EAAKD,EAAE,CAAC,EAAI,EAEZE,EAAO,AAAC,CADHF,CAAAA,EAAE,CAAC,EAAI,GACCK,CAAG,EAAKK,EAbP,GAcdN,EAAOH,EAAKE,CAClBH,CAAAA,EAAE,CAAC,CAAGE,EACNF,EAAE,CAAC,CAAGI,CACR,CACA,IAAK,IAAMJ,KAAKI,EACd,GAAKJ,EAAE,MAAM,CAGb,IAAK,IAAMC,KAAKD,EAAE,MAAM,CAAE,CACxB,IAAMA,EAAKC,EAAE,CAAC,CAERC,EAAO,AAAC,CADHD,EAAE,CAAC,CACKI,CAAG,EAAKK,EAzBT,GA0BZN,EAAOJ,EAAKG,CAClBF,CAAAA,EAAE,CAAC,CAAGC,EACND,EAAE,CAAC,CAAGG,CACR,CAEFgG,GAA2BlG,GAC3B,IAAMS,EAAYT,EAAM,MAAM,CAAC,AAACF,GAAMA,EAAE,OAAO,EAAI,CAACA,EAAE,QAAQ,EAC9D,GAAIW,AAAqB,IAArBA,EAAU,MAAM,CAIlB,MAHIV,AAAc,OAAdA,GACFoG,GAAWrG,EAAQ,KAEd,GAET,IAAMc,EAAWiF,GAAa7F,GACxBa,EAAiC,IAAIE,IAC3C,IAAK,IAAMjB,KAAKE,EAAO,CACrB,GAAIF,EAAE,OAAO,CACX,SAEF,IAAMC,EAAS+F,GAAuBhG,EAAGc,GACzC,GAAI,CAACb,EACH,SAEF,IAAMC,EAASa,EAAe,GAAG,CAACd,IAAW,EAAE,CAC/CC,EAAO,IAAI,CAACF,GACZe,EAAe,GAAG,CAACd,EAAQC,EAC7B,CACA,IAAIc,EAAS,EACb,IAAK,IAAMhB,KAAQW,EAAW,CAC5B,IAAMV,EAAMD,EAAK,OAAO,EAAI,CACxBC,CAAAA,EAAMe,GACRA,CAAAA,EAASf,CAAE,CAEf,CACA,IAAMiB,EAAa,EAAE,CACjBC,EAAkB,IAClBC,EAAkB,CAAC,IACvB,IAAK,IAAMpB,KAAQW,EAAW,CAE5B,IAAMV,EAASiG,GADEnF,EAAe,GAAG,CAACf,EAAK,EAAE,GAAK,EAAE,EAE7CC,IAGLkB,EAAkBP,KAAK,GAAG,CAACO,EAAiBlB,EAAO,IAAI,EACvDmB,EAAkBR,KAAK,GAAG,CAACQ,EAAiBnB,EAAO,IAAI,EACvDiB,EAAW,IAAI,CAAC,CACdlB,KAAAA,EACA,WAAYC,EAAO,IAAI,CACvB,cAAeA,EAAO,IAAI,CAC1B,QAAS,AAACA,CAAAA,EAAO,IAAI,CAAGA,EAAO,IAAI,AAAD,EAAK,CACzC,GACF,CACA,GAAIkB,IAAoB,KAAYC,IAAoB,CAAC,IACvD,MAAO,GAIT,IAAMC,EAAY,AAFOT,KAAK,GAAG,CAAC,EAAGQ,EAAkBD,GAElB,EADZP,KAAK,GAAG,CAACI,EAAQ,IAEpCM,EAAY,AApFI,GAoFYD,EAG5BO,EAAW,AAFGT,CAAAA,EAAkBC,CAAc,EAAK,EAC3BC,EAAY,EAtFpB,GAwFhBQ,EAAUD,EAAWN,EAAY,EACjCQ,EAAiBlB,KAAK,GAAG,CAACI,EAzFV,IA0FtBE,EAAW,IAAI,CAAC,CAAClB,EAAGC,IAAMD,EAAE,OAAO,CAAGC,EAAE,OAAO,EAC/C,IAAK,IAAID,EAAI,EAAGA,EAAIkB,EAAW,MAAM,CAAElB,IAAK,CAC1C,IACIC,EACAC,EAFEE,EAAOc,CAAU,CAAClB,EAAE,CAS1B,GALEC,EADED,AAAM,IAANA,EACQI,EAAK,UAAU,CAAG0B,EAGlB,AAAC,CADEZ,CAAU,CAAClB,EAAI,EAAE,CACd,aAAa,CAAGI,EAAK,UAAU,AAAD,EAAK,EAEjDJ,IAAMkB,EAAW,MAAM,CAAG,EAC5BhB,EAAaE,EAAK,aAAa,CAAG0B,MAC7B,CACL,IAAM7B,EAAOiB,CAAU,CAAClB,EAAI,EAAE,CAC9BE,EAAa,AAACE,CAAAA,EAAK,aAAa,CAAGH,EAAK,UAAU,AAAD,EAAK,CACxD,CACA,IAAMF,EAAaa,KAAK,GAAG,CAAC,EAAGV,EAAaD,GACtCE,EAAU,AAACF,CAAAA,EAAUC,CAAS,EAAK,CACzCE,CAAAA,EAAK,IAAI,CAAC,CAAC,CAAGyB,EACdzB,EAAK,IAAI,CAAC,CAAC,CAAGD,EACdC,EAAK,IAAI,CAAC,KAAK,CAAGkB,EAClBlB,EAAK,IAAI,CAAC,MAAM,CAAGL,EACnBK,EAAK,IAAI,CAAC,kBAAkB,CAAGA,EAAK,UAAU,CAC9CA,EAAK,IAAI,CAAC,cAAc,CAAG,CACzB,KAAMwB,EACN,MAAOA,EApHW,GAqHlB,IAAK3B,EACL,OAAQC,CACV,CACF,CAIA,MAHID,AAAc,OAAdA,GACFoG,GAAWrG,EAAQ,KAEd,EACT,CAr6CAI,EAAoBwB,EAA0B,4BAuB9CxB,EAAoByB,EAAwB,0BAqC5CzB,EAAoB0B,EAA2B,6BAyB/C1B,EAAoB2B,EAAa,eAsKjC3B,EAAoB4B,EAAuB,yBA0E3C5B,EAAoB6B,EAAsB,wBAW1C7B,EAAoB8B,EAAkB,oBAiBtC9B,EAAoBgC,EAAmB,qBAIvChC,EAAoBkC,EAAW,aAI/BlC,EAAoBmC,EAAO,SAI3BnC,EAAoBoC,EAAO,SAI3BpC,EAAoBqC,EAAqB,uBAIzCrC,EAAoBsC,EAAmB,qBAOvCtC,EAAoBuC,EAAe,iBAUnCvC,EAAoBwC,EAA8B,gCAclDxC,EAAoByC,EAA6B,+BAWjDzC,EAAoB0C,EAAsB,wBAW1C1C,EAAoB2C,EAAyB,2BAa7C3C,EAAoB4C,EAA2B,6BAQ/C5C,EAAoB6C,EAA0B,4BAI9C7C,EAAoB8C,EAAiB,mBAIrC9C,EAAoB+C,EAAkB,oBAItC/C,EAAoBgD,EAAc,gBASlChD,EAAoBiD,EAAa,eASjCjD,EAAoB+B,EAAoB,sBAIxC/B,EAAoBkD,EAAkB,oBAatClD,EAAoBmD,EAAiB,mBAiDrCnD,EAAoBoD,EAAyB,2BAI7CpD,EAAoBqD,EAAwB,0BAiB5CrD,EAAoBsD,EAAuB,yBAkB3CtD,EAAoBuD,EAAwB,0BAkB5CvD,EAAoBwD,EAAwB,0BAqB5CxD,EAAoByD,EAAqB,uBAYzCzD,EAAoB0D,GAAoB,sBA2BxC1D,EAAoB2D,GAAyB,2BAc7C3D,EAAoB4D,GAAyB,2BA0B7C5D,EAAoB6D,GAA6B,+BAmBjD7D,EAAoB8D,GAAiC,mCAMrD9D,EAAoB+D,GAAiB,mBAUrC/D,EAAoBgE,GAAyB,2BAuB7ChE,EAAoBiE,GAAsB,wBAuB1CjE,EAAoBkE,GAAuB,yBAe3ClE,EAAoBmE,GAAkB,oBAoBtCnE,EAAoBoE,GAAoB,sBAexCpE,EAAoBqE,GAAmB,qBAiBvCrE,EAAoBsE,GAAc,gBAmBlCtE,EAAoBuE,GAAmC,qCAkCvDvE,EAAoByE,GAAwB,0BAW5CzE,EAAoB0E,GAAuB,yBAM3C1E,EAAoB2E,GAAsB,wBAK1C3E,EAAoB4E,GAAwB,0BAS5C5E,EAAoB6E,GAAiB,mBAIrC7E,EAAoB8E,GAAuB,yBAsB3C9E,EAAoB+E,GAAwB,0BAI5C/E,EAAoBgF,GAAkB,oBAatChF,EAAoBiF,GAAwB,0BAmB5CjF,EAAoBkF,GAAiC,mCAcrDlF,EAAoBwE,GAAwC,0CAI5DxE,EAAoBmF,GAAuB,yBAmB3CnF,EAAoBoF,GAA0B,4BA8B9CpF,EAAoBqF,GAA+B,iCAkBnDrF,EAAoBsF,GAAsB,wBAa1CtF,EAAoBuF,GAAe,iBAmBnCvF,EAAoBwF,GAAuB,yBAoC3CxF,EAAoByF,GAA0B,4BAuB9CzF,EAAoB0F,GAAiC,mCAMrD1F,EAAoB2F,GAAc,gBAclC3F,EAAoB4F,GAAwB,0BAc5C5F,EAAoB6F,GAAY,cAwBhC7F,EAAoB8F,GAAmB,qBAQvC9F,EAAoB+F,GAAkB,oBAYtC/F,EAAoBgG,GAA4B,8BA4ChDhG,EAAoBiG,GAAY,cAQhCjG,EAAoBkG,GAA2B,6BAkJ/ClG,EAAoBmG,GAA2B,6BAM/C,IAAIC,GAAa,CAAC,EAFK,EAEU,GAAa,GAAgB,IAAgB,CAC9E,SAASC,GAAiBzG,CAAK,CAAEC,CAAK,EACpC,GAAM,CAAEC,aAAAA,CAAY,CAAEE,cAAAA,CAAa,CAAE,CAAGsD,EAAsBzD,GAC9D,IAAK,IAAMA,KAAQD,EAAO,KAsBpBD,EArBJ,GAAIE,EAAK,YAAY,CACnB,SAEF,IAAME,EAAMF,EAAK,MAAM,CACvB,GAAI,CAACE,GAAOA,EAAI,MAAM,CAAG,EACvB,SAEF,IAAME,EAAQ2C,EAA0BD,EAAwB5C,EAdzD,YAeP,GAAI,CAACE,EACH,SAEF,GAAM,CAAEC,GAAAA,CAAE,CAAE,CAAGD,EACTE,EAAQF,AAAe,QAAfA,EAAM,IAAI,CAClBG,EAAWqD,EAAoB5D,EAAMC,EApBpC,MAqBP,GAAI,CAACM,EACH,SAEF,GAAM,CAAEC,MAAAA,CAAK,CAAEC,MAAAA,CAAK,CAAEC,QAAAA,CAAO,CAAEG,QAAAA,CAAO,CAAEC,WAAAA,CAAU,CAAEC,WAAAA,CAAU,CAAE,CAAGR,EACnE,GAAIO,GAAcC,EAChB,SAGF,IAAME,EAAUP,EAAQ,IAAI,CAC5B,IAAK,IAAMT,KAASsG,GAAY,KAC1BrG,EACAE,EACAG,EACJ,GAAID,EAAO,CAET,IAAMP,EAAU,AADCc,EAAQ,EAAE,CAAGH,EAAQ,EAAE,CACbO,EAAQ,MAAM,CAAGA,EAAQ,GAAG,CACjDjB,EAAUU,EAAQ,EAAE,CAAGT,EAC7B,GAAID,GAAWiB,EAAQ,IAAI,CAtCxB,MAsCmCjB,GAAWiB,EAAQ,KAAK,CAtC3D,KAuCD,SAEFf,EAAM,CAAE,EAAGF,EAAS,EAAGD,CAAQ,EAC/BK,EAAM,CAAE,EAAGJ,EAAS,EAAGK,EAAG,CAAC,AAAC,EAC5BE,EAAM,CAAE,EAAGF,EAAG,CAAC,CAAE,EAAGA,EAAG,CAAC,AAAC,CAC3B,KAAO,CAEL,IAAMN,EAAU,AADAc,EAAQ,EAAE,CAAGH,EAAQ,EAAE,CACbO,EAAQ,KAAK,CAAGA,EAAQ,IAAI,CAChDjB,EAAUU,EAAQ,EAAE,CAAGT,EAC7B,GAAID,GAAWiB,EAAQ,GAAG,CAhDvB,MAgDkCjB,GAAWiB,EAAQ,MAAM,CAhD3D,KAiDD,SAEFf,EAAM,CAAE,EAAGH,EAAS,EAAGC,CAAQ,EAC/BI,EAAM,CAAE,EAAGC,EAAG,CAAC,CAAE,EAAGL,CAAQ,EAC5BO,EAAM,CAAE,EAAGF,EAAG,CAAC,CAAE,EAAGA,EAAG,CAAC,AAAC,CAC3B,CACA,IAAMS,EAAqBuB,EAAUnC,EAAKE,EAvDrC,MAwDCW,EAAsBsB,EAAUjC,EAAKG,EAxDtC,MAyDL,GAAIO,GAAsBC,GAGtB,CAACD,GAAsB+C,GAAmB3D,EAAKE,EAAKD,EAAe,CAACK,EAAM,CAAE,IAG5E,CAACO,GAAuB8C,GAAmBzD,EAAKG,EAAKJ,EAAe,CAACM,EAAM,CAAE,GAL/E,SAQF,IAAMS,EAAoB,CAACJ,GAAsBkD,GAA4B9D,EAAKE,EAAKL,EAAOC,EAAM,CAClG,QAnEG,KAoEH,oBAAqB,EACvB,GACMmB,EAAqB,CAACJ,GAAuBiD,GAA4B5D,EAAKG,EAAKR,EAAOC,EAAM,CACpG,QAvEG,KAwEH,oBAAqB,EACvB,GACA,GAAIkB,CAAAA,IAAqBC,GAIvBrB,EADEgB,EACO,CAACV,EAAKG,EAAI,CACVQ,EACA,CAACb,EAAKE,EAAI,CAEV,CAACF,EAAKE,EAAKG,EAAI,CAE1B,MACF,CACIT,GACFE,CAAAA,EAAK,MAAM,CAAGF,CAAK,CAEvB,CACF,CAIA,SAAS2G,GAA0B1G,CAAK,CAAEC,CAAW,EAInD,GAAM,CAAEC,cAAAA,CAAa,CAAE,eAAgBH,CAAU,CAAE,CAAG4D,EACpD1D,EAAY,MAAM,IAEpB,IAAK,IAAME,KAAQH,EAAO,KA+CpBK,EACAC,EA/CJ,GAAIH,EAAK,YAAY,CACnB,SAEF,IAAMI,EAASJ,EAAK,MAAM,CAC1B,GAAI,CAACI,GAAUA,EAAO,MAAM,CAAG,EAC7B,SAEF,IAAMC,EAAMuC,EAAwBxC,EAbnB,MAcjB,GAAIC,EAAI,MAAM,CAAG,EACf,SAEF,IAAMC,EAAQD,EAAI,MAAM,CAAG,EACrBE,EAAQF,CAAG,CAACC,EAAM,CAClBE,EAAWH,CAAG,CAACC,EAAQ,EAAE,CACzBK,EAASN,CAAG,CAACC,EAAQ,EAAE,CAGvBM,EAAUH,KAAK,KAAK,CAFXF,EAAM,CAAC,CAAGC,EAAS,CAAC,CACpBD,EAAM,CAAC,CAAGC,EAAS,CAAC,EAEnC,GAAII,GAzBW,IAyBYA,EAxBV,KAyBf,SAEF,IAAMC,EAAWL,EAAS,CAAC,CAAGG,EAAO,CAAC,CAChCI,EAAWP,EAAS,CAAC,CAAGG,EAAO,CAAC,CAEtC,GAAI,AA9Ba,KA6BCF,KAAK,KAAK,CAACI,EAAUE,GAErC,SAEF,IAAMC,EAAcsB,EAAoB9B,EAAUD,EAjCjC,MAkCXU,EAAasB,EAAkB/B,EAAUD,EAlC9B,MAmCXW,EAAgBoB,EAAoB3B,EAAQH,EAnCjC,MAoCXW,EAAeoB,EAAkB5B,EAAQH,EApC9B,MAqCjB,GAAI,CAAEQ,CAAAA,GAAeG,GAAgBF,GAAcC,CAAY,EAC7D,SAEF,IAAMO,EAAQzB,EAAK,GAAG,CAChB0B,EAAQ1B,EAAK,KAAK,CAClB2B,EAAMF,EAAQ3B,EAAY,GAAG,CAAC2B,GAAS,KAAK,EAClD,GAAI,CAACE,EACH,SAEF,IAAMC,EAAQD,EAAI,CAAC,EAAI,EACjBE,EAAQF,EAAI,CAAC,EAAI,EACjBG,EAAUqB,EAAiBxB,GACjC,GAAI,CAACG,EACH,SAIF,GAAIX,EAAc,CAChB,IAAMtB,EAAoBkB,EAAW,EACrCb,EAAU,CAAE,EAAG0B,EAAO,EAAGjB,EAAO,CAAC,AAAC,EAClCR,EAAS,CAAE,EAAGyB,EAAO,EAAG/B,EAAoBiC,EAAQ,MAAM,CAAGA,EAAQ,GAAG,AAAC,CAC3E,KAAO,CACL,IAAMjC,EAAmBgB,EAAW,EACpCX,EAAU,CAAE,EAAGS,EAAO,CAAC,CAAE,EAAGkB,CAAM,EAClC1B,EAAS,CAAE,EAAGN,EAAmBiC,EAAQ,KAAK,CAAGA,EAAQ,IAAI,CAAE,EAAGD,CAAM,CAC1E,CACA,GAAI8B,GAAmBzD,EAASC,EAAQJ,EAAe0B,EAAQ,CAACA,EAAM,CAAG,EAAE,CAAE,KAGzEkC,GAAmBzD,EAASC,EAAQP,EAAY,EAAE,CAAE,IAFtD,SAKF,GAAI8B,EAAO,CACT,IAAM7B,EAAMC,EAAY,GAAG,CAAC4B,GACtB3B,EAAUF,EAAMsD,EAAiBtD,GAAO,KAAK,EACnD,GAAIE,GAAWgD,EAAgB7C,EAASH,EAvE7B,GAwET,QAEJ,CACA,IAAMgC,EAAgC9B,EAAoB,CAACJ,EAAGC,IAAM,CAAC,EAAED,EAAE,CAAC,CAAC,OAAO,CAAC,GAAG,CAAC,EAAEA,EAAE,CAAC,CAAC,OAAO,CAAC,GAAG,CAAC,EAAEC,EAAE,CAAC,CAAC,OAAO,CAAC,GAAG,CAAC,EAAEA,EAAE,CAAC,CAAC,OAAO,CAAC,GAAG,CAAC,CAAE,iBACzImC,EAA+B,IAAIuE,IACzC,IAAK,IAAI3G,EAAI,EAAGA,EAAIQ,EAAI,MAAM,CAAG,EAAGR,IAClCoC,EAAa,GAAG,CAACF,EAAc1B,CAAG,CAACR,EAAE,CAAEQ,CAAG,CAACR,EAAI,EAAE,GAEnD,IAAMsC,EAA0ClC,EAAoB,CAACH,EAAMC,KACzE,IAAK,IAAME,KAASJ,EAAO,CACzB,GAAII,IAAUD,GAGVC,EAAM,YAAY,CAFpB,SAKF,IAAMJ,EAAOI,EAAM,MAAM,CACzB,GAAI,AAACJ,IAAQA,CAAAA,EAAK,MAAM,CAAG,GAG3B,IAAK,IAAII,EAAI,EAAGA,EAAIJ,EAAK,MAAM,CAAG,EAAGI,IAAK,CACxC,IAAML,EAAIC,CAAI,CAACI,EAAE,CACXD,EAAIH,CAAI,CAACI,EAAI,EAAE,CACrB,IAAIgC,EAAa,GAAG,CAACF,EAAcnC,EAAGI,KAGlC+D,GAAgCjE,EAAMC,EAAIH,EAAGI,EAnGtC,MAoGT,MAAO,EAEX,CACF,CACA,MAAO,EACT,EAAG,2BACH,GAAImC,EAAwBjC,EAASC,GACnC,SAEF,GAAIG,EAAQ,GAAK,EAAG,CAClB,IAAMT,EAAaQ,CAAG,CAACC,EAAQ,EAAE,CAEjC,GAAIqD,GAAmB9D,EAAYK,EAASH,EADxB,CAAC2B,EAAOD,EAAM,CAAC,MAAM,CAAC,AAAC5B,GAAO,EAAQA,GACc,KAGpEsC,EAAwBtC,EAAYK,GAFtC,QAKJ,CAEA,IAAMsC,EAAS,IADFnC,EAAI,KAAK,CAAC,EAAGC,EAAQ,GACTJ,EAASC,EAAO,AACzCH,CAAAA,EAAK,MAAM,CAAGwC,EACd,IAAMC,EAAUzC,EAAK,WAAW,CAChC,GAAIyC,EAAS,CACX,IAAM5C,EAAYC,EAAY,GAAG,CAAC2C,GAClC,GAAI5C,EAAW,CACb,IAAMC,EAAKD,EAAU,KAAK,EAAI,EACxBE,EAAKF,EAAU,MAAM,EAAI,EAC/B,GAAIC,EAAK,GAAKC,EAAK,EAAG,CAGpB,IAFIE,EACAL,EACAI,EAAU,GACd,IAAK,IAAIH,EAAI,EAAGA,EAAI2C,EAAO,MAAM,CAAG,EAAG3C,IAAK,CAC1C,IAAMK,EAAIsC,CAAM,CAAC3C,EAAE,CACbM,EAAIqC,CAAM,CAAC3C,EAAI,EAAE,CACjBO,EAASK,KAAK,KAAK,CAACN,EAAE,CAAC,CAAGD,EAAE,CAAC,CAAEC,EAAE,CAAC,CAAGD,EAAE,CAAC,EACxCG,EAAUgC,EAAMnC,EAAGC,EAxIhB,MAyIHG,EAAS8B,EAAMlC,EAAGC,EAzIf,MA0IIE,CAAAA,GAAWD,GAAUN,EAAK,GAAKQ,GAAUF,GAAUL,EAAK,IAIjEK,EAASJ,IACXA,EAAUI,EACVH,EAAW,AAACC,CAAAA,EAAE,CAAC,CAAGC,EAAE,CAAC,AAAD,EAAK,EACzBP,EAAW,AAACM,CAAAA,EAAE,CAAC,CAAGC,EAAE,CAAC,AAAD,EAAK,EAE7B,CACiB,KAAK,IAAlBF,GAAuBL,AAAa,KAAK,IAAlBA,IACzBC,EAAU,CAAC,CAAGI,EACdJ,EAAU,CAAC,CAAGD,EAElB,CACF,CACF,CACF,CACF,CAjKAK,EAAoBqG,GAAkB,oBAkKtCrG,EAAoBsG,GAA2B,6BAM/C,IAAIE,GAAsCxG,EAAoB,CAACJ,EAAGC,IAAMsC,EAAMvC,EAAGC,EAHjE,OAGkFuC,EAAMxC,EAAGC,EAH3F,MAG0G,uBAC1H,SAAS4G,GAAoC7G,CAAK,CAAEC,CAAW,EAG7D,IAAMC,EAAgCE,EAAoB,CAACJ,EAAMC,KAC/D,IAAMC,EAAIF,EAAK,CAAC,EAAI,EACdI,EAAIJ,EAAK,CAAC,EAAI,EACdD,EAAKE,EAAM,CAAC,CAAGC,EACfC,EAAKF,EAAM,CAAC,CAAGG,EACjBC,EAAI,AAACL,CAAAA,EAAK,KAAK,EAAI,GAAK,EACxBM,EAAI,AAACN,CAAAA,EAAK,MAAM,EAAI,GAAK,SAC7B,AAAIY,KAAK,GAAG,CAACT,GAAME,EAAIO,KAAK,GAAG,CAACb,GAAMO,GAChCH,EAAK,GACPG,CAAAA,EAAI,CAACA,CAAAA,EAEA,CAAE,EAAGJ,EAAKC,CAAAA,AAAO,IAAPA,EAAW,EAAIG,EAAIP,EAAKI,CAAC,EAAI,EAAGC,EAAIE,CAAE,IAErDP,EAAK,GACPM,CAAAA,EAAI,CAACA,CAAAA,EAEA,CAAE,EAAGH,EAAIG,EAAG,EAAGD,EAAKL,CAAAA,AAAO,IAAPA,EAAW,EAAIM,EAAIF,EAAKJ,CAAC,CAAG,EACzD,EAAG,iBACGA,EAAkCK,EAAoB,CAACJ,EAAMI,KACjE,IAAML,EAASgD,EAAwB/C,EAAK,MAAM,EAAI,EAAE,EACxD,GAAID,EAAO,MAAM,CAAG,EAClB,OAEF,IAAMI,EAASC,EAAUJ,EAAK,KAAK,CAAGA,EAAK,GAAG,CACxCK,EAAOF,EAASF,EAAY,GAAG,CAACE,GAAU,KAAK,EAC/CG,EAAOD,EAAOiD,EAAiBjD,GAAQ,KAAK,EAClD,GAAI,CAACA,GAAQ,CAACF,GAAU,CAACG,EACvB,OAEF,IAAMC,EAAWH,EAAUL,CAAM,CAAC,EAAE,CAAGA,CAAM,CAACA,EAAO,MAAM,CAAG,EAAE,CAC1DS,EAAWJ,EAAUL,CAAM,CAAC,EAAE,CAAGA,CAAM,CAACA,EAAO,MAAM,CAAG,EAAE,CAC1DU,EAAWP,EAAcG,EAAME,GACjCG,EAAUH,QAId,CAHIqG,GAAoBpG,EAAUC,IAChCC,CAAAA,EAAUF,CAAO,EAEf+B,EAAM9B,EAAUC,EA3CR,OA4CH,CACLV,KAAAA,EACA,OAAQqC,OAAOrC,EAAK,EAAE,EAAI,IAC1BG,OAAAA,EACAC,QAAAA,EACA,YAAa,IACb,MAAOK,EAAS,CAAC,CACjB,IAAKG,KAAK,GAAG,CAACH,EAAS,CAAC,CAAEC,EAAQ,CAAC,EACnC,IAAKE,KAAK,GAAG,CAACH,EAAS,CAAC,CAAEC,EAAQ,CAAC,EACnCD,SAAAA,EACAC,QAAAA,EACAJ,KAAAA,CACF,EAEEkC,EAAM/B,EAAUC,EA1DR,MA2DH,CACLV,KAAAA,EACA,OAAQqC,OAAOrC,EAAK,EAAE,EAAI,IAC1BG,OAAAA,EACAC,QAAAA,EACA,YAAa,IACb,MAAOK,EAAS,CAAC,CACjB,IAAKG,KAAK,GAAG,CAACH,EAAS,CAAC,CAAEC,EAAQ,CAAC,EACnC,IAAKE,KAAK,GAAG,CAACH,EAAS,CAAC,CAAEC,EAAQ,CAAC,EACnCD,SAAAA,EACAC,QAAAA,EACAJ,KAAAA,CACF,QAGJ,EAAG,mBACGH,EAAyCC,EAAoB,CAACJ,EAAGC,IAAMW,KAAK,GAAG,CAAC,EAAGA,KAAK,GAAG,CAACZ,EAAE,GAAG,CAAEC,EAAE,GAAG,EAAIW,KAAK,GAAG,CAACZ,EAAE,GAAG,CAAEC,EAAE,GAAG,GAAI,0BACrII,EAAmCD,EAAoB,CAACJ,EAAGC,IAC/D,AAAID,EAAE,MAAM,GAAKC,EAAE,MAAM,EAAID,EAAE,WAAW,GAAKC,EAAE,WAAW,GAGxDD,AAAkB,MAAlBA,EAAE,WAAW,CAER,AADmBY,CAAAA,AAAuC,EAAvCA,KAAK,GAAG,CAACZ,EAAE,QAAQ,CAAC,CAAC,CAAGA,EAAE,IAAI,CAAC,IAAI,GAASY,AAAwC,EAAxCA,KAAK,GAAG,CAACZ,EAAE,QAAQ,CAAC,CAAC,CAAGA,EAAE,IAAI,CAAC,KAAK,CAAI,GAClFuC,EAAMvC,EAAE,QAAQ,CAAEC,EAAE,QAAQ,CAAE,GAGrD,AADiBW,CAAAA,AAAsC,EAAtCA,KAAK,GAAG,CAACZ,EAAE,QAAQ,CAAC,CAAC,CAAGA,EAAE,IAAI,CAAC,GAAG,GAASY,AAAyC,EAAzCA,KAAK,GAAG,CAACZ,EAAE,QAAQ,CAAC,CAAC,CAAGA,EAAE,IAAI,CAAC,MAAM,CAAI,GAClFwC,EAAMxC,EAAE,QAAQ,CAAEC,EAAE,QAAQ,CAAE,IACvD,oBACGK,EAA4CF,EAAoB,CAACJ,EAAGC,IACxE,AAAID,EAAE,MAAM,GAAKC,EAAE,MAAM,EAAID,EAAE,WAAW,GAAKC,EAAE,WAAW,EAIrD,AADQE,EAAuBH,EAAGC,IA1F5B,GA2FkBW,AAA8B,GAA9BA,KAAK,GAAG,CAACZ,EAAE,KAAK,CAAGC,EAAE,KAAK,EACxD,6BACGM,EAA2CH,EAAoB,CAACJ,EAAGC,KACvE,GAAID,EAAE,MAAM,GAAKC,EAAE,MAAM,EAAID,EAAE,WAAW,GAAKC,EAAE,WAAW,EAAID,AAAkB,MAAlBA,EAAE,WAAW,EAAYA,EAAE,OAAO,GAAKC,EAAE,OAAO,CAC9G,MAAO,GAET,IAAMC,EAASC,EAAuBH,EAAGC,GACzC,GAAIC,EAlGS,EAmGX,MAAO,GAET,IAAME,EAAWJ,EAAE,IAAI,CAAC,MAAM,CAAGA,EAAE,IAAI,CAAC,GAAG,OAC3C,CAAIE,CAAAA,EAASE,CAAO,IAAKF,CAAAA,EAAS,EAAIE,CAAO,GAGtCC,EAAiBL,EAAGC,IAAMW,AArGR,GAqGQA,KAAK,GAAG,CAACZ,EAAE,KAAK,CAAGC,EAAE,KAAK,CAC7D,EAAG,4BACGO,EAAmCJ,EAAoB,CAACJ,EAAMC,KAClE,IAAMC,EAAS6C,EAAwB/C,EAAK,IAAI,CAAC,MAAM,EAAI,EAAE,EAC7D,GAAIE,EAAO,MAAM,CAAG,EAClB,OAEF,IAAMH,EAAkBC,AAAqB,MAArBA,EAAK,WAAW,CAAW,CAAE,EAAGA,EAAK,QAAQ,CAAC,CAAC,CAAGC,EAAO,EAAGD,EAAK,QAAQ,CAAC,CAAC,AAAC,EAAI,CAAE,EAAGA,EAAK,QAAQ,CAAC,CAAC,CAAE,EAAGA,EAAK,QAAQ,CAAC,CAAC,CAAGC,CAAM,EACnJE,EAAiBH,AAAqB,MAArBA,EAAK,WAAW,CAAW,CAAE,EAAGA,EAAK,OAAO,CAAC,CAAC,CAAGC,EAAO,EAAGD,EAAK,OAAO,CAAC,CAAC,AAAC,EAAI,CAAE,EAAGA,EAAK,OAAO,CAAC,CAAC,CAAE,EAAGA,EAAK,OAAO,CAAC,CAAC,CAAGC,CAAM,EAUpJ,GAAI,CAAC,AAT2CG,EAAoB,IAClE,AAAIQ,AAA4C,EAA5CA,KAAK,GAAG,CAACZ,EAAK,QAAQ,CAAC,CAAC,CAAGA,EAAK,IAAI,CAAC,GAAG,GAASY,AAA+C,EAA/CA,KAAK,GAAG,CAACZ,EAAK,QAAQ,CAAC,CAAC,CAAGA,EAAK,IAAI,CAAC,MAAM,EACvFwC,EAAMzC,EAAiBC,EAAK,QAAQ,CArHnC,OAqHmDD,EAAgB,CAAC,EAAIC,EAAK,IAAI,CAAC,IAAI,CAAG,GAAKD,EAAgB,CAAC,EAAIC,EAAK,IAAI,CAAC,KAAK,CAAG,IAE3IY,CAAAA,AAA6C,EAA7CA,KAAK,GAAG,CAACZ,EAAK,QAAQ,CAAC,CAAC,CAAGA,EAAK,IAAI,CAAC,IAAI,GAASY,AAA8C,EAA9CA,KAAK,GAAG,CAACZ,EAAK,QAAQ,CAAC,CAAC,CAAGA,EAAK,IAAI,CAAC,KAAK,CAAI,GAC3FuC,EAAMxC,EAAiBC,EAAK,QAAQ,CAxHnC,OAwHmDD,EAAgB,CAAC,EAAIC,EAAK,IAAI,CAAC,GAAG,CAAG,GAAKD,EAAgB,CAAC,EAAIC,EAAK,IAAI,CAAC,MAAM,CAAG,EAG9I,6BAED,OAEF,GAAIA,EAAK,OAAO,CAAE,CAChB,IAAMC,EAAqBC,EAAO,MAAM,CAAG,GAAKoC,EAAUpC,CAAM,CAAC,EAAE,CAAEF,EAAK,OAAO,CAhIvE,MAiIJI,EAAOF,EAAO,KAAK,CAACD,EAAqB,EAAI,GAC7CI,EAAOD,CAAI,CAAC,EAAE,CACpB,GAAIC,GAAQ,CAACuG,GAAoBvG,EAAMF,GACrC,OAEF,MAAO,CAACJ,EAAiBI,KAAmBC,EAAK,AACnD,CACA,IAAMC,EAAoBH,EAAO,MAAM,CAAG,GAAKoC,EAAUpC,CAAM,CAACA,EAAO,MAAM,CAAG,EAAE,CAAEF,EAAK,OAAO,CAxIpF,MAyINM,EAASJ,EAAO,KAAK,CAAC,EAAGG,EAAoB,GAAK,IAClDE,EAAWD,CAAM,CAACA,EAAO,MAAM,CAAG,EAAE,CAC1C,GAAIC,CAAAA,GAAaqG,GAAoBrG,EAAUJ,GAG/C,MAAO,IAAIG,EAAQH,EAAgBJ,EAAgB,AACrD,EAAG,oBACGU,EAAmDL,EAAoB,AAACJ,IAC5E,IAAME,EAAOF,EAAK,IAAI,CAChBI,EAAS2C,EAAwB7C,EAAK,MAAM,EAAI,EAAE,EACxD,GAAIE,AAAkB,IAAlBA,EAAO,MAAM,CACf,MAAO,GAET,IAAML,EAAUG,EAAK,KAAK,CACpBC,EAAQD,EAAK,GAAG,CAChBG,EAAQN,EAAUE,EAAY,GAAG,CAACF,GAAW,KAAK,EAClDO,EAAMH,EAAQF,EAAY,GAAG,CAACE,GAAS,KAAK,EAClD,GAAI,CAACE,GAAS,CAACC,EACb,MAAO,GAET,IAAMC,EAASF,EAAM,CAAC,EAAI,EACpBG,EAASH,EAAM,CAAC,EAAI,EACpBI,EAAOH,EAAI,CAAC,EAAI,EAChBI,EAAOJ,EAAI,CAAC,EAAI,EAChB,CAACK,EAAGG,EAAE,CAAGV,EACf,OAAOoC,EAAM7B,EAAGG,EAlKJ,OAkKqBF,AAA0B,EAA1BA,KAAK,GAAG,CAACJ,EAASE,IAAaE,KAAK,GAAG,CAACL,EAASE,GAAQ,GAAK8B,EAAM5B,EAAGG,EAlK5F,OAkK6GF,AAA0B,EAA1BA,KAAK,GAAG,CAACL,EAASE,IAAaG,KAAK,GAAG,CAACJ,EAASE,GAAQ,CACpL,EAAG,oCACGA,EAAS,CACb,GA/JkB,EAiKlB,IACA,GACA,IACA,GACD,CACD,IAAK,IAAIT,EAAY,EAAGA,EAAY,EAAGA,IAAa,CAClD,IAAMA,EAAQD,EAAM,MAAM,CAAC,AAACA,GAAS,CAACA,EAAK,YAAY,EAAE,OAAO,CAAC,AAACA,GAAS,CAACD,EAAgBC,EAAM,IAAOD,EAAgBC,EAAM,IAAO,EAAE,MAAM,CAAC,AAACA,GAAS,EAAQA,GAC7JE,EAAQ,GACZ,IAAK,IAAIF,EAAI,EAAGA,EAAIC,EAAM,MAAM,EAAI,CAACC,EAAOF,IAC1C,IAAK,IAAII,EAAIJ,EAAI,EAAGI,EAAIH,EAAM,MAAM,EAAI,CAACC,EAAOE,IAAK,CACnD,IAAMD,EAAQF,CAAK,CAACD,EAAE,CAChBK,EAASJ,CAAK,CAACG,EAAE,CACvB,GAAID,EAAM,IAAI,GAAKE,EAAO,IAAI,EAAI,CAAEC,CAAAA,EAA0BH,EAAOE,IAAWE,EAAyBJ,EAAOE,EAAM,EACpH,SAEF,IAAMM,EAAqB,CAACL,EAA0BH,EAAOE,GAS7D,IAAK,IAAML,IARQ,CAACG,EAAOE,EAAO,CAAC,IAAI,CAAC,CAACL,EAAGC,KAC1C,IAAMC,EAAqBO,EAAiCT,GACtDI,EAAqBK,EAAiCR,UAC5D,AAAIC,IAAuBE,EAClBS,OAAOX,GAAsBW,OAAOT,GAEtCS,OAAO,CAACZ,EAAE,OAAO,EAAIY,OAAO,CAACb,EAAE,OAAO,CAC/C,GAC+B,CAC7B,IAAK,IAAMI,KAASM,EAAQ,CAC1B,IAAMP,EAAYK,EAAiBR,EAAMI,GACzC,GAAI,CAACD,EACH,SAEF,IAAME,EAAWN,EAAgB,CAAE,GAAGC,EAAK,IAAI,CAAE,OAAQG,CAAU,EAAGH,EAAK,OAAO,EAClF,IAAI,EAACK,GAAYJ,EAAM,IAAI,CACzB,AAACA,GAAUA,EAAM,IAAI,GAAKD,EAAK,IAAI,EAAKM,CAAAA,EAA0BD,EAAUJ,IAAUU,GAAsBJ,EAAyBF,EAAUJ,EAAK,EACtJ,GAGAD,EAAK,IAAI,CAAC,MAAM,CAAGG,EACnBD,EAAQ,GACR,MACF,CACA,GAAIA,EACF,KAEJ,CACF,CAEF,GAAI,CAACA,EACH,MAEJ,CACF,CAEA,SAAS4G,GAAoC9G,CAAK,CAAEC,CAAW,EAG7D,GAAM,CAAEC,cAAAA,CAAa,CAAE,eAAgBH,CAAU,CAAE,CAAG4D,EACpD1D,EAAY,MAAM,IAEdE,EAAkCC,EAAoB,CAACH,EAAMG,KACjE,IAAMD,EAAWF,EAAK,KAAK,CACrBI,EAAWJ,EAAK,GAAG,CACnBK,EAAoB,AAlOZuC,EAkOwBzC,GACtC,GAAIE,EAAkB,MAAM,GAAKF,EAAU,MAAM,CAAG,EAClD,MAAO,GAET,IAAMG,EAAc,CAACJ,EAAUE,EAAS,CAAC,MAAM,CAAC,AAACL,GAAO,EAAQA,GAChE,IAAK,IAAMA,KAAWM,EACpB,GAAIwD,GAAmB9D,EAAQ,CAAC,CAAEA,EAAQ,CAAC,CAAEE,EAAeK,EAAa,KAGrEuD,GAAmB9D,EAAQ,CAAC,CAAEA,EAAQ,CAAC,CAAED,EAAY,EAAE,CAAE,IAF3D,MAAO,GAMX,IAAK,IAAMG,KAASF,EAAO,CACzB,GAAIE,IAAUD,GAAQC,EAAM,YAAY,CACtC,SAEF,IAAMF,EAAcE,EAAM,MAAM,CAChC,GAAI,AAACF,IAAeA,CAAAA,EAAY,MAAM,CAAG,GAGzC,KAAK,IAAMC,KAAoBK,EAC7B,IAAK,IAAMJ,KAAgB,AAxPjB2C,EAwP6BE,EAAwB/C,IAC7D,GAAI4C,EAA6B3C,EAAkBC,EAAc,KA1P1D,GA6PHgE,GACFjE,EAAiB,CAAC,CAClBA,EAAiB,CAAC,CAClBC,EAAa,CAAC,CACdA,EAAa,CAAC,CAlQV,MA4PJ,MAAO,EAYb,CACF,CACA,MAAO,EACT,EAAG,mBACGG,EAAgCD,EAAoB,CAACJ,EAAQC,KACjE,GAAIA,EAAI,GAAKD,EAAO,MAAM,CACxB,OAEF,IAAME,EAAKF,CAAM,CAACC,EAAE,CACdG,EAAKJ,CAAM,CAACC,EAAI,EAAE,CAClBF,EAAKC,CAAM,CAACC,EAAI,EAAE,CAClBE,EAAKH,CAAM,CAACC,EAAI,EAAE,CAClBI,EAAKL,CAAM,CAACC,EAAI,EAAE,CAClBK,EAAyBmC,EAAoBvC,EAAIE,IAAOsC,EAAkBtC,EAAIL,IAAO0C,EAAoB1C,EAAII,IAAOuC,EAAkBvC,EAAIE,IAAOkC,EAAMrC,EAAIC,EArRrJ,OAqRuKoC,EAAMrC,EAAIG,EArRjL,OAqRmMkC,EAAMnC,EAAIL,EArR7M,OAqR+N,AAACK,CAAAA,EAAG,CAAC,CAAGF,EAAG,CAAC,AAAD,EAAMC,CAAAA,EAAG,CAAC,CAAGJ,EAAG,CAAC,AAAD,EAAK,EACrQQ,EAA2BmC,EAAkBxC,EAAIE,IAAOqC,EAAoBrC,EAAIL,IAAO2C,EAAkB3C,EAAII,IAAOsC,EAAoBtC,EAAIE,IAAOmC,EAAMtC,EAAIC,EAtRvJ,OAsRyKqC,EAAMtC,EAAIG,EAtRnL,OAsRqMmC,EAAMpC,EAAIL,EAtR/M,OAsRiO,AAACK,CAAAA,EAAG,CAAC,CAAGF,EAAG,CAAC,AAAD,EAAMC,CAAAA,EAAG,CAAC,CAAGJ,EAAG,CAAC,AAAD,EAAK,EAC7Q,GAAIO,GAA0BC,EAC5B,OAAOwC,EAAwB,IAAI/C,EAAO,KAAK,CAAC,EAAGC,EAAI,GAAII,KAAOL,EAAO,KAAK,CAACC,EAAI,GAAG,EAExF,GAAIA,EAAI,GAAKD,EAAO,MAAM,CACxB,OAEF,IAAMQ,EAAKR,CAAM,CAACC,EAAI,EAAE,CAClBQ,EAAiBiC,EAAkBxC,EAAIE,IAAOqC,EAAoBrC,EAAIL,IAAO2C,EAAkB3C,EAAII,IAAOsC,EAAoBtC,EAAIE,IAAOqC,EAAkBrC,EAAIG,IAAO+B,EAAMrC,EAAIG,EA9R1K,OA8R4LkC,EAAMrC,EAAIM,EA9RtM,OA8RwN+B,EAAMxC,EAAII,EA9RlO,OA8RoP,AAACJ,CAAAA,EAAG,CAAC,CAAGK,EAAG,CAAC,AAAD,EAAMC,CAAAA,EAAG,CAAC,CAAGF,EAAG,CAAC,AAAD,EAAK,EAC1RO,EAAmB+B,EAAoBvC,EAAIE,IAAOsC,EAAkBtC,EAAIL,IAAO0C,EAAoB1C,EAAII,IAAOuC,EAAkBvC,EAAIE,IAAOoC,EAAoBpC,EAAIG,IAAOgC,EAAMtC,EAAIG,EA/R9K,OA+RgMmC,EAAMtC,EAAIM,EA/R1M,OA+R4NgC,EAAMzC,EAAII,EA/RtO,OA+RwP,AAACJ,CAAAA,EAAG,CAAC,CAAGK,EAAG,CAAC,AAAD,EAAMC,CAAAA,EAAG,CAAC,CAAGF,EAAG,CAAC,AAAD,EAAK,EACpS,GAAI,AAACM,GAAmBC,EAGxB,OAAOqC,EAAwB,IAAI/C,EAAO,KAAK,CAAC,EAAGC,EAAI,GAAIO,KAAOR,EAAO,KAAK,CAACC,EAAI,GAAG,CACxF,EAAG,iBACH,IAAK,IAAIA,EAAY,EAAGA,EAxED,EAwE6BA,IAAa,CAC/D,IAAIA,EAAQ,GACZ,IAAK,IAAMC,KAAQF,EAAO,CACxB,GAAIE,EAAK,YAAY,CACnB,SAEF,IAAMF,EAAS+C,EAAwB7C,EAAK,MAAM,EAAI,EAAE,EACxD,IAAK,IAAIE,EAAI,EAAGA,GAAKJ,EAAO,MAAM,CAAG,EAAGI,IAAK,CAC3C,IAAML,EAAYM,EAAcL,EAAQI,GACxC,GAAI,AAACL,GAAcI,EAAgBD,EAAMH,IAGzCG,EAAK,MAAM,CAAGH,EACdE,EAAQ,GACR,MACF,CACA,GAAIA,EACF,KAEJ,CACA,GAAI,CAACA,EACH,MAEJ,CACF,CAEA,SAAS8G,GAAiC/G,CAAK,CAAEC,CAAW,EAI1D,GAAM,CAAEC,cAAAA,CAAa,CAAE,eAAgBH,CAAU,CAAE,CAAG4D,EACpD1D,EAAY,MAAM,IAEdE,EAAeH,EAAM,MAAM,CAAC,AAACA,GAAS,CAACA,EAAK,YAAY,EACxDK,EAA4BD,EAAoB,CAACJ,EAAMC,EAAiBC,IAAgB6C,EAC5F/C,IAASC,EAAkBC,GAAe,EAAE,CAAGF,EAAK,MAAM,EAAI,EAAE,EAC/D,aACGM,EAAsCF,EAAoB,CAACJ,EAAiBC,KAChF,IAAIC,EAAQ,EACZ,IAAK,IAAIE,EAAI,EAAGA,EAAID,EAAa,MAAM,CAAEC,IAAK,CAC5C,IAAML,EAAgB,AA3UV8C,EA2UsBxC,EAAUF,CAAY,CAACC,EAAE,CAAEJ,EAAiBC,IAC9E,IAAK,IAAIK,EAAIF,EAAI,EAAGE,EAAIH,EAAa,MAAM,CAAEG,IAAK,CAChD,IAAMF,EAAiB,AA7UbyC,EA8URxC,EAAUF,CAAY,CAACG,EAAE,CAAEN,EAAiBC,IAE9C,IAAK,IAAMD,KAAgBD,EACzB,IAAK,IAAME,KAAiBG,EACtB8D,GACFlE,EAAa,CAAC,CACdA,EAAa,CAAC,CACdC,EAAc,CAAC,CACfA,EAAc,CAAC,CAxVb,OA2VFC,GAIR,CACF,CACA,OAAOA,CACT,EAAG,uBACGK,EAA6BH,EAAoB,AAACJ,IACtD,IAAMC,EAAW,AAlWH4C,EAkWe7C,GAC7B,GAAIC,AAAoB,IAApBA,EAAS,MAAM,CACjB,OAEF,IAAMC,EAASD,CAAQ,CAAC,EAAE,CAC1B,GAAIA,CAAQ,CAAC,EAAE,CAAC,UAAU,GAAKC,EAAO,UAAU,EAAID,CAAQ,CAAC,EAAE,CAAC,UAAU,GAAKC,EAAO,UAAU,CAGhG,MAAO,CACL,MAAOA,EAAO,KAAK,CACnB,WAAYA,EAAO,UAAU,CAC7B,SAAUA,EAAO,QAAQ,CACzB,QAASA,CACX,CACF,EAAG,cACGM,EAAmCJ,EAAoB,CAACJ,EAAMC,KAClE,IAAMG,EAAc,CAACJ,EAAK,KAAK,CAAEA,EAAK,GAAG,CAAC,CAAC,MAAM,CAC/C,AAACA,GAAO,EAAQA,GAElB,OAAOE,EAAc,MAAM,CAAC,AAACF,IAC3B,GAAII,EAAY,QAAQ,CAACJ,EAAM,EAAE,EAC/B,MAAO,GAET,IAAME,EAAOF,EAAM,IAAI,QACvB,AAAIC,EAAK,UAAU,CAEV,AADU0C,EAAc1C,EAAK,CAAC,CAAC,CAAC,CAAEA,EAAK,CAAC,CAAC,CAAC,CAAEC,EAAK,IAAI,CAAEA,EAAK,KAAK,GA5X/D,GA6XwBD,EAAK,CAAC,CAAC,CAAC,EAAIC,EAAK,GAAG,CA9D5C,GA8DyDD,EAAK,CAAC,CAAC,CAAC,EAAIC,EAAK,MAAM,CA9DhF,EAiEJ,AADUyC,EAAc1C,EAAK,CAAC,CAAC,CAAC,CAAEA,EAAK,CAAC,CAAC,CAAC,CAAEC,EAAK,GAAG,CAAEA,EAAK,MAAM,GA/X7D,GAgYsBD,EAAK,CAAC,CAAC,CAAC,EAAIC,EAAK,IAAI,CAjE3C,GAiEwDD,EAAK,CAAC,CAAC,CAAC,EAAIC,EAAK,KAAK,CAjE9E,CAkEb,EACF,EAAG,oBACGO,EAAwCL,EAAoB,CAACJ,EAAQC,EAAMC,KAC/E,IAAME,EAAYJ,EAAO,GAAG,CAAC,AAACA,GAAW,EAAE,GAAGA,CAAK,AAAC,IACpD,GAAIC,EAAK,UAAU,CACjBG,CAAS,CAACH,EAAK,KAAK,CAAC,CAAC,CAAC,CAAGC,EAC1BE,CAAS,CAACH,EAAK,KAAK,CAAG,EAAE,CAAC,CAAC,CAAGC,OACzB,IAAID,EAAK,QAAQ,CAItB,MAHAG,CAAAA,CAAS,CAACH,EAAK,KAAK,CAAC,CAAC,CAAC,CAAGC,EAC1BE,CAAS,CAACH,EAAK,KAAK,CAAG,EAAE,CAAC,CAAC,CAAGC,EAIhC,IAAMH,EAAawE,GAAiBxB,EAAwB3C,IAC5D,OAAO,AA9YOyC,EA8YK9C,GAAY,MAAM,GAAKA,EAAW,MAAM,CAAG,EAAIA,EAAa,KAAK,CACtF,EAAG,yBACGW,EAAkCN,EAAoB,CAACJ,EAAMC,EAAWG,KAC5E,IAAMG,EAAc,CAACP,EAAK,KAAK,CAAEA,EAAK,GAAG,CAAC,CAAC,MAAM,CAC/C,AAACA,GAAO,EAAQA,GAEZQ,EAAoB,AApZZqC,EAoZwB5C,GACtC,GAAIO,EAAkB,MAAM,GAAKP,EAAU,MAAM,CAAG,EAClD,MAAO,GAET,IAAK,IAAMD,KAAWQ,EACpB,GAAIsD,GAAmB9D,EAAQ,CAAC,CAAEA,EAAQ,CAAC,CAAEE,EAAeK,EAAa,KAGrEuD,GAAmB9D,EAAQ,CAAC,CAAEA,EAAQ,CAAC,CAAED,EAAY,EAAE,CAAE,IAF3D,MAAO,GAMX,IAAK,IAAME,KAASE,EAClB,GAAIF,IAAUD,EAGd,KAAK,IAAMA,KAAoBQ,EAC7B,IAAK,IAAMN,KAAgB,AArajB2C,EAqa6BxC,EAAUJ,IAC/C,GAAI2C,EAA6B5C,EAAkBE,EAAc,KAva1D,EAwaL,MAAO,EAGb,CAEF,OAAOI,EAAoBN,EAAMC,IAAcG,CACjD,EAAG,mBACH,IAAK,IAAIJ,EAAY,EAAGA,EA9GD,EA8G6BA,IAAa,CAC/D,IAAMA,EAAmBM,IACrBL,EAAQ,GACZ,IAAK,IAAMC,KAAQC,EAAc,CAC/B,IAAMC,EAASC,EAAUH,GACnBH,EAAOQ,EAAWH,GACxB,GAAI,CAACL,EACH,SAEF,IAAMI,EAAWK,EAAiBN,EAAMH,EAAK,OAAO,EACpD,GAAII,AAAoB,IAApBA,EAAS,MAAM,EAUnB,IAAK,IAAME,KAPIN,EAAK,UAAU,CAAG,CAC/Ba,KAAK,GAAG,IAAIT,EAAS,GAAG,CAAC,AAACH,GAAUA,EAAM,IAAI,CAAC,GAAG,GA7HtC,GA8HZY,KAAK,GAAG,IAAIT,EAAS,GAAG,CAAC,AAACH,GAAUA,EAAM,IAAI,CAAC,MAAM,GA9HzC,GA+Hb,CAAG,CACFY,KAAK,GAAG,IAAIT,EAAS,GAAG,CAAC,AAACH,GAAUA,EAAM,IAAI,CAAC,IAAI,GAhIvC,GAiIZY,KAAK,GAAG,IAAIT,EAAS,GAAG,CAAC,AAACH,GAAUA,EAAM,IAAI,CAAC,KAAK,GAjIxC,GAkIb,CAC2B,CAC1B,IAAMG,EAAYM,EAAsBL,EAAQL,EAAK,OAAO,CAAEM,GAC9D,GAAI,AAACF,GAAcO,EAAgBR,EAAMC,EAAWH,IAGpDE,EAAK,MAAM,CAAGC,EACdF,EAAQ,GACR,MACF,CACA,GAAIA,EACF,MAEJ,CACA,GAAI,CAACA,EACH,MAEJ,CACF,CAEA,SAAS+G,GAAgChH,CAAK,CAAEC,CAAW,EAEzD,IAAMC,EAAiCE,EAAoB,AAACJ,IAC1D,IAAMC,EAAOD,EAAK,cAAc,CAChC,GAAI,AAACC,GAAQ,AAAqB,UAArB,OAAOA,EAAK,IAAI,EAAiB,AAAsB,UAAtB,OAAOA,EAAK,KAAK,EAAiB,AAAoB,UAApB,OAAOA,EAAK,GAAG,EAAiB,AAAuB,UAAvB,OAAOA,EAAK,MAAM,EAAkBY,OAAO,QAAQ,CAACZ,EAAK,IAAI,GAAMY,OAAO,QAAQ,CAACZ,EAAK,KAAK,GAAMY,OAAO,QAAQ,CAACZ,EAAK,GAAG,GAAMY,OAAO,QAAQ,CAACZ,EAAK,MAAM,IAAKA,CAAAA,EAAK,KAAK,EAAIA,EAAK,IAAI,AAAD,IAAKA,CAAAA,EAAK,MAAM,EAAIA,EAAK,GAAG,AAAD,EAGlU,MAAO,CAAE,KAAMA,EAAK,IAAI,CAAE,MAAOA,EAAK,KAAK,CAAE,IAAKA,EAAK,GAAG,CAAE,OAAQA,EAAK,MAAM,AAAC,CAClF,EAAG,kBACGF,EAAkCK,EAAoB,AAACJ,IAC3D,GAAI,CAACA,EAAK,OAAO,EAAIA,EAAK,QAAQ,CAChC,OAEF,IAAMC,EAAeD,EAAK,SAAS,CAC7BI,EAAY,AAAwB,UAAxB,OAAOH,EAA4BA,EAAa,WAAW,GAAK,GAClF,GAAIG,AAAc,OAAdA,GAAsBA,AAAc,OAAdA,GAAsBA,AAAc,OAAdA,EAC9C,OAEF,IAAML,EAAOG,EAAeF,GACtBG,EAAIH,EAAK,CAAC,CACVK,EAASL,EAAK,MAAM,CAC1B,GAAI,CAACD,GAAQ,AAAa,UAAb,OAAOI,GAAkB,AAAkB,UAAlB,OAAOE,GAAuB,CAACQ,OAAO,QAAQ,CAACV,IAAM,CAACU,OAAO,QAAQ,CAACR,IAAWA,GAAU,EAC/H,OAEF,IAAMC,EAAaP,EAAK,KAAK,CAAGA,EAAK,IAAI,CACnCQ,EAAcR,EAAK,MAAM,CAAGA,EAAK,GAAG,CAC1C,GAAIQ,CAAAA,CAAAA,GAAe,KAAKD,CAAAA,EAAaC,CAAU,EAG/C,MAAO,CAAEP,KAAAA,EAAMD,KAAAA,CAAK,CACtB,EAAG,mBACGI,EAAmDC,EAAoB,CAACJ,EAASC,KACrF,GAAI,CAACD,EAAQ,UAAU,CACrB,MAAO,GAET,IAAME,EAAIF,EAAQ,CAAC,CAAC,CAAC,OACrB,CAAIE,CAAAA,GAAKD,EAAK,GAAG,CA3fL,IA2fgB,IAAKC,CAAAA,GAAKD,EAAK,MAAM,CA3frC,IA2fgD,GAGrD0C,EAAc3C,EAAQ,CAAC,CAAC,CAAC,CAAEA,EAAQ,CAAC,CAAC,CAAC,CAAEC,EAAK,IAAI,CAAEA,EAAK,KAAK,GA7fvD,CA8ff,EAAG,oCACGI,EAAQ,IAAIJ,EAAY,MAAM,GAAG,CAAC,GAAG,CAACF,GAAiB,MAAM,CAAC,AAACC,GAAS,EAAQA,GACtF,GAAIK,AAAiB,IAAjBA,EAAM,MAAM,CACd,OAEF,IAAIC,EAAW,EACf,IAAK,IAAML,KAAQD,EACjB,IAAIC,EAAK,YAAY,CAIrB,IAAK,IAAMD,KAAW,AAxgBR6C,EAugBCE,EAAwB9C,EAAK,MAAM,EAAI,EAAE,GAEtD,IAAK,IAAMA,KAAQI,EACZF,EAAiCH,EAASC,EAAK,IAAI,GAGxDK,CAAAA,EAAWM,KAAK,GAAG,CAACN,EAAUL,EAAK,IAAI,CAAC,MAAM,CAAGD,EAAQ,CAAC,CAAC,CAAC,CAvDhD,EAuD4D,EAI9E,IAAIM,CAAAA,GAnhBU,IAmhBU,EAGxB,IAAK,IAAMN,KAAQK,EAAO,CACxB,IAAMJ,EAAID,EAAK,IAAI,CAAC,CAAC,CACfE,EAASF,EAAK,IAAI,CAAC,MAAM,AACd,WAAb,OAAOC,GAAkB,AAAkB,UAAlB,OAAOC,GAAwBW,OAAO,QAAQ,CAACZ,IAAOY,OAAO,QAAQ,CAACX,KAAWA,CAAAA,GAAU,KAGxHF,EAAK,IAAI,CAAC,CAAC,CAAGC,EAAIK,EAAW,EAC7BN,EAAK,IAAI,CAAC,MAAM,CAAGE,EAASI,EAC5BN,EAAK,IAAI,CAAC,cAAc,CAAG,CACzB,GAAGA,EAAK,IAAI,CACZ,IAAKA,EAAK,IAAI,CAAC,GAAG,CAAGM,EACrB,OAAQN,EAAK,IAAI,CAAC,MAAM,CAAGM,CAC7B,EACF,CACF,CAEA,SAAS2G,GAAmCjH,CAAK,CAAEC,CAAW,EAE5D,IAAMC,EAAiCE,EAAoB,AAACJ,IAC1D,IAAMC,EAAOD,EAAK,cAAc,CAChC,GAAI,AAACC,GAAQ,AAAqB,UAArB,OAAOA,EAAK,IAAI,EAAiB,AAAsB,UAAtB,OAAOA,EAAK,KAAK,EAAiB,AAAoB,UAApB,OAAOA,EAAK,GAAG,EAAiB,AAAuB,UAAvB,OAAOA,EAAK,MAAM,EAAkBY,OAAO,QAAQ,CAACZ,EAAK,IAAI,GAAMY,OAAO,QAAQ,CAACZ,EAAK,KAAK,GAAMY,OAAO,QAAQ,CAACZ,EAAK,GAAG,GAAMY,OAAO,QAAQ,CAACZ,EAAK,MAAM,IAAKA,CAAAA,EAAK,KAAK,EAAIA,EAAK,IAAI,AAAD,IAAKA,CAAAA,EAAK,MAAM,EAAIA,EAAK,GAAG,AAAD,EAGlU,MAAO,CAAE,KAAMA,EAAK,IAAI,CAAE,MAAOA,EAAK,KAAK,CAAE,IAAKA,EAAK,GAAG,CAAE,OAAQA,EAAK,MAAM,AAAC,CAClF,EAAG,kBACGF,EAAmCK,EAAoB,AAACJ,IAC5D,GAAI,CAACA,EAAK,OAAO,EAAIA,EAAK,QAAQ,EAI9B,AAAiB,OADAA,EAAK,SAAS,CAFjC,OAMF,IAAMC,EAAOC,EAAeF,GACtBI,EAAIJ,EAAK,CAAC,CACVD,EAAQC,EAAK,KAAK,CACxB,GAAI,CAACC,GAAQ,AAAa,UAAb,OAAOG,GAAkB,AAAiB,UAAjB,OAAOL,GAAsB,CAACc,OAAO,QAAQ,CAACT,IAAM,CAACS,OAAO,QAAQ,CAACd,IAAUA,GAAS,EAC5H,OAEF,IAAMI,EAAaF,EAAK,KAAK,CAAGA,EAAK,IAAI,CACnCI,EAAcJ,EAAK,MAAM,CAAGA,EAAK,GAAG,CAC1C,GAAIE,CAAAA,CAAAA,GAAc,KAAKE,CAAAA,EAAcF,CAAS,EAG9C,MAAO,CAAEH,KAAAA,EAAMC,KAAAA,CAAK,CACtB,EAAG,oBACGE,EAAiDC,EAAoB,CAACJ,EAASC,KACnF,GAAI,CAACD,EAAQ,QAAQ,CACnB,MAAO,GAET,IAAME,EAAIF,EAAQ,CAAC,CAAC,CAAC,OACrB,CAAIE,CAAAA,GAAKD,EAAK,IAAI,CAzkBN,IAykBiB,IAAKC,CAAAA,GAAKD,EAAK,KAAK,CAzkBrC,IAykBgD,GAGrD0C,EAAc3C,EAAQ,CAAC,CAAC,CAAC,CAAEA,EAAQ,CAAC,CAAC,CAAC,CAAEC,EAAK,GAAG,CAAEA,EAAK,MAAM,GA3kBvD,CA4kBf,EAAG,kCACGI,EAAmDD,EAAoB,CAACJ,EAASC,KACrF,GAAI,CAACD,EAAQ,UAAU,CACrB,MAAO,GAET,IAAME,EAAIF,EAAQ,CAAC,CAAC,CAAC,OACrB,CAAIE,CAAAA,GAAKD,EAAK,GAAG,CAnlBL,IAmlBgB,IAAKC,CAAAA,GAAKD,EAAK,MAAM,CAnlBrC,IAmlBgD,GAGrD0C,EAAc3C,EAAQ,CAAC,CAAC,CAAC,CAAEA,EAAQ,CAAC,CAAC,CAAC,CAAEC,EAAK,IAAI,CAAEA,EAAK,KAAK,GArlBvD,CAslBf,EAAG,oCACGK,EAAQ,IAAIL,EAAY,MAAM,GAAG,CAAC,GAAG,CAACF,GAAkB,MAAM,CAAC,AAACC,GAAS,EAAQA,GACvF,GAAIM,AAAiB,IAAjBA,EAAM,MAAM,CACd,OAEF,IAAIC,EAAY,EAChB,IAAK,IAAMN,KAAQD,EACjB,IAAIC,EAAK,YAAY,CAIrB,KAAK,IAAMD,KAAW,AAhmBR6C,EA+lBCE,EAAwB9C,EAAK,MAAM,EAAI,EAAE,GAEtD,IAAK,IAAMA,KAAQK,EACjB,GAAIH,EAA+BH,EAASC,EAAK,IAAI,EACnDM,EAAYK,KAAK,GAAG,CAACL,EAAWN,EAAK,IAAI,CAAC,KAAK,CAAGD,EAAQ,CAAC,CAAC,CAAC,CA9DnD,QA+DL,GAAIK,EAAiCL,EAASC,EAAK,IAAI,EAAG,CAC/D,IAAMC,EAAcU,KAAK,GAAG,CAACZ,EAAQ,CAAC,CAAC,CAAC,CAAEA,EAAQ,CAAC,CAAC,CAAC,EACrDO,EAAYK,KAAK,GAAG,CAACL,EAAWN,EAAK,IAAI,CAAC,KAAK,CAAGC,EAjExC,EAkEZ,CAEJ,CAEF,IAAIK,CAAAA,GA7mBU,IA6mBW,EAGzB,IAAK,IAAMP,KAAQM,EAAO,CACxB,IAAML,EAAID,EAAK,IAAI,CAAC,CAAC,CACfE,EAAQF,EAAK,IAAI,CAAC,KAAK,AACZ,WAAb,OAAOC,GAAkB,AAAiB,UAAjB,OAAOC,GAAuBW,OAAO,QAAQ,CAACZ,IAAOY,OAAO,QAAQ,CAACX,KAAUA,CAAAA,GAAS,KAGrHF,EAAK,IAAI,CAAC,CAAC,CAAGC,EAAIM,EAAY,EAC9BP,EAAK,IAAI,CAAC,KAAK,CAAGE,EAAQK,EAC1BP,EAAK,IAAI,CAAC,cAAc,CAAG,CACzB,GAAGA,EAAK,IAAI,CACZ,KAAMA,EAAK,IAAI,CAAC,IAAI,CAAGO,EACvB,MAAOP,EAAK,IAAI,CAAC,KAAK,CAAGO,CAC3B,EACF,CACF,CAEA,SAAS2G,GAA8ClH,CAAK,CAAEC,CAAW,EAGvE,GAAM,CAAEC,cAAAA,CAAa,CAAE,CAAGyD,EAAuB1D,EAAY,MAAM,IAC7DF,EAAeC,EAAM,MAAM,CAAC,AAACA,GAAS,CAACA,EAAK,YAAY,EACxDG,EAAuCC,EAAoB,CAACJ,EAAMC,EAA+B,IAAIgB,GAAK,GAAK8B,EACnH9C,EAAa,GAAG,CAACD,IAASA,EAAK,MAAM,EAAI,EAAE,EAC1C,wBACGK,EAAgCD,EAAoB,CAACJ,EAA+B,IAAIiB,GAAK,IACjG,IAAIhB,EAAQ,EACZ,IAAK,IAAIC,EAAI,EAAGA,EAAIH,EAAa,MAAM,CAAEG,IAAK,CAC5C,IAAME,EAAgB,AAzoBVyC,EAyoBsB1C,EAAqBJ,CAAY,CAACG,EAAE,CAAEF,IACxE,IAAK,IAAIK,EAAIH,EAAI,EAAGG,EAAIN,EAAa,MAAM,CAAEM,IAAK,CAChD,IAAMH,EAAiB,AA3oBb2C,EA2oByB1C,EAAqBJ,CAAY,CAACM,EAAE,CAAEL,IACzE,IAAK,IAAMA,KAAgBI,EACzB,IAAK,IAAMA,KAAiBF,EACtBgE,GACFlE,EAAa,CAAC,CACdA,EAAa,CAAC,CACdI,EAAc,CAAC,CACfA,EAAc,CAAC,CAppBb,OAupBFH,GAIR,CACF,CACA,OAAOA,CACT,EAAG,iBACGK,EAA6BF,EAAoB,CAACJ,EAA+B,IAAIiB,GAAK,GAAKlB,EAAa,MAAM,CACtH,CAACE,EAAKC,IAASD,EAAM6C,EAAqB3C,EAAqBD,EAAMF,IACrE,GACC,cACGO,EAAkCH,EAAoB,AAACJ,IAC3D,IAAMC,EAASE,EAAqBH,GACpC,GAAIC,EAAO,MAAM,CAAG,EAClB,OAEF,IAAMC,EAAYD,CAAM,CAACA,EAAO,MAAM,CAAG,EAAE,CACrCG,EAAWH,CAAM,CAACA,EAAO,MAAM,CAAG,EAAE,CAC1C,GAAI,AAACwC,EAAoBvC,EAAWE,EA1qBxB,OA0qBiDsC,EAAkBxC,EAAWE,EA1qB9E,MA6qBZ,MAAO,CAAEF,UAAAA,EAAWE,SAAAA,CAAS,CAC/B,EAAG,mBACGI,EAA+CJ,EAAoB,CAACJ,EAAMC,KAC9E,IAMIC,EANEE,EAASD,EAAqBH,GACpC,GAAII,EAAO,MAAM,CAAG,EAClB,OAEF,IAAML,EAAQK,CAAM,CAAC,EAAE,CACjBC,EAAYD,CAAM,CAAC,EAAE,CAE3B,GAAIqC,EAAoB1C,EAAOM,EAvrBnB,MAwrBVH,EAAY,CAAE,EAAGG,EAAU,CAAC,CAAE,EAAGJ,EAAK,SAAS,CAAC,CAAC,AAAC,OAC7C,IAAIyC,EAAkB3C,EAAOM,EAzrBxB,MA4rBV,OAFAH,EAAY,CAAE,EAAGD,EAAK,SAAS,CAAC,CAAC,CAAE,EAAGI,EAAU,CAAC,AAAC,EAIpD,IAAMC,EAAYiE,GAChBxB,EAAwB,CAAChD,EAAOM,EAAWH,EAAWD,EAAK,SAAS,CAAEA,EAAK,QAAQ,CAAC,GAEtF,OAAO,AA/rBO4C,EA+rBKvC,GAAW,MAAM,GAAKA,EAAU,MAAM,CAAG,EAAIA,EAAY,KAAK,CACnF,EAAG,gCACGG,EAAiCL,EAAoB,CAACJ,EAAMC,KAChE,IAAMG,EAAc,CAACJ,EAAK,KAAK,CAAEA,EAAK,GAAG,CAAC,CAAC,MAAM,CAC/C,AAACA,GAAO,EAAQA,GAElB,IAAK,IAAMA,KAAW,AArsBR6C,EAqsBoB5C,GAChC,GAAI6D,GAAmB9D,EAAQ,CAAC,CAAEA,EAAQ,CAAC,CAAEE,EAAeE,EAAa,IACvE,MAAO,GAGX,MAAO,EACT,EAAG,kBACGM,EAAqCN,EAAoB,CAACJ,EAAMC,EAAMC,KAC1E,IAAK,IAAME,KAASL,EAClB,GAAIK,IAAUJ,EAGd,KAAK,IAAMA,KAAoB,AAjtBnB6C,EAitB+B5C,GACzC,IAAK,IAAMA,KAAgB,AAltBjB4C,EAktB6B1C,EAAqBC,EAAOF,IACjE,GAAI0C,EAA6B5C,EAAkBC,EAAc,KAptB1D,EAqtBL,MAAO,EAGb,CAEF,MAAO,EACT,EAAG,sBACGU,EAAkCP,EAAoB,CAACJ,EAAMC,EAAMC,IAAiB,CAACO,EAAeT,EAAMC,IAAS,CAACS,EAAmBV,EAAMC,EAAMC,GAAe,mBAClKY,EAAqCV,EAAoB,KAC7D,IAAMJ,EAAyB,IAAIiB,IACnC,IAAK,IAAMf,KAAQH,EAAc,CAC/B,IAAMK,EAAQF,EAAK,GAAG,CACtB,GAAI,CAACE,GAAS,CAACH,EAAY,GAAG,CAACG,IAI3B,AADWD,EAAqBD,GACzB,MAAM,CAAG,EAHlB,SAMF,IAAMH,EAASC,EAAO,GAAG,CAACI,IAAU,EAAE,CACtCL,EAAO,IAAI,CAACG,GACZF,EAAO,GAAG,CAACI,EAAOL,EACpB,CACA,OAAOC,CACT,EAAG,sBACH,IAAK,IAAIA,EAAY,EAAGA,EA7GD,EA6G6BA,IAAa,CAC/D,IAKIA,EALEC,EAAmBI,IACzB,GAAIJ,AAAqB,IAArBA,EACF,OAEF,IAAMC,EAAeI,IAEjBF,EAAgBH,EAChBF,EAAYG,EAChB,IAAK,IAAMA,KAAoBY,IAAqB,MAAM,GACxD,IAAK,IAAIX,EAAI,EAAGA,EAAID,EAAiB,MAAM,CAAEC,IAC3C,IAAK,IAAIM,EAAIN,EAAI,EAAGM,EAAIP,EAAiB,MAAM,CAAEO,IAAK,CACpD,IAAMC,EAAQR,CAAgB,CAACC,EAAE,CAC3BW,EAASZ,CAAgB,CAACO,EAAE,CAC5BM,EAAYR,EAAgBG,GAC5BM,EAAaT,EAAgBO,GACnC,GAAI,CAACC,GAAa,CAACC,EACjB,SAEF,IAAME,EAAiBV,EAA6BE,EAAOM,GACrDG,EAAkBX,EAA6BM,EAAQC,GAC7D,GAAI,CAACG,GAAkB,CAACC,EACtB,SAEF,IAAMC,EAA+B,IAAIH,IAAI,CAC3C,CAACP,EAAOQ,EAAe,CACvB,CAACJ,EAAQK,EAAgB,CAC1B,EACD,GAAI,CAACR,EAAgBD,EAAOQ,EAAgBE,IAAiB,CAACT,EAAgBG,EAAQK,EAAiBC,GACrG,SAEF,IAAMC,EAAqBhB,EAAce,GACnCE,EAAiBhB,EAAWc,IAC9BC,CAAAA,GAAsBpB,CAAe,IAGrCoB,EAAqBjB,GAAiBiB,IAAuBjB,GAAiBkB,GAAkBvB,IAGpGC,EAAmBoB,EACnBhB,EAAgBiB,EAChBtB,EAAYuB,GACd,CAGJ,GAAI,CAACtB,EACH,OAEF,IAAK,GAAM,CAACC,EAAMC,EAAO,GAAIF,EAC3BC,EAAK,MAAM,CAAGC,CAElB,CACF,CAEA,SAASiH,GAAqCnH,CAAK,CAAEC,CAAW,EAK9D,GAAM,CAAEC,cAAAA,CAAa,CAAE,eAAgBH,CAAU,CAAE,CAAG4D,EACpD1D,EAAY,MAAM,IAEdE,EAAeH,EAAM,MAAM,CAAC,AAACA,GAAS,CAACA,EAAK,YAAY,EACxDK,EAAuCD,EAAoB,CAACJ,EAAMC,EAA+B,IAAIgB,GAAK,GAAK8B,EACnH9C,EAAa,GAAG,CAACD,IAASA,EAAK,MAAM,EAAI,EAAE,EAC1C,wBACGM,EAAsCF,EAAoB,CAACJ,EAA+B,IAAIiB,GAAK,IACvG,IAAIhB,EAAQ,EACZ,IAAK,IAAIC,EAAI,EAAGA,EAAIC,EAAa,MAAM,CAAED,IAAK,CAC5C,IAAME,EAAgB,AAlzBVyC,EAkzBsBxC,EAAqBF,CAAY,CAACD,EAAE,CAAEF,IACxE,IAAK,IAAID,EAAIG,EAAI,EAAGH,EAAII,EAAa,MAAM,CAAEJ,IAAK,CAChD,IAAMG,EAAiB,AApzBb2C,EAozByBxC,EAAqBF,CAAY,CAACJ,EAAE,CAAEC,IACzE,IAAK,IAAMA,KAAgBI,EACzB,IAAK,IAAMA,KAAiBF,EACtBgE,GACFlE,EAAa,CAAC,CACdA,EAAa,CAAC,CACdI,EAAc,CAAC,CACfA,EAAc,CAAC,CA7zBb,OAg0BFH,GAIR,CACF,CACA,OAAOA,CACT,EAAG,uBACGM,EAA6BH,EAAoB,CAACJ,EAA+B,IAAIiB,GAAK,GAAKd,EAAa,MAAM,CACtH,CAACF,EAAKC,IAASD,EAAM6C,EAAqBzC,EAAqBH,EAAMF,IACrE,GACC,cACGQ,EAAmCJ,EAAoB,AAACJ,IAC5D,IAAME,EAAQF,EAAK,KAAK,CAClBI,EAAQJ,EAAK,GAAG,CAChBD,EAAUG,EAAQD,EAAY,GAAG,CAACC,GAAS,KAAK,EAChDC,EAAUC,EAAQH,EAAY,GAAG,CAACG,GAAS,KAAK,EAChDC,EAAMN,EAAUuD,EAAiBvD,GAAW,KAAK,EACjDO,EAAMH,EAAUmD,EAAiBnD,GAAW,KAAK,EACvD,OAAOE,GAAOC,EAAM,CAAED,IAAAA,EAAKC,IAAAA,CAAI,EAAI,KAAK,CAC1C,EAAG,oBACGG,EAAyCL,EAAoB,CAACJ,EAAMC,EAAQC,KAChF,GAAIA,EAAQ,KAAK,EAAI,GAAKA,EAAQ,KAAK,CAAG,GAAKD,EAAO,MAAM,CAAG,EAC7D,OAEF,IAAMG,EAAgBI,EAAiBR,GACvC,GAAKI,GAGL,GAAIF,EAAQ,QAAQ,CAAE,CACpB,IAAMH,EAAQG,EAAQ,CAAC,CAAC,CAAC,CACnBC,EAAYS,KAAK,GAAG,CAACR,EAAc,GAAG,CAAC,IAAI,CAAEA,EAAc,GAAG,CAAC,IAAI,EACnEC,EAAaO,KAAK,GAAG,CAACR,EAAc,GAAG,CAAC,KAAK,CAAEA,EAAc,GAAG,CAAC,KAAK,EACtEE,EAAOP,EAAQI,EAj2BX,KAi2BmC,OAASJ,EAAQM,EAj2BpD,KAi2B6E,QAAU,KAAK,EACtG,GAAI,CAACC,EACH,OAEF,MAAO,CACLN,KAAAA,EACAC,OAAAA,EACA,aAAcC,EAAQ,KAAK,CAC3B,KAAM,WACNI,KAAAA,EACAP,MAAAA,EACA,IAAKa,KAAK,GAAG,CAACV,EAAQ,CAAC,CAAC,CAAC,CAAEA,EAAQ,CAAC,CAAC,CAAC,EACtC,IAAKU,KAAK,GAAG,CAACV,EAAQ,CAAC,CAAC,CAAC,CAAEA,EAAQ,CAAC,CAAC,CAAC,CACxC,CACF,CACA,GAAIA,EAAQ,UAAU,CAAE,CACtB,IAAMH,EAAQG,EAAQ,CAAC,CAAC,CAAC,CACnBC,EAAWS,KAAK,GAAG,CAACR,EAAc,GAAG,CAAC,GAAG,CAAEA,EAAc,GAAG,CAAC,GAAG,EAChEC,EAAcO,KAAK,GAAG,CAACR,EAAc,GAAG,CAAC,MAAM,CAAEA,EAAc,GAAG,CAAC,MAAM,EACzEE,EAAOP,EAAQI,EAp3BX,KAo3BkC,MAAQJ,EAAQM,EAp3BlD,KAo3B4E,SAAW,KAAK,EACtG,GAAI,CAACC,EACH,OAEF,MAAO,CACLN,KAAAA,EACAC,OAAAA,EACA,aAAcC,EAAQ,KAAK,CAC3B,KAAM,aACNI,KAAAA,EACAP,MAAAA,EACA,IAAKa,KAAK,GAAG,CAACV,EAAQ,CAAC,CAAC,CAAC,CAAEA,EAAQ,CAAC,CAAC,CAAC,EACtC,IAAKU,KAAK,GAAG,CAACV,EAAQ,CAAC,CAAC,CAAC,CAAEA,EAAQ,CAAC,CAAC,CAAC,CACxC,CACF,EAEF,EAAG,0BACGQ,EAAuCN,EAAoB,KAC/D,IAAMJ,EAAQ,EAAE,CAChB,IAAK,IAAMC,KAAQE,EAAc,CAC/B,IAAMD,EAASG,EAAqBJ,GACpC,IAAK,IAAMG,KAAW,AAv4BVyC,EAu4BsB3C,GAAS,CACzC,IAAMH,EAAOU,EAAuBR,EAAMC,EAAQE,EAC9CL,CAAAA,GACFC,EAAM,IAAI,CAACD,EAEf,CACF,CACA,OAAOC,CACT,EAAG,wBACGW,EAAgCP,EAAoB,CAACJ,EAAGC,IAAMD,EAAE,IAAI,GAAKC,EAAE,IAAI,EAAID,EAAE,IAAI,GAAKC,EAAE,IAAI,EAAID,EAAE,IAAI,GAAKC,EAAE,IAAI,EAAI0C,EAAc3C,EAAE,GAAG,CAAEA,EAAE,GAAG,CAAEC,EAAE,GAAG,CAAEA,EAAE,GAAG,GAj5B5J,EAi5B6K,iBACtLa,EAAsCV,EAAoB,AAACJ,IAC/D,IAAMC,EAAS,EAAE,CACXC,EAAuB,IAAIyG,IACjC,IAAK,IAAMvG,KAAQJ,EAAO,CACxB,GAAIE,EAAK,GAAG,CAACE,GACX,SAEF,IAAML,EAAQ,CAACK,EAAK,CACdD,EAAY,EAAE,CAEpB,IADAD,EAAK,GAAG,CAACE,GACFL,EAAM,MAAM,CAAG,GAAG,CACvB,IAAME,EAAUF,EAAM,GAAG,GAEzB,IAAK,IAAMK,KADXD,EAAU,IAAI,CAACF,GACID,GACb,CAACE,EAAK,GAAG,CAACE,IAASO,EAAcV,EAASG,KAC5CF,EAAK,GAAG,CAACE,GACTL,EAAM,IAAI,CAACK,GAGjB,CACID,EAAU,MAAM,CAAG,GACrBF,EAAO,IAAI,CAACE,EAEhB,CACA,OAAOF,CACT,EAAG,uBACGc,EAAkCX,EAAoB,AAACJ,IAC3D,IAAMC,EAAS,EAAE,CACjB,IAAK,IAAMC,KAAQF,EACb,AAACC,EAAO,IAAI,CAAC,AAACD,GAAUY,AAh7BlB,KAg7BkBA,KAAK,GAAG,CAACZ,EAAQE,EAAK,KAAK,IACrDD,EAAO,IAAI,CAACC,EAAK,KAAK,EAG1B,KAAOD,EAAO,MAAM,CAAGD,EAAU,MAAM,EAAE,CACvC,IAAME,EAAMU,KAAK,GAAG,IAAIX,GAClBG,EAAMQ,KAAK,GAAG,IAAIX,GAClBF,EAAOC,CAAS,CAAC,EAAE,CAAC,IAAI,CAC9BC,EAAO,IAAI,CACTF,AAAS,SAATA,GAAmBA,AAAS,QAATA,EAAiBG,EAAM,AAlJvB,GAkJ2CF,CAAAA,EAAU,MAAM,CAAGC,EAAO,MAAM,AAAD,EAAKG,EAAM,AAlJrF,GAkJyGJ,CAAAA,EAAU,MAAM,CAAGC,EAAO,MAAM,AAAD,EAE/J,CACA,OAAOA,CACT,EAAG,mBACGe,EAA2CZ,EAAoB,AAACJ,IACpE,IAAMC,EAAUD,EAAU,GAAG,CAAC,AAACA,GAASA,EAAK,KAAK,EAC5CE,EAASa,EAAgBf,GACzBD,EAAc,EAAE,CACtB,GAAIC,EAAU,MAAM,EAzJW,EAyJmB,CAChD,IAAMG,EAAO,AAAIuB,MAAMxB,EAAO,MAAM,EAAE,IAAI,CAAC,IACrCG,EAAO,EAAE,CACTC,EAAwBF,EAAoB,KAChD,GAAIC,EAAK,MAAM,GAAKL,EAAU,MAAM,CAAE,CAChCK,EAAK,IAAI,CAAC,CAACL,EAAOE,IAAUU,KAAK,GAAG,CAACZ,EAAQC,CAAO,CAACC,EAAM,GAv8BzD,OAw8BJH,EAAY,IAAI,CAAC,IAAIM,EAAK,EAE5B,MACF,CACA,IAAK,GAAM,CAACL,EAAGC,EAAM,GAAIC,EAAO,OAAO,GACjCC,CAAI,CAACH,EAAE,GAGXG,CAAI,CAACH,EAAE,CAAG,GACVK,EAAK,IAAI,CAACJ,GACVK,IACAD,EAAK,GAAG,GACRF,CAAI,CAACH,EAAE,CAAG,GAEd,EAAG,SAEH,OADAM,IACOP,CACT,CACA,IAAK,IAAIC,EAAI,EAAGA,EAAIC,EAAQ,MAAM,CAAED,IAClC,IAAK,IAAIE,EAAIF,EAAI,EAAGE,EAAID,EAAQ,MAAM,CAAEC,IAAK,CAC3C,IAAME,EAAa,IAAIH,EAAQ,AAC/B,EAACG,CAAU,CAACJ,EAAE,CAAEI,CAAU,CAACF,EAAE,CAAC,CAAG,CAACE,CAAU,CAACF,EAAE,CAAEE,CAAU,CAACJ,EAAE,CAAC,CAC/DD,EAAY,IAAI,CAACK,EACnB,CAEF,OAAOL,CACT,EAAG,4BACGmB,EAA4Cd,EAAoB,CAACJ,EAAWC,KAChF,IAAMC,EAA8B,IAAIe,IACxC,IAAK,GAAM,CAACb,EAAGL,EAAK,GAAIC,EAAU,OAAO,GAAI,CAC3C,IAAMA,EAAQC,CAAU,CAACG,EAAE,CACrBD,EAASD,EAAY,GAAG,CAACH,EAAK,IAAI,GAAKA,EAAK,MAAM,CAAC,GAAG,CAAC,AAACC,GAAW,EAAE,EAAGA,EAAM,CAAC,CAAE,EAAGA,EAAM,CAAC,AAAC,GAC9FD,AAAc,cAAdA,EAAK,IAAI,EACXI,CAAM,CAACJ,EAAK,YAAY,CAAC,CAAC,CAAC,CAAGC,EAC9BG,CAAM,CAACJ,EAAK,YAAY,CAAG,EAAE,CAAC,CAAC,CAAGC,IAElCG,CAAM,CAACJ,EAAK,YAAY,CAAC,CAAC,CAAC,CAAGC,EAC9BG,CAAM,CAACJ,EAAK,YAAY,CAAG,EAAE,CAAC,CAAC,CAAGC,GAEpCE,EAAY,GAAG,CAACH,EAAK,IAAI,CAAEI,EAC7B,CACA,IAAMC,EAA+B,IAAIa,IACzC,IAAK,GAAM,CAACjB,EAAMC,EAAO,GAAIC,EAAa,CACxC,IAAMA,EAAaqE,GAAiBxB,EAAwB9C,IAC5D,GAAI,AAl/BQ4C,EAk/BI3C,GAAY,MAAM,GAAKA,EAAW,MAAM,CAAG,EACzD,OAEFE,EAAa,GAAG,CAACJ,EAAME,EACzB,CACA,OAAOE,CACT,EAAG,6BACGe,EAAkCf,EAAoB,AAACJ,IAC3D,IAAK,GAAM,CAACC,EAAMG,EAAO,GAAIJ,EAAc,CACzC,IAAMA,EAAc,CAClBC,EAAK,KAAK,CACVA,EAAK,GAAG,CACT,CAAC,MAAM,CAAC,AAACD,GAAO,EAAQA,GACzB,IAAK,IAAMC,KAAW,AA//BV4C,EA+/BsBzC,GAChC,GAAI0D,GAAmB7D,EAAQ,CAAC,CAAEA,EAAQ,CAAC,CAAEC,EAAeF,EAAa,KAGrE8D,GAAmB7D,EAAQ,CAAC,CAAEA,EAAQ,CAAC,CAAEF,EAAY,EAAE,CAAE,IAF3D,MAAO,EAMb,CACA,IAAK,IAAIE,EAAI,EAAGA,EAAIE,EAAa,MAAM,CAAEF,IAAK,CAC5C,IAAMC,EAAQC,CAAY,CAACF,EAAE,CACvBG,EAAeJ,EAAa,GAAG,CAACE,GAChCH,EAAgB,AA3gCV8C,EA2gCsBxC,EAAqBH,EAAOF,IAC9D,IAAK,IAAIE,EAAID,EAAI,EAAGC,EAAIC,EAAa,MAAM,CAAED,IAAK,CAChD,IAAMD,EAASE,CAAY,CAACD,EAAE,CAC9B,GAAI,CAACE,GAAgB,CAACJ,EAAa,GAAG,CAACC,GACrC,SAEF,IAAMK,EAAiB,AAjhCbuC,EAihCyBxC,EAAqBJ,EAAQD,IAChE,IAAK,IAAMA,KAAgBD,EACzB,IAAK,IAAME,KAAiBK,EAC1B,GAAIsC,EAA6B5C,EAAcC,EAAe,KArhCzD,EAshCH,MAAO,EAIf,CACF,CACA,MAAO,EACT,EAAG,mBACH,IAAK,IAAID,EAAY,EAAGA,EAvPD,EAuP6BA,IAAa,CAC/D,IAIIA,EAJEC,EAAmBK,IACzB,GAAIL,AAAqB,IAArBA,EACF,OAGF,IAAIC,EAAgBD,EAChBG,EAAYG,IACZR,EAAmB,IACvB,IAAK,IAAMI,KAAaW,EAAoBJ,KAC1C,IAAK,IAAML,KAAcW,EAAyBb,GAAY,CAC5D,IAAMK,EAAeU,EAA0Bf,EAAWE,GAC1D,GAAI,CAACG,GAAgB,CAACW,EAAgBX,GACpC,SAEF,IAAMC,EAAqBH,EAAoBE,GAC/C,GAAIC,GAAsBR,EACxB,SAEF,IAAMS,EAAiBH,EAAWC,GAC5BG,EAAwBR,EAAU,MAAM,CAC5C,CAACH,EAAKC,EAAMC,IAAUF,EAAMY,KAAK,GAAG,CAACP,CAAU,CAACH,EAAM,CAAGD,EAAK,KAAK,EACnE,GAEEQ,EAAqBP,GAAiBO,IAAuBP,GAAkBQ,CAAAA,EAAiBN,GAAaM,IAAmBN,GAAaO,GAAyBZ,CAAe,IAGzLC,EAAmBQ,EACnBN,EAAgBO,EAChBL,EAAYM,EACZX,EAAmBY,EACrB,CAEF,GAAI,CAACX,EACH,OAEF,IAAK,GAAM,CAACC,EAAMC,EAAO,GAAIF,EAC3BC,EAAK,MAAM,CAAGC,CAElB,CACF,CAEA,SAASkH,GAAgCpH,CAAK,CAAEC,CAAW,EAGzD,GAAM,CAAEC,cAAAA,CAAa,CAAE,eAAgBH,CAAU,CAAE,CAAG4D,EACpD1D,EAAY,MAAM,IAEdE,EAAeH,EAAM,MAAM,CAAC,AAACA,GAAS,CAACA,EAAK,YAAY,EACxDK,EAA4BD,EAAoB,CAACJ,EAAMC,EAAiBC,IAAgB6C,EAC5F/C,IAASC,EAAkBC,GAAe,EAAE,CAAGF,EAAK,MAAM,EAAI,EAAE,EAC/D,aACGM,EAA6BF,EAAoB,AAACJ,GAAW,AAjlCnD6C,EAilC+D7C,GAAQ,MAAM,CAAC,CAACA,EAAKC,IAG3FD,EAAMY,KAAK,KAAK,CAFZX,EAAQ,CAAC,CAAC,CAAC,CAAGA,EAAQ,CAAC,CAAC,CAAC,CACzBA,EAAQ,CAAC,CAAC,CAAC,CAAGA,EAAQ,CAAC,CAAC,CAAC,EAEnC,GAAI,cACDM,EAAsCH,EAAoB,CAACJ,EAAiBC,KAChF,IAAIC,EAAQ,EACZ,IAAK,IAAIE,EAAI,EAAGA,EAAID,EAAa,MAAM,CAAEC,IAAK,CAC5C,IAAML,EAAgB,AAzlCV8C,EAylCsBxC,EAAUF,CAAY,CAACC,EAAE,CAAEJ,EAAiBC,IAC9E,IAAK,IAAIK,EAAIF,EAAI,EAAGE,EAAIH,EAAa,MAAM,CAAEG,IAAK,CAChD,IAAMF,EAAiB,AA3lCbyC,EA4lCRxC,EAAUF,CAAY,CAACG,EAAE,CAAEN,EAAiBC,IAE9C,IAAK,IAAMD,KAAgBD,EACzB,IAAK,IAAME,KAAiBG,EACtB8D,GACFlE,EAAa,CAAC,CACdA,EAAa,CAAC,CACdC,EAAc,CAAC,CACfA,EAAc,CAAC,CAtmCb,OAymCFC,GAIR,CACF,CACA,OAAOA,CACT,EAAG,uBACGM,EAA6CJ,EAAoB,CAACJ,EAASC,KAC/E,GAAID,EAAQ,UAAU,CAAE,CACtB,IAAME,EAAIF,EAAQ,CAAC,CAAC,CAAC,CAErB,MAAO,AADUY,CAAAA,AAAyB,EAAzBA,KAAK,GAAG,CAACV,EAAID,EAAK,GAAG,GAASW,AAA4B,EAA5BA,KAAK,GAAG,CAACV,EAAID,EAAK,MAAM,CAAI,GACxD0C,EAAc3C,EAAQ,CAAC,CAAC,CAAC,CAAEA,EAAQ,CAAC,CAAC,CAAC,CAAEC,EAAK,IAAI,CAAEA,EAAK,KAAK,GApnCrE,CAqnCb,CACA,GAAID,EAAQ,QAAQ,CAAE,CACpB,IAAME,EAAIF,EAAQ,CAAC,CAAC,CAAC,CAErB,MAAO,AADUY,CAAAA,AAA0B,EAA1BA,KAAK,GAAG,CAACV,EAAID,EAAK,IAAI,GAASW,AAA2B,EAA3BA,KAAK,GAAG,CAACV,EAAID,EAAK,KAAK,CAAI,GACxD0C,EAAc3C,EAAQ,CAAC,CAAC,CAAC,CAAEA,EAAQ,CAAC,CAAC,CAAC,CAAEC,EAAK,GAAG,CAAEA,EAAK,MAAM,GAznCrE,CA0nCb,CACA,MAAO,EACT,EAAG,8BACGQ,EAAmCL,EAAoB,AAACJ,IAC5D,IAAME,EAAc,CAACF,EAAK,KAAK,CAAEA,EAAK,GAAG,CAAC,CAAC,MAAM,CAC/C,AAACA,GAAO,EAAQA,GAEZI,EAAQ,EAAE,CAChB,IAAK,IAAMJ,KAAME,EAAa,CAC5B,IAAMA,EAAOD,EAAY,GAAG,CAACD,GACvBD,EAAOG,EAAOoD,EAAiBpD,GAAQ,KAAK,CAC9CH,CAAAA,GACFK,EAAM,IAAI,CAACL,EAEf,CACA,OAAOK,CACT,EAAG,oBACGM,EAAuCN,EAAoB,CAACJ,EAAQC,KACxE,GAAIA,EAAQ,GAAKD,EAAO,MAAM,CAC5B,MAAO,EAAE,CAEX,IAAME,EAAKF,CAAM,CAACC,EAAM,CAClBG,EAAKJ,CAAM,CAACC,EAAQ,EAAE,CACtBF,EAAKC,CAAM,CAACC,EAAQ,EAAE,CACtBE,EAAKH,CAAM,CAACC,EAAQ,EAAE,CACtBI,EAAQoC,EAAoBvC,EAAIE,EAppC1B,OAopC4CsC,EAAkBtC,EAAIL,EAppClE,OAopCoF0C,EAAoB1C,EAAII,EAppC5G,MAqpCNG,EAAQoC,EAAkBxC,EAAIE,EArpCxB,OAqpC0CqC,EAAoBrC,EAAIL,EArpClE,OAqpCoF2C,EAAkB3C,EAAII,EArpC1G,MAspCZ,GAAI,CAACE,GAAS,CAACC,GAIX,CADwBD,CAAAA,EAAQO,KAAK,IAAI,CAACR,EAAG,CAAC,CAAGF,EAAG,CAAC,IAAMU,KAAK,IAAI,CAACT,EAAG,CAAC,CAAGJ,EAAG,CAAC,EAAIa,KAAK,IAAI,CAACR,EAAG,CAAC,CAAGF,EAAG,CAAC,IAAMU,KAAK,IAAI,CAACT,EAAG,CAAC,CAAGJ,EAAG,CAAC,GAFtI,MAAO,EAAE,CAMX,IAAMQ,EAAUgC,EAAMrC,EAAIC,EA7pCd,OA6pCgCqC,EAAMtC,EAAIC,EA7pC1C,MA6pC2D,EAAE,CAAG,CAC1E,CAAE,EAAGD,EAAG,CAAC,CAAE,EAAGC,EAAG,CAAC,AAAC,EACnB,CAAE,EAAGA,EAAG,CAAC,CAAE,EAAGD,EAAG,CAAC,AAAC,EACpB,CACKM,EAAgBD,AAAmB,IAAnBA,EAAQ,MAAM,CAAS,CAAC,IAAIP,EAAO,KAAK,CAAC,EAAGC,EAAQ,MAAOD,EAAO,KAAK,CAACC,EAAQ,GAAG,CAAC,CAAGM,EAAQ,GAAG,CAAC,AAACL,GAAW,IAChIF,EAAO,KAAK,CAAC,EAAGC,EAAQ,GAC3BC,KACGF,EAAO,KAAK,CAACC,EAAQ,GACzB,EACKQ,EAAuB,IAAIkG,IACjC,OAAOnG,EAAc,GAAG,CAAC,AAACR,GAAcuE,GAAiBxB,EAAwB/C,KAAa,MAAM,CAAC,AAACA,IACpG,GAAI,AAtqCQ6C,EAsqCI7C,GAAW,MAAM,GAAKA,EAAU,MAAM,CAAG,GAGrD,CAACA,EAAU,IAAI,CAAC,AAACA,GAAUsC,EAAUtC,EAAOG,EA3qCtC,OAyqCR,MAAO,GAKT,IAAMF,EAAMD,EAAU,GAAG,CAAC,AAACA,GAAU,CAAC,EAAEA,EAAM,CAAC,CAAC,OAAO,CAAC,GAAG,CAAC,EAAEA,EAAM,CAAC,CAAC,OAAO,CAAC,GAAG,CAAC,EAAE,IAAI,CAAC,WACzF,CAAIS,EAAK,GAAG,CAACR,KAGbQ,EAAK,GAAG,CAACR,GACF,GACT,EACF,EAAG,wBACGU,EAAkCP,EAAoB,CAACJ,EAAMC,EAAWG,KAC5E,IAAME,EAAc,CAACN,EAAK,KAAK,CAAEA,EAAK,GAAG,CAAC,CAAC,MAAM,CAC/C,AAACA,GAAO,EAAQA,GAEZU,EAAgBD,EAAiBT,GACvC,IAAK,IAAMA,KAAW,AAzrCR6C,EAyrCoB5C,GAChC,GAAI6D,GAAmB9D,EAAQ,CAAC,CAAEA,EAAQ,CAAC,CAAEE,EAAeI,EAAa,KAGrEwD,GAAmB9D,EAAQ,CAAC,CAAEA,EAAQ,CAAC,CAAED,EAAY,EAAE,CAAE,KAGzDW,EAAc,IAAI,CAAC,AAACT,GAASO,EAA2BR,EAASC,IALnE,MAAO,GASX,IAAK,IAAMC,KAASC,EAClB,GAAID,IAAUF,EAGd,KAAK,IAAMA,KAAoB,AAxsCnB6C,EAwsC+B5C,GACzC,IAAK,IAAMA,KAAgB,AAzsCjB4C,EAysC6BxC,EAAUH,IAC/C,GAAI0C,EAA6B5C,EAAkBC,EAAc,KA3sC1D,EA4sCL,MAAO,EAGb,CAEF,OAAOM,EAAoBP,EAAMC,IAAcG,CACjD,EAAG,mBACH,IAAK,IAAIJ,EAAY,EAAGA,EAzID,EAyI6BA,IAAa,CAC/D,IACIA,EACAC,EAFEC,EAAmBK,IAGrBH,EAAgBF,EAChBH,EAAY,IACZS,EAAa,IACjB,IAAK,IAAMC,KAAQN,EAAc,CAC/B,IAAMA,EAAgBE,EAAUI,GAC1BK,EAAegC,EAAqB3C,EA7tChC,MA8tCJY,EAAgBT,EAAWH,GACjC,IAAK,IAAIE,EAAQ,EAAGA,GAASF,EAAc,MAAM,CAAG,EAAGE,IACrD,IAAK,IAAMW,KAAaN,EAAqBP,EAAeE,GAAQ,CAClE,IAAMF,EAAiB2C,EAAqB9B,EAjuCtC,MAkuCAX,EAAkBC,EAAWU,GAEnC,GAAI,CADkBb,CAAAA,EAAiBW,GAAgBX,IAAmBW,GAAgBT,EAAkBU,EAnuCtG,IAmuC8H,GAC9G,CAACJ,EAAgBF,EAAMO,EAAWd,GACtD,SAEF,IAAMQ,EAAqBH,EAAoBE,EAAMO,GACjDN,EAAqBN,GAAiBM,IAAuBN,GAAkBD,CAAAA,EAAiBJ,GAAaI,IAAmBJ,GAAaM,GAAmBG,CAAS,IAG7KR,EAAWS,EACXR,EAAWe,EACXZ,EAAgBM,EAChBX,EAAYI,EACZK,EAAaH,EACf,CAEJ,CACA,GAAI,CAACL,GAAY,CAACC,EAChB,MAEFD,CAAAA,EAAS,MAAM,CAAGC,CACpB,CACF,CAEA,SAASoH,GAAmCrH,CAAK,CAAEC,CAAW,EAK5D,IAAMC,EAAY,EAAE,CACpB,IAAK,IAAMF,KAAQC,EAAY,MAAM,GAAI,CACvC,GAAID,EAAK,OAAO,EAAIA,EAAK,WAAW,CAClC,SAEF,IAAMC,EAAKD,EAAK,CAAC,EAAI,EACfI,EAAKJ,EAAK,CAAC,EAAI,EACfD,EAAOuD,EAAiBtD,GACzBD,GAGLG,EAAU,IAAI,CAAC,CACb,GAAImC,OAAOrC,EAAK,EAAE,EAAI,IACtBC,GAAAA,EACAG,GAAAA,EACAL,KAAAA,CACF,EACF,CACA,GAAIG,AAAqB,IAArBA,EAAU,MAAM,CAClB,OAEF,IAAMH,EAAe,IAAIkB,IAAIf,EAAU,GAAG,CAAC,AAACF,GAAS,CAACA,EAAK,EAAE,CAAEA,EAAK,GAC9DG,EAAgBD,EAAU,GAAG,CAAC,AAACF,GAAU,EAAE,GAAIA,EAAK,EAAE,CAAE,KAAMA,EAAK,IAAI,AAAC,IACxEK,EAAQ,CAAC,MAAO,SAAU,OAAQ,QAAQ,CAC1CC,EAAgB,CACpB,IAAKM,KAAK,GAAG,IAAIV,EAAU,GAAG,CAAC,AAACF,GAASA,EAAK,IAAI,CAAC,GAAG,GA7BzC,GA8Bb,OAAQY,KAAK,GAAG,IAAIV,EAAU,GAAG,CAAC,AAACF,GAASA,EAAK,IAAI,CAAC,MAAM,GA9B/C,GA+Bb,KAAMY,KAAK,GAAG,IAAIV,EAAU,GAAG,CAAC,AAACF,GAASA,EAAK,IAAI,CAAC,IAAI,GA/B3C,GAgCb,MAAOY,KAAK,GAAG,IAAIV,EAAU,GAAG,CAAC,AAACF,GAASA,EAAK,IAAI,CAAC,KAAK,GAhC7C,EAiCf,EACMO,EAAeP,EAAM,MAAM,CAAC,AAACA,GAAS,CAACA,EAAK,YAAY,EACxDQ,EAAY,IAAIS,IAAIV,EAAa,GAAG,CAAC,CAACP,EAAMC,IAAU,CAACD,EAAMC,EAAM,GACnEQ,EAAuCL,EAAoB,AAACJ,IAChE,IAAMC,EAAUD,AAAS,SAATA,GAAmBA,AAAS,QAATA,EAAiB,GAAK,EACnDE,EAAS,EAAE,CACjB,IAAK,IAAIE,EAAU,EAAGA,GAtCI,EAsC4BA,IACpDF,EAAO,IAAI,CAACI,CAAa,CAACN,EAAK,CAAGC,AAxCvB,GAwCuBA,EAAmBG,GAEvD,OAAOF,CACT,EAAG,wBACGQ,EAAuCN,EAAoB,CAACJ,EAAMC,EAA+B,IAAIgB,GAAK,GAAK8B,EACnH9C,EAAa,GAAG,CAACD,IAASA,EAAK,MAAM,EAAI,EAAE,EAC1C,wBACGW,EAA+CP,EAAoB,CAACJ,EAAeC,KACvF,IAAIC,EAAQ,EACZ,IAAK,IAAME,KAAgBJ,EACzB,IAAK,IAAMA,KAAiBC,EACtBiE,GACF9D,EAAa,CAAC,CACdA,EAAa,CAAC,CACdJ,EAAc,CAAC,CACfA,EAAc,CAAC,CAlzCT,OAqzCNE,IAIN,OAAOA,CACT,EAAG,gCACGY,EAA4CV,EAAoB,CAACJ,EAAOC,IAAWU,EAA6B,AAzzCtGkC,EAyzCkH7C,GAAQ,AAzzC1H6C,EAyzCsI5C,IAAU,6BAC1Jc,EAAmCX,EAAoB,CAACJ,EAA+B,IAAIiB,GAAK,IACpG,IAAIhB,EAAQ,EACNC,EAAQ,EAAE,CACVH,EAA0B,IAAI4G,IAC9BxG,EAAY,EAAE,CACdE,EAA0BD,EAAoB,AAACJ,IAC9CD,EAAQ,GAAG,CAACC,KACfD,EAAQ,GAAG,CAACC,GACZG,EAAU,IAAI,CAACH,GAEnB,EAAG,WACH,IAAK,IAAII,EAAI,EAAGA,EAAIG,EAAa,MAAM,CAAEH,IAAK,CAC5C,IAAML,EAAQQ,CAAY,CAACH,EAAE,CACvBD,EAAcO,EAAqBX,EAAOC,GAChD,IAAK,IAAIM,EAAIF,EAAI,EAAGE,EAAIC,EAAa,MAAM,CAAED,IAAK,CAChD,IAAMF,EAASG,CAAY,CAACD,EAAE,CACxBE,EAAYM,EAChBX,EACAO,EAAqBN,EAAQJ,IAE3BQ,EAAY,IACdP,GAASO,EACTN,EAAM,IAAI,CAAC,CAAEH,MAAAA,EAAOK,OAAAA,EAAQ,MAAOI,CAAU,GAC7CH,EAAQN,GACRM,EAAQD,GAEZ,CACF,CAEA,OADAD,EAAU,IAAI,CAAC,CAACH,EAAGC,IAAM,AAACO,CAAAA,EAAU,GAAG,CAACR,IAAM,GAAMQ,CAAAA,EAAU,GAAG,CAACP,IAAM,IACjE,CACLA,MAAAA,EACAC,MAAAA,EACAH,QAAAA,EACA,MAAOI,CACT,CACF,EAAG,oBACGa,EAAgDZ,EAAoB,CAACJ,EAASC,KAClF,IAAMC,EAAU,IAAIyG,IAAI1G,EAAa,IAAI,IACzC,GAAIC,AAAiB,IAAjBA,EAAQ,IAAI,CACd,OAAOF,EAAQ,KAAK,CAEtB,IAAII,EAAkB,EACtB,IAAK,IAAMH,KAAQD,EAAQ,KAAK,CAC1BE,CAAAA,EAAQ,GAAG,CAACD,EAAK,KAAK,GAAKC,EAAQ,GAAG,CAACD,EAAK,MAAM,IACpDG,CAAAA,GAAmBH,EAAK,KAAK,AAAD,EAGhC,IAAIF,EAAsB,EAC1B,IAAK,IAAIC,EAAI,EAAGA,EAAIO,EAAa,MAAM,CAAEP,IAAK,CAC5C,IAAMI,EAAQG,CAAY,CAACP,EAAE,CACvBG,EAAeD,EAAQ,GAAG,CAACE,GAC3BC,EAAcK,EAAqBN,EAAOH,GAChD,IAAK,IAAIG,EAAIJ,EAAI,EAAGI,EAAIG,EAAa,MAAM,CAAEH,IAAK,CAChD,IAAMJ,EAASO,CAAY,CAACH,EAAE,CAC1B,CAACD,GAAiBD,EAAQ,GAAG,CAACF,EAAM,GAGxCD,CAAAA,GAAuBe,EACrBT,EACAK,EAAqBV,EAAQC,GAC/B,CACF,CACF,CACA,OAAOD,EAAQ,KAAK,CAAGI,EAAkBL,CAC3C,EAAG,iCACGmB,EAAqCd,EAAoB,AAACJ,IAC9D,IAAMC,EAA4B,IAAIgB,IACtC,IAAK,IAAMf,KAAQF,EAAS,KAAK,CAAE,CACjC,IAAMA,EAAiBC,EAAU,GAAG,CAACC,EAAK,KAAK,GAAqB,IAAIyG,IACxE3G,EAAe,GAAG,CAACE,EAAK,MAAM,EAC9BD,EAAU,GAAG,CAACC,EAAK,KAAK,CAAEF,GAC1B,IAAMI,EAAkBH,EAAU,GAAG,CAACC,EAAK,MAAM,GAAqB,IAAIyG,IAC1EvG,EAAgB,GAAG,CAACF,EAAK,KAAK,EAC9BD,EAAU,GAAG,CAACC,EAAK,MAAM,CAAEE,EAC7B,CACA,IAAMF,EAAa,EAAE,CACfE,EAAuB,IAAIuG,IACjC,IAAK,IAAM5G,KAAQC,EAAS,KAAK,CAAE,CACjC,GAAII,EAAK,GAAG,CAACL,GACX,SAEF,IAAMC,EAAQ,CAACD,EAAK,CACdI,EAAY,EAAE,CAEpB,IADAC,EAAK,GAAG,CAACL,GACFC,EAAM,MAAM,CAAG,GAAG,CACvB,IAAME,EAAUF,EAAM,GAAG,GAEzB,IAAK,IAAMD,KADXI,EAAU,IAAI,CAACD,GACID,EAAU,GAAG,CAACC,IAAY,EAAE,EACxCE,EAAK,GAAG,CAACL,KACZK,EAAK,GAAG,CAACL,GACTC,EAAM,IAAI,CAACD,GAGjB,CACAI,EAAU,IAAI,CAAC,CAACH,EAAGC,IAAM,AAACO,CAAAA,EAAU,GAAG,CAACR,IAAM,GAAMQ,CAAAA,EAAU,GAAG,CAACP,IAAM,IACpEE,EAAU,MAAM,CAAG,GACrBD,EAAW,IAAI,CAACC,EAEpB,CACA,OAAOD,CACT,EAAG,sBACGiB,EAAiCf,EAAoB,AAACJ,GAAS,CAACA,EAAK,KAAK,CAAEA,EAAK,GAAG,CAAC,CAAC,MAAM,CAChG,AAACA,GAAO,EAAQA,GACf,kBACGoB,EAAmChB,EAAoB,AAACJ,IAC5D,IAAMC,EAAS,EAAE,CACjB,IAAK,IAAMC,KAAagB,EAAmBlB,GAAW,CACpD,IAAMA,EAAe,IAAI2G,IAAIzG,GACvBE,EAAuB,IAAIuG,IAAIzG,EAAU,OAAO,CAAC,AAACF,GAASmB,EAAenB,KAC1ED,EAAQ,IAAIG,EAAU,CAC5B,IAAK,IAAMD,KAAQM,GACbP,EAAa,GAAG,CAACC,IAGjBkB,EAAelB,GAAM,IAAI,CAAC,AAACD,GAAOI,EAAqB,GAAG,CAACJ,KAC7DD,EAAM,IAAI,CAACE,GAGfF,EAAM,IAAI,CAAC,CAACC,EAAGC,IAAM,AAACO,CAAAA,EAAU,GAAG,CAACR,IAAM,GAAMQ,CAAAA,EAAU,GAAG,CAACP,IAAM,IACpEA,EAAO,IAAI,CAACF,EACd,CACA,OAAOE,CACT,EAAG,oBACGoB,EAAqDjB,EAAoB,CAACJ,EAASC,EAAMC,IAAgBc,EAC7GhB,EACgB,IAAIiB,IAAI,CAAC,CAAChB,EAAMC,EAAY,CAAC,GAC5C,sCACGoB,EAAyClB,EAAoB,AAACJ,IAClE,IAAMC,EAAyB,IAAIgB,IACnC,IAAK,IAAMf,KAAQF,EAAQ,KAAK,CAC9BC,EAAO,GAAG,CAACC,EAAK,KAAK,CAAE,AAACD,CAAAA,EAAO,GAAG,CAACC,EAAK,KAAK,GAAK,GAAKA,EAAK,KAAK,EACjED,EAAO,GAAG,CAACC,EAAK,MAAM,CAAE,AAACD,CAAAA,EAAO,GAAG,CAACC,EAAK,MAAM,GAAK,GAAKA,EAAK,KAAK,EAErE,OAAOD,CACT,EAAG,0BACG2B,EAA6BxB,EAAoB,AAACJ,GAAWA,EAAO,KAAK,CAAC,GAAG,MAAM,CAAC,CAACC,EAAKC,EAAOE,KACrG,IAAML,EAAWC,CAAM,CAACI,EAAM,CAC9B,OAAOH,EAAMW,KAAK,GAAG,CAACV,EAAM,CAAC,CAAGH,EAAS,CAAC,EAAIa,KAAK,GAAG,CAACV,EAAM,CAAC,CAAGH,EAAS,CAAC,CAC7E,EAAG,GAAI,cACD8B,EAA6BzB,EAAoB,CAACJ,EAA+B,IAAIiB,GAAK,GAAKV,EAAa,MAAM,CACtH,CAACN,EAAKC,IAASD,EAAM6C,EAAqBpC,EAAqBR,EAAMF,IACrE,GACC,cACG8B,EAA8B1B,EAAoB,CAACJ,EAA+B,IAAIiB,GAAK,GAAKV,EAAa,MAAM,CACvH,CAACN,EAAKC,IAASD,EAAM2B,EAAWlB,EAAqBR,EAAMF,IAC3D,GACC,eACG+B,EAAyC3B,EAAoB,CAACJ,EAAMC,EAAMC,EAA+B,IAAIe,GAAK,IACtH,IAAMb,EAAe,AA98CPyC,EA88CmB5C,GACjC,IAAK,IAAMA,KAASM,EAClB,GAAIN,IAAUD,EAGd,KAAK,IAAMA,KAAoBI,EAC7B,IAAK,IAAMA,KAAgB,AAp9CjByC,EAo9C6BnC,EAAqBT,EAAOC,IACjE,GAAI0C,EAA6B5C,EAAkBI,EAAc,KAt9C1D,EAu9CL,MAAO,EAGb,CAEF,MAAO,EACT,EAAG,0BACG4B,EAA+B5B,EAAoB,CAACJ,EAAMC,KAC9D,IAAMC,EAAc,CAACF,EAAK,KAAK,CAAEA,EAAK,GAAG,CAAC,CAAC,MAAM,CAC/C,AAACA,GAAO,EAAQA,GAElB,IAAK,IAAMA,KAAW,AAj+CR6C,EAi+CoB5C,GAChC,GAAI6D,GAAmB9D,EAAQ,CAAC,CAAEA,EAAQ,CAAC,CAAEG,EAAeD,EAAa,IACvE,MAAO,GAGX,MAAO,EACT,EAAG,gBACG+B,EAA0C7B,EAAoB,CAACJ,EAAYC,KAC/E,IAAMC,EAAYqE,GAAiBxB,EAAwB9C,GACvD,CA1+CU4C,EA0+CE3C,GAAW,MAAM,GAAKA,EAAU,MAAM,CAAG,GACvDF,EAAW,IAAI,CAACE,EAEpB,EAAG,2BACGgC,EAAmC9B,EAAoB,AAACJ,GAASA,AAAS,SAATA,GAAmBA,AAAS,UAATA,EAAkB,oBACtGoC,EAAwChC,EAAoB,CAACJ,EAAKC,EAAMC,KAC5E,OAAQD,GACN,IAAK,OACH,OAAOW,KAAK,GAAG,CAACZ,EAAI,CAAC,CAAEE,EAAI,CAAC,EAzPnB,EA0PX,KAAK,QACH,OAAOU,KAAK,GAAG,CAACZ,EAAI,CAAC,CAAEE,EAAI,CAAC,EA3PnB,EA4PX,KAAK,MACH,OAAOU,KAAK,GAAG,CAACZ,EAAI,CAAC,CAAEE,EAAI,CAAC,EA7PnB,EA8PX,KAAK,SACH,OAAOU,KAAK,GAAG,CAACZ,EAAI,CAAC,CAAEE,EAAI,CAAC,EA/PnB,EAgQb,CACF,EAAG,yBACGoC,EAAwClC,EAAoB,CAACJ,EAAYC,EAAKC,EAASE,KAC3F,IAAML,EAAUG,AAAY,SAAZA,GAAsBA,AAAY,QAAZA,EAAoB,GAAK,EAE/D,IAAK,IAAMC,IADQ,CAACiC,EAAsBnC,EAAKC,EAASE,GAAME,CAAa,CAACJ,EAAQ,CAAC,CAEnF,IAAK,IAAIG,EAAU,EAAGA,GArQE,EAqQ8BA,IACpD4B,EACEjC,EACAyD,EAAuBxD,EAAKC,EAASE,EAAKD,EAAOJ,AAzQ1C,GAyQ0CA,EAAmBM,GAI5E,EAAG,yBACGkC,EAAoDnC,EAAoB,CAACJ,EAAYC,EAAKC,EAASE,EAAKL,KAC5G,IAAK,IAAMI,KAAUM,EAAqBP,GACxC,IAAK,IAAMA,KAAUO,EAAqBV,GACxCkC,EAAwBjC,EAAY,CAClCC,EACA,CAAE,EAAGE,EAAQ,EAAGF,EAAI,CAAC,AAAC,EACtB,CAAE,EAAGE,EAAQ,EAAGD,CAAO,EACvB,CAAE,EAAGE,EAAI,CAAC,CAAE,EAAGF,CAAO,EACtBE,EACD,CAGP,EAAG,qCACGoC,EAAoDpC,EAAoB,CAACJ,EAAYC,EAAKC,EAASE,EAAKL,KAC5G,IAAK,IAAMI,KAAUM,EAAqBP,GACxC,IAAK,IAAMA,KAAUO,EAAqBV,GACxCkC,EAAwBjC,EAAY,CAClCC,EACA,CAAE,EAAGA,EAAI,CAAC,CAAE,EAAGE,CAAO,EACtB,CAAE,EAAGD,EAAQ,EAAGC,CAAO,EACvB,CAAE,EAAGD,EAAQ,EAAGE,EAAI,CAAC,AAAC,EACtBA,EACD,CAGP,EAAG,qCACGuC,EAA8CvC,EAAoB,CAACJ,EAAYC,EAAKC,EAASE,EAAKL,KACtG,IAAMI,EAAU,IAAIM,EAAqB,UAAWA,EAAqB,UAAU,CACnF,IAAK,IAAMJ,KAAYI,EAAqBP,GAC1C,IAAK,IAAMA,KAAYO,EAAqBV,GAC1C,IAAK,IAAMA,KAAUI,EACnB8B,EAAwBjC,EAAY,CAClCC,EACA,CAAE,EAAGI,EAAU,EAAGJ,EAAI,CAAC,AAAC,EACxB,CAAE,EAAGI,EAAU,EAAGN,CAAO,EACzB,CAAE,EAAGG,EAAU,EAAGH,CAAO,EACzB,CAAE,EAAGG,EAAU,EAAGE,EAAI,CAAC,AAAC,EACxBA,EACD,CAIT,EAAG,+BACG4C,EAA4C5C,EAAoB,CAACJ,EAAYC,EAAKC,EAASE,EAAKL,KACpG,IAAMI,EAAU,IAAIM,EAAqB,WAAYA,EAAqB,SAAS,CACnF,IAAK,IAAMJ,KAAYI,EAAqBP,GAC1C,IAAK,IAAMA,KAAYO,EAAqBV,GAC1C,IAAK,IAAMA,KAAUI,EACnB8B,EAAwBjC,EAAY,CAClCC,EACA,CAAE,EAAGA,EAAI,CAAC,CAAE,EAAGI,CAAS,EACxB,CAAE,EAAGN,EAAQ,EAAGM,CAAS,EACzB,CAAE,EAAGN,EAAQ,EAAGG,CAAS,EACzB,CAAE,EAAGE,EAAI,CAAC,CAAE,EAAGF,CAAS,EACxBE,EACD,CAIT,EAAG,6BACG6C,EAAuC7C,EAAoB,AAACJ,IAChE,IAAMC,EAAuB,IAAI0G,IACjC,OAAO3G,EAAW,GAAG,CAAC,AAACA,GAAc+C,EAAwB/C,IAAY,MAAM,CAAC,AAACA,IAC/E,IAAME,EAAMF,EAAU,GAAG,CAAC,AAACA,GAAU,CAAC,EAAEA,EAAM,CAAC,CAAC,OAAO,CAAC,GAAG,CAAC,EAAEA,EAAM,CAAC,CAAC,OAAO,CAAC,GAAG,CAAC,EAAE,IAAI,CAAC,WACzF,CAAIC,EAAK,GAAG,CAACC,KAAQF,CAAAA,EAAU,MAAM,CAAG,KAGxCC,EAAK,GAAG,CAACC,GACF,GACT,EACF,EAAG,wBACGgD,EAA0C9C,EAAoB,CAACJ,EAAKC,EAASC,EAAKE,KACtF,IAAML,EAAa,EAAE,CACfI,EAAOqD,EAAwBxD,EAAKC,EAASC,EAAKE,EAvV3C,GA3vCD,KAmlDRD,CAAAA,GACF8B,EAAwBlC,EAAYI,GAElCF,IAAYG,GACdkC,EAAsBvC,EAAYC,EAAKC,EAASC,GAElD,IAAMG,EAAgB6B,EAAiBjC,GACjCK,EAAgB4B,EAAiB9B,GAUvC,OATIC,GAAiB,CAACC,EACpBiC,EAAkCxC,EAAYC,EAAKC,EAASC,EAAKE,GACxD,CAACC,GAAiBC,EAC3BkC,EAAkCzC,EAAYC,EAAKC,EAASC,EAAKE,GACxDC,EACTsC,EAA4B5C,EAAYC,EAAKC,EAASC,EAAKE,GAE3D4C,EAA0BjD,EAAYC,EAAKC,EAASC,EAAKE,GAEpD6C,EAAqBlD,EAC9B,EAAG,2BACGoD,EAA2D/C,EAAoB,CAACJ,EAAYC,EAAOC,EAAWE,KAClH,IAAML,EAAkB,IAAIU,EAAqB,WAAYA,EAAqB,SAAS,CACrFN,EAAkB,IAAIM,EAAqB,UAAWA,EAAqB,UAAU,CAC3F,IAAK,IAAMH,KAAQD,EAAO,CACxB,IAAMA,EAAMkD,EAAgBnD,EAASE,GAC/BC,EAAgBD,AAAS,QAATA,GAAkBA,AAAS,WAATA,EAAoBG,EAAqBH,GAAQH,EACzF,IAAK,IAAMC,KAASL,EAQlB,IAAK,IAAMA,KAPXkC,EAAwBjC,EAAY,CAClCC,EACAC,EACA,CAAE,EAAGE,EAAO,EAAGF,EAAU,CAAC,AAAC,EAC3B,CAAE,EAAGE,EAAO,EAAGC,EAAI,CAAC,AAAC,EACrBA,EACD,EACyBE,GACxB0B,EAAwBjC,EAAY,CAClCC,EACAC,EACA,CAAE,EAAGE,EAAO,EAAGF,EAAU,CAAC,AAAC,EAC3B,CAAE,EAAGE,EAAO,EAAGL,CAAY,EAC3B,CAAE,EAAGM,EAAI,CAAC,CAAE,EAAGN,CAAY,EAC3BM,EACD,CAGP,CACF,EAAG,4CACG+C,EAA6DhD,EAAoB,CAACJ,EAAYC,EAAOC,EAAWE,KACpH,IAAML,EAAkB,IAAIU,EAAqB,WAAYA,EAAqB,SAAS,CACrFN,EAAkB,IAAIM,EAAqB,UAAWA,EAAqB,UAAU,CAC3F,IAAK,IAAMH,KAAQD,EAAO,CACxB,IAAMA,EAAMkD,EAAgBnD,EAASE,GAC/BC,EAAgBD,AAAS,SAATA,GAAmBA,AAAS,UAATA,EAAmBG,EAAqBH,GAAQP,EACzF,IAAK,IAAMK,KAASD,EAQlB,IAAK,IAAMJ,KAPXkC,EAAwBjC,EAAY,CAClCC,EACAC,EACA,CAAE,EAAGA,EAAU,CAAC,CAAE,EAAGE,CAAM,EAC3B,CAAE,EAAGC,EAAI,CAAC,CAAE,EAAGD,CAAM,EACrBC,EACD,EACyBE,GACxB0B,EAAwBjC,EAAY,CAClCC,EACAC,EACA,CAAE,EAAGA,EAAU,CAAC,CAAE,EAAGE,CAAM,EAC3B,CAAE,EAAGL,EAAa,EAAGK,CAAM,EAC3B,CAAE,EAAGL,EAAa,EAAGM,EAAI,CAAC,AAAC,EAC3BA,EACD,CAGP,CACF,EAAG,8CACGgD,EAAyDjD,EAAoB,AAACJ,IAClF,IAAMC,EAAQD,EAAK,KAAK,CAClBE,EAAQF,EAAK,GAAG,CAChBI,EAAUF,EAAQH,EAAa,GAAG,CAACG,GAAS,KAAK,EACvD,GAAI,CAACD,GAAS,CAACG,EACb,MAAO,EAAE,CAEX,IAAMD,EAAS4C,EAAwB/C,EAAK,MAAM,EAAI,EAAE,EACxD,GAAIG,EAAO,MAAM,CAAG,EAClB,MAAO,EAAE,CAEX,IAAME,EAAQF,CAAM,CAAC,EAAE,CACjBG,EAAYH,CAAM,CAAC,EAAE,CACrBI,EAAa,EAAE,CAMrB,OALImC,EAAkBrC,EAAOC,EA1qDjB,MA2qDV6C,EAAyC5C,EAAYF,EAAOC,EAAWF,GAC9DqC,EAAoBpC,EAAOC,EA5qD1B,OA6qDV8C,EAA2C7C,EAAYF,EAAOC,EAAWF,GAEpEG,CACT,EAAG,0CACG4B,EAAoC/B,EAAoB,AAACJ,IAC7D,IAAMC,EAAQD,EAAK,KAAK,CAClBE,EAAQF,EAAK,GAAG,CAChBI,EAAUH,EAAQF,EAAa,GAAG,CAACE,GAAS,KAAK,EACjDE,EAAUD,EAAQH,EAAa,GAAG,CAACG,GAAS,KAAK,EACvD,GAAI,CAACE,GAAW,CAACD,EACf,MAAO,EAAE,CAEX,IAAMG,EAAa,EAAE,CACrB,IAAK,IAAMN,KAAWK,EAAO,CAC3B,IAAMJ,EAAUsD,EAAgBnD,EAASJ,GACzC,IAAK,IAAME,KAAWG,EACpBC,EAAW,IAAI,IACV4C,EAAwBjD,EAASD,EAASuD,EAAgBpD,EAASD,GAAUA,GAGtF,CAEA,OADAI,EAAW,IAAI,IAAI+C,EAAuCrD,IACnDM,CACT,EAAG,qBACGoD,EAAwCtD,EAAoB,IAAM,IAAIa,IAAIV,EAAa,GAAG,CAAC,AAACP,GAAS,CAACA,EAAM,AAnsDlG6C,EAmsD8GnC,EAAqBV,IAAO,GAAI,yBACxJ2D,EAA0CvD,EAAoB,CAACJ,EAAMC,EAAmBC,KAC5F,IAAME,EAA4B,IAAIuG,IACtC,IAAK,IAAM5G,KAASQ,EAAc,CAChC,GAAIR,IAAUC,EACZ,SAEF,IAAMG,EAAgBD,EAAa,GAAG,CAACH,IAAU,AA1sDrC8C,EA0sDiDnC,EAAqBX,GAC9EE,CAAAA,EAAkB,IAAI,CACxB,AAACD,GAAqBG,EAAc,IAAI,CACtC,AAACF,GAAiB2C,EAA6B5C,EAAkBC,EAAc,KA9sDxE,KAitDTG,EAAU,GAAG,CAACL,EAElB,CACA,OAAOK,CACT,EAAG,2BACGwD,EAAoCxD,EAAoB,CAACJ,EAAMC,EAASC,EAAcE,KAC1F,IAAML,EAAuB,IAAI4G,IAgCjC,OAAO,AA/BYxE,EAAkBnC,GAAM,GAAG,CAAC,AAACA,GAAcuE,GAAiBxB,EAAwB/C,KAAa,MAAM,CAAC,AAACC,IAC1H,GAAI+B,EAAahC,EAAMC,GACrB,MAAO,GAET,IAAMC,EAAMD,EAAU,GAAG,CAAC,AAACD,GAAU,CAAC,EAAEA,EAAM,CAAC,CAAC,OAAO,CAAC,GAAG,CAAC,EAAEA,EAAM,CAAC,CAAC,OAAO,CAAC,GAAG,CAAC,EAAE,IAAI,CAAC,WACzF,CAAID,EAAK,GAAG,CAACG,KAAQD,CAAAA,EAAU,MAAM,CAAG,KAGxCF,EAAK,GAAG,CAACG,GACF,GACT,GAAG,GAAG,CAAC,AAACH,IACN,IAAMI,EAAoB,AAluDd0C,EAkuD0B9C,GAClCM,EAAsB,EAC1B,IAAK,IAAMJ,KAASM,EACdN,IAAUD,GAGdK,CAAAA,GAAuBM,EACrBR,EACAD,EAAa,GAAG,CAACD,IAAU,AA1uDnB4C,EA0uD+BnC,EAAqBT,IAC9D,EAEF,MAAO,CACLF,UAAAA,EACAI,kBAAAA,EACA,UAAWF,EAAQ,KAAK,CAAIG,CAAAA,EAAoB,GAAG,CAACJ,IAAS,GAAKK,EAClE,MAAOyC,EAAqB/C,EAnvDpB,MAovDR,WAAY+C,EAAqB/C,GACjC,OAAQ6B,EAAW7B,EACrB,CACF,GAAG,MAAM,CAAC,CAAC,CAAEC,UAAAA,CAAS,CAAE,GAAKA,GAAaC,EAAQ,KAAK,EAAE,IAAI,CAAC,CAACD,EAAGC,IAAMD,EAAE,SAAS,CAAGC,EAAE,SAAS,EAAID,EAAE,KAAK,CAAGC,EAAE,KAAK,EAAID,EAAE,MAAM,CAAGC,EAAE,MAAM,EAC3H,KAAK,CAAC,EA1fW,IA0fsB,GAAG,CAAC,AAACA,GACrD,EACL,KAAMA,EAAU,SAAS,CACzB,SAAUA,EAAU,iBAAiB,CACrC,qBAAsB0D,EACpB3D,EACAC,EAAU,iBAAiB,CAC3BC,GAEF,WAAYD,EAAU,UAAU,CAChC,OAAQA,EAAU,MAAM,AAC1B,GAEJ,EAAG,qBACG4D,EAAoCzD,EAAoB,CAACJ,EAASC,EAAWC,EAAgBE,EAAYL,EAAiBI,KAC9H,IAAIE,EAAkB,EACtB,IAAK,IAAMH,KAAQF,EAAQ,KAAK,CAC1BE,CAAAA,EAAK,KAAK,GAAKD,GAAaC,EAAK,MAAM,GAAKD,GAAaC,EAAK,KAAK,GAAKE,GAAcF,EAAK,MAAM,GAAKE,CAAS,GACjHC,CAAAA,GAAmBH,EAAK,KAAK,AAAD,EAGhC,IAAII,EAAsBK,EACxBT,EAAe,QAAQ,CACvBH,EAAgB,QAAQ,EAE1B,IAAK,IAAMC,KAASO,EAAc,CAChC,GAAIP,IAAUC,GAAaD,IAAUI,EACnC,SAEF,IAAMC,EAAgBF,EAAa,GAAG,CAACH,IAAU,AAnxDrC6C,EAmxDiDnC,EAAqBV,IAClFM,GAAuBK,EAA6BT,EAAe,QAAQ,CAAEG,GAAiBM,EAA6BZ,EAAgB,QAAQ,CAAEM,EACvJ,CACA,OAAOL,EAAQ,KAAK,CAAGK,EAAkBC,CAC3C,EAAG,qBACGyD,GAAoC3D,EAAoB,CAACJ,EAAWC,KACxE,IAAK,IAAMC,KAAYF,EAAU,oBAAoB,CACnD,GAAIE,IAAaD,EACf,MAAO,GAGX,MAAO,EACT,EAAG,qBACG+D,GAAuC5D,EAAoB,CAACJ,EAAgBC,IAAoBD,EAAe,QAAQ,CAAC,IAAI,CAChI,AAACA,GAAiBC,EAAgB,QAAQ,CAAC,IAAI,CAC7C,AAACA,GAAkB2C,EAA6B5C,EAAcC,EAAe,KAnyDlE,IAqyDZ,wBACGgE,GAA8C7D,EAAoB,CAACJ,EAAOC,EAAgBC,EAAQE,IAAoB2D,GAAkB9D,EAAgBC,EAAO,IAAI,GAAK6D,GAAkB3D,EAAiBJ,EAAM,IAAI,GAAK,CAACgE,GAAqB/D,EAAgBG,GAAkB,+BAClR+D,GAAuC/D,EAAoB,CAACJ,EAASC,EAAOC,EAAgBE,EAAQL,KACxG,IAAMI,EAAY0D,EAChB7D,EAAQ,OAAO,CACfC,EAAM,IAAI,CACVC,EACAE,EAAO,IAAI,CACXL,EACAC,EAAQ,YAAY,EAEtB,IAAIG,CAAAA,GAAaH,EAAQ,OAAO,CAAC,KAAK,AAAD,EAGrC,MAAO,CACL,aAA8B,IAAIiB,IAAI,CACpC,CAAChB,EAAM,IAAI,CAAEC,EAAe,IAAI,CAAC,CACjC,CAACE,EAAO,IAAI,CAAEL,EAAgB,IAAI,CAAC,CACpC,EACDI,UAAAA,EACA,MAAOH,EAAQ,YAAY,CAAIA,CAAAA,EAAQ,eAAe,CAAC,GAAG,CAACC,EAAM,IAAI,GAAK,GAAMD,CAAAA,EAAQ,eAAe,CAAC,GAAG,CAACI,EAAO,IAAI,GAAK,GAAKF,EAAe,UAAU,CAAGH,EAAgB,UAAU,CACvL,OAAQC,EAAQ,aAAa,CAAIA,CAAAA,EAAQ,gBAAgB,CAAC,GAAG,CAACC,EAAM,IAAI,GAAK,GAAMD,CAAAA,EAAQ,gBAAgB,CAAC,GAAG,CAACI,EAAO,IAAI,GAAK,GAAKF,EAAe,MAAM,CAAGH,EAAgB,MAAM,AACrL,CACF,EAAG,wBACGqE,GAAoChE,EAAoB,CAACJ,EAAWC,IAASD,EAAU,SAAS,CAAGC,EAAK,SAAS,EAAID,EAAU,SAAS,GAAKC,EAAK,SAAS,EAAKD,CAAAA,EAAU,KAAK,CAAGC,EAAK,KAAK,EAAID,EAAU,KAAK,GAAKC,EAAK,KAAK,EAAID,EAAU,MAAM,CAAGC,EAAK,MAAM,AAAD,EAAI,qBACnQoE,GAAyCjE,EAAoB,CAACJ,EAASC,EAAOC,EAAQE,KAC1F,IAAIL,EAAWK,EACf,IAAK,IAAMA,KAAkBH,EAAM,UAAU,CAC3C,IAAK,IAAME,KAAmBD,EAAO,UAAU,CAAE,CAC/C,GAAI,CAAC+D,GAA4BhE,EAAOG,EAAgBF,EAAQC,GAC9D,SAEF,IAAME,EAAQ8D,GAAqBnE,EAASC,EAAOG,EAAgBF,EAAQC,EACvEE,CAAAA,GAAS+D,GAAkB/D,EAAON,IACpCA,CAAAA,EAAWM,CAAI,CAEnB,CAEF,OAAON,CACT,EAAG,0BACGuE,GAAwClE,EAAoB,AAACJ,IACjE,IAAMC,EAAe4B,IACf3B,EAAgB4B,IAChB1B,EAAesD,IACf3D,EAAsBuB,EAAuBtB,GAC7CG,EAAkB,IAAIc,IAC1BV,EAAa,GAAG,CAAC,AAACP,GAAS,CAACA,EAAM8C,EAAqBpC,EAAqBV,IAAO,GAE/EK,EAAmB,IAAIY,IAC3BV,EAAa,GAAG,CAAC,AAACP,GAAS,CAACA,EAAM4B,EAAWlB,EAAqBV,IAAO,GAErEM,EAAgC,IAAIW,IACpCT,EAASY,EAAiBpB,GAChC,IAAK,IAAMC,KAASO,EAClB,IAAK,IAAMN,KAAQD,EAAO,CACxB,GAAIK,EAAc,GAAG,CAACJ,GACpB,SAEF,IAAMD,EAAa2D,EAAkB1D,EAAMF,EAASI,EAAcL,EAC9DE,CAAAA,EAAW,MAAM,CAAG,GACtBK,EAAc,GAAG,CAACJ,EAAM,CAAEA,KAAAA,EAAMD,WAAAA,CAAW,EAE/C,CAEF,IAAIQ,EAAO,CACT,aAA8B,IAAIQ,IAClC,UAAWjB,EAAQ,KAAK,CACxB,MAAOC,EACP,OAAQC,CACV,EACMS,EAAiB,CACrBX,QAAAA,EACAC,aAAAA,EACAC,cAAAA,EACAC,gBAAAA,EACAE,iBAAAA,EACAD,aAAAA,CACF,EACA,IAAK,IAAMH,KAASO,EAAQ,CAC1B,IAAMN,EAAkB,IAAIyG,IAAI1G,EAAM,MAAM,CAAC,AAACA,GAASD,EAAQ,OAAO,CAAC,GAAG,CAACC,KACrEG,EAAUH,EAAM,GAAG,CAAC,AAACD,GAASM,EAAc,GAAG,CAACN,IAAO,MAAM,CAAC,AAACA,GAAW,EAAQA,GACxF,IAAK,IAAIA,EAAI,EAAGA,EAAII,EAAQ,MAAM,CAAEJ,IAAK,CACvC,IAAMC,EAAQG,CAAO,CAACJ,EAAE,CACxB,IAAK,IAAID,EAAIC,EAAI,EAAGD,EAAIK,EAAQ,MAAM,CAAEL,IAAK,CAC3C,IAAMC,EAASI,CAAO,CAACL,EAAE,CACrB,CAACG,EAAgB,GAAG,CAACD,EAAM,IAAI,GAAMC,EAAgB,GAAG,CAACF,EAAO,IAAI,IAGxES,CAAAA,EAAO4D,GAAuB1D,EAAgBV,EAAOD,EAAQS,EAAI,CACnE,CACF,CACF,CACA,OAAOA,EAAK,YAAY,CAAC,IAAI,CAAG,EAAIA,EAAK,YAAY,CAAG,KAAK,CAC/D,EAAG,yBACH,IAAK,IAAIT,EAAY,EAAGA,EAvoBD,EAuoB6BA,IAAa,CAC/D,IAKIA,EACAC,EANEC,EAAUa,IACVX,EAAmBF,EAAQ,KAAK,CACtC,GAAIE,AAAqB,IAArBA,EACF,OAIF,IAAIL,EAAgBK,EAChBD,EAAY,IAChB,IAAK,IAAME,KAAQH,EAAQ,KAAK,CAAE,CAChC,IAAMI,EAAmBwC,EAAqBpC,EAAqBL,GA/4DzD,MAg5DV,IAAK,IAAME,KAAa4B,EAAkB9B,GAAO,CAC/C,IAAMG,EAAoBwB,EAAa3B,EAAME,GACvCE,EAA8B,CAACD,GAAqBuB,EAAuB1B,EAAME,GACjFG,EAAqBW,EAAmCnB,EAASG,EAAME,GACvEI,EAAiBmC,EAAqBvC,EAp5DpC,OAq5DJC,IAAqBC,GAGGC,CAAAA,EAAqBN,GAAoBM,IAAuBN,GAAoBO,EAAiBL,CAAe,IAI5II,EAAqBX,GAAiBW,IAAuBX,GAAiBY,GAAkBR,IAGpGH,EAAWK,EACXJ,EAAWM,EACXR,EAAgBW,EAChBP,EAAYQ,GACd,CACF,CACA,GAAIX,GAAYC,EAAU,CACxBD,EAAS,MAAM,CAAGC,EAClB,QACF,CACA,IAAMI,EAAoBiE,GAAsBpE,GAChD,GAAI,CAACG,EACH,OAEF,IAAK,GAAM,CAACL,EAAMC,EAAO,GAAII,EAC3BL,EAAK,MAAM,CAAGC,CAElB,CACF,CAMA,SAASqH,GAAsBtH,CAAK,CAAEC,CAAK,EACzC,GAAM,CAAEC,aAAAA,CAAY,CAAEH,cAAAA,CAAa,CAAE,CAAG2D,EAAsBzD,GACxDE,EAAQ,CAAC,MAAO,SAAU,OAAQ,QAAQ,CAE1CE,EAAgB,CACpB,IAAKO,KAAK,GAAG,IAAIb,EAAc,GAAG,CAAC,AAACC,GAASA,EAAK,IAAI,CAAC,GAAG,GAF7C,GAGb,OAAQY,KAAK,GAAG,IAAIb,EAAc,GAAG,CAAC,AAACC,GAASA,EAAK,IAAI,CAAC,MAAM,GAHnD,GAIb,KAAMY,KAAK,GAAG,IAAIb,EAAc,GAAG,CAAC,AAACC,GAASA,EAAK,IAAI,CAAC,IAAI,GAJ/C,GAKb,MAAOY,KAAK,GAAG,IAAIb,EAAc,GAAG,CAAC,AAACC,GAASA,EAAK,IAAI,CAAC,KAAK,GALjD,EAMf,EACMM,EAAgDF,EAAoB,CAACJ,EAAKC,EAASC,EAAKE,KAC5F,IAAML,EAAQ,EAAE,CACVI,EAAOqD,EAAwBxD,EAAKC,EAASC,EAAKE,EAT3C,GALN,MAqBP,OANID,GACFJ,EAAM,IAAI,CAACI,GAETF,IAAYG,GACdL,EAAM,IAAI,CAAC0D,EAAuBzD,EAAKC,EAASC,EAAKG,CAAa,CAACJ,EAAQ,GAEtEF,CACT,EAAG,iCACGQ,EAA+BH,EAAoB,CAACJ,EAAKC,KAC7D,IAAK,IAAIC,EAAI,EAAGA,EAAIF,EAAI,MAAM,CAAG,EAAGE,IAGlC,GAAI4D,GAFM9D,CAAG,CAACE,EAAE,CACNF,CAAG,CAACE,EAAI,EAAE,CACSH,EAAeE,EAAY,GACtD,MAAO,GAGX,MAAO,EACT,EAAG,gBACGO,EAAoCJ,EAAoB,CAACH,EAAMC,EAAaE,EAAuB,EAAK,IAC5G,IAAIL,EAAY,EACVI,EAAe0C,EAA4B5C,EAnC1C,MAoCDI,EAAeH,EAAY,KAAK,CAChCI,EAAaJ,EAAY,GAAG,CAClC,IAAK,IAAMD,KAASD,EAAO,CACzB,GAAIC,IAAUC,GAAeD,EAAM,YAAY,CAC7C,SAEF,IAAMD,EAAaC,EAAM,KAAK,CACxBM,EAAWN,EAAM,GAAG,CAC1B,GAAI,CAACG,GAAwBC,GAAgBC,GAAeN,CAAAA,IAAeK,GAAgBL,IAAeM,GAAcC,IAAaF,GAAgBE,IAAaD,CAAS,EACzK,SAEF,IAAME,EAAWP,EAAM,MAAM,CAC7B,GAAI,AAACO,IAAYA,CAAAA,EAAS,MAAM,CAAG,GAGnC,IAAK,IAAMR,KAAeG,EACxB,IAAK,IAAMF,KAAgB4C,EAA4BrC,EApDpD,MAoDqE,CACtE,GAAIuD,GACF/D,EAAY,CAAC,CACbA,EAAY,CAAC,CACbC,EAAa,CAAC,CACdA,EAAa,CAAC,CAzDf,WA4DE,CACDF,IACA,QACF,CACI6C,EAA6B5C,EAAaC,EAhE7C,OACO,GAgENF,GAEJ,CAEJ,CACA,OAAOA,CACT,EAAG,qBAEGU,EAAoCL,EAAoB,CAACJ,EAAIC,KACjE,IAAMC,EAAOU,KAAK,GAAG,CAACZ,EAAG,CAAC,CAAGC,EAAK,IAAI,CAAC,GAAG,EACpCG,EAAUQ,KAAK,GAAG,CAACZ,EAAG,CAAC,CAAGC,EAAK,IAAI,CAAC,MAAM,EAC1CF,EAAQa,KAAK,GAAG,CAACZ,EAAG,CAAC,CAAGC,EAAK,IAAI,CAAC,IAAI,EACtCE,EAASS,KAAK,GAAG,CAACZ,EAAG,CAAC,CAAGC,EAAK,IAAI,CAAC,KAAK,EAC1CI,EAAO,MACPC,EAAWJ,EAaf,OAZIE,EAAUE,IACZD,EAAO,SACPC,EAAWF,GAETL,EAAQO,IACVD,EAAO,OACPC,EAAWP,GAETI,EAASG,IACXD,EAAO,QACPC,EAAWH,GAENE,CACT,EAAG,qBACGK,EAA6B,IAAIO,IACjCN,EAA+BP,EAAoB,CAACJ,EAAQC,EAAMC,KACtE,IAAME,EAASM,EAAW,GAAG,CAACV,IAAW,EAAE,CAC3CI,EAAO,IAAI,CAAC,CAAEH,KAAAA,EAAMC,OAAAA,CAAO,GAC3BQ,EAAW,GAAG,CAACV,EAAQI,EACzB,EAAG,gBACH,IAAK,IAAMH,KAAKD,EAAO,CACrB,GAAIC,EAAE,YAAY,CAChB,SAEF,IAAMD,EAAMC,EAAE,MAAM,EAAI,EAAE,CAC1B,GAAID,EAAI,MAAM,CAAG,EACf,SAEF,IAAMI,EAAMH,EAAE,EAAE,EAAI,GACdF,EAAUE,EAAE,KAAK,CACjBE,EAAQF,EAAE,GAAG,CACnB,GAAIF,EAAS,CACX,IAAME,EAAOC,EAAa,GAAG,CAACH,EAC1BE,CAAAA,GACFU,EAAaZ,EAASU,EAAkBT,CAAG,CAAC,EAAE,CAAEC,GAAOG,EAE3D,CACA,GAAID,EAAO,CACT,IAAMF,EAAOC,EAAa,GAAG,CAACC,EAC1BF,CAAAA,GACFU,EAAaR,EAAOM,EAAkBT,CAAG,CAACA,EAAI,MAAM,CAAG,EAAE,CAAEC,GAAOG,EAEtE,CACF,CACA,IAAMU,EAAgCV,EAAoB,CAACJ,EAAQC,EAAMC,IAChEQ,EAAW,GAAG,CAACV,IAAS,KAAK,AAACA,GAAMA,EAAE,MAAM,GAAKE,GAAgBF,EAAE,IAAI,GAAKC,IAAS,GAC3F,iBACH,IAAK,IAAMA,KAAQD,EAAO,KAyBpBA,EAxBJ,GAAIC,EAAK,YAAY,CACnB,SAEF,IAAMG,EAAMH,EAAK,MAAM,CACvB,GAAI,CAACG,GAAOA,EAAI,MAAM,CAAG,EACvB,SAEF,IAAML,EAAe+C,EAAqB1C,EAvInC,MAwIP,GAAIL,EAhEiB,EAiEnB,SAEF,IAAMM,EAAQJ,EAAK,KAAK,CAClBc,EAAQd,EAAK,GAAG,CACtB,GAAI,CAACI,GAAS,CAACU,EACb,SAEF,IAAMC,EAAUd,EAAa,GAAG,CAACG,GAC3Ba,EAAUhB,EAAa,GAAG,CAACa,GACjC,GAAI,CAACC,GAAW,CAACE,EACf,SAEF,IAAMC,EAASlB,EAAK,EAAE,EAAI,GACpBmB,EAA2BZ,EAAkBJ,EAAKH,EAAM,IACxDoB,EAA8Bb,EAAkBJ,EAAKH,GAEvDqB,EAAwBF,EACxBQ,EAAY7B,EAChB,IAAK,IAAMG,KAAWC,EAAO,CAC3B,GAAIW,EAAcT,EAAOH,EAASiB,GAChC,SAEF,IAAMf,EAAUmD,EAAgBvC,EAASd,GACzC,IAAK,IAAMH,KAAWI,EACpB,IAAIW,EAAcC,EAAOhB,EAASoB,GAIlC,IAAK,IAAMhB,KAAQG,EAA8BF,EAASF,EAD1CqD,EAAgBrC,EAASnB,GACmCA,GAAU,CACpF,GAAIQ,EAAaJ,EAAM,CAACE,EAAOU,EAAM,EACnC,SAEF,IAAMb,EAAY4C,EAAqB3C,EAzKtC,MA0KD,GAAIiB,EAA2B,EAAG,CAChC,IAAMhB,EAAwBI,EAAkBL,EAAMF,EAAM,IAC5D,GAAIG,EAAwBkB,GAAyBlB,IAA0BkB,GAAyBpB,GAAa0B,EACnH,SAEFN,EAAwBlB,EACxBwB,EAAY1B,EACZF,EAAWG,EACX,QACF,EACIK,CAAAA,EAAkBL,EAAMF,GAAQoB,CAA0B,GAG1DnB,EAAY0B,IACdA,EAAY1B,EACZF,EAAWG,EAEf,CAEJ,CACA,GAAIH,EAAU,CACZC,EAAK,MAAM,CAAGD,EACd,IAAME,EAAaQ,EAAW,GAAG,CAACL,EAC9BH,CAAAA,GACFQ,EAAW,GAAG,CACZL,EACAH,EAAW,MAAM,CAAC,AAACF,GAAMA,EAAE,MAAM,GAAKmB,IAG1C,IAAMf,EAAaM,EAAW,GAAG,CAACK,EAC9BX,CAAAA,GACFM,EAAW,GAAG,CACZK,EACAX,EAAW,MAAM,CAAC,AAACJ,GAAMA,EAAE,MAAM,GAAKmB,IAG1CR,EAAaN,EAAOI,EAAkBT,CAAQ,CAAC,EAAE,CAAEgB,GAAUG,GAC7DR,EAAaI,EAAON,EAAkBT,CAAQ,CAACA,EAAS,MAAM,CAAG,EAAE,CAAEkB,GAAUC,EACjF,CACF,CACF,CAOA,SAASoG,GAAuBvH,CAAG,CAAEC,CAAO,EAC1C,IAAMC,EAAgBD,EAAU,EAAID,EAAI,MAAM,CAAG,EAE3CI,EAAMJ,CAAG,CAACE,EAAc,CACxBH,EAAQC,CAAG,CAACE,EAFLD,CAAAA,EAAU,EAAI,EAAC,EAEW,CACvC,GAAI,CAACG,GAAO,CAACL,EACX,OAEF,IAAMI,EAAKJ,EAAM,CAAC,CAAGK,EAAI,CAAC,CACpBC,EAAKN,EAAM,CAAC,CAAGK,EAAI,CAAC,CAE1B,IAAI,CADQQ,KAAK,GAAG,CAACT,GAAMS,KAAK,GAAG,CAACP,GAb3B,IAcI,GAGb,GAAIO,AAjBK,MAiBLA,KAAK,GAAG,CAACP,GAAa,CACxB,IAAML,EAAKI,EAAI,CAAC,CAAGQ,AAjBO,GAiBPA,KAAK,IAAI,CAACT,GAC7B,MAAO,CACL,KAAMS,KAAK,GAAG,CAACR,EAAI,CAAC,CAAEJ,GACtB,MAAOY,KAAK,GAAG,CAACR,EAAI,CAAC,CAAEJ,GACvB,IAAKI,EAAI,CAAC,CApBkB,EAqB5B,OAAQA,EAAI,CAAC,CArBe,CAsB9B,CACF,CACA,GAAIQ,AA1BK,MA0BLA,KAAK,GAAG,CAACT,GAAa,CACxB,IAAMH,EAAKI,EAAI,CAAC,CAAGQ,AA1BO,GA0BPA,KAAK,IAAI,CAACP,GAC7B,MAAO,CACL,KAAMD,EAAI,CAAC,CA3BiB,EA4B5B,MAAOA,EAAI,CAAC,CA5BgB,EA6B5B,IAAKQ,KAAK,GAAG,CAACR,EAAI,CAAC,CAAEJ,GACrB,OAAQY,KAAK,GAAG,CAACR,EAAI,CAAC,CAAEJ,EAC1B,CACF,CACA,MAAO,CACL,KAAMY,KAAK,GAAG,CAACR,EAAI,CAAC,CAAEL,EAAM,CAAC,EAC7B,MAAOa,KAAK,GAAG,CAACR,EAAI,CAAC,CAAEL,EAAM,CAAC,EAC9B,IAAKa,KAAK,GAAG,CAACR,EAAI,CAAC,CAAEL,EAAM,CAAC,EAC5B,OAAQa,KAAK,GAAG,CAACR,EAAI,CAAC,CAAEL,EAAM,CAAC,CACjC,EACF,CAEA,SAASyH,GAAcxH,CAAI,EACzB,MAAO,CACL,KAAMY,KAAK,GAAG,CAACZ,EAAK,IAAI,CAAEA,EAAK,KAAK,EACpC,MAAOY,KAAK,GAAG,CAACZ,EAAK,IAAI,CAAEA,EAAK,KAAK,EACrC,IAAKY,KAAK,GAAG,CAACZ,EAAK,GAAG,CAAEA,EAAK,MAAM,EACnC,OAAQY,KAAK,GAAG,CAACZ,EAAK,GAAG,CAAEA,EAAK,MAAM,CACxC,CACF,CAEA,SAASyH,GAAuBzH,CAAI,CAAEC,CAAG,EACvC,IAAMC,EAAa6C,EAAwB9C,GAG3C,MAAO,CAFasH,GAAuBrH,EAAY,IACrCqH,GAAuBrH,EAAY,IACtB,CAAC,IAAI,CAClC,AAACD,GAAWA,GAAUmD,EAAapD,EAAMwH,GAAcvH,IAE3D,CAEA,SAASyH,GAAuB1H,CAAK,CAAEC,CAAW,EAChD,IAAMC,EAAkB,EAAE,CAC1B,IAAK,IAAMD,KAASD,EAAO,CACzB,GAAIC,EAAM,YAAY,CACpB,SAEF,IAAMD,EAAMC,EAAM,MAAM,CACxB,GAAI,AAACD,IAAOA,CAAAA,EAAI,MAAM,CAAG,GAGzB,IAAK,IAAII,EAAI,EAAGA,EAAIJ,EAAI,MAAM,CAAG,EAAGI,IAClCF,EAAgB,IAAI,CAAC,CAAE,OAAQD,EAAM,EAAE,CAAE,GAAID,CAAG,CAACI,EAAE,CAAE,GAAIJ,CAAG,CAACI,EAAI,EAAE,AAAC,EAExE,CACA,IAAML,EAAmB,EAAE,CACrBI,EAAa,EAAE,CACrB,IAAK,IAAMH,KAAKC,EAAY,MAAM,GAAI,CACpC,IAAMA,EAAUD,EAAE,OAAO,CACnBE,EAAWF,EAAE,QAAQ,CAC3B,GAAIC,GAAW,CAACC,EAAU,CACxB,IAAMD,EAAQqD,EAAiBtD,EAC3BC,CAAAA,GACFE,EAAW,IAAI,CAAC,CACd,GAAIH,EAAE,EAAE,CACR,KAAMC,CACR,GAEF,QACF,CACA,GAAIA,GAGAD,EAAE,WAAW,CAFf,SAKF,IAAMI,EAAOkD,EAAiBtD,GACzBI,GAGLL,EAAiB,IAAI,CAAC,CACpB,OAAQC,EAAE,EAAE,CACZI,KAAAA,CACF,EACF,CAIA,IAAMC,EAA2CD,EAAoB,CAACJ,EAASC,KAC7E,IAAMC,EAAWmD,EAAYpD,EAJA,GAK7B,IAAK,GAAM,CAAEA,OAAAA,CAAM,CAAE,KAAMG,CAAE,CAAE,GAAIL,EACjC,GAAIE,IAAWD,GAGXoD,EAAalD,EAAUE,GACzB,MAAO,GAGX,MAAO,EACT,EAAG,4BACGE,EAA2CF,EAAoB,CAACJ,EAAQC,KAC5E,IAAMG,EAAWiD,EAAYpD,EAhBA,GAiB7B,IAAK,IAAMA,KAAKC,EACd,GAAID,EAAE,MAAM,GAAKD,GAGbiD,EAAyBhD,EAAE,EAAE,CAAEA,EAAE,EAAE,CAAEG,GACvC,MAAO,GAGX,MAAO,EACT,EAAG,4BACGG,EAAwCH,EAAoB,CAACJ,EAASC,EAAQC,IAASG,EAAyBL,EAASE,IAASI,EAAyBL,EAAQC,GAAO,yBAC1KM,EAAmB,EAAE,CACrBC,EAAqCL,EAAoB,AAACJ,IAC9D,IAAK,GAAM,CAAEC,GAAAA,CAAE,CAAE,KAAMC,CAAQ,CAAE,GAAIC,EACnC,GAAIgD,EAAiBjD,EAAUF,GAC7B,OAAOC,CAIb,EAAG,sBACGS,EAAsCN,EAAoB,CAACJ,EAASC,IAASO,EAAiB,IAAI,CACtG,AAACN,GAAWA,EAAO,OAAO,GAAKF,GAAWoD,EAAanD,EAAMC,EAAO,IAAI,GACvE,uBACH,IAAK,IAAMA,KAAQF,EAAO,CACxB,GAAIE,EAAK,YAAY,CACnB,SAEF,IAAMF,EAAUE,EAAK,WAAW,CAChC,GAAI,CAACF,EACH,SAEF,IAAMD,EAAYE,EAAY,GAAG,CAACD,GAClC,GAAI,CAACD,EACH,SAEF,IAAMY,EAAMT,EAAK,MAAM,CACvB,GAAI,CAACS,GAAOA,EAAI,MAAM,CAAG,EACvB,SAEF,IAAMG,EAAKf,EAAU,KAAK,EAAI,EACxBgB,EAAKhB,EAAU,MAAM,EAAI,EAC/B,GAAIe,GAAM,GAAKC,GAAM,EACnB,SAEF,IAAMC,EAAW,EAAE,CACnB,IAAK,IAAIhB,EAAI,EAAGA,EAAIW,EAAI,MAAM,CAAG,EAAGX,IAAK,CACvC,IAAMC,EAAIU,CAAG,CAACX,EAAE,CACVE,EAAIS,CAAG,CAACX,EAAI,EAAE,CACdI,EAAKQ,KAAK,GAAG,CAACX,EAAE,CAAC,CAAGC,EAAE,CAAC,EACvBH,EAAKa,KAAK,GAAG,CAACX,EAAE,CAAC,CAAGC,EAAE,CAAC,EACzBE,CAAAA,CAAAA,CAAAA,EA5KC,IA4KO,IAAKL,CAAAA,EA5KZ,IA4KoB,KAGrBK,GA/KC,MA+KaL,GA/Kb,MAkLLiB,EAAS,IAAI,CAAC,CACZ,IAAKhB,EACL,OAAQI,EAAKL,EACb,YAAaK,GArLV,KAqLuB,aAAe,WACzC,KAAM,AAACH,CAAAA,EAAE,CAAC,CAAGC,EAAE,CAAC,AAAD,EAAK,EACpB,KAAM,AAACD,CAAAA,EAAE,CAAC,CAAGC,EAAE,CAAC,AAAD,EAAK,CACtB,GACF,CACA,GAAIc,AAAoB,IAApBA,EAAS,MAAM,CACjB,SAEF,IAAME,EAAiBF,EAAS,MAAM,EAAI,EAAIA,EAAS,MAAM,CAAC,AAAChB,GAAMA,EAAE,GAAG,CAAG,GAAKA,EAAE,GAAG,CAAGgB,EAAS,MAAM,CAAG,GAAKA,EAC3GG,EAAWD,EAAe,MAAM,CAAG,EAAIA,EAAiBF,EACxDI,EAAgBN,GAAMC,EAAK,aAAe,WAC1CM,EAA+BjB,EAAoB,AAACJ,GACjD,IAAIA,EAAK,CAAC,IAAI,CAAC,CAACA,EAAGC,KACxB,IAAMC,EAAYF,EAAE,WAAW,GAAKoB,EAEpC,GAAIlB,IADcD,CAAAA,EAAE,WAAW,GAAKmB,CAAY,EAE9C,OAAOlB,EAAY,GAAK,EAE1B,IAAME,EAAQJ,EAAE,MAAM,EAAI,AAACA,CAAAA,AAAkB,eAAlBA,EAAE,WAAW,CAAoBc,EAAKC,CAAC,EAAK,SAEvE,AAAIX,IADUH,EAAE,MAAM,EAAI,AAACA,CAAAA,AAAkB,eAAlBA,EAAE,WAAW,CAAoBa,EAAKC,CAAC,EAAK,EAE9DX,EAAQ,GAAK,EAEfH,EAAE,MAAM,CAAGD,EAAE,MAAM,AAC5B,GACC,gBACGsB,EAAsBN,CAAQ,CAAC,EAAE,CACjCY,EAAqBZ,CAAQ,CAACA,EAAS,MAAM,CAAG,EAAE,CAClDa,EAAmB,CAAC,GAAK,IAAM,IAAM,IAAM,IAAM,IAAM,IAAM,GAAK,GAAI,CACtEC,EAA4B1B,EAAoB,CAACJ,EAAKC,KAC1D,IAAMC,EAAIS,CAAG,CAACX,EAAI,GAAG,CAAC,CAChBI,EAAIO,CAAG,CAACX,EAAI,GAAG,CAAG,EAAE,CAC1B,MAAO,CACL,KAAME,EAAE,CAAC,CAAG,AAACE,CAAAA,EAAE,CAAC,CAAGF,EAAE,CAAC,AAAD,EAAKD,EAC1B,KAAMC,EAAE,CAAC,CAAG,AAACE,CAAAA,EAAE,CAAC,CAAGF,EAAE,CAAC,AAAD,EAAKD,CAC5B,CACF,EAAG,aACG8B,EAAwB3B,EAAoB,CAACJ,EAAOC,EAAKC,IAAQU,KAAK,GAAG,CAACV,EAAKU,KAAK,GAAG,CAACX,EAAKD,IAAS,SACtGgC,EAA2C5B,EAAoB,CAACJ,EAAOC,IAASD,EAAM,IAAI,EAAIC,EAAK,IAAI,CA3NtG,MA2NiHD,EAAM,IAAI,EAAIC,EAAK,KAAK,CA3NzI,MA2NoJD,EAAM,IAAI,EAAIC,EAAK,GAAG,CA3N1K,MA2NqLD,EAAM,IAAI,EAAIC,EAAK,MAAM,CA3N9M,KA2NuN,4BACxNgC,EAAqC7B,EAAoB,AAACJ,IAC9D,IAAMC,EAAekC,EAAmBnC,EAAO,IAAI,CAAEA,EAAO,IAAI,CAAEc,EAAIC,GAChEb,EAAeO,EAAmBR,GACxC,GAAIC,EACF,MAAO,CAAE,OAAQA,EAAcF,OAAAA,EAAQ,KAAMC,CAAa,EAE5D,IAAMG,EAAiBD,EAAW,IAAI,CAAC,CAAC,CAAEF,KAAAA,CAAI,CAAE,GAAK+B,EAAyBhC,EAAQC,IACtF,GAAI,CAACG,EACH,OAEF,IAAML,EAAOK,EAAe,IAAI,CAAC,IAAI,CAAGU,EAAK,EA5HvB,EA6HhBT,EAAOD,EAAe,IAAI,CAAC,KAAK,CAAGU,EAAK,EA7HxB,EA8HhBR,EAAOF,EAAe,IAAI,CAAC,GAAG,CAAGW,EAAK,EA9HtB,EA+HhBR,EAAOH,EAAe,IAAI,CAAC,MAAM,CAAGW,EAAK,EA/HzB,EAgItB,GAAIhB,EAAOM,GAAQC,EAAOC,EACxB,OAEF,IAAMC,EAAgB,CACpB,KAAMuB,EAAM/B,EAAO,IAAI,CAAED,EAAMM,GAC/B,KAAM0B,EAAM/B,EAAO,IAAI,CAAEM,EAAMC,EACjC,EACMG,EAAcyB,EAAmB3B,EAAc,IAAI,CAAEA,EAAc,IAAI,CAAEM,EAAIC,GACnF,OAAOiB,EAAyBhC,EAAQU,GAAe,CAAE,OAAQN,EAAe,EAAE,CAAE,OAAQI,EAAe,KAAME,CAAY,EAAI,KAAK,CACxI,EAAG,sBACGwB,EAAuC9B,EAAoB,CAACJ,EAAKC,EAAQC,IAAaF,AAAoB,eAApBA,EAAI,WAAW,CAAoBY,KAAK,GAAG,CAACX,EAAO,IAAI,CAAGC,EAAS,CAAC,EAAIU,KAAK,GAAG,CAACX,EAAO,IAAI,CAAGC,EAAS,CAAC,EAAG,wBAClMkC,EAA+ChC,EAAoB,CAACJ,EAAKC,KAE7E,IAAMC,EAAmB,AADDF,CAAAA,AAAoB,eAApBA,EAAI,WAAW,CAAoBc,EAAK,EAAIC,EAAK,GA3I5C,GA6I7B,GAAIf,IAAQsB,EAAqB,CAC/B,IAAMlB,EAAQO,CAAG,CAACX,EAAI,GAAG,CAAC,CAC1B,GAAIkC,EAAqBlC,EAAKC,EAAQG,GA1PnC,KA0PmDF,EACpD,MAAO,EAEX,CACA,GAAIF,IAAQ4B,EAAoB,CAC9B,IAAMxB,EAAMO,CAAG,CAACX,EAAI,GAAG,CAAG,EAAE,CAC5B,GAAIkC,EAAqBlC,EAAKC,EAAQG,GAhQnC,KAgQiDF,EAClD,MAAO,EAEX,CACA,MAAO,EACT,EAAG,gCACGoC,EAA0BlC,EAAoB,AAACH,IAEnD,IAAK,IAAMG,KADQiB,EAAapB,GAE9B,IAAK,IAAMA,KAAK4B,EAAkB,CAChC,IAAM9B,EAAS+B,EAAU1B,EAAKH,GAC9B,GAAI,CAACmC,EAA6BhC,EAAKL,GACrC,SAEF,IAAMI,EAAY8B,EAAmBlC,GACrC,KAAI,CAACI,GAGDsH,GAAuBtH,EAAU,IAAI,CAAEQ,MAGvCD,EAAoBV,EAASG,EAAU,IAAI,GAG3C,CAACI,EAAsBP,EAASE,EAAK,EAAE,CAAEC,EAAU,IAAI,EACzD,MAAO,CAAE,OAAQA,EAAU,MAAM,CAAE,OAAQA,EAAU,MAAM,AAAC,CAEhE,CAGJ,EAAG,WACGoC,EAA6CnC,EAAoB,CAACH,EAAMG,EAA0BL,EAA0B,EAAK,IAErI,IAAK,IAAMI,KADQkB,EAAapB,GACF,CAC5B,IAAMA,EAAS,CAAE,KAAME,EAAI,IAAI,CAAE,KAAMA,EAAI,IAAI,AAAC,EAChD,GAAIC,GAA4B,CAACgC,EAA6BjC,EAAKF,GACjE,SAEF,IAAMM,EAAY0B,EAAmBhC,GACrC,GAAIM,GAAa,CAACkH,GAAuBlH,EAAU,IAAI,CAAEI,IAAQ,CAACD,EAAoBV,EAASO,EAAU,IAAI,GAAK,CAACF,EAAyBL,EAASO,EAAU,IAAI,GAAMR,CAAAA,GAA2B,CAACO,EAAyBJ,EAAK,EAAE,CAAEK,EAAU,IAAI,GACnP,MAAO,CAAE,OAAQA,EAAU,MAAM,CAAE,OAAQA,EAAU,MAAM,AAAC,CAEhE,CAEF,EAAG,8BACGiC,EAASF,EAAQnB,IAAcA,CAAAA,EAAS,MAAM,CAAGH,EAAS,MAAM,CAAGsB,EAAQtB,GAAY,KAAK,IAAMuB,EAA2BvB,EAAU,KAASuB,EAA2BvB,EAAU,KAAUuB,EAA2BvB,EAAU,GAAO,IACjP,GAAIwB,EAAQ,CACVzC,EAAU,CAAC,CAAGyC,EAAO,MAAM,CAAC,IAAI,CAChCzC,EAAU,CAAC,CAAGyC,EAAO,MAAM,CAAC,IAAI,CAChCzC,EAAU,QAAQ,CAAGyC,EAAO,MAAM,CAClC,IAAMvC,EAAakC,EAAmBK,EAAO,MAAM,CAAC,IAAI,CAAEA,EAAO,MAAM,CAAC,IAAI,CAAE1B,EAAIC,GAC5Eb,EAAWM,EAAiB,SAAS,CAAC,AAACP,GAAWA,EAAO,OAAO,GAAKD,EACvEE,CAAAA,GAAY,EACdM,CAAgB,CAACN,EAAS,CAAG,CAAEF,QAAAA,EAAS,KAAMC,CAAW,EAEzDO,EAAiB,IAAI,CAAC,CAAER,QAAAA,EAAS,KAAMC,CAAW,EAEtD,CACF,CACF,CA5uEAG,EAAoByG,GAAqC,uCAoGzDzG,EAAoB0G,GAAqC,uCAwJzD1G,EAAoB2G,GAAkC,oCA+EtD3G,EAAoB4G,GAAiC,mCA0FrD5G,EAAoB6G,GAAoC,sCAqKxD7G,EAAoB8G,GAA+C,iDAoSnE9G,EAAoB+G,GAAsC,wCAiL1D/G,EAAoBgH,GAAiC,mCAyrBrDhH,EAAoBiH,GAAoC,sCAsNxDjH,EAAoBkH,GAAuB,yBA6C3ClH,EAAoBmH,GAAwB,0BAS5CnH,EAAoBoH,GAAe,iBASnCpH,EAAoBqH,GAAwB,0BAgQ5CrH,EAAoBsH,GAAwB,0BAO5C,SAASC,GAAQ3H,CAAC,CAAEC,CAAC,EACnB,OAAOD,EAAIC,EAAI,CAAC,EAAED,EAAE,EAAE,EAAEC,EAAE,CAAC,CAAG,CAAC,EAAEA,EAAE,EAAE,EAAED,EAAE,CAAC,AAC5C,CAEA,SAAS4H,GAAkC5H,CAAK,CAAEC,CAAK,EACrD,GAAM,CAAEC,aAAAA,CAAY,CAAEH,cAAAA,CAAa,CAAE,CAAG2D,EAAsBzD,GACxDE,EAA+B,IAAIc,IACzC,IAAK,IAAMjB,KAAKC,EAAO,CACrB,IAAMA,EAAKD,EAAE,EAAE,CACf,IAAIA,EAAE,OAAO,EAGTA,EAAE,WAAW,CAAE,CACjBG,EAAa,GAAG,CAACF,EAAI,CACnB,EAAGD,EAAE,KAAK,EAAI,EACd,EAAGA,EAAE,MAAM,EAAI,CACjB,GACA,QACF,CACF,CACA,IAAMK,EAAoCD,EAAoB,CAACH,EAAUC,EAAWH,EAAWM,KAC7F,IAAMC,EAAaqH,GAAQzH,EAAWH,GAClCQ,EAAU,EACRC,EAA2BJ,EAAoB,AAACJ,IACpD,GAAI,CAACA,EACH,OAEF,IAAMC,EAAME,EAAa,GAAG,CAACH,GAC7B,GAAI,CAACC,EACH,OAEF,IAAMC,EAAOG,AAAS,MAATA,EAAeJ,EAAI,CAAC,CAAG,EAAIA,EAAI,CAAC,CAAG,CAC5CC,CAAAA,EAAOK,GACTA,CAAAA,EAAUL,CAAG,CAEjB,EAAG,YAEH,IAAK,IAAMA,KADXM,EAASP,EAAS,WAAW,EACTD,GAAO,CACzB,GAAIE,IAAUD,GAGVC,EAAM,YAAY,CAFpB,SAKF,IAAMF,EAAOE,EAAM,KAAK,CAClBE,EAAOF,EAAM,GAAG,CACjBF,GAASI,GAGVuH,GAAQ3H,EAAMI,KAAUE,GAG5BE,EAASN,EAAM,WAAW,CAC5B,CACA,OAAOK,EAAU,EAAIA,EAvDI,EAuD+B,CAC1D,EAAG,qBACH,IAAK,IAAMN,KAAQD,EAAO,KAgBpBI,EACAD,EAhBJ,GAAIF,EAAK,YAAY,EAIjB,CAAC+C,EADO/C,EAAK,MAAM,CAhEhB,MA8DL,SAMF,IAAMK,EAAWuD,EAAoB5D,EAAMC,EApEpC,MAqEP,GAAI,CAACI,EACH,SAEF,GAAM,CAAEC,MAAAA,CAAK,CAAEC,MAAAA,CAAK,CAAEC,QAAAA,CAAO,CAAEC,QAAAA,CAAO,CAAEC,WAAAA,CAAU,CAAEG,WAAAA,CAAU,CAAE,CAAGR,EACnE,GAAIK,IAAeG,EACjB,SAIF,GAAIH,EAAY,CACd,IAAMX,EAAWU,EAAQ,EAAE,CAAGD,EAAQ,EAAE,CACxCL,EAAY,CAAE,EAAGK,EAAQ,EAAE,CAAE,EAAGT,EAAWS,EAAQ,IAAI,CAAC,MAAM,CAAGA,EAAQ,IAAI,CAAC,GAAG,AAAC,EAClFN,EAAY,CAAE,EAAGO,EAAQ,EAAE,CAAE,EAAGV,EAAWU,EAAQ,IAAI,CAAC,GAAG,CAAGA,EAAQ,IAAI,CAAC,MAAM,AAAC,CACpF,KAAO,CACL,IAAMV,EAAUU,EAAQ,EAAE,CAAGD,EAAQ,EAAE,CACvCL,EAAY,CAAE,EAAGJ,EAAUS,EAAQ,IAAI,CAAC,KAAK,CAAGA,EAAQ,IAAI,CAAC,IAAI,CAAE,EAAGA,EAAQ,EAAE,AAAC,EACjFN,EAAY,CAAE,EAAGH,EAAUU,EAAQ,IAAI,CAAC,IAAI,CAAGA,EAAQ,IAAI,CAAC,KAAK,CAAE,EAAGA,EAAQ,EAAE,AAAC,CACnF,CACA,GAAIoD,GAAmB1D,EAAWD,EAAWJ,EAAe,CAACQ,EAAOC,EAAM,CAAE,GAC1E,SAGF,IAAMO,EAAaV,EAAkBJ,EAAMM,EAAOC,EADhCG,EAAa,IAAM,KAE/BK,EAAiBD,EA1FT,EA0FoCA,EA1FpC,EA4Fd,IAAK,IAAMb,IADI,CAAC,EAAGc,EAAgB,CAACA,EAAe,CACvB,CAC1B,IAAMX,EAAa,CAAE,GAAGD,CAAS,AAAC,EAC5BE,EAAa,CAAE,GAAGH,CAAS,AAAC,EAClC,GAAIQ,EAGF,IAFAN,EAAW,CAAC,EAAIH,EAChBI,EAAW,CAAC,EAAIJ,EACZG,EAAW,CAAC,EAAII,EAAQ,IAAI,CAAC,IAAI,EAAIJ,EAAW,CAAC,EAAII,EAAQ,IAAI,CAAC,KAAK,EAGvEH,EAAW,CAAC,EAAII,EAAQ,IAAI,CAAC,IAAI,EAAIJ,EAAW,CAAC,EAAII,EAAQ,IAAI,CAAC,KAAK,CAFzE,QACF,MAOA,GAFAL,EAAW,CAAC,EAAIH,EAChBI,EAAW,CAAC,EAAIJ,EACZG,EAAW,CAAC,EAAII,EAAQ,IAAI,CAAC,GAAG,EAAIJ,EAAW,CAAC,EAAII,EAAQ,IAAI,CAAC,MAAM,EAGvEH,EAAW,CAAC,EAAII,EAAQ,IAAI,CAAC,GAAG,EAAIJ,EAAW,CAAC,EAAII,EAAQ,IAAI,CAAC,MAAM,CAFzE,SAMJ,IAAIoD,GAAmBzD,EAAYC,EAAYP,EAAe,CAACQ,EAAOC,EAAM,CAAE,KAG1EyD,GAA4B5D,EAAYC,EAAYN,EAAOC,EAAM,CAAE,QAvHlE,IAuHgF,IAGrFA,EAAK,MAAM,CAAG,CAACI,EAAYC,EAAW,CACtC,MACF,CACF,CACF,CAIA,SAASuH,GAA4B7H,CAAK,CAAEC,CAAW,EAQrD,GAAM,CAAEC,cAAAA,CAAa,CAAE,eAAgBH,CAAU,CAAE,CAAG4D,EACpD1D,EAAY,MAAM,IAEdE,EAA+BC,EAAoB,CAACJ,EAAMC,IACvD4C,EAA4B5C,EAXlB,MAWsC,GAAG,CAAC,AAACC,GAAa,EACvE,GAAGA,CAAO,CACVF,KAAAA,EACA,SAAUE,EAAQ,KAAK,EAAI,GAAKA,EAAQ,KAAK,EAAID,EAAO,MAAM,CAAG,CACnE,IACC,eACGI,EAA8BD,EAAoB,KACtD,IAAMH,EAAS,EAAE,CACjB,IAAK,IAAMC,KAAQF,EAAO,CACxB,GAAIE,EAAK,YAAY,CACnB,SAEF,IAAMF,EAASE,EAAK,MAAM,CACrBF,IAAUA,CAAAA,EAAO,MAAM,CAAG,IAG/BC,EAAO,IAAI,IAAIE,EAAaD,EAAM6C,EAAwB/C,IAC5D,CACA,OAAOC,CACT,EAAG,eACGK,EAA0CF,EAAoB,CAACJ,EAAGC,IACtE,AAAID,EAAE,UAAU,EAAIC,EAAE,UAAU,CACvB0C,EAAc3C,EAAE,CAAC,CAAC,CAAC,CAAEA,EAAE,CAAC,CAAC,CAAC,CAAEC,EAAE,CAAC,CAAC,CAAC,CAAEA,EAAE,CAAC,CAAC,CAAC,GAhC/B,GAgCmDW,AA/BnD,EA+BmDA,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAC,CAAC,CAAGC,EAAE,CAAC,CAAC,CAAC,IAEvFD,EAAE,QAAQ,IAAIC,EAAE,QAAQ,EACnB0C,EAAc3C,EAAE,CAAC,CAAC,CAAC,CAAEA,EAAE,CAAC,CAAC,CAAC,CAAEC,EAAE,CAAC,CAAC,CAAC,CAAEA,EAAE,CAAC,CAAC,CAAC,GAnC/B,GAmCmDW,AAlCnD,EAkCmDA,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAC,CAAC,CAAGC,EAAE,CAAC,CAAC,CAAC,EAG1F,2BACGM,EAAkCH,EAAoB,CAACH,EAAMG,KACjE,IAAMC,EAAWJ,EAAK,KAAK,CACrBM,EAAWN,EAAK,GAAG,CACnBO,EAAoBL,EAAaF,EAAMG,GAC7C,GAAII,EAAkB,MAAM,GAAKJ,EAAU,MAAM,CAAG,EAClD,MAAO,GAET,IAAMK,EAAc,CAACJ,EAAUE,EAAS,CAAC,MAAM,CAAC,AAACP,GAAO,EAAQA,GAC1DU,EAAcT,EAAK,WAAW,CAAG,CAACA,EAAK,WAAW,CAAC,CAAG,EAAE,CAC9D,IAAK,IAAMD,KAAWQ,EACpB,GAAIsD,GAAmB9D,EAAQ,CAAC,CAAEA,EAAQ,CAAC,CAAEE,EAAeO,EAAa,KAGrEqD,GAAmB9D,EAAQ,CAAC,CAAEA,EAAQ,CAAC,CAAED,EAAYW,EAAa,IAFpE,MAAO,GAMX,IAAK,IAAMR,KAASF,EAAO,CACzB,GAAIE,IAAUD,GAAQC,EAAM,YAAY,CACtC,SAEF,IAAMF,EAAcE,EAAM,MAAM,CAChC,GAAI,AAACF,IAAeA,CAAAA,EAAY,MAAM,CAAG,GAGzC,KAAK,IAAMC,KAAoBO,EAC7B,IAAK,IAAMJ,KAAgBD,EAAaD,EAAO6C,EAAwB/C,IACrE,GAAIM,EAAwBL,EAAkBG,IAG1C8D,GACFjE,EAAiB,CAAC,CAClBA,EAAiB,CAAC,CAClBG,EAAa,CAAC,CACdA,EAAa,CAAC,CA1EL,MAoET,MAAO,EAYb,CACF,CACA,MAAO,EACT,EAAG,mBACGI,EAAmCJ,EAAoB,CAACJ,EAASC,KACrE,IAAMC,EAAS6C,EAAwB/C,EAAQ,IAAI,CAAC,MAAM,EAAI,EAAE,EAChE,GAAIE,EAAO,MAAM,CAAG,GAAKF,EAAQ,KAAK,EAAIE,EAAO,MAAM,CAAG,EACxD,OAEF,IAAME,EAAYF,EAAO,GAAG,CAAC,AAACF,GAAO,EAAE,GAAGA,CAAC,AAAC,IAC5C,GAAIA,EAAQ,UAAU,CACpBI,CAAS,CAACJ,EAAQ,KAAK,CAAC,CAAC,CAAC,EAAIC,EAC9BG,CAAS,CAACJ,EAAQ,KAAK,CAAG,EAAE,CAAC,CAAC,EAAIC,OAC7B,IAAID,EAAQ,QAAQ,CAIzB,MAHAI,CAAAA,CAAS,CAACJ,EAAQ,KAAK,CAAC,CAAC,CAAC,EAAIC,EAC9BG,CAAS,CAACJ,EAAQ,KAAK,CAAG,EAAE,CAAC,CAAC,EAAIC,EAIpC,OAAOE,EAAaH,EAAQ,IAAI,CAAEI,GAAW,MAAM,GAAKA,EAAU,MAAM,CAAG,EAAIA,EAAY,KAAK,CAClG,EAAG,oBACGK,EAA6BL,EAAoB,CAACJ,EAAMC,IAAU,EACtE,EAAGD,EAAK,CAAC,EAAI,AAACC,CAAAA,EAAK,IAAI,CAAGA,EAAK,KAAK,AAAD,EAAK,EACxC,EAAGD,EAAK,CAAC,EAAI,AAACC,CAAAA,EAAK,GAAG,CAAGA,EAAK,MAAM,AAAD,EAAK,CAC1C,GAAI,cACES,EAAyCN,EAAoB,AAACJ,IAClE,IAAME,EAAOF,EAAQ,IAAI,CACnBI,EAAS2C,EAAwB7C,EAAK,MAAM,EAAI,EAAE,EACxD,GAAIE,AAAkB,IAAlBA,EAAO,MAAM,EAAUJ,AAAkB,IAAlBA,EAAQ,KAAK,CACtC,OAEF,IAAMD,EAAaG,EAAK,KAAK,CAAGD,EAAY,GAAG,CAACC,EAAK,KAAK,EAAI,KAAK,EAC7DC,EAAaD,EAAK,GAAG,CAAGD,EAAY,GAAG,CAACC,EAAK,GAAG,EAAI,KAAK,EACzDG,EAAaN,EAAauD,EAAiBvD,GAAc,KAAK,EAC9DO,EAAaH,EAAamD,EAAiBnD,GAAc,KAAK,EAC9DI,EAAOH,EAAO,KAAK,CAACJ,EAAQ,KAAK,CAAG,GAC1C,GAAI,AAACD,GAAeI,GAAeE,GAAeC,GAAcC,AAAgB,IAAhBA,EAAK,MAAM,CAG3E,MAAO,CACL,aAAcE,EAAWV,EAAYM,GACrC,aAAcI,EAAWN,EAAYG,GACrCD,WAAAA,EACAE,KAAAA,CACF,CACF,EAAG,0BACGI,EAAuCP,EAAoB,CAACJ,EAASC,EAAOC,EAAcE,EAAcL,EAAYI,KACxH,IAAME,EAAcD,EAAa,CAAC,EAAIF,EAAa,CAAC,CAC9CI,EAAcD,EAAcN,EAAW,MAAM,CAAGA,EAAW,GAAG,CAC9DQ,EAAQD,EAAeD,CAAAA,EA7HJ,GA6HuC,GAAkB,EAClF,GAAIA,GAAeL,EAAQ,CAAC,CAAC,CAAC,EAAIO,EAlIjB,MAkIuC,CAACF,GAAeL,EAAQ,CAAC,CAAC,CAAC,EAAIO,EAlItE,KAmIf,OAEF,IAAMC,EAAQR,EAAQ,CAAC,CAAC,CAAC,CAAGC,EAC5B,OAAO8C,EACL,CACE,CAAE,EAAG7C,EAAa,CAAC,CAAE,EAAGI,CAAY,EACpC,CAAE,EAAGJ,EAAa,CAAC,CAAE,EAAGK,CAAM,EAC9B,CAAE,EAAGC,EAAO,EAAGD,CAAM,EACrB,CAAE,EAAGC,EAAO,EAAGR,EAAQ,CAAC,CAAC,CAAC,AAAC,KACxBG,EACJ,CA7Ic,KAgJnB,EAAG,wBACGW,EAAyCV,EAAoB,CAACJ,EAASC,EAAOC,EAAcE,EAAcL,EAAYI,KAC1H,IAAME,EAAcD,EAAa,CAAC,EAAIF,EAAa,CAAC,CAC9CI,EAAcD,EAAcN,EAAW,KAAK,CAAGA,EAAW,IAAI,CAC9DQ,EAAQD,EAAeD,CAAAA,EAhJJ,GAgJuC,GAAkB,EAClF,GAAIA,GAAeL,EAAQ,CAAC,CAAC,CAAC,EAAIO,EArJjB,MAqJuC,CAACF,GAAeL,EAAQ,CAAC,CAAC,CAAC,EAAIO,EArJtE,KAsJf,OAEF,IAAMC,EAAQR,EAAQ,CAAC,CAAC,CAAC,CAAGC,EAC5B,OAAO8C,EACL,CACE,CAAE,EAAGzC,EAAa,EAAGJ,EAAa,CAAC,AAAC,EACpC,CAAE,EAAGK,EAAO,EAAGL,EAAa,CAAC,AAAC,EAC9B,CAAE,EAAGK,EAAO,EAAGC,CAAM,EACrB,CAAE,EAAGR,EAAQ,CAAC,CAAC,CAAC,CAAE,EAAGQ,CAAM,KACxBL,EACJ,CAhKc,KAmKnB,EAAG,0BACGY,EAAwCX,EAAoB,CAACJ,EAASC,KAC1E,IAAMC,EAAUQ,EAAuBV,GACvC,GAAKE,GAGL,GAAIF,EAAQ,QAAQ,CAClB,OAAOW,EACLX,EACAC,EACAC,EAAQ,YAAY,CACpBA,EAAQ,YAAY,CACpBA,EAAQ,UAAU,CAClBA,EAAQ,IAAI,EAGhB,GAAIF,EAAQ,UAAU,CACpB,OAAOc,EACLd,EACAC,EACAC,EAAQ,YAAY,CACpBA,EAAQ,YAAY,CACpBA,EAAQ,UAAU,CAClBA,EAAQ,IAAI,EAIlB,EAAG,yBACGc,EAAS,CACb,GA9LkB,EAgMlB,IACA,GACA,IACA,GACD,CACD,IAAK,IAAIhB,EAAY,EAAGA,EAjMD,GAiM6BA,IAAa,CAC/D,IAAMA,EAAWK,IACbJ,EAAQ,GACZ,IAAK,IAAIC,EAAI,EAAGA,EAAIF,EAAS,MAAM,EAAI,CAACC,EAAOC,IAC7C,IAAK,IAAIE,EAAIF,EAAI,EAAGE,EAAIJ,EAAS,MAAM,EAAI,CAACC,EAAOG,IAAK,CACtD,IAAML,EAAQC,CAAQ,CAACE,EAAE,CACnBC,EAASH,CAAQ,CAACI,EAAE,CAC1B,GAAIL,EAAM,IAAI,GAAKI,EAAO,IAAI,EAAKG,EAAwBP,EAAOI,GAIlE,IAAK,IAAMH,IADQ,CAACD,EAAOI,EAAO,CAAC,MAAM,CAAC,AAACH,GAAYA,EAAQ,QAAQ,EACrC,CAChC,IAAK,IAAME,KAASc,EAAQ,CAC1B,IAAMZ,EAASI,EAAiBR,EAASE,GACzC,GAAIE,GAAUG,EAAgBP,EAAQ,IAAI,CAAEI,GAAS,CACnDJ,EAAQ,IAAI,CAAC,MAAM,CAAGI,EACtBH,EAAQ,GACR,KACF,CACA,IAAMF,EAAWgB,EAAsBf,EAASE,GAChD,GAAIH,GAAYQ,EAAgBP,EAAQ,IAAI,CAAED,GAAW,CACvDC,EAAQ,IAAI,CAAC,MAAM,CAAGD,EACtBE,EAAQ,GACR,KACF,CACF,CACA,GAAIA,EACF,KAEJ,CACF,CAEF,GAAI,CAACA,EACH,MAEJ,CACF,CAIA,SAAS6H,GAAkB9H,CAAE,CAAEC,CAAE,CAAEC,CAAE,CAAEE,CAAE,EACvC,IAAML,EAAME,EAAG,CAAC,CAAGD,EAAG,CAAC,CACjBG,EAAMF,EAAG,CAAC,CAAGD,EAAG,CAAC,CACjBK,EAAMD,EAAG,CAAC,CAAGF,EAAG,CAAC,CACjBI,EAAMF,EAAG,CAAC,CAAGF,EAAG,CAAC,CACjBK,EAAQR,EAAMO,EAAMH,EAAME,EAChC,GAAIO,AAAkB,MAAlBA,KAAK,GAAG,CAACL,GACX,MAAO,GAET,IAAMC,EAAKN,EAAG,CAAC,CAAGF,EAAG,CAAC,CAChBS,EAAKP,EAAG,CAAC,CAAGF,EAAG,CAAC,CAChBU,EAAI,AAACF,CAAAA,EAAKF,EAAMG,EAAKJ,CAAE,EAAKE,EAC5BI,EAAI,AAACH,CAAAA,EAAKL,EAAMM,EAAKV,CAAE,EAAKQ,EAElC,OAAOG,EADK,KACMA,EAAI,KAAWC,EADrB,KACgCA,EAAI,GAClD,CAEA,SAASoH,GAAwB/H,CAAM,EACrC,IAAMC,EAAQD,EAAO,KAAK,EAAI,EAAE,CAC1BI,EAAQJ,EAAO,KAAK,EAAI,EAAE,CAC1BD,EAAS,EAAE,CACjB,GAAI,CAACK,EAAM,MAAM,EAAI,CAACH,EAAM,MAAM,CAChC,OAAOF,EAET,IAAMI,EAAYyD,EAAuB3D,GAEnCI,EAAe,EAAE,CACvB,IAAK,IAAML,KAAQI,EAAO,CACxB,GAAIJ,EAAK,YAAY,CACnB,SAEF,IAAMC,EAASD,EAAK,MAAM,CAC1B,GAAI,CAACC,GAAUA,EAAO,MAAM,CAAG,EAC7B,SAEF,IAAMC,EAAYF,EAAK,KAAK,CACtBI,EAAUJ,EAAK,GAAG,CAClBM,EAAaN,EAAK,WAAW,CAC7BO,EAASP,EAAK,EAAE,EAAI,CAAC,EAAEE,EAAU,EAAE,EAAEE,EAAQ,CAAC,CACpD,IAAK,IAAMJ,KAAQG,EACjB,GAAIH,EAAK,MAAM,GAAKE,GAAaF,EAAK,MAAM,GAAKI,GAG7CE,CAAAA,CAAAA,GAAcN,EAAK,MAAM,GAAKM,CAAS,EAG3C,KAAK,IAAIJ,EAAI,EAAGA,EAAID,EAAO,MAAM,CAAG,EAAGC,IACrC,GAAI+C,EAAyBhD,CAAM,CAACC,EAAE,CAAED,CAAM,CAACC,EAAI,EAAE,CAAEF,EAAM,IAAW,CACtED,EAAO,IAAI,CAAC,CACV,KAAM,oBACNQ,OAAAA,EACA,SAAUP,EAAK,MAAM,CACrB,OAAQ,CAAC,QAAQ,EAAEE,EAAE,sBAAsB,EAAEF,EAAK,MAAM,CAAC,CAAC,CAAC,AAC7D,GACA,KACF,CACF,CAEF,IAAK,IAAIA,EAAI,EAAGA,EAAIC,EAAO,MAAM,CAAG,EAAGD,IACrCK,EAAa,IAAI,CAAC,CAChBE,OAAAA,EACA,MAAOL,EACP,IAAKE,EACL,GAAIH,CAAM,CAACD,EAAE,CACb,GAAIC,CAAM,CAACD,EAAI,EAAE,AACnB,EAEJ,CACA,IAAMM,EAAgC,IAAIqG,IAC1C,IAAK,IAAI3G,EAAI,EAAGA,EAAIK,EAAa,MAAM,CAAEL,IACvC,IAAK,IAAIC,EAAID,EAAI,EAAGC,EAAII,EAAa,MAAM,CAAEJ,IAAK,CAChD,IAAMC,EAAIG,CAAY,CAACL,EAAE,CACnBI,EAAIC,CAAY,CAACJ,EAAE,CACzB,GAAIC,EAAE,MAAM,GAAKE,EAAE,MAAM,EAGrBF,EAAE,KAAK,GAAKE,EAAE,KAAK,EAAIF,EAAE,KAAK,GAAKE,EAAE,GAAG,EAAIF,EAAE,GAAG,GAAKE,EAAE,KAAK,EAAIF,EAAE,GAAG,GAAKE,EAAE,GAAG,EAGhF0H,GAAkB5H,EAAE,EAAE,CAAEA,EAAE,EAAE,CAAEE,EAAE,EAAE,CAAEA,EAAE,EAAE,EAAG,CAC7C,IAAMJ,EAAWE,EAAE,MAAM,CAAGE,EAAE,MAAM,CAAG,CAAC,EAAEF,EAAE,MAAM,CAAC,CAAC,EAAEE,EAAE,MAAM,CAAC,CAAC,CAAG,CAAC,EAAEA,EAAE,MAAM,CAAC,CAAC,EAAEF,EAAE,MAAM,CAAC,CAAC,CACvFI,EAAc,GAAG,CAACN,KACrBM,EAAc,GAAG,CAACN,GAClBD,EAAO,IAAI,CAAC,CACV,KAAM,qBACN,OAAQG,EAAE,MAAM,CAChB,SAAUE,EAAE,MAAM,CAClB,OAAQ,CAAC,OAAO,EAAEF,EAAE,MAAM,CAAC,OAAO,EAAEE,EAAE,MAAM,CAAC,OAAO,CAAC,AACvD,GAEJ,CACF,CAEF,GAAIL,EAAO,MAAM,CAAG,EAAG,CACrB,IAAMC,EAAWD,EAAO,MAAM,CAAC,AAACC,GAAMA,AAAW,sBAAXA,EAAE,IAAI,EAA0B,MAAM,CACtEC,EAAYF,EAAO,MAAM,CAAC,AAACC,GAAMA,AAAW,uBAAXA,EAAE,IAAI,EAA2B,MAAM,CAI9E,IAAK,IAAMI,KAHXF,EAAI,IAAI,CACN,CAAC,oBAAoB,EAAEH,EAAO,MAAM,CAAC,oBAAoB,EAAEC,EAAS,uBAAuB,EAAEC,EAAU,iBAAiB,CAAC,EAEvGF,GAClBG,EAAI,IAAI,CAAC,CAAC,sBAAsB,EAAEE,EAAM,IAAI,CAAC,EAAE,EAAEA,EAAM,MAAM,CAAC,CAAC,CAEnE,CACA,OAAOL,CACT,CAIA,SAASiI,GAA0BhI,CAAM,CAAEC,CAAS,EAClD,IAAMC,EAAQF,EAAO,KAAK,EAAI,EAAE,CAC1BD,EAAQC,EAAO,KAAK,EAAI,EAAE,CAC1BG,EAAeD,EAAM,MAAM,CAAC,AAACF,GAAM,CAACA,EAAE,OAAO,EACnD,GAAKC,CAAAA,AAAc,OAAdA,GAAsBA,AAAc,OAAdA,CAAiB,GAAME,EAAa,MAAM,CAAG,GAAK,CAACoG,GAA0BvG,EAAQC,IAG5GA,AAAc,OAAdA,GAAsBE,EAAa,MAAM,CAAG,GAAK,CAACmG,GAA0BtG,GAF9E,OAKF,IAAK,IAAMA,KAAQD,EAAO,CACxB,GAAIC,EAAK,YAAY,CACnB,SAEF,IAAMC,EAAMD,EAAK,MAAM,CAClBC,IAAOA,CAAAA,EAAI,MAAM,CAAG,IAGzBD,CAAAA,EAAK,MAAM,CAAGuE,GACZD,GAAsBrE,GACxB,CACF,CACAqH,GAAsBvH,EAAOG,GAC7B0H,GAAkC7H,EAAOG,GACzCuG,GAAiB1G,EAAOG,GACxB,IAAMG,EAA8B,IAAIY,IACxC,IAAK,IAAMjB,KAAKE,EACdG,EAAY,GAAG,CAACgC,OAAOrC,EAAE,EAAE,EAAGA,GAEhC0H,GAAuB3H,EAAOM,GAC9BsE,GAAkC5E,EAAOM,GACzCqG,GAA0B3G,EAAOM,GACjCwH,GAA4B9H,EAAOM,GACnCwG,GAAoC9G,EAAOM,GAC3CyG,GAAoC/G,EAAOM,GAC3C0G,GAAiChH,EAAOM,GACxC6G,GAA8CnH,EAAOM,GACrD,IAAMC,EAAwCF,EAAoB,KAChEiH,GAAmCtH,EAAOM,GAC1C8G,GAAqCpH,EAAOM,GAC5C+G,GAAgCrH,EAAOM,GACvCqH,GAAuB3H,EAAOM,GAC9ByF,GAAgC/F,EAAOM,GACvC0G,GAAiChH,EAAOM,GACxCqH,GAAuB3H,EAAOM,GAC9ByF,GAAgC/F,EAAOM,EACzC,EAAG,yBACHC,IACAuH,GAA4B9H,EAAOM,GACnCC,IACA0G,GAAgCjH,EAAOM,GACvC4G,GAAmClH,EAAOM,GAC1C2G,GAAgCjH,EAAOM,GACvC4G,GAAmClH,EAAOM,EAC5C,CAIA,SAAS4H,GAAejI,CAAC,EACvB,IAAMC,EAAW,IAAIgB,IAAIjB,EAAE,QAAQ,EAC7BE,EAAuB,IAAIyG,IAC3BvG,EAAQ,EAAE,CAChB,IAAK,IAAML,KAAKC,EAAE,KAAK,CAAE,CACvB,GAAI,CAACC,EAAS,GAAG,CAACF,EAAE,GAAG,GAAK,CAACE,EAAS,GAAG,CAACF,EAAE,GAAG,EAC7C,SAEF,IAAMC,EAAM,CAAC,EAAED,EAAE,EAAE,CAAC,CAAC,EAAEA,EAAE,GAAG,CAAC,EAAE,EAAEA,EAAE,GAAG,CAAC,CAAC,CACpCG,EAAK,GAAG,CAACF,KAGbE,EAAK,GAAG,CAACF,GACTI,EAAM,IAAI,CAACL,GACb,CAEA,MAAO,CAAE,MADK,IAAIE,EAAS,IAAI,GAAG,CAClBG,MAAAA,EAAO,OAAQJ,EAAE,MAAM,CAAEC,SAAAA,CAAS,CACpD,CAEA,SAASiI,GAASlI,CAAC,CAAEC,CAAC,EACpB,OAAOD,EAAE,KAAK,CAAC,MAAM,CAAC,AAACA,GAAMA,EAAE,GAAG,GAAKC,EACzC,CAEA,SAASkI,GAAkBnI,CAAC,EAC1B,IAAMC,EAAwB,IAAIgB,IAClC,IAAK,IAAMf,KAAKF,EAAE,KAAK,CACrBC,EAAM,GAAG,CAACC,EAAG,EAAE,EAEjB,IAAK,IAAMA,KAAKF,EAAE,KAAK,CACrBC,EAAM,GAAG,CAACC,EAAE,GAAG,EAAE,IAAI,CAACA,EAAE,GAAG,EAE7B,OAAOD,CACT,CAEA,SAASmI,GAAwBpI,CAAC,EAChC,IAAMC,EAAQkI,GAAkBnI,GAChC,IAAK,IAAMA,KAAcC,EAAM,MAAM,GACnCD,EAAW,IAAI,CAAC,CAACA,EAAGC,IAAMD,EAAE,aAAa,CAACC,IAE5C,OAAOA,CACT,CAEA,SAASoI,GAAiBrI,CAAC,EACzB,IAAMC,EAAwB,IAAIgB,IAClC,IAAK,IAAMf,KAAKF,EAAE,KAAK,CACrBC,EAAM,GAAG,CAACC,EAAG,GAEf,IAAK,IAAMA,KAAKF,EAAE,KAAK,CACrBC,EAAM,GAAG,CAACC,EAAE,GAAG,CAAE,AAACD,CAAAA,EAAM,GAAG,CAACC,EAAE,GAAG,GAAK,GAAK,GAE7C,OAAOD,CACT,CAEA,SAASqI,GAAwBtI,CAAK,EACpC,MAAO,IAAIA,EAAM,OAAO,GAAG,CAAC,MAAM,CAAC,CAAC,EAAGA,EAAO,GAAKA,AAAW,IAAXA,GAAc,GAAG,CAAC,CAAC,CAACA,EAAG,GAAKA,GAAI,IAAI,CAAC,CAACA,EAAGC,IAAMD,EAAE,aAAa,CAACC,GACpH,CAEA,SAASsI,GAA8BvI,CAAC,CAAEC,EAAc,IAAM,EAAI,EAChE,IAAMC,EAAwB,IAAIe,IAC5Bb,EAAwB,IAAIa,IAClC,IAAK,IAAMhB,KAAKD,EAAE,KAAK,CACrBE,EAAM,GAAG,CAACD,EAAG,EAAE,EACfG,EAAM,GAAG,CAACH,EAAG,EAAE,EAEjB,IAAK,IAAMF,KAAKC,EAAE,KAAK,CAChBC,EAAYF,KAGjBK,EAAM,GAAG,CAACL,EAAE,GAAG,EAAE,IAAI,CAACA,EAAE,GAAG,EAC3BG,EAAM,GAAG,CAACH,EAAE,GAAG,EAAE,IAAI,CAACA,EAAE,GAAG,GAE7B,MAAO,CAAEG,MAAAA,EAAOE,MAAAA,CAAM,CACxB,CAEA,SAASoI,GAAqBxI,CAAC,CAAEC,CAAK,CAAEC,CAAM,CAAEE,CAAI,EAClD,IAAIL,EAAU,EACd,IAAK,IAAME,KAAKD,EAAE,KAAK,CACjBI,GAAM,YAAcJ,EAAE,QAAQ,CAAC,GAAG,CAACC,IAAI,SAG3CF,CAAAA,EAAUa,KAAK,GAAG,CAACb,EAASG,CAAM,CAACD,EAAE,EAAI,EAAC,EAE5C,IAAME,EAASuB,MAAM,IAAI,CAAC,CAAE,OAAQ3B,EAAU,CAAE,EAAG,IAAM,EAAE,EAC3D,IAAK,IAAMA,KAAKE,EACVG,GAAM,YAAcJ,EAAE,QAAQ,CAAC,GAAG,CAACD,IAAI,SAG3CI,CAAM,CAACS,KAAK,GAAG,CAAC,EAAGV,CAAM,CAACH,EAAE,EAAI,GAAG,CAAC,IAAI,CAACA,GAE3C,OAAOI,CACT,CAEA,SAASsI,GAAkBzI,CAAC,EAC1B,IAAMC,EAAQoI,GAAiBrI,GACzBE,EAAQoI,GAAwBrI,GAChCG,EAAQ,EAAE,CACVL,EAAMqI,GAAwBpI,GACpC,KAAOE,EAAM,MAAM,EAAE,CACnB,IAAMF,EAAIE,EAAM,KAAK,GAErB,IAAK,IAAMC,KADXC,EAAM,IAAI,CAACJ,GACKD,EAAI,GAAG,CAACC,IAAM,EAAE,EAE9B,GADAC,EAAM,GAAG,CAACE,EAAG,AAACF,CAAAA,EAAM,GAAG,CAACE,IAAM,GAAK,GAC/B,AAACF,CAAAA,EAAM,GAAG,CAACE,IAAM,KAAO,EAAG,CAC7B,IAAIH,EAAI,EACR,KAAOA,EAAIE,EAAM,MAAM,EAAIA,CAAK,CAACF,EAAE,CAAGG,GACpCH,IAEFE,EAAM,MAAM,CAACF,EAAG,EAAGG,EACrB,CAEJ,CACA,OAAOC,EAAM,MAAM,GAAKJ,EAAE,KAAK,CAAC,MAAM,CAAGI,EAAQ,IACnD,CAEA,SAASsI,GAAgB1I,CAAK,EAC5B,IAAMC,EAAoB,IAAIgB,IAC1Bf,EAAQ,EACZ,IAAK,IAAME,KAAMJ,EACfC,EAAE,GAAG,CAACG,EAAIF,GACVA,IAEF,OAAOD,CACT,CAEA,SAAS0I,GAAgB3I,CAAM,EAC7B,IAAMC,EAAM,AAAIyB,MAAM1B,EAAO,MAAM,EAC7BE,EAAwBE,EAAoB,CAACA,EAAML,KACvD,GAAIA,EAAQK,GAAQ,EAClB,OAAO,EAET,IAAMD,EAAMC,EAAOL,GAAS,EACxBM,EAAaH,EAAME,EAAMD,GAAOD,EAAMC,EAAKJ,GAC3CO,EAAIF,EACJG,EAAIJ,EACJK,EAAIJ,EACR,KAAOE,EAAIH,GAAOI,EAAIR,GAChBQ,GAAKR,GAASO,EAAIH,GAAOH,CAAM,CAACM,EAAE,EAAIN,CAAM,CAACO,EAAE,CACjDN,CAAG,CAACO,IAAI,CAAGR,CAAM,CAACM,IAAI,EAEtBL,CAAG,CAACO,IAAI,CAAGR,CAAM,CAACO,IAAI,CACtBF,GAAcF,EAAMG,GAGxB,IAAK,IAAIJ,EAAIE,EAAMF,EAAIH,EAAOG,IAC5BF,CAAM,CAACE,EAAE,CAAGD,CAAG,CAACC,EAAE,CAEpB,OAAOG,CACT,EAAG,SACH,OAAOH,EAAM,EAAGF,EAAO,MAAM,CAC/B,CAIA,SAAS4I,GAAiB5I,CAAC,EACzB,IAAMC,EAAKgI,GAAejI,GACpBE,EAAsB,IAAIe,IAChC,IAAK,IAAMjB,KAAKC,EAAG,KAAK,CACtBC,EAAI,GAAG,CAACF,EAAG,EAAE,EAEf,IAAK,IAAMA,KAAKC,EAAG,KAAK,CACtBC,EAAI,GAAG,CAACF,EAAE,GAAG,EAAE,IAAI,CAACA,GAEtB,IAAK,IAAMA,KAAOE,EAAI,MAAM,GAC1BF,EAAI,IAAI,CAAC,CAACA,EAAGC,IAAMD,EAAE,GAAG,GAAKC,EAAE,GAAG,CAAGD,EAAE,EAAE,CAAC,aAAa,CAACC,EAAE,EAAE,EAAID,EAAE,GAAG,CAAC,aAAa,CAACC,EAAE,GAAG,GAE3F,IAAMF,EAAwB8I,OAAO,MAAM,CAAC,MAC5C,IAAK,IAAM7I,KAAKC,EAAG,KAAK,CACtBF,CAAK,CAACC,EAAE,CAAG,EAEb,IAAMG,EAAW,EAAE,CACbE,EAAsBD,EAAoB,AAACJ,IAE/C,IAAK,IAAMC,KADXF,CAAK,CAACC,EAAE,CAAG,EACKE,EAAI,GAAG,CAACF,IAAM,EAAE,EAAE,CAChC,IAAMA,EAAIC,EAAE,GAAG,AACXF,AAAa,KAAbA,CAAK,CAACC,EAAE,CACVK,EAAIL,GACKD,AAAa,IAAbA,CAAK,CAACC,EAAE,EACjBG,EAAS,IAAI,CAACF,EAElB,CACAF,CAAK,CAACC,EAAE,CAAG,CACb,EAAG,OAEH,IAAK,IAAMA,IADS,IAAIC,EAAG,KAAK,CAAC,CAAC,IAAI,CAAC,CAACD,EAAGC,IAAMD,EAAE,aAAa,CAACC,IAE3DF,AAAa,IAAbA,CAAK,CAACC,EAAE,EACVK,EAAIL,GAGR,IAAMM,EAAY,IAAIqG,IAAIxG,EAAS,GAAG,CAAC,AAACH,GAAM,CAAC,EAAEA,EAAE,EAAE,CAAC,CAAC,EAAEA,EAAE,GAAG,CAAC,EAAE,EAAEA,EAAE,GAAG,CAAC,CAAC,GACpEO,EAAYN,EAAG,KAAK,CAAC,GAAG,CAC5B,AAACD,GAAMM,EAAU,GAAG,CAAC,CAAC,EAAEN,EAAE,EAAE,CAAC,CAAC,EAAEA,EAAE,GAAG,CAAC,EAAE,EAAEA,EAAE,GAAG,CAAC,CAAC,EAAI,CAAE,GAAIA,EAAE,EAAE,CAAE,IAAKA,EAAE,GAAG,CAAE,IAAKA,EAAE,GAAG,CAAE,OAAQA,EAAE,MAAM,CAAE,IAAKA,EAAE,GAAG,AAAC,EAAIA,GAQ5H,MAAO,CAAE,QANO,CACd,MAAO,IAAIC,EAAG,KAAK,CAAC,CACpB,MAAOM,EACP,OAAQN,EAAG,MAAM,CACjB,SAAU,IAAIgB,IAAIhB,EAAG,QAAQ,CAC/B,EACkBE,SAAAA,CAAS,CAC7B,CAIA,SAAS2I,GAAgB9I,CAAC,EACxB,IAAMC,EAAwB,IAAIgB,IAC5Bf,EAA0BE,EAAoB,AAACA,IACnD,GAAIH,EAAM,GAAG,CAACG,GACZ,OAAOH,EAAM,GAAG,CAACG,GAEnB,IAAML,EAAOC,EAAE,QAAQ,CAAC,GAAG,CAACI,GAC5B,GAAI,CAACL,EAEH,OADAE,EAAM,GAAG,CAACG,EAAI,MACP,KAET,IAAMD,EAAWJ,EAAK,QAAQ,CAC9B,GAAI,CAACI,EAEH,OADAF,EAAM,GAAG,CAACG,EAAI,MACP,KAGT,IAAMC,EAAO,AADMH,EAAQC,IACAA,EAE3B,OADAF,EAAM,GAAG,CAACG,EAAIC,GACPA,CACT,EAAG,WACH,IAAK,IAAMJ,KAAMD,EAAE,KAAK,CACtBE,EAAQD,GAEV,OAAOA,CACT,CAEA,SAAS8I,GAAsB/I,CAAC,EAC9B,IAAMC,EAAa6I,GAAgB9I,GACnC,OAAO,AAACA,GAAOC,EAAW,GAAG,CAACD,IAAO,IACvC,CAEA,SAASgJ,GAAkBhJ,CAAC,EAC1B,IAAMC,EAAQ,EAAE,CAChB,IAAK,IAAMC,KAAQF,EAAE,MAAM,CAAC,KAAK,EAAI,EAAE,CACjCE,EAAK,OAAO,EAAI,CAACA,EAAK,QAAQ,EAChCD,EAAM,IAAI,CAACC,EAAK,EAAE,EAGtB,MAAO,IAAI,IAAIyG,IAAI1G,GAAO,CAAC,OAAO,EACpC,CAEA,SAASgJ,GAAoBjJ,CAAC,CAAEC,CAAc,EAC5C,IAAMC,EAAc8I,GAAkBhJ,GACtC,GAAI,CAACC,GAAkBA,AAA0B,IAA1BA,EAAe,MAAM,CAC1C,OAAOC,EAET,IAAME,EAAgB,IAAIuG,IAAIzG,GACxBH,EAAuB,IAAI4G,IAC3BxG,EAAW,EAAE,CACnB,IAAK,IAAMH,KAAUC,EACf,CAACG,EAAc,GAAG,CAACJ,IAAWD,EAAK,GAAG,CAACC,KAG3CD,EAAK,GAAG,CAACC,GACTG,EAAS,IAAI,CAACH,IAEhB,IAAK,IAAMA,KAAUE,EACfH,EAAK,GAAG,CAACC,IAGbG,EAAS,IAAI,CAACH,GAEhB,OAAOG,CACT,CA5xBAC,EAAoBuH,GAAS,WAwH7BvH,EAAoBwH,GAAmC,qCAgPvDxH,EAAoByH,GAA6B,+BAmBjDzH,EAAoB0H,GAAmB,qBAyFvC1H,EAAoB2H,GAAyB,2BA0D7C3H,EAAoB4H,GAA2B,6BAqB/C5H,EAAoB6H,GAAgB,kBAIpC7H,EAAoB8H,GAAU,YAW9B9H,EAAoB+H,GAAmB,qBAQvC/H,EAAoBgI,GAAyB,2BAW7ChI,EAAoBiI,GAAkB,oBAItCjI,EAAoBkI,GAAyB,2BAiB7ClI,EAAoBmI,GAA+B,iCAkBnDnI,EAAoBoI,GAAsB,wBAsB1CpI,EAAoBqI,GAAmB,qBAUvCrI,EAAoBsI,GAAiB,mBA2BrCtI,EAAoBuI,GAAiB,mBAkDrCvI,EAAoBwI,GAAkB,oBA6BtCxI,EAAoB0I,GAAiB,mBAKrC1I,EAAoB2I,GAAuB,yBAU3C3I,EAAoB4I,GAAmB,qBAwBvC5I,EAAoB6I,GAAqB,uBAuBzC,SAASC,GAAiBlJ,CAAK,CAAEC,CAAI,EACnC,IAAMC,EAAI+H,GAAejI,GACnBD,EAASE,GAAM,QAAW,KAAM,IAAG,EACnCE,EAAWF,GAAM,SACjB,CAAEI,MAAAA,CAAK,CAAE,CAAGkI,GAA8BrI,GAChD,IAAK,IAAMF,KAAOK,EAAM,MAAM,GAC5BL,EAAI,IAAI,CAAC,CAACA,EAAGC,IAAMD,EAAE,aAAa,CAACC,IAErC,IAAMK,EAAYmI,GAAkBvI,IAAM,IAAIA,EAAE,KAAK,CAAC,CAAC,IAAI,CAAC,CAACF,EAAGC,IAAMD,EAAE,aAAa,CAACC,IAChFM,EAA4B,IAAIU,IACtC,IAAK,GAAM,CAACjB,EAAKC,EAAG,GAAIK,EAAU,OAAO,GACvCC,EAAU,GAAG,CAACN,EAAID,GAEpB,IAAMQ,EAAyB,IAAIS,IAC7BR,EAA2B,IAAIQ,IACrC,IAAK,IAAMjB,KAAQE,EAAE,KAAK,CACxBO,EAAS,GAAG,CAACT,EAAM,EAAE,EAEvB,IAAK,IAAMA,KAAQM,EAAW,CAC5B,IAAML,EAAa,AAACI,CAAAA,EAAM,GAAG,CAACL,IAAS,EAAE,AAAD,EAAG,MAAM,CAAC,AAACA,GAAMQ,EAAO,GAAG,CAACR,IACpE,GAAIC,EAAW,MAAM,CAAG,EAAG,CACzB,IAAMC,EAASiJ,GAAanJ,EAAMC,EAAY,CAC5CF,OAAAA,EACAI,SAAAA,EACAI,UAAAA,CACF,GACAC,EAAO,GAAG,CAACR,EAAME,GACjBO,EAAS,GAAG,CAACP,GAAQ,IAAI,CAACF,EAC5B,MAAW,AAACQ,EAAO,GAAG,CAACR,IACrBQ,EAAO,GAAG,CAACR,EAAM,KAErB,CACA,IAAK,IAAMA,KAAQE,EAAE,KAAK,CACpB,AAACM,EAAO,GAAG,CAACR,IACdQ,EAAO,GAAG,CAACR,EAAM,MAGrB,IAAMU,EAA0B,IAAIiG,IACpC,IAAK,IAAM3G,KAAQE,EAAE,KAAK,CACpB,AAACM,CAAAA,EAAO,GAAG,CAACR,IAAS,IAAG,IAAO,MACjCU,EAAQ,GAAG,CAACV,GAGhB,IAAMW,EAAQ,IAAID,EAAQ,CAAC,IAAI,CAAC,CAACV,EAAGC,KAClC,IAAMC,EAAKK,EAAU,GAAG,CAACP,IAAM,EACzBI,EAAKG,EAAU,GAAG,CAACN,IAAM,SAC/B,AAAIC,IAAOE,EACFJ,EAAE,aAAa,CAACC,GAElBC,EAAKE,CACd,GACMU,EAAYsI,GAAelJ,GAC3Ba,EAAgC,IAAIE,IAC1C,IAAK,GAAM,CAACjB,EAAMC,EAAI,GAAIa,EAAU,OAAO,GACzCC,EAAc,GAAG,CACff,EACA,IAAIC,EAAI,CAAC,IAAI,CAAC,CAACD,EAAGC,IAAMD,EAAE,aAAa,CAACC,KAG5C,IAAMe,EAAcqI,GAAiBtI,GAC/BG,EAASoI,GAAcvI,GACvBI,EAA6B,IAAIF,IACvC,IAAK,IAAMjB,KAAQE,EAAE,KAAK,CACxBiB,EAAW,GAAG,CAACnB,EAAM,EAAE,EAEzB,IAAK,IAAMA,KAASkB,EAClB,IAAK,IAAMjB,KAAQD,EAAM,KAAK,CAAE,CAC9B,IAAME,EAAOiB,EAAW,GAAG,CAAClB,GACxBC,EACFA,EAAK,IAAI,CAACF,EAAM,EAAE,EAElBmB,EAAW,GAAG,CAAClB,EAAM,CAACD,EAAM,EAAE,CAAC,CAEnC,CAEF,IAAMoB,EAAW,EAAE,CACbC,EAAY,EAAE,CACdC,EAAuB,IAAIqF,IAC3B/E,EAAuBxB,EAAoB,AAACJ,IAChD,IAAIsB,EAAK,GAAG,CAACtB,IAKb,IAAK,IAAMC,KAFXqB,EAAK,GAAG,CAACtB,GACToB,EAAS,IAAI,CAACpB,GACMS,EAAS,GAAG,CAACT,IAAS,EAAE,EAC1C4B,EAAK3B,GAEPoB,EAAU,IAAI,CAACrB,GACjB,EAAG,QACH,IAAK,IAAMA,KAAQW,EACjBiB,EAAK5B,GAEP,IAAK,IAAMA,KAAQM,EACjBsB,EAAK5B,GAEP,MAAO,CACLQ,OAAAA,EACAC,SAAAA,EACAE,MAAAA,EACAK,YAAAA,EACAE,OAAAA,EACAC,WAAAA,EACA,UAAWJ,EACXK,SAAAA,EACAC,UAAAA,EACA,iBAAkBf,CACpB,CACF,CAEA,SAAS6I,GAAanJ,CAAI,CAAEC,CAAU,CAAEC,CAAG,EACzC,IAAME,EAAWF,EAAI,MAAM,CAACF,GAqB5B,MAAO,AApBQ,IAAIC,EAAW,CAAC,IAAI,CAAC,CAACD,EAAGC,KACtC,IAAMF,EAAQG,EAAI,MAAM,CAACF,GACnBG,EAAQD,EAAI,MAAM,CAACD,GACnBI,EAAYN,AAAS,MAATA,GAAiBA,IAAUK,EAE7C,GAAIC,IADcF,CAAAA,AAAS,MAATA,GAAiBA,IAAUC,CAAO,EAElD,OAAOC,EAAY,GAAK,EAE1B,IAAMC,EAAQJ,EAAI,QAAQ,EAAE,CAACF,EAAE,CACzBO,EAAQL,EAAI,QAAQ,EAAE,CAACD,EAAE,CAC/B,GAAIK,AAAS,MAATA,GAAiBC,AAAS,MAATA,GAAiBD,IAAUC,EAC9C,OAAOA,EAAQD,EAEjB,IAAME,EAAON,EAAI,SAAS,CAAC,GAAG,CAACF,IAAM,EAC/BS,EAAOP,EAAI,SAAS,CAAC,GAAG,CAACD,IAAM,SACrC,AAAIO,IAASC,EACJD,EAAOC,EAETT,EAAE,aAAa,CAACC,EACzB,EACa,CAAC,EAAE,AAClB,CAEA,SAASmJ,GAAepJ,CAAC,EACvB,IAAMC,EAA4B,IAAIgB,IACtC,IAAK,IAAMf,KAAQF,EAAE,KAAK,CACxBC,EAAU,GAAG,CAACC,EAAsB,IAAIyG,KAE1C,IAAK,IAAMzG,KAAKF,EAAE,KAAK,CACrBC,EAAU,GAAG,CAACC,EAAE,GAAG,EAAE,GAAG,CAACA,EAAE,GAAG,EAC9BD,EAAU,GAAG,CAACC,EAAE,GAAG,EAAE,GAAG,CAACA,EAAE,GAAG,EAEhC,OAAOD,CACT,CAEA,SAASoJ,GAAiBrJ,CAAS,EACjC,IAAMC,EAA8B,IAAIgB,IACpCf,EAAc,EAClB,IAAK,IAAME,KAAQJ,EAAU,IAAI,GAAI,CACnC,GAAIC,EAAY,GAAG,CAACG,GAClB,SAEF,IAAML,EAAQ,CAACK,EAAK,CACpB,KAAOL,EAAM,MAAM,CAAG,GAAG,CACvB,IAAMK,EAAML,EAAM,GAAG,GACrB,IAAIE,EAAY,GAAG,CAACG,GAIpB,IAAK,IAAMD,KADXF,EAAY,GAAG,CAACG,EAAKF,GACFF,EAAU,GAAG,CAACI,IAAQ,EAAE,EACrC,AAACH,EAAY,GAAG,CAACE,IACnBJ,EAAM,IAAI,CAACI,EAGjB,CACAD,GACF,CACA,OAAOD,CACT,CAEA,SAASqJ,GAActJ,CAAS,EAC9B,IAAMC,EAA4B,IAAIgB,IAChCf,EAAsB,IAAIe,IAC1BlB,EAAY,EAAE,CACdI,EAAS,EAAE,CACbE,EAAO,EACLC,EAAwBF,EAAoB,CAACA,EAAMG,KAGvD,IAAK,IAAMC,KAFXP,EAAU,GAAG,CAACG,EAAM,EAAEC,GACtBH,EAAI,GAAG,CAACE,EAAMC,GACKL,EAAU,GAAG,CAACI,IAAS,EAAE,EACtCI,IAASD,IAGRN,EAAU,GAAG,CAACO,GAOPP,CAAAA,EAAU,GAAG,CAACO,IAAS,GAAMP,CAAAA,EAAU,GAAG,CAACG,IAAS,KAC9DL,EAAU,IAAI,CAAC,CAACK,EAAMI,EAAK,EAC3BN,EAAI,GAAG,CAACE,EAAMQ,KAAK,GAAG,CAACV,EAAI,GAAG,CAACE,IAASC,EAAMJ,EAAU,GAAG,CAACO,IAASH,MARrEN,EAAU,IAAI,CAAC,CAACK,EAAMI,EAAK,EAC3BF,EAAME,EAAMJ,GACZF,EAAI,GAAG,CAACE,EAAMQ,KAAK,GAAG,CAACV,EAAI,GAAG,CAACE,IAASC,EAAMH,EAAI,GAAG,CAACM,IAASH,IAC3D,AAACH,CAAAA,EAAI,GAAG,CAACM,IAAS,IAAOP,CAAAA,EAAU,GAAG,CAACG,IAAS,IAClDD,EAAO,IAAI,CAACoJ,GAASnJ,EAAMI,EAAMT,EAAWI,EAAO,MAAM,IAOjE,EAAG,SACH,IAAK,IAAMD,KAAQF,EAAU,IAAI,GAC3B,AAACC,EAAU,GAAG,CAACC,IACjBI,EAAMJ,EAAM,MAGhB,OAAOC,CACT,CAEA,SAASoJ,GAASvJ,CAAC,CAAEC,CAAC,CAAEC,CAAK,CAAEE,CAAE,EAC/B,IAAML,EAAQ,EAAE,CACVI,EAAwB,IAAIwG,IAClC,KAAOzG,EAAM,MAAM,CAAG,GAAG,CACvB,IAAME,EAAOF,EAAM,GAAG,GAItB,GAHAH,EAAM,IAAI,CAACK,GACXD,EAAM,GAAG,CAACC,CAAI,CAAC,EAAE,EACjBD,EAAM,GAAG,CAACC,CAAI,CAAC,EAAE,EACbA,CAAI,CAAC,EAAE,GAAKJ,GAAKI,CAAI,CAAC,EAAE,GAAKH,GAAKG,CAAI,CAAC,EAAE,GAAKH,GAAKG,CAAI,CAAC,EAAE,GAAKJ,EACjE,KAEJ,CACA,MAAO,CAAEI,GAAAA,EAAIL,MAAAA,EAAO,MAAO,IAAII,EAAM,AAAC,CACxC,CAIA,SAASqJ,GAA0BxJ,CAAC,CAAEC,CAAM,CAAEC,CAAI,EAChD,IAAMH,EAAQ,IAAIC,EAAE,KAAK,CAAC,CACpBG,EAA0B,IAAIc,IACpC,IAAK,GAAM,CAACjB,EAAGC,EAAK,GAAIF,EAAM,OAAO,GACnCI,EAAQ,GAAG,CAACF,EAAMD,GAEpB,IAAMK,EAAIN,EAAM,MAAM,CAChBO,EAAY,AAAIoB,MAAMrB,GAAG,IAAI,CAAC,IAC9BE,EAAQ,AAAImB,MAAMrB,GAAG,IAAI,CAAC,GAC1BG,EAAQ,EAAE,CACVC,EAAuB,IAAIkG,IACjC,IAAK,IAAM3G,KAAQD,EAAO,CACxB,IAAME,EAAWC,EAAK,MAAM,CAAC,GAAG,CAACF,IAAS,KACpCI,EAAMD,EAAQ,GAAG,CAACH,EACb,OAAPI,GAGAH,AAAY,MAAZA,IACFK,CAAS,CAACF,EAAI,CAAG,GACjBG,CAAK,CAACH,EAAI,CAAG,EACRK,EAAK,GAAG,CAACT,KACZS,EAAK,GAAG,CAACT,GACTQ,EAAM,IAAI,CAACR,IAGjB,CACA,KAAOQ,EAAM,MAAM,CAAG,GAAG,CACvB,IAAMR,EAAUQ,EAAM,KAAK,GACrBP,EAAaE,EAAQ,GAAG,CAACH,GAC/B,GAAIC,AAAc,MAAdA,EAIJ,IAAK,IAAMG,KADOF,EAAK,QAAQ,CAAC,GAAG,CAACF,IAAY,EAAE,CACnB,CAC7B,GAAIS,EAAK,GAAG,CAACL,GACX,SAEF,IAAMJ,EAAWG,EAAQ,GAAG,CAACC,EACb,OAAZJ,IAGJM,CAAS,CAACN,EAAS,CAAGC,EACtBM,CAAK,CAACP,EAAS,CAAGO,CAAK,CAACN,EAAW,CAAG,EACtCQ,EAAK,GAAG,CAACL,GACTI,EAAM,IAAI,CAACJ,GACb,CACF,CACA,IAAK,IAAMJ,KAAQD,EAAO,CACxB,GAAIU,EAAK,GAAG,CAACT,GACX,SAEF,IAAMC,EAAME,EAAQ,GAAG,CAACH,EACb,OAAPC,IAGJK,CAAS,CAACL,EAAI,CAAG,GACjBM,CAAK,CAACN,EAAI,CAAG,EACbQ,EAAK,GAAG,CAACT,GACX,CACA,IAAMU,EAASE,KAAK,GAAG,CAAC,EAAGA,KAAK,IAAI,CAACA,KAAK,IAAI,CAACA,KAAK,GAAG,CAAC,EAAGP,KAAO,GAC5DM,EAAKe,MAAM,IAAI,CAAC,CAAE,OAAQhB,CAAO,EAAG,IAAM,AAAIgB,MAAMrB,GAAG,IAAI,CAAC,KAClE,IAAK,IAAIL,EAAI,EAAGA,EAAIK,EAAGL,IACrBW,CAAE,CAAC,EAAE,CAACX,EAAE,CAAGM,CAAS,CAACN,EAAE,CAEzB,IAAK,IAAIA,EAAI,EAAGA,EAAIU,EAAQV,IAC1B,IAAK,IAAIC,EAAI,EAAGA,EAAII,EAAGJ,IAAK,CAC1B,IAAMC,EAAOS,CAAE,CAACX,EAAI,EAAE,CAACC,EAAE,AACzBU,CAAAA,CAAE,CAACX,EAAE,CAACC,EAAE,CAAGC,AAAS,KAATA,EAAc,GAAKS,CAAE,CAACX,EAAI,EAAE,CAACE,EAAK,AAC/C,CAEF,IAAMY,EAA2BV,EAAoB,CAACJ,EAAMC,KAC1D,GAAID,AAAS,KAATA,GAAeC,AAAS,KAATA,EACjB,OAAO,EAELM,CAAAA,CAAK,CAACP,EAAK,CAAGO,CAAK,CAACN,EAAK,EAC3B,EAACD,EAAMC,EAAK,CAAG,CAACA,EAAMD,EAAK,AAAD,EAE5B,IAAME,EAAOK,CAAK,CAACP,EAAK,CAAGO,CAAK,CAACN,EAAK,CACtC,IAAK,IAAIA,EAAI,EAAGA,EAAIS,EAAQT,IAC1B,GAAIC,GAAQD,EAAI,GAEV,AAAS,KADbD,CAAAA,EAAOW,CAAE,CAACV,EAAE,CAACD,EAAK,AAAD,EAEf,OAAO,GAIb,GAAIA,IAASC,EACX,OAAOD,EAET,IAAK,IAAIE,EAAIQ,EAAS,EAAGR,GAAK,EAAGA,IAAK,CACpC,IAAME,EAAMO,CAAE,CAACT,EAAE,CAACF,EAAK,CACjBD,EAAMY,CAAE,CAACT,EAAE,CAACD,EAAK,AACX,MAARG,GAAcL,AAAQ,KAARA,GAGdK,IAAQL,IACVC,EAAOI,EACPH,EAAOF,EAEX,CACA,OAAOY,CAAE,CAAC,EAAE,CAACX,EAAK,AACpB,EAAG,YACGe,EAAYW,MAAM,IAAI,CAAC,CAAE,OAAQrB,CAAE,EAAG,IAAsB,IAAIY,KACtE,IAAK,IAAMf,KAAQF,EAAE,KAAK,CAAE,CAC1B,IAAIA,EAAME,EAAK,GAAG,CACdE,EAAMF,EAAK,GAAG,CACdH,EAAKE,CAAM,CAACD,EAAI,CAChBK,EAAKJ,CAAM,CAACG,EAAI,CACpB,GAAU,MAANL,GAAcM,AAAM,MAANA,IAGdN,EAAKM,IACP,CAACL,EAAKI,EAAI,CAAG,CAACA,EAAKJ,EAAI,CACvB,CAACD,EAAIM,EAAG,CAAG,CAACA,EAAIN,EAAG,EAEjBA,AAAM,MAANA,GAAcM,AAAM,MAANA,GAAcN,IAAOM,GANrC,SASF,IAAMC,EAAWH,EAAQ,GAAG,CAACH,GACvBO,EAAWJ,EAAQ,GAAG,CAACC,GAC7B,GAAIE,AAAY,MAAZA,GAAoBC,AAAY,MAAZA,EACtB,SAEF,IAAMC,EAAMM,EAASR,EAAUC,GAC/B,GAAIC,AAAQ,KAARA,EACF,SAEF,IAAMC,EAASM,CAAS,CAACP,EAAI,CAC7B,IAAK,IAAIR,EAAQD,EAAIC,EAAQK,EAAIL,IAC/BS,EAAO,GAAG,CAACT,EAAO,AAACS,CAAAA,EAAO,GAAG,CAACT,IAAU,GAAK,EAEjD,CACA,IAAMgB,EAA8B,IAAIC,IAClCC,EAA4Bd,EAAoB,CAACJ,EAAQC,KAC7D,GAAIA,AAAgB,IAAhBA,EAAO,IAAI,CAGf,IAAK,GAAM,CAACC,EAAOE,EAAM,GAAIH,EAC3BD,EAAO,GAAG,CAACE,EAAO,AAACF,CAAAA,EAAO,GAAG,CAACE,IAAU,GAAKE,EAEjD,EAAG,aACGe,EAA0B,IAAIwF,IAC9BvF,EAAsBhB,EAAoB,AAACJ,IAC/C,IAAMI,EAAMD,EAAQ,GAAG,CAACH,GACxBmB,EAAQ,GAAG,CAACnB,GACZ,IAAMD,EAAOK,AAAO,MAAPA,EAAc,KAAK,EAAIW,CAAS,CAACX,EAAI,CAC5CC,EAAcN,EAAO,IAAIkB,IAAIlB,GAAwB,IAAIkB,IAE/D,IAAK,IAAMb,KADOF,EAAK,QAAQ,CAAC,GAAG,CAACF,IAAS,EAAE,CAChB,CAC7B,IAAME,EAAWkB,EAAIhB,GACfL,EAAcE,CAAM,CAACD,EAAK,CAChC,GAAID,AAAe,MAAfA,EAAqB,CACvB,IAAII,EAAMa,EAAY,GAAG,CAAChB,GACrBG,IACHA,EAAsB,IAAIc,IAC1BD,EAAY,GAAG,CAAChB,EAAMG,IAExB,IAAIE,EAAQH,EAAS,GAAG,CAACH,IAAgB,EACnCO,EAAaL,CAAM,CAACG,EAAM,AAC5BE,AAAc,OAAdA,GAAsBA,EAAaP,GACrCM,CAAAA,GAAS,GAEXF,EAAI,GAAG,CAACC,EAAOC,EACjB,CACAa,EAAUb,EAAaH,EACzB,CACA,OAAOG,CACT,EAAG,OACH,IAAK,IAAML,KAAQE,EAAK,KAAK,CACvB,AAACiB,EAAQ,GAAG,CAACnB,IACfoB,EAAIpB,GAGR,IAAK,IAAMA,KAAQD,EACb,AAACoB,EAAQ,GAAG,CAACnB,IACfoB,EAAIpB,GAGR,OAAOgB,CACT,CAIA,SAASyI,GAAsBzJ,CAAK,CAAEC,CAAQ,CAAEC,CAAM,EACpD,IAAMH,EAA2B,IAAIkB,IAC/Bd,EAA2BC,EAAoB,AAACJ,IACpD,IAAII,EAAOF,CAAM,CAACF,EAAK,EAAI,EACrBK,EAAY,IAAIJ,EAAS,GAAG,CAACD,IAAS,EAAE,CAAC,CAE/C,IAAK,IAAMA,KADXK,EAAU,IAAI,CAACqJ,GAAoBxJ,IACfG,GAAW,CAC7BF,EAASH,GACT,IAAMC,EAAWF,EAAS,GAAG,CAACC,EAC1BC,AAAY,OAAZA,GACFG,CAAAA,EAAOQ,KAAK,GAAG,CAACR,EAAMH,EAAQ,CAElC,CACAF,EAAS,GAAG,CAACC,EAAMI,EACrB,EAAG,YACH,IAAK,IAAMH,KAAQD,EACjBG,EAASF,GAEX,OAAOF,CACT,CAEA,SAAS2J,GAAoB1J,CAAM,EACjC,MAAO,CAACC,EAAGC,KACT,IAAME,EAAKJ,CAAM,CAACC,EAAE,EAAI,EAClBF,EAAKC,CAAM,CAACE,EAAE,EAAI,EACxB,OAAOE,IAAOL,EAAKE,EAAE,aAAa,CAACC,GAAKE,EAAKL,CAC/C,CACF,CAEA,SAAS4J,GAAqB3J,CAAK,CAAEC,CAAQ,CAAEC,CAAM,CAAEH,CAAa,EAClE,IAAII,EAAU,EACd,IAAK,IAAMH,KAAQC,EAAU,CAC3B,IAAMA,EAAIC,CAAM,CAACF,EAAK,EAAI,CACtBC,CAAAA,EAAIE,GACNA,CAAAA,EAAUF,CAAAA,CAEd,CACA,IAAMI,EAASqB,MAAM,IAAI,CAAC,CAAE,OAAQvB,EAAU,CAAE,EAAG,IAAM,EAAE,EACrDG,EAA0B,IAAIqG,IAC9BpG,EAAuBH,EAAoB,AAACJ,IAChD,GAAIM,EAAQ,GAAG,CAACN,GACd,OAEFM,EAAQ,GAAG,CAACN,GACZ,IAAMC,EAAQC,CAAM,CAACF,EAAK,EAAI,EAK9B,IAAK,IAAME,KAJP,AAACG,CAAM,CAACJ,EAAM,EAChBI,CAAAA,CAAM,CAACJ,EAAM,CAAG,EAAE,AAAD,EAEnBI,CAAM,CAACJ,EAAM,CAAC,IAAI,CAACD,GACCD,EAAcC,IAChCO,EAAKL,EAET,EAAG,QACH,IAAK,IAAMD,KAAQD,EACjBO,EAAKN,GAEP,IAAK,IAAMD,KAAQC,EACjB,GAAI,CAACK,EAAQ,GAAG,CAACN,GAAO,CACtB,IAAMC,EAAQC,CAAM,CAACF,EAAK,EAAI,CAC1B,AAACK,CAAAA,CAAM,CAACJ,EAAM,EAChBI,CAAAA,CAAM,CAACJ,EAAM,CAAG,EAAE,AAAD,EAEnBI,CAAM,CAACJ,EAAM,CAAC,IAAI,CAACD,GACnBM,EAAQ,GAAG,CAACN,EACd,CAEF,OAAOK,CACT,CAEA,SAASuJ,GAAkB5J,CAAM,EAC/B,IAAMC,EAAS,EAAE,CACjB,IAAK,IAAMC,KAASF,EAAQ,CAC1B,IAAMA,EAAuB,IAAI2G,IAC3BvG,EAAU,EAAE,CAClB,IAAK,IAAMH,KAAMC,EACXF,EAAK,GAAG,CAACC,KAGbD,EAAK,GAAG,CAACC,GACTG,EAAQ,IAAI,CAACH,IAEfA,EAAO,IAAI,CAACG,EACd,CACA,OAAOH,CACT,CAIA,SAAS4J,GAAmB7J,CAAQ,CAAEC,CAAM,CAAEC,CAAW,CAAEE,CAAQ,EACjE,OAAO,AAACL,IACN,IAAMI,EAAMH,EAAS,GAAG,CAACD,IAAS,EAAE,CACpC,GAAII,AAAe,IAAfA,EAAI,MAAM,CACZ,MAAO,EAAE,CAEX,IAAME,EAAQJ,CAAM,CAACF,EAAK,EAAI,EACxBO,EAAS,EAAE,CACXC,EAAU,EAAE,CACZC,EAAWN,EAAY,GAAG,CAACH,GACjC,IAAK,IAAMC,KAASG,EAAK,CACvB,IAAMF,EAAOG,EAAS,GAAG,CAACJ,IAAUK,CAChCJ,CAAAA,EAAOI,EACTC,EAAO,IAAI,CAAC,CAAEN,MAAAA,EAAO,IAAKC,CAAK,GAE/BM,EAAQ,IAAI,CAACP,EAEjB,CAoBA,OAnBAM,EAAO,IAAI,CAAC,CAACN,EAAGC,IACd,AAAID,EAAE,GAAG,GAAKC,EAAE,GAAG,CACVD,EAAE,KAAK,CAAC,aAAa,CAACC,EAAE,KAAK,EAE/BD,EAAE,GAAG,CAAGC,EAAE,GAAG,EAEtBM,EAAQ,IAAI,CAAC,CAACP,EAAGC,KACf,IAAMC,EAAKM,GAAU,IAAIR,IAAM,EACzBD,EAAKS,GAAU,IAAIP,IAAM,EAC/B,GAAIC,IAAOH,EACT,OAAOG,EAAKH,EAEd,IAAMI,EAAKC,EAAS,GAAG,CAACJ,IAAMK,EACxBC,EAAKF,EAAS,GAAG,CAACH,IAAMI,SAC9B,AAAIF,IAAOG,EACFH,EAAKG,EAEPN,EAAE,aAAa,CAACC,EACzB,GACO,IAAIK,EAAO,GAAG,CAAC,AAACN,GAASA,EAAK,KAAK,KAAMO,EAAQ,AAC1D,CACF,CAEA,SAASuJ,GAAyB9J,CAAC,CAAEC,CAAM,CAAEC,CAAM,EACjD,IAAME,EAAO8I,GAAiBlJ,EAAG,CAC/B,SAAUC,EACVC,OAAAA,CACF,GACM,CAAEH,SAAAA,CAAQ,CAAEI,MAAAA,CAAK,CAAE,CAAGC,EAC5B,IAAK,IAAMH,KAAQD,EAAE,KAAK,CACpB,AAACD,EAAS,GAAG,CAACE,IAChBF,EAAS,GAAG,CAACE,EAAM,EAAE,EAGzB,IAAMI,EAAcmJ,GAA0BxJ,EAAGC,EAAQG,GACnDE,EAAc,IAAIH,EAAM,CAAC,IAAI,CAACuJ,GAAoBzJ,IAClDM,EAAWkJ,GAAsBnJ,EAAaP,EAAUE,GACxDO,EAAgBqJ,GAAmB9J,EAAUE,EAAQI,EAAaE,GACpEE,EAASkJ,GAAqBrJ,EAAaN,EAAE,KAAK,CAAEC,EAAQO,GAEhE,OADSoJ,GAAkBnJ,EAE7B,CAIA,SAASsJ,GAA8B/J,CAAK,CAAEC,CAAK,CAAEC,CAAK,EACxD,IAAME,EAAW,IAAIuG,IAAI3G,GACnBD,EAAW,IAAI4G,IAAI1G,GACnBE,EAAKuI,GAAgBzI,GACrBI,EAAK,EAAE,CACb,IAAK,IAAML,KAAKE,EACVE,EAAS,GAAG,CAACJ,EAAE,GAAG,GAAKD,EAAS,GAAG,CAACC,EAAE,GAAG,GAC3CK,EAAG,IAAI,CAACF,EAAG,GAAG,CAACH,EAAE,GAAG,GAGxB,OAAO2I,GAAgBtI,EACzB,CAEA,SAAS2J,GAAehK,CAAM,CAAEC,CAAK,CAAEC,CAAM,EAC3C,IAAME,EAAW,EAAE,CACnB,IAAK,IAAMJ,KAAKC,EAAO,CACrB,IAAMA,EAAKC,CAAM,CAACF,EAAE,GAAG,CAAC,CAClBD,EAAKG,CAAM,CAACF,EAAE,GAAG,CAAC,CACxB,GAAIC,AAAM,MAANA,GAAcF,AAAM,MAANA,GAAcE,IAAOF,EACrC,SAEF,IAAII,EAAQH,EAAE,GAAG,CACbK,EAAQL,EAAE,GAAG,CACbM,EAASL,EACTM,EAASR,EACTE,EAAKF,IACPI,EAAQH,EAAE,GAAG,CACbK,EAAQL,EAAE,GAAG,CACbM,EAASP,EACTQ,EAASN,GAEX,IAAK,IAAIA,EAAIK,EAAQL,EAAIM,EAAQN,IAC/BG,EAAS,IAAI,CAAC,CAAE,GAAI,CAAC,EAAEJ,EAAE,EAAE,CAAC,CAAC,EAAEC,EAAE,CAAC,CAAE,IAAKE,EAAO,IAAKE,EAAO,IAAKL,EAAE,GAAG,AAAC,EAE3E,CACA,IAAID,EAAM,EACV,IAAK,IAAIE,EAAI,EAAGA,EAAI,EAAID,EAAO,MAAM,CAAEC,IACrCF,GAAOgK,GAA8B/J,CAAM,CAACC,EAAE,CAAED,CAAM,CAACC,EAAI,EAAE,CAAEG,GAEjE,OAAOL,CACT,CAEA,SAASkK,GAAyBjK,CAAC,CAAEC,CAAW,EAC9C,IAAMC,EAAS,CAAE,GAAGD,CAAW,AAAC,EAC1B,CAAEG,MAAAA,CAAK,CAAE,CAAGmI,GAA8BvI,GAC1CD,EAASgJ,GAAsB/I,GAEjCG,EAAO6J,GADIF,GAAyB9J,EAAGE,EAAQH,GACjBC,EAAE,KAAK,CAAEE,GAE3C,IAAK,IAAID,EAAO,EAAGA,EAzmBe,EAymBGA,IAAQ,CAC3C,IAAIA,EAAU,GAEd,IAAK,IAAMI,IADS,IAAIL,EAAE,KAAK,CAAC,CAAC,IAAI,CAAC,CAACA,EAAGC,IAAM,AAACC,CAAAA,CAAM,CAACD,EAAE,EAAI,GAAMC,CAAAA,CAAM,CAACF,EAAE,EAAI,IACpD,CAC3B,IAAMM,EAAIJ,CAAM,CAACG,EAAE,EAAI,EACvB,GAAIC,AAAM,IAANA,EACF,SAEF,IAAIC,EAAK,EACT,IAAK,IAAMP,KAAKI,EAAM,GAAG,CAACC,IAAM,EAAE,CAChCE,EAAKK,KAAK,GAAG,CAACL,EAAI,AAACL,CAAAA,CAAM,CAACF,EAAE,EAAI,GAAK,GAEvC,GAAIO,GAAMD,EACR,QAGFJ,CAAAA,CAAM,CAACG,EAAE,CAAGE,EAEZ,IAAMC,EAAQwJ,GADMF,GAAyB9J,EAAGE,EAAQH,GACdC,EAAE,KAAK,CAAEE,EAC/CM,CAAAA,EAAQL,GACVA,EAAOK,EACPP,EAAU,IAEVC,CAAM,CAACG,EAAE,CARCC,CAUd,CACA,GAAI,CAACL,EACH,KAEJ,CACA,OAAOC,CACT,CAIA,SAASgK,GAAuBlK,CAAC,CAAEC,CAAM,EACvC,IAAMC,EAAY6I,GAAsB/I,GAIxC,IAAK,IAAMI,IAHS,IAAIJ,EAAE,KAAK,CAAC,CAAC,IAAI,CACnC,CAACA,EAAGE,IAAM,AAACD,CAAAA,CAAM,CAACD,EAAE,EAAI,GAAMC,CAAAA,CAAM,CAACC,EAAE,EAAI,IAAMF,EAAE,aAAa,CAACE,IAEtC,CAC3B,IAAMH,EAAQG,EAAUE,GACxB,GAAI,CAACL,EACH,SAEF,IAAMI,EAAWH,EAAE,KAAK,CAAC,MAAM,CAAC,AAACA,GAAMA,EAAE,GAAG,GAAKI,GACjD,GAAID,AAAoB,IAApBA,EAAS,MAAM,CACjB,SAEF,IAAIE,EAAkB,GAClBC,EAAiB,EACrB,IAAK,IAAMN,KAAKG,EAAU,CACxB,IAAMF,EAAUC,EAAUF,EAAE,GAAG,CAC3BC,AAAW,OAAXA,GAAmBA,IAAYF,EACjCM,EAAkB,GAElBC,GAEJ,CACA,GAAIA,AAAmB,IAAnBA,GAAwBD,EAC1B,SAEF,IAAIE,EAAoB,EACpBC,EAAkB,GACtB,IAAK,IAAMP,KAAKD,EAAE,KAAK,CAAE,CACvB,GAAIC,EAAE,GAAG,GAAKG,EACZ,SAEF,IAAMJ,EAAUE,EAAUD,EAAE,GAAG,EAC1BD,IAGDA,IAAYD,EACdS,EAAkB,GAElBD,IAEJ,CACA,GAAIA,EAAoB,GAAK,CAACC,EAC5B,SAEF,IAAMC,EAAUR,CAAM,CAACG,EAAE,EAAI,EACvBM,EAASD,EAAUH,EACrBK,EAAK,EACT,IAAK,IAAMT,KAAKF,EAAE,KAAK,CACjBE,EAAE,GAAG,GAAKE,GACZO,CAAAA,EAAKC,KAAK,GAAG,CAACD,EAAI,AAACV,CAAAA,CAAM,CAACC,EAAE,GAAG,CAAC,EAAI,GAAK,EAAC,EAG9C,IAAMY,EAAUF,KAAK,GAAG,CAACH,EAASE,EAAID,EAClCI,CAAAA,IAAYL,GACdR,CAAAA,CAAM,CAACG,EAAE,CAAGU,CAAM,CAEtB,CACF,CAIA,SAASqJ,GAAyBnK,CAAQ,CAAEC,CAAI,EAC9C,IAAMC,EAAI+H,GAAejI,GACnBI,EAAQqI,GAAkBvI,IAAM,IAAIA,EAAE,KAAK,CAAC,CAAC,IAAI,GACjDH,EAAUE,GAAM,oBAAsB,GACtCE,EAAY4I,GAAsB7I,GACpCG,EAAyBwI,OAAO,MAAM,CAAC,MAC3C,IAAK,IAAM7I,KAAKI,EAAO,CACrB,IAAMA,EAAS8H,GAAShI,EAAGF,GACrBM,EAAML,GAAM,qBAAuBG,EAAO,MAAM,CAAC,AAACH,IACtD,IAAMC,EAAUC,EAAUF,EAAE,GAAG,EACzBG,EAAUD,EAAUH,SAC1B,CAAKE,IAAYE,GAGVF,IAAYE,CACrB,GAAKA,EACL,GAAIE,AAAe,IAAfA,EAAI,MAAM,CACZD,CAAM,CAACL,EAAE,CAAG,OACP,GAAID,GAAWO,AAAe,IAAfA,EAAI,MAAM,CAAQ,CACtC,IAAML,EAAIK,CAAG,CAAC,EAAE,CAAC,GAAG,AAGhB,CAFUH,EAAUF,KACVE,EAAUH,GAEtBK,CAAM,CAACL,EAAE,CAAGK,CAAM,CAACJ,EAAE,EAAI,EAEzBI,CAAM,CAACL,EAAE,CAAG,AAACK,CAAAA,CAAM,CAACJ,EAAE,EAAI,GAAK,CAEnC,KAAO,CACL,IAAIA,EAAK,CAAC,IACV,IAAK,IAAMD,KAAKM,EACdL,EAAKW,KAAK,GAAG,CAACX,EAAI,AAACI,CAAAA,CAAM,CAACL,EAAE,GAAG,CAAC,EAAI,GAAK,EAE3CK,CAAAA,CAAM,CAACL,EAAE,CAAGC,IAAO,CAAC,IAAW,EAAIA,CACrC,CACF,CAQA,OAPIA,GAAM,0BACRI,CAAAA,EAAS4J,GAAyB/J,EAAGG,EAAM,EAEzCJ,GAAM,sBACRiK,GAAuBhK,EAAGG,GAGrB,CAAE,OADMyJ,GAAyB5J,EAAGG,EAAQF,GAClCE,OAAAA,EAAQ,MAAuB,IAAIsG,GAAM,CAC5D,CAIA,SAASyD,GAAqBpK,CAAQ,CAAEC,CAAI,EAC1C,IAAMC,EAAI+H,GAAejI,GAMnBD,EAAS,CAAE,GAAG,AALPoK,GAAyBjK,EAAG,CACvC,mBAAoBD,GAAM,mBAC1B,qBAAsBA,GAAM,qBAC5B,yBAA0BA,GAAM,wBAClC,GACyB,MAAM,AAAC,EAC1BE,EAAY4I,GAAsB7I,GAClC,CAAEG,MAAAA,CAAK,CAAEC,MAAAA,CAAK,CAAE,CAAGiI,GAA8BrI,EAAG,AAACF,IACzD,GAAIC,GAAM,qBAAsB,CAC9B,IAAMA,EAAUE,EAAUH,EAAE,GAAG,EACzBE,EAAUC,EAAUH,EAAE,GAAG,EAC/B,GAAIC,GAAWC,GAAWD,IAAYC,EACpC,MAAO,EAEX,CACA,MAAO,EACT,GACMK,EAAQkI,GAAkBvI,IAAM,IAAIA,EAAE,KAAK,CAAC,CAC5CM,EAAW,IAAID,EAAM,CAAC,OAAO,GAC7BE,EAAgCL,EAAoB,CAACJ,EAAGC,KAC5D,IAAIC,EAAK,EACT,IAAK,IAAMD,KAAKI,EAAM,GAAG,CAACL,IAAM,EAAE,CAChCE,EAAKU,KAAK,GAAG,CAACV,EAAI,AAACH,CAAAA,CAAM,CAACE,EAAE,EAAI,GAAK,GAEvC,IAAIG,EAAK,IACHD,EAAIG,EAAM,GAAG,CAACN,IAAM,EAAE,CAO5B,OANIG,EAAE,MAAM,CAAG,GACbC,CAAAA,EAAKQ,KAAK,GAAG,IAAIT,EAAE,GAAG,CAAC,AAACH,GAAM,AAACD,CAAAA,CAAM,CAACC,EAAE,EAAI,GAAK,GAAE,EAEjD,AAACa,OAAO,QAAQ,CAACT,IACnBA,CAAAA,EAAKQ,KAAK,GAAG,CAACV,EAAID,EAAO,EAEpBW,KAAK,GAAG,CAACA,KAAK,GAAG,CAACX,EAASC,GAAKE,EACzC,EAAG,iBAEGM,EAA6BN,EAAoB,AAACJ,IACtD,IAAIC,EAAU,GACd,IAAK,IAAMC,KAAKF,EAAW,CACzB,IAAMA,EAAKK,EAAM,GAAG,CAACH,IAAM,EAAE,CACvBE,EAAKE,EAAM,GAAG,CAACJ,IAAM,EAAE,CAC7B,GAAIF,AAAc,IAAdA,EAAG,MAAM,EAAUI,AAAc,IAAdA,EAAG,MAAM,CAC9B,SAEF,IAEMD,EAAUS,KAAK,KAAK,CAAC,AAAC,CAFZZ,CAAAA,EAAG,MAAM,CAAG,EAAIA,EAAG,MAAM,CAAC,CAACA,EAAGC,IAAMD,EAAKD,CAAAA,CAAM,CAACE,EAAE,EAAI,GAAK,EAAG,GAAKD,EAAG,MAAM,CAAGD,CAAM,CAACG,EAAE,EAAI,GAC5FE,CAAAA,EAAG,MAAM,CAAG,EAAIA,EAAG,MAAM,CAAC,CAACJ,EAAGC,IAAMD,EAAKD,CAAAA,CAAM,CAACE,EAAE,EAAI,GAAK,EAAG,GAAKG,EAAG,MAAM,CAAGL,CAAM,CAACG,EAAE,EAAI,EAChE,EAAK,GAC3CK,EAAUE,EAAcP,EAAGC,GAC7BI,IAAYR,CAAM,CAACG,EAAE,GACvBH,CAAM,CAACG,EAAE,CAAGK,EACZN,EAAU,GAEd,CACA,OAAOA,CACT,EAAG,cACH,IAAK,IAAID,EAAK,EAAGA,EApzBG,EAozBSA,IAAM,CACjC,IAAMA,EAAiBU,EAAWH,GAC5BN,EAAkBS,EAAWF,GACnC,GAAI,CAACR,GAAkB,CAACC,EACtB,KAEJ,CACA,IAAK,IAAMD,KAAKO,EAAO,CACrB,IAAIN,EAAK,EACT,IAAK,IAAMC,KAAKG,EAAM,GAAG,CAACL,IAAM,EAAE,CAChCC,EAAKW,KAAK,GAAG,CAACX,EAAI,AAACF,CAAAA,CAAM,CAACG,EAAE,EAAI,GAAK,EAEnC,CAACH,CAAAA,CAAM,CAACC,EAAE,EAAI,GAAKC,GACrBF,CAAAA,CAAM,CAACC,EAAE,CAAGC,CAAC,CAEjB,CACA,IAAK,IAAMD,KAAKQ,EAAU,CACxB,IAAMP,EAAIK,EAAM,GAAG,CAACN,IAAM,EAAE,CAC5B,GAAIC,EAAE,MAAM,CAAG,EAAG,CAChB,IAAMC,EAAKU,KAAK,GAAG,IAAIX,EAAE,GAAG,CAAC,AAACD,GAAM,AAACD,CAAAA,CAAM,CAACC,EAAE,EAAI,GAAK,GACnD,CAACD,CAAAA,CAAM,CAACC,EAAE,EAAI,GAAKE,GACrBH,CAAAA,CAAM,CAACC,EAAE,CAAGE,CAAC,CAEjB,CACF,CAEA,MAAO,CAAE,OADMsI,GAAqBtI,EAAGK,EAAOR,GAC7BA,OAAAA,EAAQ,MAAuB,IAAI4G,GAAM,CAC5D,CAIA,SAAS0D,GAA8BrK,CAAC,EACtC,IAAMC,EAAQoI,GAAiBrI,GACzBE,EAAMkI,GAAwBpI,GAChCI,EAAWkI,GAAwBrI,GACjCF,EAAQ,EAAE,CAChB,KAAOK,EAAS,MAAM,CAAG,GAAG,CAC1B,IAAMJ,EAAe,EAAE,CACvB,IAAK,IAAMG,KAAKC,EAEd,IAAK,IAAMA,KADXL,EAAM,IAAI,CAACI,GACKD,EAAI,GAAG,CAACC,IAAM,EAAE,EAC9BF,EAAM,GAAG,CAACG,EAAG,AAACH,CAAAA,EAAM,GAAG,CAACG,IAAM,GAAK,GAC/B,AAACH,CAAAA,EAAM,GAAG,CAACG,IAAM,KAAO,GAC1BJ,EAAa,IAAI,CAACI,GAIxBA,EAAWJ,EAAa,IAAI,CAAC,CAACA,EAAGC,IAAMD,EAAE,aAAa,CAACC,GACzD,CACA,OAAOF,EAAM,MAAM,GAAKC,EAAE,KAAK,CAAC,MAAM,CAAGD,EAAQ,IACnD,CAEA,SAASuK,GAA8BtK,CAAQ,CAAEC,CAAI,EACnD,IAAMC,EAAI+H,GAAejI,GACnBD,EAAQE,GAAM,YAAc,KAAOoK,GAA8BnK,IAAM,IAAIA,EAAE,KAAK,CAAC,CAAC,IAAI,GAAKuI,GAAkBvI,IAAM,IAAIA,EAAE,KAAK,CAAC,CAAC,IAAI,GACtIC,EAAY4I,GAAsB7I,GAClCG,EAAyBD,EAAoB,AAACJ,GAAOG,EAAUH,IAAOA,EAAI,UAC1EM,EAAyBuI,OAAO,MAAM,CAAC,MACvCtI,EAA2B,IAAIU,IAC/BT,EAA6BJ,EAAoB,CAACJ,EAAGE,IAEzD,AADwBD,GAAM,sBAAwB,GAE7CI,CAAAA,CAAAA,EAAOL,KAAOK,EAAOH,EAAC,EAExB,EACN,cACH,IAAK,IAAMF,KAAKD,EAAO,CACrB,IAAME,EAAOC,EAAE,QAAQ,CAAC,GAAG,CAACF,GAC5B,GAAIC,GAAM,QACR,SAEF,IAAMG,EAAQ8H,GAAShI,EAAGF,GACtBD,EAAO,EACX,GAAIK,EAAM,MAAM,CAAG,EACjB,IAAK,IAAMH,KAAKG,EAAO,CACrB,IAAMF,EAAID,EAAE,GAAG,CAEfF,EAAOa,KAAK,GAAG,CAACb,EAAM,AADXO,CAAAA,CAAM,CAACJ,EAAE,EAAI,GACGM,EAAWN,EAAGF,GAC3C,CAEF,IAAMG,EAAOE,EAAOL,GAEdS,EAAIG,KAAK,GAAG,CAACb,EADRQ,EAAS,GAAG,CAACJ,IAAS,EAEjCG,CAAAA,CAAM,CAACN,EAAE,CAAGS,EACZF,EAAS,GAAG,CAACJ,EAAMM,EAAI,EACzB,CAEA,MAAO,CAAE,OADM+H,GAAqBtI,EAAGH,EAAOO,EAAQ,CAAE,WAAY,EAAK,GACxDA,OAAAA,EAAQ,MAAuB,IAAIqG,GAAM,CAC5D,CAIA,SAAS4D,GAAmBvK,CAAQ,CAAEC,CAAQ,EAC5C,IAAMC,EAAI+H,GAAehI,GACnB,CAAEF,OAAAA,CAAM,CAAE,CAAGC,EACbG,EAASH,EAAS,MAAM,CAAC,GAAG,CAAC,AAACA,GAAM,IAAIA,EAAE,EAC1CK,EAAQ,IAAIsG,IAAI3G,EAAS,KAAK,CAAG,IAAIA,EAAS,KAAK,CAAC,CAAG,EAAE,EAC3DM,EAAW,EACTC,EAAW,IAAIU,IAAIf,EAAE,QAAQ,EAC7BM,EAA6BJ,EAAoB,AAACJ,IACtD,IAAMC,EAAK,CAAC,YAAY,EAAEK,IAAW,CAAC,CAItC,IAFAC,EAAS,GAAG,CAACN,EADF,CAAEA,GAAAA,EAAI,QAAS,GAAO,QAAS,GAAM,MAAO,EAAG,OAAQ,CAAE,GAEpEI,EAAM,GAAG,CAACJ,GACHE,EAAO,MAAM,EAAIH,GACtBG,EAAO,IAAI,CAAC,EAAE,EAIhB,OAFAA,CAAM,CAACH,EAAE,CAAC,IAAI,CAACC,GACfF,CAAM,CAACE,EAAG,CAAGD,EACNC,CACT,EAAG,cACGQ,EAAc,IAAIP,EAAE,KAAK,CAAC,CAAC,IAAI,CACnC,CAACF,EAAGC,IAAMD,EAAE,EAAE,GAAKC,EAAE,EAAE,CAAGD,EAAE,GAAG,GAAKC,EAAE,GAAG,CAAGD,EAAE,GAAG,CAAC,aAAa,CAACC,EAAE,GAAG,EAAID,EAAE,GAAG,CAAC,aAAa,CAACC,EAAE,GAAG,EAAID,EAAE,EAAE,CAAC,aAAa,CAACC,EAAE,EAAE,GAEzHS,EAAW,EAAE,CACnB,IAAK,IAAMV,KAAKS,EAAa,CAC3B,IAAMR,EAAKF,CAAM,CAACC,EAAE,GAAG,CAAC,EAAI,EACtBE,EAAKH,CAAM,CAACC,EAAE,GAAG,CAAC,EAAI,EAC5B,GAAIE,EAAKD,GAAM,EAAG,CAChBS,EAAS,IAAI,CAACV,GACd,QACF,CACA,IAAII,EAAOJ,EAAE,GAAG,CAChB,IAAK,IAAID,EAAIE,EAAK,EAAGE,EAAI,EAAGJ,EAAIG,EAAIH,IAAKI,IAAK,CAC5C,IAAMF,EAAIO,EAAWT,GACrBW,EAAS,IAAI,CAAC,CAAE,GAAI,CAAC,EAAEV,EAAE,EAAE,CAAC,CAAC,EAAEG,EAAE,CAAC,CAAE,IAAKC,EAAM,IAAKH,EAAG,OAAQD,EAAE,MAAM,CAAE,IAAKA,EAAE,GAAG,AAAC,GACpFI,EAAOH,CACT,CACA,IAAME,EAAYD,EAAKD,EAAK,EAC5BS,EAAS,IAAI,CAAC,CACZ,GAAI,CAAC,EAAEV,EAAE,EAAE,CAAC,CAAC,EAAEY,KAAK,GAAG,CAACT,EAAY,EAAG,GAAG,CAAC,CAC3C,IAAKC,EACL,IAAKJ,EAAE,GAAG,CACV,OAAQA,EAAE,MAAM,CAChB,IAAKA,EAAE,GAAG,AACZ,EACF,CAEA,IAAMW,EAAmB,CAAE,MADb,IAAIT,EAAE,KAAK,IAAK,IAAIG,EAAM,CAAC,MAAM,CAAC,AAACL,GAAO,CAACE,EAAE,KAAK,CAAC,QAAQ,CAACF,IAAK,CAC7C,MAAOU,EAAU,OAAQR,EAAE,MAAM,CAAEK,SAAAA,CAAS,EAC9E,MAAO,CAAE,SAAU,CAAEJ,OAAAA,EAAQJ,OAAAA,EAAQM,MAAAA,CAAM,EAAGM,iBAAAA,CAAiB,CACjE,CAIA,SAAS6J,GAAOxK,CAAM,EACpB,IAAMC,EAAID,EAAO,MAAM,CACvB,GAAIC,AAAM,IAANA,EACF,OAAO,IAET,IAAMC,EAAI,IAAIF,EAAO,CAAC,IAAI,CAAC,CAACA,EAAGC,IAAMD,EAAIC,UACzC,AAAIA,EAAI,GAAM,EACLC,CAAC,CAAC,AAACD,CAAAA,EAAI,GAAK,EAAE,CAEhB,GAAOC,CAAAA,CAAC,CAACD,EAAI,EAAI,EAAE,CAAGC,CAAC,CAACD,EAAI,EAAE,AAAD,CACtC,CAEA,SAASwK,GAAWzK,CAAM,SACxB,AAAIA,AAAkB,IAAlBA,EAAO,MAAM,CACR,IAGF,AADGA,EAAO,MAAM,CAAC,CAACA,EAAKC,IAAMD,EAAMC,EAAG,GAClCD,EAAO,MAAM,AAC1B,CAEA,SAAS0K,GAAqB1K,CAAW,CAAEC,CAAU,CAAEC,CAAK,CAAEE,CAAS,EACrE,IAAML,EAAoC,IAAIkB,IAC9C,IAAK,IAAMhB,KAAKD,EACdD,EAAkB,GAAG,CAACE,EAAG,EAAE,EAE7B,IAAK,IAAMD,KAAKE,EACVE,AAAc,SAAdA,EACEH,EAAW,GAAG,CAACD,EAAE,GAAG,GAAKD,EAAkB,GAAG,CAACC,EAAE,GAAG,GACtDD,EAAkB,GAAG,CAACC,EAAE,GAAG,EAAE,IAAI,CAACC,EAAW,GAAG,CAACD,EAAE,GAAG,GAE/CC,EAAW,GAAG,CAACD,EAAE,GAAG,GAAKD,EAAkB,GAAG,CAACC,EAAE,GAAG,GAC7DD,EAAkB,GAAG,CAACC,EAAE,GAAG,EAAE,IAAI,CAACC,EAAW,GAAG,CAACD,EAAE,GAAG,GAG1D,OAAOD,CACT,CAEA,SAAS4K,GAAqB3K,CAAC,CAAEC,CAAC,CAAEC,CAAiB,EACnD,IAAME,EAAKF,EAAkB,GAAG,CAACF,IAAM,EACjCD,EAAKG,EAAkB,GAAG,CAACD,IAAM,EACvC,OAAOG,IAAOL,EAAKK,EAAKL,EAAKC,EAAE,aAAa,CAACC,EAC/C,CAEA,SAAS2K,GAA+B5K,CAAK,CAAEC,CAAK,CAAEC,CAAK,EACzD,IAAME,EAAW,IAAIuG,IAAI3G,GACnBD,EAAW,IAAI4G,IAAI1G,GACnBE,EAAauI,GAAgB1I,GAC7BK,EAAaqI,GAAgBzI,GAC7BK,EAAQ,EAAE,CAChB,IAAK,IAAMN,KAAKE,EACVE,EAAS,GAAG,CAACJ,EAAE,GAAG,GAAKD,EAAS,GAAG,CAACC,EAAE,GAAG,GAC3CM,EAAM,IAAI,CAAC,CAAE,EAAGH,EAAW,GAAG,CAACH,EAAE,GAAG,EAAG,EAAGK,EAAW,GAAG,CAACL,EAAE,GAAG,CAAE,GAKpE,OAFAM,EAAM,IAAI,CAAC,CAACN,EAAGC,IAAMD,EAAE,CAAC,GAAKC,EAAE,CAAC,CAAGD,EAAE,CAAC,CAAGC,EAAE,CAAC,CAAGD,EAAE,CAAC,CAAGC,EAAE,CAAC,EAEjD0I,GADIrI,EAAM,GAAG,CAAC,AAACN,GAAMA,EAAE,CAAC,EAEjC,CAEA,SAAS6K,GAAgB7K,CAAK,CAAEC,CAAiB,CAAEC,CAAiB,EAClE,MAAO,IAAIF,EAAM,CAAC,IAAI,CAAC,CAACA,EAAGI,KACzB,IAAML,EAAKyK,GAAOvK,EAAkB,GAAG,CAACD,IAAM,EAAE,EAC1CG,EAAKqK,GAAOvK,EAAkB,GAAG,CAACG,IAAM,EAAE,SAChD,AAAIL,IAAOI,EACFwK,GAAqB3K,EAAGI,EAAGF,GAE/B4K,SAAS/K,GAGT+K,SAAS3K,GAGPJ,EAAKI,EAFH,GAHA,CAMX,EACF,CAEA,SAAS4K,GAAa/K,CAAU,CAAEC,CAAW,CAAEC,CAAK,CAAEE,CAAS,CAAEL,CAAS,CAAEI,CAAS,EACnF,IAAME,EAAaqI,GAAgB1I,GAC7BM,EAAYoI,GAAgBzI,GAC5BM,EAAoBmK,GAAqBzK,EAAaI,EAAYH,EAAOE,GAC/E,GAAI,CAACL,GAAa,CAACI,GAAaA,AAAqB,IAArBA,EAAU,MAAM,CAC9C,OAAO0K,GAAgB5K,EAAaM,EAAmBD,GAEzD,IAAME,EAAyB,IAAIS,IACnC,IAAK,IAAMjB,KAAMC,EAAa,CAC5B,IAAMA,EAAOF,EAAUC,GACjBE,EAAMM,EAAO,GAAG,CAACP,IAAS,EAAE,CAClCC,EAAI,IAAI,CAACF,GACTQ,EAAO,GAAG,CAACP,EAAMC,EACnB,CACA,IAAMO,EAAS,EAAE,CACjB,IAAK,IAAMT,KAAQG,EAAW,CAC5B,IAAMF,EAAcO,EAAO,GAAG,CAACR,GAC/B,GAAI,CAACC,GAAeA,AAAuB,IAAvBA,EAAY,MAAM,CACpC,SAEF,IAAMC,EAAS2K,GAAgB5K,EAAaM,EAAmBD,GAC/DG,EAAO,IAAI,IAAIP,EACjB,CACA,IAAMQ,EAAYF,EAAO,GAAG,CAAC,MAC7B,GAAIE,GAAaA,EAAU,MAAM,CAAG,EAElC,IAAK,IAAMV,KADI6K,GAAgBnK,EAAWH,EAAmBD,GACnC,CACxB,IAAML,EAAKwK,GAAWlK,EAAkB,GAAG,CAACP,IAAQ,EAAE,EAClDE,EAAUO,EAAO,MAAM,CAC3B,GAAIqK,SAAS7K,GACX,KAAK,GAAM,CAACD,EAAGI,EAAI,GAAIK,EAAO,OAAO,GAEnC,GAAIR,EADQwK,GAAWlK,EAAkB,GAAG,CAACH,IAAQ,EAAE,EACzC,CACZF,EAAUF,EACV,KACF,CACF,CAEFS,EAAO,MAAM,CAACP,EAAS,EAAGF,EAC5B,CAEF,OAAOS,CACT,CAEA,SAASuK,GAAiBhL,CAAK,CAAEC,CAAO,CAAEC,CAAK,CAAEH,CAAI,CAAEI,CAAS,EAC9D,IAAME,EAAO,IAAIJ,EAAQ,CACnBK,EAAW,IAAIqG,IAAI3G,GACnBO,EAAW,IAAIoG,IAAI1G,GACnBO,EAAUT,EAAO,IAAI4G,IAAI5G,GAAQ,KACjCU,EAAUP,EAAM,MAAM,CAAC,AAACF,GAAMM,EAAS,GAAG,CAACN,EAAE,GAAG,GAAKO,EAAS,GAAG,CAACP,EAAE,GAAG,GACvEU,EAAWF,EAAUN,EAAM,MAAM,CAAC,AAACF,GAAMO,EAAS,GAAG,CAACP,EAAE,GAAG,GAAKQ,EAAQ,GAAG,CAACR,EAAE,GAAG,GAAK,KAAK,EAC3FW,EAAgCP,EAAoB,AAACH,IACzD,IAAIC,EAAQ0K,GAA+B5K,EAAOC,EAAOQ,GAIzD,OAHIC,GAAYX,GACdG,CAAAA,GAAS0K,GAA+B3K,EAAOF,EAAMW,EAAQ,EAExDR,CACT,EAAG,iBACGY,EAASX,EAA4B,IAAIc,IAAQ,KACvD,GAAId,GAAaW,EACf,IAAK,IAAMd,KAAMC,EACfa,EAAO,GAAG,CAACd,EAAIG,EAAUH,IAG7B,IAAIe,EAAW,GACXC,EAAYL,EAAcN,GAC9B,KAAOU,GAAU,CACfA,EAAW,GACX,IAAK,IAAIf,EAAI,EAAGA,EAAI,EAAIK,EAAK,MAAM,CAAEL,IAAK,CACxC,GAAIc,GAGE,AAFUA,EAAO,GAAG,CAACT,CAAI,CAACL,EAAE,IAClBc,EAAO,GAAG,CAACT,CAAI,CAACL,EAAI,EAAE,EAElC,SAGJ,IAAMC,EAAOe,CACb,EAACX,CAAI,CAACL,EAAE,CAAEK,CAAI,CAACL,EAAI,EAAE,CAAC,CAAG,CAACK,CAAI,CAACL,EAAI,EAAE,CAAEK,CAAI,CAACL,EAAE,CAAC,CAC/C,IAAME,EAAYS,EAAcN,EAC5BH,CAAAA,EAAYD,GACde,EAAYd,EACZa,EAAW,IAEX,CAACV,CAAI,CAACL,EAAE,CAAEK,CAAI,CAACL,EAAI,EAAE,CAAC,CAAG,CAACK,CAAI,CAACL,EAAI,EAAE,CAAEK,CAAI,CAACL,EAAE,CAAC,AAEnD,CACF,CACA,OAAOK,CACT,CAEA,SAAS4K,GAAYjL,CAAQ,CAAEC,CAAY,CAAEC,CAAI,EAC/C,IAAME,EAASJ,EAAS,MAAM,CAAC,GAAG,CAAC,AAACA,GAAM,IAAIA,EAAE,EAC1CD,EAAQE,EAAa,KAAK,CAC1BE,EAAY4I,GAAsB9I,GAClCI,EAAY4I,GAAoBhJ,EAAcC,GAAM,WAC1D,IAAK,IAAIF,EAAI,EAAGA,EAAI,EAAGA,IAAK,CAC1B,IAAK,IAAIA,EAAI,EAAGA,EAAII,EAAO,MAAM,CAAEJ,IACjCI,CAAM,CAACJ,EAAE,CAAG+K,GAAa3K,CAAM,CAACJ,EAAI,EAAE,CAAEI,CAAM,CAACJ,EAAE,CAAED,EAAO,OAAQI,EAAWE,GAC7ED,CAAM,CAACJ,EAAE,CAAGgL,GAAiB5K,CAAM,CAACJ,EAAI,EAAE,CAAEI,CAAM,CAACJ,EAAE,CAAED,EAAOK,CAAM,CAACJ,EAAI,EAAE,CAAEG,GAE/E,IAAK,IAAIH,EAAII,EAAO,MAAM,CAAG,EAAGJ,GAAK,EAAGA,IACtCI,CAAM,CAACJ,EAAE,CAAG+K,GAAa3K,CAAM,CAACJ,EAAI,EAAE,CAAEI,CAAM,CAACJ,EAAE,CAAED,EAAO,KAAMI,EAAWE,GAC3ED,CAAM,CAACJ,EAAE,CAAGgL,GAAiB5K,CAAM,CAACJ,EAAI,EAAE,CAAEI,CAAM,CAACJ,EAAE,CAAED,EAAOK,CAAM,CAACJ,EAAI,EAAE,CAAEG,EAEjF,CACA,MAAO,CAAEC,OAAAA,CAAO,CAClB,CAIA,SAAS8K,GAAkBlL,CAAO,CAAEC,CAAY,CAAEC,CAAI,EACpD,IAAMH,EAAWG,GAAM,UAvnCJ,IAwnCbC,EAAUD,GAAM,SAtnCJ,GAunCZG,EAAUH,GAAM,SAAWC,AAAU,EAAVA,EAC3BG,EAAYJ,GAAM,WAAa,KAE/BK,EAASP,EAAQ,MAAM,CACvBQ,EAAoBqI,OAAO,MAAM,CAAC,MAClCpI,EAAoBoI,OAAO,MAAM,CAAC,MAClCnI,EAA0BN,EAAoB,AAACJ,GAAOC,EAAa,QAAQ,CAAC,GAAG,CAACD,GAAK,WACrFW,EAA2BP,EAAoB,AAACJ,GAAOU,EAAQV,IAAK,OAAS,EAAG,YAChFc,EAA4BV,EAAoB,AAACJ,GAAOU,EAAQV,IAAK,QAAU,EAAG,aAClFe,EAAYgI,GAAsB9I,GAClCe,EAAkBiI,GAAoBhJ,EAAcC,GAAM,WAC1DgB,EAAeX,EAAO,GAAG,CAC7B,AAACP,GAAUA,EAAM,MAAM,CAAC,CAACA,EAAGC,IAAMW,KAAK,GAAG,CAACZ,EAAGc,EAAUb,IAAK,IAEzDkB,EAAiB,EAAE,CACzB,GAbqBb,AAAc,OAAdA,GAAsBA,AAAc,OAAdA,EAczC,IAAK,IAAIN,EAAI,EAAGA,EAAI,EAAIO,EAAO,MAAM,CAAEP,IAAK,CAC1C,IAMMC,EAAcW,KAAK,GAAG,CAAC,EAAG,AADP,CALCL,CAAM,CAACP,EAAE,CAAC,MAAM,CAAC,CAACA,EAAGC,IAAMW,KAAK,GAAG,CAACZ,EAAGW,EAASV,IAAK,GACrDM,CAAM,CAACP,EAAI,EAAE,CAAC,MAAM,CAAC,CAACA,EAAGC,IAAMW,KAAK,GAAG,CAACZ,EAAGW,EAASV,IAAK,EAItB,EAAK,EAD5C,CAFKiB,CAAY,CAAClB,EAAE,CAEC,EAAI,AADpBkB,CAAY,CAAClB,EAAI,EAAE,CACsB,GAEFD,GAClEoB,EAAe,IAAI,CAAClB,EACtB,CAEF,IAAMmB,EAA+B,IAAIuF,IACzC,IAAK,IAAM3G,KAASO,EAClB,IAAK,IAAMN,KAAMD,EACfoB,EAAa,GAAG,CAACL,EAAUd,IAG/B,IAAMoB,EAAcD,EAAa,GAAG,CAAC,MAC/BE,EAAYN,EAAgB,MAAM,CAAC,AAAChB,GAAMoB,EAAa,GAAG,CAACpB,IAC3D4B,EAAmB,IAAIP,EAAc,CAAC,KAAK,CAAG,EAAE,IAAKC,EAAU,CAC/DO,EAA4BgH,OAAO,MAAM,CAAC,MAChD,IAAK,IAAM7I,KAAKsB,EACdO,CAAS,CAAC7B,EAAE,CAAG,EAKjB,IAAK,IAAMA,KAHPqB,GACFQ,CAAAA,EAAU,IAAI,CAAG,GAECtB,GAAQ,CAC1B,IAAMN,EAA0B4I,OAAO,MAAM,CAAC,MACxC3I,EAAU,EAAE,CAClB,IAAK,IAAME,KAAMJ,EAAO,CACtB,IAAMA,EAAIe,EAAUX,EAChBJ,AAAM,QAANA,EACFE,EAAQ,IAAI,CAACE,GAEb,AAACH,CAAAA,CAAO,CAACD,EAAE,GAAK,EAAE,AAAD,EAAG,IAAI,CAACI,EAE7B,CACA,IAAK,GAAM,CAACJ,EAAGE,EAAI,GAAI2I,OAAO,OAAO,CAAC5I,GAAU,CAC9C,IAAMA,EAAQC,EAAI,MAAM,CAAC,CAACF,EAAGC,IAAOD,EAAIW,EAASV,GAAK,GAAKE,EAAUS,KAAK,GAAG,CAAC,EAAGV,EAAI,MAAM,CAAG,EAC9F2B,CAAAA,CAAS,CAAC7B,EAAE,CAAGY,KAAK,GAAG,CAACiB,CAAS,CAAC7B,EAAE,EAAI,EAAGC,EAC7C,CACA,GAAIoB,GAAenB,EAAQ,MAAM,CAAE,CACjC,IAAMF,EAAYE,EAAQ,MAAM,CAAC,CAACF,EAAGC,IAAOD,EAAIW,EAASV,GAAK,GAAKE,EAAUS,KAAK,GAAG,CAAC,EAAGV,EAAQ,MAAM,CAAG,EAC1G2B,CAAAA,EAAU,IAAI,CAAGjB,KAAK,GAAG,CAACiB,EAAU,IAAI,EAAI,EAAG7B,EACjD,CACF,CACA,IAAM8B,EAA0B,IAAIb,GACpC,EACE,IAAMjB,EAAS4B,EAAiB,GAAG,CACjC,AAAC5B,GAAM,AAACA,CAAAA,AAAM,OAANA,EAAa6B,EAAU,IAAI,CAAGA,CAAS,CAAC7B,EAAE,AAAD,GAAM,GAGrDC,EAAS,CADED,CAAAA,EAAO,MAAM,CAAC,CAACA,EAAGC,IAAMD,EAAIC,EAAG,GAAKI,EAAUO,KAAK,GAAG,CAAC,EAAGgB,EAAiB,MAAM,CAAG,EAAC,EAC7E,EACvB,IAAK,IAAI1B,EAAI,EAAGA,EAAI0B,EAAiB,MAAM,CAAE1B,IAAK,CAChD,IAAME,EAAIwB,CAAgB,CAAC1B,EAAE,CACvBH,EAAIC,CAAM,CAACE,EAAE,EAAI,EACjBC,EAAKF,EAASF,EAAI,EACxB+B,EAAQ,GAAG,CAAC1B,EAAGD,GACfF,GAAUF,EACNG,EAAI0B,EAAiB,MAAM,CAAG,GAChC3B,CAAAA,GAAUI,CAAM,CAEpB,CACF,CACA,IAAI0B,EAAU,EACd,IAAK,GAAM,CAAC/B,EAAIC,EAAM,GAAIM,EAAO,OAAO,GAAI,CAC1C,IAAML,EAASgB,CAAY,CAAClB,EAAG,EAAI,EAC7BI,EAAyB,IAAIa,IACnC,IAAK,IAAMjB,KAAMC,EAAO,CACtB,IAAMA,EAASc,EAAUf,GACnBE,EAAME,EAAO,GAAG,CAACH,IAAW,EAAE,CACpCC,EAAI,IAAI,CAACF,GACTI,EAAO,GAAG,CAACH,EAAQC,EACrB,CACA,IAAK,IAAMF,KAAK4B,EAAkB,CAChC,IAAM3B,EAAcG,EAAO,GAAG,CAACJ,IAAM,EAAE,CACvC,GAAIC,AAAuB,IAAvBA,EAAY,MAAM,CACpB,SAEF,IAAMF,EAAK+B,EAAQ,GAAG,CAAC9B,GACvB,GAAIC,AAAuB,IAAvBA,EAAY,MAAM,CAAQ,CAC5B,IAAMD,EAAKC,CAAW,CAAC,EAAE,AACzBO,CAAAA,CAAC,CAACR,EAAG,CAAGD,EACRU,CAAC,CAACT,EAAG,CAAG+B,EAAU7B,EAAS,CAC7B,KAAO,CACL,IAAMF,EAASC,EAAY,GAAG,CAAC,AAACD,GAAOW,EAASX,IAE5CI,EAAQL,EAAK,AADHC,CAAAA,EAAO,MAAM,CAAC,CAACA,EAAGC,IAAMD,EAAIC,EAAG,GAAKE,EAAWF,CAAAA,EAAY,MAAM,CAAG,EAAC,EAC1D,EACzB,IAAK,GAAM,CAACF,EAAGM,EAAG,GAAIJ,EAAY,OAAO,GAAI,CAC3C,IAAMA,EAAID,CAAM,CAACD,EAAE,AACnBS,CAAAA,CAAC,CAACH,EAAG,CAAGD,EAAQH,EAAI,EACpBQ,CAAC,CAACJ,EAAG,CAAG0B,EAAU7B,EAAS,EAC3BE,GAASH,EAAIE,CACf,CACF,CACF,CAEA4B,GAAW7B,EAASH,EADHoB,CAAAA,CAAc,CAACnB,EAAG,EAAI,EAEzC,CACA,IAAMgC,EAAwB,IAAIf,IAClC,IAAK,IAAMjB,KAAKC,EAAa,KAAK,CAAE,CAClC,IAAMA,EAAMD,EAAE,GAAG,CAAC,EAAE,AAChB,CAACgC,EAAM,GAAG,CAAC/B,IACb+B,EAAM,GAAG,CAAC/B,EAAK,EAAE,EAEnB+B,EAAM,GAAG,CAAC/B,GAAK,IAAI,CAACD,EACtB,CACA,IAAK,GAAM,EAAGA,EAAW,GAAIgC,EAAO,CAClC,GAAIhC,AAAsB,IAAtBA,EAAW,MAAM,CACnB,SAEF,IAAME,EAAMF,CAAU,CAAC,EAAE,CAAC,GAAG,CACvBI,EAAMF,EAAI,KAAK,CACfH,EAAMG,EAAI,GAAG,CACnB,GAAIE,AAAO,MAAPA,GAAeL,AAAO,MAAPA,EACjB,SAEF,IAAMI,EAAOS,KAAK,KAAK,CAAC,AAAC,CAACJ,CAAAA,CAAC,CAACJ,EAAI,EAAI,GAAMI,CAAAA,CAAC,CAACT,EAAI,EAAI,EAAC,EAAK,GACpDM,EAA2B,IAAIsG,IACrC,IAAK,IAAM1G,KAAKD,EACdK,EAAS,GAAG,CAACJ,EAAE,GAAG,EAClBI,EAAS,GAAG,CAACJ,EAAE,GAAG,EAEpB,IAAK,IAAMD,KAAOK,EAAU,CAC1B,GAAIL,IAAQI,GAAOJ,IAAQD,EACzB,SAEF,IAAMG,EAAOD,EAAa,QAAQ,CAAC,GAAG,CAACD,EACnCE,CAAAA,GAAM,SACRM,CAAAA,CAAC,CAACR,EAAI,CAAGG,CAAG,CAEhB,CACF,CACA,MAAO,CAAEK,EAAAA,EAAGC,EAAAA,CAAE,CAChB,CAKA,SAAS0K,GAAWnL,CAAK,EACvB,IAAIC,EAAO,WACX,IAAK,IAAIC,EAAI,EAAGA,EAAIF,EAAM,MAAM,CAAEE,IAChCD,GAAQD,EAAM,UAAU,CAACE,GACzBD,EAAOW,KAAK,IAAI,CAACX,EAAM,WAEzB,OAAOA,IAAS,CAClB,CAEA,SAASmL,GAAWpL,CAAI,EACtB,IAAIC,EAAQD,IAAS,EACrB,MAAO,KAEL,IAAIA,EADJC,GAAS,WAIT,OAFAD,EAAIY,KAAK,IAAI,CAACZ,EAAIA,IAAM,GAAIA,AAAI,EAAJA,GAErB,AAAC,CAAC,CADTA,CAAAA,GAAKA,EAAIY,KAAK,IAAI,CAACZ,EAAIA,IAAM,EAAGA,AAAI,GAAJA,EAAM,EACzBA,IAAM,EAAC,IAAO,GAAK,WAClC,CACF,CAEA,SAASqL,GAAqBrL,CAAK,CAAEC,CAAI,EACvC,IAAMC,EAAW,IAAIF,EAAM,CACrBI,EAASgL,GAAWnL,GAC1B,IAAK,IAAID,EAAIE,EAAS,MAAM,CAAG,EAAGF,EAAI,EAAGA,IAAK,CAC5C,IAAMC,EAAIW,KAAK,KAAK,CAACR,IAAYJ,CAAAA,EAAI,GACrC,EAACE,CAAQ,CAACF,EAAE,CAAEE,CAAQ,CAACD,EAAE,CAAC,CAAG,CAACC,CAAQ,CAACD,EAAE,CAAEC,CAAQ,CAACF,EAAE,CAAC,AACzD,CACA,OAAOE,CACT,CAEA,SAASoL,GAAetL,CAAK,CAAEC,CAAW,EACxC,IAAIC,EAAW,EACf,IAAK,GAAM,CAACE,EAAOL,EAAO,GAAIC,EAAM,OAAO,GACzCE,GAAYU,KAAK,GAAG,CAACR,EAASH,CAAAA,EAAY,GAAG,CAACF,IAAWK,CAAI,GAE/D,OAAOF,CACT,CAEA,SAASqL,GAAoBvL,CAAK,CAAEC,CAAO,EACzC,IAAMC,EAA2B,IAAIe,IACrC,IAAK,GAAM,CAAChB,EAAOG,EAAO,GAAIJ,EAAM,OAAO,GACzCE,EAAS,GAAG,CAACE,EAAQH,GAEvB,IAAIG,EAAO,EACX,IAAK,GAAM,CAAEJ,EAAAA,CAAC,CAAED,EAAAA,CAAC,CAAEI,OAAAA,CAAM,CAAE,GAAIF,EAAS,CACtC,IAAMA,EAAKC,EAAS,GAAG,CAACF,GAClBK,EAAKH,EAAS,GAAG,CAACH,EACd,OAANE,GAAcI,AAAM,MAANA,GAGlBD,CAAAA,GAAQD,EAASS,KAAK,GAAG,CAACX,EAAKI,EAAE,CACnC,CACA,OAAOD,CACT,CAEA,SAASoL,GAAuBxL,CAAC,EAC/B,IAAMC,EAAc+I,GAAkBhJ,GACtC,GAAIC,EAAY,MAAM,CAAG,EACvB,MAAO,EAAE,CAEX,IAAMC,EAAc,IAAIe,IAAIhB,EAAY,GAAG,CAAC,CAACD,EAAQC,IAAU,CAACD,EAAQC,EAAM,GACxEG,EAAY2I,GAAsB/I,GAClCD,EAA0B,IAAIkB,IACpC,IAAK,IAAMhB,KAAQD,EAAE,MAAM,CAAC,KAAK,EAAI,EAAE,CAAE,CACvC,GAAIC,EAAK,YAAY,CACnB,SAEF,IAAME,EAAM,AAAsB,UAAtB,OAAOF,EAAK,KAAK,CAAgBA,EAAK,KAAK,CAAG,KAAK,EACzDI,EAAM,AAAoB,UAApB,OAAOJ,EAAK,GAAG,CAAgBA,EAAK,GAAG,CAAG,KAAK,EAC3D,GAAI,CAACE,GAAO,CAACE,GAAO,CAACL,EAAE,QAAQ,CAAC,GAAG,CAACG,IAAQ,CAACH,EAAE,QAAQ,CAAC,GAAG,CAACK,GAC1D,SAEF,IAAMC,EAAQF,EAAUD,GAClBI,EAAQH,EAAUC,GACxB,GAAI,CAACC,GAAS,CAACC,GAASD,IAAUC,EAChC,SAEF,IAAMC,EAAKN,EAAY,GAAG,CAACI,GACrBG,EAAKP,EAAY,GAAG,CAACK,GAC3B,GAAIC,AAAM,MAANA,GAAcC,AAAM,MAANA,EAChB,SAEF,GAAM,CAACC,EAAGC,EAAE,CAAGH,GAAMC,EAAK,CAACH,EAAOC,EAAM,CAAG,CAACA,EAAOD,EAAM,CACnDQ,EAAM,CAAC,EAAEJ,EAAE,EAAE,EAAEC,EAAE,CAAC,CAClBI,EAAWhB,EAAQ,GAAG,CAACe,GACzBC,EACFA,EAAS,MAAM,GAEfhB,EAAQ,GAAG,CAACe,EAAK,CAAEJ,EAAAA,EAAGC,EAAAA,EAAG,OAAQ,CAAE,EAEvC,CACA,MAAO,IAAIZ,EAAQ,MAAM,GAAG,AAC9B,CAEA,SAAS0L,GAAazL,CAAU,CAAEC,CAAO,CAAEC,CAAW,EACpD,IAAME,EAAQ,IAAIJ,EAAW,CACzBD,EAAOwL,GAAoBnL,EAAOH,GAClCE,EAAU,GACVE,EAAS,EACPC,EAAYM,KAAK,GAAG,CAAC,EAAGR,EAAM,MAAM,EAC1C,KAAOD,GAAWE,EAASC,GAAW,CACpCH,EAAU,GACVE,IACA,IAAK,IAAIL,EAAI,EAAGA,EAAI,EAAII,EAAM,MAAM,CAAEJ,IAAK,CACzC,CAACI,CAAK,CAACJ,EAAE,CAAEI,CAAK,CAACJ,EAAI,EAAE,CAAC,CAAG,CAACI,CAAK,CAACJ,EAAI,EAAE,CAAEI,CAAK,CAACJ,EAAE,CAAC,CACnD,IAAME,EAAWqL,GAAoBnL,EAAOH,EACxCC,CAAAA,EAAWH,GACbA,EAAOG,EACPC,EAAU,IAEV,CAACC,CAAK,CAACJ,EAAE,CAAEI,CAAK,CAACJ,EAAI,EAAE,CAAC,CAAG,CAACI,CAAK,CAACJ,EAAI,EAAE,CAAEI,CAAK,CAACJ,EAAE,CAAC,AAEvD,CACF,CACA,MAAO,CACLI,MAAAA,EACAL,KAAAA,EACA,eAAgBuL,GAAelL,EAAOF,EACxC,CACF,CAEA,SAASwL,GAAkB1L,CAAS,CAAEC,CAAI,SACxC,AAAID,EAAU,IAAI,GAAKC,EAAK,IAAI,CACvBD,EAAU,IAAI,CAAGC,EAAK,IAAI,CAE5BD,EAAU,cAAc,CAAGC,EAAK,cAAc,AACvD,CAEA,SAAS0L,GAAe3L,CAAW,CAAEC,CAAO,CAAEC,CAAY,EACxD,IAAME,EAAkB,IAAIH,EAAQ,CAAC,IAAI,CAAC,CAACD,EAAGC,IAAMD,EAAE,CAAC,GAAKC,EAAE,CAAC,CAAGD,EAAE,CAAC,CAAC,aAAa,CAACC,EAAE,CAAC,EAAID,EAAE,CAAC,CAAC,aAAa,CAACC,EAAE,CAAC,GAAG,GAAG,CAAC,CAAC,CAAED,EAAAA,CAAC,CAAEC,EAAAA,CAAC,CAAEC,OAAAA,CAAM,CAAE,GAAK,CAAC,EAAEF,EAAE,CAAC,EAAEC,EAAE,CAAC,EAAEC,EAAO,CAAC,EAAE,IAAI,CAAC,KACzK,OAAOiL,GAAW,CAAC,EAAEnL,EAAY,IAAI,CAAC,KAAK,CAAC,EAAEI,EAAgB,CAAC,EAAEF,EAAa,CAAC,CACjF,CAEA,SAAS0L,GAAqB5L,CAAC,CAAEC,EAAO,CAAC,CAAC,EACxC,IAAMC,EAAc8I,GAAkBhJ,GACtC,GAAIE,EAAY,MAAM,CAAG,EACvB,OAAOA,EAET,IAAME,EAAUoL,GAAuBxL,GACvC,GAAII,AAAmB,IAAnBA,EAAQ,MAAM,CAChB,OAAOF,EAET,IAAMH,EAAc,IAAIkB,IAAIf,EAAY,GAAG,CAAC,CAACF,EAAQC,IAAU,CAACD,EAAQC,EAAM,GAC1EE,EAAOsL,GAAavL,EAAaE,EAASL,GACxCM,EAAWO,KAAK,GAAG,CAAC,EAAGX,EAAK,QAAQ,EAjJL,GAkJrC,IAAK,IAAID,EAAI,EAAGA,EAAIK,EAAUL,IAAK,CACjC,IAAMC,EAAO0L,GAAezL,EAAaE,EAASJ,GAE5CK,EAAYoL,GADJJ,GAAqBnL,EAAaD,GACVG,EAASL,EAC3C2L,CAAAA,GAAkBrL,EAAWF,IAC/BA,CAAAA,EAAOE,CAAQ,CAEnB,CACA,OAAOF,EAAK,KAAK,AACnB,CAIA,SAAS0L,GAAe7L,CAAC,CAAEC,CAAI,EAC7B,IAAMC,EAAuBD,GAAM,sBAAwB,GACrDG,EAA4BH,GAAM,0BAA4B,GAC9DF,EAAKkI,GAAejI,GACpBG,EAAYF,GAAM,sBAAwB2L,GAAqB7L,EAAI,CAAE,SAnKtC,CAmKiF,GAAK,KAAK,EAC1HM,EAAWuI,GAAiB7I,GAC5BO,EAAWD,EAAS,OAAO,CAU3B,CAAE,SAAUE,CAAc,CAAEC,iBAAAA,CAAgB,CAAE,CAAG+J,GATtCrK,EAAuBoK,GAA8BhK,EAAU,CAC9E,mBAAoBL,GAAM,oBA/7CE,GAg8C5B,qBAAsB,GACtB,UAAWA,GAAM,SACnB,GAAKmK,GAAqB9J,EAAU,CAClC,mBAAoBL,GAAM,oBAn8CE,GAo8C5B,qBAAsB,GACtB,yBAA0BG,CAC5B,GACoFE,GAC9EG,EAAUwK,GAAY1K,EAAgBC,EAAkB,CAAEL,UAAAA,CAAU,GACpEO,EAAcwK,GAAkBzK,EAASD,EAAkB,CAC/D,SAAUP,GAAM,SAChB,QAASA,GAAM,QACf,UAAWA,GAAM,UACjBE,UAAAA,CACF,GACA,MAAO,CACL,QAASG,EACT,SAAUD,EAAS,QAAQ,CAC3B,SAAUE,EACVE,QAAAA,EACAC,YAAAA,CACF,CACF,CAWA,SAASoL,GAAqB9L,CAAI,CAAEC,CAAM,CAAEC,CAAQ,EAClD,IAAME,EAAKJ,EAAK,CAAC,EAAI,EACfD,EAAKC,EAAK,CAAC,EAAI,EACfG,EAAKF,EAAO,CAAC,CAAGG,EAChBC,EAAKJ,EAAO,CAAC,CAAGF,EAChBO,EAAQM,KAAK,GAAG,CAACT,GACjBI,EAAQK,KAAK,GAAG,CAACP,UACvB,AAAIC,EAh/CK,MAg/CWC,EAh/CX,KAi/CAL,EAGLK,EAp/CK,MAo/CWA,AADC,EACDA,GAAwBD,EACnCD,EAAK,EAAI,SAAW,MAEzBC,EAv/CK,KAw/CAH,EAAK,EAAI,QAAU,OAErBD,CACT,CAEA,SAAS6L,GAAwB/L,CAAI,CAAEC,CAAQ,EAC7C,OAAOW,AA9/CE,KA8/CFA,KAAK,GAAG,CAACZ,EAAK,EAAE,CAAGC,EAAS,IAAI,GAAYW,AA9/C1C,KA8/C0CA,KAAK,GAAG,CAACZ,EAAK,EAAE,CAAGC,EAAS,EAAE,EAAWD,EAAK,EAAE,CAAGA,EAAK,IAAI,AACjH,CAEA,SAASgM,GAAYhM,CAAI,CAAEC,CAAK,EAC9B,MAAOD,AAAgB,aAAhBA,EAAK,MAAM,CAAkB,CAAE,EAAGA,EAAK,KAAK,CAAE,EAAGC,CAAM,EAAI,CAAE,EAAGA,EAAO,EAAGD,EAAK,KAAK,AAAC,CAC9F,CAEA,SAASiM,GAAqBjM,CAAI,CAAEC,CAAS,EAC3C,IAAMC,EAAQF,EAAK,KAAK,EAAI,EAAE,CACxBD,EAAgBC,EAAK,KAAK,EAAI,EAAE,CAChCG,EAAQ,EAAE,CAChB,IAAK,IAAMH,KAAMD,EACXC,EAAG,YAAY,EAGnBG,EAAM,IAAI,CAAC,CACT,GAAGH,CAAE,CACL,eAAgBA,CAClB,GAEF,IAAMK,EAA2B,IAAIY,IAC/BX,EAA+B,IAAIW,IACnCV,EAAQ,EAAE,CACVC,EAAOP,AAAc,OAAdA,EACb,IAAK,IAAMD,KAAKE,EACdG,EAAS,GAAG,CAACL,EAAE,EAAE,CAAEA,GAGrB,IAAK,IAAMA,KADYE,EAAM,MAAM,CAAC,AAACF,GAAMA,EAAE,OAAO,EAAI,CAACA,EAAE,QAAQ,EAC/B,CAClC,IAAMC,EAAO,CAAE,GAAID,EAAM,EAAE,AAAC,EACtBD,EAA6BK,EAAoB,AAACJ,IACtDM,EAAa,GAAG,CAACN,EAAE,EAAE,CAAEC,GACvBC,EAAM,MAAM,CAAC,AAACD,GAAUA,EAAM,QAAQ,GAAKD,EAAE,EAAE,EAAE,OAAO,CAACD,EAC3D,EAAG,cACHA,EAAWC,EACb,CACA,IAAMS,EAAYP,EAAM,MAAM,CAAC,AAACF,GAAM,CAACA,EAAE,OAAO,EAAI,CAACA,EAAE,WAAW,EAAE,GAAG,CAAC,AAACA,IACvE,IAAMC,EAAID,EAAE,KAAK,EAAI,GACfE,EAAIF,EAAE,MAAM,EAAI,GAChBI,EAAIJ,EAAE,CAAC,EAAI,EACXD,EAAIC,EAAE,CAAC,EAAI,EAEjB,MAAO,CACL,OAAQA,EAAE,EAAE,CACZ,KAAMI,EAAIH,EAAI,EAvED,EAwEb,KAAMG,EAAIH,EAAI,EAxED,EAyEb,KAAMF,EAAIG,EAAI,EAzED,EA0Eb,KAAMH,EAAIG,EAAI,EA1ED,EA4Eb,kBAAmBM,EAAON,EAAI,EA5EjB,EA4E+BD,EAAI,EA5EnC,CA6Ef,CACF,GACMS,EAA+BN,EAAoB,CAACJ,EAAaC,EAAOC,EAASE,KACrF,IAAIL,EAAOQ,EAAM,IAAI,CAAC,AAACL,GAAMA,EAAE,WAAW,GAAKF,GAAeY,AAA4B,EAA5BA,KAAK,GAAG,CAACV,EAAE,KAAK,CAAGD,IAcjF,OAbKF,IACHA,EAAO,CACL,GAAI,CAAC,KAAK,EAAEC,EAAY,CAAC,EAAEC,EAAM,OAAO,CAAC,GAAG,CAAC,CAC7CD,YAAAA,EACAC,MAAAA,EACAC,QAAAA,EACAE,QAAAA,EACA,OAAQ,EAAE,AACZ,EACAG,EAAM,IAAI,CAACR,IAEbA,EAAK,OAAO,CAAGa,KAAK,GAAG,CAACb,EAAK,OAAO,CAAEG,GACtCH,EAAK,OAAO,CAAGa,KAAK,GAAG,CAACb,EAAK,OAAO,CAAEK,GAC/BL,CACT,EAAG,gBACGY,EAA8BP,EAAoB,CAACJ,EAAMC,KAC7D,IAAMC,EAAIF,EAAK,KAAK,EAAI,GAClBI,EAAIJ,EAAK,MAAM,EAAI,GACnBD,EAAKC,EAAK,CAAC,EAAI,EACfG,EAAKH,EAAK,CAAC,EAAI,EACrB,OAAQC,GACN,IAAK,MACH,MAAO,CAAE,EAAGF,EAAI,EAAGI,EAAKC,EAAI,CAAE,CAChC,KAAK,SACH,MAAO,CAAE,EAAGL,EAAI,EAAGI,EAAKC,EAAI,CAAE,CAChC,KAAK,OACH,MAAO,CAAE,EAAGL,EAAKG,EAAI,EAAG,EAAGC,CAAG,CAChC,KAAK,QACH,MAAO,CAAE,EAAGJ,EAAKG,EAAI,EAAG,EAAGC,CAAG,CAClC,CACF,EAAG,eACGW,EAAoCV,EAAoB,CAACJ,EAAMC,EAAQC,IAAaS,EAAYX,EAAM8L,GAAqB9L,EAAMC,EAAQC,EAAW,SAAW,QAAS,qBACxKa,EAAoB,EAAE,CACtBC,EAAqB,EAAE,CACvBE,EAAyC,IAAIyF,IAE7CxF,EAAkCf,EAAoB,CAACJ,EAASC,EAAMC,KAC1E,GAAIa,AAA6B,IAA7BA,EAAkB,MAAM,CAC1B,OAAO,EAET,IAAMX,EAAeQ,AA5lDd,KA4lDcA,KAAK,GAAG,CAACX,EAAK,CAAC,CAAGC,EAAG,CAAC,EACrCH,EAAaa,AA7lDZ,KA6lDYA,KAAK,GAAG,CAACX,EAAK,CAAC,CAAGC,EAAG,CAAC,EACzC,GAAI,CAACE,GAAgB,CAACL,EACpB,OAAO,EAET,IAAII,EAAY,EAChB,GAAIC,EAAc,CAChB,IAAMA,EAAIH,EAAK,CAAC,CACVF,EAAOa,KAAK,GAAG,CAACX,EAAK,CAAC,CAAEC,EAAG,CAAC,EApmD7B,KAqmDCG,EAAOO,KAAK,GAAG,CAACX,EAAK,CAAC,CAAEC,EAAG,CAAC,EArmD7B,KAsmDL,GAAIG,GAAQN,EACV,OAAO,EAET,IAAK,IAAME,KAAOc,EACZd,EAAI,SAAS,GAAKD,GAAWC,AAAoB,aAApBA,EAAI,WAAW,GAG5CA,CAAAA,EAAI,IAAI,CAAC,KAAK,CAAGF,CAAG,IAAKE,CAAAA,EAAI,IAAI,CAAC,KAAK,CAAGI,CAAG,GAG7CJ,EAAI,IAAI,CAhnDT,MAgnDoBG,GAAKH,EAAI,EAAE,CAhnD/B,MAgnD0CG,GAC3CD,CAAAA,GA1BiB,GA0BW,CAGlC,MAAO,GAAIJ,EAAY,CACrB,IAAMK,EAAIH,EAAK,CAAC,CACVF,EAAOa,KAAK,GAAG,CAACX,EAAK,CAAC,CAAEC,EAAG,CAAC,EAtnD7B,KAunDCG,EAAOO,KAAK,GAAG,CAACX,EAAK,CAAC,CAAEC,EAAG,CAAC,EAvnD7B,KAwnDL,GAAIG,GAAQN,EACV,OAAO,EAET,IAAK,IAAME,KAAOc,EACZd,EAAI,SAAS,GAAKD,GAAWC,AAAoB,eAApBA,EAAI,WAAW,GAG5CA,CAAAA,EAAI,IAAI,CAAC,KAAK,CAAGF,CAAG,IAAKE,CAAAA,EAAI,IAAI,CAAC,KAAK,CAAGI,CAAG,GAG7CJ,EAAI,IAAI,CAloDT,MAkoDoBG,GAAKH,EAAI,EAAE,CAloD/B,MAkoD0CG,GAC3CD,CAAAA,GA5CiB,GA4CW,CAGlC,CACA,OAAOA,CACT,EAAG,mBACGiB,EAAejB,EAAM,GAAG,CAAC,CAACH,EAAMC,KACpC,GAAI,CAACD,EAAK,KAAK,EAAI,CAACA,EAAK,GAAG,CAC1B,MAAO,CAAEC,IAAAA,EAAK,UAAW,EAAG,GAAI,EAAG,GAAI,CAAE,EAE3C,IAAMC,EAAUG,EAAS,GAAG,CAACL,EAAK,KAAK,EACjCI,EAAUC,EAAS,GAAG,CAACL,EAAK,GAAG,EAC/BD,EAAUO,EAAa,GAAG,CAACN,EAAK,KAAK,EACrCG,EAAUG,EAAa,GAAG,CAACN,EAAK,GAAG,EACnCO,EAAYR,GAAWI,GAAWJ,EAAQ,EAAE,GAAKI,EAAQ,EAAE,CAAG,EAAI,EAGxE,MAAO,CAAEF,IAAAA,EAAKM,UAAAA,EAAW,GAFdL,GAAWE,EAAUQ,KAAK,GAAG,CAAC,AAACR,CAAAA,EAAQ,CAAC,EAAI,GAAMF,CAAAA,EAAQ,CAAC,EAAI,IAAM,EAEnD,GADlBA,GAAWE,EAAUQ,KAAK,GAAG,CAAC,AAACR,CAAAA,EAAQ,CAAC,EAAI,GAAMF,CAAAA,EAAQ,CAAC,EAAI,IAAM,CAChD,CAClC,GAAG,IAAI,CAAC,CAACF,EAAGC,KACV,GAAID,EAAE,SAAS,GAAKC,EAAE,SAAS,CAC7B,OAAOA,EAAE,SAAS,CAAGD,EAAE,SAAS,CAElC,IAAME,EAAQF,EAAE,EAAE,CAAGA,EAAE,EAAE,CACnBI,EAAQH,EAAE,EAAE,CAAGA,EAAE,EAAE,QACzB,AAAIW,KAAK,GAAG,CAACV,EAAQE,GAAS,EACrBF,EAAQE,EAEVJ,EAAE,GAAG,CAAGC,EAAE,GAAG,AACtB,GAAG,GAAG,CAAC,AAACD,GAAUA,EAAM,GAAG,EACrBqB,EAAmCjB,EAAoB,CAACJ,EAAIC,EAAIC,EAAcE,KAClF,IAAML,EAAUa,KAAK,GAAG,CAACZ,EAAG,CAAC,CAAEC,EAAG,CAAC,EAC7BE,EAAUS,KAAK,GAAG,CAACZ,EAAG,CAAC,CAAEC,EAAG,CAAC,EAC7BI,EAAUO,KAAK,GAAG,CAACZ,EAAG,CAAC,CAAEC,EAAG,CAAC,EAC7BK,EAAUM,KAAK,GAAG,CAACZ,EAAG,CAAC,CAAEC,EAAG,CAAC,EAcnC,MAAO,CAAC,CAbYQ,EAAU,IAAI,CAAC,AAACF,GAClC,AAAIL,CAAAA,CAAAA,GAAgBK,EAAI,MAAM,GAAKL,CAAW,GAG1CE,CAAAA,CAAAA,GAAcG,EAAI,MAAM,GAAKH,CAAS,IAGtCQ,KAAK,GAAG,CAACZ,EAAG,CAAC,CAAGC,EAAG,CAAC,EA5qDnB,KA6qDIM,EAAI,IAAI,CAAGP,EAAG,CAAC,EAAIO,EAAI,IAAI,CAAGP,EAAG,CAAC,EAAIO,EAAI,IAAI,CAAGR,GAAWQ,EAAI,IAAI,CAAGJ,EAEvEI,EAAI,IAAI,CAAGP,EAAG,CAAC,EAAIO,EAAI,IAAI,CAAGP,EAAG,CAAC,EAAIO,EAAI,IAAI,CAAGF,GAAWE,EAAI,IAAI,CAAGD,GAIpF,EAAG,oBACGgB,EAA6B,IAAIL,IACjCW,EAAqC,IAAIX,IAC/C,IAAK,IAAMjB,KAAQG,EACZH,EAAK,KAAK,EAAKA,EAAK,GAAG,EAAIA,EAAK,KAAK,GAAKA,EAAK,GAAG,GAGvD4B,EAAmB,GAAG,CAAC5B,EAAK,KAAK,CAAE,AAAC4B,CAAAA,EAAmB,GAAG,CAAC5B,EAAK,KAAK,GAAK,GAAK,GAC/E4B,EAAmB,GAAG,CAAC5B,EAAK,GAAG,CAAE,AAAC4B,CAAAA,EAAmB,GAAG,CAAC5B,EAAK,GAAG,GAAK,GAAK,IAE7E,IAAM6B,EAAgCzB,EAAoB,CAACJ,EAAMC,IAAW6L,GAAqB9L,EAAMC,EAAQ,UAAW,iBACpH6B,EAAgC,IAAIb,IAC1C,IAAK,GAAM,CAACjB,EAAGC,EAAE,GAAIE,EAAM,OAAO,GAAI,CACpC,GAAI,CAACF,EAAE,KAAK,EAAI,CAACA,EAAE,GAAG,EAAIA,EAAE,KAAK,GAAKA,EAAE,GAAG,EAGvCA,EAAE,MAAM,EAAIA,EAAE,MAAM,CAAC,MAAM,CAAG,EAFhC,SAKF,IAAMC,EAAMG,EAAS,GAAG,CAACJ,EAAE,KAAK,EAC1BG,EAAMC,EAAS,GAAG,CAACJ,EAAE,GAAG,EAC9B,GAAI,CAACC,GAAO,CAACE,EACX,SAEF,IAAML,EAAK,AAACK,CAAAA,EAAI,CAAC,EAAI,GAAMF,CAAAA,EAAI,CAAC,EAAI,GAC9BC,EAAK,AAACC,CAAAA,EAAI,CAAC,EAAI,GAAMF,CAAAA,EAAI,CAAC,EAAI,GACpC4B,EAAc,GAAG,CAAC9B,EAAG,CACnB,QAASA,EACT,MAAOC,EAAE,KAAK,CACd,MAAOA,EAAE,GAAG,CACZ,QAAS4B,EAAc3B,EAAK,CAAE,EAAGE,EAAI,CAAC,EAAI,EAAG,EAAGA,EAAI,CAAC,EAAI,CAAE,GAC3D,QAASyB,EAAczB,EAAK,CAAE,EAAGF,EAAI,CAAC,EAAI,EAAG,EAAGA,EAAI,CAAC,EAAI,CAAE,GAC3D,MAAOU,KAAK,GAAG,CAACb,GAChB,MAAOa,KAAK,GAAG,CAACT,GAChB,OAAQS,KAAK,IAAI,CAACb,GAClB,OAAQa,KAAK,IAAI,CAACT,EACpB,EACF,CACA,IAAM4B,EAAqC3B,EAAoB,AAACJ,GAC9D,AAAIA,AAAiB,QAAjBA,EAAK,OAAO,EAAcA,AAAiB,WAAjBA,EAAK,OAAO,CACjCA,AAAe,IAAfA,EAAK,KAAK,CAAS,IAAWA,EAAK,KAAK,CAAGA,EAAK,KAAK,CAEvDA,AAAe,IAAfA,EAAK,KAAK,CAAS,IAAWA,EAAK,KAAK,CAAGA,EAAK,KAAK,CAC3D,sBACGgC,EAAgC5B,EAAoB,AAACJ,GACzD,AAAIA,AAAiB,QAAjBA,EAAK,OAAO,EAAcA,AAAiB,WAAjBA,EAAK,OAAO,CACjCA,EAAK,MAAM,EAAI,EAAI,QAAU,OAE/BA,EAAK,MAAM,EAAI,EAAI,SAAW,MACpC,iBACGiC,EAAmC,IAAIhB,IAC7C,IAAK,IAAMjB,KAAQ8B,EAAc,MAAM,GAAI,CACzC,IAAM7B,EAAM,CAAC,EAAED,EAAK,KAAK,CAAC,CAAC,EAAEA,EAAK,OAAO,CAAC,CAAC,AACvC,CAACiC,EAAiB,GAAG,CAAChC,IACxBgC,EAAiB,GAAG,CAAChC,EAAK,EAAE,EAE9BgC,EAAiB,GAAG,CAAChC,GAAK,IAAI,CAACD,EACjC,CACA,IAAMkC,EAA2B,IAAIjB,IAC/BmB,EAA0BhC,EAAoB,CAACJ,EAAQC,IAAS,CAAC,EAAED,EAAO,CAAC,EAAEC,EAAK,CAAC,CAAE,WAC3F,IAAK,IAAMD,KAAQ8B,EAAc,MAAM,GACrCI,EAAS,GAAG,CACVE,EAAQpC,EAAK,KAAK,CAAEA,EAAK,OAAO,EAChC,AAACkC,CAAAA,EAAS,GAAG,CAACE,EAAQpC,EAAK,KAAK,CAAEA,EAAK,OAAO,IAAM,GAAK,GAE3DkC,EAAS,GAAG,CACVE,EAAQpC,EAAK,KAAK,CAAEA,EAAK,OAAO,EAChC,AAACkC,CAAAA,EAAS,GAAG,CAACE,EAAQpC,EAAK,KAAK,CAAEA,EAAK,OAAO,IAAM,GAAK,GAG7D,IAAK,IAAMA,KAASiC,EAAiB,MAAM,GACzC,IAAIjC,CAAAA,EAAM,MAAM,CAAG,IAGnBA,EAAM,IAAI,CAAC,CAACA,EAAGC,KACb,IAAMC,EAAK6B,EAAmB/B,GACxBI,EAAK2B,EAAmB9B,UAC9B,AAAIW,KAAK,GAAG,CAACV,EAAKE,GAAM,KACfA,EAAKF,EAEPF,EAAE,OAAO,CAAGC,EAAE,OAAO,AAC9B,GACA,IAAK,IAAIA,EAAI,EAAGA,EAAID,EAAM,MAAM,CAAEC,IAAK,CACrC,IAAMC,EAAOF,CAAK,CAACC,EAAE,CACfG,EAAY4B,EAAc9B,GAC1BH,EAAcmC,EAAS,GAAG,CAACE,EAAQlC,EAAK,KAAK,CAAEA,EAAK,OAAO,IAAM,EACjEC,EAAgB+B,EAAS,GAAG,CAACE,EAAQlC,EAAK,KAAK,CAAEE,KAAe,EAClED,GAAiBJ,IAGrBmC,EAAS,GAAG,CAACE,EAAQlC,EAAK,KAAK,CAAEA,EAAK,OAAO,EAAGH,EAAc,GAC9DmC,EAAS,GAAG,CAACE,EAAQlC,EAAK,KAAK,CAAEE,GAAYD,EAAgB,GAC7DD,EAAK,OAAO,CAAGE,EACjB,EAEF,IAAMkC,EAAgClC,EAAoB,AAACJ,IACzD,IAAMC,EAAQD,GAAM,MACpB,MAAOC,AAAU,aAAVA,GAAwBA,AAAU,YAAVA,CACjC,EAAG,iBACGsC,EAAgC,IAAItB,IAC1C,IAAK,IAAMjB,KAAQ8B,EAAc,MAAM,GACjC,AAACS,EAAc,GAAG,CAACvC,EAAK,KAAK,GAC/BuC,EAAc,GAAG,CAACvC,EAAK,KAAK,CAAkB,IAAI2G,KAEpDpE,EAAc,GAAG,CAACvC,EAAK,KAAK,EAAE,GAAG,CAACA,EAAK,OAAO,EAEhD,IAAK,IAAMA,KAAQ8B,EAAc,MAAM,GAAI,CACzC,GAAI,CAACQ,EAAcjC,EAAS,GAAG,CAACL,EAAK,KAAK,GACxC,SAEF,IAAMC,EAAUsC,EAAc,GAAG,CAACvC,EAAK,KAAK,EAC5C,GAAI,CAACC,GAAS,IAAID,EAAK,OAAO,EAC5B,SAEF,IAAME,EAAY8B,EAAchC,GAChC,GAAIC,EAAQ,GAAG,CAACC,IAAc,AAACgC,CAAAA,EAAS,GAAG,CAACE,EAAQpC,EAAK,KAAK,CAAEE,KAAe,GAAK,EAClF,SAEF,IAAME,EAAc8B,EAAS,GAAG,CAACE,EAAQpC,EAAK,KAAK,CAAEA,EAAK,OAAO,IAAM,EACvEkC,EAAS,GAAG,CAACE,EAAQpC,EAAK,KAAK,CAAEA,EAAK,OAAO,EAAGY,KAAK,GAAG,CAAC,EAAGR,EAAc,IAC1E8B,EAAS,GAAG,CAACE,EAAQpC,EAAK,KAAK,CAAEE,GAAY,GAC7CF,EAAK,OAAO,CAAGE,CACjB,CACA,IAAK,IAAMF,KAAQ8B,EAAc,MAAM,GAAI,CACzC,GAAM,CAAE,QAAS7B,CAAC,CAAEC,MAAAA,CAAK,CAAEE,MAAAA,CAAK,CAAEL,QAAAA,CAAO,CAAEI,QAAAA,CAAO,CAAE,CAAGH,EACjDM,EAAMD,EAAS,GAAG,CAACH,GACnBK,EAAMF,EAAS,GAAG,CAACD,GACnBI,EAAS,CAAC,EAAEN,EAAM,CAAC,EAAEH,EAAQ,IAAI,CAAC,CAClCU,EAAWV,AAAY,QAAZA,GAAqBA,AAAY,WAAZA,EAAuBQ,EAAI,CAAC,EAAI,EAAIA,EAAI,CAAC,EAAI,CAC/E,CAACe,EAAW,GAAG,CAACd,IAClBc,EAAW,GAAG,CAACd,EAAQ,EAAE,EAE3Bc,EAAW,GAAG,CAACd,GAAQ,IAAI,CAAC,CAAE,QAASP,EAAG,cAAeQ,CAAS,GAClE,IAAMC,EAAS,CAAC,EAAEN,EAAM,CAAC,EAAED,EAAQ,IAAI,CAAC,CAClCQ,EAAWR,AAAY,QAAZA,GAAqBA,AAAY,WAAZA,EAAuBG,EAAI,CAAC,EAAI,EAAIA,EAAI,CAAC,EAAI,CAC/E,CAACgB,EAAW,GAAG,CAACZ,IAClBY,EAAW,GAAG,CAACZ,EAAQ,EAAE,EAE3BY,EAAW,GAAG,CAACZ,GAAQ,IAAI,CAAC,CAAE,QAAST,EAAG,cAAeU,CAAS,EACpE,CACA,IAAM6B,EAA8B,IAAIvB,IAExC,IAAK,GAAM,CAACjB,EAAKC,EAAM,GAAIqB,EAAY,CACrC,GAAIrB,EAAM,MAAM,CAAG,EACjB,SAEFA,EAAM,IAAI,CAAC,CAACD,EAAGC,IAAMD,EAAE,aAAa,CAAGC,EAAE,aAAa,EACtD,IAAMC,EAAQF,EAAI,KAAK,CAAC,KAClBI,EAASF,EAAM,KAAK,CAAC,EAAG,IAAI,IAAI,CAAC,KACjCH,EAAOG,CAAK,CAACA,EAAM,MAAM,CAAG,EAAE,CAC9BC,EAAOD,CAAK,CAACA,EAAM,MAAM,CAAG,EAAE,CAC9BI,EAAOD,EAAS,GAAG,CAACD,GAC1B,GAAI,CAACE,EACH,SAGF,IAAMC,EAAa,AADIR,AAAS,SAATA,GAAmBA,AAAS,UAATA,EACNO,EAAK,MAAM,EAAI,GAAKA,EAAK,KAAK,EAAI,GAChEE,EAAQF,EAAK,KAAK,CAIlBG,EAAUG,KAAK,GAAG,CADC,GAGvBA,KAAK,GAAG,CAtBc,EAsBM,AAJN,CADNJ,AAAU,aAAVA,GAAwBA,AAAU,YAAVA,EACND,AAAa,GAAbA,EAAmBA,CAAS,EAIfN,CAAAA,EAAM,MAAM,CAAG,KAG1DS,EAAc,CADFD,CAAAA,EAAWR,CAAAA,EAAM,MAAM,CAAG,EAAC,EACZ,EACjC,IAAK,GAAM,CAACD,EAAGE,EAAQ,GAAID,EAAM,OAAO,GAAI,CAC1C,IAAMA,EAASS,EAAcV,EAAIS,EAC3BL,EAAY,CAAC,EAAEF,EAAQ,OAAO,CAAC,CAAC,EAAEC,EAAK,CAAC,CAC9CqC,EAAY,GAAG,CAACpC,EAAWH,EAC7B,CACF,CACA,IAAMwC,EAAmCrC,EAAoB,AAACJ,GAAY,EAAQG,CAAK,CAACH,EAAQ,EAAE,YAAc,oBAC1G0C,EAAmCtC,EAAoB,CAACJ,EAAQC,IACpE,CAAI,CAACD,GAGE,CAACsB,CAAAA,EAAW,GAAG,CAAC,CAAC,EAAEtB,EAAO,CAAC,EAAEC,EAAK,IAAI,CAAC,GAAK,EAAE,AAAD,EAAG,IAAI,CACzD,CAAC,CAAED,QAAAA,CAAO,CAAE,GAAKyC,EAAiBzC,KAC/B,AAACsB,CAAAA,EAAW,GAAG,CAAC,CAAC,EAAEtB,EAAO,CAAC,EAAEC,EAAK,IAAI,CAAC,GAAK,EAAE,AAAD,EAAG,IAAI,CACvD,CAAC,CAAED,QAAAA,CAAO,CAAE,GAAKyC,EAAiBzC,GACpC,EACC,oBACG2C,EAAkCvC,EAAoB,CAACJ,EAAUC,EAAMC,IAC3E,AAAID,AAAS,QAATA,GAAkBA,AAAS,WAATA,EACb,CAAE,EAAGD,EAAS,CAAC,CAAGE,EAAQ,EAAGF,EAAS,CAAC,AAAC,EAExC,CAAE,EAAGA,EAAS,CAAC,CAAE,EAAGA,EAAS,CAAC,CAAGE,CAAO,EAEhD,mBACG0C,EAA+BxC,EAAoB,CAACJ,EAAWC,EAAKC,KACxE,IAAME,EAAW0B,EAAc,GAAG,CAAC9B,GAC7BD,EAAY,CAAE,EAAGG,EAAI,CAAC,EAAI,EAAG,EAAGA,EAAI,CAAC,EAAI,CAAE,EAC3CC,EAAY,CAAE,EAAGF,EAAI,CAAC,EAAI,EAAG,EAAGA,EAAI,CAAC,EAAI,CAAE,EAC3CI,EAAUD,GAAU,SAAWyB,EAAc5B,EAAKF,GAClDO,EAAUF,GAAU,SAAWyB,EAAc3B,EAAKC,GACpDI,EAAWH,EAAWO,EAAYV,EAAKG,EAAS,OAAO,EAAIU,EAAkBb,EAAKF,EAAW,IAC7FS,EAAWJ,EAAWO,EAAYT,EAAKE,EAAS,OAAO,EAAIU,EAAkBZ,EAAKC,EAAW,IAC3FM,EAAY+B,EAAY,GAAG,CAAC,CAAC,EAAExC,EAAU,IAAI,CAAC,EAC9CU,EAAY8B,EAAY,GAAG,CAAC,CAAC,EAAExC,EAAU,IAAI,CAAC,EAOpD,OANIS,AAAc,KAAK,IAAnBA,GACFF,CAAAA,EAAWoC,EAAgBpC,EAAUF,EAASI,EAAS,EAErDC,AAAc,KAAK,IAAnBA,GACFF,CAAAA,EAAWmC,EAAgBnC,EAAUF,EAASI,EAAS,EAElD,CAAEH,SAAAA,EAAUC,SAAAA,EAAUH,QAAAA,EAASC,QAAAA,CAAQ,CAChD,EAAG,gBACH,IAAK,IAAMN,KAAKoB,EAAc,CAC5B,IAAMnB,EAAIE,CAAK,CAACH,EAAE,CAElB,GADAgB,CAAkB,CAAChB,EAAE,CAAG,EAAE,CACtB,CAACC,EAAE,KAAK,EAAI,CAACA,EAAE,GAAG,EAGlBA,EAAE,MAAM,EAAIA,EAAE,MAAM,CAAC,MAAM,CAAG,GAG9BA,EAAE,KAAK,GAAKA,EAAE,GAAG,CALnB,SAQF,IAAMC,EAAMG,EAAS,GAAG,CAACJ,EAAE,KAAK,EAC1BF,EAAMM,EAAS,GAAG,CAACJ,EAAE,GAAG,EAC9B,GAAI,CAACC,GAAO,CAACH,EACX,SAEF,GAAM,CACJO,SAAAA,CAAQ,CACRK,SAAAA,CAAQ,CACR,QAASG,CAAW,CACpB,QAASM,CAAW,CACrB,CAAGwB,EAAa5C,EAAGE,EAAKH,GACnB8B,EAAa,CAAE,GAAGvB,CAAQ,AAAC,EAC3BwB,EAAa,CAAE,GAAGnB,CAAQ,AAAC,EAC3BoB,EAAoBjB,AAAgB,QAAhBA,GAAyBA,AAAgB,WAAhBA,EAC7CkB,EAAoBZ,AAAgB,QAAhBA,GAAyBA,AAAgB,WAAhBA,EAC/CW,EAEFF,EAAW,CAAC,CAAG,AADEvB,EAAS,CAAC,CAAIJ,CAAAA,EAAI,CAAC,EAAI,GACdI,EAAS,CAAC,CA1btB,GA0byCA,EAAS,CAAC,CA1bnD,GA6bduB,EAAW,CAAC,CAAG,AADCvB,EAAS,CAAC,CAAIJ,CAAAA,EAAI,CAAC,EAAI,GACdI,EAAS,CAAC,CA7brB,GA6bwCA,EAAS,CAAC,CA7blD,GA+bZ0B,EAEFF,EAAW,CAAC,CAAG,AADEnB,EAAS,CAAC,CAAIZ,CAAAA,EAAI,CAAC,EAAI,GACdY,EAAS,CAAC,CAjctB,GAicyCA,EAAS,CAAC,CAjcnD,GAocdmB,EAAW,CAAC,CAAG,AADCnB,EAAS,CAAC,CAAIZ,CAAAA,EAAI,CAAC,EAAI,GACdY,EAAS,CAAC,CApcrB,GAocwCA,EAAS,CAAC,CApclD,GAschB,IAAMsB,EAAoC7B,EAAoB,CAACJ,EAAIC,KACjE,IAAK,IAAMC,KAAOO,EAChB,IAAIR,EAAe,QAAQ,CAACC,EAAI,MAAM,GAGlCF,EAAG,CAAC,CAAGE,EAAI,IAAI,EAAIF,EAAG,CAAC,CAAGE,EAAI,IAAI,EAAIF,EAAG,CAAC,CAAGE,EAAI,IAAI,EAAIF,EAAG,CAAC,CAAGE,EAAI,IAAI,CAC1E,MAAO,CAAE,OAAQ,GAAM,SAAUA,CAAI,EAGzC,MAAO,CAAE,OAAQ,EAAM,CACzB,EAAG,qBACGgC,EAAiC9B,EAAoB,CAACJ,EAAMC,EAAMC,EAAUE,EAAKL,KACrF,GAAIA,EAAgB,CAClB,IAAMA,EAAsBC,EAAK,CAAC,CAAIC,CAAAA,EAAK,CAAC,EAAI,GAEhD,MAAO,CACL,EAAG,AAFW,AAACC,CAAAA,EAAS,CAAC,EAAI,IAAMF,EAAK,CAAC,CAE5BI,EAAI,IAAI,CAzdF,GAyd8BA,EAAI,IAAI,CAzdtC,GA0dnB,EAAGL,EAAsBK,EAAI,IAAI,CAzdhB,GAyd0CA,EAAI,IAAI,CAzdlD,GA0djB,mBAAoBL,CACtB,CACF,CACA,IAAMI,EAAqBH,EAAK,CAAC,CAAIC,CAAAA,EAAK,CAAC,EAAI,GACzCI,EAAS,AAACH,CAAAA,EAAS,CAAC,EAAI,IAAMF,EAAK,CAAC,CAC1C,MAAO,CACL,EAAGG,EAAqBC,EAAI,IAAI,CAjeX,GAieuCA,EAAI,IAAI,CAje/C,GAkerB,EAAGC,EAASD,EAAI,IAAI,CAjeD,GAie2BA,EAAI,IAAI,CAjenC,GAkenBD,mBAAAA,CACF,CACF,EAAG,kBACCiC,EAAqB,EAAE,CACrBE,EAAc,CAACrC,EAAE,KAAK,CAAEA,EAAE,GAAG,CAAC,CAC9BsC,EAAWN,EAAkBJ,EAAYS,GAC/C,GAAIC,EAAS,MAAM,EAAIA,EAAS,QAAQ,CAAE,CACxC,IAAMvC,EAAMuC,EAAS,QAAQ,CAC7B,GAAIR,EAAmB,CACrB,IAAM9B,EAASiC,EAAe5B,EAAUJ,EAAKH,EAAKC,EAAK,GACvD6B,CAAAA,EAAW,CAAC,CAAG5B,EAAO,CAAC,CACvB4B,EAAW,CAAC,CAAG5B,EAAO,CAAC,CACvB,IAAMG,EAAOH,EAAO,kBAAkB,CAAGW,KAAK,GAAG,CAACZ,EAAI,IAAI,CAAG,EAAGM,EAAS,CAAC,CA5e9D,IA4ekFM,KAAK,GAAG,CAACZ,EAAI,IAAI,CAAG,EAAGM,EAAS,CAAC,CA5enH,IA6eZ8B,EAAqB,CACnB,CAAE,EAAG9B,EAAS,CAAC,CAAE,EAAGF,CAAK,EAEzB,CAAE,EAAGH,EAAO,CAAC,CAAE,EAAGG,CAAK,EAEvB,CAAE,EAAGH,EAAO,CAAC,CAAE,EAAGA,EAAO,CAAC,AAAC,EAE5B,AACH,KAAO,CACL,IAAMA,EAASiC,EAAe5B,EAAUJ,EAAKH,EAAKC,EAAK,IACjDI,EAAOH,EAAO,kBAAkB,CAAGW,KAAK,GAAG,CAACZ,EAAI,IAAI,CAAG,EAAGM,EAAS,CAAC,CAvf9D,IAufkFM,KAAK,GAAG,CAACZ,EAAI,IAAI,CAAG,EAAGM,EAAS,CAAC,CAvfnH,GAwfZuB,CAAAA,EAAW,CAAC,CAAG5B,EAAO,CAAC,CACvB4B,EAAW,CAAC,CAAG5B,EAAO,CAAC,CACvBmC,EAAqB,CACnB,CAAE,EAAGhC,EAAM,EAAGE,EAAS,CAAC,AAAC,EAEzB,CAAE,EAAGF,EAAM,EAAGH,EAAO,CAAC,AAAC,EAEvB,CAAE,EAAGA,EAAO,CAAC,CAAE,EAAGA,EAAO,CAAC,AAAC,EAE5B,AACH,CACF,CACA,IAAIwC,EAAqB,EAAE,CACrBE,EAAWV,EAAkBH,EAAYQ,GAC/C,GAAIK,EAAS,MAAM,EAAIA,EAAS,QAAQ,CAAE,CACxC,IAAM3C,EAAM2C,EAAS,QAAQ,CAC7B,GAAIX,EAAmB,CACrB,IAAM/B,EAASiC,EAAevB,EAAUZ,EAAKG,EAAKF,EAAK,GACvD8B,CAAAA,EAAW,CAAC,CAAG7B,EAAO,CAAC,CACvB6B,EAAW,CAAC,CAAG7B,EAAO,CAAC,CACvBwC,EAAqB,CACnB,CAAE,EAAGxC,EAAO,CAAC,CAAE,EAAGA,EAAO,CAAC,AAAC,EAE3B,CAAE,EAAGU,EAAS,CAAC,CAAE,EAAGV,EAAO,CAAC,AAAC,EAG9B,AACH,KAAO,CACL,IAAMA,EAASiC,EAAevB,EAAUZ,EAAKG,EAAKF,EAAK,GACvD8B,CAAAA,EAAW,CAAC,CAAG7B,EAAO,CAAC,CACvB6B,EAAW,CAAC,CAAG7B,EAAO,CAAC,CACvBwC,EAAqB,CACnB,CAAE,EAAGxC,EAAO,CAAC,CAAE,EAAGA,EAAO,CAAC,AAAC,EAE3B,CAAE,EAAGA,EAAO,CAAC,CAAE,EAAGU,EAAS,CAAC,AAAC,EAE9B,AACH,CACF,CACA,GAAIyB,AAA8B,IAA9BA,EAAmB,MAAM,EAAUK,AAA8B,IAA9BA,EAAmB,MAAM,CAAQ,CAEtE,IAAMvC,EAAeU,AApiBE,GAoiBFA,KAAK,GAAG,CAACiB,EAAW,CAAC,CAAGC,EAAW,CAAC,EACnD1B,EAAeQ,AAriBE,GAqiBFA,KAAK,GAAG,CAACiB,EAAW,CAAC,CAAGC,EAAW,CAAC,EACnD/B,EAAgByC,AAAgC,KAAK,IAArCA,EAAY,GAAG,CAAC,CAAC,EAAExC,EAAE,IAAI,CAAC,GAAgBwC,AAAgC,KAAK,IAArCA,EAAY,GAAG,CAAC,CAAC,EAAExC,EAAE,IAAI,CAAC,EACpFG,EAAe,AAACmB,CAAAA,EAAW,GAAG,CAAC,CAAC,EAAErB,EAAE,KAAK,EAAI,GAAG,CAAC,EAAEa,EAAY,IAAI,CAAC,GAAG,QAAU,GAAMQ,CAAAA,EAAW,GAAG,CAAC,CAAC,EAAErB,EAAE,KAAK,EAAI,GAAG,CAAC,EAAEa,EAAY,IAAI,CAAC,GAAG,QAAU,GACxJT,EAAe,AAACiB,CAAAA,EAAW,GAAG,CAAC,CAAC,EAAErB,EAAE,GAAG,EAAI,GAAG,CAAC,EAAEmB,EAAY,IAAI,CAAC,GAAG,QAAU,GAAME,CAAAA,EAAW,GAAG,CAAC,CAAC,EAAErB,EAAE,GAAG,EAAI,GAAG,CAAC,EAAEmB,EAAY,IAAI,CAAC,GAAG,QAAU,GACpJb,EAAgBJ,EAAe,GAAKE,EAAe,EACnDG,EAAmBoB,EAAmB,GAAG,CAAC3B,EAAE,KAAK,EAAI,KAAO,EAC5DQ,EAAmBmB,EAAmB,GAAG,CAAC3B,EAAE,GAAG,EAAI,KAAO,EAC1DS,EAAwBP,EAAe,GAAKuC,EAAiBzC,EAAE,KAAK,CAAEa,IAAgBT,EAAe,GAAKqC,EAAiBzC,EAAE,GAAG,CAAEmB,GAClIJ,EAAmCb,GAAgB,GAAKK,GAAoB,EAC5EW,EAAmCd,GAAgB,GAAKI,GAAoB,EAC5EsB,EAAqCxB,GAAiB,CAACG,GAAyBM,GAAoCG,EAC1H,GAAI,AAACjB,CAAAA,GAAgBE,CAAW,GAAM,CAACL,GAAkB,EAACQ,GAAiBwB,CAAiC,GAEtG,CADkBV,EAAiBf,EAAUK,EAAUV,EAAE,KAAK,CAAEA,EAAE,GAAG,EACrD,CAClBA,EAAE,MAAM,CAAG,CAAC,CAAE,GAAGK,CAAQ,AAAC,EAAG,CAAE,GAAGuB,CAAU,AAAC,EAAG,CAAE,GAAGC,CAAU,AAAC,EAAG,CAAE,GAAGnB,CAAQ,AAAC,EAAE,CACnFO,EAAuB,GAAG,CAAClB,GAC3B,IAAME,EAAsBE,EAAe,aAAe,WACpDL,EAAgBK,EAAeE,EAAS,CAAC,CAAGA,EAAS,CAAC,CACtDH,EAAeC,EAAeQ,KAAK,GAAG,CAACN,EAAS,CAAC,CAAEK,EAAS,CAAC,EAAIC,KAAK,GAAG,CAACN,EAAS,CAAC,CAAEK,EAAS,CAAC,EAChGN,EAAaD,EAAeQ,KAAK,GAAG,CAACN,EAAS,CAAC,CAAEK,EAAS,CAAC,EAAIC,KAAK,GAAG,CAACN,EAAS,CAAC,CAAEK,EAAS,CAAC,EAC9FJ,EAAe,CACnB,GAAI,CAAC,UAAU,EAAEL,EAAoB,CAAC,EAAEH,EAAc,OAAO,CAAC,GAAG,CAAC,EAAEC,EAAE,CAAC,CACvE,YAAaE,EACb,MAAOH,EACP,QAASI,EACT,QAASE,EACT,OAAQ,EAAE,AACZ,EACAU,EAAkB,IAAI,CAAC,CACrB,UAAWf,EACX,aAAc,EACd,YAAaE,EACb,KAAMK,EACN,WAAY,EACZ,KAAMJ,EACN,GAAIE,CACN,GACA,QACF,CAEJ,CACA,IAAMwC,EAAUnC,EAAa,WAAYmB,EAAW,CAAC,CAAEA,EAAW,CAAC,CAAEA,EAAW,CAAC,CACjFA,CAAAA,EAAW,CAAC,CAAGgB,EAAQ,KAAK,CAC5B,IAAMC,EAAUpC,EAAa,WAAYoB,EAAW,CAAC,CAAEA,EAAW,CAAC,CAAEA,EAAW,CAAC,CACjFA,CAAAA,EAAW,CAAC,CAAGgB,EAAQ,KAAK,CAC5B,IAAIC,EAASnC,KAAK,GAAG,CAACiB,EAAW,CAAC,CAAEC,EAAW,CAAC,EAAI,GAChDkB,EAASpC,KAAK,GAAG,CAACiB,EAAW,CAAC,CAAEC,EAAW,CAAC,EAAI,GAChDmB,EAASrC,KAAK,GAAG,CAACiB,EAAW,CAAC,CAAEC,EAAW,CAAC,EAAI,GAChDoB,EAAStC,KAAK,GAAG,CAACiB,EAAW,CAAC,CAAEC,EAAW,CAAC,EAAI,GACpD,IAAK,IAAM9B,KAAOS,EAAW,CAC3B,IAAMR,EAAWW,KAAK,GAAG,CAACiB,EAAW,CAAC,CAAEC,EAAW,CAAC,EAC9C5B,EAAWU,KAAK,GAAG,CAACiB,EAAW,CAAC,CAAEC,EAAW,CAAC,EAC9C1B,EAAWQ,KAAK,GAAG,CAACiB,EAAW,CAAC,CAAEC,EAAW,CAAC,EAC9C/B,EAAWa,KAAK,GAAG,CAACiB,EAAW,CAAC,CAAEC,EAAW,CAAC,CAC9B9B,CAAAA,EAAI,IAAI,CAAGE,GAAYF,EAAI,IAAI,CAAGC,GAAYD,EAAI,IAAI,CAAGD,GAAYC,EAAI,IAAI,CAAGI,IAEpG2C,EAASnC,KAAK,GAAG,CAACmC,EAAQ/C,EAAI,IAAI,CA3lBrB,IA4lBbgD,EAASpC,KAAK,GAAG,CAACoC,EAAQhD,EAAI,IAAI,CA5lBrB,IA6lBbiD,EAASrC,KAAK,GAAG,CAACqC,EAAQjD,EAAI,IAAI,CA7lBrB,IA8lBbkD,EAAStC,KAAK,GAAG,CAACsC,EAAQlD,EAAI,IAAI,CA9lBrB,IAgmBjB,CACA,IAAK,IAAMA,KAAOS,EACZT,EAAI,IAAI,CAAG+C,GAAU/C,EAAI,IAAI,CAAGgD,GAAUhD,EAAI,IAAI,CAAGiD,GAAUjD,EAAI,IAAI,CAAGkD,IAI9ExC,EAAa,aAAcV,EAAI,IAAI,CAxmBZ,GAwmBwB+C,EAAQC,GACvDtC,EAAa,aAAcV,EAAI,IAAI,CAzmBZ,GAymBwB+C,EAAQC,GAEvDtC,EAAa,WAAYV,EAAI,IAAI,CA1mBZ,GA0mBwBiD,EAAQC,GACrDxC,EAAa,WAAYV,EAAI,IAAI,CA3mBZ,GA2mBwBiD,EAAQC,IAEvDxC,EAAa,aAAcmB,EAAW,CAAC,CAAEkB,EAAQC,GACjDtC,EAAa,aAAcoB,EAAW,CAAC,CAAEiB,EAAQC,GACjD,IAAMG,EAAS5C,EAAM,MAAM,CACzB,AAACP,GAAMA,AAAkB,eAAlBA,EAAE,WAAW,EAAqBA,EAAE,KAAK,EAAIiD,GAAUjD,EAAE,KAAK,EAAIkD,GAErEE,EAAS7C,EAAM,MAAM,CACzB,AAACP,GAAMA,AAAkB,aAAlBA,EAAE,WAAW,EAAmBA,EAAE,KAAK,EAAI+C,GAAU/C,EAAE,KAAK,EAAIgD,GAEnEK,EAAyBjD,EAAoB,CAACJ,EAAGC,IAAM,CAAC,EAAED,EAAE,OAAO,CAAC,GAAG,CAAC,EAAEC,EAAE,OAAO,CAAC,GAAG,CAAC,CAAE,UAC1FkC,EAAWkB,EAAOxB,EAAW,CAAC,CAAEA,EAAW,CAAC,EAC5CyB,EAASD,EAAOvB,EAAW,CAAC,CAAEA,EAAW,CAAC,EAC1CyB,EAAyB,IAAItC,IAC7BuC,EAA2B,IAAIvC,IAC/BwC,EAA6B,IAAIxC,IACjCyC,EAA0B,IAAIiD,IAC9BhD,EAAW,EAAE,CACnBJ,EAAO,GAAG,CAACpB,EAAU,GACrBsB,EAAW,GAAG,CAACtB,EAAU,KACzBwB,EAAS,IAAI,CAAC,CACZ,IAAKxB,EACL,EAAGvB,KAAK,KAAK,CAACkB,EAAW,CAAC,CAAGD,EAAW,CAAC,CAAEC,EAAW,CAAC,CAAGD,EAAW,CAAC,EACtE,GAAIA,CACN,GACA6B,EAAQ,GAAG,CAACvB,GACZ,IAAIyB,EAAY,EAAE,CACZC,EAAsCzD,EAAoB,CAACJ,EAAIE,IAC5DmB,EAAiBrB,EAAIE,EAAID,EAAE,KAAK,CAAEA,EAAE,GAAG,EAC7C,uBACG6D,GAAW,CAAE,EAAGhC,EAAW,CAAC,CAAE,EAAGD,EAAW,CAAC,AAAC,EAC9CkC,GAAiBF,EAAoBhC,EAAYiC,IACjDE,GAAiBH,EAAoBC,GAAUhC,GAC/CmC,GAAiBF,IAAkBC,GACnCE,GAAW,CAAE,EAAGrC,EAAW,CAAC,CAAE,EAAGC,EAAW,CAAC,AAAC,EAC9CqC,GAAiBN,EAAoBhC,EAAYqC,IACjDE,GAAiBP,EAAoBK,GAAUpC,GAC/CuC,GAAiBF,IAAkBC,GAczC,GAbKH,GAMOI,KAERT,EADEhD,AA7nEC,KA6nEDA,KAAK,GAAG,CAACiB,EAAW,CAAC,CAAGC,EAAW,CAAC,EAC1B,CAACD,EAAYC,EAAW,CAExB,CAACD,EAAYqC,GAAUpC,EAAW,EAR9C8B,EADEhD,AAvnEC,KAunEDA,KAAK,GAAG,CAACiB,EAAW,CAAC,CAAGC,EAAW,CAAC,GAAYlB,AAvnE/C,KAunE+CA,KAAK,GAAG,CAACiB,EAAW,CAAC,CAAGC,EAAW,CAAC,EAC1E,CAACD,EAAYC,EAAW,CAExB,CAACD,EAAYiC,GAAUhC,EAAW,CAS9C8B,AAAqB,IAArBA,EAAU,MAAM,CAClB,KAAOD,EAAS,MAAM,CAAG,GAAG,CAC1BA,EAAS,IAAI,CAAC,CAAC3D,EAAGC,IAAMD,EAAE,CAAC,CAAGC,EAAE,CAAC,EACjC,IAAMC,EAAUyD,EAAS,KAAK,GAE9B,GADAD,EAAQ,MAAM,CAACxD,EAAQ,GAAG,EACtBA,EAAQ,GAAG,GAAKoD,EAAQ,CAC1B,IAAItD,EAAUsD,EACVrD,EAAS6B,EAEb,IADA8B,EAAY,CAAC3D,EAAO,CACbuD,EAAS,GAAG,CAACxD,IAAU,CAC5B,IAAME,EAAOsD,EAAS,GAAG,CAACxD,GAC1B4D,EAAU,OAAO,CAAC1D,GAClBD,EAASC,EACTF,EAAUqD,EAAOnD,EAAK,CAAC,CAAEA,EAAK,CAAC,CACjC,CACA,KACF,CACA,IAAME,EAAKF,EAAQ,EAAE,CAAC,CAAC,CACjBH,EAAKG,EAAQ,EAAE,CAAC,CAAC,CACjBC,EAAeiD,EAAO,IAAI,CAAC,CAACpD,EAAGC,IAAMD,EAAE,KAAK,CAAGC,EAAE,KAAK,EACtDI,EAAOF,EAAa,SAAS,CAAC,AAACH,GAAMY,AAAyB,EAAzBA,KAAK,GAAG,CAACZ,EAAE,KAAK,CAAGI,IACxDE,EAAe6C,EAAO,IAAI,CAAC,CAACnD,EAAGC,IAAMD,EAAE,KAAK,CAAGC,EAAE,KAAK,EACtDM,EAAOD,EAAa,SAAS,CAAC,AAACN,GAAMY,AAAyB,EAAzBA,KAAK,GAAG,CAACZ,EAAE,KAAK,CAAGD,IACxDS,EAAY,EAAE,CAapB,IAAK,IAAME,KAZPL,EAAO,GACTG,EAAU,IAAI,CAAC,CAAE,EAAGL,CAAY,CAACE,EAAO,EAAE,CAAC,KAAK,CAAE,EAAGN,CAAG,GAEtDM,GAAQ,GAAKA,EAAOF,EAAa,MAAM,CAAG,GAC5CK,EAAU,IAAI,CAAC,CAAE,EAAGL,CAAY,CAACE,EAAO,EAAE,CAAC,KAAK,CAAE,EAAGN,CAAG,GAEtDQ,EAAO,GACTC,EAAU,IAAI,CAAC,CAAE,EAAGJ,EAAI,EAAGE,CAAY,CAACC,EAAO,EAAE,CAAC,KAAK,AAAC,GAEtDA,GAAQ,GAAKA,EAAOD,EAAa,MAAM,CAAG,GAC5CE,EAAU,IAAI,CAAC,CAAE,EAAGJ,EAAI,EAAGE,CAAY,CAACC,EAAO,EAAE,CAAC,KAAK,AAAC,GAEnCC,GAAW,CAChC,IAAML,EAAOS,KAAK,GAAG,CAACR,EAAIM,EAAS,CAAC,EAC9BL,EAAOO,KAAK,GAAG,CAACR,EAAIM,EAAS,CAAC,EAC9BJ,EAAOM,KAAK,GAAG,CAACb,EAAIW,EAAS,CAAC,EAC9BH,EAAOK,KAAK,GAAG,CAACb,EAAIW,EAAS,CAAC,EAWpC,GAVgBD,EAAU,IAAI,CAAC,AAACT,GAC9B,AAAIA,EAAI,MAAM,GAAKC,EAAE,KAAK,EAAID,EAAI,MAAM,GAAKC,EAAE,GAAG,GAG9CE,IAASE,EACJL,EAAI,IAAI,CAAGD,GAAMC,EAAI,IAAI,CAAGD,GAAMC,EAAI,IAAI,CAAGG,GAAQH,EAAI,IAAI,CAAGK,EAEhEL,EAAI,IAAI,CAAGI,GAAMJ,EAAI,IAAI,CAAGI,GAAMJ,EAAI,IAAI,CAAGM,GAAQN,EAAI,IAAI,CAAGO,IAIzE,SAEF,IAAMC,EAAO6C,EAAO3C,EAAS,CAAC,CAAEA,EAAS,CAAC,EACpCC,EAAOC,KAAK,GAAG,CAACF,EAAS,CAAC,CAAGN,GAAMQ,KAAK,GAAG,CAACF,EAAS,CAAC,CAAGX,GACzDe,EAAUK,EAAgBnB,EAAGE,EAAQ,EAAE,CAAEQ,GAC3CK,EAAa,EACXC,EAASc,EAAW,CAAC,CAAGD,EAAW,CAAC,CACpCX,EAASY,EAAW,CAAC,CAAGD,EAAW,CAAC,CACpCT,EAASV,EAAS,CAAC,CAAGN,EACtBiB,EAASX,EAAS,CAAC,CAAGX,CACxBmB,CAAAA,CAAAA,EAAS,IAAMG,EAAS,IAAMH,EAAS,KAAOG,EAAS,IACzDN,CAAAA,EAAaH,AAAmB,IAAnBA,KAAK,GAAG,CAACS,EAAY,EAEhCL,CAAAA,EAAS,IAAMI,EAAS,IAAMJ,EAAS,KAAOI,EAAS,IACzDL,CAAAA,GAAcH,AAAmB,GAAnBA,KAAK,GAAG,CAACQ,EAAW,EAEpC,IAAIE,EAAc,EACZM,EAAa6B,EAAW,GAAG,CAACvD,EAAQ,GAAG,GAAK,IAC5C6B,EAAUnB,KAAK,GAAG,CAACQ,GAzsExB,KAysEyC,IAAM,GAC5CQ,AAAe,OAAfA,GAAsBA,IAAeG,GACvCT,CAAAA,EAAc,EAAC,EAEjB,IAAMU,EAAWrB,EAAOG,EAAUC,EAAaO,EACzCW,EAAa,AAACsB,CAAAA,EAAO,GAAG,CAACrD,EAAQ,GAAG,GAAK,GAAO,EAAK8B,EACrDE,EAAItB,KAAK,GAAG,CAACkB,EAAW,CAAC,CAAGpB,EAAS,CAAC,EAAIE,KAAK,GAAG,CAACkB,EAAW,CAAC,CAAGpB,EAAS,CAAC,EAClF,GAAIuB,EAAcsB,CAAAA,EAAO,GAAG,CAAC/C,IAAS,GAAO,EAI3C,GAHAgD,EAAS,GAAG,CAAChD,EAAMN,EAAQ,EAAE,EAC7BqD,EAAO,GAAG,CAAC/C,EAAMyB,GACjBwB,EAAW,GAAG,CAACjD,EAAMuB,GAChB2B,EAAQ,GAAG,CAAClD,GAGV,CACL,IAAMR,EAAM2D,EAAS,SAAS,CAAC,AAAC3D,GAAMA,EAAE,GAAG,GAAKQ,EAC5CR,AAAQ,MAARA,GACF2D,CAAAA,CAAQ,CAAC3D,EAAI,CAAC,CAAC,CAAGiC,EAAaC,CAAAA,CAEnC,MAPEyB,EAAS,IAAI,CAAC,CAAE,IAAKnD,EAAM,EAAGyB,EAAaC,EAAG,GAAIxB,CAAS,GAC3DgD,EAAQ,GAAG,CAAClD,EAQlB,CACF,CAKF,GAHIoD,AAAqB,IAArBA,EAAU,MAAM,EAClBA,CAAAA,EAAY,CAAC/B,EAAY,CAAE,EAAGA,EAAW,CAAC,CAAE,EAAGC,EAAW,CAAC,AAAC,EAAGA,EAAW,AAAD,EAEvE8B,EAAU,MAAM,CAAG,EAAG,CACxB,IAAM5D,EAAQ4D,CAAS,CAAC,EAAE,CACpB3D,EAAM2D,CAAS,CAACA,EAAU,MAAM,CAAG,EAAE,CACvC1D,EAAOU,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EAC9BF,EAAOa,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EAC9BE,EAAOS,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EAC9BI,EAAOO,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EAClC,IAAK,IAAMD,KAAM4D,EACf1D,EAAOU,KAAK,GAAG,CAACV,EAAMF,EAAG,CAAC,EAC1BD,EAAOa,KAAK,GAAG,CAACb,EAAMC,EAAG,CAAC,EAC1BG,EAAOS,KAAK,GAAG,CAACT,EAAMH,EAAG,CAAC,EAC1BK,EAAOO,KAAK,GAAG,CAACP,EAAML,EAAG,CAAC,EAE5B,IAAMM,EAAYP,EAAOa,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EAC1CM,EAAWL,EAAOU,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EAC/C,GAAIO,EAAM,CAER,GAAIF,EAAW,CACb,IAAMJ,EAAQU,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EAC/BG,EAAWQ,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EAClCE,EAAWS,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EAClCI,EAAkBI,EAAU,MAAM,CACtC,AAACT,GAAQA,EAAI,IAAI,CAAGE,GAASF,EAAI,IAAI,CAAGE,GACxCF,EAAI,IAAI,CAAGG,GAAYH,EAAI,IAAI,CAAGI,GAGpC,GAAIC,EAAgB,MAAM,CAAG,EAAG,CAC9B,IAAIH,EAAaU,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EACxC,IAAK,IAAMD,KAAOK,EAAiB,CACjC,IAAMJ,EAAa,AAACD,CAAAA,EAAI,IAAI,CAAGA,EAAI,IAAI,AAAD,EAAK,CACb,MAAK,IAA/BA,EAAI,iBAAiB,EAAekM,MAAMlM,EAAI,iBAAiB,GAInEE,CAAAA,EAAaU,KAAK,GAAG,CAACV,EADFD,EAAaD,EAAI,iBAAiB,CAhyBzC,GAiyBgC,CAC/C,CACI,AAACkM,MAAMhM,IACTH,CAAAA,EAAOG,CAAS,CAEpB,CACF,CACA,GAAIK,EAAU,CACZ,IAAMH,EAAkBK,EAAU,MAAM,CACtC,AAACP,GAAQA,EAAI,IAAI,CAAGU,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EA1yB5B,IA2yBfC,EAAI,IAAI,CAAGU,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,GAAKC,EAAI,IAAI,CAAGU,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,GAG3E,GAAIG,EAAgB,MAAM,CAAG,EAAG,CAC9B,IAAIL,EAAaa,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EACxC,IAAK,IAAMD,KAAOI,EAGhBL,EAAaa,KAAK,GAAG,CAACb,EADH,AADCC,CAAAA,EAAI,IAAI,CAAGA,EAAI,IAAI,AAAD,EAAK,EACXA,EAAI,iBAAiB,CAlzBxC,IAqzBfE,EAAOH,CACT,CACF,CACF,CACA,IAAMW,EAAkCN,EAAoB,AAACF,IAC3D,IAAME,EAAYH,EAAI,CAAC,CAAGD,EAAM,CAAC,CAC3BD,EAAcU,EAAU,MAAM,CAAC,AAACP,IACpC,IAAME,EAAcQ,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EAAIC,EAAI,IAAI,EAAIU,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EAAIC,EAAI,IAAI,CACxFH,EAAca,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EAAIC,EAAI,IAAI,EAAIU,KAAK,GAAG,CAACZ,EAAM,CAAC,CAAEC,EAAI,CAAC,EAAIC,EAAI,IAAI,CAC9F,OAAOE,GAAeL,CACxB,GACII,EAAcJ,EAClB,GAAIS,GAAQT,EAAY,MAAM,CAAG,EAAG,CAClC,IAAMC,EAAeD,EAAY,MAAM,CACrC,AAACC,GAAQA,EAAI,IAAI,CAAGE,GAAWF,EAAI,IAAI,CAAGE,EAExCF,CAAAA,EAAa,MAAM,CAAG,GACxBG,CAAAA,EAAcH,CAAW,CAE7B,CACA,GAAIG,AAAuB,IAAvBA,EAAY,MAAM,CACpB,OAAOF,EAAI,CAAC,CAGd,GAAIG,EAAW,CAEb,IAAMJ,EAAQ,AADUY,KAAK,GAAG,IAAIT,EAAY,GAAG,CAAC,AAACH,GAAQA,EAAI,IAAI,GA/0BlD,GAi1BnB,GAAIA,EAAQC,EAAI,CAAC,CArzEhB,KAszEC,OAAOD,CAEX,KAAO,CAEL,IAAMA,EAAQ,AADQY,KAAK,GAAG,IAAIT,EAAY,GAAG,CAAC,AAACH,GAAQA,EAAI,IAAI,GAr1BhD,GAu1BnB,GAAIA,EAAQC,EAAI,CAAC,CA3zEhB,KA4zEC,OAAOD,CAEX,CACA,OAAOC,EAAI,CAAC,AACd,EAAG,mBACGU,EAAyCP,EAAoB,AAACF,IAClE,IAAME,EAAQM,EAAgBR,GACxBH,EAAU,CAAE,EAAGG,EAAS,EAAGF,EAAM,CAAC,AAAC,EACnCG,EAAU,CAAE,EAAGD,EAAS,EAAGE,CAAM,EACjCC,EAAU,CAAE,EAAGJ,EAAI,CAAC,CAAE,EAAGG,CAAM,EAC/BE,EAAcuD,EAAoB7D,EAAOD,GACzCQ,EAAcsD,EAAoB9D,EAASI,GAC3CK,EAAcqD,EAAoB1D,EAASE,GAC3CI,EAAcL,IAAUH,EAAI,CAAC,EAAG4D,EAAoBxD,EAASJ,UACnE,AAAI,AAACK,GAAgBC,GAAgBC,GAAgBC,EAM9C,KALL,AAAIG,AA30EH,KA20EGA,KAAK,GAAG,CAACR,EAAQH,EAAI,CAAC,EACjB,CAACD,EAAOD,EAASI,EAASF,EAAI,CAEhC,CAACD,EAAOD,EAASI,EAASE,EAASJ,EAAI,AAGlD,EAAG,0BACGa,EAAcR,GAAa,CAACC,EAAWI,EAAuBZ,GAAQQ,GAAY,CAACD,EAAYK,EAAuBT,GAAQ,IAChIY,CAAAA,GACF8C,CAAAA,EAAY9C,CAAU,CAE1B,CACA,IAAMwD,GAAa,CACjBhE,KACG8B,KACAwB,KACAnB,EAAmB,OAAO,GAE7B9B,EACD,CACD,GAAI2D,GAAW,MAAM,EAAI,EAAG,CAC1B,IAAMtE,EAAIsE,EAAU,CAACA,GAAW,MAAM,CAAG,EAAE,CACrCrE,EAAIqE,EAAU,CAACA,GAAW,MAAM,CAAG,EAAE,CACrCpE,EAAIoE,EAAU,CAACA,GAAW,MAAM,CAAG,EAAE,CACrClE,EAAUQ,AAn2EX,KAm2EWA,KAAK,GAAG,CAACV,EAAE,CAAC,CAAGD,EAAE,CAAC,GAAYW,AAn2EzC,KAm2EyCA,KAAK,GAAG,CAACX,EAAE,CAAC,CAAGD,EAAE,CAAC,EAC1DD,EAASa,AAp2EV,KAo2EUA,KAAK,GAAG,CAACV,EAAE,CAAC,CAAGD,EAAE,CAAC,GAAYW,AAp2ExC,KAo2EwCA,KAAK,GAAG,CAACX,EAAE,CAAC,CAAGD,EAAE,CAAC,EAC/D,GAAII,EAAS,CACX,IAAMA,EAASQ,KAAK,IAAI,CAACX,EAAE,CAAC,CAAGC,EAAE,CAAC,EAC5BH,EAASa,KAAK,IAAI,CAACZ,EAAE,CAAC,CAAGE,EAAE,CAAC,CAC9BE,AAAW,KAAXA,GAAgBA,IAAWL,GAAUa,KAAK,GAAG,CAACX,EAAE,CAAC,CAAGC,EAAE,CAAC,EAAIU,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAGE,EAAE,CAAC,GAC/EoE,GAAW,MAAM,CAAC,GAAI,EAE1B,MAAO,GAAIvE,EAAQ,CACjB,IAAMK,EAASQ,KAAK,IAAI,CAACX,EAAE,CAAC,CAAGC,EAAE,CAAC,EAC5BH,EAASa,KAAK,IAAI,CAACZ,EAAE,CAAC,CAAGE,EAAE,CAAC,CAC9BE,AAAW,KAAXA,GAAgBA,IAAWL,GAAUa,KAAK,GAAG,CAACX,EAAE,CAAC,CAAGC,EAAE,CAAC,EAAIU,KAAK,GAAG,CAACZ,EAAE,CAAC,CAAGE,EAAE,CAAC,GAC/EoE,GAAW,MAAM,CAAC,GAAI,EAE1B,CACF,CACA,IAAMC,GAAa,CAACD,EAAU,CAAC,EAAE,CAAC,CAClC,IAAK,IAAItE,EAAI,EAAGA,EAAIsE,GAAW,MAAM,CAAG,EAAGtE,IAAK,CAC9C,GAAIA,AAAM,IAANA,EAAS,CACXuE,GAAW,IAAI,CAACD,EAAU,CAACtE,EAAE,EAC7B,QACF,CACA,IAAMC,EAAOsE,EAAU,CAACA,GAAW,MAAM,CAAG,EAAE,CACxCrE,EAAOoE,EAAU,CAACtE,EAAE,CACpBI,EAAOkE,EAAU,CAACtE,EAAI,EAAE,CAC9B,GAAIY,AA53EC,KA43EDA,KAAK,GAAG,CAACX,EAAK,CAAC,CAAGC,EAAK,CAAC,GAAYU,AA53EnC,KA43EmCA,KAAK,GAAG,CAACV,EAAK,CAAC,CAAGE,EAAK,CAAC,EAAU,CAC3DF,EAAK,CAAC,CAAGD,EAAK,CAAC,EACfG,EAAK,CAAC,CAAGF,EAAK,CAAC,EAE1BqE,GAAW,IAAI,CAACrE,GAGlB,QACF,CACA,GAAIU,AAr4EC,KAq4EDA,KAAK,GAAG,CAACX,EAAK,CAAC,CAAGC,EAAK,CAAC,GAAYU,AAr4EnC,KAq4EmCA,KAAK,GAAG,CAACV,EAAK,CAAC,CAAGE,EAAK,CAAC,EAAU,CAC3DF,EAAK,CAAC,CAAGD,EAAK,CAAC,EACfG,EAAK,CAAC,CAAGF,EAAK,CAAC,EAE1BqE,GAAW,IAAI,CAACrE,GAGlB,QACF,CACAqE,GAAW,IAAI,CAACrE,EAClB,CACAqE,GAAW,IAAI,CAACD,EAAU,CAACA,GAAW,MAAM,CAAG,EAAE,EACjD,IAAK,IAAIrE,EAAI,EAAGA,EAAIsE,GAAW,MAAM,CAAG,EAAGtE,IAAK,CAC9C,IAAMC,EAAKqE,EAAU,CAACtE,EAAE,CAClBG,EAAKmE,EAAU,CAACtE,EAAI,EAAE,CACtBF,EAAca,AAp5Ef,KAo5EeA,KAAK,GAAG,CAACV,EAAG,CAAC,CAAGE,EAAG,CAAC,EAAW,WAAa,aAC1DD,EAAQJ,AAAgB,aAAhBA,EAA6BG,EAAG,CAAC,CAAGA,EAAG,CAAC,CAChDG,EAAON,AAAgB,aAAhBA,EAA6Ba,KAAK,GAAG,CAACV,EAAG,CAAC,CAAEE,EAAG,CAAC,EAAIQ,KAAK,GAAG,CAACV,EAAG,CAAC,CAAEE,EAAG,CAAC,EAC9EE,EAAKP,AAAgB,aAAhBA,EAA6Ba,KAAK,GAAG,CAACV,EAAG,CAAC,CAAEE,EAAG,CAAC,EAAIQ,KAAK,GAAG,CAACV,EAAG,CAAC,CAAEE,EAAG,CAAC,EAC5EG,EAAOG,EAAaX,EAAaI,EAAOE,EAAMC,GAC9CE,EAAO,CACX,UAAWR,EACX,aAAcC,EACdF,YAAAA,EACAQ,KAAAA,EACA,WAAY,EAEZF,KAAAA,EACAC,GAAAA,CACF,EACAS,EAAkB,IAAI,CAACP,GACvBQ,CAAkB,CAAChB,EAAE,CAAC,IAAI,CAACe,EAAkB,MAAM,CAAG,GAClD,AAACR,EAAK,MAAM,CAAC,EAAE,EACjBA,CAAAA,EAAK,MAAM,CAAC,EAAE,CAAG,CAAE,MAAO,EAAG,MAAOA,EAAK,KAAK,CAAE,SAAU,EAAE,AAAC,GAE/DA,EAAK,MAAM,CAAC,EAAE,CAAC,QAAQ,CAAC,IAAI,CAAC,CAC3B,UAAWP,EACX,aAAcC,EACdI,KAAAA,EACAC,GAAAA,CACF,EACF,CACF,CACA,IAAMuC,EAAkCzC,EAAoB,CAACJ,EAAIC,IACxDD,EAAG,IAAI,CAAGC,EAAG,EAAE,EAAIA,EAAG,IAAI,CAAGD,EAAG,EAAE,CACxC,mBACG8C,EAA8C1C,EAAoB,CAACJ,EAAIC,EAAIC,EAAIE,KACnF,IAAML,EAAY,CAACK,EAAG,QAAQ,CAAC,IAAI,CACjC,AAACF,GAAM,AAACA,CAAAA,EAAE,SAAS,GAAKD,EAAG,SAAS,EAAIC,EAAE,YAAY,GAAKD,EAAG,YAAY,AAAD,GAAM4C,EAAgB3C,EAAGF,IAE9FG,EAAY,CAACD,EAAG,QAAQ,CAAC,IAAI,CACjC,AAACA,GAAM,AAACA,CAAAA,EAAE,SAAS,GAAKF,EAAG,SAAS,EAAIE,EAAE,YAAY,GAAKF,EAAG,YAAY,AAAD,GAAM6C,EAAgB3C,EAAGD,UAEpG,EAAIF,KAAaI,IACfH,EAAG,UAAU,CAAGI,EAAG,KAAK,CACxBH,EAAG,UAAU,CAAGC,EAAG,KAAK,CACxBA,EAAG,QAAQ,CAAG,IACTA,EAAG,QAAQ,CAAC,MAAM,CACnB,AAACD,GAAMA,EAAE,SAAS,GAAKD,EAAG,SAAS,EAAIC,EAAE,YAAY,GAAKD,EAAG,YAAY,EAE3E,CACE,UAAWC,EAAG,SAAS,CACvB,aAAcA,EAAG,YAAY,CAC7B,KAAMA,EAAG,IAAI,CACb,GAAIA,EAAG,EAAE,AACX,EACD,CACDG,EAAG,QAAQ,CAAG,IACTA,EAAG,QAAQ,CAAC,MAAM,CACnB,AAACJ,GAAMA,EAAE,SAAS,GAAKC,EAAG,SAAS,EAAID,EAAE,YAAY,GAAKC,EAAG,YAAY,EAE3E,CACE,UAAWD,EAAG,SAAS,CACvB,aAAcA,EAAG,YAAY,CAC7B,KAAMA,EAAG,IAAI,CACb,GAAIA,EAAG,EAAE,AACX,EACD,CACM,GAGX,EAAG,+BACG+C,EAAiC3C,EAAoB,AAACJ,IAC1D,IAAMC,EAAMD,EAAK,MAAM,CAAC,MAAM,CAE9B,OADAA,EAAK,MAAM,CAACC,EAAI,CAAG,CAAE,MAAOA,EAAK,MAAOD,EAAK,KAAK,CAAE,SAAU,EAAE,AAAC,EAC1DC,CACT,EAAG,kBACG+C,EAAqC5C,EAAoB,CAACJ,EAAKC,KACnE,IAAMC,EAAWF,EAAI,IAAI,CAAC,MAAM,CAACA,EAAI,UAAU,CAAC,AAChDE,CAAAA,EAAS,QAAQ,CAAGA,EAAS,QAAQ,CAAC,MAAM,CAC1C,AAACD,GAAMA,EAAE,SAAS,GAAKD,EAAI,SAAS,EAAIC,EAAE,YAAY,GAAKD,EAAI,YAAY,EAE7EA,EAAI,UAAU,CAAGC,EAEjB,AADiBD,EAAI,IAAI,CAAC,MAAM,CAACC,EAAS,CACjC,QAAQ,CAAC,IAAI,CAAC,CACrB,UAAWD,EAAI,SAAS,CACxB,aAAcA,EAAI,YAAY,CAC9B,KAAMA,EAAI,IAAI,CACd,GAAIA,EAAI,EAAE,AACZ,EACF,EAAG,sBACGiD,EAA0C7C,EAAoB,CAACJ,EAAKC,KAExE,IAAK,IAAMC,KADKc,CAAkB,CAAChB,EAAI,SAAS,CAAC,CACtB,CACzB,IAAMI,EAAIW,CAAiB,CAACb,EAAI,AAC5BE,CAAAA,EAAE,IAAI,GAAKJ,EAAI,IAAI,EACrBgD,EAAmB5C,EAAGH,EAE1B,CACF,EAAG,2BACGiD,EAA+C9C,EAAoB,AAACJ,IACxE,IAAMC,EAAUe,CAAkB,CAAChB,EAAI,SAAS,CAAC,CAC3CE,EAAYD,EAAQ,OAAO,CAACc,EAAkB,OAAO,CAACf,IACtDI,EAAM,EAAE,CAOd,OANIF,EAAY,GACdE,EAAI,IAAI,CAACW,CAAiB,CAACd,CAAO,CAACC,EAAY,EAAE,CAAC,EAEhDA,EAAYD,EAAQ,MAAM,CAAG,GAC/BG,EAAI,IAAI,CAACW,CAAiB,CAACd,CAAO,CAACC,EAAY,EAAE,CAAC,EAE7CE,CACT,EAAG,gCACG+C,EAAkC/C,EAAoB,CAACJ,EAAMC,KACjE,GAAID,EAAK,WAAW,GAAKC,EAAK,WAAW,CACvC,MAAO,GAET,IAAMC,EAAIF,AAAqB,eAArBA,EAAK,WAAW,CAAoBA,EAAOC,EAC/CG,EAAIJ,AAAqB,eAArBA,EAAK,WAAW,CAAoBC,EAAOD,EACrD,OAAOI,EAAE,IAAI,CAAC,KAAK,CAAGF,EAAE,IAAI,EAAIE,EAAE,IAAI,CAAC,KAAK,CAAGF,EAAE,EAAE,EAAIA,EAAE,IAAI,CAAC,KAAK,CAAGE,EAAE,IAAI,EAAIF,EAAE,IAAI,CAAC,KAAK,CAAGE,EAAE,EAAE,AACrG,EAAG,mBACGgD,EAAqChD,EAAoB,CAACJ,EAAMC,KACpE,IAAK,IAAMC,KAASF,EAAK,MAAM,CAI7B,GAAI,CAHYE,EAAM,QAAQ,CAAC,IAAI,CACjC,AAACF,GAAM,AAACA,CAAAA,EAAE,SAAS,GAAKC,EAAI,SAAS,EAAID,EAAE,YAAY,GAAKC,EAAI,YAAY,AAAD,GAAM4C,EAAgB7C,EAAGC,IAGpG,OAAOC,EAAM,KAAK,CAGtB,OAAO,EACT,EAAG,sBACGmD,EAAmCjD,EAAoB,CAACJ,EAAIC,KAChE,GAAID,EAAG,UAAU,GAAKC,EAAG,UAAU,CACjC,OAAO4C,EAAgB7C,EAAIC,GAE7B,IAAMC,EAAOgD,EAA6BlD,GACpCI,EAAO8C,EAA6BjD,GAC1C,OAAOC,EAAK,IAAI,CAAC,AAACF,GAAOI,EAAK,IAAI,CAAC,AAACH,GAAOkD,EAAgBnD,EAAIC,IACjE,EAAG,oBACGkC,EAAuC/B,EAAoB,CAACJ,EAAIC,EAAIC,KACxE,GAAI4C,EACF9C,EACAC,EACAD,EAAG,IAAI,CAAC,MAAM,CAACA,EAAG,UAAU,CAAC,CAC7BC,EAAG,IAAI,CAAC,MAAM,CAACA,EAAG,UAAU,CAAC,EAE7B,OAEF,IAAMG,EAAQgD,EAAmBpD,EAAG,IAAI,CAAEC,GAC1CC,EAAKD,EAAIG,AAAU,KAAVA,EAAeA,EAAQ2C,EAAe/C,EAAG,IAAI,EACxD,EAAG,wBACGsD,EAAyClD,EAAoB,AAACJ,IAClE,IAAIC,EAAY,EAChB,IAAK,IAAIC,EAAI,EAAGA,EAAIF,EAAQ,MAAM,CAAEE,IAClC,IAAK,IAAIE,EAAIF,EAAI,EAAGE,EAAIJ,EAAQ,MAAM,CAAEI,IAAK,CAC3C,IAAML,EAAKC,CAAO,CAACE,EAAE,CACfC,EAAKH,CAAO,CAACI,EAAE,AACjBL,CAAAA,EAAG,IAAI,GAAKI,EAAG,IAAI,EAGnBkD,EAAiBtD,EAAII,KACvBF,IACAkC,EAAqBpC,EAAII,EAAI8C,GAEjC,CAEF,OAAOhD,CACT,EAAG,0BACGsD,EAAgC,IAAItC,IACpCuC,EAA8BpD,EAAoB,AAACJ,IACvD,GAAIuD,EAAc,GAAG,CAACvD,GACpB,OAAOuD,EAAc,GAAG,CAACvD,GAE3B,IAAMC,EAAUe,CAAkB,CAAChB,EAAQ,CAC3C,GAAIC,AAAmB,IAAnBA,EAAQ,MAAM,CAAQ,CACxB,IAAMA,EAAQ,CAAE,KAAM,EAAG,UAAW,EAAG,KAAM,EAAG,MAAO,CAAE,EAEzD,OADAsD,EAAc,GAAG,CAACvD,EAASC,GACpBA,CACT,CAEA,IAAMC,EAAO,AADIa,CAAiB,CAACd,CAAO,CAAC,EAAE,CAAC,CACxB,IAAI,CAAC,KAAK,CAC5BG,EAAOF,EACX,IAAK,IAAIF,EAAM,EAAGA,EAAMC,EAAQ,MAAM,CAAED,IAAO,CAC7C,IAAMD,EAAMgB,CAAiB,CAACd,CAAO,CAACD,EAAI,CAAC,CAC3C,GAAID,AAAoB,eAApBA,EAAI,WAAW,CAAmB,CACpC,IAAMC,EAAaD,EAAI,IAAI,CACrBE,EAAaF,EAAI,EAAE,CACzBK,EAAOQ,KAAK,GAAG,CAACZ,EAAaE,GAAQU,KAAK,GAAG,CAACX,EAAaC,GAAQF,EAAaC,EAChF,KACF,CACF,CACA,IAAMF,EAAYa,KAAK,GAAG,CAACR,EAAOF,GAC5BC,EAAO,CAAEC,KAAAA,EAAML,UAAAA,EAAWG,KAAAA,EAAM,MAAOE,EAAOF,CAAK,EAEzD,OADAqD,EAAc,GAAG,CAACvD,EAASG,GACpBA,CACT,EAAG,eACGsD,EAA2CrD,EAAoB,KACnE,IAAIJ,EAAY,EACVC,EAAgC,IAAIgB,IAC1C,IAAK,GAAM,CAACjB,EAAGE,EAAE,GAAIC,EAAM,OAAO,GACK,IAAjCa,CAAkB,CAAChB,EAAE,CAAC,MAAM,EAG3BE,EAAE,KAAK,GAGR,AAACD,EAAc,GAAG,CAACC,EAAE,KAAK,GAC5BD,EAAc,GAAG,CAACC,EAAE,KAAK,CAAE,EAAE,EAE/BD,EAAc,GAAG,CAACC,EAAE,KAAK,EAAE,IAAI,CAACF,IAElC,IAAME,EAAkCE,EAAoB,AAACJ,IAC3D,IAAMC,EAAOE,CAAK,CAACH,EAAQ,CAC3B,GAAI,CAACC,EAAK,KAAK,EAAI,CAACA,EAAK,GAAG,CAC1B,OAAO,EAET,IAAMC,EAAUG,EAAS,GAAG,CAACJ,EAAK,KAAK,EACjCG,EAAUC,EAAS,GAAG,CAACJ,EAAK,GAAG,SACrC,AAAI,AAACC,GAAYE,EAKVQ,KAAK,GAAG,CAFJ,AAACR,CAAAA,EAAQ,CAAC,EAAI,GAAMF,CAAAA,EAAQ,CAAC,EAAI,IAEtBU,KAAK,GAAG,CADnB,AAACR,CAAAA,EAAQ,CAAC,EAAI,GAAMF,CAAAA,EAAQ,CAAC,EAAI,IAHnC,CAKX,EAAG,mBACH,IAAK,IAAME,KAAOH,EAAc,MAAM,GACpCG,EAAI,IAAI,CAAC,CAACJ,EAAGC,KACX,IAAMG,EAAQoD,EAAYxD,GACpBD,EAAQyD,EAAYvD,GAC1B,GAAIW,KAAK,GAAG,CAACR,EAAM,SAAS,CAAGL,EAAM,SAAS,EAAI,EAChD,OAAOK,EAAM,SAAS,CAAGL,EAAM,SAAS,CAE1C,GAAIa,KAAK,GAAG,CAACR,EAAM,IAAI,CAAGL,EAAM,IAAI,EAAI,EACtC,OAAOK,EAAM,IAAI,CAAGL,EAAM,IAAI,CAEhC,IAAMI,EAAQD,EAAgBF,GACxBK,EAAQH,EAAgBD,GAC9B,GAAIW,KAAK,GAAG,CAACT,EAAQE,GAAS,EAC5B,OAAOA,EAAQF,EAEjB,IAAMG,EAAOU,CAAkB,CAAChB,EAAE,CAAC,MAAM,CACnCO,EAAOS,CAAkB,CAACf,EAAE,CAAC,MAAM,CACzC,GAAIK,IAASC,EACX,OAAOD,EAAOC,EAEhB,GAAID,AAAS,IAATA,EAAY,CACd,IAAMJ,EAAOc,CAAkB,CAAChB,EAAE,CAAC,EAAE,CAC/BI,EAAOY,CAAkB,CAACf,EAAE,CAAC,EAAE,CACrC,GAAIc,CAAiB,CAACb,EAAK,EAAIa,CAAiB,CAACX,EAAK,CAAE,CACtD,IAAMJ,EAAOe,CAAiB,CAACb,EAAK,CAC9BD,EAAOc,CAAiB,CAACX,EAAK,CAC9BL,EAASa,KAAK,GAAG,CAACZ,EAAK,EAAE,CAAGA,EAAK,IAAI,EACrCG,EAASS,KAAK,GAAG,CAACX,EAAK,EAAE,CAAGA,EAAK,IAAI,EAC3C,GAAIW,KAAK,GAAG,CAACb,EAASI,GAAU,EAC9B,OAAOJ,EAASI,CAEpB,CACF,CACA,OAAO,CACT,GAEAH,GAAasD,EADGlD,EAAI,GAAG,CAAC,AAACJ,GAAOe,CAAiB,CAACC,CAAkB,CAAChB,EAAG,CAAC,EAAE,CAAC,GAG9E,OAAOA,CACT,EAAG,4BACG0D,EAA2CtD,EAAoB,KACnE,IAAIJ,EAAY,EACVC,EAAgC,IAAIgB,IAC1C,IAAK,GAAM,CAACjB,EAAGE,EAAE,GAAIC,EAAM,OAAO,GAET,IAAnB,AADYa,CAAkB,CAAChB,EAAE,CACzB,MAAM,EAGbE,EAAE,GAAG,GAGN,AAACD,EAAc,GAAG,CAACC,EAAE,GAAG,GAC1BD,EAAc,GAAG,CAACC,EAAE,GAAG,CAAE,EAAE,EAE7BD,EAAc,GAAG,CAACC,EAAE,GAAG,EAAE,IAAI,CAACF,IAEhC,IAAK,IAAME,KAAOD,EAAc,MAAM,GACpCC,EAAI,IAAI,CAAC,CAACF,EAAGC,KACX,IAAMC,EAA0BE,EAAoB,AAACJ,IACnD,IAAMC,EAAUe,CAAkB,CAAChB,EAAQ,CAC3C,GAAIC,EAAQ,MAAM,CAAG,EACnB,OAAO,EAET,IAAMC,EAAOa,CAAiB,CAACd,CAAO,CAACA,EAAQ,MAAM,CAAG,EAAE,CAAC,CAC3D,OAAOW,KAAK,GAAG,CAACV,EAAK,EAAE,CAAGA,EAAK,IAAI,CACrC,EAAG,WACGH,EAASG,EAAQF,GACjBG,EAASD,EAAQD,UACvB,AAAIW,KAAK,GAAG,CAACb,EAASI,GAAU,GACvBJ,EAASI,EAEXH,EAAIC,CACb,GAIAD,GAAasD,EAHGpD,EAAI,GAAG,CACrB,AAACF,GAAOe,CAAiB,CAACC,CAAkB,CAAChB,EAAG,CAACgB,CAAkB,CAAChB,EAAG,CAAC,MAAM,CAAG,EAAE,CAAC,GAIxF,OAAOA,CACT,EAAG,4BACG2D,EAAmCvD,EAAoB,KAC3D,IAAIJ,EAAY,EAChB,IAAK,IAAMC,KAAQM,EAAO,CACxB,IAAML,EAAe,EAAE,CACvB,IAAK,IAAMF,KAAKC,EAAK,MAAM,CACzB,IAAK,IAAMA,KAAOD,EAAE,QAAQ,CAAE,CAC5B,IAAMA,EAAMgB,CAAkB,CAACf,EAAI,SAAS,CAAC,CAAC,IAAI,CAChD,AAACD,GAAOe,CAAiB,CAACf,EAAG,CAAC,YAAY,GAAKC,EAAI,YAAY,CAE7DD,AAAQ,MAAK,IAAbA,GACFE,EAAa,IAAI,CAACa,CAAiB,CAACf,EAAI,CAE5C,CAEFE,EAAa,IAAI,CAAC,CAACF,EAAGC,IAAMD,EAAE,SAAS,CAAGC,EAAE,SAAS,EAAID,EAAE,YAAY,CAAGC,EAAE,YAAY,EACxF,IAAK,IAAIA,EAAI,EAAGA,EAAIC,EAAa,MAAM,CAAED,IACvC,IAAK,IAAIG,EAAIH,EAAI,EAAGG,EAAIF,EAAa,MAAM,CAAEE,IAAK,CAChD,IAAML,EAAKG,CAAY,CAACD,EAAE,CACpBE,EAAKD,CAAY,CAACE,EAAE,CACtBiD,EAAiBtD,EAAII,KACvBH,IACAmC,EAAqBpC,EAAII,EAAI6C,GAEjC,CAEJ,CACA,OAAOhD,CACT,EAAG,oBACC4D,EAAa,EAEjB,KAKE,AALKA,EADU,IAMX,AAAY,IAJF,EACHH,IACAC,IACAC,KAJiB,EAQ5BC,GACF,CACA,IAAMC,EAAgC,IAAI5C,IAC1C,IAAK,IAAMjB,KAAQO,EAAO,CACxB,IAAMN,EAAW,EAAE,CACnBD,EAAK,MAAM,CAAC,OAAO,CAAC,AAACA,IACnBA,EAAE,QAAQ,CAAC,OAAO,CAAC,AAACE,IAClBD,EAAS,IAAI,CAAC,CACZ,UAAWC,EAAE,SAAS,CACtB,aAAcA,EAAE,YAAY,CAC5B,WAAYF,EAAE,KAAK,CACnB,KAAME,EAAE,IAAI,CACZ,GAAIA,EAAE,EAAE,AACV,EACF,EACF,GACAD,EAAS,IAAI,CAAC,CAACD,EAAGC,IAAMD,EAAE,IAAI,CAAGC,EAAE,IAAI,EACvC,IAAMC,EAAW,EAAE,CACnB,GAAID,EAAS,MAAM,CAAG,EAAG,CACvB,IAAID,EAAiB,CAACC,CAAQ,CAAC,EAAE,CAAC,CAC9BG,EAAaH,CAAQ,CAAC,EAAE,CAAC,EAAE,CAC/B,IAAK,IAAIF,EAAI,EAAGA,EAAIE,EAAS,MAAM,CAAEF,IAAK,CACxC,IAAMI,EAAIF,CAAQ,CAACF,EAAE,AACjBI,CAAAA,EAAE,IAAI,CAAGC,GACXJ,EAAe,IAAI,CAACG,GACpBC,EAAaQ,KAAK,GAAG,CAACR,EAAYD,EAAE,EAAE,IAEtCD,EAAS,IAAI,CAACF,GACdA,EAAiB,CAACG,EAAE,CACpBC,EAAaD,EAAE,EAAE,CAErB,CACAD,EAAS,IAAI,CAACF,EAChB,CACA,IAAK,IAAMC,KAAWC,EAAU,CAC9B,IAAMA,EAA6B,IAAIyG,IACvC1G,EAAQ,OAAO,CAAC,AAACD,GAAME,EAAW,GAAG,CAACF,EAAE,UAAU,GAClD,IAAMD,EAA8B,IAAIkB,IACxChB,EAAQ,OAAO,CAAC,AAACD,IACf,IAAMC,EAAOuD,EAAYxD,EAAE,SAAS,EACpCD,EAAY,GAAG,CAACC,EAAE,UAAU,CAAE,AAACD,CAAAA,EAAY,GAAG,CAACC,EAAE,UAAU,GAAK,GAAKC,EAAK,KAAK,CACjF,GACA,IAAME,EAAa,IAAID,EAAW,CAAC,MAAM,CAAC,AAACF,GAAM,AAACD,CAAAA,EAAY,GAAG,CAACC,IAAM,GAAK,IACvEK,EAAc,IAAIH,EAAW,CAAC,MAAM,CAAC,AAACF,GAAM,AAACD,CAAAA,EAAY,GAAG,CAACC,IAAM,GAAK,GACxEM,EAAgB,IAAIJ,EAAW,CAAC,MAAM,CAAC,AAACF,GAAMY,AAAqC,GAArCA,KAAK,GAAG,CAACb,EAAY,GAAG,CAACC,IAAM,IACnFG,EAAW,IAAI,CAAC,CAACH,EAAGC,IAAM,AAACF,CAAAA,EAAY,GAAG,CAACE,IAAM,GAAMF,CAAAA,EAAY,GAAG,CAACC,IAAM,IAC7EK,EAAY,IAAI,CAAC,CAACL,EAAGC,IAAM,AAACF,CAAAA,EAAY,GAAG,CAACC,IAAM,GAAMD,CAAAA,EAAY,GAAG,CAACE,IAAM,IAC9E,IAAMM,EAA8BH,EAAoB,CAACF,EAAYE,KACnEH,EAAQ,MAAM,CAAC,AAACD,GAAMA,EAAE,UAAU,GAAKE,GAAY,OAAO,CAAC,AAACD,IAC1D,IAAMC,EAAiBgB,EAAuB,GAAG,CAACjB,EAAE,SAAS,EAAID,EAAK,KAAK,CAAGI,EAC9EyD,EAAc,GAAG,CAAC,CAAC,EAAE5D,EAAE,SAAS,CAAC,CAAC,EAAEA,EAAE,YAAY,CAAC,CAAC,CAAEC,EACxD,EACF,EAAG,eACCM,EAAY,EAChB,IAAK,IAAMP,KAAcE,EACvBK,IACAD,EAAYN,EAAYD,EAAK,KAAK,CAAGQ,AAtzCzB,GAszCyBA,GAEvC,GAAIF,AAAyB,IAAzBA,EAAc,MAAM,EAAUJ,EAAW,IAAI,CAAG,EAAG,CACrD,IAAMF,EAAY,IAAIE,EAAW,CAAC,IAAI,CACpC,CAACF,EAAGC,IAAMW,KAAK,GAAG,CAACb,EAAY,GAAG,CAACC,IAAM,GAAKY,KAAK,GAAG,CAACb,EAAY,GAAG,CAACE,IAAM,GAC9E,CAAC,EAAE,CACEA,EAAUE,EAAW,OAAO,CAACH,EAC/BC,AAAY,MAAZA,GACFE,EAAW,MAAM,CAACF,EAAS,GAE7B,IAAMG,EAAWC,EAAY,OAAO,CAACL,EACjCI,AAAa,MAAbA,GACFC,EAAY,MAAM,CAACD,EAAU,GAE/BE,EAAc,IAAI,CAACN,EACrB,CACA,IAAIS,EAAkB,EACtB,IAAK,IAAMR,KAAcK,EAAe,CACtC,GAAIG,AAAoB,IAApBA,EACFF,EAAYN,EAAYD,EAAK,KAAK,MAC7B,CACL,IAAME,EAAMO,EAAkB,GAAM,EAAI,EAAI,GACtCL,EAAYQ,KAAK,IAAI,CAACH,EAAkB,GAC9CF,EAAYN,EAAYD,EAAK,KAAK,CAAGE,EAAME,EAANF,EACvC,CACAO,GACF,CACA,IAAIC,EAAa,EACjB,IAAK,IAAMT,KAAcI,EACvBK,IACAH,EAAYN,EAAYD,EAAK,KAAK,CAAGU,AAp1CzB,GAo1CyBA,EAEzC,CACF,CACA,IAAK,GAAM,CAACV,EAAGC,EAAE,GAAIE,EAAM,OAAO,GAAI,CACpC,IAAMD,EAAUc,CAAkB,CAAChB,EAAE,EAAI,EAAE,CAC3C,GAAIE,AAAmB,IAAnBA,EAAQ,MAAM,CAChB,SAEF,IAAME,EAAY,EAAE,CAGd,CAAEL,SAAAA,CAAQ,CAAEI,SAAAA,CAAQ,CAAE,CAAGyC,EAAa5C,EAFhCK,EAAS,GAAG,CAACJ,EAAE,KAAK,EACpBI,EAAS,GAAG,CAACJ,EAAE,GAAG,GAExBK,EAAQJ,EAAQ,GAAG,CAAC,AAACF,IACzB,IAAMC,EAAIc,CAAiB,CAACf,EAAI,CAC1BE,EAAQ2D,EAAc,GAAG,CAAC,CAAC,EAAE5D,EAAE,SAAS,CAAC,CAAC,EAAEA,EAAE,YAAY,CAAC,CAAC,GAAKA,EAAE,IAAI,CAAC,KAAK,CACnF,MAAO,CACL,OAAQA,EAAE,WAAW,CACrBC,MAAAA,EACA,KAAMD,EAAE,IAAI,CACZ,GAAIA,EAAE,EAAE,AACV,CACF,GACAG,EAAU,IAAI,CAACL,GACf,IAAK,IAAIC,EAAI,EAAGA,EAAIM,EAAM,MAAM,CAAEN,IAAK,CACrC,IAAMC,EAAOK,CAAK,CAACN,EAAE,CACfE,EAASE,CAAS,CAACA,EAAU,MAAM,CAAG,EAAE,CACxCL,EAAYE,AAAgB,aAAhBA,EAAK,MAAM,CAAkBC,EAAO,CAAC,CAAGA,EAAO,CAAC,CAC5DC,EAAiBF,AAAgB,aAAhBA,EAAK,MAAM,CAAkBC,EAAO,CAAC,CAAGA,EAAO,CAAC,CACjEG,EAAWC,CAAK,CAACN,EAAI,EAAE,CACvBO,EAAcP,EAAIM,EAAM,MAAM,CAAG,EAIvC,GAHIM,KAAK,GAAG,CAACT,EAAiBF,EAAK,KAAK,EA31FnC,MA41FHG,EAAU,IAAI,CAAC4L,GAAY/L,EAAMF,IAE/BQ,GAAeF,EAAS,MAAM,GAAKJ,EAAK,MAAM,CAChD,GAAIW,KAAK,GAAG,CAACX,EAAK,KAAK,CAAGI,EAAS,KAAK,EA/1FrC,KA+1F+C,CAChD,IAAML,EAAWC,AAAgB,aAAhBA,EAAK,MAAM,CAAkB,AAACF,CAAAA,EAAYM,EAAS,IAAI,AAAD,EAAK,EAAI0L,GAAwB9L,EAAMI,GAC9GD,EAAU,IAAI,CAAC4L,GAAY/L,EAAMD,GAAWgM,GAAY3L,EAAUL,GACpE,KAAWA,CAAAA,AAAM,IAANA,GAAWA,IAAMM,EAAM,MAAM,CAAG,IACzCF,EAAU,IAAI,CAAC4L,GAAY/L,EAAM8L,GAAwB9L,EAAMI,UAE5D,GAAIE,EACTH,EAAU,IAAI,CAAC4L,GAAY/L,EAAMI,EAAS,KAAK,OAC1C,CACL,IAAML,EAAWY,KAAK,GAAG,CAACX,EAAK,IAAI,CAAGF,GAAaa,KAAK,GAAG,CAACX,EAAK,EAAE,CAAGF,GAAaE,EAAK,EAAE,CAAGA,EAAK,IAAI,CACtGG,EAAU,IAAI,CAAC4L,GAAY/L,EAAMD,GACnC,CACF,CACA,IAAMO,EAAOH,CAAS,CAACA,EAAU,MAAM,CAAG,EAAE,AACxCQ,CAAAA,CAAAA,KAAK,GAAG,CAACL,EAAK,CAAC,CAAGJ,EAAS,CAAC,EA72FzB,MA62FqCS,KAAK,GAAG,CAACL,EAAK,CAAC,CAAGJ,EAAS,CAAC,EA72FjE,IA62FwE,GAC7EC,EAAU,IAAI,CAACD,GAEjB,IAAMK,EAAW,EAAE,AACfJ,CAAAA,EAAU,MAAM,CAAG,GACrBI,EAAS,IAAI,CAACJ,CAAS,CAAC,EAAE,EAE5B,IAAK,IAAIJ,EAAI,EAAGA,EAAII,EAAU,MAAM,CAAEJ,IAAK,CACzC,IAAMC,EAAIG,CAAS,CAACJ,EAAE,CAChBE,EAAOM,CAAQ,CAACA,EAAS,MAAM,CAAG,EAAE,AACtCI,CAAAA,CAAAA,KAAK,GAAG,CAACX,EAAE,CAAC,CAAGC,EAAK,CAAC,EAv3FpB,MAu3FgCU,KAAK,GAAG,CAACX,EAAE,CAAC,CAAGC,EAAK,CAAC,EAv3FrD,IAu3F4D,GAC/DM,EAAS,IAAI,CAACP,EAElB,CACAA,EAAE,MAAM,CAAGO,CACb,CACA,IAAK,IAAMR,KAAMG,EAAO,CACtB,IAAMF,EAAOD,EAAG,cAAc,AAC1BC,CAAAA,GAAQD,EAAG,MAAM,EACnBC,CAAAA,EAAK,MAAM,CAAGD,EAAG,MAAM,AAAD,CAE1B,CACAA,EAAK,KAAK,CAAG,AAACA,CAAAA,EAAK,KAAK,EAAI,EAAE,AAAD,EAAG,MAAM,CAAC,AAACA,GAAM,CAACA,EAAE,YAAY,EAC7D,IAAM8D,GAAoC1D,EAAoB,CAACJ,EAAGC,KAChE,IAAMC,EAAKD,EAAK,CAAC,EAAI,EACfG,EAAKH,EAAK,CAAC,EAAI,EACfF,EAAIE,EAAK,KAAK,EAAI,EAClBE,EAAIF,EAAK,MAAM,EAAI,EACzB,GAAIF,GAAK,GAAKI,GAAK,EACjB,OAAOH,EAET,IAAMK,EAAOH,EAAKH,EAAI,EAChBO,EAAQJ,EAAKH,EAAI,EACjBQ,EAAMH,EAAKD,EAAI,EACfK,EAASJ,EAAKD,EAAI,EACxB,GAAIH,EAAE,CAAC,CAAGK,GAAQL,EAAE,CAAC,CAAGM,GAASN,EAAE,CAAC,CAAGO,GAAOP,EAAE,CAAC,CAAGQ,EAClD,OAAOR,EAET,IAAMS,EAAQT,EAAE,CAAC,CAAGK,EACdK,EAASJ,EAAQN,EAAE,CAAC,CACpBW,EAAOX,EAAE,CAAC,CAAGO,EAEbO,EAAOF,KAAK,GAAG,CAACH,EAAOC,EAAQC,EADrBH,EAASR,EAAE,CAAC,SAE5B,AAAIc,IAASL,EACJ,CAAE,EAAGJ,EAAM,EAAGL,EAAE,CAAC,AAAC,EAEvBc,IAASJ,EACJ,CAAE,EAAGJ,EAAO,EAAGN,EAAE,CAAC,AAAC,EAExBc,IAASH,EACJ,CAAE,EAAGX,EAAE,CAAC,CAAE,EAAGO,CAAI,EAEnB,CAAE,EAAGP,EAAE,CAAC,CAAE,EAAGQ,CAAO,CAC7B,EAAG,qBACH,IAAK,IAAMP,KAAQD,EAAK,KAAK,CAAE,CAC7B,IAAMA,EAAMC,EAAK,MAAM,CACvB,GAAI,CAACD,GAAOA,EAAI,MAAM,CAAG,EACvB,SAEF,IAAME,EAAQD,EAAK,KAAK,CAClBG,EAAQH,EAAK,GAAG,CAChBF,EAAMG,EAAQG,EAAS,GAAG,CAACH,GAAS,KAAK,EACzCC,EAAMC,EAAQC,EAAS,GAAG,CAACD,GAAS,KAAK,CAC3CL,CAAAA,GACFC,CAAAA,CAAG,CAAC,EAAE,CAAG8D,GAAkB9D,CAAG,CAAC,EAAE,CAAED,EAAG,EAEpCI,GACFH,CAAAA,CAAG,CAACA,EAAI,MAAM,CAAG,EAAE,CAAG8D,GAAkB9D,CAAG,CAACA,EAAI,MAAM,CAAG,EAAE,CAAEG,EAAG,CAEpE,CACA,OAAOH,CACT,CAIA,SAASmM,GAAqBnM,CAAW,EACvC,OAAOA,EAAY,SAAS,EAAI,IAClC,CAEA,SAASoM,GAAsBpM,CAAW,EACxC,IAAMC,EAAI8B,EAAY/B,GAChBE,EAAUF,EAAY,MAAM,CAAC,SAAS,EAAE,aAAe,GACvDI,EAAWJ,EAAY,MAAM,CAAC,SAAS,EAAE,aAAe,IACxDD,EAAuBC,EAAY,MAAM,CAAC,QAAQ,EAAE,sBAAwB,GAC5EG,EAA4BH,EAAY,MAAM,CAAC,QAAQ,EAAE,0BAA4B,GACrFK,EAAwBL,EAAY,MAAM,CAAC,QAAQ,EAAE,uBAAyB,GAC9EM,EAAY6L,GAAqBnM,GACjC,CAAEO,QAAAA,CAAO,CAAEC,YAAAA,CAAW,CAAE,CAAGqL,GAAe5L,EAAG,CACjDC,QAAAA,EACAE,SAAAA,EACAL,qBAAAA,EACA,yBAA0BI,EAC1BE,sBAAAA,EACAC,UAAAA,CACF,GAEA,IAAK,IAAMP,KADXiC,EAAsB/B,EAAGM,EAASC,EAAa,CAAEN,QAAAA,EAASE,SAAAA,CAAS,GAChDJ,EAAY,KAAK,EAAI,EAAE,EACxC,OAAOD,EAAK,MAAM,CAGpB,IAAK,IAAME,KADXgM,GAAqBjM,EAAaM,GACfN,EAAY,KAAK,EAAI,EAAE,EACpC,AAACC,EAAK,KAAK,EAAIA,AAAe,UAAfA,EAAK,KAAK,EAC3BA,CAAAA,EAAK,KAAK,CAAG,SAAQ,EAKzB,OAFA+H,GAA0BhI,EAAaM,GACvCyH,GAAwB/H,GACjBM,CACT,CAIA,SAAS+L,GAAsBrM,CAAW,EACxC8B,EAA0B9B,GAC1B,IAAMC,EAAkBgC,EAAqBjC,EAC7CA,CAAAA,EAAY,KAAK,CAAGC,EAAgB,KAAK,CACzCD,EAAY,KAAK,CAAGC,EAAgB,KAAK,AAC3C,CAp2FAG,EAAoB8I,GAAkB,oBAyBtC9I,EAAoB+I,GAAc,gBAYlC/I,EAAoBgJ,GAAgB,kBAyBpChJ,EAAoBiJ,GAAkB,oBAkCtCjJ,EAAoBkJ,GAAe,iBAenClJ,EAAoBmJ,GAAU,YAuL9BnJ,EAAoBoJ,GAA2B,6BAuB/CpJ,EAAoBqJ,GAAuB,yBAQ3CrJ,EAAoBsJ,GAAqB,uBAwCzCtJ,EAAoBuJ,GAAsB,wBAiB1CvJ,EAAoBwJ,GAAmB,qBA2CvCxJ,EAAoByJ,GAAoB,sBAoBxCzJ,EAAoB0J,GAA0B,4BAe9C1J,EAAoB2J,GAA+B,iCA6BnD3J,EAAoB4J,GAAgB,kBAwCpC5J,EAAoB6J,GAA0B,4BA+D9C7J,EAAoB8J,GAAwB,0BA+C5C9J,EAAoB+J,GAA0B,4BAuF9C/J,EAAoBgK,GAAsB,wBAuB1ChK,EAAoBiK,GAA+B,iCAsCnDjK,EAAoBkK,GAA+B,iCAoDnDlK,EAAoBmK,GAAoB,sBAcxCnK,EAAoBoK,GAAQ,UAQ5BpK,EAAoBqK,GAAY,cAiBhCrK,EAAoBsK,GAAsB,wBAM1CtK,EAAoBuK,GAAsB,wBAgB1CvK,EAAoBwK,GAAgC,iCAiBpDxK,EAAoByK,GAAiB,mBA4CrCzK,EAAoB2K,GAAc,gBA8ClC3K,EAAoB4K,GAAkB,oBAkBtC5K,EAAoB6K,GAAa,eA8JjC7K,EAAoB8K,GAAmB,qBAYvC9K,EAAoB+K,GAAY,cAWhC/K,EAAoBgL,GAAY,cAUhChL,EAAoBiL,GAAsB,wBAQ1CjL,EAAoBkL,GAAgB,kBAiBpClL,EAAoBmL,GAAqB,uBAuCzCnL,EAAoBoL,GAAwB,0BA2B5CpL,EAAoBqL,GAAc,gBAOlCrL,EAAoBsL,GAAmB,qBAKvCtL,EAAoBuL,GAAgB,kBAuBpCvL,EAAoBwL,GAAsB,wBAmC1CxL,EAAoByL,GAAgB,kBA6BpCzL,EAAoB0L,GAAsB,wBAI1C1L,EAAoB2L,GAAyB,2BAI7C3L,EAAoB4L,GAAa,eAi7CjC5L,EAAoB6L,GAAsB,wBAM1C7L,EAAoB+L,GAAsB,wBA+B1C/L,EAAoBgM,GAAuB,yBAS3ChM,EAAoBiM,GAAuB,yBAC3C,IAAIC,GAASrM,EAA2B,CACtC,cAAeoM,GACf,cAAeD,GACf,WAAY/K,CACd,UAGSkL,MAAAA,MAAM"}{"version":3,"sources":["/home/rome/.rome/default/projects/apps/account-growth/node_modules/.pnpm/mermaid@11.17.0/node_modules/mermaid/dist/chunks/mermaid.core/swimlanes-XN3QIQJK.mjs"],"sourcesContent":["import {\n  createCommonLayoutRenderer\n} from \"./chunk-DSNCTWBM.mjs\";\nimport \"./chunk-JQ64N6SF.mjs\";\nimport {\n  markerOffsets\n} from \"./chunk-R7TYR2AO.mjs\";\nimport \"./chunk-OBVCFTLP.mjs\";\nimport \"./chunk-TEH6E4GO.mjs\";\nimport \"./chunk-P2QGCYS3.mjs\";\nimport \"./chunk-GMAD6QVW.mjs\";\nimport \"./chunk-PWAF6VOD.mjs\";\nimport \"./chunk-75Z2AOVW.mjs\";\nimport \"./chunk-DU6HZSFF.mjs\";\nimport {\n  log\n} from \"./chunk-X3CZISLH.mjs\";\nimport {\n  __name\n} from \"./chunk-Y2CYZVJY.mjs\";\n\n// src/rendering-util/rendering-elements/lineJump.ts\nvar ROUNDED_CORNER_RADIUS = 5;\nvar CORNER_EPSILON = 1e-5;\nvar ENDPOINT_EPSILON = 1e-6;\nfunction buildSegmentList(points) {\n  const segments = [];\n  for (let i = 0; i < points.length - 1; i++) {\n    segments.push({ a: points[i], b: points[i + 1] });\n  }\n  return segments;\n}\n__name(buildSegmentList, \"buildSegmentList\");\nfunction segmentIntersection(a1, a2, b1, b2) {\n  const dxA = a2.x - a1.x;\n  const dyA = a2.y - a1.y;\n  const dxB = b2.x - b1.x;\n  const dyB = b2.y - b1.y;\n  const denom = dxA * dyB - dyA * dxB;\n  if (denom === 0) {\n    return null;\n  }\n  const dx = b1.x - a1.x;\n  const dy = b1.y - a1.y;\n  const tA = (dx * dyB - dy * dxB) / denom;\n  const tB = (dx * dyA - dy * dxA) / denom;\n  if (tA <= ENDPOINT_EPSILON || tA >= 1 - ENDPOINT_EPSILON || tB <= ENDPOINT_EPSILON || tB >= 1 - ENDPOINT_EPSILON) {\n    return null;\n  }\n  return {\n    point: { x: a1.x + tA * dxA, y: a1.y + tA * dyA },\n    tA,\n    tB\n  };\n}\n__name(segmentIntersection, \"segmentIntersection\");\nfunction isHorizontalSeg(seg) {\n  return Math.abs(seg.b.x - seg.a.x) >= Math.abs(seg.b.y - seg.a.y);\n}\n__name(isHorizontalSeg, \"isHorizontalSeg\");\nfunction findEdgeIntersections(edges) {\n  const crossings = [];\n  for (let i = 0; i < edges.length; i++) {\n    const edgeA = edges[i];\n    const segmentsA = buildSegmentList(edgeA.points);\n    for (let j = i + 1; j < edges.length; j++) {\n      const edgeB = edges[j];\n      const segmentsB = buildSegmentList(edgeB.points);\n      for (const [si, segA] of segmentsA.entries()) {\n        for (const [sj, segB] of segmentsB.entries()) {\n          const hit = segmentIntersection(segA.a, segA.b, segB.a, segB.b);\n          if (!hit) {\n            continue;\n          }\n          const aHoriz = isHorizontalSeg(segA);\n          const bHoriz = isHorizontalSeg(segB);\n          const orthogonalPair = aHoriz !== bHoriz;\n          const jumpOnA = orthogonalPair ? aHoriz : false;\n          if (jumpOnA) {\n            crossings.push({\n              jumpEdgeId: edgeA.id,\n              otherEdgeId: edgeB.id,\n              segIndex: si,\n              t: hit.tA,\n              point: hit.point\n            });\n          } else {\n            crossings.push({\n              jumpEdgeId: edgeB.id,\n              otherEdgeId: edgeA.id,\n              segIndex: sj,\n              t: hit.tB,\n              point: hit.point\n            });\n          }\n        }\n      }\n    }\n  }\n  return crossings;\n}\n__name(findEdgeIntersections, \"findEdgeIntersections\");\nfunction fmt(n) {\n  const rounded = Math.round(n * 1e3) / 1e3;\n  return Number.isInteger(rounded) ? `${rounded}` : `${rounded}`;\n}\n__name(fmt, \"fmt\");\nfunction pointToString(p) {\n  return `${fmt(p.x)},${fmt(p.y)}`;\n}\n__name(pointToString, \"pointToString\");\nfunction getArcSweepFlag(seg) {\n  const dx = seg.b.x - seg.a.x;\n  const dy = seg.b.y - seg.a.y;\n  if (Math.abs(dx) >= Math.abs(dy)) {\n    return dx >= 0 ? 1 : 0;\n  }\n  return dy >= 0 ? 1 : 0;\n}\n__name(getArcSweepFlag, \"getArcSweepFlag\");\nvar MIN_JUMP_RADIUS = 1e-3;\nfunction applyMarkerOffsets(points, edge) {\n  if (points.length < 2) {\n    return points.map((p) => ({ ...p }));\n  }\n  const out = points.map((p) => ({ ...p }));\n  const startOff = edge.arrowTypeStart && markerOffsets[edge.arrowTypeStart];\n  if (startOff) {\n    const a = points[0];\n    const b = points[1];\n    const ang = Math.atan2(b.y - a.y, b.x - a.x);\n    out[0].x = a.x + startOff * Math.cos(ang);\n    out[0].y = a.y + startOff * Math.sin(ang);\n  }\n  const endOff = edge.arrowTypeEnd && markerOffsets[edge.arrowTypeEnd];\n  if (endOff) {\n    const n = points.length;\n    const a = points[n - 2];\n    const b = points[n - 1];\n    const ang = Math.atan2(b.y - a.y, b.x - a.x);\n    out[n - 1].x = b.x - endOff * Math.cos(ang);\n    out[n - 1].y = b.y - endOff * Math.sin(ang);\n  }\n  return out;\n}\n__name(applyMarkerOffsets, \"applyMarkerOffsets\");\nfunction emitJump(jump, ux, uy, sweep, style) {\n  const cx = jump.point.x;\n  const cy = jump.point.y;\n  const pre = { x: cx - ux * jump.r, y: cy - uy * jump.r };\n  const post = { x: cx + ux * jump.r, y: cy + uy * jump.r };\n  const out = [`L${pointToString(pre)}`];\n  if (style === \"arc\") {\n    out.push(`A${fmt(jump.r)},${fmt(jump.r)} 0 0 ${sweep} ${pointToString(post)}`);\n  } else {\n    out.push(`M${pointToString(post)}`);\n  }\n  return out;\n}\n__name(emitJump, \"emitJump\");\nfunction computeRoundedCorner(prev, curr, next, radius) {\n  const dx1 = curr.x - prev.x;\n  const dy1 = curr.y - prev.y;\n  const dx2 = next.x - curr.x;\n  const dy2 = next.y - curr.y;\n  const len1 = Math.hypot(dx1, dy1);\n  const len2 = Math.hypot(dx2, dy2);\n  if (len1 < CORNER_EPSILON || len2 < CORNER_EPSILON) {\n    return null;\n  }\n  const nx1 = dx1 / len1;\n  const ny1 = dy1 / len1;\n  const nx2 = dx2 / len2;\n  const ny2 = dy2 / len2;\n  const dot = nx1 * nx2 + ny1 * ny2;\n  const clamped = Math.max(-1, Math.min(1, dot));\n  const angle = Math.acos(clamped);\n  if (angle < CORNER_EPSILON || Math.abs(Math.PI - angle) < CORNER_EPSILON) {\n    return null;\n  }\n  const cutLen = Math.min(radius / Math.sin(angle / 2), len1 / 2, len2 / 2);\n  return {\n    startX: curr.x - nx1 * cutLen,\n    startY: curr.y - ny1 * cutLen,\n    endX: curr.x + nx2 * cutLen,\n    endY: curr.y + ny2 * cutLen,\n    ctrlX: curr.x,\n    ctrlY: curr.y,\n    cutLen\n  };\n}\n__name(computeRoundedCorner, \"computeRoundedCorner\");\nfunction rewriteEdgePath(edge, jumps, config) {\n  const rawPoints = edge.points;\n  if (rawPoints.length < 2) {\n    return \"\";\n  }\n  const points = applyMarkerOffsets(rawPoints, edge);\n  const rounded = edge.curve === \"rounded\";\n  const segments = buildSegmentList(points);\n  const bySeg = /* @__PURE__ */ new Map();\n  for (const j of jumps) {\n    const seg = segments[j.segIndex];\n    if (!seg) {\n      continue;\n    }\n    const segLen = Math.hypot(seg.b.x - seg.a.x, seg.b.y - seg.a.y);\n    const list = bySeg.get(j.segIndex) ?? [];\n    list.push({\n      t: j.t,\n      point: j.point,\n      d: j.t * segLen,\n      r: config.jumpRadius\n    });\n    bySeg.set(j.segIndex, list);\n  }\n  const parts = [`M${pointToString(points[0])}`];\n  for (let i = 0; i < segments.length; i++) {\n    const seg = segments[i];\n    const segLen = Math.hypot(seg.b.x - seg.a.x, seg.b.y - seg.a.y);\n    const ux = segLen === 0 ? 0 : (seg.b.x - seg.a.x) / segLen;\n    const uy = segLen === 0 ? 0 : (seg.b.y - seg.a.y) / segLen;\n    const sweep = getArcSweepFlag(seg);\n    let segStartConsumed = 0;\n    if (rounded && i > 0) {\n      const corner = computeRoundedCorner(\n        points[i - 1],\n        points[i],\n        points[i + 1] ?? points[i],\n        ROUNDED_CORNER_RADIUS\n      );\n      if (corner) {\n        segStartConsumed = corner.cutLen;\n      }\n    }\n    let segEndStop = segLen;\n    let upcomingCorner = null;\n    if (rounded && i < segments.length - 1) {\n      upcomingCorner = computeRoundedCorner(\n        points[i],\n        points[i + 1],\n        points[i + 2] ?? points[i + 1],\n        ROUNDED_CORNER_RADIUS\n      );\n      if (upcomingCorner) {\n        segEndStop = segLen - upcomingCorner.cutLen;\n      }\n    }\n    const segJumps = [...bySeg.get(i) ?? []].sort((a, b) => a.t - b.t);\n    for (const j of segJumps) {\n      j.r = Math.min(j.r, j.d - segStartConsumed, segEndStop - j.d);\n    }\n    for (let k = 0; k < segJumps.length - 1; k++) {\n      const gap = segJumps[k + 1].d - segJumps[k].d;\n      if (segJumps[k].r + segJumps[k + 1].r > gap) {\n        const half = gap / 2;\n        segJumps[k].r = Math.min(segJumps[k].r, half);\n        segJumps[k + 1].r = Math.min(segJumps[k + 1].r, half);\n      }\n    }\n    for (const j of segJumps) {\n      if (j.r < MIN_JUMP_RADIUS) {\n        continue;\n      }\n      parts.push(...emitJump(j, ux, uy, sweep, config.jumpStyle));\n    }\n    if (rounded && upcomingCorner) {\n      parts.push(`L${fmt(upcomingCorner.startX)},${fmt(upcomingCorner.startY)}`);\n      parts.push(\n        `Q${fmt(upcomingCorner.ctrlX)},${fmt(upcomingCorner.ctrlY)} ${fmt(upcomingCorner.endX)},${fmt(upcomingCorner.endY)}`\n      );\n    } else {\n      parts.push(`L${pointToString(seg.b)}`);\n    }\n  }\n  return parts.join(\" \");\n}\n__name(rewriteEdgePath, \"rewriteEdgePath\");\nfunction isStraightPath(d) {\n  return /^[\\d\\s+,.LMelm-]*$/.test(d);\n}\n__name(isStraightPath, \"isStraightPath\");\nfunction curveSupportsLineHops(curve) {\n  if (!curve) {\n    return true;\n  }\n  return curve === \"linear\" || curve === \"rounded\" || curve === \"step\" || curve === \"stepBefore\" || curve === \"stepAfter\";\n}\n__name(curveSupportsLineHops, \"curveSupportsLineHops\");\nfunction decodeDataPoints(raw) {\n  if (!raw) {\n    return null;\n  }\n  try {\n    const json = typeof atob === \"function\" ? atob(raw) : Buffer.from(raw, \"base64\").toString();\n    const parsed = JSON.parse(json);\n    if (!Array.isArray(parsed)) {\n      return null;\n    }\n    const pts = [];\n    for (const p of parsed) {\n      if (p && typeof p.x === \"number\" && typeof p.y === \"number\") {\n        pts.push({ x: p.x, y: p.y });\n      }\n    }\n    return pts.length >= 2 ? pts : null;\n  } catch {\n    return null;\n  }\n}\n__name(decodeDataPoints, \"decodeDataPoints\");\nfunction applyLineJumpsToSvg(edgePathsGroup, edges, config) {\n  if (!config.enabled) {\n    return;\n  }\n  const groupNode = edgePathsGroup.node();\n  if (!groupNode) {\n    return;\n  }\n  const edgeMeta = /* @__PURE__ */ new Map();\n  for (const e of edges) {\n    edgeMeta.set(e.id, e);\n  }\n  const renderedEdges = [];\n  const pathById = /* @__PURE__ */ new Map();\n  for (const e of edges) {\n    const escapedId = typeof CSS !== \"undefined\" && CSS.escape ? CSS.escape(e.id) : e.id;\n    const pathEl = groupNode.querySelector(`path[data-id=\"${escapedId}\"]`);\n    if (!pathEl) {\n      continue;\n    }\n    pathById.set(e.id, pathEl);\n    const decoded = decodeDataPoints(pathEl.getAttribute(\"data-points\"));\n    const points = decoded ?? e.points;\n    renderedEdges.push({ ...e, points });\n  }\n  const crossings = findEdgeIntersections(renderedEdges);\n  if (crossings.length === 0) {\n    return;\n  }\n  const jumpsByEdge = /* @__PURE__ */ new Map();\n  for (const c of crossings) {\n    const list = jumpsByEdge.get(c.jumpEdgeId) ?? [];\n    list.push(c);\n    jumpsByEdge.set(c.jumpEdgeId, list);\n  }\n  for (const renderedEdge of renderedEdges) {\n    const jumps = jumpsByEdge.get(renderedEdge.id);\n    if (!jumps || jumps.length === 0) {\n      continue;\n    }\n    const meta = edgeMeta.get(renderedEdge.id);\n    const curveHint = meta?.curve;\n    if (curveHint !== void 0 && !curveSupportsLineHops(curveHint)) {\n      continue;\n    }\n    const pathEl = pathById.get(renderedEdge.id);\n    if (!pathEl) {\n      continue;\n    }\n    if (curveHint === void 0) {\n      const currentD = pathEl.getAttribute(\"d\") ?? \"\";\n      if (!isStraightPath(currentD)) {\n        continue;\n      }\n    }\n    const originalStyle = pathEl.getAttribute(\"style\") ?? \"\";\n    const dasharrayMatch = /stroke-dasharray\\s*:\\s*0\\s+([\\d.]+)\\s+[\\d.]+\\s+([\\d.]+)/.exec(\n      originalStyle\n    );\n    const preservedOValueS = dasharrayMatch ? Number.parseFloat(dasharrayMatch[1]) : null;\n    const preservedOValueE = dasharrayMatch ? Number.parseFloat(dasharrayMatch[2]) : null;\n    const newD = rewriteEdgePath(renderedEdge, jumps, config);\n    pathEl.setAttribute(\"d\", newD);\n    if (preservedOValueS !== null && preservedOValueE !== null && typeof pathEl.getTotalLength === \"function\") {\n      const newLen = pathEl.getTotalLength();\n      const onLen = Math.max(0, newLen - preservedOValueS - preservedOValueE);\n      const newDasharray = `0 ${preservedOValueS} ${onLen} ${preservedOValueE}`;\n      const cleaned = originalStyle.replace(/stroke-dasharray\\s*:[^;]*;?/g, `stroke-dasharray: ${newDasharray};`).replace(/;\\s*;+/g, \";\");\n      pathEl.setAttribute(\"style\", cleaned);\n    }\n  }\n}\n__name(applyLineJumpsToSvg, \"applyLineJumpsToSvg\");\n\n// src/rendering-util/layout-algorithms/swimlanes/adjustLayout.ts\nfunction applySwimlaneLineJumps(data4Layout, { measure }) {\n  const lineHopsConfig = data4Layout.config?.swimlane?.lineHops;\n  if (lineHopsConfig === false) {\n    return;\n  }\n  const jumpStyle = lineHopsConfig === \"gap\" ? \"gap\" : \"arc\";\n  const edgeGeometries = data4Layout.edges.filter((edge) => Array.isArray(edge.points) && edge.points.length >= 2).map((edge) => ({\n    id: edge.id,\n    points: edge.points,\n    curve: edge.curve,\n    arrowTypeStart: edge.arrowTypeStart,\n    arrowTypeEnd: edge.arrowTypeEnd\n  }));\n  applyLineJumpsToSvg(measure.groups.edgePaths, edgeGeometries, {\n    enabled: true,\n    jumpRadius: 6,\n    jumpStyle\n  });\n}\n__name(applySwimlaneLineJumps, \"applySwimlaneLineJumps\");\n\n// src/rendering-util/layout-algorithms/swimlanes/helpers.ts\nvar DEFAULT_SWIMLANE_ID = \"__swimlane_default__\";\nvar TOP_LANE_TITLE_BAND_HEIGHT = 21;\nvar MIN_TOP_LANE_HORIZONTAL_PADDING = 20;\nfunction topLaneHorizontalPadding(lane) {\n  return Math.max(lane.padding ?? MIN_TOP_LANE_HORIZONTAL_PADDING, MIN_TOP_LANE_HORIZONTAL_PADDING);\n}\n__name(topLaneHorizontalPadding, \"topLaneHorizontalPadding\");\nfunction assignTopLaneTitleRect(lane) {\n  const { x, y, width, height } = lane;\n  const contentTop = lane.swimlaneContentTop;\n  if (typeof x !== \"number\" || typeof y !== \"number\" || typeof width !== \"number\" || typeof height !== \"number\" || typeof contentTop !== \"number\" || !Number.isFinite(x) || !Number.isFinite(y) || !Number.isFinite(width) || !Number.isFinite(height) || !Number.isFinite(contentTop) || width <= 0 || height <= 0) {\n    delete lane.groupTitleRect;\n    return;\n  }\n  const top = y - height / 2;\n  const headerBottom = Math.min(contentTop, y + height / 2);\n  const titleHeight = Math.min(TOP_LANE_TITLE_BAND_HEIGHT, Math.max(0, headerBottom - top));\n  const bottom = top + titleHeight;\n  if (bottom <= top) {\n    delete lane.groupTitleRect;\n    return;\n  }\n  lane.groupTitleRect = {\n    left: x - width / 2,\n    right: x + width / 2,\n    top,\n    bottom\n  };\n}\n__name(assignTopLaneTitleRect, \"assignTopLaneTitleRect\");\nfunction prepareLayoutForSwimlanes(layout) {\n  const direction = layout.direction;\n  const nodes = layout.nodes ??= [];\n  for (const node of layout.nodes ?? []) {\n    if (node.isGroup && !node.parentId) {\n      node.shape = \"swimlane\";\n      if (direction) {\n        node.direction = direction;\n      }\n    }\n  }\n  const looseNodes = nodes.filter((node) => !node.isGroup && !node.parentId);\n  if (looseNodes.length === 0) {\n    return;\n  }\n  let defaultLane = nodes.find((node) => node.id === DEFAULT_SWIMLANE_ID);\n  if (!defaultLane) {\n    defaultLane = {\n      id: DEFAULT_SWIMLANE_ID,\n      label: \"\",\n      isGroup: true,\n      shape: \"swimlane\",\n      padding: 20,\n      ...direction ? { direction } : {}\n    };\n    nodes.push(defaultLane);\n  } else if (defaultLane.isGroup) {\n    defaultLane.shape = \"swimlane\";\n    if (direction) {\n      defaultLane.direction = direction;\n    }\n  }\n  for (const node of looseNodes) {\n    node.parentId = DEFAULT_SWIMLANE_ID;\n  }\n}\n__name(prepareLayoutForSwimlanes, \"prepareLayoutForSwimlanes\");\nfunction toGraphView(layout) {\n  const nodeById = /* @__PURE__ */ new Map();\n  for (const n of layout.nodes ?? []) {\n    nodeById.set(n.id, n);\n  }\n  const edges = [];\n  for (const e of layout.edges ?? []) {\n    const src = typeof e.start === \"string\" ? e.start : void 0;\n    const dst = typeof e.end === \"string\" ? e.end : void 0;\n    if (!src || !dst) {\n      continue;\n    }\n    if (e.labelNodeId) {\n      continue;\n    }\n    edges.push({ id: e.id, src, dst, ref: e });\n  }\n  const allNodes = layout.nodes ?? [];\n  const groupNodes = allNodes.filter((n) => n.isGroup);\n  const nonGroupNodes = allNodes.filter((n) => !n.isGroup);\n  const nodesInGroupOrder = [...groupNodes].reverse();\n  const nodes = [...nodesInGroupOrder, ...nonGroupNodes].map((n) => n.id);\n  return { nodes, edges, layout, nodeById };\n}\n__name(toGraphView, \"toGraphView\");\nfunction writeBackToLayoutData(g, ordered, coords, opts) {\n  const { layout } = g;\n  const nodeMap = g.nodeById;\n  const layerGap = opts?.layerGap ?? 100;\n  const nodeGap = opts?.nodeGap ?? 40;\n  let layerIndex = 0;\n  for (const layer of ordered.layers) {\n    let orderIndex = 0;\n    for (const id of layer) {\n      const node = nodeMap.get(id);\n      if (!node) {\n        orderIndex++;\n        continue;\n      }\n      node.layer = layerIndex;\n      node.order = orderIndex;\n      const x = coords.x[id] ?? orderIndex * nodeGap;\n      const y = coords.y[id] ?? layerIndex * layerGap;\n      node.x = x;\n      node.y = y;\n      orderIndex++;\n    }\n    layerIndex++;\n  }\n  const allNodes = layout.nodes ?? [];\n  const groupBounds = /* @__PURE__ */ new Map();\n  const topLevelGroups = [];\n  for (const group of allNodes) {\n    if (!group?.isGroup) {\n      continue;\n    }\n    if (!group.parentId) {\n      topLevelGroups.push(group);\n    }\n    const children = allNodes.filter((n) => n.parentId === group.id);\n    let minX = Infinity;\n    let maxX = -Infinity;\n    let minY = Infinity;\n    let maxY = -Infinity;\n    for (const child of children) {\n      const cx = child.x ?? coords.x[child.id];\n      const cy = child.y ?? coords.y[child.id];\n      const cw = child.width ?? 0;\n      const ch = child.height ?? 0;\n      if (cx != null && cy != null) {\n        minX = Math.min(minX, cx - cw / 2);\n        maxX = Math.max(maxX, cx + cw / 2);\n        minY = Math.min(minY, cy - ch / 2);\n        maxY = Math.max(maxY, cy + ch / 2);\n      }\n    }\n    if (minX === Infinity || minY === Infinity) {\n      group.x = group.x ?? 0;\n      group.y = group.y ?? 0;\n      group.width = group.width ?? 0;\n      group.height = group.height ?? 0;\n    } else {\n      const pad = group.padding ?? 20;\n      const horizontalPad = group.parentId ? pad : 2 * topLaneHorizontalPadding(group);\n      const verticalPad = pad;\n      const w = Math.max(0, maxX - minX) + horizontalPad;\n      const h = Math.max(0, maxY - minY) + verticalPad;\n      const cx = (minX + maxX) / 2;\n      const cy = (minY + maxY) / 2;\n      group.x = cx;\n      group.y = cy;\n      group.width = w;\n      group.height = h;\n      groupBounds.set(group.id, { minX, maxX, minY, maxY });\n    }\n  }\n  if (topLevelGroups.length > 0 && groupBounds.size > 0) {\n    let globalMinY = Infinity;\n    let globalMaxY = -Infinity;\n    let maxPad = 0;\n    for (const lane of topLevelGroups) {\n      const pad = lane.padding ?? 20;\n      if (pad > maxPad) {\n        maxPad = pad;\n      }\n      const b = groupBounds.get(lane.id);\n      if (!b) {\n        continue;\n      }\n      globalMinY = Math.min(globalMinY, b.minY);\n      globalMaxY = Math.max(globalMaxY, b.maxY);\n    }\n    if (globalMinY !== Infinity && globalMaxY !== -Infinity) {\n      const contentHeight = Math.max(0, globalMaxY - globalMinY);\n      const minHeaderMargin = 36;\n      const verticalMargin = Math.max(maxPad, minHeaderMargin);\n      const laneHeight = contentHeight + 2 * verticalMargin;\n      const centerY = (globalMinY + globalMaxY) / 2;\n      for (const lane of topLevelGroups) {\n        lane.y = centerY;\n        lane.height = laneHeight;\n        lane.swimlaneContentTop = globalMinY;\n      }\n      const sortedLanes = [...topLevelGroups].sort((a, b) => {\n        const ax = a.x ?? 0;\n        const bx = b.x ?? 0;\n        return ax - bx;\n      });\n      const laneIds = [];\n      const centers = [];\n      const baseWidths = [];\n      for (const lane of sortedLanes) {\n        const b = groupBounds.get(lane.id);\n        if (!b) {\n          continue;\n        }\n        const contentWidth = Math.max(0, b.maxX - b.minX) + 2 * topLaneHorizontalPadding(lane);\n        const cx = (b.minX + b.maxX) / 2;\n        laneIds.push(lane.id);\n        centers.push(cx);\n        baseWidths.push(contentWidth);\n      }\n      const count = laneIds.length;\n      if (count > 0) {\n        const laneWidths = /* @__PURE__ */ new Map();\n        if (count === 1) {\n          laneWidths.set(laneIds[0], baseWidths[0]);\n        } else {\n          const d = [];\n          for (let i = 0; i < count - 1; i++) {\n            d.push(centers[i + 1] - centers[i]);\n          }\n          const u = new Array(count);\n          u[0] = 0;\n          for (let i = 0; i < count - 1; i++) {\n            u[i + 1] = 2 * d[i] - u[i];\n          }\n          let lowerBound = 0;\n          let upperBound = Number.POSITIVE_INFINITY;\n          for (let i = 0; i < count; i++) {\n            const baseW = baseWidths[i];\n            if (i % 2 === 0) {\n              lowerBound = Math.max(lowerBound, baseW - u[i]);\n            } else {\n              upperBound = Math.min(upperBound, u[i] - baseW);\n            }\n          }\n          let x = lowerBound;\n          if (lowerBound <= upperBound) {\n            x = (lowerBound + upperBound) / 2;\n          } else {\n            x = lowerBound;\n          }\n          for (let i = 0; i < count; i++) {\n            const w = u[i] + (i % 2 === 0 ? x : -x);\n            const finalWidth = Math.max(baseWidths[i], w);\n            laneWidths.set(laneIds[i], finalWidth);\n          }\n        }\n        for (const lane of topLevelGroups) {\n          const w = laneWidths.get(lane.id);\n          if (w != null) {\n            lane.width = w;\n          }\n          assignTopLaneTitleRect(lane);\n        }\n      }\n    }\n  }\n}\n__name(writeBackToLayoutData, \"writeBackToLayoutData\");\n\n// src/rendering-util/layout-algorithms/swimlanes/edgeLabelNodes.ts\nvar EDGE_LABEL_LOG_PREFIX = \"[EdgeLabelNodes]\";\nfunction createEdgeLabelNodes(data) {\n  const nodesToAdd = [];\n  const layoutOnlyEdges = [];\n  const nodeById = /* @__PURE__ */ new Map();\n  for (const node of data.nodes) {\n    nodeById.set(node.id, node);\n  }\n  for (const edge of data.edges) {\n    if (!edge.label || edge.label.length === 0) {\n      continue;\n    }\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    if (edge.labelNodeId) {\n      continue;\n    }\n    const sourceNode = edge.start ? nodeById.get(edge.start) : void 0;\n    const targetNode = edge.end ? nodeById.get(edge.end) : void 0;\n    if (!sourceNode || !targetNode) {\n      log.warn(EDGE_LABEL_LOG_PREFIX, `Edge ${edge.id} has missing source or target node`);\n      continue;\n    }\n    const labelNodeId = `edge-label-${edge.start}-${edge.end}-${edge.id}`;\n    const isCrossLane = sourceNode.parentId !== targetNode.parentId;\n    const labelLane = isCrossLane ? targetNode.parentId : sourceNode.parentId;\n    const labelNode = {\n      id: labelNodeId,\n      label: edge.label,\n      edgeStart: edge.start ?? \"\",\n      edgeEnd: edge.end ?? \"\",\n      shape: \"labelRect\",\n      width: 0,\n      // populated when rendered / applied from fixture\n      height: 0,\n      isEdgeLabel: true,\n      isDummy: true,\n      parentId: labelLane,\n      isGroup: false,\n      labelStyle: Array.isArray(edge.labelStyle) ? edge.labelStyle[0] : edge.labelStyle ?? \"\",\n      ...sourceNode.dir ? { dir: sourceNode.dir } : {}\n    };\n    nodesToAdd.push(labelNode);\n    edge.labelNodeId = labelNodeId;\n    edge.label = void 0;\n    edge.text = void 0;\n    const toLabelVirtual = {\n      id: `${edge.id}-to-label`,\n      start: edge.start,\n      end: labelNodeId,\n      type: \"normal\",\n      isLayoutOnly: true\n    };\n    const fromLabelVirtual = {\n      id: `${edge.id}-from-label`,\n      start: labelNodeId,\n      end: edge.end,\n      type: \"normal\",\n      isLayoutOnly: true\n    };\n    layoutOnlyEdges.push(toLabelVirtual, fromLabelVirtual);\n  }\n  const newNodes = [...data.nodes, ...nodesToAdd];\n  const newEdges = [...data.edges, ...layoutOnlyEdges];\n  return {\n    ...data,\n    nodes: newNodes,\n    edges: newEdges\n  };\n}\n__name(createEdgeLabelNodes, \"createEdgeLabelNodes\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/geometry.ts\nvar EPS = 1e-3;\nfunction measuredNodeRect(node) {\n  const cx = node.x ?? 0;\n  const cy = node.y ?? 0;\n  const width = node.width ?? 0;\n  const height = node.height ?? 0;\n  return width > 0 && height > 0 ? { cx, cy, rect: rectFromCenterSize(cx, cy, width, height) } : void 0;\n}\n__name(measuredNodeRect, \"measuredNodeRect\");\nfunction nodeBoundsInfoFor(node) {\n  if (node.isGroup) {\n    return void 0;\n  }\n  const measured = measuredNodeRect(node);\n  if (!measured) {\n    return void 0;\n  }\n  const id = String(node.id ?? \"\");\n  return {\n    id,\n    cx: measured.cx,\n    cy: measured.cy,\n    rect: measured.rect\n  };\n}\n__name(nodeBoundsInfoFor, \"nodeBoundsInfoFor\");\nfunction samePoint(a, b, epsilon = EPS) {\n  return Math.abs(a.x - b.x) < epsilon && Math.abs(a.y - b.y) < epsilon;\n}\n__name(samePoint, \"samePoint\");\nfunction sameX(a, b, epsilon = EPS) {\n  return Math.abs(a.x - b.x) < epsilon;\n}\n__name(sameX, \"sameX\");\nfunction sameY(a, b, epsilon = EPS) {\n  return Math.abs(a.y - b.y) < epsilon;\n}\n__name(sameY, \"sameY\");\nfunction isHorizontalSegment(a, b, epsilon = EPS) {\n  return sameY(a, b, epsilon) && Math.abs(a.x - b.x) > epsilon;\n}\n__name(isHorizontalSegment, \"isHorizontalSegment\");\nfunction isVerticalSegment(a, b, epsilon = EPS) {\n  return sameX(a, b, epsilon) && Math.abs(a.y - b.y) > epsilon;\n}\n__name(isVerticalSegment, \"isVerticalSegment\");\nfunction overlapLength(a1, a2, b1, b2) {\n  return Math.max(\n    0,\n    Math.min(Math.max(a1, a2), Math.max(b1, b2)) - Math.max(Math.min(a1, a2), Math.min(b1, b2))\n  );\n}\n__name(overlapLength, \"overlapLength\");\nfunction sameAxisSegmentOverlapLength(a, b, epsilon = EPS) {\n  if (a.horizontal && b.horizontal && sameY(a.a, b.a, epsilon)) {\n    return overlapLength(a.a.x, a.b.x, b.a.x, b.b.x);\n  }\n  if (a.vertical && b.vertical && sameX(a.a, b.a, epsilon)) {\n    return overlapLength(a.a.y, a.b.y, b.a.y, b.b.y);\n  }\n  return 0;\n}\n__name(sameAxisSegmentOverlapLength, \"sameAxisSegmentOverlapLength\");\nfunction orthogonalSegmentsForPoints(points, epsilon = EPS) {\n  const result = [];\n  for (let i = 0; i < points.length - 1; i++) {\n    const a = points[i];\n    const b = points[i + 1];\n    const horizontal = isHorizontalSegment(a, b, epsilon);\n    const vertical = isVerticalSegment(a, b, epsilon);\n    if (horizontal || vertical) {\n      result.push({ index: i, a, b, horizontal, vertical });\n    }\n  }\n  return result;\n}\n__name(orthogonalSegmentsForPoints, \"orthogonalSegmentsForPoints\");\nfunction countOrthogonalBends(points, epsilon = EPS) {\n  const segments = orthogonalSegmentsForPoints(points, epsilon);\n  let bends = 0;\n  for (let i = 1; i < segments.length; i++) {\n    if (segments[i - 1].horizontal !== segments[i].horizontal) {\n      bends++;\n    }\n  }\n  return bends;\n}\n__name(countOrthogonalBends, \"countOrthogonalBends\");\nfunction dedupeConsecutivePoints(points, epsilon = EPS) {\n  const result = [];\n  for (const point of points) {\n    const last = result.length > 0 ? result[result.length - 1] : void 0;\n    if (!last || !samePoint(last, point, epsilon)) {\n      result.push({ x: point.x, y: point.y });\n    }\n  }\n  return result;\n}\n__name(dedupeConsecutivePoints, \"dedupeConsecutivePoints\");\nfunction classifyThreeSegmentRoute(points, epsilon = EPS) {\n  if (!points || points.length !== 4) {\n    return void 0;\n  }\n  const [p0, p1, p2, p3] = points;\n  const isHVH = isHorizontalSegment(p0, p1, epsilon) && isVerticalSegment(p1, p2, epsilon) && isHorizontalSegment(p2, p3, epsilon);\n  if (isHVH) {\n    return { kind: \"HVH\", p0, p1, p2, p3 };\n  }\n  const isVHV = isVerticalSegment(p0, p1, epsilon) && isHorizontalSegment(p1, p2, epsilon) && isVerticalSegment(p2, p3, epsilon);\n  return isVHV ? { kind: \"VHV\", p0, p1, p2, p3 } : void 0;\n}\n__name(classifyThreeSegmentRoute, \"classifyThreeSegmentRoute\");\nfunction segmentBoundsOverlapRect(a, b, rect, buffer = 0) {\n  const segMinX = Math.min(a.x, b.x);\n  const segMaxX = Math.max(a.x, b.x);\n  const segMinY = Math.min(a.y, b.y);\n  const segMaxY = Math.max(a.y, b.y);\n  return segMaxX > rect.left - buffer && segMinX < rect.right + buffer && segMaxY > rect.top - buffer && segMinY < rect.bottom + buffer;\n}\n__name(segmentBoundsOverlapRect, \"segmentBoundsOverlapRect\");\nfunction pointInsideRect(point, rect, buffer = 0) {\n  return point.x > rect.left + buffer && point.x < rect.right - buffer && point.y > rect.top + buffer && point.y < rect.bottom - buffer;\n}\n__name(pointInsideRect, \"pointInsideRect\");\nfunction rectContainsRect(outer, inner) {\n  return outer.left <= inner.left && outer.right >= inner.right && outer.top <= inner.top && outer.bottom >= inner.bottom;\n}\n__name(rectContainsRect, \"rectContainsRect\");\nfunction rectsOverlap(a, b) {\n  return a.left < b.right && a.right > b.left && a.top < b.bottom && a.bottom > b.top;\n}\n__name(rectsOverlap, \"rectsOverlap\");\nfunction inflateRect(rect, margin) {\n  return {\n    left: rect.left - margin,\n    right: rect.right + margin,\n    top: rect.top - margin,\n    bottom: rect.bottom + margin\n  };\n}\n__name(inflateRect, \"inflateRect\");\nfunction rectFromCenterSize(cx, cy, width, height) {\n  return {\n    left: cx - width / 2,\n    right: cx + width / 2,\n    top: cy - height / 2,\n    bottom: cy + height / 2\n  };\n}\n__name(rectFromCenterSize, \"rectFromCenterSize\");\nfunction rectOfNodeBounds(node) {\n  return measuredNodeRect(node)?.rect;\n}\n__name(rectOfNodeBounds, \"rectOfNodeBounds\");\nfunction portForRectSide(node, side) {\n  switch (side) {\n    case \"top\":\n      return { x: node.cx, y: node.rect.top };\n    case \"bottom\":\n      return { x: node.cx, y: node.rect.bottom };\n    case \"left\":\n      return { x: node.rect.left, y: node.cy };\n    case \"right\":\n      return { x: node.rect.right, y: node.cy };\n  }\n}\n__name(portForRectSide, \"portForRectSide\");\nfunction buildOrthogonalPortPath(src, srcSide, dst, dstSide, anchor, epsilon = EPS) {\n  const srcH = srcSide === \"left\" || srcSide === \"right\";\n  const dstH = dstSide === \"left\" || dstSide === \"right\";\n  if (srcH && dstH) {\n    const opposingDir = srcSide === \"right\" && dstSide === \"left\" && src.x < dst.x || srcSide === \"left\" && dstSide === \"right\" && src.x > dst.x;\n    if (opposingDir) {\n      if (sameY(src, dst, epsilon)) {\n        return [src, dst];\n      }\n      const midX = (src.x + dst.x) / 2;\n      return [src, { x: midX, y: src.y }, { x: midX, y: dst.y }, dst];\n    }\n    if (srcSide === dstSide) {\n      if (sameY(src, dst, epsilon)) {\n        return void 0;\n      }\n      const intX = srcSide === \"left\" ? Math.min(src.x, dst.x) - anchor : Math.max(src.x, dst.x) + anchor;\n      return [src, { x: intX, y: src.y }, { x: intX, y: dst.y }, dst];\n    }\n    return void 0;\n  }\n  if (!srcH && !dstH) {\n    if (srcSide === dstSide) {\n      if (sameX(src, dst, epsilon)) {\n        return void 0;\n      }\n      const intY = srcSide === \"top\" ? Math.min(src.y, dst.y) - anchor : Math.max(src.y, dst.y) + anchor;\n      return [src, { x: src.x, y: intY }, { x: dst.x, y: intY }, dst];\n    }\n    const sameDir = srcSide === \"bottom\" && dstSide === \"top\" && src.y < dst.y || srcSide === \"top\" && dstSide === \"bottom\" && src.y > dst.y;\n    if (!sameDir) {\n      return void 0;\n    }\n    if (sameX(src, dst, epsilon)) {\n      return [src, dst];\n    }\n    const midY = (src.y + dst.y) / 2;\n    return [src, { x: src.x, y: midY }, { x: dst.x, y: midY }, dst];\n  }\n  if (srcH && !dstH) {\n    const sameDirSrc2 = srcSide === \"right\" && dst.x > src.x || srcSide === \"left\" && dst.x < src.x;\n    const sameDirDst2 = dstSide === \"top\" && src.y < dst.y || dstSide === \"bottom\" && src.y > dst.y;\n    return sameDirSrc2 && sameDirDst2 ? [src, { x: dst.x, y: src.y }, dst] : void 0;\n  }\n  const sameDirSrc = srcSide === \"bottom\" && dst.y > src.y || srcSide === \"top\" && dst.y < src.y;\n  const sameDirDst = dstSide === \"left\" && src.x < dst.x || dstSide === \"right\" && src.x > dst.x;\n  return sameDirSrc && sameDirDst ? [src, { x: src.x, y: dst.y }, dst] : void 0;\n}\n__name(buildOrthogonalPortPath, \"buildOrthogonalPortPath\");\nfunction buildSameSideTrackPath(src, side, dst, track) {\n  return side === \"left\" || side === \"right\" ? [src, { x: track, y: src.y }, { x: track, y: dst.y }, dst] : [src, { x: src.x, y: track }, { x: dst.x, y: track }, dst];\n}\n__name(buildSameSideTrackPath, \"buildSameSideTrackPath\");\nfunction collectRealNodeBounds(nodes) {\n  const nodeInfoById = /* @__PURE__ */ new Map();\n  const realNodeRects = [];\n  for (const node of nodes) {\n    if (node.isEdgeLabel) {\n      continue;\n    }\n    const info = nodeBoundsInfoFor(node);\n    if (!info) {\n      continue;\n    }\n    nodeInfoById.set(info.id, info);\n    realNodeRects.push({ id: info.id, rect: info.rect });\n  }\n  return { nodeInfoById, realNodeRects };\n}\n__name(collectRealNodeBounds, \"collectRealNodeBounds\");\nfunction collectNodeRectEntries(nodes) {\n  const realNodeRects = [];\n  const labelNodeRects = [];\n  for (const node of nodes) {\n    const info = nodeBoundsInfoFor(node);\n    if (!info) {\n      continue;\n    }\n    const entry = { id: info.id, rect: info.rect };\n    if (node.isEdgeLabel) {\n      labelNodeRects.push(entry);\n    } else {\n      realNodeRects.push(entry);\n    }\n  }\n  return { realNodeRects, labelNodeRects };\n}\n__name(collectNodeRectEntries, \"collectNodeRectEntries\");\nfunction collectLayoutNodeRects(nodes, { includeEdgeLabels = true } = {}) {\n  const result = [];\n  for (const node of nodes) {\n    if (node.isGroup || !includeEdgeLabels && node.isEdgeLabel) {\n      continue;\n    }\n    const cx = node.x ?? 0;\n    const cy = node.y ?? 0;\n    const width = node.width ?? 0;\n    const height = node.height ?? 0;\n    result.push({\n      nodeId: node.id,\n      ...rectFromCenterSize(cx, cy, width, height)\n    });\n  }\n  return result;\n}\n__name(collectLayoutNodeRects, \"collectLayoutNodeRects\");\nfunction getNodePairGeometry(edge, nodeInfoById, epsilon = EPS) {\n  const srcId = edge.start;\n  const dstId = edge.end;\n  if (!srcId || !dstId) {\n    return void 0;\n  }\n  const srcInfo = nodeInfoById.get(srcId);\n  const dstInfo = nodeInfoById.get(dstId);\n  if (!srcInfo || !dstInfo) {\n    return void 0;\n  }\n  return {\n    srcId,\n    dstId,\n    srcInfo,\n    dstInfo,\n    collinearX: Math.abs(srcInfo.cx - dstInfo.cx) < epsilon,\n    collinearY: Math.abs(srcInfo.cy - dstInfo.cy) < epsilon\n  };\n}\n__name(getNodePairGeometry, \"getNodePairGeometry\");\nfunction segmentHitsAnyRect(a, b, rects, excludeIds = [], shrink = 0) {\n  for (const entry of rects) {\n    if (excludeIds.includes(entry.id)) {\n      continue;\n    }\n    if (segmentBoundsOverlapRect(a, b, entry.rect, -shrink)) {\n      return true;\n    }\n  }\n  return false;\n}\n__name(segmentHitsAnyRect, \"segmentHitsAnyRect\");\nfunction orthogonalSegmentsCross(a1, b1, a2, b2, epsilon = EPS, endpointTolerance = 1e-6) {\n  const s1H = sameY(a1, b1, epsilon);\n  const s1V = sameX(a1, b1, epsilon);\n  const s2H = sameY(a2, b2, epsilon);\n  const s2V = sameX(a2, b2, epsilon);\n  if (s1H && s2H || s1V && s2V) {\n    return false;\n  }\n  if (!(s1H || s1V) || !(s2H || s2V)) {\n    return false;\n  }\n  const horiz = s1H ? { a: a1, b: b1 } : { a: a2, b: b2 };\n  const vert = s1V ? { a: a1, b: b1 } : { a: a2, b: b2 };\n  const hY = horiz.a.y;\n  const hX1 = Math.min(horiz.a.x, horiz.b.x);\n  const hX2 = Math.max(horiz.a.x, horiz.b.x);\n  const vX = vert.a.x;\n  const vY1 = Math.min(vert.a.y, vert.b.y);\n  const vY2 = Math.max(vert.a.y, vert.b.y);\n  if (vX < hX1 || vX > hX2 || hY < vY1 || hY > vY2) {\n    return false;\n  }\n  const matchesHorizEndpoint = Math.abs(vX - horiz.a.x) < endpointTolerance && Math.abs(hY - horiz.a.y) < endpointTolerance || Math.abs(vX - horiz.b.x) < endpointTolerance && Math.abs(hY - horiz.b.y) < endpointTolerance;\n  const matchesVertEndpoint = Math.abs(vX - vert.a.x) < endpointTolerance && Math.abs(hY - vert.a.y) < endpointTolerance || Math.abs(vX - vert.b.x) < endpointTolerance && Math.abs(hY - vert.b.y) < endpointTolerance;\n  return !(matchesHorizEndpoint && matchesVertEndpoint);\n}\n__name(orthogonalSegmentsCross, \"orthogonalSegmentsCross\");\nfunction sameAxisSegmentsOverlap(a1, b1, a2, b2, epsilon = EPS) {\n  const s1H = sameY(a1, b1, epsilon);\n  const s1V = sameX(a1, b1, epsilon);\n  const s2H = sameY(a2, b2, epsilon);\n  const s2V = sameX(a2, b2, epsilon);\n  if (s1V && s2V && sameX(a1, a2, epsilon)) {\n    return overlapLength(a1.y, b1.y, a2.y, b2.y) > epsilon;\n  }\n  if (s1H && s2H && sameY(a1, a2, epsilon)) {\n    return overlapLength(a1.x, b1.x, a2.x, b2.x) > epsilon;\n  }\n  return false;\n}\n__name(sameAxisSegmentsOverlap, \"sameAxisSegmentsOverlap\");\nfunction segmentConflictsWithAnyEdge(a, b, edges, excludeEdge, {\n  epsilon = EPS,\n  skipDegenerateOther = false\n} = {}) {\n  for (const other of edges) {\n    if (other === excludeEdge || other.isLayoutOnly) {\n      continue;\n    }\n    const points = other.points;\n    if (!points || points.length < 2) {\n      continue;\n    }\n    for (let i = 0; i < points.length - 1; i++) {\n      const oa = points[i];\n      const ob = points[i + 1];\n      if (skipDegenerateOther && samePoint(oa, ob, epsilon)) {\n        continue;\n      }\n      if (orthogonalSegmentsCross(a, b, oa, ob, epsilon) || sameAxisSegmentsOverlap(a, b, oa, ob, epsilon)) {\n        return true;\n      }\n    }\n  }\n  return false;\n}\n__name(segmentConflictsWithAnyEdge, \"segmentConflictsWithAnyEdge\");\nfunction orthogonalSegmentsStrictlyCross(a1, b1, a2, b2, epsilon = EPS) {\n  const aHoriz = sameY(a1, b1, epsilon);\n  const aVert = sameX(a1, b1, epsilon);\n  const bHoriz = sameY(a2, b2, epsilon);\n  const bVert = sameX(a2, b2, epsilon);\n  if (!(aHoriz && bVert || aVert && bHoriz)) {\n    return false;\n  }\n  const horiz = aHoriz ? { a: a1, b: b1 } : { a: a2, b: b2 };\n  const vert = aHoriz ? { a: a2, b: b2 } : { a: a1, b: b1 };\n  const hY = horiz.a.y;\n  const hXmin = Math.min(horiz.a.x, horiz.b.x);\n  const hXmax = Math.max(horiz.a.x, horiz.b.x);\n  const vX = vert.a.x;\n  const vYmin = Math.min(vert.a.y, vert.b.y);\n  const vYmax = Math.max(vert.a.y, vert.b.y);\n  return vX > hXmin + epsilon && vX < hXmax - epsilon && hY > vYmin + epsilon && hY < vYmax - epsilon;\n}\n__name(orthogonalSegmentsStrictlyCross, \"orthogonalSegmentsStrictlyCross\");\nfunction strictlyBetween(value, a, b) {\n  const lo = Math.min(a, b);\n  const hi = Math.max(a, b);\n  return value > lo + EPS && value < hi - EPS;\n}\n__name(strictlyBetween, \"strictlyBetween\");\nfunction isCollinearIntermediate(prev, cur, next) {\n  if (sameX(prev, cur) && sameX(cur, next)) {\n    return strictlyBetween(cur.y, prev.y, next.y);\n  }\n  if (sameY(prev, cur) && sameY(cur, next)) {\n    return strictlyBetween(cur.x, prev.x, next.x);\n  }\n  return false;\n}\n__name(isCollinearIntermediate, \"isCollinearIntermediate\");\nfunction simplifyPolylineOnce(points) {\n  let changed = false;\n  const out = [];\n  for (let i = 0; i < points.length; i++) {\n    const prev = out[out.length - 1];\n    const cur = points[i];\n    const next = i + 1 < points.length ? points[i + 1] : void 0;\n    if (prev && next) {\n      if (samePoint(prev, next)) {\n        i++;\n        changed = true;\n        continue;\n      }\n      if (isCollinearIntermediate(prev, cur, next)) {\n        changed = true;\n        continue;\n      }\n    }\n    out.push(cur);\n  }\n  return { points: out, changed };\n}\n__name(simplifyPolylineOnce, \"simplifyPolylineOnce\");\nfunction orthogonalizePolyline(pts) {\n  const cleaned = [pts[0]];\n  for (let i = 1; i < pts.length; i++) {\n    const prev = cleaned[cleaned.length - 1];\n    const curr = pts[i];\n    if (!sameX(prev, curr) && !sameY(prev, curr)) {\n      const prevPrev = cleaned.length >= 2 ? cleaned[cleaned.length - 2] : void 0;\n      const incomingVertical = prevPrev ? sameX(prevPrev, prev) : false;\n      const corner = incomingVertical ? { x: prev.x, y: curr.y } : { x: curr.x, y: prev.y };\n      cleaned.push(corner);\n    }\n    cleaned.push(curr);\n  }\n  const deduped = [];\n  for (const p of cleaned) {\n    const last = deduped[deduped.length - 1];\n    if (!last || !samePoint(last, p)) {\n      deduped.push(p);\n    }\n  }\n  return deduped;\n}\n__name(orthogonalizePolyline, \"orthogonalizePolyline\");\nfunction simplifyPolyline(pts) {\n  if (pts.length < 3) {\n    return pts;\n  }\n  let work = [...pts];\n  for (let guard = 0; guard < 32; guard++) {\n    const result = simplifyPolylineOnce(work);\n    work = result.points;\n    if (!result.changed) {\n      break;\n    }\n  }\n  return work;\n}\n__name(simplifyPolyline, \"simplifyPolyline\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/endpointClip.ts\nvar EPS2 = 1e-3;\nvar INSIDE_EPS = 0.5;\nvar CORNER_CLEARANCE = 4;\nfunction endpointContextFor(edge, nodeByIdMap, minPoints) {\n  const candidate = edge;\n  if (candidate.isLayoutOnly || !candidate.points || candidate.points.length < minPoints) {\n    return void 0;\n  }\n  const src = candidate.start ? nodeByIdMap.get(candidate.start) : void 0;\n  const dst = candidate.end ? nodeByIdMap.get(candidate.end) : void 0;\n  return {\n    edge: candidate,\n    points: candidate.points,\n    srcRect: src ? rectOfNodeBounds(src) : void 0,\n    dstRect: dst ? rectOfNodeBounds(dst) : void 0\n  };\n}\n__name(endpointContextFor, \"endpointContextFor\");\nfunction segmentEnterPoint(outside, inside, r) {\n  if (sameY(outside, inside, EPS2)) {\n    const x = outside.x < r.left ? r.left : r.right;\n    return { x, y: outside.y };\n  }\n  if (sameX(outside, inside, EPS2)) {\n    const y = outside.y < r.top ? r.top : r.bottom;\n    return { x: outside.x, y };\n  }\n  return {\n    x: Math.min(r.right, Math.max(r.left, outside.x)),\n    y: Math.min(r.bottom, Math.max(r.top, outside.y))\n  };\n}\n__name(segmentEnterPoint, \"segmentEnterPoint\");\nfunction clipEndpoint(points, rect, atStart) {\n  const step = atStart ? 1 : -1;\n  let outsideIndex = atStart ? 0 : points.length - 1;\n  while (outsideIndex >= 0 && outsideIndex < points.length && pointInsideRect(points[outsideIndex], rect, INSIDE_EPS)) {\n    outsideIndex += step;\n  }\n  if (outsideIndex < 0 || outsideIndex >= points.length) {\n    return points;\n  }\n  const insideIndex = outsideIndex - step;\n  if (insideIndex < 0 || insideIndex >= points.length) {\n    return points;\n  }\n  const entry = segmentEnterPoint(points[outsideIndex], points[insideIndex], rect);\n  return atStart ? [entry, ...points.slice(outsideIndex)] : [...points.slice(0, outsideIndex + 1), entry];\n}\n__name(clipEndpoint, \"clipEndpoint\");\nfunction clipEdgeEndpointsToNodeBoundaries(edges, nodeByIdMap) {\n  for (const edge of edges) {\n    const context = endpointContextFor(edge, nodeByIdMap, 2);\n    if (!context) {\n      continue;\n    }\n    let next = [...context.points];\n    if (context.srcRect) {\n      next = clipEndpoint(next, context.srcRect, true);\n    }\n    if (context.dstRect) {\n      next = clipEndpoint(next, context.dstRect, false);\n    }\n    next = simplifyPolyline(orthogonalizePolyline(next));\n    next = clearStraightEndpointCornerConnections(next, context.srcRect, context.dstRect);\n    context.edge.points = simplifyPolyline(orthogonalizePolyline(next));\n  }\n}\n__name(clipEdgeEndpointsToNodeBoundaries, \"clipEdgeEndpointsToNodeBoundaries\");\nfunction snapEndpointToBoundary(inner, endpoint, r, useApproachSide = false) {\n  if (sameY(inner, endpoint, EPS2)) {\n    if (endpoint.y < r.top - EPS2 || endpoint.y > r.bottom + EPS2) {\n      return endpoint;\n    }\n    if (useApproachSide) {\n      if (inner.x < r.left - EPS2) {\n        return { x: r.left, y: inner.y };\n      }\n      if (inner.x > r.right + EPS2) {\n        return { x: r.right, y: inner.y };\n      }\n    }\n    const toLeft = Math.abs(endpoint.x - r.left) <= Math.abs(endpoint.x - r.right);\n    return { x: toLeft ? r.left : r.right, y: inner.y };\n  }\n  if (sameX(inner, endpoint, EPS2)) {\n    if (endpoint.x < r.left - EPS2 || endpoint.x > r.right + EPS2) {\n      return endpoint;\n    }\n    if (useApproachSide) {\n      if (inner.y < r.top - EPS2) {\n        return { x: inner.x, y: r.top };\n      }\n      if (inner.y > r.bottom + EPS2) {\n        return { x: inner.x, y: r.bottom };\n      }\n    }\n    const toTop = Math.abs(endpoint.y - r.top) <= Math.abs(endpoint.y - r.bottom);\n    return { x: inner.x, y: toTop ? r.top : r.bottom };\n  }\n  return endpoint;\n}\n__name(snapEndpointToBoundary, \"snapEndpointToBoundary\");\nfunction firstDistinctAdjacent(points, endpointIndex, step) {\n  const endpoint = points[endpointIndex];\n  for (let index = endpointIndex + step; index >= 0 && index < points.length; index += step) {\n    const candidate = points[index];\n    if (!samePoint(candidate, endpoint, EPS2)) {\n      return candidate;\n    }\n  }\n  return points[endpointIndex + step];\n}\n__name(firstDistinctAdjacent, \"firstDistinctAdjacent\");\nfunction cornerClearanceRange(min, max) {\n  const lo = min + CORNER_CLEARANCE;\n  const hi = max - CORNER_CLEARANCE;\n  return lo <= hi ? { lo, hi } : { lo: (min + max) / 2, hi: (min + max) / 2 };\n}\n__name(cornerClearanceRange, \"cornerClearanceRange\");\nfunction clampToCornerClearance(value, min, max) {\n  const { lo, hi } = cornerClearanceRange(min, max);\n  return Math.min(hi, Math.max(lo, value));\n}\n__name(clampToCornerClearance, \"clampToCornerClearance\");\nfunction intersectRanges(ranges) {\n  const lo = Math.max(...ranges.map((range) => range.lo));\n  const hi = Math.min(...ranges.map((range) => range.hi));\n  if (lo > hi) {\n    return void 0;\n  }\n  return { lo, hi };\n}\n__name(intersectRanges, \"intersectRanges\");\nfunction clearanceRangeForSide(r, side) {\n  return side === \"left\" || side === \"right\" ? cornerClearanceRange(r.top, r.bottom) : cornerClearanceRange(r.left, r.right);\n}\n__name(clearanceRangeForSide, \"clearanceRangeForSide\");\nfunction terminalSideForSegment(endpoint, adjacent, r) {\n  const yWithin = endpoint.y >= r.top - EPS2 && endpoint.y <= r.bottom + EPS2;\n  const xWithin = endpoint.x >= r.left - EPS2 && endpoint.x <= r.right + EPS2;\n  if (sameY(endpoint, adjacent, EPS2) && yWithin) {\n    if (Math.abs(endpoint.x - r.left) < EPS2) {\n      return \"left\";\n    }\n    if (Math.abs(endpoint.x - r.right) < EPS2) {\n      return \"right\";\n    }\n  }\n  if (sameX(endpoint, adjacent, EPS2) && xWithin) {\n    if (Math.abs(endpoint.y - r.top) < EPS2) {\n      return \"top\";\n    }\n    if (Math.abs(endpoint.y - r.bottom) < EPS2) {\n      return \"bottom\";\n    }\n  }\n  return void 0;\n}\n__name(terminalSideForSegment, \"terminalSideForSegment\");\nfunction isHorizontalSide(side) {\n  return side === \"left\" || side === \"right\";\n}\n__name(isHorizontalSide, \"isHorizontalSide\");\nfunction straightClearanceRange(start, end, srcRect, dstRect, horizontal) {\n  const ranges = [];\n  const srcSide = srcRect ? terminalSideForSegment(start, end, srcRect) : void 0;\n  const dstSide = dstRect ? terminalSideForSegment(end, start, dstRect) : void 0;\n  if (srcRect && srcSide && isHorizontalSide(srcSide) === horizontal) {\n    ranges.push(clearanceRangeForSide(srcRect, srcSide));\n  }\n  if (dstRect && dstSide && isHorizontalSide(dstSide) === horizontal) {\n    ranges.push(clearanceRangeForSide(dstRect, dstSide));\n  }\n  return ranges.length > 0 ? intersectRanges(ranges) : void 0;\n}\n__name(straightClearanceRange, \"straightClearanceRange\");\nfunction clearStraightEndpointCornerAxis(start, end, srcRect, dstRect, horizontal) {\n  const range = straightClearanceRange(start, end, srcRect, dstRect, horizontal);\n  if (!range) {\n    return void 0;\n  }\n  const current = horizontal ? start.y : start.x;\n  const next = Math.min(range.hi, Math.max(range.lo, current));\n  if (Math.abs(next - current) < EPS2) {\n    return void 0;\n  }\n  return horizontal ? [\n    { x: start.x, y: next },\n    { x: end.x, y: next }\n  ] : [\n    { x: next, y: start.y },\n    { x: next, y: end.y }\n  ];\n}\n__name(clearStraightEndpointCornerAxis, \"clearStraightEndpointCornerAxis\");\nfunction clearStraightEndpointCornerConnections(points, srcRect, dstRect) {\n  if (points.length !== 2) {\n    return points;\n  }\n  const [start, end] = points;\n  if (sameY(start, end, EPS2)) {\n    return clearStraightEndpointCornerAxis(start, end, srcRect, dstRect, true) ?? points;\n  }\n  if (sameX(start, end, EPS2)) {\n    return clearStraightEndpointCornerAxis(start, end, srcRect, dstRect, false) ?? points;\n  }\n  return points;\n}\n__name(clearStraightEndpointCornerConnections, \"clearStraightEndpointCornerConnections\");\nfunction cornerClearedEndpoint(endpoint, r, side) {\n  return isHorizontalSide(side) ? { x: endpoint.x, y: clampToCornerClearance(endpoint.y, r.top, r.bottom) } : { x: clampToCornerClearance(endpoint.x, r.left, r.right), y: endpoint.y };\n}\n__name(cornerClearedEndpoint, \"cornerClearedEndpoint\");\nfunction moveCollinearEndpointRun(points, endpointIndex, step, endpoint, adjusted, horizontalTerminal) {\n  const next = points.map((point) => ({ ...point }));\n  for (let index = endpointIndex; index >= 0 && index < points.length; index += step) {\n    const point = points[index];\n    if (horizontalTerminal && !sameY(point, endpoint, EPS2)) {\n      break;\n    }\n    if (!horizontalTerminal && !sameX(point, endpoint, EPS2)) {\n      break;\n    }\n    if (horizontalTerminal) {\n      next[index].y = adjusted.y;\n    } else {\n      next[index].x = adjusted.x;\n    }\n  }\n  return next;\n}\n__name(moveCollinearEndpointRun, \"moveCollinearEndpointRun\");\nfunction clearEndpointCornerConnection(points, r, atStart) {\n  if (points.length < 2) {\n    return points;\n  }\n  const endpointIndex = atStart ? 0 : points.length - 1;\n  const step = atStart ? 1 : -1;\n  const endpoint = points[endpointIndex];\n  const adjacent = firstDistinctAdjacent(points, endpointIndex, step);\n  if (!adjacent) {\n    return points;\n  }\n  const side = terminalSideForSegment(endpoint, adjacent, r);\n  if (!side) {\n    return points;\n  }\n  const horizontalTerminal = isHorizontalSide(side);\n  const adjusted = cornerClearedEndpoint(endpoint, r, side);\n  if (samePoint(endpoint, adjusted, EPS2)) {\n    return points;\n  }\n  return moveCollinearEndpointRun(\n    points,\n    endpointIndex,\n    step,\n    endpoint,\n    adjusted,\n    horizontalTerminal\n  );\n}\n__name(clearEndpointCornerConnection, \"clearEndpointCornerConnection\");\nfunction borderSideForSegment(a, b, r) {\n  const xWithin = Math.min(a.x, b.x) >= r.left - EPS2 && Math.max(a.x, b.x) <= r.right + EPS2;\n  const yWithin = Math.min(a.y, b.y) >= r.top - EPS2 && Math.max(a.y, b.y) <= r.bottom + EPS2;\n  if (Math.abs(a.y - r.top) < EPS2 && Math.abs(b.y - r.top) < EPS2 && xWithin) {\n    return \"top\";\n  }\n  if (Math.abs(a.y - r.bottom) < EPS2 && Math.abs(b.y - r.bottom) < EPS2 && xWithin) {\n    return \"bottom\";\n  }\n  if (Math.abs(a.x - r.left) < EPS2 && Math.abs(b.x - r.left) < EPS2 && yWithin) {\n    return \"left\";\n  }\n  if (Math.abs(a.x - r.right) < EPS2 && Math.abs(b.x - r.right) < EPS2 && yWithin) {\n    return \"right\";\n  }\n  return void 0;\n}\n__name(borderSideForSegment, \"borderSideForSegment\");\nfunction leavesOutward(side, from, to, r) {\n  switch (side) {\n    case \"top\":\n      return sameX(from, to, EPS2) && to.y < r.top - EPS2;\n    case \"bottom\":\n      return sameX(from, to, EPS2) && to.y > r.bottom + EPS2;\n    case \"left\":\n      return sameY(from, to, EPS2) && to.x < r.left - EPS2;\n    case \"right\":\n      return sameY(from, to, EPS2) && to.x > r.right + EPS2;\n  }\n}\n__name(leavesOutward, \"leavesOutward\");\nfunction collapseOwnBorderStub(points, r, atStart) {\n  if (points.length < 3) {\n    return points;\n  }\n  if (atStart) {\n    const side2 = borderSideForSegment(points[0], points[1], r);\n    if (side2 && leavesOutward(side2, points[1], points[2], r)) {\n      return points.slice(1);\n    }\n    return points;\n  }\n  const last = points.length - 1;\n  const side = borderSideForSegment(points[last - 1], points[last], r);\n  if (side && leavesOutward(side, points[last - 1], points[last - 2], r)) {\n    return points.slice(0, last);\n  }\n  return points;\n}\n__name(collapseOwnBorderStub, \"collapseOwnBorderStub\");\nfunction snapAndCollapseEndpoints(points, srcRect, dstRect) {\n  let next = points;\n  if (srcRect) {\n    const adjacent = firstDistinctAdjacent(next, 0, 1);\n    if (adjacent) {\n      const snapped = snapEndpointToBoundary(adjacent, next[0], srcRect);\n      if (snapped !== next[0]) {\n        next = [snapped, ...next.slice(1)];\n      }\n    }\n    next = collapseOwnBorderStub(next, srcRect, true);\n  }\n  if (dstRect) {\n    const last = next.length - 1;\n    const adjacent = firstDistinctAdjacent(next, last, -1);\n    if (adjacent) {\n      const snapped = snapEndpointToBoundary(adjacent, next[last], dstRect, true);\n      if (snapped !== next[last]) {\n        next = [...next.slice(0, last), snapped];\n      }\n    }\n    next = collapseOwnBorderStub(next, dstRect, false);\n  }\n  const straightCleared = clearStraightEndpointCornerConnections(next, srcRect, dstRect);\n  if (straightCleared !== next || next.length === 2) {\n    return straightCleared;\n  }\n  if (srcRect) {\n    next = clearEndpointCornerConnection(next, srcRect, true);\n  }\n  if (dstRect) {\n    next = clearEndpointCornerConnection(next, dstRect, false);\n  }\n  return next;\n}\n__name(snapAndCollapseEndpoints, \"snapAndCollapseEndpoints\");\nfunction prepareEdgeEndpointsForRenderer(edges, nodeByIdMap) {\n  for (const edge of edges) {\n    const context = endpointContextFor(edge, nodeByIdMap, 2);\n    if (!context) {\n      continue;\n    }\n    const input = dedupeConsecutivePoints(context.points, EPS2);\n    const newPts = snapAndCollapseEndpoints(input, context.srcRect, context.dstRect);\n    if (newPts.length < 3) {\n      context.edge.points = newPts;\n      continue;\n    }\n    const duplicated = [\n      newPts[0],\n      { ...newPts[0] },\n      ...newPts.slice(1, -1),\n      newPts[newPts.length - 1],\n      { ...newPts[newPts.length - 1] }\n    ];\n    context.edge.points = duplicated;\n  }\n}\n__name(prepareEdgeEndpointsForRenderer, \"prepareEdgeEndpointsForRenderer\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/lrTransform.ts\nfunction buildNodeMap(nodes) {\n  return new Map(nodes.map((node) => [node.id, node]));\n}\n__name(buildNodeMap, \"buildNodeMap\");\nfunction resolveTopLevelGroupId(node, nodeById) {\n  let parentId = node.parentId;\n  let topLevelGroupId = null;\n  while (parentId) {\n    const parent = nodeById.get(parentId);\n    if (!parent?.isGroup) {\n      break;\n    }\n    topLevelGroupId = parent.id;\n    parentId = parent.parentId;\n  }\n  return topLevelGroupId;\n}\n__name(resolveTopLevelGroupId, \"resolveTopLevelGroupId\");\nfunction groupDepth(group, nodeById) {\n  let depth = 0;\n  let parentId = group.parentId;\n  while (parentId) {\n    const parent = nodeById.get(parentId);\n    if (!parent?.isGroup) {\n      break;\n    }\n    depth++;\n    parentId = parent.parentId;\n  }\n  return depth;\n}\n__name(groupDepth, \"groupDepth\");\nfunction boundsForChildren(children) {\n  let minX = Infinity;\n  let maxX = -Infinity;\n  let minY = Infinity;\n  let maxY = -Infinity;\n  for (const child of children) {\n    const cx = child.x;\n    const cy = child.y;\n    if (typeof cx !== \"number\" || typeof cy !== \"number\") {\n      continue;\n    }\n    const w = child.width ?? 0;\n    const h = child.height ?? 0;\n    minX = Math.min(minX, cx - w / 2);\n    maxX = Math.max(maxX, cx + w / 2);\n    minY = Math.min(minY, cy - h / 2);\n    maxY = Math.max(maxY, cy + h / 2);\n  }\n  if (minX === Infinity || minY === Infinity) {\n    return null;\n  }\n  return { minX, maxX, minY, maxY };\n}\n__name(boundsForChildren, \"boundsForChildren\");\nfunction applyGroupBounds(group, bounds) {\n  const pad = group.padding ?? 20;\n  group.x = (bounds.minX + bounds.maxX) / 2;\n  group.y = (bounds.minY + bounds.maxY) / 2;\n  group.width = Math.max(0, bounds.maxX - bounds.minX) + pad;\n  group.height = Math.max(0, bounds.maxY - bounds.minY) + pad;\n}\n__name(applyGroupBounds, \"applyGroupBounds\");\nfunction recomputeNestedGroupBounds(nodes) {\n  const nodeById = buildNodeMap(nodes);\n  const groupsByDepth = nodes.filter((node) => node.isGroup && node.parentId).sort((a, b) => groupDepth(b, nodeById) - groupDepth(a, nodeById));\n  for (const group of groupsByDepth) {\n    const children = nodes.filter((node) => node.parentId === group.id);\n    const bounds = boundsForChildren(children);\n    if (bounds) {\n      applyGroupBounds(group, bounds);\n    }\n  }\n}\n__name(recomputeNestedGroupBounds, \"recomputeNestedGroupBounds\");\nfunction mirrorAxis(layout, axis) {\n  const nodes = layout.nodes ?? [];\n  const edges = layout.edges ?? [];\n  const contentNodes = nodes.filter((node) => !node.isGroup);\n  let min = Infinity;\n  let max = -Infinity;\n  for (const node of contentNodes) {\n    const value = node[axis];\n    if (typeof value !== \"number\") {\n      continue;\n    }\n    min = Math.min(min, value);\n    max = Math.max(max, value);\n  }\n  if (!Number.isFinite(min) || !Number.isFinite(max)) {\n    return false;\n  }\n  const mirror = /* @__PURE__ */ __name((value) => min + max - value, \"mirror\");\n  for (const node of nodes) {\n    const value = node[axis];\n    if (typeof value === \"number\") {\n      node[axis] = mirror(value);\n    }\n    const titleRect = node.groupTitleRect;\n    if (titleRect) {\n      node.groupTitleRect = axis === \"x\" ? {\n        ...titleRect,\n        left: mirror(titleRect.right),\n        right: mirror(titleRect.left)\n      } : {\n        ...titleRect,\n        top: mirror(titleRect.bottom),\n        bottom: mirror(titleRect.top)\n      };\n    }\n  }\n  for (const edge of edges) {\n    for (const point of edge.points ?? []) {\n      point[axis] = mirror(point[axis]);\n    }\n  }\n  return true;\n}\n__name(mirrorAxis, \"mirrorAxis\");\nfunction applyBtDirectionTransform(layout) {\n  const nodes = layout.nodes ?? [];\n  if (!nodes.some((node) => !node.isGroup)) {\n    return true;\n  }\n  return mirrorAxis(layout, \"y\");\n}\n__name(applyBtDirectionTransform, \"applyBtDirectionTransform\");\nfunction applyLrDirectionTransform(layout, direction = \"LR\") {\n  const nodes = layout.nodes ?? [];\n  const edges = layout.edges ?? [];\n  const contentNodes = nodes.filter((n) => !n.isGroup);\n  let minX = Infinity;\n  let minY = Infinity;\n  for (const n of contentNodes) {\n    const x0 = n.x ?? 0;\n    const y0 = n.y ?? 0;\n    if (x0 < minX) {\n      minX = x0;\n    }\n    if (y0 < minY) {\n      minY = y0;\n    }\n  }\n  if (!Number.isFinite(minX) || !Number.isFinite(minY)) {\n    return false;\n  }\n  const titleBandSize = 36;\n  let totalWidth = 0;\n  let totalHeight = 0;\n  for (const n of contentNodes) {\n    totalWidth += n.width ?? 0;\n    totalHeight += n.height ?? 0;\n  }\n  const avgWidth = totalWidth / contentNodes.length;\n  const avgHeight = totalHeight / contentNodes.length;\n  const horizontalScaleFactor = avgHeight > 0 ? Math.max(1, avgWidth / avgHeight) : 1;\n  for (const n of contentNodes) {\n    const x0 = n.x ?? 0;\n    const y0 = n.y ?? 0;\n    const newX = (y0 - minY) * horizontalScaleFactor + titleBandSize;\n    const newY = x0 - minX;\n    n.x = newX;\n    n.y = newY;\n  }\n  for (const e of edges) {\n    if (!e.points) {\n      continue;\n    }\n    for (const p of e.points) {\n      const x0 = p.x;\n      const y0 = p.y;\n      const newX = (y0 - minY) * horizontalScaleFactor + titleBandSize;\n      const newY = x0 - minX;\n      p.x = newX;\n      p.y = newY;\n    }\n  }\n  recomputeNestedGroupBounds(nodes);\n  const laneNodes = nodes.filter((n) => n.isGroup && !n.parentId);\n  if (laneNodes.length === 0) {\n    if (direction === \"RL\") {\n      mirrorAxis(layout, \"x\");\n    }\n    return true;\n  }\n  const nodeById = buildNodeMap(nodes);\n  const childrenByLane = /* @__PURE__ */ new Map();\n  for (const n of nodes) {\n    if (n.isGroup) {\n      continue;\n    }\n    const laneId = resolveTopLevelGroupId(n, nodeById);\n    if (!laneId) {\n      continue;\n    }\n    const bucket = childrenByLane.get(laneId) ?? [];\n    bucket.push(n);\n    childrenByLane.set(laneId, bucket);\n  }\n  let maxPad = 0;\n  for (const lane of laneNodes) {\n    const pad = lane.padding ?? 0;\n    if (pad > maxPad) {\n      maxPad = pad;\n    }\n  }\n  const laneBounds = [];\n  let globalMinXChild = Infinity;\n  let globalMaxXChild = -Infinity;\n  for (const lane of laneNodes) {\n    const children = childrenByLane.get(lane.id) ?? [];\n    const bounds = boundsForChildren(children);\n    if (!bounds) {\n      continue;\n    }\n    globalMinXChild = Math.min(globalMinXChild, bounds.minX);\n    globalMaxXChild = Math.max(globalMaxXChild, bounds.maxX);\n    laneBounds.push({\n      lane,\n      contentTop: bounds.minY,\n      contentBottom: bounds.maxY,\n      centerY: (bounds.minY + bounds.maxY) / 2\n    });\n  }\n  if (globalMinXChild === Infinity || globalMaxXChild === -Infinity) {\n    return true;\n  }\n  const fullContentWidth = Math.max(0, globalMaxXChild - globalMinXChild);\n  const horizontalMargin = Math.max(maxPad, 10);\n  const bodyWidth = fullContentWidth + 2 * horizontalMargin;\n  const laneWidth = titleBandSize + bodyWidth;\n  const bodyCenter = (globalMinXChild + globalMaxXChild) / 2;\n  const bodyLeft = bodyCenter - bodyWidth / 2;\n  const laneLeft = bodyLeft - titleBandSize;\n  const centerX = laneLeft + laneWidth / 2;\n  const verticalMargin = Math.max(maxPad, titleBandSize);\n  laneBounds.sort((a, b) => a.centerY - b.centerY);\n  for (let i = 0; i < laneBounds.length; i++) {\n    const curr = laneBounds[i];\n    let laneTop;\n    let laneBottom;\n    if (i === 0) {\n      laneTop = curr.contentTop - verticalMargin;\n    } else {\n      const prev = laneBounds[i - 1];\n      laneTop = (prev.contentBottom + curr.contentTop) / 2;\n    }\n    if (i === laneBounds.length - 1) {\n      laneBottom = curr.contentBottom + verticalMargin;\n    } else {\n      const next = laneBounds[i + 1];\n      laneBottom = (curr.contentBottom + next.contentTop) / 2;\n    }\n    const laneHeight = Math.max(0, laneBottom - laneTop);\n    const centerY = (laneTop + laneBottom) / 2;\n    curr.lane.x = centerX;\n    curr.lane.y = centerY;\n    curr.lane.width = laneWidth;\n    curr.lane.height = laneHeight;\n    curr.lane.swimlaneContentTop = curr.contentTop;\n    curr.lane.groupTitleRect = {\n      left: laneLeft,\n      right: laneLeft + titleBandSize,\n      top: laneTop,\n      bottom: laneBottom\n    };\n  }\n  if (direction === \"RL\") {\n    mirrorAxis(layout, \"x\");\n  }\n  return true;\n}\n__name(applyLrDirectionTransform, \"applyLrDirectionTransform\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/portSwap.ts\nvar EPS3 = 1e-6;\nvar MIN_PORT_SPACING = 8;\nvar PORT_SHIFT = MIN_PORT_SPACING;\nvar TRY_DELTAS = [0, PORT_SHIFT, -PORT_SHIFT, 2 * PORT_SHIFT, -2 * PORT_SHIFT];\nfunction portSwapToLShape(edges, nodes) {\n  const { nodeInfoById, realNodeRects } = collectRealNodeBounds(nodes);\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const pts = edge.points;\n    if (!pts || pts.length < 4) {\n      continue;\n    }\n    const route = classifyThreeSegmentRoute(dedupeConsecutivePoints(pts, EPS3), EPS3);\n    if (!route) {\n      continue;\n    }\n    const { p3 } = route;\n    const isHVH = route.kind === \"HVH\";\n    const nodePair = getNodePairGeometry(edge, nodeInfoById, EPS3);\n    if (!nodePair) {\n      continue;\n    }\n    const { srcId, dstId, srcInfo, dstInfo, collinearX, collinearY } = nodePair;\n    if (collinearX || collinearY) {\n      continue;\n    }\n    let newPts;\n    const srcRect = srcInfo.rect;\n    for (const delta of TRY_DELTAS) {\n      let np0;\n      let np1;\n      let np2;\n      if (isHVH) {\n        const dstBelow = dstInfo.cy > srcInfo.cy;\n        const newSrcY = dstBelow ? srcRect.bottom : srcRect.top;\n        const newSrcX = srcInfo.cx + delta;\n        if (newSrcX <= srcRect.left + EPS3 || newSrcX >= srcRect.right - EPS3) {\n          continue;\n        }\n        np0 = { x: newSrcX, y: newSrcY };\n        np1 = { x: newSrcX, y: p3.y };\n        np2 = { x: p3.x, y: p3.y };\n      } else {\n        const dstEast = dstInfo.cx > srcInfo.cx;\n        const newSrcX = dstEast ? srcRect.right : srcRect.left;\n        const newSrcY = srcInfo.cy + delta;\n        if (newSrcY <= srcRect.top + EPS3 || newSrcY >= srcRect.bottom - EPS3) {\n          continue;\n        }\n        np0 = { x: newSrcX, y: newSrcY };\n        np1 = { x: p3.x, y: newSrcY };\n        np2 = { x: p3.x, y: p3.y };\n      }\n      const firstSegDegenerate = samePoint(np0, np1, EPS3);\n      const secondSegDegenerate = samePoint(np1, np2, EPS3);\n      if (firstSegDegenerate && secondSegDegenerate) {\n        continue;\n      }\n      if (!firstSegDegenerate && segmentHitsAnyRect(np0, np1, realNodeRects, [srcId], 1)) {\n        continue;\n      }\n      if (!secondSegDegenerate && segmentHitsAnyRect(np1, np2, realNodeRects, [dstId], 1)) {\n        continue;\n      }\n      const firstSegConflicts = !firstSegDegenerate && segmentConflictsWithAnyEdge(np0, np1, edges, edge, {\n        epsilon: EPS3,\n        skipDegenerateOther: true\n      });\n      const secondSegConflicts = !secondSegDegenerate && segmentConflictsWithAnyEdge(np1, np2, edges, edge, {\n        epsilon: EPS3,\n        skipDegenerateOther: true\n      });\n      if (firstSegConflicts || secondSegConflicts) {\n        continue;\n      }\n      if (firstSegDegenerate) {\n        newPts = [np1, np2];\n      } else if (secondSegDegenerate) {\n        newPts = [np0, np1];\n      } else {\n        newPts = [np0, np1, np2];\n      }\n      break;\n    }\n    if (newPts) {\n      edge.points = newPts;\n    }\n  }\n}\n__name(portSwapToLShape, \"portSwapToLShape\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/terminalStub.ts\nfunction collapseShortTerminalStub(edges, nodeByIdMap) {\n  const MIN_STUB = 10;\n  const EPS_LOCAL2 = 1e-3;\n  const BUFFER = 2;\n  const { realNodeRects, labelNodeRects: labelRects } = collectNodeRectEntries(\n    nodeByIdMap.values()\n  );\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const rawPts = edge.points;\n    if (!rawPts || rawPts.length < 4) {\n      continue;\n    }\n    const pts = dedupeConsecutivePoints(rawPts, EPS_LOCAL2);\n    if (pts.length < 4) {\n      continue;\n    }\n    const nLast = pts.length - 1;\n    const endPt = pts[nLast];\n    const penultPt = pts[nLast - 1];\n    const prevPt = pts[nLast - 2];\n    const lastDx = endPt.x - penultPt.x;\n    const lastDy = endPt.y - penultPt.y;\n    const lastLen = Math.hypot(lastDx, lastDy);\n    if (lastLen >= MIN_STUB || lastLen < EPS_LOCAL2) {\n      continue;\n    }\n    const penultDx = penultPt.x - prevPt.x;\n    const penultDy = penultPt.y - prevPt.y;\n    const penultLen = Math.hypot(penultDx, penultDy);\n    if (penultLen < EPS_LOCAL2) {\n      continue;\n    }\n    const lastIsHoriz = isHorizontalSegment(penultPt, endPt, EPS_LOCAL2);\n    const lastIsVert = isVerticalSegment(penultPt, endPt, EPS_LOCAL2);\n    const penultIsHoriz = isHorizontalSegment(prevPt, penultPt, EPS_LOCAL2);\n    const penultIsVert = isVerticalSegment(prevPt, penultPt, EPS_LOCAL2);\n    if (!(lastIsHoriz && penultIsVert || lastIsVert && penultIsHoriz)) {\n      continue;\n    }\n    const dstId = edge.end;\n    const srcId = edge.start;\n    const dst = dstId ? nodeByIdMap.get(dstId) : void 0;\n    if (!dst) {\n      continue;\n    }\n    const dstCx = dst.x ?? 0;\n    const dstCy = dst.y ?? 0;\n    const dstRect = rectOfNodeBounds(dst);\n    if (!dstRect) {\n      continue;\n    }\n    let newPrev;\n    let newEnd;\n    if (penultIsVert) {\n      const approachFromBelow = penultDy < 0;\n      newPrev = { x: dstCx, y: prevPt.y };\n      newEnd = { x: dstCx, y: approachFromBelow ? dstRect.bottom : dstRect.top };\n    } else {\n      const approachFromLeft = penultDx > 0;\n      newPrev = { x: prevPt.x, y: dstCy };\n      newEnd = { x: approachFromLeft ? dstRect.right : dstRect.left, y: dstCy };\n    }\n    if (segmentHitsAnyRect(newPrev, newEnd, realNodeRects, dstId ? [dstId] : [], -BUFFER)) {\n      continue;\n    }\n    if (segmentHitsAnyRect(newPrev, newEnd, labelRects, [], -BUFFER)) {\n      continue;\n    }\n    if (srcId) {\n      const src = nodeByIdMap.get(srcId);\n      const srcRect = src ? rectOfNodeBounds(src) : void 0;\n      if (srcRect && pointInsideRect(newPrev, srcRect, BUFFER)) {\n        continue;\n      }\n    }\n    const ownSegmentKey = /* @__PURE__ */ __name((a, b) => `${a.x.toFixed(3)},${a.y.toFixed(3)}|${b.x.toFixed(3)},${b.y.toFixed(3)}`, \"ownSegmentKey\");\n    const selfSegments = /* @__PURE__ */ new Set();\n    for (let i = 0; i < pts.length - 1; i++) {\n      selfSegments.add(ownSegmentKey(pts[i], pts[i + 1]));\n    }\n    const segmentCrossesOtherEdge = /* @__PURE__ */ __name((from, to) => {\n      for (const other of edges) {\n        if (other === edge) {\n          continue;\n        }\n        if (other.isLayoutOnly) {\n          continue;\n        }\n        const oPts = other.points;\n        if (!oPts || oPts.length < 2) {\n          continue;\n        }\n        for (let i = 0; i < oPts.length - 1; i++) {\n          const a = oPts[i];\n          const b = oPts[i + 1];\n          if (selfSegments.has(ownSegmentKey(a, b))) {\n            continue;\n          }\n          if (orthogonalSegmentsStrictlyCross(from, to, a, b, EPS_LOCAL2)) {\n            return true;\n          }\n        }\n      }\n      return false;\n    }, \"segmentCrossesOtherEdge\");\n    if (segmentCrossesOtherEdge(newPrev, newEnd)) {\n      continue;\n    }\n    if (nLast - 3 >= 0) {\n      const beforePrev = pts[nLast - 3];\n      const endpointIds = [srcId, dstId].filter((id) => Boolean(id));\n      if (segmentHitsAnyRect(beforePrev, newPrev, realNodeRects, endpointIds, -BUFFER)) {\n        continue;\n      }\n      if (segmentCrossesOtherEdge(beforePrev, newPrev)) {\n        continue;\n      }\n    }\n    const head = pts.slice(0, nLast - 2);\n    const newPts = [...head, newPrev, newEnd];\n    edge.points = newPts;\n    const labelId = edge.labelNodeId;\n    if (labelId) {\n      const labelNode = nodeByIdMap.get(labelId);\n      if (labelNode) {\n        const lw = labelNode.width ?? 0;\n        const lh = labelNode.height ?? 0;\n        if (lw > 0 && lh > 0) {\n          let bestMidX;\n          let bestMidY;\n          let bestLen = -1;\n          for (let i = 0; i < newPts.length - 1; i++) {\n            const a = newPts[i];\n            const b = newPts[i + 1];\n            const segLen = Math.hypot(b.x - a.x, b.y - a.y);\n            const isHoriz = sameY(a, b, EPS_LOCAL2);\n            const isVert = sameX(a, b, EPS_LOCAL2);\n            const fits = isHoriz && segLen >= lw + 2 || isVert && segLen >= lh + 2;\n            if (!fits) {\n              continue;\n            }\n            if (segLen > bestLen) {\n              bestLen = segLen;\n              bestMidX = (a.x + b.x) / 2;\n              bestMidY = (a.y + b.y) / 2;\n            }\n          }\n          if (bestMidX !== void 0 && bestMidY !== void 0) {\n            labelNode.x = bestMidX;\n            labelNode.y = bestMidY;\n          }\n        }\n      }\n    }\n  }\n}\n__name(collapseShortTerminalStub, \"collapseShortTerminalStub\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/materializedGeometry.ts\nvar EPS_LOCAL = 1e-3;\nvar MIN_SHARED = 8;\nvar segmentsFor = orthogonalSegmentsForPoints;\nvar orthogonallyAligned = /* @__PURE__ */ __name((a, b) => sameX(a, b, EPS_LOCAL) || sameY(a, b, EPS_LOCAL), \"orthogonallyAligned\");\nfunction separateSharedRenderedTerminalLanes(edges, nodeByIdMap) {\n  const MIN_FACE_CLEARANCE = 16;\n  const TRACK_SHIFT = 7;\n  const rectIntersect = /* @__PURE__ */ __name((node, point) => {\n    const x = node.x ?? 0;\n    const y = node.y ?? 0;\n    const dx = point.x - x;\n    const dy = point.y - y;\n    let w = (node.width ?? 0) / 2;\n    let h = (node.height ?? 0) / 2;\n    if (Math.abs(dy) * w > Math.abs(dx) * h) {\n      if (dy < 0) {\n        h = -h;\n      }\n      return { x: x + (dy === 0 ? 0 : h * dx / dy), y: y + h };\n    }\n    if (dx < 0) {\n      w = -w;\n    }\n    return { x: x + w, y: y + (dx === 0 ? 0 : w * dy / dx) };\n  }, \"rectIntersect\");\n  const terminalLaneFor = /* @__PURE__ */ __name((edge, atStart) => {\n    const points = dedupeConsecutivePoints(edge.points ?? []);\n    if (points.length < 2) {\n      return void 0;\n    }\n    const nodeId = atStart ? edge.start : edge.end;\n    const node = nodeId ? nodeByIdMap.get(nodeId) : void 0;\n    const rect = node ? rectOfNodeBounds(node) : void 0;\n    if (!node || !nodeId || !rect) {\n      return void 0;\n    }\n    const endpoint = atStart ? points[0] : points[points.length - 1];\n    const adjacent = atStart ? points[1] : points[points.length - 2];\n    const boundary = rectIntersect(node, endpoint);\n    let railEnd = endpoint;\n    if (orthogonallyAligned(adjacent, boundary)) {\n      railEnd = adjacent;\n    }\n    if (sameX(boundary, railEnd, EPS_LOCAL)) {\n      return {\n        edge,\n        edgeId: String(edge.id ?? \"\"),\n        nodeId,\n        atStart,\n        orientation: \"V\",\n        coord: boundary.x,\n        min: Math.min(boundary.y, railEnd.y),\n        max: Math.max(boundary.y, railEnd.y),\n        boundary,\n        railEnd,\n        rect\n      };\n    }\n    if (sameY(boundary, railEnd, EPS_LOCAL)) {\n      return {\n        edge,\n        edgeId: String(edge.id ?? \"\"),\n        nodeId,\n        atStart,\n        orientation: \"H\",\n        coord: boundary.y,\n        min: Math.min(boundary.x, railEnd.x),\n        max: Math.max(boundary.x, railEnd.x),\n        boundary,\n        railEnd,\n        rect\n      };\n    }\n    return void 0;\n  }, \"terminalLaneFor\");\n  const projectedOverlapLength = /* @__PURE__ */ __name((a, b) => Math.max(0, Math.min(a.max, b.max) - Math.max(a.min, b.min)), \"projectedOverlapLength\");\n  const sameTerminalFace = /* @__PURE__ */ __name((a, b) => {\n    if (a.nodeId !== b.nodeId || a.orientation !== b.orientation) {\n      return false;\n    }\n    if (a.orientation === \"H\") {\n      const aOnHorizontalFace = Math.abs(a.boundary.x - a.rect.left) < 1 || Math.abs(a.boundary.x - a.rect.right) < 1;\n      return aOnHorizontalFace && sameX(a.boundary, b.boundary, 1);\n    }\n    const aOnVerticalFace = Math.abs(a.boundary.y - a.rect.top) < 1 || Math.abs(a.boundary.y - a.rect.bottom) < 1;\n    return aOnVerticalFace && sameY(a.boundary, b.boundary, 1);\n  }, \"sameTerminalFace\");\n  const exactTerminalLaneConflict = /* @__PURE__ */ __name((a, b) => {\n    if (a.nodeId !== b.nodeId || a.orientation !== b.orientation) {\n      return false;\n    }\n    const shared = projectedOverlapLength(a, b);\n    return shared >= MIN_SHARED && Math.abs(a.coord - b.coord) < 0.5;\n  }, \"exactTerminalLaneConflict\");\n  const nearTerminalLaneConflict = /* @__PURE__ */ __name((a, b) => {\n    if (a.nodeId !== b.nodeId || a.orientation !== b.orientation || a.orientation !== \"H\" || a.atStart === b.atStart) {\n      return false;\n    }\n    const shared = projectedOverlapLength(a, b);\n    if (shared < MIN_SHARED) {\n      return false;\n    }\n    const faceSpan = a.rect.bottom - a.rect.top;\n    if (shared < faceSpan || shared > 2 * faceSpan) {\n      return false;\n    }\n    return sameTerminalFace(a, b) && Math.abs(a.coord - b.coord) < MIN_FACE_CLEARANCE;\n  }, \"nearTerminalLaneConflict\");\n  const shiftedCandidate = /* @__PURE__ */ __name((lane, shift) => {\n    const points = dedupeConsecutivePoints(lane.edge.points ?? []);\n    if (points.length < 2) {\n      return void 0;\n    }\n    const shiftedBoundary = lane.orientation === \"V\" ? { x: lane.boundary.x + shift, y: lane.boundary.y } : { x: lane.boundary.x, y: lane.boundary.y + shift };\n    const shiftedRailEnd = lane.orientation === \"V\" ? { x: lane.railEnd.x + shift, y: lane.railEnd.y } : { x: lane.railEnd.x, y: lane.railEnd.y + shift };\n    const boundaryStaysOnSameFace = /* @__PURE__ */ __name(() => {\n      if (Math.abs(lane.boundary.y - lane.rect.top) < 1 || Math.abs(lane.boundary.y - lane.rect.bottom) < 1) {\n        return sameY(shiftedBoundary, lane.boundary, EPS_LOCAL) && shiftedBoundary.x >= lane.rect.left + 1 && shiftedBoundary.x <= lane.rect.right - 1;\n      }\n      if (Math.abs(lane.boundary.x - lane.rect.left) < 1 || Math.abs(lane.boundary.x - lane.rect.right) < 1) {\n        return sameX(shiftedBoundary, lane.boundary, EPS_LOCAL) && shiftedBoundary.y >= lane.rect.top + 1 && shiftedBoundary.y <= lane.rect.bottom - 1;\n      }\n      return false;\n    }, \"boundaryStaysOnSameFace\");\n    if (!boundaryStaysOnSameFace()) {\n      return void 0;\n    }\n    if (lane.atStart) {\n      const railEndIsAdjacent2 = points.length > 1 && samePoint(points[1], lane.railEnd, EPS_LOCAL);\n      const rest = points.slice(railEndIsAdjacent2 ? 2 : 1);\n      const next = rest[0];\n      if (next && !orthogonallyAligned(next, shiftedRailEnd)) {\n        return void 0;\n      }\n      return [shiftedBoundary, shiftedRailEnd, ...rest];\n    }\n    const railEndIsAdjacent = points.length > 1 && samePoint(points[points.length - 2], lane.railEnd, EPS_LOCAL);\n    const before = points.slice(0, railEndIsAdjacent ? -2 : -1);\n    const previous = before[before.length - 1];\n    if (previous && !orthogonallyAligned(previous, shiftedRailEnd)) {\n      return void 0;\n    }\n    return [...before, shiftedRailEnd, shiftedBoundary];\n  }, \"shiftedCandidate\");\n  const laneIsStraightCollinearConnector = /* @__PURE__ */ __name((lane) => {\n    const edge = lane.edge;\n    const points = dedupeConsecutivePoints(edge.points ?? []);\n    if (points.length !== 2) {\n      return false;\n    }\n    const startId = edge.start;\n    const endId = edge.end;\n    const start = startId ? nodeByIdMap.get(startId) : void 0;\n    const end = endId ? nodeByIdMap.get(endId) : void 0;\n    if (!start || !end) {\n      return false;\n    }\n    const startX = start.x ?? 0;\n    const startY = start.y ?? 0;\n    const endX = end.x ?? 0;\n    const endY = end.y ?? 0;\n    const [a, b] = points;\n    return sameY(a, b, EPS_LOCAL) && Math.abs(startY - endY) < 1 && Math.abs(startX - endX) > 1 || sameX(a, b, EPS_LOCAL) && Math.abs(startX - endX) < 1 && Math.abs(startY - endY) > 1;\n  }, \"laneIsStraightCollinearConnector\");\n  const shifts = [\n    -TRACK_SHIFT,\n    TRACK_SHIFT,\n    -2 * TRACK_SHIFT,\n    2 * TRACK_SHIFT,\n    -3 * TRACK_SHIFT,\n    3 * TRACK_SHIFT\n  ];\n  for (let iteration = 0; iteration < 8; iteration++) {\n    const lanes = edges.filter((edge) => !edge.isLayoutOnly).flatMap((edge) => [terminalLaneFor(edge, true), terminalLaneFor(edge, false)]).filter((lane) => Boolean(lane));\n    let fixed = false;\n    for (let i = 0; i < lanes.length && !fixed; i++) {\n      for (let j = i + 1; j < lanes.length && !fixed; j++) {\n        const first = lanes[i];\n        const second = lanes[j];\n        if (first.edge === second.edge || !(exactTerminalLaneConflict(first, second) || nearTerminalLaneConflict(first, second))) {\n          continue;\n        }\n        const fixingNearConflict = !exactTerminalLaneConflict(first, second);\n        const candidates = [first, second].sort((a, b) => {\n          const aPreservesStraight = laneIsStraightCollinearConnector(a);\n          const bPreservesStraight = laneIsStraightCollinearConnector(b);\n          if (aPreservesStraight !== bPreservesStraight) {\n            return Number(aPreservesStraight) - Number(bPreservesStraight);\n          }\n          return Number(!b.atStart) - Number(!a.atStart);\n        });\n        for (const lane of candidates) {\n          for (const shift of shifts) {\n            const candidate = shiftedCandidate(lane, shift);\n            if (!candidate) {\n              continue;\n            }\n            const nextLane = terminalLaneFor({ ...lane.edge, points: candidate }, lane.atStart);\n            if (!nextLane || lanes.some(\n              (other) => other.edge !== lane.edge && (exactTerminalLaneConflict(nextLane, other) || fixingNearConflict && nearTerminalLaneConflict(nextLane, other))\n            )) {\n              continue;\n            }\n            lane.edge.points = candidate;\n            fixed = true;\n            break;\n          }\n          if (fixed) {\n            break;\n          }\n        }\n      }\n    }\n    if (!fixed) {\n      return;\n    }\n  }\n}\n__name(separateSharedRenderedTerminalLanes, \"separateSharedRenderedTerminalLanes\");\nfunction collapseRedundantRectangularDoglegs(edges, nodeByIdMap) {\n  const BUFFER = 2;\n  const MAX_ITERATIONS = 8;\n  const { realNodeRects, labelNodeRects: labelRects } = collectNodeRectEntries(\n    nodeByIdMap.values()\n  );\n  const candidateIsSafe = /* @__PURE__ */ __name((edge, candidate) => {\n    const sourceId = edge.start;\n    const targetId = edge.end;\n    const candidateSegments = segmentsFor(candidate);\n    if (candidateSegments.length !== candidate.length - 1) {\n      return false;\n    }\n    const endpointIds = [sourceId, targetId].filter((id) => Boolean(id));\n    for (const segment of candidateSegments) {\n      if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -BUFFER)) {\n        return false;\n      }\n      if (segmentHitsAnyRect(segment.a, segment.b, labelRects, [], -BUFFER)) {\n        return false;\n      }\n    }\n    for (const other of edges) {\n      if (other === edge || other.isLayoutOnly) {\n        continue;\n      }\n      const otherPoints = other.points;\n      if (!otherPoints || otherPoints.length < 2) {\n        continue;\n      }\n      for (const candidateSegment of candidateSegments) {\n        for (const otherSegment of segmentsFor(dedupeConsecutivePoints(otherPoints))) {\n          if (sameAxisSegmentOverlapLength(candidateSegment, otherSegment, 0.5) >= MIN_SHARED) {\n            return false;\n          }\n          if (orthogonalSegmentsStrictlyCross(\n            candidateSegment.a,\n            candidateSegment.b,\n            otherSegment.a,\n            otherSegment.b,\n            EPS_LOCAL\n          )) {\n            return false;\n          }\n        }\n      }\n    }\n    return true;\n  }, \"candidateIsSafe\");\n  const withoutDogleg = /* @__PURE__ */ __name((points, i) => {\n    if (i + 4 >= points.length) {\n      return void 0;\n    }\n    const p0 = points[i];\n    const p1 = points[i + 1];\n    const p2 = points[i + 2];\n    const p3 = points[i + 3];\n    const p4 = points[i + 4];\n    const terminalVerticalDogleg = isHorizontalSegment(p0, p1) && isVerticalSegment(p1, p2) && isHorizontalSegment(p2, p3) && isVerticalSegment(p3, p4) && sameX(p0, p3, EPS_LOCAL) && sameX(p0, p4, EPS_LOCAL) && sameX(p1, p2, EPS_LOCAL) && (p1.x - p0.x) * (p3.x - p2.x) < 0;\n    const terminalHorizontalDogleg = isVerticalSegment(p0, p1) && isHorizontalSegment(p1, p2) && isVerticalSegment(p2, p3) && isHorizontalSegment(p3, p4) && sameY(p0, p3, EPS_LOCAL) && sameY(p0, p4, EPS_LOCAL) && sameY(p1, p2, EPS_LOCAL) && (p1.y - p0.y) * (p3.y - p2.y) < 0;\n    if (terminalVerticalDogleg || terminalHorizontalDogleg) {\n      return dedupeConsecutivePoints([...points.slice(0, i + 1), p4, ...points.slice(i + 5)]);\n    }\n    if (i + 5 >= points.length) {\n      return void 0;\n    }\n    const p5 = points[i + 5];\n    const verticalDogleg = isVerticalSegment(p0, p1) && isHorizontalSegment(p1, p2) && isVerticalSegment(p2, p3) && isHorizontalSegment(p3, p4) && isVerticalSegment(p4, p5) && sameX(p0, p4, EPS_LOCAL) && sameX(p0, p5, EPS_LOCAL) && sameX(p2, p3, EPS_LOCAL) && (p2.x - p1.x) * (p4.x - p3.x) < 0;\n    const horizontalDogleg = isHorizontalSegment(p0, p1) && isVerticalSegment(p1, p2) && isHorizontalSegment(p2, p3) && isVerticalSegment(p3, p4) && isHorizontalSegment(p4, p5) && sameY(p0, p4, EPS_LOCAL) && sameY(p0, p5, EPS_LOCAL) && sameY(p2, p3, EPS_LOCAL) && (p2.y - p1.y) * (p4.y - p3.y) < 0;\n    if (!verticalDogleg && !horizontalDogleg) {\n      return void 0;\n    }\n    return dedupeConsecutivePoints([...points.slice(0, i + 1), p5, ...points.slice(i + 6)]);\n  }, \"withoutDogleg\");\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    let fixed = false;\n    for (const edge of edges) {\n      if (edge.isLayoutOnly) {\n        continue;\n      }\n      const points = dedupeConsecutivePoints(edge.points ?? []);\n      for (let i = 0; i <= points.length - 5; i++) {\n        const candidate = withoutDogleg(points, i);\n        if (!candidate || !candidateIsSafe(edge, candidate)) {\n          continue;\n        }\n        edge.points = candidate;\n        fixed = true;\n        break;\n      }\n      if (fixed) {\n        break;\n      }\n    }\n    if (!fixed) {\n      return;\n    }\n  }\n}\n__name(collapseRedundantRectangularDoglegs, \"collapseRedundantRectangularDoglegs\");\nfunction liftObstacleHuggingSameSideRails(edges, nodeByIdMap) {\n  const BUFFER = 2;\n  const CLEARANCE = 20;\n  const MAX_ITERATIONS = 8;\n  const { realNodeRects, labelNodeRects: labelRects } = collectNodeRectEntries(\n    nodeByIdMap.values()\n  );\n  const visibleEdges = edges.filter((edge) => !edge.isLayoutOnly);\n  const pointsFor = /* @__PURE__ */ __name((edge, replacementEdge, replacement) => dedupeConsecutivePoints(\n    edge === replacementEdge ? replacement ?? [] : edge.points ?? []\n  ), \"pointsFor\");\n  const strictCrossingCount = /* @__PURE__ */ __name((replacementEdge, replacement) => {\n    let count = 0;\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const firstSegments = segmentsFor(pointsFor(visibleEdges[i], replacementEdge, replacement));\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const secondSegments = segmentsFor(\n          pointsFor(visibleEdges[j], replacementEdge, replacement)\n        );\n        for (const firstSegment of firstSegments) {\n          for (const secondSegment of secondSegments) {\n            if (orthogonalSegmentsStrictlyCross(\n              firstSegment.a,\n              firstSegment.b,\n              secondSegment.a,\n              secondSegment.b,\n              EPS_LOCAL\n            )) {\n              count++;\n            }\n          }\n        }\n      }\n    }\n    return count;\n  }, \"strictCrossingCount\");\n  const middleRail = /* @__PURE__ */ __name((points) => {\n    const segments = segmentsFor(points);\n    if (segments.length !== 3) {\n      return void 0;\n    }\n    const middle = segments[1];\n    if (segments[0].horizontal === middle.horizontal || segments[2].horizontal === middle.horizontal) {\n      return void 0;\n    }\n    return {\n      index: middle.index,\n      horizontal: middle.horizontal,\n      vertical: middle.vertical,\n      segment: middle\n    };\n  }, \"middleRail\");\n  const blockingRectsFor = /* @__PURE__ */ __name((edge, rail) => {\n    const endpointIds = [edge.start, edge.end].filter(\n      (id) => Boolean(id)\n    );\n    return realNodeRects.filter((entry) => {\n      if (endpointIds.includes(entry.id)) {\n        return false;\n      }\n      const rect = entry.rect;\n      if (rail.horizontal) {\n        const xOverlap = overlapLength(rail.a.x, rail.b.x, rect.left, rect.right);\n        return xOverlap >= MIN_SHARED && rail.a.y >= rect.top - BUFFER && rail.a.y <= rect.bottom + BUFFER;\n      }\n      const yOverlap = overlapLength(rail.a.y, rail.b.y, rect.top, rect.bottom);\n      return yOverlap >= MIN_SHARED && rail.a.x >= rect.left - BUFFER && rail.a.x <= rect.right + BUFFER;\n    });\n  }, \"blockingRectsFor\");\n  const candidateByMovingRail = /* @__PURE__ */ __name((points, rail, coord) => {\n    const candidate = points.map((point) => ({ ...point }));\n    if (rail.horizontal) {\n      candidate[rail.index].y = coord;\n      candidate[rail.index + 1].y = coord;\n    } else if (rail.vertical) {\n      candidate[rail.index].x = coord;\n      candidate[rail.index + 1].x = coord;\n    } else {\n      return void 0;\n    }\n    const simplified = simplifyPolyline(dedupeConsecutivePoints(candidate));\n    return segmentsFor(simplified).length === simplified.length - 1 ? simplified : void 0;\n  }, \"candidateByMovingRail\");\n  const candidateIsSafe = /* @__PURE__ */ __name((edge, candidate, currentCrossings) => {\n    const endpointIds = [edge.start, edge.end].filter(\n      (id) => Boolean(id)\n    );\n    const candidateSegments = segmentsFor(candidate);\n    if (candidateSegments.length !== candidate.length - 1) {\n      return false;\n    }\n    for (const segment of candidateSegments) {\n      if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -BUFFER)) {\n        return false;\n      }\n      if (segmentHitsAnyRect(segment.a, segment.b, labelRects, [], -BUFFER)) {\n        return false;\n      }\n    }\n    for (const other of visibleEdges) {\n      if (other === edge) {\n        continue;\n      }\n      for (const candidateSegment of candidateSegments) {\n        for (const otherSegment of segmentsFor(pointsFor(other))) {\n          if (sameAxisSegmentOverlapLength(candidateSegment, otherSegment, 0.5) >= MIN_SHARED) {\n            return false;\n          }\n        }\n      }\n    }\n    return strictCrossingCount(edge, candidate) <= currentCrossings;\n  }, \"candidateIsSafe\");\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    const currentCrossings = strictCrossingCount();\n    let fixed = false;\n    for (const edge of visibleEdges) {\n      const points = pointsFor(edge);\n      const rail = middleRail(points);\n      if (!rail) {\n        continue;\n      }\n      const blockers = blockingRectsFor(edge, rail.segment);\n      if (blockers.length === 0) {\n        continue;\n      }\n      const coords = rail.horizontal ? [\n        Math.min(...blockers.map((entry) => entry.rect.top)) - CLEARANCE,\n        Math.max(...blockers.map((entry) => entry.rect.bottom)) + CLEARANCE\n      ] : [\n        Math.min(...blockers.map((entry) => entry.rect.left)) - CLEARANCE,\n        Math.max(...blockers.map((entry) => entry.rect.right)) + CLEARANCE\n      ];\n      for (const coord of coords) {\n        const candidate = candidateByMovingRail(points, rail.segment, coord);\n        if (!candidate || !candidateIsSafe(edge, candidate, currentCrossings)) {\n          continue;\n        }\n        edge.points = candidate;\n        fixed = true;\n        break;\n      }\n      if (fixed) {\n        break;\n      }\n    }\n    if (!fixed) {\n      return;\n    }\n  }\n}\n__name(liftObstacleHuggingSameSideRails, \"liftObstacleHuggingSameSideRails\");\nfunction liftTopLaneTitleBandsAboveRails(edges, nodeByIdMap) {\n  const CLEARANCE = 4;\n  const validTitleRect = /* @__PURE__ */ __name((node) => {\n    const rect = node.groupTitleRect;\n    if (!rect || typeof rect.left !== \"number\" || typeof rect.right !== \"number\" || typeof rect.top !== \"number\" || typeof rect.bottom !== \"number\" || !Number.isFinite(rect.left) || !Number.isFinite(rect.right) || !Number.isFinite(rect.top) || !Number.isFinite(rect.bottom) || rect.right <= rect.left || rect.bottom <= rect.top) {\n      return void 0;\n    }\n    return { left: rect.left, right: rect.right, top: rect.top, bottom: rect.bottom };\n  }, \"validTitleRect\");\n  const topLaneTitleFor = /* @__PURE__ */ __name((node) => {\n    if (!node.isGroup || node.parentId) {\n      return void 0;\n    }\n    const rawDirection = node.direction;\n    const direction = typeof rawDirection === \"string\" ? rawDirection.toUpperCase() : \"\";\n    if (direction === \"LR\" || direction === \"RL\" || direction === \"BT\") {\n      return void 0;\n    }\n    const rect = validTitleRect(node);\n    const y = node.y;\n    const height = node.height;\n    if (!rect || typeof y !== \"number\" || typeof height !== \"number\" || !Number.isFinite(y) || !Number.isFinite(height) || height <= 0) {\n      return void 0;\n    }\n    const titleWidth = rect.right - rect.left;\n    const titleHeight = rect.bottom - rect.top;\n    if (titleHeight <= 0 || titleWidth < titleHeight) {\n      return void 0;\n    }\n    return { node, rect };\n  }, \"topLaneTitleFor\");\n  const horizontalSegmentIntersectsTitle = /* @__PURE__ */ __name((segment, rect) => {\n    if (!segment.horizontal) {\n      return false;\n    }\n    const y = segment.a.y;\n    if (y <= rect.top + EPS_LOCAL || y >= rect.bottom - EPS_LOCAL) {\n      return false;\n    }\n    return overlapLength(segment.a.x, segment.b.x, rect.left, rect.right) >= MIN_SHARED;\n  }, \"horizontalSegmentIntersectsTitle\");\n  const lanes = [...nodeByIdMap.values()].map(topLaneTitleFor).filter((lane) => Boolean(lane));\n  if (lanes.length === 0) {\n    return;\n  }\n  let topDelta = 0;\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const points = dedupeConsecutivePoints(edge.points ?? []);\n    for (const segment of segmentsFor(points)) {\n      for (const lane of lanes) {\n        if (!horizontalSegmentIntersectsTitle(segment, lane.rect)) {\n          continue;\n        }\n        topDelta = Math.max(topDelta, lane.rect.bottom - segment.a.y + CLEARANCE);\n      }\n    }\n  }\n  if (topDelta <= EPS_LOCAL) {\n    return;\n  }\n  for (const lane of lanes) {\n    const y = lane.node.y;\n    const height = lane.node.height;\n    if (typeof y !== \"number\" || typeof height !== \"number\" || !Number.isFinite(y) || !Number.isFinite(height) || height <= 0) {\n      continue;\n    }\n    lane.node.y = y - topDelta / 2;\n    lane.node.height = height + topDelta;\n    lane.node.groupTitleRect = {\n      ...lane.rect,\n      top: lane.rect.top - topDelta,\n      bottom: lane.rect.bottom - topDelta\n    };\n  }\n}\n__name(liftTopLaneTitleBandsAboveRails, \"liftTopLaneTitleBandsAboveRails\");\nfunction shiftLeftLaneTitleBandsLeftOfRails(edges, nodeByIdMap) {\n  const CLEARANCE = 4;\n  const validTitleRect = /* @__PURE__ */ __name((node) => {\n    const rect = node.groupTitleRect;\n    if (!rect || typeof rect.left !== \"number\" || typeof rect.right !== \"number\" || typeof rect.top !== \"number\" || typeof rect.bottom !== \"number\" || !Number.isFinite(rect.left) || !Number.isFinite(rect.right) || !Number.isFinite(rect.top) || !Number.isFinite(rect.bottom) || rect.right <= rect.left || rect.bottom <= rect.top) {\n      return void 0;\n    }\n    return { left: rect.left, right: rect.right, top: rect.top, bottom: rect.bottom };\n  }, \"validTitleRect\");\n  const leftLaneTitleFor = /* @__PURE__ */ __name((node) => {\n    if (!node.isGroup || node.parentId) {\n      return void 0;\n    }\n    const rawDirection = node.direction;\n    if (rawDirection !== \"LR\") {\n      return void 0;\n    }\n    const rect = validTitleRect(node);\n    const x = node.x;\n    const width = node.width;\n    if (!rect || typeof x !== \"number\" || typeof width !== \"number\" || !Number.isFinite(x) || !Number.isFinite(width) || width <= 0) {\n      return void 0;\n    }\n    const titleWidth = rect.right - rect.left;\n    const titleHeight = rect.bottom - rect.top;\n    if (titleWidth <= 0 || titleHeight < titleWidth) {\n      return void 0;\n    }\n    return { node, rect };\n  }, \"leftLaneTitleFor\");\n  const verticalSegmentIntersectsTitle = /* @__PURE__ */ __name((segment, rect) => {\n    if (!segment.vertical) {\n      return false;\n    }\n    const x = segment.a.x;\n    if (x <= rect.left + EPS_LOCAL || x >= rect.right - EPS_LOCAL) {\n      return false;\n    }\n    return overlapLength(segment.a.y, segment.b.y, rect.top, rect.bottom) >= MIN_SHARED;\n  }, \"verticalSegmentIntersectsTitle\");\n  const horizontalSegmentIntersectsTitle = /* @__PURE__ */ __name((segment, rect) => {\n    if (!segment.horizontal) {\n      return false;\n    }\n    const y = segment.a.y;\n    if (y <= rect.top + EPS_LOCAL || y >= rect.bottom - EPS_LOCAL) {\n      return false;\n    }\n    return overlapLength(segment.a.x, segment.b.x, rect.left, rect.right) >= MIN_SHARED;\n  }, \"horizontalSegmentIntersectsTitle\");\n  const lanes = [...nodeByIdMap.values()].map(leftLaneTitleFor).filter((lane) => Boolean(lane));\n  if (lanes.length === 0) {\n    return;\n  }\n  let leftDelta = 0;\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const points = dedupeConsecutivePoints(edge.points ?? []);\n    for (const segment of segmentsFor(points)) {\n      for (const lane of lanes) {\n        if (verticalSegmentIntersectsTitle(segment, lane.rect)) {\n          leftDelta = Math.max(leftDelta, lane.rect.right - segment.a.x + CLEARANCE);\n        } else if (horizontalSegmentIntersectsTitle(segment, lane.rect)) {\n          const segmentLeft = Math.min(segment.a.x, segment.b.x);\n          leftDelta = Math.max(leftDelta, lane.rect.right - segmentLeft + CLEARANCE);\n        }\n      }\n    }\n  }\n  if (leftDelta <= EPS_LOCAL) {\n    return;\n  }\n  for (const lane of lanes) {\n    const x = lane.node.x;\n    const width = lane.node.width;\n    if (typeof x !== \"number\" || typeof width !== \"number\" || !Number.isFinite(x) || !Number.isFinite(width) || width <= 0) {\n      continue;\n    }\n    lane.node.x = x - leftDelta / 2;\n    lane.node.width = width + leftDelta;\n    lane.node.groupTitleRect = {\n      ...lane.rect,\n      left: lane.rect.left - leftDelta,\n      right: lane.rect.right - leftDelta\n    };\n  }\n}\n__name(shiftLeftLaneTitleBandsLeftOfRails, \"shiftLeftLaneTitleBandsLeftOfRails\");\nfunction swapDestinationTerminalTailsToReduceCrossings(edges, nodeByIdMap) {\n  const BUFFER = 2;\n  const MAX_ITERATIONS = 4;\n  const { realNodeRects } = collectNodeRectEntries(nodeByIdMap.values());\n  const visibleEdges = edges.filter((edge) => !edge.isLayoutOnly);\n  const replacementPointsFor = /* @__PURE__ */ __name((edge, replacements = /* @__PURE__ */ new Map()) => dedupeConsecutivePoints(\n    replacements.get(edge) ?? edge.points ?? []\n  ), \"replacementPointsFor\");\n  const crossingCount = /* @__PURE__ */ __name((replacements = /* @__PURE__ */ new Map()) => {\n    let count = 0;\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const firstSegments = segmentsFor(replacementPointsFor(visibleEdges[i], replacements));\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const secondSegments = segmentsFor(replacementPointsFor(visibleEdges[j], replacements));\n        for (const firstSegment of firstSegments) {\n          for (const secondSegment of secondSegments) {\n            if (orthogonalSegmentsStrictlyCross(\n              firstSegment.a,\n              firstSegment.b,\n              secondSegment.a,\n              secondSegment.b,\n              EPS_LOCAL\n            )) {\n              count++;\n            }\n          }\n        }\n      }\n    }\n    return count;\n  }, \"crossingCount\");\n  const totalBends = /* @__PURE__ */ __name((replacements = /* @__PURE__ */ new Map()) => visibleEdges.reduce(\n    (sum, edge) => sum + countOrthogonalBends(replacementPointsFor(edge, replacements)),\n    0\n  ), \"totalBends\");\n  const terminalTailFor = /* @__PURE__ */ __name((edge) => {\n    const points = replacementPointsFor(edge);\n    if (points.length < 4) {\n      return void 0;\n    }\n    const tailStart = points[points.length - 2];\n    const terminal = points[points.length - 1];\n    if (!isHorizontalSegment(tailStart, terminal, EPS_LOCAL) && !isVerticalSegment(tailStart, terminal, EPS_LOCAL)) {\n      return void 0;\n    }\n    return { tailStart, terminal };\n  }, \"terminalTailFor\");\n  const candidateWithDestinationTail = /* @__PURE__ */ __name((edge, tail) => {\n    const points = replacementPointsFor(edge);\n    if (points.length < 3) {\n      return void 0;\n    }\n    const start = points[0];\n    const firstTurn = points[1];\n    let connector;\n    if (isHorizontalSegment(start, firstTurn, EPS_LOCAL)) {\n      connector = { x: firstTurn.x, y: tail.tailStart.y };\n    } else if (isVerticalSegment(start, firstTurn, EPS_LOCAL)) {\n      connector = { x: tail.tailStart.x, y: firstTurn.y };\n    } else {\n      return void 0;\n    }\n    const candidate = simplifyPolyline(\n      dedupeConsecutivePoints([start, firstTurn, connector, tail.tailStart, tail.terminal])\n    );\n    return segmentsFor(candidate).length === candidate.length - 1 ? candidate : void 0;\n  }, \"candidateWithDestinationTail\");\n  const pathHasNodeHit = /* @__PURE__ */ __name((edge, path) => {\n    const endpointIds = [edge.start, edge.end].filter(\n      (id) => Boolean(id)\n    );\n    for (const segment of segmentsFor(path)) {\n      if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -BUFFER)) {\n        return true;\n      }\n    }\n    return false;\n  }, \"pathHasNodeHit\");\n  const pathHasSharedTrack = /* @__PURE__ */ __name((edge, path, replacements) => {\n    for (const other of visibleEdges) {\n      if (other === edge) {\n        continue;\n      }\n      for (const candidateSegment of segmentsFor(path)) {\n        for (const otherSegment of segmentsFor(replacementPointsFor(other, replacements))) {\n          if (sameAxisSegmentOverlapLength(candidateSegment, otherSegment, 0.5) >= MIN_SHARED) {\n            return true;\n          }\n        }\n      }\n    }\n    return false;\n  }, \"pathHasSharedTrack\");\n  const candidateIsSafe = /* @__PURE__ */ __name((edge, path, replacements) => !pathHasNodeHit(edge, path) && !pathHasSharedTrack(edge, path, replacements), \"candidateIsSafe\");\n  const edgesByDestination = /* @__PURE__ */ __name(() => {\n    const result = /* @__PURE__ */ new Map();\n    for (const edge of visibleEdges) {\n      const dstId = edge.end;\n      if (!dstId || !nodeByIdMap.has(dstId)) {\n        continue;\n      }\n      const points = replacementPointsFor(edge);\n      if (points.length < 4) {\n        continue;\n      }\n      const bucket = result.get(dstId) ?? [];\n      bucket.push(edge);\n      result.set(dstId, bucket);\n    }\n    return result;\n  }, \"edgesByDestination\");\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    const currentCrossings = crossingCount();\n    if (currentCrossings === 0) {\n      return;\n    }\n    const currentBends = totalBends();\n    let bestReplacements;\n    let bestCrossings = currentCrossings;\n    let bestBends = currentBends;\n    for (const destinationEdges of edgesByDestination().values()) {\n      for (let i = 0; i < destinationEdges.length; i++) {\n        for (let j = i + 1; j < destinationEdges.length; j++) {\n          const first = destinationEdges[i];\n          const second = destinationEdges[j];\n          const firstTail = terminalTailFor(first);\n          const secondTail = terminalTailFor(second);\n          if (!firstTail || !secondTail) {\n            continue;\n          }\n          const firstCandidate = candidateWithDestinationTail(first, secondTail);\n          const secondCandidate = candidateWithDestinationTail(second, firstTail);\n          if (!firstCandidate || !secondCandidate) {\n            continue;\n          }\n          const replacements = /* @__PURE__ */ new Map([\n            [first, firstCandidate],\n            [second, secondCandidate]\n          ]);\n          if (!candidateIsSafe(first, firstCandidate, replacements) || !candidateIsSafe(second, secondCandidate, replacements)) {\n            continue;\n          }\n          const candidateCrossings = crossingCount(replacements);\n          const candidateBends = totalBends(replacements);\n          if (candidateCrossings >= currentCrossings) {\n            continue;\n          }\n          if (candidateCrossings > bestCrossings || candidateCrossings === bestCrossings && candidateBends >= bestBends) {\n            continue;\n          }\n          bestReplacements = replacements;\n          bestCrossings = candidateCrossings;\n          bestBends = candidateBends;\n        }\n      }\n    }\n    if (!bestReplacements) {\n      return;\n    }\n    for (const [edge, points] of bestReplacements) {\n      edge.points = points;\n    }\n  }\n}\n__name(swapDestinationTerminalTailsToReduceCrossings, \"swapDestinationTerminalTailsToReduceCrossings\");\nfunction reassignCrossingExternalRailChannels(edges, nodeByIdMap) {\n  const BUFFER = 2;\n  const RAIL_CHANNEL_GAP = 12;\n  const MAX_ITERATIONS = 4;\n  const MAX_EXHAUSTIVE_COMPONENT = 6;\n  const { realNodeRects, labelNodeRects: labelRects } = collectNodeRectEntries(\n    nodeByIdMap.values()\n  );\n  const visibleEdges = edges.filter((edge) => !edge.isLayoutOnly);\n  const replacementPointsFor = /* @__PURE__ */ __name((edge, replacements = /* @__PURE__ */ new Map()) => dedupeConsecutivePoints(\n    replacements.get(edge) ?? edge.points ?? []\n  ), \"replacementPointsFor\");\n  const strictCrossingCount = /* @__PURE__ */ __name((replacements = /* @__PURE__ */ new Map()) => {\n    let count = 0;\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const firstSegments = segmentsFor(replacementPointsFor(visibleEdges[i], replacements));\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const secondSegments = segmentsFor(replacementPointsFor(visibleEdges[j], replacements));\n        for (const firstSegment of firstSegments) {\n          for (const secondSegment of secondSegments) {\n            if (orthogonalSegmentsStrictlyCross(\n              firstSegment.a,\n              firstSegment.b,\n              secondSegment.a,\n              secondSegment.b,\n              EPS_LOCAL\n            )) {\n              count++;\n            }\n          }\n        }\n      }\n    }\n    return count;\n  }, \"strictCrossingCount\");\n  const totalBends = /* @__PURE__ */ __name((replacements = /* @__PURE__ */ new Map()) => visibleEdges.reduce(\n    (sum, edge) => sum + countOrthogonalBends(replacementPointsFor(edge, replacements)),\n    0\n  ), \"totalBends\");\n  const endpointRectsFor = /* @__PURE__ */ __name((edge) => {\n    const srcId = edge.start;\n    const dstId = edge.end;\n    const srcNode = srcId ? nodeByIdMap.get(srcId) : void 0;\n    const dstNode = dstId ? nodeByIdMap.get(dstId) : void 0;\n    const src = srcNode ? rectOfNodeBounds(srcNode) : void 0;\n    const dst = dstNode ? rectOfNodeBounds(dstNode) : void 0;\n    return src && dst ? { src, dst } : void 0;\n  }, \"endpointRectsFor\");\n  const externalRailForSegment = /* @__PURE__ */ __name((edge, points, segment) => {\n    if (segment.index <= 0 || segment.index + 1 >= points.length - 1) {\n      return void 0;\n    }\n    const endpointRects = endpointRectsFor(edge);\n    if (!endpointRects) {\n      return void 0;\n    }\n    if (segment.vertical) {\n      const coord = segment.a.x;\n      const leftBound = Math.min(endpointRects.src.left, endpointRects.dst.left);\n      const rightBound = Math.max(endpointRects.src.right, endpointRects.dst.right);\n      const side = coord < leftBound - EPS_LOCAL ? \"left\" : coord > rightBound + EPS_LOCAL ? \"right\" : void 0;\n      if (!side) {\n        return void 0;\n      }\n      return {\n        edge,\n        points,\n        segmentIndex: segment.index,\n        axis: \"vertical\",\n        side,\n        coord,\n        min: Math.min(segment.a.y, segment.b.y),\n        max: Math.max(segment.a.y, segment.b.y)\n      };\n    }\n    if (segment.horizontal) {\n      const coord = segment.a.y;\n      const topBound = Math.min(endpointRects.src.top, endpointRects.dst.top);\n      const bottomBound = Math.max(endpointRects.src.bottom, endpointRects.dst.bottom);\n      const side = coord < topBound - EPS_LOCAL ? \"top\" : coord > bottomBound + EPS_LOCAL ? \"bottom\" : void 0;\n      if (!side) {\n        return void 0;\n      }\n      return {\n        edge,\n        points,\n        segmentIndex: segment.index,\n        axis: \"horizontal\",\n        side,\n        coord,\n        min: Math.min(segment.a.x, segment.b.x),\n        max: Math.max(segment.a.x, segment.b.x)\n      };\n    }\n    return void 0;\n  }, \"externalRailForSegment\");\n  const collectExternalRails = /* @__PURE__ */ __name(() => {\n    const rails = [];\n    for (const edge of visibleEdges) {\n      const points = replacementPointsFor(edge);\n      for (const segment of segmentsFor(points)) {\n        const rail = externalRailForSegment(edge, points, segment);\n        if (rail) {\n          rails.push(rail);\n        }\n      }\n    }\n    return rails;\n  }, \"collectExternalRails\");\n  const railsInteract = /* @__PURE__ */ __name((a, b) => a.edge !== b.edge && a.axis === b.axis && a.side === b.side && overlapLength(a.min, a.max, b.min, b.max) >= MIN_SHARED, \"railsInteract\");\n  const connectedComponents = /* @__PURE__ */ __name((rails) => {\n    const result = [];\n    const seen = /* @__PURE__ */ new Set();\n    for (const rail of rails) {\n      if (seen.has(rail)) {\n        continue;\n      }\n      const queue = [rail];\n      const component = [];\n      seen.add(rail);\n      while (queue.length > 0) {\n        const current = queue.pop();\n        component.push(current);\n        for (const next of rails) {\n          if (!seen.has(next) && railsInteract(current, next)) {\n            seen.add(next);\n            queue.push(next);\n          }\n        }\n      }\n      if (component.length > 1) {\n        result.push(component);\n      }\n    }\n    return result;\n  }, \"connectedComponents\");\n  const uniqueCoordsFor = /* @__PURE__ */ __name((component) => {\n    const coords = [];\n    for (const rail of component) {\n      if (!coords.some((coord) => Math.abs(coord - rail.coord) < EPS_LOCAL)) {\n        coords.push(rail.coord);\n      }\n    }\n    while (coords.length < component.length) {\n      const min = Math.min(...coords);\n      const max = Math.max(...coords);\n      const side = component[0].side;\n      coords.push(\n        side === \"left\" || side === \"top\" ? min - RAIL_CHANNEL_GAP * (component.length - coords.length) : max + RAIL_CHANNEL_GAP * (component.length - coords.length)\n      );\n    }\n    return coords;\n  }, \"uniqueCoordsFor\");\n  const coordinateAssignmentsFor = /* @__PURE__ */ __name((component) => {\n    const current = component.map((rail) => rail.coord);\n    const coords = uniqueCoordsFor(component);\n    const assignments = [];\n    if (component.length <= MAX_EXHAUSTIVE_COMPONENT) {\n      const used = new Array(coords.length).fill(false);\n      const next = [];\n      const visit = /* @__PURE__ */ __name(() => {\n        if (next.length === component.length) {\n          if (next.some((coord, index) => Math.abs(coord - current[index]) >= EPS_LOCAL)) {\n            assignments.push([...next]);\n          }\n          return;\n        }\n        for (const [i, coord] of coords.entries()) {\n          if (used[i]) {\n            continue;\n          }\n          used[i] = true;\n          next.push(coord);\n          visit();\n          next.pop();\n          used[i] = false;\n        }\n      }, \"visit\");\n      visit();\n      return assignments;\n    }\n    for (let i = 0; i < current.length; i++) {\n      for (let j = i + 1; j < current.length; j++) {\n        const assignment = [...current];\n        [assignment[i], assignment[j]] = [assignment[j], assignment[i]];\n        assignments.push(assignment);\n      }\n    }\n    return assignments;\n  }, \"coordinateAssignmentsFor\");\n  const replacementsForAssignment = /* @__PURE__ */ __name((component, assignment) => {\n    const draftByEdge = /* @__PURE__ */ new Map();\n    for (const [i, rail] of component.entries()) {\n      const coord = assignment[i];\n      const points = draftByEdge.get(rail.edge) ?? rail.points.map((point) => ({ x: point.x, y: point.y }));\n      if (rail.axis === \"vertical\") {\n        points[rail.segmentIndex].x = coord;\n        points[rail.segmentIndex + 1].x = coord;\n      } else {\n        points[rail.segmentIndex].y = coord;\n        points[rail.segmentIndex + 1].y = coord;\n      }\n      draftByEdge.set(rail.edge, points);\n    }\n    const replacements = /* @__PURE__ */ new Map();\n    for (const [edge, points] of draftByEdge) {\n      const simplified = simplifyPolyline(dedupeConsecutivePoints(points));\n      if (segmentsFor(simplified).length !== simplified.length - 1) {\n        return void 0;\n      }\n      replacements.set(edge, simplified);\n    }\n    return replacements;\n  }, \"replacementsForAssignment\");\n  const candidateIsSafe = /* @__PURE__ */ __name((replacements) => {\n    for (const [edge, points] of replacements) {\n      const endpointIds = [\n        edge.start,\n        edge.end\n      ].filter((id) => Boolean(id));\n      for (const segment of segmentsFor(points)) {\n        if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -BUFFER)) {\n          return false;\n        }\n        if (segmentHitsAnyRect(segment.a, segment.b, labelRects, [], -BUFFER)) {\n          return false;\n        }\n      }\n    }\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const first = visibleEdges[i];\n      const firstChanged = replacements.has(first);\n      const firstSegments = segmentsFor(replacementPointsFor(first, replacements));\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const second = visibleEdges[j];\n        if (!firstChanged && !replacements.has(second)) {\n          continue;\n        }\n        const secondSegments = segmentsFor(replacementPointsFor(second, replacements));\n        for (const firstSegment of firstSegments) {\n          for (const secondSegment of secondSegments) {\n            if (sameAxisSegmentOverlapLength(firstSegment, secondSegment, 0.5) >= MIN_SHARED) {\n              return false;\n            }\n          }\n        }\n      }\n    }\n    return true;\n  }, \"candidateIsSafe\");\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    const currentCrossings = strictCrossingCount();\n    if (currentCrossings === 0) {\n      return;\n    }\n    let bestReplacements;\n    let bestCrossings = currentCrossings;\n    let bestBends = totalBends();\n    let bestDisplacement = Number.POSITIVE_INFINITY;\n    for (const component of connectedComponents(collectExternalRails())) {\n      for (const assignment of coordinateAssignmentsFor(component)) {\n        const replacements = replacementsForAssignment(component, assignment);\n        if (!replacements || !candidateIsSafe(replacements)) {\n          continue;\n        }\n        const candidateCrossings = strictCrossingCount(replacements);\n        if (candidateCrossings >= currentCrossings) {\n          continue;\n        }\n        const candidateBends = totalBends(replacements);\n        const candidateDisplacement = component.reduce(\n          (sum, rail, index) => sum + Math.abs(assignment[index] - rail.coord),\n          0\n        );\n        if (candidateCrossings > bestCrossings || candidateCrossings === bestCrossings && (candidateBends > bestBends || candidateBends === bestBends && candidateDisplacement >= bestDisplacement)) {\n          continue;\n        }\n        bestReplacements = replacements;\n        bestCrossings = candidateCrossings;\n        bestBends = candidateBends;\n        bestDisplacement = candidateDisplacement;\n      }\n    }\n    if (!bestReplacements) {\n      return;\n    }\n    for (const [edge, points] of bestReplacements) {\n      edge.points = points;\n    }\n  }\n}\n__name(reassignCrossingExternalRailChannels, \"reassignCrossingExternalRailChannels\");\nfunction shortcutRedundantOrthogonalJogs(edges, nodeByIdMap) {\n  const BUFFER = 2;\n  const MAX_ITERATIONS = 8;\n  const { realNodeRects, labelNodeRects: labelRects } = collectNodeRectEntries(\n    nodeByIdMap.values()\n  );\n  const visibleEdges = edges.filter((edge) => !edge.isLayoutOnly);\n  const pointsFor = /* @__PURE__ */ __name((edge, replacementEdge, replacement) => dedupeConsecutivePoints(\n    edge === replacementEdge ? replacement ?? [] : edge.points ?? []\n  ), \"pointsFor\");\n  const pathLength = /* @__PURE__ */ __name((points) => segmentsFor(points).reduce((sum, segment) => {\n    const dx = segment.a.x - segment.b.x;\n    const dy = segment.a.y - segment.b.y;\n    return sum + Math.hypot(dx, dy);\n  }, 0), \"pathLength\");\n  const strictCrossingCount = /* @__PURE__ */ __name((replacementEdge, replacement) => {\n    let count = 0;\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const firstSegments = segmentsFor(pointsFor(visibleEdges[i], replacementEdge, replacement));\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const secondSegments = segmentsFor(\n          pointsFor(visibleEdges[j], replacementEdge, replacement)\n        );\n        for (const firstSegment of firstSegments) {\n          for (const secondSegment of secondSegments) {\n            if (orthogonalSegmentsStrictlyCross(\n              firstSegment.a,\n              firstSegment.b,\n              secondSegment.a,\n              secondSegment.b,\n              EPS_LOCAL\n            )) {\n              count++;\n            }\n          }\n        }\n      }\n    }\n    return count;\n  }, \"strictCrossingCount\");\n  const segmentRunsAlongRectBorder = /* @__PURE__ */ __name((segment, rect) => {\n    if (segment.horizontal) {\n      const y = segment.a.y;\n      const onBorder = Math.abs(y - rect.top) < 1 || Math.abs(y - rect.bottom) < 1;\n      return onBorder && overlapLength(segment.a.x, segment.b.x, rect.left, rect.right) >= MIN_SHARED;\n    }\n    if (segment.vertical) {\n      const x = segment.a.x;\n      const onBorder = Math.abs(x - rect.left) < 1 || Math.abs(x - rect.right) < 1;\n      return onBorder && overlapLength(segment.a.y, segment.b.y, rect.top, rect.bottom) >= MIN_SHARED;\n    }\n    return false;\n  }, \"segmentRunsAlongRectBorder\");\n  const endpointRectsFor = /* @__PURE__ */ __name((edge) => {\n    const endpointIds = [edge.start, edge.end].filter(\n      (id) => Boolean(id)\n    );\n    const rects = [];\n    for (const id of endpointIds) {\n      const node = nodeByIdMap.get(id);\n      const rect = node ? rectOfNodeBounds(node) : void 0;\n      if (rect) {\n        rects.push(rect);\n      }\n    }\n    return rects;\n  }, \"endpointRectsFor\");\n  const shortcutCandidatesAt = /* @__PURE__ */ __name((points, index) => {\n    if (index + 3 >= points.length) {\n      return [];\n    }\n    const p0 = points[index];\n    const p1 = points[index + 1];\n    const p2 = points[index + 2];\n    const p3 = points[index + 3];\n    const isHVH = isHorizontalSegment(p0, p1, EPS_LOCAL) && isVerticalSegment(p1, p2, EPS_LOCAL) && isHorizontalSegment(p2, p3, EPS_LOCAL);\n    const isVHV = isVerticalSegment(p0, p1, EPS_LOCAL) && isHorizontalSegment(p1, p2, EPS_LOCAL) && isVerticalSegment(p2, p3, EPS_LOCAL);\n    if (!isHVH && !isVHV) {\n      return [];\n    }\n    const outerSegmentsOppose = isHVH ? Math.sign(p1.x - p0.x) !== Math.sign(p3.x - p2.x) : Math.sign(p1.y - p0.y) !== Math.sign(p3.y - p2.y);\n    if (!outerSegmentsOppose) {\n      return [];\n    }\n    const corners = sameX(p0, p3, EPS_LOCAL) || sameY(p0, p3, EPS_LOCAL) ? [] : [\n      { x: p0.x, y: p3.y },\n      { x: p3.x, y: p0.y }\n    ];\n    const rawCandidates = corners.length === 0 ? [[...points.slice(0, index + 1), ...points.slice(index + 3)]] : corners.map((corner) => [\n      ...points.slice(0, index + 1),\n      corner,\n      ...points.slice(index + 3)\n    ]);\n    const seen = /* @__PURE__ */ new Set();\n    return rawCandidates.map((candidate) => simplifyPolyline(dedupeConsecutivePoints(candidate))).filter((candidate) => {\n      if (segmentsFor(candidate).length !== candidate.length - 1) {\n        return false;\n      }\n      if (!candidate.some((point) => samePoint(point, p3, EPS_LOCAL))) {\n        return false;\n      }\n      const key = candidate.map((point) => `${point.x.toFixed(3)},${point.y.toFixed(3)}`).join(\"|\");\n      if (seen.has(key)) {\n        return false;\n      }\n      seen.add(key);\n      return true;\n    });\n  }, \"shortcutCandidatesAt\");\n  const candidateIsSafe = /* @__PURE__ */ __name((edge, candidate, currentCrossings) => {\n    const endpointIds = [edge.start, edge.end].filter(\n      (id) => Boolean(id)\n    );\n    const endpointRects = endpointRectsFor(edge);\n    for (const segment of segmentsFor(candidate)) {\n      if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -BUFFER)) {\n        return false;\n      }\n      if (segmentHitsAnyRect(segment.a, segment.b, labelRects, [], -BUFFER)) {\n        return false;\n      }\n      if (endpointRects.some((rect) => segmentRunsAlongRectBorder(segment, rect))) {\n        return false;\n      }\n    }\n    for (const other of visibleEdges) {\n      if (other === edge) {\n        continue;\n      }\n      for (const candidateSegment of segmentsFor(candidate)) {\n        for (const otherSegment of segmentsFor(pointsFor(other))) {\n          if (sameAxisSegmentOverlapLength(candidateSegment, otherSegment, 0.5) >= MIN_SHARED) {\n            return false;\n          }\n        }\n      }\n    }\n    return strictCrossingCount(edge, candidate) <= currentCrossings;\n  }, \"candidateIsSafe\");\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    const currentCrossings = strictCrossingCount();\n    let bestEdge;\n    let bestPath;\n    let bestCrossings = currentCrossings;\n    let bestBends = Number.POSITIVE_INFINITY;\n    let bestLength = Number.POSITIVE_INFINITY;\n    for (const edge of visibleEdges) {\n      const currentPoints = pointsFor(edge);\n      const currentBends = countOrthogonalBends(currentPoints, EPS_LOCAL);\n      const currentLength = pathLength(currentPoints);\n      for (let index = 0; index <= currentPoints.length - 4; index++) {\n        for (const candidate of shortcutCandidatesAt(currentPoints, index)) {\n          const candidateBends = countOrthogonalBends(candidate, EPS_LOCAL);\n          const candidateLength = pathLength(candidate);\n          const improvesShape = candidateBends < currentBends || candidateBends === currentBends && candidateLength < currentLength - EPS_LOCAL;\n          if (!improvesShape || !candidateIsSafe(edge, candidate, currentCrossings)) {\n            continue;\n          }\n          const candidateCrossings = strictCrossingCount(edge, candidate);\n          if (candidateCrossings > bestCrossings || candidateCrossings === bestCrossings && (candidateBends > bestBends || candidateBends === bestBends && candidateLength >= bestLength)) {\n            continue;\n          }\n          bestEdge = edge;\n          bestPath = candidate;\n          bestCrossings = candidateCrossings;\n          bestBends = candidateBends;\n          bestLength = candidateLength;\n        }\n      }\n    }\n    if (!bestEdge || !bestPath) {\n      return;\n    }\n    bestEdge.points = bestPath;\n  }\n}\n__name(shortcutRedundantOrthogonalJogs, \"shortcutRedundantOrthogonalJogs\");\nfunction resolveRenderedOrthogonalCrossings(edges, nodeByIdMap) {\n  const ANCHOR = 20;\n  const EXTRA_CHANNEL_COUNT = 2;\n  const MAX_ITERATIONS = 4;\n  const MAX_PAIR_CANDIDATES_PER_EDGE = 48;\n  const realNodes = [];\n  for (const node of nodeByIdMap.values()) {\n    if (node.isGroup || node.isEdgeLabel) {\n      continue;\n    }\n    const cx = node.x ?? 0;\n    const cy = node.y ?? 0;\n    const rect = rectOfNodeBounds(node);\n    if (!rect) {\n      continue;\n    }\n    realNodes.push({\n      id: String(node.id ?? \"\"),\n      cx,\n      cy,\n      rect\n    });\n  }\n  if (realNodes.length === 0) {\n    return;\n  }\n  const nodeInfoById = new Map(realNodes.map((node) => [node.id, node]));\n  const realNodeRects = realNodes.map((node) => ({ id: node.id, rect: node.rect }));\n  const sides = [\"top\", \"bottom\", \"left\", \"right\"];\n  const outsideTracks = {\n    top: Math.min(...realNodes.map((node) => node.rect.top)) - ANCHOR,\n    bottom: Math.max(...realNodes.map((node) => node.rect.bottom)) + ANCHOR,\n    left: Math.min(...realNodes.map((node) => node.rect.left)) - ANCHOR,\n    right: Math.max(...realNodes.map((node) => node.rect.right)) + ANCHOR\n  };\n  const visibleEdges = edges.filter((edge) => !edge.isLayoutOnly);\n  const edgeIndex = new Map(visibleEdges.map((edge, index) => [edge, index]));\n  const outwardTracksForSide = /* @__PURE__ */ __name((side) => {\n    const outward = side === \"left\" || side === \"top\" ? -1 : 1;\n    const tracks = [];\n    for (let channel = 0; channel <= EXTRA_CHANNEL_COUNT; channel++) {\n      tracks.push(outsideTracks[side] + outward * ANCHOR * channel);\n    }\n    return tracks;\n  }, \"outwardTracksForSide\");\n  const replacementPointsFor = /* @__PURE__ */ __name((edge, replacements = /* @__PURE__ */ new Map()) => dedupeConsecutivePoints(\n    replacements.get(edge) ?? edge.points ?? []\n  ), \"replacementPointsFor\");\n  const crossingCountBetweenSegments = /* @__PURE__ */ __name((firstSegments, secondSegments) => {\n    let count = 0;\n    for (const firstSegment of firstSegments) {\n      for (const secondSegment of secondSegments) {\n        if (orthogonalSegmentsStrictlyCross(\n          firstSegment.a,\n          firstSegment.b,\n          secondSegment.a,\n          secondSegment.b,\n          EPS_LOCAL\n        )) {\n          count++;\n        }\n      }\n    }\n    return count;\n  }, \"crossingCountBetweenSegments\");\n  const crossingCountBetweenPaths = /* @__PURE__ */ __name((first, second) => crossingCountBetweenSegments(segmentsFor(first), segmentsFor(second)), \"crossingCountBetweenPaths\");\n  const crossingSnapshot = /* @__PURE__ */ __name((replacements = /* @__PURE__ */ new Map()) => {\n    let count = 0;\n    const pairs = [];\n    const edgeSet = /* @__PURE__ */ new Set();\n    const edgeOrder = [];\n    const addEdge = /* @__PURE__ */ __name((edge) => {\n      if (!edgeSet.has(edge)) {\n        edgeSet.add(edge);\n        edgeOrder.push(edge);\n      }\n    }, \"addEdge\");\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const first = visibleEdges[i];\n      const firstPoints = replacementPointsFor(first, replacements);\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const second = visibleEdges[j];\n        const pairCount = crossingCountBetweenPaths(\n          firstPoints,\n          replacementPointsFor(second, replacements)\n        );\n        if (pairCount > 0) {\n          count += pairCount;\n          pairs.push({ first, second, count: pairCount });\n          addEdge(first);\n          addEdge(second);\n        }\n      }\n    }\n    edgeOrder.sort((a, b) => (edgeIndex.get(a) ?? 0) - (edgeIndex.get(b) ?? 0));\n    return {\n      count,\n      pairs,\n      edgeSet,\n      edges: edgeOrder\n    };\n  }, \"crossingSnapshot\");\n  const crossingCountWithReplacements = /* @__PURE__ */ __name((current, replacements) => {\n    const changed = new Set(replacements.keys());\n    if (changed.size === 0) {\n      return current.count;\n    }\n    let currentAffected = 0;\n    for (const pair of current.pairs) {\n      if (changed.has(pair.first) || changed.has(pair.second)) {\n        currentAffected += pair.count;\n      }\n    }\n    let replacementAffected = 0;\n    for (let i = 0; i < visibleEdges.length; i++) {\n      const first = visibleEdges[i];\n      const firstChanged = changed.has(first);\n      const firstPoints = replacementPointsFor(first, replacements);\n      for (let j = i + 1; j < visibleEdges.length; j++) {\n        const second = visibleEdges[j];\n        if (!firstChanged && !changed.has(second)) {\n          continue;\n        }\n        replacementAffected += crossingCountBetweenPaths(\n          firstPoints,\n          replacementPointsFor(second, replacements)\n        );\n      }\n    }\n    return current.count - currentAffected + replacementAffected;\n  }, \"crossingCountWithReplacements\");\n  const crossingComponents = /* @__PURE__ */ __name((snapshot) => {\n    const neighbors = /* @__PURE__ */ new Map();\n    for (const pair of snapshot.pairs) {\n      const firstNeighbors = neighbors.get(pair.first) ?? /* @__PURE__ */ new Set();\n      firstNeighbors.add(pair.second);\n      neighbors.set(pair.first, firstNeighbors);\n      const secondNeighbors = neighbors.get(pair.second) ?? /* @__PURE__ */ new Set();\n      secondNeighbors.add(pair.first);\n      neighbors.set(pair.second, secondNeighbors);\n    }\n    const components = [];\n    const seen = /* @__PURE__ */ new Set();\n    for (const edge of snapshot.edges) {\n      if (seen.has(edge)) {\n        continue;\n      }\n      const queue = [edge];\n      const component = [];\n      seen.add(edge);\n      while (queue.length > 0) {\n        const current = queue.pop();\n        component.push(current);\n        for (const next of neighbors.get(current) ?? []) {\n          if (!seen.has(next)) {\n            seen.add(next);\n            queue.push(next);\n          }\n        }\n      }\n      component.sort((a, b) => (edgeIndex.get(a) ?? 0) - (edgeIndex.get(b) ?? 0));\n      if (component.length > 1) {\n        components.push(component);\n      }\n    }\n    return components;\n  }, \"crossingComponents\");\n  const endpointIdsFor = /* @__PURE__ */ __name((edge) => [edge.start, edge.end].filter(\n    (id) => Boolean(id)\n  ), \"endpointIdsFor\");\n  const pairSearchGroups = /* @__PURE__ */ __name((snapshot) => {\n    const groups = [];\n    for (const component of crossingComponents(snapshot)) {\n      const componentSet = new Set(component);\n      const componentEndpointIds = new Set(component.flatMap((edge) => endpointIdsFor(edge)));\n      const group = [...component];\n      for (const edge of visibleEdges) {\n        if (componentSet.has(edge)) {\n          continue;\n        }\n        if (endpointIdsFor(edge).some((id) => componentEndpointIds.has(id))) {\n          group.push(edge);\n        }\n      }\n      group.sort((a, b) => (edgeIndex.get(a) ?? 0) - (edgeIndex.get(b) ?? 0));\n      groups.push(group);\n    }\n    return groups;\n  }, \"pairSearchGroups\");\n  const crossingCountWithSingleReplacement = /* @__PURE__ */ __name((current, edge, replacement) => crossingCountWithReplacements(\n    current,\n    /* @__PURE__ */ new Map([[edge, replacement]])\n  ), \"crossingCountWithSingleReplacement\");\n  const currentCrossingsByEdge = /* @__PURE__ */ __name((current) => {\n    const result = /* @__PURE__ */ new Map();\n    for (const pair of current.pairs) {\n      result.set(pair.first, (result.get(pair.first) ?? 0) + pair.count);\n      result.set(pair.second, (result.get(pair.second) ?? 0) + pair.count);\n    }\n    return result;\n  }, \"currentCrossingsByEdge\");\n  const pathLength = /* @__PURE__ */ __name((points) => points.slice(1).reduce((sum, point, index) => {\n    const previous = points[index];\n    return sum + Math.abs(point.x - previous.x) + Math.abs(point.y - previous.y);\n  }, 0), \"pathLength\");\n  const totalBends = /* @__PURE__ */ __name((replacements = /* @__PURE__ */ new Map()) => visibleEdges.reduce(\n    (sum, edge) => sum + countOrthogonalBends(replacementPointsFor(edge, replacements)),\n    0\n  ), \"totalBends\");\n  const totalLength = /* @__PURE__ */ __name((replacements = /* @__PURE__ */ new Map()) => visibleEdges.reduce(\n    (sum, edge) => sum + pathLength(replacementPointsFor(edge, replacements)),\n    0\n  ), \"totalLength\");\n  const pathHasSegmentConflict = /* @__PURE__ */ __name((edge, path, replacements = /* @__PURE__ */ new Map()) => {\n    const pathSegments = segmentsFor(path);\n    for (const other of visibleEdges) {\n      if (other === edge) {\n        continue;\n      }\n      for (const candidateSegment of pathSegments) {\n        for (const otherSegment of segmentsFor(replacementPointsFor(other, replacements))) {\n          if (sameAxisSegmentOverlapLength(candidateSegment, otherSegment, 0.5) >= MIN_SHARED) {\n            return true;\n          }\n        }\n      }\n    }\n    return false;\n  }, \"pathHasSegmentConflict\");\n  const pathHitsNode = /* @__PURE__ */ __name((edge, path) => {\n    const endpointIds = [edge.start, edge.end].filter(\n      (id) => Boolean(id)\n    );\n    for (const segment of segmentsFor(path)) {\n      if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -2)) {\n        return true;\n      }\n    }\n    return false;\n  }, \"pathHitsNode\");\n  const pushOrthogonalCandidate = /* @__PURE__ */ __name((candidates, points) => {\n    const candidate = simplifyPolyline(dedupeConsecutivePoints(points));\n    if (segmentsFor(candidate).length === candidate.length - 1) {\n      candidates.push(candidate);\n    }\n  }, \"pushOrthogonalCandidate\");\n  const sideIsHorizontal = /* @__PURE__ */ __name((side) => side === \"left\" || side === \"right\", \"sideIsHorizontal\");\n  const localTrackForSameSide = /* @__PURE__ */ __name((src, side, dst) => {\n    switch (side) {\n      case \"left\":\n        return Math.min(src.x, dst.x) - ANCHOR;\n      case \"right\":\n        return Math.max(src.x, dst.x) + ANCHOR;\n      case \"top\":\n        return Math.min(src.y, dst.y) - ANCHOR;\n      case \"bottom\":\n        return Math.max(src.y, dst.y) + ANCHOR;\n    }\n  }, \"localTrackForSameSide\");\n  const addSameSideCandidates = /* @__PURE__ */ __name((candidates, src, srcSide, dst) => {\n    const outward = srcSide === \"left\" || srcSide === \"top\" ? -1 : 1;\n    const trackSeeds = [localTrackForSameSide(src, srcSide, dst), outsideTracks[srcSide]];\n    for (const seed of trackSeeds) {\n      for (let channel = 0; channel <= EXTRA_CHANNEL_COUNT; channel++) {\n        pushOrthogonalCandidate(\n          candidates,\n          buildSameSideTrackPath(src, srcSide, dst, seed + outward * ANCHOR * channel)\n        );\n      }\n    }\n  }, \"addSameSideCandidates\");\n  const addHorizontalToVerticalCandidates = /* @__PURE__ */ __name((candidates, src, srcSide, dst, dstSide) => {\n    for (const xTrack of outwardTracksForSide(srcSide)) {\n      for (const yTrack of outwardTracksForSide(dstSide)) {\n        pushOrthogonalCandidate(candidates, [\n          src,\n          { x: xTrack, y: src.y },\n          { x: xTrack, y: yTrack },\n          { x: dst.x, y: yTrack },\n          dst\n        ]);\n      }\n    }\n  }, \"addHorizontalToVerticalCandidates\");\n  const addVerticalToHorizontalCandidates = /* @__PURE__ */ __name((candidates, src, srcSide, dst, dstSide) => {\n    for (const yTrack of outwardTracksForSide(srcSide)) {\n      for (const xTrack of outwardTracksForSide(dstSide)) {\n        pushOrthogonalCandidate(candidates, [\n          src,\n          { x: src.x, y: yTrack },\n          { x: xTrack, y: yTrack },\n          { x: xTrack, y: dst.y },\n          dst\n        ]);\n      }\n    }\n  }, \"addVerticalToHorizontalCandidates\");\n  const addHorizontalPairCandidates = /* @__PURE__ */ __name((candidates, src, srcSide, dst, dstSide) => {\n    const yTracks = [...outwardTracksForSide(\"top\"), ...outwardTracksForSide(\"bottom\")];\n    for (const srcTrack of outwardTracksForSide(srcSide)) {\n      for (const dstTrack of outwardTracksForSide(dstSide)) {\n        for (const yTrack of yTracks) {\n          pushOrthogonalCandidate(candidates, [\n            src,\n            { x: srcTrack, y: src.y },\n            { x: srcTrack, y: yTrack },\n            { x: dstTrack, y: yTrack },\n            { x: dstTrack, y: dst.y },\n            dst\n          ]);\n        }\n      }\n    }\n  }, \"addHorizontalPairCandidates\");\n  const addVerticalPairCandidates = /* @__PURE__ */ __name((candidates, src, srcSide, dst, dstSide) => {\n    const xTracks = [...outwardTracksForSide(\"left\"), ...outwardTracksForSide(\"right\")];\n    for (const srcTrack of outwardTracksForSide(srcSide)) {\n      for (const dstTrack of outwardTracksForSide(dstSide)) {\n        for (const xTrack of xTracks) {\n          pushOrthogonalCandidate(candidates, [\n            src,\n            { x: src.x, y: srcTrack },\n            { x: xTrack, y: srcTrack },\n            { x: xTrack, y: dstTrack },\n            { x: dst.x, y: dstTrack },\n            dst\n          ]);\n        }\n      }\n    }\n  }, \"addVerticalPairCandidates\");\n  const dedupeCandidatePaths = /* @__PURE__ */ __name((candidates) => {\n    const seen = /* @__PURE__ */ new Set();\n    return candidates.map((candidate) => dedupeConsecutivePoints(candidate)).filter((candidate) => {\n      const key = candidate.map((point) => `${point.x.toFixed(3)},${point.y.toFixed(3)}`).join(\"|\");\n      if (seen.has(key) || candidate.length < 2) {\n        return false;\n      }\n      seen.add(key);\n      return true;\n    });\n  }, \"dedupeCandidatePaths\");\n  const buildCandidatesForSides = /* @__PURE__ */ __name((src, srcSide, dst, dstSide) => {\n    const candidates = [];\n    const base = buildOrthogonalPortPath(src, srcSide, dst, dstSide, ANCHOR, EPS_LOCAL);\n    if (base) {\n      pushOrthogonalCandidate(candidates, base);\n    }\n    if (srcSide === dstSide) {\n      addSameSideCandidates(candidates, src, srcSide, dst);\n    }\n    const srcHorizontal = sideIsHorizontal(srcSide);\n    const dstHorizontal = sideIsHorizontal(dstSide);\n    if (srcHorizontal && !dstHorizontal) {\n      addHorizontalToVerticalCandidates(candidates, src, srcSide, dst, dstSide);\n    } else if (!srcHorizontal && dstHorizontal) {\n      addVerticalToHorizontalCandidates(candidates, src, srcSide, dst, dstSide);\n    } else if (srcHorizontal) {\n      addHorizontalPairCandidates(candidates, src, srcSide, dst, dstSide);\n    } else {\n      addVerticalPairCandidates(candidates, src, srcSide, dst, dstSide);\n    }\n    return dedupeCandidatePaths(candidates);\n  }, \"buildCandidatesForSides\");\n  const addVerticalDepartureOuterTrackCandidates = /* @__PURE__ */ __name((candidates, first, departure, dstNode) => {\n    const externalXTracks = [...outwardTracksForSide(\"left\"), ...outwardTracksForSide(\"right\")];\n    const externalYTracks = [...outwardTracksForSide(\"top\"), ...outwardTracksForSide(\"bottom\")];\n    for (const side of sides) {\n      const dst = portForRectSide(dstNode, side);\n      const targetYTracks = side === \"top\" || side === \"bottom\" ? outwardTracksForSide(side) : externalYTracks;\n      for (const track of externalXTracks) {\n        pushOrthogonalCandidate(candidates, [\n          first,\n          departure,\n          { x: track, y: departure.y },\n          { x: track, y: dst.y },\n          dst\n        ]);\n        for (const targetTrack of targetYTracks) {\n          pushOrthogonalCandidate(candidates, [\n            first,\n            departure,\n            { x: track, y: departure.y },\n            { x: track, y: targetTrack },\n            { x: dst.x, y: targetTrack },\n            dst\n          ]);\n        }\n      }\n    }\n  }, \"addVerticalDepartureOuterTrackCandidates\");\n  const addHorizontalDepartureOuterTrackCandidates = /* @__PURE__ */ __name((candidates, first, departure, dstNode) => {\n    const externalXTracks = [...outwardTracksForSide(\"left\"), ...outwardTracksForSide(\"right\")];\n    const externalYTracks = [...outwardTracksForSide(\"top\"), ...outwardTracksForSide(\"bottom\")];\n    for (const side of sides) {\n      const dst = portForRectSide(dstNode, side);\n      const targetXTracks = side === \"left\" || side === \"right\" ? outwardTracksForSide(side) : externalXTracks;\n      for (const track of externalYTracks) {\n        pushOrthogonalCandidate(candidates, [\n          first,\n          departure,\n          { x: departure.x, y: track },\n          { x: dst.x, y: track },\n          dst\n        ]);\n        for (const targetTrack of targetXTracks) {\n          pushOrthogonalCandidate(candidates, [\n            first,\n            departure,\n            { x: departure.x, y: track },\n            { x: targetTrack, y: track },\n            { x: targetTrack, y: dst.y },\n            dst\n          ]);\n        }\n      }\n    }\n  }, \"addHorizontalDepartureOuterTrackCandidates\");\n  const terminalPreservingOuterTrackCandidates = /* @__PURE__ */ __name((edge) => {\n    const srcId = edge.start;\n    const dstId = edge.end;\n    const dstNode = dstId ? nodeInfoById.get(dstId) : void 0;\n    if (!srcId || !dstNode) {\n      return [];\n    }\n    const points = dedupeConsecutivePoints(edge.points ?? []);\n    if (points.length < 4) {\n      return [];\n    }\n    const first = points[0];\n    const departure = points[1];\n    const candidates = [];\n    if (isVerticalSegment(first, departure, EPS_LOCAL)) {\n      addVerticalDepartureOuterTrackCandidates(candidates, first, departure, dstNode);\n    } else if (isHorizontalSegment(first, departure, EPS_LOCAL)) {\n      addHorizontalDepartureOuterTrackCandidates(candidates, first, departure, dstNode);\n    }\n    return candidates;\n  }, \"terminalPreservingOuterTrackCandidates\");\n  const candidatePathsFor = /* @__PURE__ */ __name((edge) => {\n    const srcId = edge.start;\n    const dstId = edge.end;\n    const srcNode = srcId ? nodeInfoById.get(srcId) : void 0;\n    const dstNode = dstId ? nodeInfoById.get(dstId) : void 0;\n    if (!srcNode || !dstNode) {\n      return [];\n    }\n    const candidates = [];\n    for (const srcSide of sides) {\n      const srcPort = portForRectSide(srcNode, srcSide);\n      for (const dstSide of sides) {\n        candidates.push(\n          ...buildCandidatesForSides(srcPort, srcSide, portForRectSide(dstNode, dstSide), dstSide)\n        );\n      }\n    }\n    candidates.push(...terminalPreservingOuterTrackCandidates(edge));\n    return candidates;\n  }, \"candidatePathsFor\");\n  const currentSegmentsByEdge = /* @__PURE__ */ __name(() => new Map(visibleEdges.map((edge) => [edge, segmentsFor(replacementPointsFor(edge))])), \"currentSegmentsByEdge\");\n  const sharedTrackConflictsFor = /* @__PURE__ */ __name((edge, candidateSegments, baseSegments) => {\n    const conflicts = /* @__PURE__ */ new Set();\n    for (const other of visibleEdges) {\n      if (other === edge) {\n        continue;\n      }\n      const otherSegments = baseSegments.get(other) ?? segmentsFor(replacementPointsFor(other));\n      if (candidateSegments.some(\n        (candidateSegment) => otherSegments.some(\n          (otherSegment) => sameAxisSegmentOverlapLength(candidateSegment, otherSegment, 0.5) >= MIN_SHARED\n        )\n      )) {\n        conflicts.add(other);\n      }\n    }\n    return conflicts;\n  }, \"sharedTrackConflictsFor\");\n  const pairCandidatesFor = /* @__PURE__ */ __name((edge, current, baseSegments, crossingCountByEdge) => {\n    const seen = /* @__PURE__ */ new Set();\n    const candidates = candidatePathsFor(edge).map((candidate) => simplifyPolyline(dedupeConsecutivePoints(candidate))).filter((candidate) => {\n      if (pathHitsNode(edge, candidate)) {\n        return false;\n      }\n      const key = candidate.map((point) => `${point.x.toFixed(3)},${point.y.toFixed(3)}`).join(\"|\");\n      if (seen.has(key) || candidate.length < 2) {\n        return false;\n      }\n      seen.add(key);\n      return true;\n    }).map((candidate) => {\n      const candidateSegments = segmentsFor(candidate);\n      let replacementAffected = 0;\n      for (const other of visibleEdges) {\n        if (other === edge) {\n          continue;\n        }\n        replacementAffected += crossingCountBetweenSegments(\n          candidateSegments,\n          baseSegments.get(other) ?? segmentsFor(replacementPointsFor(other))\n        );\n      }\n      return {\n        candidate,\n        candidateSegments,\n        crossings: current.count - (crossingCountByEdge.get(edge) ?? 0) + replacementAffected,\n        bends: countOrthogonalBends(candidate, EPS_LOCAL),\n        totalBends: countOrthogonalBends(candidate),\n        length: pathLength(candidate)\n      };\n    }).filter(({ crossings }) => crossings <= current.count).sort((a, b) => a.crossings - b.crossings || a.bends - b.bends || a.length - b.length);\n    return candidates.slice(0, MAX_PAIR_CANDIDATES_PER_EDGE).map((candidate) => {\n      return {\n        path: candidate.candidate,\n        segments: candidate.candidateSegments,\n        sharedTrackConflicts: sharedTrackConflictsFor(\n          edge,\n          candidate.candidateSegments,\n          baseSegments\n        ),\n        totalBends: candidate.totalBends,\n        length: candidate.length\n      };\n    });\n  }, \"pairCandidatesFor\");\n  const pairCrossingCount = /* @__PURE__ */ __name((current, firstEdge, firstCandidate, secondEdge, secondCandidate, baseSegments) => {\n    let currentAffected = 0;\n    for (const pair of current.pairs) {\n      if (pair.first === firstEdge || pair.second === firstEdge || pair.first === secondEdge || pair.second === secondEdge) {\n        currentAffected += pair.count;\n      }\n    }\n    let replacementAffected = crossingCountBetweenSegments(\n      firstCandidate.segments,\n      secondCandidate.segments\n    );\n    for (const other of visibleEdges) {\n      if (other === firstEdge || other === secondEdge) {\n        continue;\n      }\n      const otherSegments = baseSegments.get(other) ?? segmentsFor(replacementPointsFor(other));\n      replacementAffected += crossingCountBetweenSegments(firstCandidate.segments, otherSegments) + crossingCountBetweenSegments(secondCandidate.segments, otherSegments);\n    }\n    return current.count - currentAffected + replacementAffected;\n  }, \"pairCrossingCount\");\n  const conflictsOnlyWith = /* @__PURE__ */ __name((candidate, edge) => {\n    for (const conflict of candidate.sharedTrackConflicts) {\n      if (conflict !== edge) {\n        return false;\n      }\n    }\n    return true;\n  }, \"conflictsOnlyWith\");\n  const candidatesShareTrack = /* @__PURE__ */ __name((firstCandidate, secondCandidate) => firstCandidate.segments.some(\n    (firstSegment) => secondCandidate.segments.some(\n      (secondSegment) => sameAxisSegmentOverlapLength(firstSegment, secondSegment, 0.5) >= MIN_SHARED\n    )\n  ), \"candidatesShareTrack\");\n  const pairCandidatesAreCompatible = /* @__PURE__ */ __name((first, firstCandidate, second, secondCandidate) => conflictsOnlyWith(firstCandidate, second.edge) && conflictsOnlyWith(secondCandidate, first.edge) && !candidatesShareTrack(firstCandidate, secondCandidate), \"pairCandidatesAreCompatible\");\n  const scorePairReplacement = /* @__PURE__ */ __name((context, first, firstCandidate, second, secondCandidate) => {\n    const crossings = pairCrossingCount(\n      context.current,\n      first.edge,\n      firstCandidate,\n      second.edge,\n      secondCandidate,\n      context.baseSegments\n    );\n    if (crossings >= context.current.count) {\n      return void 0;\n    }\n    return {\n      replacements: /* @__PURE__ */ new Map([\n        [first.edge, firstCandidate.path],\n        [second.edge, secondCandidate.path]\n      ]),\n      crossings,\n      bends: context.currentBends - (context.baseBendsByEdge.get(first.edge) ?? 0) - (context.baseBendsByEdge.get(second.edge) ?? 0) + firstCandidate.totalBends + secondCandidate.totalBends,\n      length: context.currentLength - (context.baseLengthByEdge.get(first.edge) ?? 0) - (context.baseLengthByEdge.get(second.edge) ?? 0) + firstCandidate.length + secondCandidate.length\n    };\n  }, \"scorePairReplacement\");\n  const pairScoreIsBetter = /* @__PURE__ */ __name((candidate, best) => candidate.crossings < best.crossings || candidate.crossings === best.crossings && (candidate.bends < best.bends || candidate.bends === best.bends && candidate.length < best.length), \"pairScoreIsBetter\");\n  const bestScoreForOptionPair = /* @__PURE__ */ __name((context, first, second, best) => {\n    let pairBest = best;\n    for (const firstCandidate of first.candidates) {\n      for (const secondCandidate of second.candidates) {\n        if (!pairCandidatesAreCompatible(first, firstCandidate, second, secondCandidate)) {\n          continue;\n        }\n        const score = scorePairReplacement(context, first, firstCandidate, second, secondCandidate);\n        if (score && pairScoreIsBetter(score, pairBest)) {\n          pairBest = score;\n        }\n      }\n    }\n    return pairBest;\n  }, \"bestScoreForOptionPair\");\n  const bestPairedReplacement = /* @__PURE__ */ __name((current) => {\n    const currentBends = totalBends();\n    const currentLength = totalLength();\n    const baseSegments = currentSegmentsByEdge();\n    const crossingCountByEdge = currentCrossingsByEdge(current);\n    const baseBendsByEdge = new Map(\n      visibleEdges.map((edge) => [edge, countOrthogonalBends(replacementPointsFor(edge))])\n    );\n    const baseLengthByEdge = new Map(\n      visibleEdges.map((edge) => [edge, pathLength(replacementPointsFor(edge))])\n    );\n    const optionsByEdge = /* @__PURE__ */ new Map();\n    const groups = pairSearchGroups(current);\n    for (const group of groups) {\n      for (const edge of group) {\n        if (optionsByEdge.has(edge)) {\n          continue;\n        }\n        const candidates = pairCandidatesFor(edge, current, baseSegments, crossingCountByEdge);\n        if (candidates.length > 0) {\n          optionsByEdge.set(edge, { edge, candidates });\n        }\n      }\n    }\n    let best = {\n      replacements: /* @__PURE__ */ new Map(),\n      crossings: current.count,\n      bends: currentBends,\n      length: currentLength\n    };\n    const scoringContext = {\n      current,\n      currentBends,\n      currentLength,\n      baseBendsByEdge,\n      baseLengthByEdge,\n      baseSegments\n    };\n    for (const group of groups) {\n      const crossingEdgeSet = new Set(group.filter((edge) => current.edgeSet.has(edge)));\n      const options = group.map((edge) => optionsByEdge.get(edge)).filter((option) => Boolean(option));\n      for (let i = 0; i < options.length; i++) {\n        const first = options[i];\n        for (let j = i + 1; j < options.length; j++) {\n          const second = options[j];\n          if (!crossingEdgeSet.has(first.edge) && !crossingEdgeSet.has(second.edge)) {\n            continue;\n          }\n          best = bestScoreForOptionPair(scoringContext, first, second, best);\n        }\n      }\n    }\n    return best.replacements.size > 0 ? best.replacements : void 0;\n  }, \"bestPairedReplacement\");\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    const current = crossingSnapshot();\n    const currentCrossings = current.count;\n    if (currentCrossings === 0) {\n      return;\n    }\n    let bestEdge;\n    let bestPath;\n    let bestCrossings = currentCrossings;\n    let bestBends = Number.POSITIVE_INFINITY;\n    for (const edge of current.edges) {\n      const currentEdgeBends = countOrthogonalBends(replacementPointsFor(edge), EPS_LOCAL);\n      for (const candidate of candidatePathsFor(edge)) {\n        const candidateHitsNode = pathHitsNode(edge, candidate);\n        const candidateHasSegmentConflict = !candidateHitsNode && pathHasSegmentConflict(edge, candidate);\n        const candidateCrossings = crossingCountWithSingleReplacement(current, edge, candidate);\n        const candidateBends = countOrthogonalBends(candidate, EPS_LOCAL);\n        if (candidateHitsNode || candidateHasSegmentConflict) {\n          continue;\n        }\n        const improvesCurrentEdge = candidateCrossings < currentCrossings || candidateCrossings === currentCrossings && candidateBends < currentEdgeBends;\n        if (!improvesCurrentEdge) {\n          continue;\n        }\n        if (candidateCrossings > bestCrossings || candidateCrossings === bestCrossings && candidateBends >= bestBends) {\n          continue;\n        }\n        bestEdge = edge;\n        bestPath = candidate;\n        bestCrossings = candidateCrossings;\n        bestBends = candidateBends;\n      }\n    }\n    if (bestEdge && bestPath) {\n      bestEdge.points = bestPath;\n      continue;\n    }\n    const pairedReplacement = bestPairedReplacement(current);\n    if (!pairedReplacement) {\n      return;\n    }\n    for (const [edge, points] of pairedReplacement) {\n      edge.points = points;\n    }\n  }\n}\n__name(resolveRenderedOrthogonalCrossings, \"resolveRenderedOrthogonalCrossings\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/detourSimplification.ts\nvar EPS4 = 1e-3;\nvar MIN_SHARED2 = 8;\nfunction simplifyDetouredEdges(edges, nodes) {\n  const { nodeInfoById, realNodeRects } = collectRealNodeBounds(nodes);\n  const sides = [\"top\", \"bottom\", \"left\", \"right\"];\n  const ANCHOR = 20;\n  const outsideTracks = {\n    top: Math.min(...realNodeRects.map((node) => node.rect.top)) - ANCHOR,\n    bottom: Math.max(...realNodeRects.map((node) => node.rect.bottom)) + ANCHOR,\n    left: Math.min(...realNodeRects.map((node) => node.rect.left)) - ANCHOR,\n    right: Math.max(...realNodeRects.map((node) => node.rect.right)) + ANCHOR\n  };\n  const buildOrthogonalPathCandidates = /* @__PURE__ */ __name((src, srcSide, dst, dstSide) => {\n    const paths = [];\n    const base = buildOrthogonalPortPath(src, srcSide, dst, dstSide, ANCHOR, EPS4);\n    if (base) {\n      paths.push(base);\n    }\n    if (srcSide === dstSide) {\n      paths.push(buildSameSideTrackPath(src, srcSide, dst, outsideTracks[srcSide]));\n    }\n    return paths;\n  }, \"buildOrthogonalPathCandidates\");\n  const pathHitsNode = /* @__PURE__ */ __name((pts, excludeIds) => {\n    for (let i = 0; i < pts.length - 1; i++) {\n      const a = pts[i];\n      const b = pts[i + 1];\n      if (segmentHitsAnyRect(a, b, realNodeRects, excludeIds, 1)) {\n        return true;\n      }\n    }\n    return false;\n  }, \"pathHitsNode\");\n  const pathConflictCount = /* @__PURE__ */ __name((path, currentEdge, includeIncidentEdges = false) => {\n    let conflicts = 0;\n    const pathSegments = orthogonalSegmentsForPoints(path, EPS4);\n    const currentStart = currentEdge.start;\n    const currentEnd = currentEdge.end;\n    for (const other of edges) {\n      if (other === currentEdge || other.isLayoutOnly) {\n        continue;\n      }\n      const otherStart = other.start;\n      const otherEnd = other.end;\n      if (!includeIncidentEdges && currentStart && currentEnd && (otherStart === currentStart || otherStart === currentEnd || otherEnd === currentStart || otherEnd === currentEnd)) {\n        continue;\n      }\n      const otherPts = other.points;\n      if (!otherPts || otherPts.length < 2) {\n        continue;\n      }\n      for (const pathSegment of pathSegments) {\n        for (const otherSegment of orthogonalSegmentsForPoints(otherPts, EPS4)) {\n          if (orthogonalSegmentsCross(\n            pathSegment.a,\n            pathSegment.b,\n            otherSegment.a,\n            otherSegment.b,\n            EPS4,\n            EPS4\n          )) {\n            conflicts++;\n            continue;\n          }\n          if (sameAxisSegmentOverlapLength(pathSegment, otherSegment, EPS4) >= MIN_SHARED2) {\n            conflicts++;\n          }\n        }\n      }\n    }\n    return conflicts;\n  }, \"pathConflictCount\");\n  const BEND_THRESHOLD = 4;\n  const nearestSideOfRect = /* @__PURE__ */ __name((pt, info) => {\n    const dTop = Math.abs(pt.y - info.rect.top);\n    const dBottom = Math.abs(pt.y - info.rect.bottom);\n    const dLeft = Math.abs(pt.x - info.rect.left);\n    const dRight = Math.abs(pt.x - info.rect.right);\n    let best = \"top\";\n    let bestDist = dTop;\n    if (dBottom < bestDist) {\n      best = \"bottom\";\n      bestDist = dBottom;\n    }\n    if (dLeft < bestDist) {\n      best = \"left\";\n      bestDist = dLeft;\n    }\n    if (dRight < bestDist) {\n      best = \"right\";\n      bestDist = dRight;\n    }\n    return best;\n  }, \"nearestSideOfRect\");\n  const faceClaims = /* @__PURE__ */ new Map();\n  const addFaceClaim = /* @__PURE__ */ __name((nodeId, side, edgeId) => {\n    const claims = faceClaims.get(nodeId) ?? [];\n    claims.push({ side, edgeId });\n    faceClaims.set(nodeId, claims);\n  }, \"addFaceClaim\");\n  for (const e of edges) {\n    if (e.isLayoutOnly) {\n      continue;\n    }\n    const pts = e.points ?? [];\n    if (pts.length < 1) {\n      continue;\n    }\n    const eId = e.id ?? \"\";\n    const startId = e.start;\n    const endId = e.end;\n    if (startId) {\n      const info = nodeInfoById.get(startId);\n      if (info) {\n        addFaceClaim(startId, nearestSideOfRect(pts[0], info), eId);\n      }\n    }\n    if (endId) {\n      const info = nodeInfoById.get(endId);\n      if (info) {\n        addFaceClaim(endId, nearestSideOfRect(pts[pts.length - 1], info), eId);\n      }\n    }\n  }\n  const faceIsClaimed = /* @__PURE__ */ __name((nodeId, side, ignoreEdgeId) => {\n    return faceClaims.get(nodeId)?.some((c) => c.edgeId !== ignoreEdgeId && c.side === side) ?? false;\n  }, \"faceIsClaimed\");\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const pts = edge.points;\n    if (!pts || pts.length < 2) {\n      continue;\n    }\n    const currentBends = countOrthogonalBends(pts, EPS4);\n    if (currentBends < BEND_THRESHOLD) {\n      continue;\n    }\n    const srcId = edge.start;\n    const dstId = edge.end;\n    if (!srcId || !dstId) {\n      continue;\n    }\n    const srcInfo = nodeInfoById.get(srcId);\n    const dstInfo = nodeInfoById.get(dstId);\n    if (!srcInfo || !dstInfo) {\n      continue;\n    }\n    const edgeId = edge.id ?? \"\";\n    const currentCrossingConflicts = pathConflictCount(pts, edge, true);\n    const currentNonIncidentConflicts = pathConflictCount(pts, edge);\n    let bestPath;\n    let bestCrossingConflicts = currentCrossingConflicts;\n    let bestBends = currentBends;\n    for (const srcSide of sides) {\n      if (faceIsClaimed(srcId, srcSide, edgeId)) {\n        continue;\n      }\n      const srcPort = portForRectSide(srcInfo, srcSide);\n      for (const dstSide of sides) {\n        if (faceIsClaimed(dstId, dstSide, edgeId)) {\n          continue;\n        }\n        const dstPort = portForRectSide(dstInfo, dstSide);\n        for (const path of buildOrthogonalPathCandidates(srcPort, srcSide, dstPort, dstSide)) {\n          if (pathHitsNode(path, [srcId, dstId])) {\n            continue;\n          }\n          const pathBends = countOrthogonalBends(path, EPS4);\n          if (currentCrossingConflicts > 0) {\n            const pathCrossingConflicts = pathConflictCount(path, edge, true);\n            if (pathCrossingConflicts > bestCrossingConflicts || pathCrossingConflicts === bestCrossingConflicts && pathBends >= bestBends) {\n              continue;\n            }\n            bestCrossingConflicts = pathCrossingConflicts;\n            bestBends = pathBends;\n            bestPath = path;\n            continue;\n          }\n          if (pathConflictCount(path, edge) > currentNonIncidentConflicts) {\n            continue;\n          }\n          if (pathBends < bestBends) {\n            bestBends = pathBends;\n            bestPath = path;\n          }\n        }\n      }\n    }\n    if (bestPath) {\n      edge.points = bestPath;\n      const refreshSrc = faceClaims.get(srcId);\n      if (refreshSrc) {\n        faceClaims.set(\n          srcId,\n          refreshSrc.filter((c) => c.edgeId !== edgeId)\n        );\n      }\n      const refreshDst = faceClaims.get(dstId);\n      if (refreshDst) {\n        faceClaims.set(\n          dstId,\n          refreshDst.filter((c) => c.edgeId !== edgeId)\n        );\n      }\n      addFaceClaim(srcId, nearestSideOfRect(bestPath[0], srcInfo), edgeId);\n      addFaceClaim(dstId, nearestSideOfRect(bestPath[bestPath.length - 1], dstInfo), edgeId);\n    }\n  }\n}\n__name(simplifyDetouredEdges, \"simplifyDetouredEdges\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/labelAnchoring.ts\nvar EPS5 = 1e-3;\nvar MARKER_CLEARANCE_LENGTH = 10;\nvar MARKER_CLEARANCE_HALF_WIDTH = 7;\nfunction markerClearanceRectFor(pts, atStart) {\n  const terminalIndex = atStart ? 0 : pts.length - 1;\n  const step = atStart ? 1 : -1;\n  const tip = pts[terminalIndex];\n  const inner = pts[terminalIndex + step];\n  if (!tip || !inner) {\n    return void 0;\n  }\n  const dx = inner.x - tip.x;\n  const dy = inner.y - tip.y;\n  const len = Math.abs(dx) + Math.abs(dy);\n  if (len < EPS5) {\n    return void 0;\n  }\n  if (Math.abs(dy) <= EPS5) {\n    const x2 = tip.x + Math.sign(dx) * MARKER_CLEARANCE_LENGTH;\n    return {\n      left: Math.min(tip.x, x2),\n      right: Math.max(tip.x, x2),\n      top: tip.y - MARKER_CLEARANCE_HALF_WIDTH,\n      bottom: tip.y + MARKER_CLEARANCE_HALF_WIDTH\n    };\n  }\n  if (Math.abs(dx) <= EPS5) {\n    const y2 = tip.y + Math.sign(dy) * MARKER_CLEARANCE_LENGTH;\n    return {\n      left: tip.x - MARKER_CLEARANCE_HALF_WIDTH,\n      right: tip.x + MARKER_CLEARANCE_HALF_WIDTH,\n      top: Math.min(tip.y, y2),\n      bottom: Math.max(tip.y, y2)\n    };\n  }\n  return {\n    left: Math.min(tip.x, inner.x),\n    right: Math.max(tip.x, inner.x),\n    top: Math.min(tip.y, inner.y),\n    bottom: Math.max(tip.y, inner.y)\n  };\n}\n__name(markerClearanceRectFor, \"markerClearanceRectFor\");\nfunction normalizeRect(rect) {\n  return {\n    left: Math.min(rect.left, rect.right),\n    right: Math.max(rect.left, rect.right),\n    top: Math.min(rect.top, rect.bottom),\n    bottom: Math.max(rect.top, rect.bottom)\n  };\n}\n__name(normalizeRect, \"normalizeRect\");\nfunction labelOverlapsOwnMarker(rect, pts) {\n  const visiblePts = dedupeConsecutivePoints(pts);\n  const startMarker = markerClearanceRectFor(visiblePts, true);\n  const endMarker = markerClearanceRectFor(visiblePts, false);\n  return [startMarker, endMarker].some(\n    (marker) => marker && rectsOverlap(rect, normalizeRect(marker))\n  );\n}\n__name(labelOverlapsOwnMarker, \"labelOverlapsOwnMarker\");\nfunction anchorLabelsToPolyline(edges, nodeByIdMap) {\n  const allEdgeSegments = [];\n  for (const other of edges) {\n    if (other.isLayoutOnly) {\n      continue;\n    }\n    const pts = other.points;\n    if (!pts || pts.length < 2) {\n      continue;\n    }\n    for (let i = 0; i < pts.length - 1; i++) {\n      allEdgeSegments.push({ edgeId: other.id, p1: pts[i], p2: pts[i + 1] });\n    }\n  }\n  const foreignNodeRects = [];\n  const laneGroups = [];\n  for (const n of nodeByIdMap.values()) {\n    const isGroup = n.isGroup;\n    const parentId = n.parentId;\n    if (isGroup && !parentId) {\n      const rect2 = rectOfNodeBounds(n);\n      if (rect2) {\n        laneGroups.push({\n          id: n.id,\n          rect: rect2\n        });\n      }\n      continue;\n    }\n    if (isGroup) {\n      continue;\n    }\n    if (n.isEdgeLabel) {\n      continue;\n    }\n    const rect = rectOfNodeBounds(n);\n    if (!rect) {\n      continue;\n    }\n    foreignNodeRects.push({\n      nodeId: n.id,\n      rect\n    });\n  }\n  const LABEL_PLACEMENT_BUFFER = 3;\n  const LABEL_LANE_MARGIN = 1;\n  const LABEL_ENDPOINT_CLEARANCE = 12;\n  const labelOverlapsForeignNode = /* @__PURE__ */ __name((labelId, rect) => {\n    const buffered = inflateRect(rect, LABEL_PLACEMENT_BUFFER);\n    for (const { nodeId, rect: nr } of foreignNodeRects) {\n      if (nodeId === labelId) {\n        continue;\n      }\n      if (rectsOverlap(buffered, nr)) {\n        return true;\n      }\n    }\n    return false;\n  }, \"labelOverlapsForeignNode\");\n  const labelOverlapsForeignEdge = /* @__PURE__ */ __name((edgeId, rect) => {\n    const buffered = inflateRect(rect, LABEL_PLACEMENT_BUFFER);\n    for (const s of allEdgeSegments) {\n      if (s.edgeId === edgeId) {\n        continue;\n      }\n      if (segmentBoundsOverlapRect(s.p1, s.p2, buffered)) {\n        return true;\n      }\n    }\n    return false;\n  }, \"labelOverlapsForeignEdge\");\n  const labelOverlapsAnything = /* @__PURE__ */ __name((labelId, edgeId, rect) => labelOverlapsForeignNode(labelId, rect) || labelOverlapsForeignEdge(edgeId, rect), \"labelOverlapsAnything\");\n  const placedLabelRects = [];\n  const findContainingLane = /* @__PURE__ */ __name((rect) => {\n    for (const { id, rect: laneRect } of laneGroups) {\n      if (rectContainsRect(laneRect, rect)) {\n        return id;\n      }\n    }\n    return void 0;\n  }, \"findContainingLane\");\n  const overlapsPlacedLabel = /* @__PURE__ */ __name((labelId, rect) => placedLabelRects.some(\n    (placed) => placed.labelId !== labelId && rectsOverlap(rect, placed.rect)\n  ), \"overlapsPlacedLabel\");\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const labelId = edge.labelNodeId;\n    if (!labelId) {\n      continue;\n    }\n    const labelNode = nodeByIdMap.get(labelId);\n    if (!labelNode) {\n      continue;\n    }\n    const pts = edge.points;\n    if (!pts || pts.length < 2) {\n      continue;\n    }\n    const lw = labelNode.width ?? 0;\n    const lh = labelNode.height ?? 0;\n    if (lw <= 0 || lh <= 0) {\n      continue;\n    }\n    const segments = [];\n    for (let i = 0; i < pts.length - 1; i++) {\n      const a = pts[i];\n      const b = pts[i + 1];\n      const dx = Math.abs(a.x - b.x);\n      const dy = Math.abs(a.y - b.y);\n      if (dx < EPS5 && dy < EPS5) {\n        continue;\n      }\n      if (dx >= EPS5 && dy >= EPS5) {\n        continue;\n      }\n      segments.push({\n        idx: i,\n        length: dx + dy,\n        orientation: dx >= EPS5 ? \"horizontal\" : \"vertical\",\n        midX: (a.x + b.x) / 2,\n        midY: (a.y + b.y) / 2\n      });\n    }\n    if (segments.length === 0) {\n      continue;\n    }\n    const middleSegments = segments.length >= 3 ? segments.filter((s) => s.idx > 0 && s.idx < segments.length - 1) : segments;\n    const poolBase = middleSegments.length > 0 ? middleSegments : segments;\n    const labelLongAxis = lw >= lh ? \"horizontal\" : \"vertical\";\n    const rankSegments = /* @__PURE__ */ __name((pool) => {\n      return [...pool].sort((a, b) => {\n        const aLongAxis = a.orientation === labelLongAxis;\n        const bLongAxis = b.orientation === labelLongAxis;\n        if (aLongAxis !== bLongAxis) {\n          return aLongAxis ? -1 : 1;\n        }\n        const aFits = a.length >= (a.orientation === \"horizontal\" ? lw : lh) + 2;\n        const bFits = b.length >= (b.orientation === \"horizontal\" ? lw : lh) + 2;\n        if (aFits !== bFits) {\n          return aFits ? -1 : 1;\n        }\n        return b.length - a.length;\n      });\n    }, \"rankSegments\");\n    const firstVisibleSegment = segments[0];\n    const lastVisibleSegment = segments[segments.length - 1];\n    const ALONG_SEGMENT_TS = [0.5, 0.25, 0.75, 0.05, 0.95, 0.15, 0.85, 0.1, 0.9];\n    const anchorAtT = /* @__PURE__ */ __name((seg, t) => {\n      const a = pts[seg.idx];\n      const b = pts[seg.idx + 1];\n      return {\n        midX: a.x + (b.x - a.x) * t,\n        midY: a.y + (b.y - a.y) * t\n      };\n    }, \"anchorAtT\");\n    const clamp = /* @__PURE__ */ __name((value, min, max) => Math.min(max, Math.max(min, value)), \"clamp\");\n    const pointInsideRectInclusive = /* @__PURE__ */ __name((point, rect) => point.midX >= rect.left - EPS5 && point.midX <= rect.right + EPS5 && point.midY >= rect.top - EPS5 && point.midY <= rect.bottom + EPS5, \"pointInsideRectInclusive\");\n    const placementForAnchor = /* @__PURE__ */ __name((anchor) => {\n      const centeredRect = rectFromCenterSize(anchor.midX, anchor.midY, lw, lh);\n      const centeredLane = findContainingLane(centeredRect);\n      if (centeredLane) {\n        return { laneId: centeredLane, anchor, rect: centeredRect };\n      }\n      const containingLane = laneGroups.find(({ rect }) => pointInsideRectInclusive(anchor, rect));\n      if (!containingLane) {\n        return void 0;\n      }\n      const minX = containingLane.rect.left + lw / 2 + LABEL_LANE_MARGIN;\n      const maxX = containingLane.rect.right - lw / 2 - LABEL_LANE_MARGIN;\n      const minY = containingLane.rect.top + lh / 2 + LABEL_LANE_MARGIN;\n      const maxY = containingLane.rect.bottom - lh / 2 - LABEL_LANE_MARGIN;\n      if (minX > maxX || minY > maxY) {\n        return void 0;\n      }\n      const clampedAnchor = {\n        midX: clamp(anchor.midX, minX, maxX),\n        midY: clamp(anchor.midY, minY, maxY)\n      };\n      const clampedRect = rectFromCenterSize(clampedAnchor.midX, clampedAnchor.midY, lw, lh);\n      return pointInsideRectInclusive(anchor, clampedRect) ? { laneId: containingLane.id, anchor: clampedAnchor, rect: clampedRect } : void 0;\n    }, \"placementForAnchor\");\n    const distanceAlongSegment = /* @__PURE__ */ __name((seg, anchor, endpoint) => seg.orientation === \"horizontal\" ? Math.abs(anchor.midX - endpoint.x) : Math.abs(anchor.midY - endpoint.y), \"distanceAlongSegment\");\n    const labelClearsTerminalEndpoints = /* @__PURE__ */ __name((seg, anchor) => {\n      const labelHalfExtent = seg.orientation === \"horizontal\" ? lw / 2 : lh / 2;\n      const requiredDistance = labelHalfExtent + LABEL_ENDPOINT_CLEARANCE;\n      if (seg === firstVisibleSegment) {\n        const start = pts[seg.idx];\n        if (distanceAlongSegment(seg, anchor, start) + EPS5 < requiredDistance) {\n          return false;\n        }\n      }\n      if (seg === lastVisibleSegment) {\n        const end = pts[seg.idx + 1];\n        if (distanceAlongSegment(seg, anchor, end) + EPS5 < requiredDistance) {\n          return false;\n        }\n      }\n      return true;\n    }, \"labelClearsTerminalEndpoints\");\n    const tryPool = /* @__PURE__ */ __name((pool) => {\n      const rankedPool = rankSegments(pool);\n      for (const seg of rankedPool) {\n        for (const t of ALONG_SEGMENT_TS) {\n          const anchor = anchorAtT(seg, t);\n          if (!labelClearsTerminalEndpoints(seg, anchor)) {\n            continue;\n          }\n          const placement = placementForAnchor(anchor);\n          if (!placement) {\n            continue;\n          }\n          if (labelOverlapsOwnMarker(placement.rect, pts)) {\n            continue;\n          }\n          if (overlapsPlacedLabel(labelId, placement.rect)) {\n            continue;\n          }\n          if (!labelOverlapsAnything(labelId, edge.id, placement.rect)) {\n            return { laneId: placement.laneId, anchor: placement.anchor };\n          }\n        }\n      }\n      return void 0;\n    }, \"tryPool\");\n    const findLaneContainingFallback = /* @__PURE__ */ __name((pool, requireEndpointClearance, allowForeignEdgeOverlap = false) => {\n      const rankedPool = rankSegments(pool);\n      for (const seg of rankedPool) {\n        const anchor = { midX: seg.midX, midY: seg.midY };\n        if (requireEndpointClearance && !labelClearsTerminalEndpoints(seg, anchor)) {\n          continue;\n        }\n        const placement = placementForAnchor(anchor);\n        if (placement && !labelOverlapsOwnMarker(placement.rect, pts) && !overlapsPlacedLabel(labelId, placement.rect) && !labelOverlapsForeignNode(labelId, placement.rect) && (allowForeignEdgeOverlap || !labelOverlapsForeignEdge(edge.id, placement.rect))) {\n          return { laneId: placement.laneId, anchor: placement.anchor };\n        }\n      }\n      return void 0;\n    }, \"findLaneContainingFallback\");\n    const chosen = tryPool(poolBase) ?? (poolBase.length < segments.length ? tryPool(segments) : void 0) ?? findLaneContainingFallback(segments, true) ?? findLaneContainingFallback(segments, false) ?? findLaneContainingFallback(segments, false, true);\n    if (chosen) {\n      labelNode.x = chosen.anchor.midX;\n      labelNode.y = chosen.anchor.midY;\n      labelNode.parentId = chosen.laneId;\n      const chosenRect = rectFromCenterSize(chosen.anchor.midX, chosen.anchor.midY, lw, lh);\n      const priorIdx = placedLabelRects.findIndex((placed) => placed.labelId === labelId);\n      if (priorIdx >= 0) {\n        placedLabelRects[priorIdx] = { labelId, rect: chosenRect };\n      } else {\n        placedLabelRects.push({ labelId, rect: chosenRect });\n      }\n    }\n  }\n}\n__name(anchorLabelsToPolyline, \"anchorLabelsToPolyline\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/siblingSharedFaceRouting.ts\nvar EPS6 = 1e-6;\nvar MIN_PORT_SPACING2 = 8;\nvar PORT_SHIFT2 = MIN_PORT_SPACING2 / 2;\nvar LABEL_CLEARANCE_BUFFER = 3;\nfunction pairKey(a, b) {\n  return a < b ? `${a}::${b}` : `${b}::${a}`;\n}\n__name(pairKey, \"pairKey\");\nfunction straightenCollinearSiblingDetours(edges, nodes) {\n  const { nodeInfoById, realNodeRects } = collectRealNodeBounds(nodes);\n  const labelDimById = /* @__PURE__ */ new Map();\n  for (const n of nodes) {\n    const id = n.id;\n    if (n.isGroup) {\n      continue;\n    }\n    if (n.isEdgeLabel) {\n      labelDimById.set(id, {\n        w: n.width ?? 0,\n        h: n.height ?? 0\n      });\n      continue;\n    }\n  }\n  const labelClearanceFor = /* @__PURE__ */ __name((thisEdge, thisSrcId, thisDstId, axis) => {\n    const targetPair = pairKey(thisSrcId, thisDstId);\n    let maxHalf = 0;\n    const consider = /* @__PURE__ */ __name((labelId) => {\n      if (!labelId) {\n        return;\n      }\n      const dim = labelDimById.get(labelId);\n      if (!dim) {\n        return;\n      }\n      const half = axis === \"x\" ? dim.w / 2 : dim.h / 2;\n      if (half > maxHalf) {\n        maxHalf = half;\n      }\n    }, \"consider\");\n    consider(thisEdge.labelNodeId);\n    for (const other of edges) {\n      if (other === thisEdge) {\n        continue;\n      }\n      if (other.isLayoutOnly) {\n        continue;\n      }\n      const oSrc = other.start;\n      const oDst = other.end;\n      if (!oSrc || !oDst) {\n        continue;\n      }\n      if (pairKey(oSrc, oDst) !== targetPair) {\n        continue;\n      }\n      consider(other.labelNodeId);\n    }\n    return maxHalf > 0 ? maxHalf + LABEL_CLEARANCE_BUFFER : 0;\n  }, \"labelClearanceFor\");\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const pts = edge.points;\n    if (!classifyThreeSegmentRoute(pts, EPS6)) {\n      continue;\n    }\n    const nodePair = getNodePairGeometry(edge, nodeInfoById, EPS6);\n    if (!nodePair) {\n      continue;\n    }\n    const { srcId, dstId, srcInfo, dstInfo, collinearX, collinearY } = nodePair;\n    if (collinearX === collinearY) {\n      continue;\n    }\n    let targetSrc;\n    let targetDst;\n    if (collinearX) {\n      const dstBelow = dstInfo.cy > srcInfo.cy;\n      targetSrc = { x: srcInfo.cx, y: dstBelow ? srcInfo.rect.bottom : srcInfo.rect.top };\n      targetDst = { x: dstInfo.cx, y: dstBelow ? dstInfo.rect.top : dstInfo.rect.bottom };\n    } else {\n      const dstEast = dstInfo.cx > srcInfo.cx;\n      targetSrc = { x: dstEast ? srcInfo.rect.right : srcInfo.rect.left, y: srcInfo.cy };\n      targetDst = { x: dstEast ? dstInfo.rect.left : dstInfo.rect.right, y: dstInfo.cy };\n    }\n    if (segmentHitsAnyRect(targetSrc, targetDst, realNodeRects, [srcId, dstId], 1)) {\n      continue;\n    }\n    const shiftAxis = collinearX ? \"x\" : \"y\";\n    const labelShift = labelClearanceFor(edge, srcId, dstId, shiftAxis);\n    const effectiveShift = labelShift > PORT_SHIFT2 ? labelShift : PORT_SHIFT2;\n    const deltas = [0, effectiveShift, -effectiveShift];\n    for (const delta of deltas) {\n      const shiftedSrc = { ...targetSrc };\n      const shiftedDst = { ...targetDst };\n      if (collinearX) {\n        shiftedSrc.x += delta;\n        shiftedDst.x += delta;\n        if (shiftedSrc.x <= srcInfo.rect.left || shiftedSrc.x >= srcInfo.rect.right) {\n          continue;\n        }\n        if (shiftedDst.x <= dstInfo.rect.left || shiftedDst.x >= dstInfo.rect.right) {\n          continue;\n        }\n      } else {\n        shiftedSrc.y += delta;\n        shiftedDst.y += delta;\n        if (shiftedSrc.y <= srcInfo.rect.top || shiftedSrc.y >= srcInfo.rect.bottom) {\n          continue;\n        }\n        if (shiftedDst.y <= dstInfo.rect.top || shiftedDst.y >= dstInfo.rect.bottom) {\n          continue;\n        }\n      }\n      if (segmentHitsAnyRect(shiftedSrc, shiftedDst, realNodeRects, [srcId, dstId], 1)) {\n        continue;\n      }\n      if (segmentConflictsWithAnyEdge(shiftedSrc, shiftedDst, edges, edge, { epsilon: EPS6 })) {\n        continue;\n      }\n      edge.points = [shiftedSrc, shiftedDst];\n      break;\n    }\n  }\n}\n__name(straightenCollinearSiblingDetours, \"straightenCollinearSiblingDetours\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/sharedTrackNudging.ts\nfunction nudgeSharedInteriorSubpaths(edges, nodeByIdMap) {\n  const EPS_LOCAL2 = 1e-3;\n  const MIN_SHARED3 = 8;\n  const TRACK_SHIFT = 7;\n  const MIN_TRACK_GAP = TRACK_SHIFT;\n  const SOURCE_DETOUR_STUB = 20;\n  const BUFFER = 2;\n  const MAX_ITERATIONS = 12;\n  const { realNodeRects, labelNodeRects: labelRects } = collectNodeRectEntries(\n    nodeByIdMap.values()\n  );\n  const segmentsFor2 = /* @__PURE__ */ __name((edge, points) => {\n    return orthogonalSegmentsForPoints(points, EPS_LOCAL2).map((segment) => ({\n      ...segment,\n      edge,\n      interior: segment.index >= 1 && segment.index <= points.length - 3\n    }));\n  }, \"segmentsFor\");\n  const allSegments = /* @__PURE__ */ __name(() => {\n    const result = [];\n    for (const edge of edges) {\n      if (edge.isLayoutOnly) {\n        continue;\n      }\n      const points = edge.points;\n      if (!points || points.length < 2) {\n        continue;\n      }\n      result.push(...segmentsFor2(edge, dedupeConsecutivePoints(points)));\n    }\n    return result;\n  }, \"allSegments\");\n  const hasCrowdedParallelTrack = /* @__PURE__ */ __name((a, b) => {\n    if (a.horizontal && b.horizontal) {\n      return overlapLength(a.a.x, a.b.x, b.a.x, b.b.x) >= MIN_SHARED3 && Math.abs(a.a.y - b.a.y) < MIN_TRACK_GAP;\n    }\n    if (a.vertical && b.vertical) {\n      return overlapLength(a.a.y, a.b.y, b.a.y, b.b.y) >= MIN_SHARED3 && Math.abs(a.a.x - b.a.x) < MIN_TRACK_GAP;\n    }\n    return false;\n  }, \"hasCrowdedParallelTrack\");\n  const candidateIsSafe = /* @__PURE__ */ __name((edge, candidate) => {\n    const sourceId = edge.start;\n    const targetId = edge.end;\n    const candidateSegments = segmentsFor2(edge, candidate);\n    if (candidateSegments.length !== candidate.length - 1) {\n      return false;\n    }\n    const endpointIds = [sourceId, targetId].filter((id) => Boolean(id));\n    const ownLabelIds = edge.labelNodeId ? [edge.labelNodeId] : [];\n    for (const segment of candidateSegments) {\n      if (segmentHitsAnyRect(segment.a, segment.b, realNodeRects, endpointIds, -BUFFER)) {\n        return false;\n      }\n      if (segmentHitsAnyRect(segment.a, segment.b, labelRects, ownLabelIds, -BUFFER)) {\n        return false;\n      }\n    }\n    for (const other of edges) {\n      if (other === edge || other.isLayoutOnly) {\n        continue;\n      }\n      const otherPoints = other.points;\n      if (!otherPoints || otherPoints.length < 2) {\n        continue;\n      }\n      for (const candidateSegment of candidateSegments) {\n        for (const otherSegment of segmentsFor2(other, dedupeConsecutivePoints(otherPoints))) {\n          if (hasCrowdedParallelTrack(candidateSegment, otherSegment)) {\n            return false;\n          }\n          if (orthogonalSegmentsStrictlyCross(\n            candidateSegment.a,\n            candidateSegment.b,\n            otherSegment.a,\n            otherSegment.b,\n            EPS_LOCAL2\n          )) {\n            return false;\n          }\n        }\n      }\n    }\n    return true;\n  }, \"candidateIsSafe\");\n  const shiftedCandidate = /* @__PURE__ */ __name((segment, shift) => {\n    const points = dedupeConsecutivePoints(segment.edge.points ?? []);\n    if (points.length < 4 || segment.index >= points.length - 1) {\n      return void 0;\n    }\n    const candidate = points.map((p) => ({ ...p }));\n    if (segment.horizontal) {\n      candidate[segment.index].y += shift;\n      candidate[segment.index + 1].y += shift;\n    } else if (segment.vertical) {\n      candidate[segment.index].x += shift;\n      candidate[segment.index + 1].x += shift;\n    } else {\n      return void 0;\n    }\n    return segmentsFor2(segment.edge, candidate).length === candidate.length - 1 ? candidate : void 0;\n  }, \"shiftedCandidate\");\n  const nodeCenter = /* @__PURE__ */ __name((node, rect) => ({\n    x: node.x ?? (rect.left + rect.right) / 2,\n    y: node.y ?? (rect.top + rect.bottom) / 2\n  }), \"nodeCenter\");\n  const sourceDetourContextFor = /* @__PURE__ */ __name((segment) => {\n    const edge = segment.edge;\n    const points = dedupeConsecutivePoints(edge.points ?? []);\n    if (points.length !== 4 || segment.index !== 1) {\n      return void 0;\n    }\n    const sourceNode = edge.start ? nodeByIdMap.get(edge.start) : void 0;\n    const targetNode = edge.end ? nodeByIdMap.get(edge.end) : void 0;\n    const sourceRect = sourceNode ? rectOfNodeBounds(sourceNode) : void 0;\n    const targetRect = targetNode ? rectOfNodeBounds(targetNode) : void 0;\n    const tail = points.slice(segment.index + 2);\n    if (!sourceNode || !targetNode || !sourceRect || !targetRect || tail.length === 0) {\n      return void 0;\n    }\n    return {\n      sourceCenter: nodeCenter(sourceNode, sourceRect),\n      targetCenter: nodeCenter(targetNode, targetRect),\n      sourceRect,\n      tail\n    };\n  }, \"sourceDetourContextFor\");\n  const verticalSourceDetour = /* @__PURE__ */ __name((segment, shift, sourceCenter, targetCenter, sourceRect, tail) => {\n    const targetBelow = targetCenter.y >= sourceCenter.y;\n    const sourcePortY = targetBelow ? sourceRect.bottom : sourceRect.top;\n    const stubY = sourcePortY + (targetBelow ? SOURCE_DETOUR_STUB : -SOURCE_DETOUR_STUB);\n    if (targetBelow && segment.b.y <= stubY + EPS_LOCAL2 || !targetBelow && segment.b.y >= stubY - EPS_LOCAL2) {\n      return void 0;\n    }\n    const railX = segment.a.x + shift;\n    return dedupeConsecutivePoints(\n      [\n        { x: sourceCenter.x, y: sourcePortY },\n        { x: sourceCenter.x, y: stubY },\n        { x: railX, y: stubY },\n        { x: railX, y: segment.b.y },\n        ...tail\n      ],\n      EPS_LOCAL2\n    );\n  }, \"verticalSourceDetour\");\n  const horizontalSourceDetour = /* @__PURE__ */ __name((segment, shift, sourceCenter, targetCenter, sourceRect, tail) => {\n    const targetRight = targetCenter.x >= sourceCenter.x;\n    const sourcePortX = targetRight ? sourceRect.right : sourceRect.left;\n    const stubX = sourcePortX + (targetRight ? SOURCE_DETOUR_STUB : -SOURCE_DETOUR_STUB);\n    if (targetRight && segment.b.x <= stubX + EPS_LOCAL2 || !targetRight && segment.b.x >= stubX - EPS_LOCAL2) {\n      return void 0;\n    }\n    const railY = segment.a.y + shift;\n    return dedupeConsecutivePoints(\n      [\n        { x: sourcePortX, y: sourceCenter.y },\n        { x: stubX, y: sourceCenter.y },\n        { x: stubX, y: railY },\n        { x: segment.b.x, y: railY },\n        ...tail\n      ],\n      EPS_LOCAL2\n    );\n  }, \"horizontalSourceDetour\");\n  const sourceDetourCandidate = /* @__PURE__ */ __name((segment, shift) => {\n    const context = sourceDetourContextFor(segment);\n    if (!context) {\n      return void 0;\n    }\n    if (segment.vertical) {\n      return verticalSourceDetour(\n        segment,\n        shift,\n        context.sourceCenter,\n        context.targetCenter,\n        context.sourceRect,\n        context.tail\n      );\n    }\n    if (segment.horizontal) {\n      return horizontalSourceDetour(\n        segment,\n        shift,\n        context.sourceCenter,\n        context.targetCenter,\n        context.sourceRect,\n        context.tail\n      );\n    }\n    return void 0;\n  }, \"sourceDetourCandidate\");\n  const shifts = [\n    -TRACK_SHIFT,\n    TRACK_SHIFT,\n    -2 * TRACK_SHIFT,\n    2 * TRACK_SHIFT,\n    -3 * TRACK_SHIFT,\n    3 * TRACK_SHIFT\n  ];\n  for (let iteration = 0; iteration < MAX_ITERATIONS; iteration++) {\n    const segments = allSegments();\n    let fixed = false;\n    for (let i = 0; i < segments.length && !fixed; i++) {\n      for (let j = i + 1; j < segments.length && !fixed; j++) {\n        const first = segments[i];\n        const second = segments[j];\n        if (first.edge === second.edge || !hasCrowdedParallelTrack(first, second)) {\n          continue;\n        }\n        const candidates = [first, second].filter((segment) => segment.interior);\n        for (const segment of candidates) {\n          for (const shift of shifts) {\n            const direct = shiftedCandidate(segment, shift);\n            if (direct && candidateIsSafe(segment.edge, direct)) {\n              segment.edge.points = direct;\n              fixed = true;\n              break;\n            }\n            const detoured = sourceDetourCandidate(segment, shift);\n            if (detoured && candidateIsSafe(segment.edge, detoured)) {\n              segment.edge.points = detoured;\n              fixed = true;\n              break;\n            }\n          }\n          if (fixed) {\n            break;\n          }\n        }\n      }\n    }\n    if (!fixed) {\n      return;\n    }\n  }\n}\n__name(nudgeSharedInteriorSubpaths, \"nudgeSharedInteriorSubpaths\");\n\n// src/rendering-util/layout-algorithms/swimlanes/direction/validation.ts\nfunction segmentsIntersect(p1, p2, p3, p4) {\n  const d1x = p2.x - p1.x;\n  const d1y = p2.y - p1.y;\n  const d2x = p4.x - p3.x;\n  const d2y = p4.y - p3.y;\n  const cross = d1x * d2y - d1y * d2x;\n  if (Math.abs(cross) < 1e-10) {\n    return false;\n  }\n  const dx = p3.x - p1.x;\n  const dy = p3.y - p1.y;\n  const t = (dx * d2y - dy * d2x) / cross;\n  const u = (dx * d1y - dy * d1x) / cross;\n  const eps = 0.01;\n  return t > eps && t < 1 - eps && u > eps && u < 1 - eps;\n}\n__name(segmentsIntersect, \"segmentsIntersect\");\nfunction validateSwimlanesLayout(layout) {\n  const nodes = layout.nodes ?? [];\n  const edges = layout.edges ?? [];\n  const issues = [];\n  if (!edges.length || !nodes.length) {\n    return issues;\n  }\n  const nodeRects = collectLayoutNodeRects(nodes);\n  const epsilon = 1;\n  const edgeSegments = [];\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const points = edge.points;\n    if (!points || points.length < 2) {\n      continue;\n    }\n    const edgeStart = edge.start;\n    const edgeEnd = edge.end;\n    const ownLabelId = edge.labelNodeId;\n    const edgeId = edge.id ?? `${edgeStart}->${edgeEnd}`;\n    for (const rect of nodeRects) {\n      if (rect.nodeId === edgeStart || rect.nodeId === edgeEnd) {\n        continue;\n      }\n      if (ownLabelId && rect.nodeId === ownLabelId) {\n        continue;\n      }\n      for (let i = 0; i < points.length - 1; i++) {\n        if (segmentBoundsOverlapRect(points[i], points[i + 1], rect, -epsilon)) {\n          issues.push({\n            type: \"edge-node-overlap\",\n            edgeId,\n            targetId: rect.nodeId,\n            detail: `segment ${i} passes through node \"${rect.nodeId}\"`\n          });\n          break;\n        }\n      }\n    }\n    for (let i = 0; i < points.length - 1; i++) {\n      edgeSegments.push({\n        edgeId,\n        start: edgeStart,\n        end: edgeEnd,\n        p1: points[i],\n        p2: points[i + 1]\n      });\n    }\n  }\n  const crossingPairs = /* @__PURE__ */ new Set();\n  for (let i = 0; i < edgeSegments.length; i++) {\n    for (let j = i + 1; j < edgeSegments.length; j++) {\n      const a = edgeSegments[i];\n      const b = edgeSegments[j];\n      if (a.edgeId === b.edgeId) {\n        continue;\n      }\n      if (a.start === b.start || a.start === b.end || a.end === b.start || a.end === b.end) {\n        continue;\n      }\n      if (segmentsIntersect(a.p1, a.p2, b.p1, b.p2)) {\n        const pairKey2 = a.edgeId < b.edgeId ? `${a.edgeId}|${b.edgeId}` : `${b.edgeId}|${a.edgeId}`;\n        if (!crossingPairs.has(pairKey2)) {\n          crossingPairs.add(pairKey2);\n          issues.push({\n            type: \"edge-edge-crossing\",\n            edgeId: a.edgeId,\n            targetId: b.edgeId,\n            detail: `edges \"${a.edgeId}\" and \"${b.edgeId}\" cross`\n          });\n        }\n      }\n    }\n  }\n  if (issues.length > 0) {\n    const overlaps = issues.filter((i) => i.type === \"edge-node-overlap\").length;\n    const crossings = issues.filter((i) => i.type === \"edge-edge-crossing\").length;\n    log.warn(\n      `[SWIMLANE_VALIDATE] ${issues.length} issue(s) detected: ${overlaps} edge-node overlap(s), ${crossings} edge crossing(s)`\n    );\n    for (const issue of issues) {\n      log.warn(`[SWIMLANE_VALIDATE]   ${issue.type}: ${issue.detail}`);\n    }\n  }\n  return issues;\n}\n__name(validateSwimlanesLayout, \"validateSwimlanesLayout\");\n\n// src/rendering-util/layout-algorithms/swimlanes/postProcessing.ts\nfunction postProcessSwimlaneLayout(layout, direction) {\n  const nodes = layout.nodes ?? [];\n  const edges = layout.edges ?? [];\n  const contentNodes = nodes.filter((n) => !n.isGroup);\n  if ((direction === \"LR\" || direction === \"RL\") && contentNodes.length > 0 && !applyLrDirectionTransform(layout, direction)) {\n    return;\n  }\n  if (direction === \"BT\" && contentNodes.length > 0 && !applyBtDirectionTransform(layout)) {\n    return;\n  }\n  for (const edge of edges) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const pts = edge.points;\n    if (!pts || pts.length < 2) {\n      continue;\n    }\n    edge.points = simplifyPolyline(\n      orthogonalizePolyline(pts)\n    );\n  }\n  simplifyDetouredEdges(edges, nodes);\n  straightenCollinearSiblingDetours(edges, nodes);\n  portSwapToLShape(edges, nodes);\n  const nodeByIdMap = /* @__PURE__ */ new Map();\n  for (const n of nodes) {\n    nodeByIdMap.set(String(n.id), n);\n  }\n  anchorLabelsToPolyline(edges, nodeByIdMap);\n  clipEdgeEndpointsToNodeBoundaries(edges, nodeByIdMap);\n  collapseShortTerminalStub(edges, nodeByIdMap);\n  nudgeSharedInteriorSubpaths(edges, nodeByIdMap);\n  separateSharedRenderedTerminalLanes(edges, nodeByIdMap);\n  collapseRedundantRectangularDoglegs(edges, nodeByIdMap);\n  liftObstacleHuggingSameSideRails(edges, nodeByIdMap);\n  swapDestinationTerminalTailsToReduceCrossings(edges, nodeByIdMap);\n  const finalizeRenderedEdges = /* @__PURE__ */ __name(() => {\n    resolveRenderedOrthogonalCrossings(edges, nodeByIdMap);\n    reassignCrossingExternalRailChannels(edges, nodeByIdMap);\n    shortcutRedundantOrthogonalJogs(edges, nodeByIdMap);\n    anchorLabelsToPolyline(edges, nodeByIdMap);\n    prepareEdgeEndpointsForRenderer(edges, nodeByIdMap);\n    liftObstacleHuggingSameSideRails(edges, nodeByIdMap);\n    anchorLabelsToPolyline(edges, nodeByIdMap);\n    prepareEdgeEndpointsForRenderer(edges, nodeByIdMap);\n  }, \"finalizeRenderedEdges\");\n  finalizeRenderedEdges();\n  nudgeSharedInteriorSubpaths(edges, nodeByIdMap);\n  finalizeRenderedEdges();\n  liftTopLaneTitleBandsAboveRails(edges, nodeByIdMap);\n  shiftLeftLaneTitleBandsLeftOfRails(edges, nodeByIdMap);\n  liftTopLaneTitleBandsAboveRails(edges, nodeByIdMap);\n  shiftLeftLaneTitleBandsLeftOfRails(edges, nodeByIdMap);\n}\n__name(postProcessSwimlaneLayout, \"postProcessSwimlaneLayout\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase0.helpers.ts\nfunction normalizeGraph(g) {\n  const nodeById = new Map(g.nodeById);\n  const seen = /* @__PURE__ */ new Set();\n  const edges = [];\n  for (const e of g.edges) {\n    if (!nodeById.has(e.src) || !nodeById.has(e.dst)) {\n      continue;\n    }\n    const key = `${e.id}:${e.src}->${e.dst}`;\n    if (seen.has(key)) {\n      continue;\n    }\n    seen.add(key);\n    edges.push(e);\n  }\n  const nodes = [...nodeById.keys()];\n  return { nodes, edges, layout: g.layout, nodeById };\n}\n__name(normalizeGraph, \"normalizeGraph\");\nfunction incoming(g, v) {\n  return g.edges.filter((e) => e.dst === v);\n}\n__name(incoming, \"incoming\");\nfunction buildSuccessorMap(g) {\n  const succs = /* @__PURE__ */ new Map();\n  for (const v of g.nodes) {\n    succs.set(v, []);\n  }\n  for (const e of g.edges) {\n    succs.get(e.src).push(e.dst);\n  }\n  return succs;\n}\n__name(buildSuccessorMap, \"buildSuccessorMap\");\nfunction buildSortedSuccessorMap(g) {\n  const succs = buildSuccessorMap(g);\n  for (const successors of succs.values()) {\n    successors.sort((a, b) => a.localeCompare(b));\n  }\n  return succs;\n}\n__name(buildSortedSuccessorMap, \"buildSortedSuccessorMap\");\nfunction buildInDegreeMap(g) {\n  const indeg = /* @__PURE__ */ new Map();\n  for (const v of g.nodes) {\n    indeg.set(v, 0);\n  }\n  for (const e of g.edges) {\n    indeg.set(e.dst, (indeg.get(e.dst) ?? 0) + 1);\n  }\n  return indeg;\n}\n__name(buildInDegreeMap, \"buildInDegreeMap\");\nfunction sortedZeroInDegreeNodes(indeg) {\n  return [...indeg.entries()].filter(([, degree]) => degree === 0).map(([id]) => id).sort((a, b) => a.localeCompare(b));\n}\n__name(sortedZeroInDegreeNodes, \"sortedZeroInDegreeNodes\");\nfunction buildPredecessorSuccessorMaps(g, includeEdge = () => true) {\n  const preds = /* @__PURE__ */ new Map();\n  const succs = /* @__PURE__ */ new Map();\n  for (const v of g.nodes) {\n    preds.set(v, []);\n    succs.set(v, []);\n  }\n  for (const e of g.edges) {\n    if (!includeEdge(e)) {\n      continue;\n    }\n    succs.get(e.src).push(e.dst);\n    preds.get(e.dst).push(e.src);\n  }\n  return { preds, succs };\n}\n__name(buildPredecessorSuccessorMaps, \"buildPredecessorSuccessorMaps\");\nfunction buildLayersFromRanks(g, order, rankOf, opts) {\n  let maxRank = 0;\n  for (const v of g.nodes) {\n    if (opts?.skipGroups && g.nodeById.get(v)?.isGroup) {\n      continue;\n    }\n    maxRank = Math.max(maxRank, rankOf[v] ?? 0);\n  }\n  const layers = Array.from({ length: maxRank + 1 }, () => []);\n  for (const v of order) {\n    if (opts?.skipGroups && g.nodeById.get(v)?.isGroup) {\n      continue;\n    }\n    layers[Math.max(0, rankOf[v] ?? 0)].push(v);\n  }\n  return layers;\n}\n__name(buildLayersFromRanks, \"buildLayersFromRanks\");\nfunction topoSortIfAcyclic(g) {\n  const indeg = buildInDegreeMap(g);\n  const queue = sortedZeroInDegreeNodes(indeg);\n  const order = [];\n  const adj = buildSortedSuccessorMap(g);\n  while (queue.length) {\n    const u = queue.shift();\n    order.push(u);\n    for (const v of adj.get(u) ?? []) {\n      indeg.set(v, (indeg.get(v) ?? 0) - 1);\n      if ((indeg.get(v) ?? 0) === 0) {\n        let i = 0;\n        while (i < queue.length && queue[i] < v) {\n          i++;\n        }\n        queue.splice(i, 0, v);\n      }\n    }\n  }\n  return order.length === g.nodes.length ? order : null;\n}\n__name(topoSortIfAcyclic, \"topoSortIfAcyclic\");\nfunction buildLayerIndex(layer) {\n  const m = /* @__PURE__ */ new Map();\n  let index = 0;\n  for (const id of layer) {\n    m.set(id, index);\n    index++;\n  }\n  return m;\n}\n__name(buildLayerIndex, \"buildLayerIndex\");\nfunction countInversions(values) {\n  const tmp = new Array(values.length);\n  const count = /* @__PURE__ */ __name((left, right) => {\n    if (right - left <= 1) {\n      return 0;\n    }\n    const mid = left + right >> 1;\n    let inversions = count(left, mid) + count(mid, right);\n    let i = left;\n    let j = mid;\n    let k = left;\n    while (i < mid || j < right) {\n      if (j >= right || i < mid && values[i] <= values[j]) {\n        tmp[k++] = values[i++];\n      } else {\n        tmp[k++] = values[j++];\n        inversions += mid - i;\n      }\n    }\n    for (let t = left; t < right; t++) {\n      values[t] = tmp[t];\n    }\n    return inversions;\n  }, \"count\");\n  return count(0, values.length);\n}\n__name(countInversions, \"countInversions\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase1.cycles.ts\nfunction removeCycles_DFS(g) {\n  const gn = normalizeGraph(g);\n  const adj = /* @__PURE__ */ new Map();\n  for (const v of gn.nodes) {\n    adj.set(v, []);\n  }\n  for (const e of gn.edges) {\n    adj.get(e.src).push(e);\n  }\n  for (const arr of adj.values()) {\n    arr.sort((a, b) => a.dst === b.dst ? a.id.localeCompare(b.id) : a.dst.localeCompare(b.dst));\n  }\n  const color = /* @__PURE__ */ Object.create(null);\n  for (const v of gn.nodes) {\n    color[v] = 0;\n  }\n  const reversed = [];\n  const dfs = /* @__PURE__ */ __name((u) => {\n    color[u] = 1;\n    for (const e of adj.get(u) ?? []) {\n      const v = e.dst;\n      if (color[v] === 0) {\n        dfs(v);\n      } else if (color[v] === 1) {\n        reversed.push(e);\n      }\n    }\n    color[u] = 2;\n  }, \"dfs\");\n  const nodesSorted = [...gn.nodes].sort((a, b) => a.localeCompare(b));\n  for (const v of nodesSorted) {\n    if (color[v] === 0) {\n      dfs(v);\n    }\n  }\n  const toReverse = new Set(reversed.map((e) => `${e.id}:${e.src}->${e.dst}`));\n  const acycEdges = gn.edges.map(\n    (e) => toReverse.has(`${e.id}:${e.src}->${e.dst}`) ? { id: e.id, src: e.dst, dst: e.src, weight: e.weight, ref: e.ref } : e\n  );\n  const acyclic = {\n    nodes: [...gn.nodes],\n    edges: acycEdges,\n    layout: gn.layout,\n    nodeById: new Map(gn.nodeById)\n  };\n  return { acyclic, reversed };\n}\n__name(removeCycles_DFS, \"removeCycles_DFS\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.options.ts\nfunction buildTopLaneMap(g) {\n  const cache = /* @__PURE__ */ new Map();\n  const resolve = /* @__PURE__ */ __name((id) => {\n    if (cache.has(id)) {\n      return cache.get(id);\n    }\n    const node = g.nodeById.get(id);\n    if (!node) {\n      cache.set(id, null);\n      return null;\n    }\n    const parentId = node.parentId;\n    if (!parentId) {\n      cache.set(id, null);\n      return null;\n    }\n    const parentLane = resolve(parentId);\n    const lane = parentLane ?? parentId;\n    cache.set(id, lane);\n    return lane;\n  }, \"resolve\");\n  for (const id of g.nodes) {\n    resolve(id);\n  }\n  return cache;\n}\n__name(buildTopLaneMap, \"buildTopLaneMap\");\nfunction createTopLaneResolver(g) {\n  const topLaneMap = buildTopLaneMap(g);\n  return (id) => topLaneMap.get(id) ?? null;\n}\n__name(createTopLaneResolver, \"createTopLaneResolver\");\nfunction buildTopLaneOrder(g) {\n  const lanes = [];\n  for (const node of g.layout.nodes ?? []) {\n    if (node.isGroup && !node.parentId) {\n      lanes.push(node.id);\n    }\n  }\n  return [...new Set(lanes)].reverse();\n}\n__name(buildTopLaneOrder, \"buildTopLaneOrder\");\nfunction resolveTopLaneOrder(g, preferredOrder) {\n  const sourceOrder = buildTopLaneOrder(g);\n  if (!preferredOrder || preferredOrder.length === 0) {\n    return sourceOrder;\n  }\n  const sourceLaneIds = new Set(sourceOrder);\n  const seen = /* @__PURE__ */ new Set();\n  const resolved = [];\n  for (const laneId of preferredOrder) {\n    if (!sourceLaneIds.has(laneId) || seen.has(laneId)) {\n      continue;\n    }\n    seen.add(laneId);\n    resolved.push(laneId);\n  }\n  for (const laneId of sourceOrder) {\n    if (seen.has(laneId)) {\n      continue;\n    }\n    resolved.push(laneId);\n  }\n  return resolved;\n}\n__name(resolveTopLaneOrder, \"resolveTopLaneOrder\");\n\n// src/rendering-util/layout-algorithms/swimlanes/config.ts\nvar PRECISION = {\n  /** Epsilon for floating-point comparisons */\n  EPSILON: 1e-6\n};\nvar LAYERING = {\n  /** Default number of iterations for gravity-based layering */\n  GRAVITY_ITERATIONS: 8,\n  /** Maximum number of passes for crossing-based rank optimization */\n  MAX_CROSSING_OPTIMIZATION_PASSES: 4,\n  /** Whether to compact single-input nodes by default */\n  DEFAULT_COMPACT_SINGLE_INPUT: true\n};\nvar COORDINATES = {\n  /** Default vertical gap between layers (px) */\n  DEFAULT_LAYER_GAP: 100,\n  /** Default horizontal gap between nodes (px) */\n  DEFAULT_NODE_GAP: 40\n};\n\n// src/rendering-util/layout-algorithms/swimlanes/driving-tree.ts\nfunction buildDrivingTree(graph, opts) {\n  const g = normalizeGraph(graph);\n  const laneOf = opts?.laneOf ?? (() => null);\n  const rankHint = opts?.rankHint;\n  const { preds } = buildPredecessorSuccessorMaps(g);\n  for (const arr of preds.values()) {\n    arr.sort((a, b) => a.localeCompare(b));\n  }\n  const topoOrder = topoSortIfAcyclic(g) ?? [...g.nodes].sort((a, b) => a.localeCompare(b));\n  const topoIndex = /* @__PURE__ */ new Map();\n  for (const [idx, id] of topoOrder.entries()) {\n    topoIndex.set(id, idx);\n  }\n  const parent = /* @__PURE__ */ new Map();\n  const children = /* @__PURE__ */ new Map();\n  for (const node of g.nodes) {\n    children.set(node, []);\n  }\n  for (const node of topoOrder) {\n    const candidates = (preds.get(node) ?? []).filter((p) => parent.has(p));\n    if (candidates.length > 0) {\n      const chosen = chooseParent(node, candidates, {\n        laneOf,\n        rankHint,\n        topoIndex\n      });\n      parent.set(node, chosen);\n      children.get(chosen).push(node);\n    } else if (!parent.has(node)) {\n      parent.set(node, null);\n    }\n  }\n  for (const node of g.nodes) {\n    if (!parent.has(node)) {\n      parent.set(node, null);\n    }\n  }\n  const rootSet = /* @__PURE__ */ new Set();\n  for (const node of g.nodes) {\n    if ((parent.get(node) ?? null) === null) {\n      rootSet.add(node);\n    }\n  }\n  const roots = [...rootSet].sort((a, b) => {\n    const ta = topoIndex.get(a) ?? 0;\n    const tb = topoIndex.get(b) ?? 0;\n    if (ta === tb) {\n      return a.localeCompare(b);\n    }\n    return ta - tb;\n  });\n  const adjacency = buildAdjacency(g);\n  const adjacencyList = /* @__PURE__ */ new Map();\n  for (const [node, set] of adjacency.entries()) {\n    adjacencyList.set(\n      node,\n      [...set].sort((a, b) => a.localeCompare(b))\n    );\n  }\n  const componentOf = assignComponents(adjacencyList);\n  const blocks = computeBlocks(adjacencyList);\n  const nodeBlocks = /* @__PURE__ */ new Map();\n  for (const node of g.nodes) {\n    nodeBlocks.set(node, []);\n  }\n  for (const block of blocks) {\n    for (const node of block.nodes) {\n      const list = nodeBlocks.get(node);\n      if (list) {\n        list.push(block.id);\n      } else {\n        nodeBlocks.set(node, [block.id]);\n      }\n    }\n  }\n  const preorder = [];\n  const postorder = [];\n  const seen = /* @__PURE__ */ new Set();\n  const walk = /* @__PURE__ */ __name((node) => {\n    if (seen.has(node)) {\n      return;\n    }\n    seen.add(node);\n    preorder.push(node);\n    for (const child of children.get(node) ?? []) {\n      walk(child);\n    }\n    postorder.push(node);\n  }, \"walk\");\n  for (const root of roots) {\n    walk(root);\n  }\n  for (const node of topoOrder) {\n    walk(node);\n  }\n  return {\n    parent,\n    children,\n    roots,\n    componentOf,\n    blocks,\n    nodeBlocks,\n    adjacency: adjacencyList,\n    preorder,\n    postorder,\n    topologicalOrder: topoOrder\n  };\n}\n__name(buildDrivingTree, \"buildDrivingTree\");\nfunction chooseParent(node, candidates, ctx) {\n  const laneNode = ctx.laneOf(node);\n  const sorted = [...candidates].sort((a, b) => {\n    const laneA = ctx.laneOf(a);\n    const laneB = ctx.laneOf(b);\n    const sameLaneA = laneA != null && laneA === laneNode;\n    const sameLaneB = laneB != null && laneB === laneNode;\n    if (sameLaneA !== sameLaneB) {\n      return sameLaneA ? -1 : 1;\n    }\n    const rankA = ctx.rankHint?.[a];\n    const rankB = ctx.rankHint?.[b];\n    if (rankA != null && rankB != null && rankA !== rankB) {\n      return rankB - rankA;\n    }\n    const idxA = ctx.topoIndex.get(a) ?? 0;\n    const idxB = ctx.topoIndex.get(b) ?? 0;\n    if (idxA !== idxB) {\n      return idxA - idxB;\n    }\n    return a.localeCompare(b);\n  });\n  return sorted[0];\n}\n__name(chooseParent, \"chooseParent\");\nfunction buildAdjacency(g) {\n  const adjacency = /* @__PURE__ */ new Map();\n  for (const node of g.nodes) {\n    adjacency.set(node, /* @__PURE__ */ new Set());\n  }\n  for (const e of g.edges) {\n    adjacency.get(e.src).add(e.dst);\n    adjacency.get(e.dst).add(e.src);\n  }\n  return adjacency;\n}\n__name(buildAdjacency, \"buildAdjacency\");\nfunction assignComponents(adjacency) {\n  const componentOf = /* @__PURE__ */ new Map();\n  let componentId = 0;\n  for (const node of adjacency.keys()) {\n    if (componentOf.has(node)) {\n      continue;\n    }\n    const stack = [node];\n    while (stack.length > 0) {\n      const cur = stack.pop();\n      if (componentOf.has(cur)) {\n        continue;\n      }\n      componentOf.set(cur, componentId);\n      for (const next of adjacency.get(cur) ?? []) {\n        if (!componentOf.has(next)) {\n          stack.push(next);\n        }\n      }\n    }\n    componentId++;\n  }\n  return componentOf;\n}\n__name(assignComponents, \"assignComponents\");\nfunction computeBlocks(adjacency) {\n  const discovery = /* @__PURE__ */ new Map();\n  const low = /* @__PURE__ */ new Map();\n  const edgeStack = [];\n  const blocks = [];\n  let time = 0;\n  const visit = /* @__PURE__ */ __name((node, parent) => {\n    discovery.set(node, ++time);\n    low.set(node, time);\n    for (const next of adjacency.get(node) ?? []) {\n      if (next === parent) {\n        continue;\n      }\n      if (!discovery.has(next)) {\n        edgeStack.push([node, next]);\n        visit(next, node);\n        low.set(node, Math.min(low.get(node) ?? time, low.get(next) ?? time));\n        if ((low.get(next) ?? 0) >= (discovery.get(node) ?? 0)) {\n          blocks.push(popBlock(node, next, edgeStack, blocks.length));\n        }\n      } else if ((discovery.get(next) ?? 0) < (discovery.get(node) ?? 0)) {\n        edgeStack.push([node, next]);\n        low.set(node, Math.min(low.get(node) ?? time, discovery.get(next) ?? time));\n      }\n    }\n  }, \"visit\");\n  for (const node of adjacency.keys()) {\n    if (!discovery.has(node)) {\n      visit(node, null);\n    }\n  }\n  return blocks;\n}\n__name(computeBlocks, \"computeBlocks\");\nfunction popBlock(u, v, stack, id) {\n  const edges = [];\n  const nodes = /* @__PURE__ */ new Set();\n  while (stack.length > 0) {\n    const edge = stack.pop();\n    edges.push(edge);\n    nodes.add(edge[0]);\n    nodes.add(edge[1]);\n    if (edge[0] === u && edge[1] === v || edge[0] === v && edge[1] === u) {\n      break;\n    }\n  }\n  return { id, edges, nodes: [...nodes] };\n}\n__name(popBlock, \"popBlock\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.crossCounts.ts\nfunction computeSubtreeCrossCounts(g, rankOf, tree) {\n  const nodes = [...g.nodes];\n  const indexOf = /* @__PURE__ */ new Map();\n  for (const [i, node] of nodes.entries()) {\n    indexOf.set(node, i);\n  }\n  const n = nodes.length;\n  const parentIdx = new Array(n).fill(-1);\n  const depth = new Array(n).fill(0);\n  const queue = [];\n  const seen = /* @__PURE__ */ new Set();\n  for (const node of nodes) {\n    const parentId = tree.parent.get(node) ?? null;\n    const idx = indexOf.get(node);\n    if (idx == null) {\n      continue;\n    }\n    if (parentId == null) {\n      parentIdx[idx] = -1;\n      depth[idx] = 0;\n      if (!seen.has(node)) {\n        seen.add(node);\n        queue.push(node);\n      }\n    }\n  }\n  while (queue.length > 0) {\n    const current = queue.shift();\n    const currentIdx = indexOf.get(current);\n    if (currentIdx == null) {\n      continue;\n    }\n    const childList = tree.children.get(current) ?? [];\n    for (const child of childList) {\n      if (seen.has(child)) {\n        continue;\n      }\n      const childIdx = indexOf.get(child);\n      if (childIdx == null) {\n        continue;\n      }\n      parentIdx[childIdx] = currentIdx;\n      depth[childIdx] = depth[currentIdx] + 1;\n      seen.add(child);\n      queue.push(child);\n    }\n  }\n  for (const node of nodes) {\n    if (seen.has(node)) {\n      continue;\n    }\n    const idx = indexOf.get(node);\n    if (idx == null) {\n      continue;\n    }\n    parentIdx[idx] = -1;\n    depth[idx] = 0;\n    seen.add(node);\n  }\n  const maxLog = Math.max(1, Math.ceil(Math.log2(Math.max(1, n))) + 1);\n  const up = Array.from({ length: maxLog }, () => new Array(n).fill(-1));\n  for (let i = 0; i < n; i++) {\n    up[0][i] = parentIdx[i];\n  }\n  for (let k = 1; k < maxLog; k++) {\n    for (let i = 0; i < n; i++) {\n      const prev = up[k - 1][i];\n      up[k][i] = prev === -1 ? -1 : up[k - 1][prev];\n    }\n  }\n  const lcaIndex = /* @__PURE__ */ __name((aIdx, bIdx) => {\n    if (aIdx === -1 || bIdx === -1) {\n      return -1;\n    }\n    if (depth[aIdx] < depth[bIdx]) {\n      [aIdx, bIdx] = [bIdx, aIdx];\n    }\n    const diff = depth[aIdx] - depth[bIdx];\n    for (let k = 0; k < maxLog; k++) {\n      if (diff >> k & 1) {\n        aIdx = up[k][aIdx];\n        if (aIdx === -1) {\n          return -1;\n        }\n      }\n    }\n    if (aIdx === bIdx) {\n      return aIdx;\n    }\n    for (let k = maxLog - 1; k >= 0; k--) {\n      const upA = up[k][aIdx];\n      const upB = up[k][bIdx];\n      if (upA === -1 || upB === -1) {\n        continue;\n      }\n      if (upA !== upB) {\n        aIdx = upA;\n        bIdx = upB;\n      }\n    }\n    return up[0][aIdx];\n  }, \"lcaIndex\");\n  const ownCounts = Array.from({ length: n }, () => /* @__PURE__ */ new Map());\n  for (const edge of g.edges) {\n    let src = edge.src;\n    let dst = edge.dst;\n    let ru = rankOf[src];\n    let rv = rankOf[dst];\n    if (ru == null || rv == null) {\n      continue;\n    }\n    if (ru > rv) {\n      [src, dst] = [dst, src];\n      [ru, rv] = [rv, ru];\n    }\n    if (ru == null || rv == null || ru === rv) {\n      continue;\n    }\n    const upperIdx = indexOf.get(src);\n    const lowerIdx = indexOf.get(dst);\n    if (upperIdx == null || lowerIdx == null) {\n      continue;\n    }\n    const lca = lcaIndex(upperIdx, lowerIdx);\n    if (lca === -1) {\n      continue;\n    }\n    const bucket = ownCounts[lca];\n    for (let layer = ru; layer < rv; layer++) {\n      bucket.set(layer, (bucket.get(layer) ?? 0) + 1);\n    }\n  }\n  const crossCounts = /* @__PURE__ */ new Map();\n  const mergeInto = /* @__PURE__ */ __name((target, source) => {\n    if (source.size === 0) {\n      return;\n    }\n    for (const [layer, value] of source) {\n      target.set(layer, (target.get(layer) ?? 0) + value);\n    }\n  }, \"mergeInto\");\n  const visited = /* @__PURE__ */ new Set();\n  const dfs = /* @__PURE__ */ __name((node) => {\n    const idx = indexOf.get(node);\n    visited.add(node);\n    const base = idx == null ? void 0 : ownCounts[idx];\n    const accumulator = base ? new Map(base) : /* @__PURE__ */ new Map();\n    const childList = tree.children.get(node) ?? [];\n    for (const child of childList) {\n      const childMap = dfs(child);\n      const parentLayer = rankOf[node];\n      if (parentLayer != null) {\n        let map = crossCounts.get(node);\n        if (!map) {\n          map = /* @__PURE__ */ new Map();\n          crossCounts.set(node, map);\n        }\n        let value = childMap.get(parentLayer) ?? 0;\n        const childLayer = rankOf[child];\n        if (childLayer != null && childLayer > parentLayer) {\n          value += 1;\n        }\n        map.set(child, value);\n      }\n      mergeInto(accumulator, childMap);\n    }\n    return accumulator;\n  }, \"dfs\");\n  for (const root of tree.roots) {\n    if (!visited.has(root)) {\n      dfs(root);\n    }\n  }\n  for (const node of nodes) {\n    if (!visited.has(node)) {\n      dfs(node);\n    }\n  }\n  return crossCounts;\n}\n__name(computeSubtreeCrossCounts, \"computeSubtreeCrossCounts\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.multitree.core.ts\nfunction annotateMinimumLayers(nodes, children, rankOf) {\n  const minLayer = /* @__PURE__ */ new Map();\n  const annotate = /* @__PURE__ */ __name((node) => {\n    let minL = rankOf[node] ?? 0;\n    const childList = [...children.get(node) ?? []];\n    childList.sort(compareByRankThenId(rankOf));\n    for (const child of childList) {\n      annotate(child);\n      const childMin = minLayer.get(child);\n      if (childMin != null) {\n        minL = Math.min(minL, childMin);\n      }\n    }\n    minLayer.set(node, minL);\n  }, \"annotate\");\n  for (const node of nodes) {\n    annotate(node);\n  }\n  return minLayer;\n}\n__name(annotateMinimumLayers, \"annotateMinimumLayers\");\nfunction compareByRankThenId(rankOf) {\n  return (a, b) => {\n    const ra = rankOf[a] ?? 0;\n    const rb = rankOf[b] ?? 0;\n    return ra === rb ? a.localeCompare(b) : ra - rb;\n  };\n}\n__name(compareByRankThenId, \"compareByRankThenId\");\nfunction emitNodesInTreeOrder(roots, allNodes, rankOf, orderChildren) {\n  let maxRank = 0;\n  for (const node of allNodes) {\n    const r = rankOf[node] ?? 0;\n    if (r > maxRank) {\n      maxRank = r;\n    }\n  }\n  const layers = Array.from({ length: maxRank + 1 }, () => []);\n  const emitted = /* @__PURE__ */ new Set();\n  const emit = /* @__PURE__ */ __name((node) => {\n    if (emitted.has(node)) {\n      return;\n    }\n    emitted.add(node);\n    const layer = rankOf[node] ?? 0;\n    if (!layers[layer]) {\n      layers[layer] = [];\n    }\n    layers[layer].push(node);\n    for (const child of orderChildren(node)) {\n      emit(child);\n    }\n  }, \"emit\");\n  for (const root of roots) {\n    emit(root);\n  }\n  for (const node of allNodes) {\n    if (!emitted.has(node)) {\n      const layer = rankOf[node] ?? 0;\n      if (!layers[layer]) {\n        layers[layer] = [];\n      }\n      layers[layer].push(node);\n      emitted.add(node);\n    }\n  }\n  return layers;\n}\n__name(emitNodesInTreeOrder, \"emitNodesInTreeOrder\");\nfunction deduplicateLayers(layers) {\n  const result = [];\n  for (const layer of layers) {\n    const seen = /* @__PURE__ */ new Set();\n    const deduped = [];\n    for (const id of layer) {\n      if (seen.has(id)) {\n        continue;\n      }\n      seen.add(id);\n      deduped.push(id);\n    }\n    result.push(deduped);\n  }\n  return result;\n}\n__name(deduplicateLayers, \"deduplicateLayers\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.multitree.order.ts\nfunction createChildOrderer(children, rankOf, crossCounts, minLayer) {\n  return (node) => {\n    const raw = children.get(node) ?? [];\n    if (raw.length === 0) {\n      return [];\n    }\n    const layer = rankOf[node] ?? 0;\n    const future = [];\n    const present = [];\n    const crossMap = crossCounts.get(node);\n    for (const child of raw) {\n      const minL = minLayer.get(child) ?? layer;\n      if (minL > layer) {\n        future.push({ child, min: minL });\n      } else {\n        present.push(child);\n      }\n    }\n    future.sort((a, b) => {\n      if (a.min === b.min) {\n        return a.child.localeCompare(b.child);\n      }\n      return a.min - b.min;\n    });\n    present.sort((a, b) => {\n      const ca = crossMap?.get(a) ?? 0;\n      const cb = crossMap?.get(b) ?? 0;\n      if (ca !== cb) {\n        return ca - cb;\n      }\n      const ma = minLayer.get(a) ?? layer;\n      const mb = minLayer.get(b) ?? layer;\n      if (ma !== mb) {\n        return ma - mb;\n      }\n      return a.localeCompare(b);\n    });\n    return [...future.map((item) => item.child), ...present];\n  };\n}\n__name(createChildOrderer, \"createChildOrderer\");\nfunction buildMultitreeLayerOrder(g, rankOf, laneOf) {\n  const tree = buildDrivingTree(g, {\n    rankHint: rankOf,\n    laneOf\n  });\n  const { children, roots } = tree;\n  for (const node of g.nodes) {\n    if (!children.has(node)) {\n      children.set(node, []);\n    }\n  }\n  const crossCounts = computeSubtreeCrossCounts(g, rankOf, tree);\n  const rootsSorted = [...roots].sort(compareByRankThenId(rankOf));\n  const minLayer = annotateMinimumLayers(rootsSorted, children, rankOf);\n  const orderChildren = createChildOrderer(children, rankOf, crossCounts, minLayer);\n  let layers = emitNodesInTreeOrder(rootsSorted, g.nodes, rankOf, orderChildren);\n  layers = deduplicateLayers(layers);\n  return layers;\n}\n__name(buildMultitreeLayerOrder, \"buildMultitreeLayerOrder\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.crossOptimization.ts\nfunction countCrossingsBetweenAdjacent(upper, lower, edges) {\n  const upperSet = new Set(upper);\n  const lowerSet = new Set(lower);\n  const li = buildLayerIndex(lower);\n  const vs = [];\n  for (const e of edges) {\n    if (upperSet.has(e.src) && lowerSet.has(e.dst)) {\n      vs.push(li.get(e.dst));\n    }\n  }\n  return countInversions(vs);\n}\n__name(countCrossingsBetweenAdjacent, \"countCrossingsBetweenAdjacent\");\nfunction totalCrossings(layers, edges, rankOf) {\n  const expanded = [];\n  for (const e of edges) {\n    const ru = rankOf[e.src];\n    const rv = rankOf[e.dst];\n    if (ru == null || rv == null || ru === rv) {\n      continue;\n    }\n    let upper = e.src;\n    let lower = e.dst;\n    let rUpper = ru;\n    let rLower = rv;\n    if (ru > rv) {\n      upper = e.dst;\n      lower = e.src;\n      rUpper = rv;\n      rLower = ru;\n    }\n    for (let L = rUpper; L < rLower; L++) {\n      expanded.push({ id: `${e.id}@${L}`, src: upper, dst: lower, ref: e.ref });\n    }\n  }\n  let sum = 0;\n  for (let i = 0; i + 1 < layers.length; i++) {\n    sum += countCrossingsBetweenAdjacent(layers[i], layers[i + 1], expanded);\n  }\n  return sum;\n}\n__name(totalCrossings, \"totalCrossings\");\nfunction optimizeRanksByCrossings(g, initialRank) {\n  const rankOf = { ...initialRank };\n  const { preds } = buildPredecessorSuccessorMaps(g);\n  const laneOf = createTopLaneResolver(g);\n  const layers = buildMultitreeLayerOrder(g, rankOf, laneOf);\n  let best = totalCrossings(layers, g.edges, rankOf);\n  const maxPasses = LAYERING.MAX_CROSSING_OPTIMIZATION_PASSES;\n  for (let pass = 0; pass < maxPasses; pass++) {\n    let changed = false;\n    const nodesByRank = [...g.nodes].sort((a, b) => (rankOf[b] ?? 0) - (rankOf[a] ?? 0));\n    for (const v of nodesByRank) {\n      const r = rankOf[v] ?? 0;\n      if (r === 0) {\n        continue;\n      }\n      let lb = 0;\n      for (const u of preds.get(v) ?? []) {\n        lb = Math.max(lb, (rankOf[u] ?? 0) + 1);\n      }\n      if (lb >= r) {\n        continue;\n      }\n      const old = r;\n      rankOf[v] = lb;\n      const trialLayers = buildMultitreeLayerOrder(g, rankOf, laneOf);\n      const score = totalCrossings(trialLayers, g.edges, rankOf);\n      if (score < best) {\n        best = score;\n        changed = true;\n      } else {\n        rankOf[v] = old;\n      }\n    }\n    if (!changed) {\n      break;\n    }\n  }\n  return rankOf;\n}\n__name(optimizeRanksByCrossings, \"optimizeRanksByCrossings\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.crossLaneAdjust.ts\nfunction adjustCrossLaneSources(g, rankOf) {\n  const topLaneOf = createTopLaneResolver(g);\n  const nodesByRank = [...g.nodes].sort(\n    (a, b) => (rankOf[a] ?? 0) - (rankOf[b] ?? 0) || a.localeCompare(b)\n  );\n  for (const v of nodesByRank) {\n    const laneV = topLaneOf(v);\n    if (!laneV) {\n      continue;\n    }\n    const outEdges = g.edges.filter((e) => e.src === v);\n    if (outEdges.length === 0) {\n      continue;\n    }\n    let hasSameLaneSucc = false;\n    let crossLaneCount = 0;\n    for (const e of outEdges) {\n      const laneDst = topLaneOf(e.dst);\n      if (laneDst == null || laneDst === laneV) {\n        hasSameLaneSucc = true;\n      } else {\n        crossLaneCount++;\n      }\n    }\n    if (crossLaneCount === 0 || hasSameLaneSucc) {\n      continue;\n    }\n    let crossLaneIncoming = 0;\n    let hasSameLanePred = false;\n    for (const e of g.edges) {\n      if (e.dst !== v) {\n        continue;\n      }\n      const laneSrc = topLaneOf(e.src);\n      if (!laneSrc) {\n        continue;\n      }\n      if (laneSrc === laneV) {\n        hasSameLanePred = true;\n      } else {\n        crossLaneIncoming++;\n      }\n    }\n    if (crossLaneIncoming > 0 || !hasSameLanePred) {\n      continue;\n    }\n    const current = rankOf[v] ?? 0;\n    const target = current + crossLaneCount;\n    let lb = 0;\n    for (const e of g.edges) {\n      if (e.dst === v) {\n        lb = Math.max(lb, (rankOf[e.src] ?? 0) + 1);\n      }\n    }\n    const newRank = Math.max(current, lb, target);\n    if (newRank !== current) {\n      rankOf[v] = newRank;\n    }\n  }\n}\n__name(adjustCrossLaneSources, \"adjustCrossLaneSources\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.longestPath.ts\nfunction assignLayers_LongestPath(gAcyclic, opts) {\n  const g = normalizeGraph(gAcyclic);\n  const order = topoSortIfAcyclic(g) ?? [...g.nodes].sort();\n  const compact = opts?.compactSingleInput ?? false;\n  const topLaneOf = createTopLaneResolver(g);\n  let rankOf = /* @__PURE__ */ Object.create(null);\n  for (const v of order) {\n    const incAll = incoming(g, v);\n    const inc = opts?.ignoreCrossLaneEdges ? incAll.filter((e) => {\n      const laneSrc = topLaneOf(e.src);\n      const laneDst = topLaneOf(v);\n      if (!laneSrc || !laneDst) {\n        return true;\n      }\n      return laneSrc === laneDst;\n    }) : incAll;\n    if (inc.length === 0) {\n      rankOf[v] = 0;\n    } else if (compact && inc.length === 1) {\n      const u = inc[0].src;\n      const laneU = topLaneOf(u);\n      const laneV = topLaneOf(v);\n      if (laneU !== laneV) {\n        rankOf[v] = rankOf[u] ?? 0;\n      } else {\n        rankOf[v] = (rankOf[u] ?? 0) + 1;\n      }\n    } else {\n      let mx = -Infinity;\n      for (const e of inc) {\n        mx = Math.max(mx, (rankOf[e.src] ?? 0) + 1);\n      }\n      rankOf[v] = mx === -Infinity ? 0 : mx;\n    }\n  }\n  if (opts?.optimizeRanksByCrossings ?? false) {\n    rankOf = optimizeRanksByCrossings(g, rankOf);\n  }\n  if (opts?.ignoreCrossLaneEdges) {\n    adjustCrossLaneSources(g, rankOf);\n  }\n  const layers = buildMultitreeLayerOrder(g, rankOf, topLaneOf);\n  return { layers, rankOf, dummy: /* @__PURE__ */ new Set() };\n}\n__name(assignLayers_LongestPath, \"assignLayers_LongestPath\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.gravity.ts\nfunction assignLayers_Gravity(gAcyclic, opts) {\n  const g = normalizeGraph(gAcyclic);\n  const base = assignLayers_LongestPath(g, {\n    compactSingleInput: opts?.compactSingleInput,\n    ignoreCrossLaneEdges: opts?.ignoreCrossLaneEdges,\n    optimizeRanksByCrossings: opts?.optimizeRanksByCrossings\n  });\n  const rankOf = { ...base.rankOf };\n  const topLaneOf = createTopLaneResolver(g);\n  const { preds, succs } = buildPredecessorSuccessorMaps(g, (e) => {\n    if (opts?.ignoreCrossLaneEdges) {\n      const laneSrc = topLaneOf(e.src);\n      const laneDst = topLaneOf(e.dst);\n      if (laneSrc && laneDst && laneSrc !== laneDst) {\n        return false;\n      }\n    }\n    return true;\n  });\n  const order = topoSortIfAcyclic(g) ?? [...g.nodes];\n  const revOrder = [...order].reverse();\n  const clampFeasible = /* @__PURE__ */ __name((v, desired) => {\n    let lb = 0;\n    for (const u of preds.get(v) ?? []) {\n      lb = Math.max(lb, (rankOf[u] ?? 0) + 1);\n    }\n    let ub = Number.POSITIVE_INFINITY;\n    const s = succs.get(v) ?? [];\n    if (s.length > 0) {\n      ub = Math.min(...s.map((w) => (rankOf[w] ?? 0) - 1));\n    }\n    if (!Number.isFinite(ub)) {\n      ub = Math.max(lb, desired);\n    }\n    return Math.min(Math.max(desired, lb), ub);\n  }, \"clampFeasible\");\n  const iters = LAYERING.GRAVITY_ITERATIONS;\n  const relaxOrder = /* @__PURE__ */ __name((nodeOrder) => {\n    let changed = false;\n    for (const v of nodeOrder) {\n      const ps = preds.get(v) ?? [];\n      const ss = succs.get(v) ?? [];\n      if (ps.length === 0 && ss.length === 0) {\n        continue;\n      }\n      const predAvg = ps.length > 0 ? ps.reduce((a, u) => a + (rankOf[u] ?? 0) + 1, 0) / ps.length : rankOf[v] ?? 0;\n      const succAvg = ss.length > 0 ? ss.reduce((a, w) => a + (rankOf[w] ?? 0) - 1, 0) / ss.length : rankOf[v] ?? 0;\n      const desired = Math.round((predAvg + succAvg) / 2);\n      const clamped = clampFeasible(v, desired);\n      if (clamped !== rankOf[v]) {\n        rankOf[v] = clamped;\n        changed = true;\n      }\n    }\n    return changed;\n  }, \"relaxOrder\");\n  for (let it = 0; it < iters; it++) {\n    const forwardChanged = relaxOrder(order);\n    const backwardChanged = relaxOrder(revOrder);\n    if (!forwardChanged && !backwardChanged) {\n      break;\n    }\n  }\n  for (const v of order) {\n    let lb = 0;\n    for (const u of preds.get(v) ?? []) {\n      lb = Math.max(lb, (rankOf[u] ?? 0) + 1);\n    }\n    if ((rankOf[v] ?? 0) < lb) {\n      rankOf[v] = lb;\n    }\n  }\n  for (const v of revOrder) {\n    const s = succs.get(v) ?? [];\n    if (s.length > 0) {\n      const ub = Math.min(...s.map((w) => (rankOf[w] ?? 0) - 1));\n      if ((rankOf[v] ?? 0) > ub) {\n        rankOf[v] = ub;\n      }\n    }\n  }\n  const layers = buildLayersFromRanks(g, order, rankOf);\n  return { layers, rankOf, dummy: /* @__PURE__ */ new Set() };\n}\n__name(assignLayers_Gravity, \"assignLayers_Gravity\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.laneAwareCompact.ts\nfunction topoSortByGenerationIfAcyclic(g) {\n  const indeg = buildInDegreeMap(g);\n  const adj = buildSortedSuccessorMap(g);\n  let frontier = sortedZeroInDegreeNodes(indeg);\n  const order = [];\n  while (frontier.length > 0) {\n    const nextFrontier = [];\n    for (const u of frontier) {\n      order.push(u);\n      for (const v of adj.get(u) ?? []) {\n        indeg.set(v, (indeg.get(v) ?? 0) - 1);\n        if ((indeg.get(v) ?? 0) === 0) {\n          nextFrontier.push(v);\n        }\n      }\n    }\n    frontier = nextFrontier.sort((a, b) => a.localeCompare(b));\n  }\n  return order.length === g.nodes.length ? order : null;\n}\n__name(topoSortByGenerationIfAcyclic, \"topoSortByGenerationIfAcyclic\");\nfunction assignLayers_LaneAwareCompact(gAcyclic, opts) {\n  const g = normalizeGraph(gAcyclic);\n  const order = opts?.direction === \"LR\" ? topoSortByGenerationIfAcyclic(g) ?? [...g.nodes].sort() : topoSortIfAcyclic(g) ?? [...g.nodes].sort();\n  const topLaneOf = createTopLaneResolver(g);\n  const laneOf = /* @__PURE__ */ __name((id) => topLaneOf(id) ?? id, \"laneOf\");\n  const rankOf = /* @__PURE__ */ Object.create(null);\n  const nextFree = /* @__PURE__ */ new Map();\n  const edgeWeight = /* @__PURE__ */ __name((u, v) => {\n    const ignoreCrossLane = opts?.ignoreCrossLaneEdges ?? true;\n    if (ignoreCrossLane) {\n      return laneOf(u) === laneOf(v) ? 1 : 0;\n    }\n    return 1;\n  }, \"edgeWeight\");\n  for (const v of order) {\n    const node = g.nodeById.get(v);\n    if (node?.isGroup) {\n      continue;\n    }\n    const preds = incoming(g, v);\n    let base = 0;\n    if (preds.length > 0) {\n      for (const e of preds) {\n        const u = e.src;\n        const ru = rankOf[u] ?? 0;\n        base = Math.max(base, ru + edgeWeight(u, v));\n      }\n    }\n    const lane = laneOf(v);\n    const nf = nextFree.get(lane) ?? 0;\n    const L = Math.max(base, nf);\n    rankOf[v] = L;\n    nextFree.set(lane, L + 1);\n  }\n  const layers = buildLayersFromRanks(g, order, rankOf, { skipGroups: true });\n  return { layers, rankOf, dummy: /* @__PURE__ */ new Set() };\n}\n__name(assignLayers_LaneAwareCompact, \"assignLayers_LaneAwareCompact\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase2.dummies.ts\nfunction makeProperLayering(layering, gAcyclic) {\n  const g = normalizeGraph(gAcyclic);\n  const { rankOf } = layering;\n  const layers = layering.layers.map((l) => [...l]);\n  const dummy = new Set(layering.dummy ? [...layering.dummy] : []);\n  let dummySeq = 0;\n  const nodeById = new Map(g.nodeById);\n  const addDummyAt = /* @__PURE__ */ __name((L) => {\n    const id = `placeholder-${dummySeq++}`;\n    const dn = { id, isGroup: false, isDummy: true, width: 0, height: 0 };\n    nodeById.set(id, dn);\n    dummy.add(id);\n    while (layers.length <= L) {\n      layers.push([]);\n    }\n    layers[L].push(id);\n    rankOf[id] = L;\n    return id;\n  }, \"addDummyAt\");\n  const edgesSorted = [...g.edges].sort(\n    (a, b) => a.id === b.id ? a.src === b.src ? a.dst.localeCompare(b.dst) : a.src.localeCompare(b.src) : a.id.localeCompare(b.id)\n  );\n  const newEdges = [];\n  for (const e of edgesSorted) {\n    const rU = rankOf[e.src] ?? 0;\n    const rV = rankOf[e.dst] ?? 0;\n    if (rV - rU <= 1) {\n      newEdges.push(e);\n      continue;\n    }\n    let prev = e.src;\n    for (let L = rU + 1, k = 0; L < rV; L++, k++) {\n      const d = addDummyAt(L);\n      newEdges.push({ id: `${e.id}#${k}`, src: prev, dst: d, weight: e.weight, ref: e.ref });\n      prev = d;\n    }\n    const lastIndex = rV - rU - 2;\n    newEdges.push({\n      id: `${e.id}#${Math.max(lastIndex + 1, 0)}`,\n      src: prev,\n      dst: e.dst,\n      weight: e.weight,\n      ref: e.ref\n    });\n  }\n  const nodes = [...g.nodes, ...[...dummy].filter((id) => !g.nodes.includes(id))];\n  const graphWithDummies = { nodes, edges: newEdges, layout: g.layout, nodeById };\n  return { layering: { layers, rankOf, dummy }, graphWithDummies };\n}\n__name(makeProperLayering, \"makeProperLayering\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase3.ordering.ts\nfunction median(values) {\n  const n = values.length;\n  if (n === 0) {\n    return Number.POSITIVE_INFINITY;\n  }\n  const a = [...values].sort((x, y) => x - y);\n  if (n % 2 === 1) {\n    return a[(n - 1) / 2];\n  }\n  return 0.5 * (a[n / 2 - 1] + a[n / 2]);\n}\n__name(median, \"median\");\nfunction barycenter(values) {\n  if (values.length === 0) {\n    return Number.POSITIVE_INFINITY;\n  }\n  const s = values.reduce((acc, v) => acc + v, 0);\n  return s / values.length;\n}\n__name(barycenter, \"barycenter\");\nfunction neighborPositionsFor(targetNodes, fixedIndex, edges, direction) {\n  const neighborPositions = /* @__PURE__ */ new Map();\n  for (const v of targetNodes) {\n    neighborPositions.set(v, []);\n  }\n  for (const e of edges) {\n    if (direction === \"down\") {\n      if (fixedIndex.has(e.src) && neighborPositions.has(e.dst)) {\n        neighborPositions.get(e.dst).push(fixedIndex.get(e.src));\n      }\n    } else if (fixedIndex.has(e.dst) && neighborPositions.has(e.src)) {\n      neighborPositions.get(e.src).push(fixedIndex.get(e.dst));\n    }\n  }\n  return neighborPositions;\n}\n__name(neighborPositionsFor, \"neighborPositionsFor\");\nfunction currentOrderTieBreak(a, b, currentLayerIndex) {\n  const ia = currentLayerIndex.get(a) ?? 0;\n  const ib = currentLayerIndex.get(b) ?? 0;\n  return ia !== ib ? ia - ib : a.localeCompare(b);\n}\n__name(currentOrderTieBreak, \"currentOrderTieBreak\");\nfunction countCrossingsBetweenAdjacent2(upper, lower, edges) {\n  const upperSet = new Set(upper);\n  const lowerSet = new Set(lower);\n  const upperIndex = buildLayerIndex(upper);\n  const lowerIndex = buildLayerIndex(lower);\n  const pairs = [];\n  for (const e of edges) {\n    if (upperSet.has(e.src) && lowerSet.has(e.dst)) {\n      pairs.push({ u: upperIndex.get(e.src), v: lowerIndex.get(e.dst) });\n    }\n  }\n  pairs.sort((a, b) => a.u === b.u ? a.v - b.v : a.u - b.u);\n  const vs = pairs.map((p) => p.v);\n  return countInversions(vs);\n}\n__name(countCrossingsBetweenAdjacent2, \"countCrossingsBetweenAdjacent\");\nfunction sortByHeuristic(nodes, neighborPositions, currentLayerIndex) {\n  return [...nodes].sort((a, b) => {\n    const sa = median(neighborPositions.get(a) ?? []);\n    const sb = median(neighborPositions.get(b) ?? []);\n    if (sa === sb) {\n      return currentOrderTieBreak(a, b, currentLayerIndex);\n    }\n    if (!isFinite(sa)) {\n      return 1;\n    }\n    if (!isFinite(sb)) {\n      return -1;\n    }\n    return sa - sb;\n  });\n}\n__name(sortByHeuristic, \"sortByHeuristic\");\nfunction reorderLayer(fixedLayer, targetLayer, edges, direction, topLaneOf, laneOrder) {\n  const fixedIndex = buildLayerIndex(fixedLayer);\n  const currIndex = buildLayerIndex(targetLayer);\n  const neighborPositions = neighborPositionsFor(targetLayer, fixedIndex, edges, direction);\n  if (!topLaneOf || !laneOrder || laneOrder.length === 0) {\n    return sortByHeuristic(targetLayer, neighborPositions, currIndex);\n  }\n  const byLane = /* @__PURE__ */ new Map();\n  for (const id of targetLayer) {\n    const lane = topLaneOf(id);\n    const arr = byLane.get(lane) ?? [];\n    arr.push(id);\n    byLane.set(lane, arr);\n  }\n  const result = [];\n  for (const lane of laneOrder) {\n    const nodesInLane = byLane.get(lane);\n    if (!nodesInLane || nodesInLane.length === 0) {\n      continue;\n    }\n    const sorted = sortByHeuristic(nodesInLane, neighborPositions, currIndex);\n    result.push(...sorted);\n  }\n  const nullNodes = byLane.get(null);\n  if (nullNodes && nullNodes.length > 0) {\n    const sorted = sortByHeuristic(nullNodes, neighborPositions, currIndex);\n    for (const nid of sorted) {\n      const bc = barycenter(neighborPositions.get(nid) ?? []);\n      let bestIdx = result.length;\n      if (isFinite(bc)) {\n        for (const [i, rid] of result.entries()) {\n          const rBc = barycenter(neighborPositions.get(rid) ?? []);\n          if (bc < rBc) {\n            bestIdx = i;\n            break;\n          }\n        }\n      }\n      result.splice(bestIdx, 0, nid);\n    }\n  }\n  return result;\n}\n__name(reorderLayer, \"reorderLayer\");\nfunction transposeImprove(upper, current, edges, next, topLaneOf) {\n  const best = [...current];\n  const upperSet = new Set(upper);\n  const layerSet = new Set(current);\n  const nextSet = next ? new Set(next) : null;\n  const edgesIn = edges.filter((e) => upperSet.has(e.src) && layerSet.has(e.dst));\n  const edgesOut = nextSet ? edges.filter((e) => layerSet.has(e.src) && nextSet.has(e.dst)) : void 0;\n  const crossingScore = /* @__PURE__ */ __name((order) => {\n    let score = countCrossingsBetweenAdjacent2(upper, order, edgesIn);\n    if (edgesOut && next) {\n      score += countCrossingsBetweenAdjacent2(order, next, edgesOut);\n    }\n    return score;\n  }, \"crossingScore\");\n  const laneOf = topLaneOf ? /* @__PURE__ */ new Map() : null;\n  if (topLaneOf && laneOf) {\n    for (const id of current) {\n      laneOf.set(id, topLaneOf(id));\n    }\n  }\n  let improved = true;\n  let bestScore = crossingScore(best);\n  while (improved) {\n    improved = false;\n    for (let i = 0; i + 1 < best.length; i++) {\n      if (laneOf) {\n        const laneA = laneOf.get(best[i]);\n        const laneB = laneOf.get(best[i + 1]);\n        if (laneA !== laneB) {\n          continue;\n        }\n      }\n      const prev = bestScore;\n      [best[i], best[i + 1]] = [best[i + 1], best[i]];\n      const nextScore = crossingScore(best);\n      if (nextScore < prev) {\n        bestScore = nextScore;\n        improved = true;\n      } else {\n        [best[i], best[i + 1]] = [best[i + 1], best[i]];\n      }\n    }\n  }\n  return best;\n}\n__name(transposeImprove, \"transposeImprove\");\nfunction orderLayers(layering, gWithDummies, opts) {\n  const layers = layering.layers.map((l) => [...l]);\n  const edges = gWithDummies.edges;\n  const topLaneOf = createTopLaneResolver(gWithDummies);\n  const laneOrder = resolveTopLaneOrder(gWithDummies, opts?.laneOrder);\n  for (let s = 0; s < 3; s++) {\n    for (let i = 1; i < layers.length; i++) {\n      layers[i] = reorderLayer(layers[i - 1], layers[i], edges, \"down\", topLaneOf, laneOrder);\n      layers[i] = transposeImprove(layers[i - 1], layers[i], edges, layers[i + 1], topLaneOf);\n    }\n    for (let i = layers.length - 2; i >= 0; i--) {\n      layers[i] = reorderLayer(layers[i + 1], layers[i], edges, \"up\", topLaneOf, laneOrder);\n      layers[i] = transposeImprove(layers[i + 1], layers[i], edges, layers[i - 1], topLaneOf);\n    }\n  }\n  return { layers };\n}\n__name(orderLayers, \"orderLayers\");\n\n// src/rendering-util/layout-algorithms/swimlanes/phase4.coordinates.ts\nfunction assignCoordinates(ordered, gWithDummies, opts) {\n  const layerGap = opts?.layerGap ?? COORDINATES.DEFAULT_LAYER_GAP;\n  const nodeGap = opts?.nodeGap ?? COORDINATES.DEFAULT_NODE_GAP;\n  const laneGap = opts?.laneGap ?? nodeGap * 2;\n  const direction = opts?.direction ?? \"TB\";\n  const isHorizontal = direction === \"LR\" || direction === \"RL\";\n  const layers = ordered.layers;\n  const x = /* @__PURE__ */ Object.create(null);\n  const y = /* @__PURE__ */ Object.create(null);\n  const getNode = /* @__PURE__ */ __name((id) => gWithDummies.nodeById.get(id), \"getNode\");\n  const getWidth = /* @__PURE__ */ __name((id) => getNode(id)?.width ?? 0, \"getWidth\");\n  const getHeight = /* @__PURE__ */ __name((id) => getNode(id)?.height ?? 0, \"getHeight\");\n  const topLaneOf = createTopLaneResolver(gWithDummies);\n  const laneOrderGlobal = resolveTopLaneOrder(gWithDummies, opts?.laneOrder);\n  const layerHeights = layers.map(\n    (layer) => layer.reduce((m, v) => Math.max(m, getHeight(v)), 0)\n  );\n  const extraLayerGaps = [];\n  if (isHorizontal) {\n    for (let i = 0; i + 1 < layers.length; i++) {\n      const thisLayerMaxWidth = layers[i].reduce((m, v) => Math.max(m, getWidth(v)), 0);\n      const nextLayerMaxWidth = layers[i + 1].reduce((m, v) => Math.max(m, getWidth(v)), 0);\n      const thisLayerMaxHeight = layerHeights[i];\n      const nextLayerMaxHeight = layerHeights[i + 1];\n      const normalSpacing = thisLayerMaxHeight / 2 + nextLayerMaxHeight / 2;\n      const requiredSpacing = (thisLayerMaxWidth + nextLayerMaxWidth) / 2;\n      const extraNeeded = Math.max(0, requiredSpacing - normalSpacing - layerGap);\n      extraLayerGaps.push(extraNeeded);\n    }\n  }\n  const lanesUsedSet = /* @__PURE__ */ new Set();\n  for (const layer of layers) {\n    for (const id of layer) {\n      lanesUsedSet.add(topLaneOf(id));\n    }\n  }\n  const hasNullLane = lanesUsedSet.has(null);\n  const lanesUsed = laneOrderGlobal.filter((L) => lanesUsedSet.has(L));\n  const laneOrderColumns = [...hasNullLane ? [null] : [], ...lanesUsed];\n  const laneWidth = /* @__PURE__ */ Object.create(null);\n  for (const L of lanesUsed) {\n    laneWidth[L] = 0;\n  }\n  if (hasNullLane) {\n    laneWidth.null = 0;\n  }\n  for (const layer of layers) {\n    const perLane = /* @__PURE__ */ Object.create(null);\n    const nullIds = [];\n    for (const id of layer) {\n      const L = topLaneOf(id);\n      if (L === null) {\n        nullIds.push(id);\n      } else {\n        (perLane[L] ||= []).push(id);\n      }\n    }\n    for (const [L, ids] of Object.entries(perLane)) {\n      const total = ids.reduce((s, id) => s + getWidth(id), 0) + nodeGap * Math.max(0, ids.length - 1);\n      laneWidth[L] = Math.max(laneWidth[L] ?? 0, total);\n    }\n    if (hasNullLane && nullIds.length) {\n      const totalNull = nullIds.reduce((s, id) => s + getWidth(id), 0) + nodeGap * Math.max(0, nullIds.length - 1);\n      laneWidth.null = Math.max(laneWidth.null ?? 0, totalNull);\n    }\n  }\n  const centerX = /* @__PURE__ */ new Map();\n  {\n    const widths = laneOrderColumns.map(\n      (L) => (L === null ? laneWidth.null : laneWidth[L]) ?? 0\n    );\n    const totalW = widths.reduce((a, b) => a + b, 0) + laneGap * Math.max(0, laneOrderColumns.length - 1);\n    let cursor = -totalW / 2;\n    for (let i = 0; i < laneOrderColumns.length; i++) {\n      const L = laneOrderColumns[i];\n      const w = widths[i] ?? 0;\n      const cx = cursor + w / 2;\n      centerX.set(L, cx);\n      cursor += w;\n      if (i < laneOrderColumns.length - 1) {\n        cursor += laneGap;\n      }\n    }\n  }\n  let yOffset = 0;\n  for (const [li, layer] of layers.entries()) {\n    const layerH = layerHeights[li] ?? 0;\n    const byLane = /* @__PURE__ */ new Map();\n    for (const id of layer) {\n      const laneId = topLaneOf(id);\n      const arr = byLane.get(laneId) ?? [];\n      arr.push(id);\n      byLane.set(laneId, arr);\n    }\n    for (const L of laneOrderColumns) {\n      const nodesInLane = byLane.get(L) ?? [];\n      if (nodesInLane.length === 0) {\n        continue;\n      }\n      const cx = centerX.get(L);\n      if (nodesInLane.length === 1) {\n        const id = nodesInLane[0];\n        x[id] = cx;\n        y[id] = yOffset + layerH / 2;\n      } else {\n        const widths = nodesInLane.map((id) => getWidth(id));\n        const total = widths.reduce((a, b) => a + b, 0) + nodeGap * (nodesInLane.length - 1);\n        let start = cx - total / 2;\n        for (const [i, id] of nodesInLane.entries()) {\n          const w = widths[i];\n          x[id] = start + w / 2;\n          y[id] = yOffset + layerH / 2;\n          start += w + nodeGap;\n        }\n      }\n    }\n    const extraGap = extraLayerGaps[li] ?? 0;\n    yOffset += layerH + layerGap + extraGap;\n  }\n  const byRef = /* @__PURE__ */ new Map();\n  for (const e of gWithDummies.edges) {\n    const rid = e.ref.id;\n    if (!byRef.has(rid)) {\n      byRef.set(rid, []);\n    }\n    byRef.get(rid).push(e);\n  }\n  for (const [, chainEdges] of byRef) {\n    if (chainEdges.length === 0) {\n      continue;\n    }\n    const ref = chainEdges[0].ref;\n    const src = ref.start;\n    const dst = ref.end;\n    if (src == null || dst == null) {\n      continue;\n    }\n    const midX = Math.round(((x[src] ?? 0) + (x[dst] ?? 0)) / 2);\n    const involved = /* @__PURE__ */ new Set();\n    for (const e of chainEdges) {\n      involved.add(e.src);\n      involved.add(e.dst);\n    }\n    for (const vid of involved) {\n      if (vid === src || vid === dst) {\n        continue;\n      }\n      const node = gWithDummies.nodeById.get(vid);\n      if (node?.isDummy) {\n        x[vid] = midX;\n      }\n    }\n  }\n  return { x, y };\n}\n__name(assignCoordinates, \"assignCoordinates\");\n\n// src/rendering-util/layout-algorithms/swimlanes/laneOrdering.ts\nvar AUTOMATIC_LANE_ORDERING_RESTARTS = 8;\nfunction hashString(input) {\n  let hash = 2166136261;\n  for (let i = 0; i < input.length; i++) {\n    hash ^= input.charCodeAt(i);\n    hash = Math.imul(hash, 16777619);\n  }\n  return hash >>> 0;\n}\n__name(hashString, \"hashString\");\nfunction mulberry32(seed) {\n  let state = seed >>> 0;\n  return () => {\n    state += 1831565813;\n    let t = state;\n    t = Math.imul(t ^ t >>> 15, t | 1);\n    t ^= t + Math.imul(t ^ t >>> 7, t | 61);\n    return ((t ^ t >>> 14) >>> 0) / 4294967296;\n  };\n}\n__name(mulberry32, \"mulberry32\");\nfunction deterministicShuffle(order, seed) {\n  const shuffled = [...order];\n  const random = mulberry32(seed);\n  for (let i = shuffled.length - 1; i > 0; i--) {\n    const j = Math.floor(random() * (i + 1));\n    [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];\n  }\n  return shuffled;\n}\n__name(deterministicShuffle, \"deterministicShuffle\");\nfunction sourceDistance(order, sourceIndex) {\n  let distance = 0;\n  for (const [index, laneId] of order.entries()) {\n    distance += Math.abs(index - (sourceIndex.get(laneId) ?? index));\n  }\n  return distance;\n}\n__name(sourceDistance, \"sourceDistance\");\nfunction laneArrangementCost(order, weights) {\n  const position = /* @__PURE__ */ new Map();\n  for (const [index, laneId] of order.entries()) {\n    position.set(laneId, index);\n  }\n  let cost = 0;\n  for (const { a, b, weight } of weights) {\n    const ai = position.get(a);\n    const bi = position.get(b);\n    if (ai == null || bi == null) {\n      continue;\n    }\n    cost += weight * Math.abs(ai - bi);\n  }\n  return cost;\n}\n__name(laneArrangementCost, \"laneArrangementCost\");\nfunction buildWeightedLaneEdges(g) {\n  const sourceOrder = buildTopLaneOrder(g);\n  if (sourceOrder.length < 2) {\n    return [];\n  }\n  const sourceIndex = new Map(sourceOrder.map((laneId, index) => [laneId, index]));\n  const topLaneOf = createTopLaneResolver(g);\n  const weights = /* @__PURE__ */ new Map();\n  for (const edge of g.layout.edges ?? []) {\n    if (edge.isLayoutOnly) {\n      continue;\n    }\n    const src = typeof edge.start === \"string\" ? edge.start : void 0;\n    const dst = typeof edge.end === \"string\" ? edge.end : void 0;\n    if (!src || !dst || !g.nodeById.has(src) || !g.nodeById.has(dst)) {\n      continue;\n    }\n    const laneA = topLaneOf(src);\n    const laneB = topLaneOf(dst);\n    if (!laneA || !laneB || laneA === laneB) {\n      continue;\n    }\n    const ia = sourceIndex.get(laneA);\n    const ib = sourceIndex.get(laneB);\n    if (ia == null || ib == null) {\n      continue;\n    }\n    const [a, b] = ia <= ib ? [laneA, laneB] : [laneB, laneA];\n    const key = `${a}\\0${b}`;\n    const existing = weights.get(key);\n    if (existing) {\n      existing.weight++;\n    } else {\n      weights.set(key, { a, b, weight: 1 });\n    }\n  }\n  return [...weights.values()];\n}\n__name(buildWeightedLaneEdges, \"buildWeightedLaneEdges\");\nfunction greedySwitch(startOrder, weights, sourceIndex) {\n  const order = [...startOrder];\n  let cost = laneArrangementCost(order, weights);\n  let changed = true;\n  let sweeps = 0;\n  const maxSweeps = Math.max(1, order.length);\n  while (changed && sweeps < maxSweeps) {\n    changed = false;\n    sweeps++;\n    for (let i = 0; i + 1 < order.length; i++) {\n      [order[i], order[i + 1]] = [order[i + 1], order[i]];\n      const nextCost = laneArrangementCost(order, weights);\n      if (nextCost < cost) {\n        cost = nextCost;\n        changed = true;\n      } else {\n        [order[i], order[i + 1]] = [order[i + 1], order[i]];\n      }\n    }\n  }\n  return {\n    order,\n    cost,\n    sourceDistance: sourceDistance(order, sourceIndex)\n  };\n}\n__name(greedySwitch, \"greedySwitch\");\nfunction isBetterCandidate(candidate, best) {\n  if (candidate.cost !== best.cost) {\n    return candidate.cost < best.cost;\n  }\n  return candidate.sourceDistance < best.sourceDistance;\n}\n__name(isBetterCandidate, \"isBetterCandidate\");\nfunction seedForRestart(sourceOrder, weights, restartIndex) {\n  const weightSignature = [...weights].sort((a, b) => a.a === b.a ? a.b.localeCompare(b.b) : a.a.localeCompare(b.a)).map(({ a, b, weight }) => `${a}:${b}:${weight}`).join(\"|\");\n  return hashString(`${sourceOrder.join(\"|\")}#${weightSignature}#${restartIndex}`);\n}\n__name(seedForRestart, \"seedForRestart\");\nfunction optimizeTopLaneOrder(g, opts = {}) {\n  const sourceOrder = buildTopLaneOrder(g);\n  if (sourceOrder.length < 2) {\n    return sourceOrder;\n  }\n  const weights = buildWeightedLaneEdges(g);\n  if (weights.length === 0) {\n    return sourceOrder;\n  }\n  const sourceIndex = new Map(sourceOrder.map((laneId, index) => [laneId, index]));\n  let best = greedySwitch(sourceOrder, weights, sourceIndex);\n  const restarts = Math.max(0, opts.restarts ?? AUTOMATIC_LANE_ORDERING_RESTARTS);\n  for (let i = 0; i < restarts; i++) {\n    const seed = seedForRestart(sourceOrder, weights, i);\n    const start = deterministicShuffle(sourceOrder, seed);\n    const candidate = greedySwitch(start, weights, sourceIndex);\n    if (isBetterCandidate(candidate, best)) {\n      best = candidate;\n    }\n  }\n  return best.order;\n}\n__name(optimizeTopLaneOrder, \"optimizeTopLaneOrder\");\n\n// src/rendering-util/layout-algorithms/swimlanes/pipeline.ts\nfunction sugiyamaLayout(g, opts) {\n  const ignoreCrossLaneEdges = opts?.ignoreCrossLaneEdges ?? true;\n  const optimizeRanksByCrossings2 = opts?.optimizeRanksByCrossings ?? true;\n  const g0 = normalizeGraph(g);\n  const laneOrder = opts?.automaticLaneOrdering ? optimizeTopLaneOrder(g0, { restarts: AUTOMATIC_LANE_ORDERING_RESTARTS }) : void 0;\n  const cycleRes = removeCycles_DFS(g0);\n  const gAcyclic = cycleRes.acyclic;\n  const layering = ignoreCrossLaneEdges ? assignLayers_LaneAwareCompact(gAcyclic, {\n    compactSingleInput: opts?.compactSingleInput ?? LAYERING.DEFAULT_COMPACT_SINGLE_INPUT,\n    ignoreCrossLaneEdges: true,\n    direction: opts?.direction\n  }) : assignLayers_Gravity(gAcyclic, {\n    compactSingleInput: opts?.compactSingleInput ?? LAYERING.DEFAULT_COMPACT_SINGLE_INPUT,\n    ignoreCrossLaneEdges: false,\n    optimizeRanksByCrossings: optimizeRanksByCrossings2\n  });\n  const { layering: properLayering, graphWithDummies } = makeProperLayering(layering, gAcyclic);\n  const ordered = orderLayers(properLayering, graphWithDummies, { laneOrder });\n  const coordinates = assignCoordinates(ordered, graphWithDummies, {\n    layerGap: opts?.layerGap,\n    nodeGap: opts?.nodeGap,\n    direction: opts?.direction,\n    laneOrder\n  });\n  return {\n    acyclic: gAcyclic,\n    reversed: cycleRes.reversed,\n    layering: properLayering,\n    ordered,\n    coordinates\n  };\n}\n__name(sugiyamaLayout, \"sugiyamaLayout\");\n\n// src/rendering-util/layout-algorithms/swimlanes/orthogonalRouter/router.ts\nvar EPS7 = PRECISION.EPSILON;\nvar NODE_PADDING = 8;\nvar HORIZONTAL_PIPE_MARGIN = 15;\nvar VERTICAL_PIPE_MARGIN = 15;\nvar ROUTING_MARGIN = 25;\nvar ANCHOR_OFFSET = 20;\nvar TRACK_SPACING = 10;\nfunction chooseOrthogonalSide(node, target, fallback) {\n  const cx = node.x ?? 0;\n  const cy = node.y ?? 0;\n  const dx = target.x - cx;\n  const dy = target.y - cy;\n  const absDx = Math.abs(dx);\n  const absDy = Math.abs(dy);\n  if (absDx < EPS7 && absDy < EPS7) {\n    return fallback;\n  }\n  const verticalBias = 3;\n  if (absDy > EPS7 && absDy * verticalBias >= absDx) {\n    return dy > 0 ? \"bottom\" : \"top\";\n  }\n  if (absDx > EPS7) {\n    return dx > 0 ? \"right\" : \"left\";\n  }\n  return fallback;\n}\n__name(chooseOrthogonalSide, \"chooseOrthogonalSide\");\nfunction sharedLineEndpointCoord(line, nextLine) {\n  return Math.abs(line.to - nextLine.from) < EPS7 || Math.abs(line.to - nextLine.to) < EPS7 ? line.to : line.from;\n}\n__name(sharedLineEndpointCoord, \"sharedLineEndpointCoord\");\nfunction pointOnLine(line, along) {\n  return line.orient === \"vertical\" ? { x: line.coord, y: along } : { x: along, y: line.coord };\n}\n__name(pointOnLine, \"pointOnLine\");\nfunction routeEdgesOrthogonal(data, direction) {\n  const nodes = data.nodes ?? [];\n  const originalEdges = data.edges ?? [];\n  const edges = [];\n  for (const oe of originalEdges) {\n    if (oe.isLayoutOnly) {\n      continue;\n    }\n    edges.push({\n      ...oe,\n      __originalEdge: oe\n    });\n  }\n  const nodeById = /* @__PURE__ */ new Map();\n  const laneByNodeId = /* @__PURE__ */ new Map();\n  const pipes = [];\n  const isLR = direction === \"LR\";\n  for (const n of nodes) {\n    nodeById.set(n.id, n);\n  }\n  const topLevelGroups = nodes.filter((n) => n.isGroup && !n.parentId);\n  for (const group of topLevelGroups) {\n    const lane = { id: group.id };\n    const assignLane = /* @__PURE__ */ __name((n) => {\n      laneByNodeId.set(n.id, lane);\n      nodes.filter((child) => child.parentId === n.id).forEach(assignLane);\n    }, \"assignLane\");\n    assignLane(group);\n  }\n  const obstacles = nodes.filter((n) => !n.isGroup && !n.isEdgeLabel).map((n) => {\n    const w = n.width ?? 10;\n    const h = n.height ?? 10;\n    const x = n.x ?? 0;\n    const y = n.y ?? 0;\n    const padding = NODE_PADDING;\n    return {\n      nodeId: n.id,\n      minX: x - w / 2 - padding,\n      maxX: x + w / 2 + padding,\n      minY: y - h / 2 - padding,\n      maxY: y + h / 2 + padding,\n      // For LR: TB x becomes LR y, so visual Y extent should be based on height\n      visualXHalfExtent: isLR ? h / 2 + padding : w / 2 + padding\n    };\n  });\n  const getOrAddPipe = /* @__PURE__ */ __name((orientation, coord, spanMin, spanMax) => {\n    let pipe = pipes.find((p) => p.orientation === orientation && Math.abs(p.coord - coord) < 1);\n    if (!pipe) {\n      pipe = {\n        id: `pipe-${orientation}-${coord.toFixed(0)}`,\n        orientation,\n        coord,\n        spanMin,\n        spanMax,\n        tracks: []\n      };\n      pipes.push(pipe);\n    }\n    pipe.spanMin = Math.min(pipe.spanMin, spanMin);\n    pipe.spanMax = Math.max(pipe.spanMax, spanMax);\n    return pipe;\n  }, \"getOrAddPipe\");\n  const portForSide = /* @__PURE__ */ __name((node, side) => {\n    const w = node.width ?? 10;\n    const h = node.height ?? 10;\n    const cx = node.x ?? 0;\n    const cy = node.y ?? 0;\n    switch (side) {\n      case \"top\":\n        return { x: cx, y: cy - h / 2 };\n      case \"bottom\":\n        return { x: cx, y: cy + h / 2 };\n      case \"left\":\n        return { x: cx - w / 2, y: cy };\n      case \"right\":\n        return { x: cx + w / 2, y: cy };\n    }\n  }, \"portForSide\");\n  const getOrthogonalPort = /* @__PURE__ */ __name((node, target, isSource) => portForSide(node, chooseOrthogonalSide(node, target, isSource ? \"bottom\" : \"top\")), \"getOrthogonalPort\");\n  const allRoutedSegments = [];\n  const edgeSegmentIndices = [];\n  const straightIntraLaneEdges = /* @__PURE__ */ new Set();\n  const CROSSING_PENALTY = 1e3;\n  const crossingPenalty = /* @__PURE__ */ __name((edgeIdx, from, to) => {\n    if (allRoutedSegments.length === 0) {\n      return 0;\n    }\n    const isHorizontal = Math.abs(from.y - to.y) < EPS7;\n    const isVertical = Math.abs(from.x - to.x) < EPS7;\n    if (!isHorizontal && !isVertical) {\n      return 0;\n    }\n    let penalties = 0;\n    if (isHorizontal) {\n      const y = from.y;\n      const minX = Math.min(from.x, to.x) - EPS7;\n      const maxX = Math.max(from.x, to.x) + EPS7;\n      if (maxX <= minX) {\n        return 0;\n      }\n      for (const seg of allRoutedSegments) {\n        if (seg.edgeIndex === edgeIdx || seg.orientation !== \"vertical\") {\n          continue;\n        }\n        if (seg.pipe.coord < minX || seg.pipe.coord > maxX) {\n          continue;\n        }\n        if (seg.from - EPS7 <= y && seg.to + EPS7 >= y) {\n          penalties += CROSSING_PENALTY;\n        }\n      }\n    } else if (isVertical) {\n      const x = from.x;\n      const minY = Math.min(from.y, to.y) - EPS7;\n      const maxY = Math.max(from.y, to.y) + EPS7;\n      if (maxY <= minY) {\n        return 0;\n      }\n      for (const seg of allRoutedSegments) {\n        if (seg.edgeIndex === edgeIdx || seg.orientation !== \"horizontal\") {\n          continue;\n        }\n        if (seg.pipe.coord < minY || seg.pipe.coord > maxY) {\n          continue;\n        }\n        if (seg.from - EPS7 <= x && seg.to + EPS7 >= x) {\n          penalties += CROSSING_PENALTY;\n        }\n      }\n    }\n    return penalties;\n  }, \"crossingPenalty\");\n  const routingOrder = edges.map((edge, idx) => {\n    if (!edge.start || !edge.end) {\n      return { idx, crossLane: 0, dx: 0, dy: 0 };\n    }\n    const srcNode = nodeById.get(edge.start);\n    const dstNode = nodeById.get(edge.end);\n    const srcLane = laneByNodeId.get(edge.start);\n    const dstLane = laneByNodeId.get(edge.end);\n    const crossLane = srcLane && dstLane && srcLane.id !== dstLane.id ? 1 : 0;\n    const dx = srcNode && dstNode ? Math.abs((dstNode.x ?? 0) - (srcNode.x ?? 0)) : 0;\n    const dy = srcNode && dstNode ? Math.abs((dstNode.y ?? 0) - (srcNode.y ?? 0)) : 0;\n    return { idx, crossLane, dx, dy };\n  }).sort((a, b) => {\n    if (a.crossLane !== b.crossLane) {\n      return b.crossLane - a.crossLane;\n    }\n    const aDist = a.dx + a.dy;\n    const bDist = b.dx + b.dy;\n    if (Math.abs(aDist - bDist) > 1) {\n      return aDist - bDist;\n    }\n    return a.idx - b.idx;\n  }).map((entry) => entry.idx);\n  const isSegmentBlocked = /* @__PURE__ */ __name((p1, p2, excludeStart, excludeEnd) => {\n    const segMinX = Math.min(p1.x, p2.x);\n    const segMaxX = Math.max(p1.x, p2.x);\n    const segMinY = Math.min(p1.y, p2.y);\n    const segMaxY = Math.max(p1.y, p2.y);\n    const blockingObs = obstacles.find((obs) => {\n      if (excludeStart && obs.nodeId === excludeStart) {\n        return false;\n      }\n      if (excludeEnd && obs.nodeId === excludeEnd) {\n        return false;\n      }\n      if (Math.abs(p1.x - p2.x) > EPS7) {\n        return obs.minY < p1.y && obs.maxY > p1.y && obs.maxX > segMinX && obs.minX < segMaxX;\n      } else {\n        return obs.minX < p1.x && obs.maxX > p1.x && obs.maxY > segMinY && obs.minY < segMaxY;\n      }\n    });\n    return !!blockingObs;\n  }, \"isSegmentBlocked\");\n  const portGroups = /* @__PURE__ */ new Map();\n  const incidentEdgeTotals = /* @__PURE__ */ new Map();\n  for (const edge of edges) {\n    if (!edge.start || !edge.end || edge.start === edge.end) {\n      continue;\n    }\n    incidentEdgeTotals.set(edge.start, (incidentEdgeTotals.get(edge.start) ?? 0) + 1);\n    incidentEdgeTotals.set(edge.end, (incidentEdgeTotals.get(edge.end) ?? 0) + 1);\n  }\n  const determineSide = /* @__PURE__ */ __name((node, target) => chooseOrthogonalSide(node, target, \"bottom\"), \"determineSide\");\n  const sideInfoByIdx = /* @__PURE__ */ new Map();\n  for (const [i, e] of edges.entries()) {\n    if (!e.start || !e.end || e.start === e.end) {\n      continue;\n    }\n    if (e.points && e.points.length > 0) {\n      continue;\n    }\n    const src = nodeById.get(e.start);\n    const dst = nodeById.get(e.end);\n    if (!src || !dst) {\n      continue;\n    }\n    const dx = (dst.x ?? 0) - (src.x ?? 0);\n    const dy = (dst.y ?? 0) - (src.y ?? 0);\n    sideInfoByIdx.set(i, {\n      edgeIdx: i,\n      srcId: e.start,\n      dstId: e.end,\n      srcSide: determineSide(src, { x: dst.x ?? 0, y: dst.y ?? 0 }),\n      dstSide: determineSide(dst, { x: src.x ?? 0, y: src.y ?? 0 }),\n      absDx: Math.abs(dx),\n      absDy: Math.abs(dy),\n      dxSign: Math.sign(dx),\n      dySign: Math.sign(dy)\n    });\n  }\n  const preferenceStrength = /* @__PURE__ */ __name((info) => {\n    if (info.srcSide === \"top\" || info.srcSide === \"bottom\") {\n      return info.absDx === 0 ? Infinity : info.absDy / info.absDx;\n    }\n    return info.absDy === 0 ? Infinity : info.absDx / info.absDy;\n  }, \"preferenceStrength\");\n  const secondarySide = /* @__PURE__ */ __name((info) => {\n    if (info.srcSide === \"top\" || info.srcSide === \"bottom\") {\n      return info.dxSign >= 0 ? \"right\" : \"left\";\n    }\n    return info.dySign >= 0 ? \"bottom\" : \"top\";\n  }, \"secondarySide\");\n  const sourceSideGroups = /* @__PURE__ */ new Map();\n  for (const info of sideInfoByIdx.values()) {\n    const key = `${info.srcId}:${info.srcSide}`;\n    if (!sourceSideGroups.has(key)) {\n      sourceSideGroups.set(key, []);\n    }\n    sourceSideGroups.get(key).push(info);\n  }\n  const sideLoad = /* @__PURE__ */ new Map();\n  const loadKey = /* @__PURE__ */ __name((nodeId, side) => `${nodeId}:${side}`, \"loadKey\");\n  for (const info of sideInfoByIdx.values()) {\n    sideLoad.set(\n      loadKey(info.srcId, info.srcSide),\n      (sideLoad.get(loadKey(info.srcId, info.srcSide)) ?? 0) + 1\n    );\n    sideLoad.set(\n      loadKey(info.dstId, info.dstSide),\n      (sideLoad.get(loadKey(info.dstId, info.dstSide)) ?? 0) + 1\n    );\n  }\n  for (const group of sourceSideGroups.values()) {\n    if (group.length < 2) {\n      continue;\n    }\n    group.sort((a, b) => {\n      const sa = preferenceStrength(a);\n      const sb = preferenceStrength(b);\n      if (Math.abs(sa - sb) > 1e-9) {\n        return sb - sa;\n      }\n      return a.edgeIdx - b.edgeIdx;\n    });\n    for (let g = 1; g < group.length; g++) {\n      const info = group[g];\n      const secondary = secondarySide(info);\n      const primaryLoad = sideLoad.get(loadKey(info.srcId, info.srcSide)) ?? 0;\n      const secondaryLoad = sideLoad.get(loadKey(info.srcId, secondary)) ?? 0;\n      if (secondaryLoad >= primaryLoad) {\n        continue;\n      }\n      sideLoad.set(loadKey(info.srcId, info.srcSide), primaryLoad - 1);\n      sideLoad.set(loadKey(info.srcId, secondary), secondaryLoad + 1);\n      info.srcSide = secondary;\n    }\n  }\n  const isDiamondNode = /* @__PURE__ */ __name((node) => {\n    const shape = node?.shape;\n    return shape === \"question\" || shape === \"diamond\";\n  }, \"isDiamondNode\");\n  const inSidesByNode = /* @__PURE__ */ new Map();\n  for (const info of sideInfoByIdx.values()) {\n    if (!inSidesByNode.has(info.dstId)) {\n      inSidesByNode.set(info.dstId, /* @__PURE__ */ new Set());\n    }\n    inSidesByNode.get(info.dstId).add(info.dstSide);\n  }\n  for (const info of sideInfoByIdx.values()) {\n    if (!isDiamondNode(nodeById.get(info.srcId))) {\n      continue;\n    }\n    const inSides = inSidesByNode.get(info.srcId);\n    if (!inSides?.has(info.srcSide)) {\n      continue;\n    }\n    const secondary = secondarySide(info);\n    if (inSides.has(secondary) || (sideLoad.get(loadKey(info.srcId, secondary)) ?? 0) > 0) {\n      continue;\n    }\n    const primaryLoad = sideLoad.get(loadKey(info.srcId, info.srcSide)) ?? 0;\n    sideLoad.set(loadKey(info.srcId, info.srcSide), Math.max(0, primaryLoad - 1));\n    sideLoad.set(loadKey(info.srcId, secondary), 1);\n    info.srcSide = secondary;\n  }\n  for (const info of sideInfoByIdx.values()) {\n    const { edgeIdx: i, srcId, dstId, srcSide, dstSide } = info;\n    const src = nodeById.get(srcId);\n    const dst = nodeById.get(dstId);\n    const srcKey = `${srcId}:${srcSide}:src`;\n    const dstCoord = srcSide === \"top\" || srcSide === \"bottom\" ? dst.x ?? 0 : dst.y ?? 0;\n    if (!portGroups.has(srcKey)) {\n      portGroups.set(srcKey, []);\n    }\n    portGroups.get(srcKey).push({ edgeIdx: i, oppositeCoord: dstCoord });\n    const dstKey = `${dstId}:${dstSide}:dst`;\n    const srcCoord = dstSide === \"top\" || dstSide === \"bottom\" ? src.x ?? 0 : src.y ?? 0;\n    if (!portGroups.has(dstKey)) {\n      portGroups.set(dstKey, []);\n    }\n    portGroups.get(dstKey).push({ edgeIdx: i, oppositeCoord: srcCoord });\n  }\n  const portOffsets = /* @__PURE__ */ new Map();\n  const MIN_PORT_SPACING3 = 8;\n  for (const [key, group] of portGroups) {\n    if (group.length < 2) {\n      continue;\n    }\n    group.sort((a, b) => a.oppositeCoord - b.oppositeCoord);\n    const parts = key.split(\":\");\n    const nodeId = parts.slice(0, -2).join(\":\");\n    const side = parts[parts.length - 2];\n    const role = parts[parts.length - 1];\n    const node = nodeById.get(nodeId);\n    if (!node) {\n      continue;\n    }\n    const isVerticalSide = side === \"left\" || side === \"right\";\n    const sideLength = isVerticalSide ? node.height ?? 10 : node.width ?? 10;\n    const shape = node.shape;\n    const isDiamond = shape === \"question\" || shape === \"diamond\";\n    const effectiveLength = isDiamond ? sideLength * 0.3 : sideLength;\n    const MAX_PORT_SPACING = 20;\n    const spacing = Math.min(\n      MAX_PORT_SPACING,\n      Math.max(MIN_PORT_SPACING3, effectiveLength / (group.length + 1))\n    );\n    const totalSpan = spacing * (group.length - 1);\n    const startOffset = -totalSpan / 2;\n    for (const [j, element] of group.entries()) {\n      const offset = startOffset + j * spacing;\n      const offsetKey = `${element.edgeIdx}:${role}`;\n      portOffsets.set(offsetKey, offset);\n    }\n  }\n  const edgeHasLabelNode = /* @__PURE__ */ __name((edgeIdx) => Boolean(edges[edgeIdx]?.labelNodeId), \"edgeHasLabelNode\");\n  const faceHasLabelNode = /* @__PURE__ */ __name((nodeId, side) => {\n    if (!nodeId) {\n      return false;\n    }\n    return (portGroups.get(`${nodeId}:${side}:src`) ?? []).some(\n      ({ edgeIdx }) => edgeHasLabelNode(edgeIdx)\n    ) || (portGroups.get(`${nodeId}:${side}:dst`) ?? []).some(\n      ({ edgeIdx }) => edgeHasLabelNode(edgeIdx)\n    );\n  }, \"faceHasLabelNode\");\n  const applyPortOffset = /* @__PURE__ */ __name((basePort, side, offset) => {\n    if (side === \"top\" || side === \"bottom\") {\n      return { x: basePort.x + offset, y: basePort.y };\n    } else {\n      return { x: basePort.x, y: basePort.y + offset };\n    }\n  }, \"applyPortOffset\");\n  const portsForEdge = /* @__PURE__ */ __name((edgeIndex, src, dst) => {\n    const sideInfo = sideInfoByIdx.get(edgeIndex);\n    const srcTarget = { x: dst.x ?? 0, y: dst.y ?? 0 };\n    const dstTarget = { x: src.x ?? 0, y: src.y ?? 0 };\n    const srcSide = sideInfo?.srcSide ?? determineSide(src, srcTarget);\n    const dstSide = sideInfo?.dstSide ?? determineSide(dst, dstTarget);\n    let pSrcPort = sideInfo ? portForSide(src, sideInfo.srcSide) : getOrthogonalPort(src, srcTarget, true);\n    let pDstPort = sideInfo ? portForSide(dst, sideInfo.dstSide) : getOrthogonalPort(dst, dstTarget, false);\n    const srcOffset = portOffsets.get(`${edgeIndex}:src`);\n    const dstOffset = portOffsets.get(`${edgeIndex}:dst`);\n    if (srcOffset !== void 0) {\n      pSrcPort = applyPortOffset(pSrcPort, srcSide, srcOffset);\n    }\n    if (dstOffset !== void 0) {\n      pDstPort = applyPortOffset(pDstPort, dstSide, dstOffset);\n    }\n    return { pSrcPort, pDstPort, srcSide, dstSide };\n  }, \"portsForEdge\");\n  for (const i of routingOrder) {\n    const e = edges[i];\n    edgeSegmentIndices[i] = [];\n    if (!e.start || !e.end) {\n      continue;\n    }\n    if (e.points && e.points.length > 0) {\n      continue;\n    }\n    if (e.start === e.end) {\n      continue;\n    }\n    const src = nodeById.get(e.start);\n    const dst = nodeById.get(e.end);\n    if (!src || !dst) {\n      continue;\n    }\n    const {\n      pSrcPort,\n      pDstPort,\n      srcSide: srcPortSide,\n      dstSide: dstPortSide\n    } = portsForEdge(i, src, dst);\n    const pSrcAnchor = { ...pSrcPort };\n    const pDstAnchor = { ...pDstPort };\n    const srcPortIsVertical = srcPortSide === \"top\" || srcPortSide === \"bottom\";\n    const dstPortIsVertical = dstPortSide === \"top\" || dstPortSide === \"bottom\";\n    if (srcPortIsVertical) {\n      const isBottom = pSrcPort.y > (src.y ?? 0);\n      pSrcAnchor.y = isBottom ? pSrcPort.y + ANCHOR_OFFSET : pSrcPort.y - ANCHOR_OFFSET;\n    } else {\n      const isRight = pSrcPort.x > (src.x ?? 0);\n      pSrcAnchor.x = isRight ? pSrcPort.x + ANCHOR_OFFSET : pSrcPort.x - ANCHOR_OFFSET;\n    }\n    if (dstPortIsVertical) {\n      const isBottom = pDstPort.y > (dst.y ?? 0);\n      pDstAnchor.y = isBottom ? pDstPort.y + ANCHOR_OFFSET : pDstPort.y - ANCHOR_OFFSET;\n    } else {\n      const isRight = pDstPort.x > (dst.x ?? 0);\n      pDstAnchor.x = isRight ? pDstPort.x + ANCHOR_OFFSET : pDstPort.x - ANCHOR_OFFSET;\n    }\n    const isPointInObstacle = /* @__PURE__ */ __name((pt, excludeNodeIds) => {\n      for (const obs of obstacles) {\n        if (excludeNodeIds.includes(obs.nodeId)) {\n          continue;\n        }\n        if (pt.x > obs.minX && pt.x < obs.maxX && pt.y > obs.minY && pt.y < obs.maxY) {\n          return { inside: true, obstacle: obs };\n        }\n      }\n      return { inside: false };\n    }, \"isPointInObstacle\");\n    const obstacleDetour = /* @__PURE__ */ __name((port, node, opposite, obs, portIsVertical) => {\n      if (portIsVertical) {\n        const leavesPositiveSide2 = port.y > (node.y ?? 0);\n        const goRight = (opposite.x ?? 0) >= port.x;\n        return {\n          x: goRight ? obs.maxX + HORIZONTAL_PIPE_MARGIN : obs.minX - HORIZONTAL_PIPE_MARGIN,\n          y: leavesPositiveSide2 ? obs.maxY + VERTICAL_PIPE_MARGIN : obs.minY - VERTICAL_PIPE_MARGIN,\n          leavesPositiveSide: leavesPositiveSide2\n        };\n      }\n      const leavesPositiveSide = port.x > (node.x ?? 0);\n      const goDown = (opposite.y ?? 0) >= port.y;\n      return {\n        x: leavesPositiveSide ? obs.maxX + HORIZONTAL_PIPE_MARGIN : obs.minX - HORIZONTAL_PIPE_MARGIN,\n        y: goDown ? obs.maxY + VERTICAL_PIPE_MARGIN : obs.minY - VERTICAL_PIPE_MARGIN,\n        leavesPositiveSide\n      };\n    }, \"obstacleDetour\");\n    let srcHandleWaypoints = [];\n    const endpointIds = [e.start, e.end];\n    const srcCheck = isPointInObstacle(pSrcAnchor, endpointIds);\n    if (srcCheck.inside && srcCheck.obstacle) {\n      const obs = srcCheck.obstacle;\n      if (srcPortIsVertical) {\n        const detour = obstacleDetour(pSrcPort, src, dst, obs, true);\n        pSrcAnchor.x = detour.x;\n        pSrcAnchor.y = detour.y;\n        const gapY = detour.leavesPositiveSide ? Math.min(obs.minY - 2, pSrcPort.y + ANCHOR_OFFSET) : Math.max(obs.maxY + 2, pSrcPort.y - ANCHOR_OFFSET);\n        srcHandleWaypoints = [\n          { x: pSrcPort.x, y: gapY },\n          // Orthogonal step in the gap\n          { x: detour.x, y: gapY },\n          // Horizontal detour\n          { x: detour.x, y: detour.y }\n          // Down past obstacle\n        ];\n      } else {\n        const detour = obstacleDetour(pSrcPort, src, dst, obs, false);\n        const gapX = detour.leavesPositiveSide ? Math.min(obs.minX - 2, pSrcPort.x + ANCHOR_OFFSET) : Math.max(obs.maxX + 2, pSrcPort.x - ANCHOR_OFFSET);\n        pSrcAnchor.x = detour.x;\n        pSrcAnchor.y = detour.y;\n        srcHandleWaypoints = [\n          { x: gapX, y: pSrcPort.y },\n          // Orthogonal step in the gap\n          { x: gapX, y: detour.y },\n          // Vertical detour\n          { x: detour.x, y: detour.y }\n          // Horizontal past obstacle\n        ];\n      }\n    }\n    let dstHandleWaypoints = [];\n    const dstCheck = isPointInObstacle(pDstAnchor, endpointIds);\n    if (dstCheck.inside && dstCheck.obstacle) {\n      const obs = dstCheck.obstacle;\n      if (dstPortIsVertical) {\n        const detour = obstacleDetour(pDstPort, dst, src, obs, true);\n        pDstAnchor.x = detour.x;\n        pDstAnchor.y = detour.y;\n        dstHandleWaypoints = [\n          { x: detour.x, y: detour.y },\n          // From anchor position\n          { x: pDstPort.x, y: detour.y }\n          // Go sideways to port's X\n          // Then orthogonally to port\n        ];\n      } else {\n        const detour = obstacleDetour(pDstPort, dst, src, obs, false);\n        pDstAnchor.x = detour.x;\n        pDstAnchor.y = detour.y;\n        dstHandleWaypoints = [\n          { x: detour.x, y: detour.y },\n          // From anchor position\n          { x: detour.x, y: pDstPort.y }\n          // Go vertically to port's Y\n        ];\n      }\n    }\n    if (srcHandleWaypoints.length === 0 && dstHandleWaypoints.length === 0) {\n      const hpMargin = HORIZONTAL_PIPE_MARGIN;\n      const anchorsSameX = Math.abs(pSrcAnchor.x - pDstAnchor.x) < hpMargin;\n      const anchorsSameY = Math.abs(pSrcAnchor.y - pDstAnchor.y) < hpMargin;\n      const hasPortOffset = portOffsets.get(`${i}:src`) !== void 0 || portOffsets.get(`${i}:dst`) !== void 0;\n      const srcFaceTotal = (portGroups.get(`${e.start ?? \"\"}:${srcPortSide}:src`)?.length ?? 0) + (portGroups.get(`${e.start ?? \"\"}:${srcPortSide}:dst`)?.length ?? 0);\n      const dstFaceTotal = (portGroups.get(`${e.end ?? \"\"}:${dstPortSide}:src`)?.length ?? 0) + (portGroups.get(`${e.end ?? \"\"}:${dstPortSide}:dst`)?.length ?? 0);\n      const faceContested = srcFaceTotal > 1 || dstFaceTotal > 1;\n      const srcIncidentTotal = incidentEdgeTotals.get(e.start ?? \"\") ?? 0;\n      const dstIncidentTotal = incidentEdgeTotals.get(e.end ?? \"\") ?? 0;\n      const contestedFaceHasLabel = srcFaceTotal > 1 && faceHasLabelNode(e.start, srcPortSide) || dstFaceTotal > 1 && faceHasLabelNode(e.end, dstPortSide);\n      const srcContestAllowsCenteredStraight = srcFaceTotal <= 1 || srcIncidentTotal <= 2;\n      const dstContestAllowsCenteredStraight = dstFaceTotal <= 1 || dstIncidentTotal <= 2;\n      const canPreserveSimpleContestedStraight = faceContested && !contestedFaceHasLabel && srcContestAllowsCenteredStraight && dstContestAllowsCenteredStraight;\n      if ((anchorsSameX || anchorsSameY) && !hasPortOffset && (!faceContested || canPreserveSimpleContestedStraight)) {\n        const directBlocked = isSegmentBlocked(pSrcPort, pDstPort, e.start, e.end);\n        if (!directBlocked) {\n          e.points = [{ ...pSrcPort }, { ...pSrcAnchor }, { ...pDstAnchor }, { ...pDstPort }];\n          straightIntraLaneEdges.add(i);\n          const fastPathOrientation = anchorsSameY ? \"horizontal\" : \"vertical\";\n          const fastPathCoord = anchorsSameY ? pSrcPort.y : pSrcPort.x;\n          const fastPathFrom = anchorsSameY ? Math.min(pSrcPort.x, pDstPort.x) : Math.min(pSrcPort.y, pDstPort.y);\n          const fastPathTo = anchorsSameY ? Math.max(pSrcPort.x, pDstPort.x) : Math.max(pSrcPort.y, pDstPort.y);\n          const fastPathPipe = {\n            id: `fast-path-${fastPathOrientation}-${fastPathCoord.toFixed(0)}-${i}`,\n            orientation: fastPathOrientation,\n            coord: fastPathCoord,\n            spanMin: fastPathFrom,\n            spanMax: fastPathTo,\n            tracks: []\n          };\n          allRoutedSegments.push({\n            edgeIndex: i,\n            segmentIndex: 0,\n            orientation: fastPathOrientation,\n            pipe: fastPathPipe,\n            trackIndex: 0,\n            from: fastPathFrom,\n            to: fastPathTo\n          });\n          continue;\n        }\n      }\n    }\n    const srcPipe = getOrAddPipe(\"vertical\", pSrcAnchor.x, pSrcAnchor.y, pSrcAnchor.y);\n    pSrcAnchor.x = srcPipe.coord;\n    const dstPipe = getOrAddPipe(\"vertical\", pDstAnchor.x, pDstAnchor.y, pDstAnchor.y);\n    pDstAnchor.x = dstPipe.coord;\n    let bbMinX = Math.min(pSrcAnchor.x, pDstAnchor.x) - 50;\n    let bbMaxX = Math.max(pSrcAnchor.x, pDstAnchor.x) + 50;\n    let bbMinY = Math.min(pSrcAnchor.y, pDstAnchor.y) - 50;\n    let bbMaxY = Math.max(pSrcAnchor.y, pDstAnchor.y) + 50;\n    for (const obs of obstacles) {\n      const pathMinX = Math.min(pSrcAnchor.x, pDstAnchor.x);\n      const pathMaxX = Math.max(pSrcAnchor.x, pDstAnchor.x);\n      const pathMinY = Math.min(pSrcAnchor.y, pDstAnchor.y);\n      const pathMaxY = Math.max(pSrcAnchor.y, pDstAnchor.y);\n      const obsBlocksPath = obs.minX < pathMaxX && obs.maxX > pathMinX && obs.minY < pathMaxY && obs.maxY > pathMinY;\n      if (obsBlocksPath) {\n        bbMinX = Math.min(bbMinX, obs.minX - ROUTING_MARGIN);\n        bbMaxX = Math.max(bbMaxX, obs.maxX + ROUTING_MARGIN);\n        bbMinY = Math.min(bbMinY, obs.minY - ROUTING_MARGIN);\n        bbMaxY = Math.max(bbMaxY, obs.maxY + ROUTING_MARGIN);\n      }\n    }\n    for (const obs of obstacles) {\n      if (obs.maxX < bbMinX || obs.minX > bbMaxX || obs.maxY < bbMinY || obs.minY > bbMaxY) {\n        continue;\n      }\n      const hMargin = HORIZONTAL_PIPE_MARGIN;\n      getOrAddPipe(\"horizontal\", obs.minY - hMargin, bbMinX, bbMaxX);\n      getOrAddPipe(\"horizontal\", obs.maxY + hMargin, bbMinX, bbMaxX);\n      const vMargin = VERTICAL_PIPE_MARGIN;\n      getOrAddPipe(\"vertical\", obs.minX - vMargin, bbMinY, bbMaxY);\n      getOrAddPipe(\"vertical\", obs.maxX + vMargin, bbMinY, bbMaxY);\n    }\n    getOrAddPipe(\"horizontal\", pSrcAnchor.y, bbMinX, bbMaxX);\n    getOrAddPipe(\"horizontal\", pDstAnchor.y, bbMinX, bbMaxX);\n    const hPipes = pipes.filter(\n      (p) => p.orientation === \"horizontal\" && p.coord >= bbMinY && p.coord <= bbMaxY\n    );\n    const vPipes = pipes.filter(\n      (p) => p.orientation === \"vertical\" && p.coord >= bbMinX && p.coord <= bbMaxX\n    );\n    const getKey = /* @__PURE__ */ __name((x, y) => `${x.toFixed(1)},${y.toFixed(1)}`, \"getKey\");\n    const startKey = getKey(pSrcAnchor.x, pSrcAnchor.y);\n    const endKey = getKey(pDstAnchor.x, pDstAnchor.y);\n    const gScore = /* @__PURE__ */ new Map();\n    const cameFrom = /* @__PURE__ */ new Map();\n    const arrivalDir = /* @__PURE__ */ new Map();\n    const openSet = /* @__PURE__ */ new Set();\n    const openList = [];\n    gScore.set(startKey, 0);\n    arrivalDir.set(startKey, \"n\");\n    openList.push({\n      key: startKey,\n      f: Math.hypot(pDstAnchor.x - pSrcAnchor.x, pDstAnchor.y - pSrcAnchor.y),\n      pt: pSrcAnchor\n    });\n    openSet.add(startKey);\n    let foundPath = [];\n    const checkSegmentBlocked = /* @__PURE__ */ __name((p1, p2) => {\n      return isSegmentBlocked(p1, p2, e.start, e.end);\n    }, \"checkSegmentBlocked\");\n    const cornerHV = { x: pDstAnchor.x, y: pSrcAnchor.y };\n    const seg1HV_blocked = checkSegmentBlocked(pSrcAnchor, cornerHV);\n    const seg2HV_blocked = checkSegmentBlocked(cornerHV, pDstAnchor);\n    const pathHV_blocked = seg1HV_blocked || seg2HV_blocked;\n    const cornerVH = { x: pSrcAnchor.x, y: pDstAnchor.y };\n    const seg1VH_blocked = checkSegmentBlocked(pSrcAnchor, cornerVH);\n    const seg2VH_blocked = checkSegmentBlocked(cornerVH, pDstAnchor);\n    const pathVH_blocked = seg1VH_blocked || seg2VH_blocked;\n    if (!pathHV_blocked) {\n      if (Math.abs(pSrcAnchor.y - pDstAnchor.y) < EPS7 || Math.abs(pSrcAnchor.x - pDstAnchor.x) < EPS7) {\n        foundPath = [pSrcAnchor, pDstAnchor];\n      } else {\n        foundPath = [pSrcAnchor, cornerHV, pDstAnchor];\n      }\n    } else if (!pathVH_blocked) {\n      if (Math.abs(pSrcAnchor.x - pDstAnchor.x) < EPS7) {\n        foundPath = [pSrcAnchor, pDstAnchor];\n      } else {\n        foundPath = [pSrcAnchor, cornerVH, pDstAnchor];\n      }\n    }\n    if (foundPath.length === 0) {\n      while (openList.length > 0) {\n        openList.sort((a, b) => a.f - b.f);\n        const current = openList.shift();\n        openSet.delete(current.key);\n        if (current.key === endKey) {\n          let currKey = endKey;\n          let currPt = pDstAnchor;\n          foundPath = [currPt];\n          while (cameFrom.has(currKey)) {\n            const prev = cameFrom.get(currKey);\n            foundPath.unshift(prev);\n            currPt = prev;\n            currKey = getKey(prev.x, prev.y);\n          }\n          break;\n        }\n        const cx = current.pt.x;\n        const cy = current.pt.y;\n        const sortedVPipes = vPipes.sort((a, b) => a.coord - b.coord);\n        const vIdx = sortedVPipes.findIndex((p) => Math.abs(p.coord - cx) < 1);\n        const hPipesSorted = hPipes.sort((a, b) => a.coord - b.coord);\n        const hIdx = hPipesSorted.findIndex((p) => Math.abs(p.coord - cy) < 1);\n        const neighbors = [];\n        if (vIdx > 0) {\n          neighbors.push({ x: sortedVPipes[vIdx - 1].coord, y: cy });\n        }\n        if (vIdx >= 0 && vIdx < sortedVPipes.length - 1) {\n          neighbors.push({ x: sortedVPipes[vIdx + 1].coord, y: cy });\n        }\n        if (hIdx > 0) {\n          neighbors.push({ x: cx, y: hPipesSorted[hIdx - 1].coord });\n        }\n        if (hIdx >= 0 && hIdx < hPipesSorted.length - 1) {\n          neighbors.push({ x: cx, y: hPipesSorted[hIdx + 1].coord });\n        }\n        for (const neighbor of neighbors) {\n          const minX = Math.min(cx, neighbor.x);\n          const maxX = Math.max(cx, neighbor.x);\n          const minY = Math.min(cy, neighbor.y);\n          const maxY = Math.max(cy, neighbor.y);\n          const blocked = obstacles.some((obs) => {\n            if (obs.nodeId === e.start || obs.nodeId === e.end) {\n              return false;\n            }\n            if (minX !== maxX) {\n              return obs.minY < cy && obs.maxY > cy && obs.maxX > minX && obs.minX < maxX;\n            } else {\n              return obs.minX < cx && obs.maxX > cx && obs.maxY > minY && obs.minY < maxY;\n            }\n          });\n          if (blocked) {\n            continue;\n          }\n          const nKey = getKey(neighbor.x, neighbor.y);\n          const dist = Math.abs(neighbor.x - cx) + Math.abs(neighbor.y - cy);\n          const penalty = crossingPenalty(i, current.pt, neighbor);\n          let dirPenalty = 0;\n          const destDx = pDstAnchor.x - pSrcAnchor.x;\n          const destDy = pDstAnchor.y - pSrcAnchor.y;\n          const moveDx = neighbor.x - cx;\n          const moveDy = neighbor.y - cy;\n          if (destDy > 10 && moveDy < -5 || destDy < -10 && moveDy > 5) {\n            dirPenalty = Math.abs(moveDy) * 100;\n          }\n          if (destDx > 10 && moveDx < -5 || destDx < -10 && moveDx > 5) {\n            dirPenalty += Math.abs(moveDx) * 50;\n          }\n          let bendPenalty = 0;\n          const currentDir = arrivalDir.get(current.key) ?? \"n\";\n          const moveDir = Math.abs(moveDx) > EPS7 ? \"h\" : \"v\";\n          if (currentDir !== \"n\" && currentDir !== moveDir) {\n            bendPenalty = 50;\n          }\n          const stepCost = dist + penalty + dirPenalty + bendPenalty;\n          const tentativeG = (gScore.get(current.key) ?? Infinity) + stepCost;\n          const h = Math.abs(pDstAnchor.x - neighbor.x) + Math.abs(pDstAnchor.y - neighbor.y);\n          if (tentativeG < (gScore.get(nKey) ?? Infinity)) {\n            cameFrom.set(nKey, current.pt);\n            gScore.set(nKey, tentativeG);\n            arrivalDir.set(nKey, moveDir);\n            if (!openSet.has(nKey)) {\n              openList.push({ key: nKey, f: tentativeG + h, pt: neighbor });\n              openSet.add(nKey);\n            } else {\n              const idx = openList.findIndex((x) => x.key === nKey);\n              if (idx !== -1) {\n                openList[idx].f = tentativeG + h;\n              }\n            }\n          }\n        }\n      }\n    }\n    if (foundPath.length === 0) {\n      foundPath = [pSrcAnchor, { x: pSrcAnchor.x, y: pDstAnchor.y }, pDstAnchor];\n    }\n    if (foundPath.length > 4) {\n      const start = foundPath[0];\n      const end = foundPath[foundPath.length - 1];\n      let minX = Math.min(start.x, end.x);\n      let maxX = Math.max(start.x, end.x);\n      let minY = Math.min(start.y, end.y);\n      let maxY = Math.max(start.y, end.y);\n      for (const pt of foundPath) {\n        minX = Math.min(minX, pt.x);\n        maxX = Math.max(maxX, pt.x);\n        minY = Math.min(minY, pt.y);\n        maxY = Math.max(maxY, pt.y);\n      }\n      const wentRight = maxX > Math.max(start.x, end.x);\n      const wentLeft = minX < Math.min(start.x, end.x);\n      if (isLR) {\n        const margin = VERTICAL_PIPE_MARGIN;\n        if (wentRight) {\n          const pathX = Math.max(start.x, end.x);\n          const pathMinY = Math.min(start.y, end.y);\n          const pathMaxY = Math.max(start.y, end.y);\n          const detourObstacles = obstacles.filter(\n            (obs) => obs.minX < pathX && obs.maxX > pathX && // obstacle's x-range contains the path x\n            obs.minY < pathMaxY && obs.maxY > pathMinY\n            // obstacle's y-range overlaps with path y-range\n          );\n          if (detourObstacles.length > 0) {\n            let visualMaxX = Math.max(start.x, end.x);\n            for (const obs of detourObstacles) {\n              const obsCenterX = (obs.minX + obs.maxX) / 2;\n              if (obs.visualXHalfExtent === void 0 || isNaN(obs.visualXHalfExtent)) {\n                continue;\n              }\n              const visualRight = obsCenterX + obs.visualXHalfExtent + margin;\n              visualMaxX = Math.max(visualMaxX, visualRight);\n            }\n            if (!isNaN(visualMaxX)) {\n              maxX = visualMaxX;\n            }\n          }\n        }\n        if (wentLeft) {\n          const detourObstacles = obstacles.filter(\n            (obs) => obs.minX < Math.min(start.x, end.x) + margin && // obstacle extends past the direct path\n            obs.minY < Math.max(start.y, end.y) && obs.maxY > Math.min(start.y, end.y)\n            // obstacle is in Y range\n          );\n          if (detourObstacles.length > 0) {\n            let visualMinX = Math.min(start.x, end.x);\n            for (const obs of detourObstacles) {\n              const obsCenterX = (obs.minX + obs.maxX) / 2;\n              const visualLeft = obsCenterX - obs.visualXHalfExtent - margin;\n              visualMinX = Math.min(visualMinX, visualLeft);\n            }\n            minX = visualMinX;\n          }\n        }\n      }\n      const findBestReturnY = /* @__PURE__ */ __name((detourX) => {\n        const goingDown = end.y > start.y;\n        const relevantObs = obstacles.filter((obs) => {\n          const obsInXRange = Math.min(start.x, end.x) < obs.maxX && Math.max(start.x, end.x) > obs.minX;\n          const obsInYRange = Math.min(start.y, end.y) < obs.maxY && Math.max(start.y, end.y) > obs.minY;\n          return obsInXRange && obsInYRange;\n        });\n        let filteredObs = relevantObs;\n        if (isLR && relevantObs.length > 0) {\n          const obsAtDetourX = relevantObs.filter(\n            (obs) => obs.minX < detourX && obs.maxX > detourX\n          );\n          if (obsAtDetourX.length > 0) {\n            filteredObs = obsAtDetourX;\n          }\n        }\n        if (filteredObs.length === 0) {\n          return end.y;\n        }\n        const margin = HORIZONTAL_PIPE_MARGIN;\n        if (goingDown) {\n          const lowestObsBottom = Math.max(...filteredObs.map((obs) => obs.maxY));\n          const bestY = lowestObsBottom + margin;\n          if (bestY < end.y - EPS7) {\n            return bestY;\n          }\n        } else {\n          const highestObsTop = Math.min(...filteredObs.map((obs) => obs.minY));\n          const bestY = highestObsTop - margin;\n          if (bestY > end.y + EPS7) {\n            return bestY;\n          }\n        }\n        return end.y;\n      }, \"findBestReturnY\");\n      const trySimplifyWithDetourX = /* @__PURE__ */ __name((detourX) => {\n        const bestY = findBestReturnY(detourX);\n        const corner1 = { x: detourX, y: start.y };\n        const corner2 = { x: detourX, y: bestY };\n        const corner3 = { x: end.x, y: bestY };\n        const seg1Blocked = checkSegmentBlocked(start, corner1);\n        const seg2Blocked = checkSegmentBlocked(corner1, corner2);\n        const seg3Blocked = checkSegmentBlocked(corner2, corner3);\n        const seg4Blocked = bestY !== end.y ? checkSegmentBlocked(corner3, end) : false;\n        if (!seg1Blocked && !seg2Blocked && !seg3Blocked && !seg4Blocked) {\n          if (Math.abs(bestY - end.y) < EPS7) {\n            return [start, corner1, corner2, end];\n          }\n          return [start, corner1, corner2, corner3, end];\n        }\n        return null;\n      }, \"trySimplifyWithDetourX\");\n      const simplified2 = wentRight && !wentLeft ? trySimplifyWithDetourX(maxX) : wentLeft && !wentRight ? trySimplifyWithDetourX(minX) : null;\n      if (simplified2) {\n        foundPath = simplified2;\n      }\n    }\n    const fullPoints = [\n      pSrcPort,\n      ...srcHandleWaypoints,\n      ...foundPath,\n      ...dstHandleWaypoints.reverse(),\n      // Reverse because they're stored anchor->waypoint->port\n      pDstPort\n    ];\n    if (fullPoints.length >= 3) {\n      const C = fullPoints[fullPoints.length - 1];\n      const B = fullPoints[fullPoints.length - 2];\n      const A = fullPoints[fullPoints.length - 3];\n      const isHoriz = Math.abs(A.y - B.y) < EPS7 && Math.abs(B.y - C.y) < EPS7;\n      const isVert = Math.abs(A.x - B.x) < EPS7 && Math.abs(B.x - C.x) < EPS7;\n      if (isHoriz) {\n        const signAB = Math.sign(B.x - A.x);\n        const signAC = Math.sign(C.x - A.x);\n        if (signAB !== 0 && signAB === signAC && Math.abs(B.x - A.x) > Math.abs(C.x - A.x)) {\n          fullPoints.splice(-2, 1);\n        }\n      } else if (isVert) {\n        const signAB = Math.sign(B.y - A.y);\n        const signAC = Math.sign(C.y - A.y);\n        if (signAB !== 0 && signAB === signAC && Math.abs(B.y - A.y) > Math.abs(C.y - A.y)) {\n          fullPoints.splice(-2, 1);\n        }\n      }\n    }\n    const simplified = [fullPoints[0]];\n    for (let k = 1; k < fullPoints.length - 1; k++) {\n      if (k === 1) {\n        simplified.push(fullPoints[k]);\n        continue;\n      }\n      const prev = simplified[simplified.length - 1];\n      const curr = fullPoints[k];\n      const next = fullPoints[k + 1];\n      if (Math.abs(prev.y - curr.y) < EPS7 && Math.abs(curr.y - next.y) < EPS7) {\n        const dir1 = curr.x > prev.x;\n        const dir2 = next.x > curr.x;\n        if (dir1 !== dir2) {\n          simplified.push(curr);\n          continue;\n        }\n        continue;\n      }\n      if (Math.abs(prev.x - curr.x) < EPS7 && Math.abs(curr.x - next.x) < EPS7) {\n        const dir1 = curr.y > prev.y;\n        const dir2 = next.y > curr.y;\n        if (dir1 !== dir2) {\n          simplified.push(curr);\n          continue;\n        }\n        continue;\n      }\n      simplified.push(curr);\n    }\n    simplified.push(fullPoints[fullPoints.length - 1]);\n    for (let k = 0; k < simplified.length - 1; k++) {\n      const p1 = simplified[k];\n      const p2 = simplified[k + 1];\n      const orientation = Math.abs(p1.x - p2.x) < EPS7 ? \"vertical\" : \"horizontal\";\n      const coord = orientation === \"vertical\" ? p1.x : p1.y;\n      const from = orientation === \"vertical\" ? Math.min(p1.y, p2.y) : Math.min(p1.x, p2.x);\n      const to = orientation === \"vertical\" ? Math.max(p1.y, p2.y) : Math.max(p1.x, p2.x);\n      const pipe = getOrAddPipe(orientation, coord, from, to);\n      const rSeg = {\n        edgeIndex: i,\n        segmentIndex: k,\n        orientation,\n        pipe,\n        trackIndex: 0,\n        // Initial track\n        from,\n        to\n      };\n      allRoutedSegments.push(rSeg);\n      edgeSegmentIndices[i].push(allRoutedSegments.length - 1);\n      if (!pipe.tracks[0]) {\n        pipe.tracks[0] = { index: 0, coord: pipe.coord, segments: [] };\n      }\n      pipe.tracks[0].segments.push({\n        edgeIndex: i,\n        segmentIndex: k,\n        from,\n        to\n      });\n    }\n  }\n  const segmentsOverlap = /* @__PURE__ */ __name((s1, s2) => {\n    return s1.from < s2.to && s2.from < s1.to;\n  }, \"segmentsOverlap\");\n  const trySwapSegmentsAcrossTracks = /* @__PURE__ */ __name((s1, s2, t1, t2) => {\n    const canS1GoT2 = !t2.segments.some(\n      (r) => (r.edgeIndex !== s2.edgeIndex || r.segmentIndex !== s2.segmentIndex) && segmentsOverlap(r, s1)\n    );\n    const canS2GoT1 = !t1.segments.some(\n      (r) => (r.edgeIndex !== s1.edgeIndex || r.segmentIndex !== s1.segmentIndex) && segmentsOverlap(r, s2)\n    );\n    if (canS1GoT2 && canS2GoT1) {\n      s1.trackIndex = t2.index;\n      s2.trackIndex = t1.index;\n      t1.segments = [\n        ...t1.segments.filter(\n          (r) => r.edgeIndex !== s1.edgeIndex || r.segmentIndex !== s1.segmentIndex\n        ),\n        {\n          edgeIndex: s2.edgeIndex,\n          segmentIndex: s2.segmentIndex,\n          from: s2.from,\n          to: s2.to\n        }\n      ];\n      t2.segments = [\n        ...t2.segments.filter(\n          (r) => r.edgeIndex !== s2.edgeIndex || r.segmentIndex !== s2.segmentIndex\n        ),\n        {\n          edgeIndex: s1.edgeIndex,\n          segmentIndex: s1.segmentIndex,\n          from: s1.from,\n          to: s1.to\n        }\n      ];\n      return true;\n    }\n    return false;\n  }, \"trySwapSegmentsAcrossTracks\");\n  const createNewTrack = /* @__PURE__ */ __name((pipe) => {\n    const idx = pipe.tracks.length;\n    pipe.tracks[idx] = { index: idx, coord: pipe.coord, segments: [] };\n    return idx;\n  }, \"createNewTrack\");\n  const moveSegmentToTrack = /* @__PURE__ */ __name((seg, trackIdx) => {\n    const oldTrack = seg.pipe.tracks[seg.trackIndex];\n    oldTrack.segments = oldTrack.segments.filter(\n      (r) => r.edgeIndex !== seg.edgeIndex || r.segmentIndex !== seg.segmentIndex\n    );\n    seg.trackIndex = trackIdx;\n    const newTrack = seg.pipe.tracks[trackIdx];\n    newTrack.segments.push({\n      edgeIndex: seg.edgeIndex,\n      segmentIndex: seg.segmentIndex,\n      from: seg.from,\n      to: seg.to\n    });\n  }, \"moveSegmentToTrack\");\n  const moveSegmentChainToTrack = /* @__PURE__ */ __name((seg, trackIdx) => {\n    const indices = edgeSegmentIndices[seg.edgeIndex];\n    for (const idx of indices) {\n      const s = allRoutedSegments[idx];\n      if (s.pipe === seg.pipe) {\n        moveSegmentToTrack(s, trackIdx);\n      }\n    }\n  }, \"moveSegmentChainToTrack\");\n  const getAdjacentSegmentsAlongEdge = /* @__PURE__ */ __name((seg) => {\n    const indices = edgeSegmentIndices[seg.edgeIndex];\n    const idxInList = indices.indexOf(allRoutedSegments.indexOf(seg));\n    const adj = [];\n    if (idxInList > 0) {\n      adj.push(allRoutedSegments[indices[idxInList - 1]]);\n    }\n    if (idxInList < indices.length - 1) {\n      adj.push(allRoutedSegments[indices[idxInList + 1]]);\n    }\n    return adj;\n  }, \"getAdjacentSegmentsAlongEdge\");\n  const haveAnyCrossing = /* @__PURE__ */ __name((segA, segB) => {\n    if (segA.orientation === segB.orientation) {\n      return false;\n    }\n    const h = segA.orientation === \"horizontal\" ? segA : segB;\n    const v = segA.orientation === \"horizontal\" ? segB : segA;\n    return v.pipe.coord > h.from && v.pipe.coord < h.to && h.pipe.coord > v.from && h.pipe.coord < v.to;\n  }, \"haveAnyCrossing\");\n  const findAvailableTrack = /* @__PURE__ */ __name((pipe, seg) => {\n    for (const track of pipe.tracks) {\n      const overlap = track.segments.some(\n        (r) => (r.edgeIndex !== seg.edgeIndex || r.segmentIndex !== seg.segmentIndex) && segmentsOverlap(r, seg)\n      );\n      if (!overlap) {\n        return track.index;\n      }\n    }\n    return -1;\n  }, \"findAvailableTrack\");\n  const segmentsConflict = /* @__PURE__ */ __name((s1, s2) => {\n    if (s1.trackIndex === s2.trackIndex) {\n      return segmentsOverlap(s1, s2);\n    }\n    const adj1 = getAdjacentSegmentsAlongEdge(s1);\n    const adj2 = getAdjacentSegmentsAlongEdge(s2);\n    return adj1.some((a1) => adj2.some((a2) => haveAnyCrossing(a1, a2)));\n  }, \"segmentsConflict\");\n  const resolveTrackConflict = /* @__PURE__ */ __name((s1, s2, move) => {\n    if (trySwapSegmentsAcrossTracks(\n      s1,\n      s2,\n      s1.pipe.tracks[s1.trackIndex],\n      s2.pipe.tracks[s2.trackIndex]\n    )) {\n      return;\n    }\n    const avail = findAvailableTrack(s1.pipe, s2);\n    move(s2, avail !== -1 ? avail : createNewTrack(s1.pipe));\n  }, \"resolveTrackConflict\");\n  const resolveHandleConflicts = /* @__PURE__ */ __name((handles) => {\n    let crossings = 0;\n    for (let i = 0; i < handles.length; i++) {\n      for (let j = i + 1; j < handles.length; j++) {\n        const h1 = handles[i];\n        const h2 = handles[j];\n        if (h1.pipe !== h2.pipe) {\n          continue;\n        }\n        if (segmentsConflict(h1, h2)) {\n          crossings++;\n          resolveTrackConflict(h1, h2, moveSegmentChainToTrack);\n        }\n      }\n    }\n    return crossings;\n  }, \"resolveHandleConflicts\");\n  const destInfoCache = /* @__PURE__ */ new Map();\n  const getDestInfo = /* @__PURE__ */ __name((edgeIdx) => {\n    if (destInfoCache.has(edgeIdx)) {\n      return destInfoCache.get(edgeIdx);\n    }\n    const indices = edgeSegmentIndices[edgeIdx];\n    if (indices.length === 0) {\n      const info2 = { dest: 0, deviation: 0, base: 0, delta: 0 };\n      destInfoCache.set(edgeIdx, info2);\n      return info2;\n    }\n    const firstSeg = allRoutedSegments[indices[0]];\n    const base = firstSeg.pipe.coord;\n    let dest = base;\n    for (let idx = 1; idx < indices.length; idx++) {\n      const seg = allRoutedSegments[indices[idx]];\n      if (seg.orientation === \"horizontal\") {\n        const candidateA = seg.from;\n        const candidateB = seg.to;\n        dest = Math.abs(candidateA - base) > Math.abs(candidateB - base) ? candidateA : candidateB;\n        break;\n      }\n    }\n    const deviation = Math.abs(dest - base);\n    const info = { dest, deviation, base, delta: dest - base };\n    destInfoCache.set(edgeIdx, info);\n    return info;\n  }, \"getDestInfo\");\n  const fixSourceHandleCrossings = /* @__PURE__ */ __name(() => {\n    let crossings = 0;\n    const edgesBySource = /* @__PURE__ */ new Map();\n    for (const [i, e] of edges.entries()) {\n      if (edgeSegmentIndices[i].length === 0) {\n        continue;\n      }\n      if (!e.start) {\n        continue;\n      }\n      if (!edgesBySource.has(e.start)) {\n        edgesBySource.set(e.start, []);\n      }\n      edgesBySource.get(e.start).push(i);\n    }\n    const getEdgeDistance = /* @__PURE__ */ __name((edgeIdx) => {\n      const edge = edges[edgeIdx];\n      if (!edge.start || !edge.end) {\n        return 0;\n      }\n      const srcNode = nodeById.get(edge.start);\n      const dstNode = nodeById.get(edge.end);\n      if (!srcNode || !dstNode) {\n        return 0;\n      }\n      const dx = (dstNode.x ?? 0) - (srcNode.x ?? 0);\n      const dy = (dstNode.y ?? 0) - (srcNode.y ?? 0);\n      return Math.abs(dx) + Math.abs(dy);\n    }, \"getEdgeDistance\");\n    for (const grp of edgesBySource.values()) {\n      grp.sort((a, b) => {\n        const infoA = getDestInfo(a);\n        const infoB = getDestInfo(b);\n        if (Math.abs(infoA.deviation - infoB.deviation) > 1) {\n          return infoA.deviation - infoB.deviation;\n        }\n        if (Math.abs(infoA.dest - infoB.dest) > 1) {\n          return infoA.dest - infoB.dest;\n        }\n        const distA = getEdgeDistance(a);\n        const distB = getEdgeDistance(b);\n        if (Math.abs(distA - distB) > 1) {\n          return distB - distA;\n        }\n        const lenA = edgeSegmentIndices[a].length;\n        const lenB = edgeSegmentIndices[b].length;\n        if (lenA !== lenB) {\n          return lenA - lenB;\n        }\n        if (lenA === 1) {\n          const idxA = edgeSegmentIndices[a][0];\n          const idxB = edgeSegmentIndices[b][0];\n          if (allRoutedSegments[idxA] && allRoutedSegments[idxB]) {\n            const segA = allRoutedSegments[idxA];\n            const segB = allRoutedSegments[idxB];\n            const distA2 = Math.abs(segA.to - segA.from);\n            const distB2 = Math.abs(segB.to - segB.from);\n            if (Math.abs(distA2 - distB2) > 1) {\n              return distA2 - distB2;\n            }\n          }\n        }\n        return 0;\n      });\n      const handles = grp.map((ei) => allRoutedSegments[edgeSegmentIndices[ei][0]]);\n      crossings += resolveHandleConflicts(handles);\n    }\n    return crossings;\n  }, \"fixSourceHandleCrossings\");\n  const fixTargetHandleCrossings = /* @__PURE__ */ __name(() => {\n    let crossings = 0;\n    const edgesByTarget = /* @__PURE__ */ new Map();\n    for (const [i, e] of edges.entries()) {\n      const indices = edgeSegmentIndices[i];\n      if (indices.length === 0) {\n        continue;\n      }\n      if (!e.end) {\n        continue;\n      }\n      if (!edgesByTarget.has(e.end)) {\n        edgesByTarget.set(e.end, []);\n      }\n      edgesByTarget.get(e.end).push(i);\n    }\n    for (const grp of edgesByTarget.values()) {\n      grp.sort((a, b) => {\n        const getDist = /* @__PURE__ */ __name((edgeIdx) => {\n          const indices = edgeSegmentIndices[edgeIdx];\n          if (indices.length < 2) {\n            return 0;\n          }\n          const prev = allRoutedSegments[indices[indices.length - 2]];\n          return Math.abs(prev.to - prev.from);\n        }, \"getDist\");\n        const scoreA = getDist(a);\n        const scoreB = getDist(b);\n        if (Math.abs(scoreA - scoreB) > 0.1) {\n          return scoreA - scoreB;\n        }\n        return a - b;\n      });\n      const handles = grp.map(\n        (ei) => allRoutedSegments[edgeSegmentIndices[ei][edgeSegmentIndices[ei].length - 1]]\n      );\n      crossings += resolveHandleConflicts(handles);\n    }\n    return crossings;\n  }, \"fixTargetHandleCrossings\");\n  const fixPipeCrossings = /* @__PURE__ */ __name(() => {\n    let crossings = 0;\n    for (const pipe of pipes) {\n      const pipeSegments = [];\n      for (const t of pipe.tracks) {\n        for (const ref of t.segments) {\n          const idx = edgeSegmentIndices[ref.edgeIndex].find(\n            (ix) => allRoutedSegments[ix].segmentIndex === ref.segmentIndex\n          );\n          if (idx !== void 0) {\n            pipeSegments.push(allRoutedSegments[idx]);\n          }\n        }\n      }\n      pipeSegments.sort((a, b) => a.edgeIndex - b.edgeIndex || a.segmentIndex - b.segmentIndex);\n      for (let i = 0; i < pipeSegments.length; i++) {\n        for (let j = i + 1; j < pipeSegments.length; j++) {\n          const s1 = pipeSegments[i];\n          const s2 = pipeSegments[j];\n          if (segmentsConflict(s1, s2)) {\n            crossings++;\n            resolveTrackConflict(s1, s2, moveSegmentToTrack);\n          }\n        }\n      }\n    }\n    return crossings;\n  }, \"fixPipeCrossings\");\n  let iterations = 0;\n  const MAX_ITER = 10;\n  while (iterations < MAX_ITER) {\n    let changed = 0;\n    changed += fixSourceHandleCrossings();\n    changed += fixTargetHandleCrossings();\n    changed += fixPipeCrossings();\n    if (changed === 0) {\n      break;\n    }\n    iterations++;\n  }\n  const segmentCoords = /* @__PURE__ */ new Map();\n  for (const pipe of pipes) {\n    const segments = [];\n    pipe.tracks.forEach((t) => {\n      t.segments.forEach((s) => {\n        segments.push({\n          edgeIndex: s.edgeIndex,\n          segmentIndex: s.segmentIndex,\n          trackIndex: t.index,\n          from: s.from,\n          to: s.to\n        });\n      });\n    });\n    segments.sort((a, b) => a.from - b.from);\n    const clusters = [];\n    if (segments.length > 0) {\n      let currentCluster = [segments[0]];\n      let clusterEnd = segments[0].to;\n      for (let k = 1; k < segments.length; k++) {\n        const s = segments[k];\n        if (s.from < clusterEnd) {\n          currentCluster.push(s);\n          clusterEnd = Math.max(clusterEnd, s.to);\n        } else {\n          clusters.push(currentCluster);\n          currentCluster = [s];\n          clusterEnd = s.to;\n        }\n      }\n      clusters.push(currentCluster);\n    }\n    for (const cluster of clusters) {\n      const usedTracks = /* @__PURE__ */ new Set();\n      cluster.forEach((s) => usedTracks.add(s.trackIndex));\n      const trackScores = /* @__PURE__ */ new Map();\n      cluster.forEach((s) => {\n        const info = getDestInfo(s.edgeIndex);\n        trackScores.set(s.trackIndex, (trackScores.get(s.trackIndex) ?? 0) + info.delta);\n      });\n      const leftTracks = [...usedTracks].filter((t) => (trackScores.get(t) ?? 0) < -1);\n      const rightTracks = [...usedTracks].filter((t) => (trackScores.get(t) ?? 0) > 1);\n      const neutralTracks = [...usedTracks].filter((t) => Math.abs(trackScores.get(t) ?? 0) <= 1);\n      leftTracks.sort((a, b) => (trackScores.get(b) ?? 0) - (trackScores.get(a) ?? 0));\n      rightTracks.sort((a, b) => (trackScores.get(a) ?? 0) - (trackScores.get(b) ?? 0));\n      const assignCoord = /* @__PURE__ */ __name((trackIndex, coord) => {\n        cluster.filter((s) => s.trackIndex === trackIndex).forEach((s) => {\n          const effectiveCoord = straightIntraLaneEdges.has(s.edgeIndex) ? pipe.coord : coord;\n          segmentCoords.set(`${s.edgeIndex}-${s.segmentIndex}`, effectiveCoord);\n        });\n      }, \"assignCoord\");\n      let leftCount = 0;\n      for (const trackIndex of leftTracks) {\n        leftCount++;\n        assignCoord(trackIndex, pipe.coord - leftCount * TRACK_SPACING);\n      }\n      if (neutralTracks.length === 0 && usedTracks.size > 0) {\n        const bestTrack = [...usedTracks].sort(\n          (a, b) => Math.abs(trackScores.get(a) ?? 0) - Math.abs(trackScores.get(b) ?? 0)\n        )[0];\n        const leftIdx = leftTracks.indexOf(bestTrack);\n        if (leftIdx !== -1) {\n          leftTracks.splice(leftIdx, 1);\n        }\n        const rightIdx = rightTracks.indexOf(bestTrack);\n        if (rightIdx !== -1) {\n          rightTracks.splice(rightIdx, 1);\n        }\n        neutralTracks.push(bestTrack);\n      }\n      let neutralAssigned = 0;\n      for (const trackIndex of neutralTracks) {\n        if (neutralAssigned === 0) {\n          assignCoord(trackIndex, pipe.coord);\n        } else {\n          const dir = neutralAssigned % 2 === 1 ? 1 : -1;\n          const magnitude = Math.ceil(neutralAssigned / 2);\n          assignCoord(trackIndex, pipe.coord + dir * magnitude * TRACK_SPACING * 0.5);\n        }\n        neutralAssigned++;\n      }\n      let rightCount = 0;\n      for (const trackIndex of rightTracks) {\n        rightCount++;\n        assignCoord(trackIndex, pipe.coord + rightCount * TRACK_SPACING);\n      }\n    }\n  }\n  for (const [i, e] of edges.entries()) {\n    const indices = edgeSegmentIndices[i] ?? [];\n    if (indices.length === 0) {\n      continue;\n    }\n    const newPoints = [];\n    const src = nodeById.get(e.start);\n    const dst = nodeById.get(e.end);\n    const { pSrcPort, pDstPort } = portsForEdge(i, src, dst);\n    const lines = indices.map((idx) => {\n      const s = allRoutedSegments[idx];\n      const coord = segmentCoords.get(`${s.edgeIndex}-${s.segmentIndex}`) ?? s.pipe.coord;\n      return {\n        orient: s.orientation,\n        coord,\n        from: s.from,\n        to: s.to\n      };\n    });\n    newPoints.push(pSrcPort);\n    for (let k = 0; k < lines.length; k++) {\n      const line = lines[k];\n      const prevPt = newPoints[newPoints.length - 1];\n      const prevAlong = line.orient === \"vertical\" ? prevPt.y : prevPt.x;\n      const prevTrackCoord = line.orient === \"vertical\" ? prevPt.x : prevPt.y;\n      const nextLine = lines[k + 1];\n      const hasNextLine = k < lines.length - 1;\n      if (Math.abs(prevTrackCoord - line.coord) > EPS7) {\n        newPoints.push(pointOnLine(line, prevAlong));\n      }\n      if (hasNextLine && nextLine.orient === line.orient) {\n        if (Math.abs(line.coord - nextLine.coord) > EPS7) {\n          const junction = line.orient === \"vertical\" ? (prevAlong + nextLine.from) / 2 : sharedLineEndpointCoord(line, nextLine);\n          newPoints.push(pointOnLine(line, junction), pointOnLine(nextLine, junction));\n        } else if (k === 0 || k === lines.length - 2) {\n          newPoints.push(pointOnLine(line, sharedLineEndpointCoord(line, nextLine)));\n        }\n      } else if (hasNextLine) {\n        newPoints.push(pointOnLine(line, nextLine.coord));\n      } else {\n        const endAlong = Math.abs(line.from - prevAlong) < Math.abs(line.to - prevAlong) ? line.to : line.from;\n        newPoints.push(pointOnLine(line, endAlong));\n      }\n    }\n    const last = newPoints[newPoints.length - 1];\n    if (Math.abs(last.x - pDstPort.x) > EPS7 || Math.abs(last.y - pDstPort.y) > EPS7) {\n      newPoints.push(pDstPort);\n    }\n    const filtered = [];\n    if (newPoints.length > 0) {\n      filtered.push(newPoints[0]);\n    }\n    for (let k = 1; k < newPoints.length; k++) {\n      const p = newPoints[k];\n      const prev = filtered[filtered.length - 1];\n      if (Math.abs(p.x - prev.x) > EPS7 || Math.abs(p.y - prev.y) > EPS7) {\n        filtered.push(p);\n      }\n    }\n    e.points = filtered;\n  }\n  for (const re of edges) {\n    const orig = re.__originalEdge;\n    if (orig && re.points) {\n      orig.points = re.points;\n    }\n  }\n  data.edges = (data.edges ?? []).filter((e) => !e.isLayoutOnly);\n  const nodeBoundaryClamp = /* @__PURE__ */ __name((p, node) => {\n    const cx = node.x ?? 0;\n    const cy = node.y ?? 0;\n    const w = node.width ?? 0;\n    const h = node.height ?? 0;\n    if (w <= 0 || h <= 0) {\n      return p;\n    }\n    const left = cx - w / 2;\n    const right = cx + w / 2;\n    const top = cy - h / 2;\n    const bottom = cy + h / 2;\n    if (p.x < left || p.x > right || p.y < top || p.y > bottom) {\n      return p;\n    }\n    const dLeft = p.x - left;\n    const dRight = right - p.x;\n    const dTop = p.y - top;\n    const dBottom = bottom - p.y;\n    const minD = Math.min(dLeft, dRight, dTop, dBottom);\n    if (minD === dLeft) {\n      return { x: left, y: p.y };\n    }\n    if (minD === dRight) {\n      return { x: right, y: p.y };\n    }\n    if (minD === dTop) {\n      return { x: p.x, y: top };\n    }\n    return { x: p.x, y: bottom };\n  }, \"nodeBoundaryClamp\");\n  for (const edge of data.edges) {\n    const pts = edge.points;\n    if (!pts || pts.length < 2) {\n      continue;\n    }\n    const srcId = edge.start;\n    const dstId = edge.end;\n    const src = srcId ? nodeById.get(srcId) : void 0;\n    const dst = dstId ? nodeById.get(dstId) : void 0;\n    if (src) {\n      pts[0] = nodeBoundaryClamp(pts[0], src);\n    }\n    if (dst) {\n      pts[pts.length - 1] = nodeBoundaryClamp(pts[pts.length - 1], dst);\n    }\n  }\n  return data;\n}\n__name(routeEdgesOrthogonal, \"routeEdgesOrthogonal\");\n\n// src/rendering-util/layout-algorithms/swimlanes/layoutCore.ts\nfunction getSwimlaneDirection(data4Layout) {\n  return data4Layout.direction ?? \"TB\";\n}\n__name(getSwimlaneDirection, \"getSwimlaneDirection\");\nfunction runSwimlaneLayoutCore(data4Layout) {\n  const g = toGraphView(data4Layout);\n  const nodeGap = data4Layout.config.flowchart?.nodeSpacing ?? 40;\n  const layerGap = data4Layout.config.flowchart?.rankSpacing ?? 100;\n  const ignoreCrossLaneEdges = data4Layout.config.swimlane?.ignoreCrossLaneEdges ?? true;\n  const optimizeRanksByCrossings2 = data4Layout.config.swimlane?.optimizeRanksByCrossings ?? true;\n  const automaticLaneOrdering = data4Layout.config.swimlane?.automaticLaneOrdering ?? false;\n  const direction = getSwimlaneDirection(data4Layout);\n  const { ordered, coordinates } = sugiyamaLayout(g, {\n    nodeGap,\n    layerGap,\n    ignoreCrossLaneEdges,\n    optimizeRanksByCrossings: optimizeRanksByCrossings2,\n    automaticLaneOrdering,\n    direction\n  });\n  writeBackToLayoutData(g, ordered, coordinates, { nodeGap, layerGap });\n  for (const edge of data4Layout.edges ?? []) {\n    delete edge.points;\n  }\n  routeEdgesOrthogonal(data4Layout, direction);\n  for (const edge of data4Layout.edges ?? []) {\n    if (!edge.curve || edge.curve === \"basis\") {\n      edge.curve = \"rounded\";\n    }\n  }\n  postProcessSwimlaneLayout(data4Layout, direction);\n  validateSwimlanesLayout(data4Layout);\n  return direction;\n}\n__name(runSwimlaneLayoutCore, \"runSwimlaneLayoutCore\");\n\n// src/rendering-util/layout-algorithms/swimlanes/index.ts\nfunction prepareSwimlaneLayout(data4Layout) {\n  prepareLayoutForSwimlanes(data4Layout);\n  const transformedData = createEdgeLabelNodes(data4Layout);\n  data4Layout.nodes = transformedData.nodes;\n  data4Layout.edges = transformedData.edges;\n}\n__name(prepareSwimlaneLayout, \"prepareSwimlaneLayout\");\nvar render = createCommonLayoutRenderer({\n  prepareLayout: prepareSwimlaneLayout,\n  runLayoutCore: runSwimlaneLayoutCore,\n  afterPaint: applySwimlaneLineJumps\n});\nexport {\n  render\n};\n"],"names":[],"mappings":";;AAE8B;AACA;AAGA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AAGA;AAGA;;AAE9B;AACA,IAAI,qBAAqB;AACzB,IAAI,cAAc;AAClB,IAAI,gBAAgB;AACpB,SAAS,gBAAgB;AACzB;AACA,kBAAkB,uBAAuB;AACzC,oBAAoB,gCAAgC;AACpD;AACA;AACA;AACA,mBAAM,CAAC,gBAAgB;AACvB,SAAS,mBAAmB;AAC5B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,YAAY,gBAAgB,cAAc,gBAAgB,UAAU,gBAAgB,cAAc,gBAAgB;AAClH;AACA;AACA;AACA,aAAa,wCAAwC;AACrD;AACA;AACA;AACA;AACA,mBAAM,CAAC,mBAAmB;AAC1B,SAAS,eAAe;AACxB;AACA;AACA,mBAAM,CAAC,eAAe;AACtB,SAAS,qBAAqB;AAC9B;AACA,kBAAkB,kBAAkB;AACpC;AACA,sBAAsB,gBAAgB;AACtC,wBAAwB,kBAAkB;AAC1C;AACA,wBAAwB,gBAAgB;AACxC;AACA;AACA,sBAAsB,mBAAmB;AACzC;AACA;AACA;AACA,yBAAyB,eAAe;AACxC,yBAAyB,eAAe;AACxC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,aAAa;AACb,YAAY;AACZ;AACA;AACA;AACA;AACA;AACA;AACA,aAAa;AACb;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,qBAAqB;AAC5B,SAAS,GAAG;AACZ;AACA,wCAAwC,QAAQ,OAAO,QAAQ;AAC/D;AACA,mBAAM,CAAC,GAAG;AACV,SAAS,aAAa;AACtB,YAAY,GAAG,MAAM,GAAG,GAAG,MAAM;AACjC;AACA,mBAAM,CAAC,aAAa;AACpB,SAAS,eAAe;AACxB;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,eAAe;AACtB,IAAI,eAAe;AACnB,SAAS,kBAAkB;AAC3B;AACA,gCAAgC,MAAM;AACtC;AACA,mCAAmC,MAAM;AACzC,0CAA0C,aAAa;AACvD;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sCAAsC,aAAa;AACnD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,kBAAkB;AACzB,SAAS,QAAQ;AACjB;AACA;AACA,gBAAgB;AAChB,iBAAiB;AACjB,mBAAmB,aAAa,MAAM;AACtC;AACA,iBAAiB,GAAG,SAAS,GAAG,GAAG,UAAU,MAAM,OAAO,EAAE,aAAa,OAAO;AAChF,IAAI;AACJ,iBAAiB,aAAa,OAAO;AACrC;AACA;AACA;AACA,mBAAM,CAAC,QAAQ;AACf,SAAS,oBAAoB;AAC7B;AACA;AACA;AACA;AACA;AACA;AACA,aAAa,cAAc,WAAW,cAAc;AACpD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,cAAc,cAAc,gCAAgC,cAAc;AAC1E;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,oBAAoB;AAC3B,SAAS,eAAe;AACxB;AACA;AACA;AACA;AACA,iBAAiB,kBAAkB;AACnC;AACA,mBAAmB,gBAAgB;AACnC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL;AACA;AACA,qBAAqB,aAAa,YAAY;AAC9C,kBAAkB,qBAAqB;AACvC;AACA;AACA;AACA;AACA,kBAAkB,eAAe;AACjC;AACA;AACA,qBAAqB,oBAAoB;AACzC;AACA;AACA;AACA,QAAQ,qBAAqB;AAC7B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,uBAAuB,oBAAoB;AAC3C;AACA;AACA;AACA,QAAQ,qBAAqB;AAC7B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,yBAAyB;AAC7C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,gBAAgB,eAAe;AAC/B;AACA;AACA,oBAAoB,QAAQ;AAC5B;AACA;AACA,qBAAqB,GAAG,wBAAwB,GAAG,GAAG,wBAAwB;AAC9E;AACA,YAAY,GAAG,uBAAuB,GAAG,GAAG,wBAAwB,EAAE,GAAG,sBAAsB,GAAG,GAAG,sBAAsB;AAC3H;AACA,MAAM;AACN,qBAAqB,aAAa,QAAQ;AAC1C;AACA;AACA;AACA;AACA,mBAAM,CAAC,eAAe;AACtB,SAAS,cAAc;AACvB;AACA;AACA,mBAAM,CAAC,cAAc;AACrB,SAAS,qBAAqB;AAC9B;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,qBAAqB;AAC5B,SAAS,gBAAgB;AACzB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAmB,gBAAgB;AACnC;AACA;AACA;AACA,IAAI;AACJ;AACA;AACA;AACA,mBAAM,CAAC,gBAAgB;AACvB,SAAS,mBAAmB;AAC5B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,4DAA4D,UAAU;AACtE;AACA;AACA;AACA;AACA,oBAAoB,gBAAgB;AACpC;AACA,yBAAyB,cAAc;AACvC;AACA,oBAAoB,qBAAqB;AACzC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,iCAAiC,qBAAqB;AACtD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,WAAW,cAAc;AACzB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,iBAAiB,eAAe;AAChC;AACA;AACA;AACA;AACA,gCAAgC,kBAAkB,EAAE,OAAO,EAAE,iBAAiB;AAC9E,oEAAoE,GAAG,0BAA0B,cAAc,aAAa,IAAI,OAAO;AACvI;AACA;AACA;AACA;AACA,mBAAM,CAAC,mBAAmB;;AAE1B;AACA,SAAS,sBAAsB,gBAAgB,SAAS;AACxD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,EAAE,mBAAmB;AACrB;AACA;AACA;AACA,GAAG;AACH;AACA,mBAAM,CAAC,sBAAsB;;AAE7B;AACA,IAAI,mBAAmB;AACvB,IAAI,0BAA0B;AAC9B,IAAI,+BAA+B;AACnC,SAAS,wBAAwB;AACjC,kCAAkC,+BAA+B,EAAE,+BAA+B;AAClG;AACA,mBAAM,CAAC,wBAAwB;AAC/B,SAAS,sBAAsB;AAC/B,UAAU,sBAAsB;AAChC;AACA;AACA;AACA;AACA;AACA;AACA;AACA,+BAA+B,0BAA0B;AACzD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,sBAAsB;AAC7B,SAAS,yBAAyB;AAClC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,qDAAqD,mBAAmB;AACxE;AACA;AACA,UAAU,mBAAmB;AAC7B;AACA;AACA;AACA;AACA,uBAAuB,YAAY;AACnC;AACA;AACA,IAAI;AACJ;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,mBAAmB;AACvC;AACA;AACA,mBAAM,CAAC,yBAAyB;AAChC,SAAS,WAAW;AACpB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,iBAAiB,4BAA4B;AAC7C;AACA;AACA;AACA;AACA;AACA;AACA,WAAW;AACX;AACA,mBAAM,CAAC,WAAW;AAClB,SAAS,qBAAqB;AAC9B,UAAU,SAAS;AACnB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,MAAM;AACN;AACA,uDAAuD,wBAAwB;AAC/E;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,kCAAkC,wBAAwB;AAC1D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,OAAO;AACP;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,gEAAgE,wBAAwB;AACxF;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,UAAU;AACV;AACA,0BAA0B,eAAe;AACzC;AACA;AACA;AACA;AACA,0BAA0B,eAAe;AACzC;AACA;AACA;AACA;AACA,0BAA0B,WAAW;AACrC;AACA;AACA;AACA,cAAc;AACd;AACA;AACA;AACA;AACA;AACA;AACA,YAAY;AACZ;AACA;AACA,0BAA0B,WAAW;AACrC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,UAAU,sBAAsB;AAChC;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,qBAAqB;;AAE5B;AACA,IAAI,qBAAqB;AACzB,SAAS,oBAAoB;AAC7B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,MAAM,QAAQ,CAAC,qBAAqB,UAAU,SAAS;AACvD;AACA;AACA,sCAAsC,WAAW,GAAG,SAAS,GAAG,QAAQ;AACxE;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,4BAA4B,sBAAsB;AAClD;AACA;AACA;AACA;AACA;AACA;AACA,aAAa,QAAQ;AACrB;AACA;AACA;AACA;AACA;AACA;AACA,aAAa,QAAQ;AACrB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,oBAAoB;;AAE3B;AACA,IAAI,GAAG;AACP,SAAS,gBAAgB;AACzB;AACA;AACA;AACA;AACA,qCAAqC,cAAc,kBAAkB,0BAA0B;AAC/F;AACA,mBAAM,CAAC,gBAAgB;AACvB,SAAS,iBAAiB;AAC1B;AACA;AACA;AACA,mBAAmB,gBAAgB;AACnC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,iBAAiB;AACxB,SAAS,SAAS,iBAAiB,GAAG;AACtC;AACA;AACA,mBAAM,CAAC,SAAS;AAChB,SAAS,KAAK,iBAAiB,GAAG;AAClC;AACA;AACA,mBAAM,CAAC,KAAK;AACZ,SAAS,KAAK,iBAAiB,GAAG;AAClC;AACA;AACA,mBAAM,CAAC,KAAK;AACZ,SAAS,mBAAmB,iBAAiB,GAAG;AAChD,SAAS,KAAK;AACd;AACA,mBAAM,CAAC,mBAAmB;AAC1B,SAAS,iBAAiB,iBAAiB,GAAG;AAC9C,SAAS,KAAK;AACd;AACA,mBAAM,CAAC,iBAAiB;AACxB,SAAS,aAAa;AACtB;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,aAAa;AACpB,SAAS,4BAA4B,iBAAiB,GAAG;AACzD,sCAAsC,KAAK;AAC3C,WAAW,aAAa;AACxB;AACA,kCAAkC,KAAK;AACvC,WAAW,aAAa;AACxB;AACA;AACA;AACA,mBAAM,CAAC,4BAA4B;AACnC,SAAS,2BAA2B,mBAAmB,GAAG;AAC1D;AACA,kBAAkB,uBAAuB;AACzC;AACA;AACA,uBAAuB,mBAAmB;AAC1C,qBAAqB,iBAAiB;AACtC;AACA,oBAAoB,sCAAsC;AAC1D;AACA;AACA;AACA;AACA,mBAAM,CAAC,2BAA2B;AAClC,SAAS,oBAAoB,mBAAmB,GAAG;AACnD,mBAAmB,2BAA2B;AAC9C;AACA,kBAAkB,qBAAqB;AACvC;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,oBAAoB;AAC3B,SAAS,uBAAuB,mBAAmB,GAAG;AACtD;AACA;AACA;AACA,kBAAkB,SAAS;AAC3B,oBAAoB,wBAAwB;AAC5C;AACA;AACA;AACA;AACA,mBAAM,CAAC,uBAAuB;AAC9B,SAAS,yBAAyB,mBAAmB,GAAG;AACxD;AACA;AACA;AACA;AACA,gBAAgB,mBAAmB,qBAAqB,iBAAiB,qBAAqB,mBAAmB;AACjH;AACA,aAAa;AACb;AACA,gBAAgB,iBAAiB,qBAAqB,mBAAmB,qBAAqB,iBAAiB;AAC/G,mBAAmB,8BAA8B;AACjD;AACA,mBAAM,CAAC,yBAAyB;AAChC,SAAS,wBAAwB;AACjC;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,wBAAwB;AAC/B,SAAS,eAAe;AACxB;AACA;AACA,mBAAM,CAAC,eAAe;AACtB,SAAS,gBAAgB;AACzB;AACA;AACA,mBAAM,CAAC,gBAAgB;AACvB,SAAS,YAAY;AACrB;AACA;AACA,mBAAM,CAAC,YAAY;AACnB,SAAS,WAAW;AACpB;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,WAAW;AAClB,SAAS,kBAAkB;AAC3B;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,kBAAkB;AACzB,SAAS,gBAAgB;AACzB,SAAS,gBAAgB;AACzB;AACA,mBAAM,CAAC,gBAAgB;AACvB,SAAS,eAAe;AACxB;AACA;AACA,eAAe;AACf;AACA,eAAe;AACf;AACA,eAAe;AACf;AACA,eAAe;AACf;AACA;AACA,mBAAM,CAAC,eAAe;AACtB,SAAS,uBAAuB,+CAA+C,GAAG;AAClF;AACA;AACA;AACA;AACA;AACA,UAAU,KAAK;AACf;AACA;AACA;AACA,qBAAqB,mBAAmB,IAAI,mBAAmB;AAC/D;AACA;AACA,UAAU,KAAK;AACf;AACA;AACA;AACA,qBAAqB,mBAAmB,IAAI,mBAAmB;AAC/D;AACA;AACA;AACA;AACA;AACA,UAAU,KAAK;AACf;AACA;AACA;AACA,qBAAqB,mBAAmB,IAAI,mBAAmB;AAC/D;AACA;AACA;AACA;AACA;AACA,QAAQ,KAAK;AACb;AACA;AACA;AACA,mBAAmB,mBAAmB,IAAI,mBAAmB;AAC7D;AACA;AACA;AACA;AACA,gDAAgD,oBAAoB;AACpE;AACA;AACA;AACA,4CAA4C,oBAAoB;AAChE;AACA,mBAAM,CAAC,uBAAuB;AAC9B,SAAS,sBAAsB;AAC/B,uDAAuD,oBAAoB,IAAI,oBAAoB,iBAAiB,oBAAoB,IAAI,oBAAoB;AAChK;AACA,mBAAM,CAAC,sBAAsB;AAC7B,SAAS,qBAAqB;AAC9B;AACA;AACA;AACA;AACA;AACA;AACA,iBAAiB,iBAAiB;AAClC;AACA;AACA;AACA;AACA,yBAAyB,8BAA8B;AACvD;AACA,WAAW;AACX;AACA,mBAAM,CAAC,qBAAqB;AAC5B,SAAS,sBAAsB;AAC/B;AACA;AACA;AACA,iBAAiB,iBAAiB;AAClC;AACA;AACA;AACA,oBAAoB;AACpB;AACA;AACA,MAAM;AACN;AACA;AACA;AACA,WAAW;AACX;AACA,mBAAM,CAAC,sBAAsB;AAC7B,SAAS,sBAAsB,UAAU,2BAA2B,IAAI;AACxE;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,SAAS,kBAAkB;AAC3B,KAAK;AACL;AACA;AACA;AACA,mBAAM,CAAC,sBAAsB;AAC7B,SAAS,mBAAmB,+BAA+B,GAAG;AAC9D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,mBAAmB;AAC1B,SAAS,kBAAkB;AAC3B;AACA;AACA;AACA;AACA,QAAQ,wBAAwB;AAChC;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,kBAAkB;AACzB,SAAS,uBAAuB,2BAA2B,GAAG;AAC9D,cAAc,KAAK;AACnB,cAAc,KAAK;AACnB,cAAc,KAAK;AACnB,cAAc,KAAK;AACnB;AACA;AACA;AACA;AACA;AACA;AACA,wBAAwB,eAAe,IAAI;AAC3C,uBAAuB,eAAe,IAAI;AAC1C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,uBAAuB;AAC9B,SAAS,uBAAuB,2BAA2B,GAAG;AAC9D,cAAc,KAAK;AACnB,cAAc,KAAK;AACnB,cAAc,KAAK;AACnB,cAAc,KAAK;AACnB,oBAAoB,KAAK;AACzB,WAAW,aAAa;AACxB;AACA,oBAAoB,KAAK;AACzB,WAAW,aAAa;AACxB;AACA;AACA;AACA,mBAAM,CAAC,uBAAuB;AAC9B,SAAS,2BAA2B;AACpC,YAAY,GAAG;AACf;AACA,EAAE,IAAI;AACN;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,uBAAuB;AAC3C;AACA;AACA,iCAAiC,SAAS;AAC1C;AACA;AACA,UAAU,uBAAuB,2BAA2B,uBAAuB;AACnF;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,2BAA2B;AAClC,SAAS,+BAA+B,2BAA2B,GAAG;AACtE,iBAAiB,KAAK;AACtB,gBAAgB,KAAK;AACrB,iBAAiB,KAAK;AACtB,gBAAgB,KAAK;AACrB;AACA;AACA;AACA,2BAA2B,eAAe,IAAI;AAC9C,0BAA0B,eAAe,IAAI;AAC7C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,+BAA+B;AACtC,SAAS,eAAe;AACxB;AACA;AACA,sBAAsB,GAAG,iBAAiB,GAAG;AAC7C;AACA,mBAAM,CAAC,eAAe;AACtB,SAAS,uBAAuB;AAChC,MAAM,KAAK,eAAe,KAAK;AAC/B,WAAW,eAAe;AAC1B;AACA,MAAM,KAAK,eAAe,KAAK;AAC/B,WAAW,eAAe;AAC1B;AACA;AACA;AACA,mBAAM,CAAC,uBAAuB;AAC9B,SAAS,oBAAoB;AAC7B;AACA;AACA,kBAAkB,mBAAmB;AACrC;AACA;AACA;AACA;AACA,UAAU,SAAS;AACnB;AACA;AACA;AACA;AACA,UAAU,uBAAuB;AACjC;AACA;AACA;AACA;AACA;AACA;AACA,WAAW;AACX;AACA,mBAAM,CAAC,oBAAoB;AAC3B,SAAS,qBAAqB;AAC9B;AACA,kBAAkB,gBAAgB;AAClC;AACA;AACA,SAAS,KAAK,iBAAiB,KAAK;AACpC;AACA,0CAA0C,KAAK;AAC/C,0CAA0C,uBAAuB,IAAI;AACrE;AACA;AACA;AACA;AACA;AACA;AACA;AACA,kBAAkB,SAAS;AAC3B;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,qBAAqB;AAC5B,SAAS,gBAAgB;AACzB;AACA;AACA;AACA;AACA,sBAAsB,YAAY;AAClC,mBAAmB,oBAAoB;AACvC;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,gBAAgB;;AAEvB;AACA,IAAI,IAAI;AACR,IAAI,UAAU;AACd,IAAI,gBAAgB;AACpB,SAAS,kBAAkB;AAC3B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAmB,gBAAgB;AACnC,mBAAmB,gBAAgB;AACnC;AACA;AACA,mBAAM,CAAC,kBAAkB;AACzB,SAAS,iBAAiB;AAC1B,MAAM,KAAK,kBAAkB,IAAI;AACjC;AACA,aAAa;AACb;AACA,MAAM,KAAK,kBAAkB,IAAI;AACjC;AACA,aAAa;AACb;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,iBAAiB;AACxB,SAAS,YAAY;AACrB;AACA;AACA,8DAA8D,eAAe,6BAA6B,UAAU;AACpH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,gBAAgB,iBAAiB;AACjC;AACA;AACA,mBAAM,CAAC,YAAY;AACnB,SAAS,iCAAiC;AAC1C;AACA,oBAAoB,kBAAkB;AACtC;AACA;AACA;AACA;AACA;AACA,aAAa,YAAY;AACzB;AACA;AACA,aAAa,YAAY;AACzB;AACA,WAAW,gBAAgB,CAAC,qBAAqB;AACjD,WAAW,sCAAsC;AACjD,0BAA0B,gBAAgB,CAAC,qBAAqB;AAChE;AACA;AACA,mBAAM,CAAC,iCAAiC;AACxC,SAAS,sBAAsB;AAC/B,MAAM,KAAK,kBAAkB,IAAI;AACjC,6BAA6B,IAAI,4BAA4B,IAAI;AACjE;AACA;AACA;AACA,6BAA6B,IAAI;AACjC,iBAAiB;AACjB;AACA,8BAA8B,IAAI;AAClC,iBAAiB;AACjB;AACA;AACA;AACA,aAAa;AACb;AACA,MAAM,KAAK,kBAAkB,IAAI;AACjC,8BAA8B,IAAI,2BAA2B,IAAI;AACjE;AACA;AACA;AACA,4BAA4B,IAAI;AAChC,iBAAiB;AACjB;AACA,+BAA+B,IAAI;AACnC,iBAAiB;AACjB;AACA;AACA;AACA,aAAa;AACb;AACA;AACA;AACA,mBAAM,CAAC,sBAAsB;AAC7B,SAAS,qBAAqB;AAC9B;AACA,yCAAyC,qCAAqC;AAC9E;AACA,SAAS,SAAS,sBAAsB,IAAI;AAC5C;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,qBAAqB;AAC5B,SAAS,oBAAoB;AAC7B,mBAAmB,gBAAgB;AACnC,mBAAmB,gBAAgB;AACnC,sBAAsB,SAAS,IAAI;AACnC;AACA,mBAAM,CAAC,oBAAoB;AAC3B,SAAS,sBAAsB;AAC/B,UAAU,SAAS,EAAE,oBAAoB;AACzC;AACA;AACA,mBAAM,CAAC,sBAAsB;AAC7B,SAAS,eAAe;AACxB;AACA;AACA;AACA;AACA;AACA,WAAW;AACX;AACA,mBAAM,CAAC,eAAe;AACtB,SAAS,qBAAqB;AAC9B,+CAA+C,oBAAoB,oBAAoB,oBAAoB;AAC3G;AACA,mBAAM,CAAC,qBAAqB;AAC5B,SAAS,sBAAsB;AAC/B,wCAAwC,IAAI,6BAA6B,IAAI;AAC7E,yCAAyC,IAAI,4BAA4B,IAAI;AAC7E,MAAM,KAAK,qBAAqB,IAAI;AACpC,wCAAwC,IAAI;AAC5C;AACA;AACA,yCAAyC,IAAI;AAC7C;AACA;AACA;AACA,MAAM,KAAK,qBAAqB,IAAI;AACpC,uCAAuC,IAAI;AAC3C;AACA;AACA,0CAA0C,IAAI;AAC9C;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,sBAAsB;AAC7B,SAAS,gBAAgB;AACzB;AACA;AACA,mBAAM,CAAC,gBAAgB;AACvB,SAAS,sBAAsB;AAC/B;AACA,4BAA4B,sBAAsB;AAClD,4BAA4B,sBAAsB;AAClD,4BAA4B,gBAAgB;AAC5C,gBAAgB,qBAAqB;AACrC;AACA,4BAA4B,gBAAgB;AAC5C,gBAAgB,qBAAqB;AACrC;AACA,6BAA6B,eAAe;AAC5C;AACA,mBAAM,CAAC,sBAAsB;AAC7B,SAAS,+BAA+B;AACxC,gBAAgB,sBAAsB;AACtC;AACA;AACA;AACA;AACA;AACA,iCAAiC,IAAI;AACrC;AACA;AACA;AACA,MAAM,qBAAqB;AAC3B,MAAM;AACN;AACA,MAAM,qBAAqB;AAC3B,MAAM;AACN;AACA;AACA,mBAAM,CAAC,+BAA+B;AACtC,SAAS,sCAAsC;AAC/C;AACA;AACA;AACA;AACA,MAAM,KAAK,aAAa,IAAI;AAC5B,WAAW,+BAA+B;AAC1C;AACA,MAAM,KAAK,aAAa,IAAI;AAC5B,WAAW,+BAA+B;AAC1C;AACA;AACA;AACA,mBAAM,CAAC,sCAAsC;AAC7C,SAAS,qBAAqB;AAC9B,SAAS,gBAAgB,WAAW,kBAAkB,sBAAsB,gCAAgC,IAAI,GAAG,sBAAsB;AACzI;AACA,mBAAM,CAAC,qBAAqB;AAC5B,SAAS,wBAAwB;AACjC,wCAAwC,UAAU;AAClD,kCAAkC,qCAAqC;AACvE;AACA,+BAA+B,KAAK,kBAAkB,IAAI;AAC1D;AACA;AACA,gCAAgC,KAAK,kBAAkB,IAAI;AAC3D;AACA;AACA;AACA;AACA,MAAM;AACN;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,wBAAwB;AAC/B,SAAS,6BAA6B;AACtC;AACA;AACA;AACA;AACA;AACA;AACA,mBAAmB,qBAAqB;AACxC;AACA;AACA;AACA,eAAe,sBAAsB;AACrC;AACA;AACA;AACA,6BAA6B,gBAAgB;AAC7C,mBAAmB,qBAAqB;AACxC,MAAM,SAAS,qBAAqB,IAAI;AACxC;AACA;AACA,SAAS,wBAAwB;AACjC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,6BAA6B;AACpC,SAAS,oBAAoB;AAC7B,iDAAiD,IAAI,oCAAoC,IAAI;AAC7F,gDAAgD,IAAI,qCAAqC,IAAI;AAC7F,8BAA8B,IAAI,4BAA4B,IAAI;AAClE;AACA;AACA,iCAAiC,IAAI,+BAA+B,IAAI;AACxE;AACA;AACA,+BAA+B,IAAI,6BAA6B,IAAI;AACpE;AACA;AACA,gCAAgC,IAAI,8BAA8B,IAAI;AACtE;AACA;AACA;AACA;AACA,mBAAM,CAAC,oBAAoB;AAC3B,SAAS,aAAa;AACtB;AACA;AACA,aAAa,KAAK,WAAW,IAAI,oBAAoB,IAAI;AACzD;AACA,aAAa,KAAK,WAAW,IAAI,uBAAuB,IAAI;AAC5D;AACA,aAAa,KAAK,WAAW,IAAI,qBAAqB,IAAI;AAC1D;AACA,aAAa,KAAK,WAAW,IAAI,sBAAsB,IAAI;AAC3D;AACA;AACA,mBAAM,CAAC,aAAa;AACpB,SAAS,qBAAqB;AAC9B;AACA;AACA;AACA;AACA,kBAAkB,oBAAoB;AACtC,iBAAiB,aAAa;AAC9B;AACA;AACA;AACA;AACA;AACA,eAAe,oBAAoB;AACnC,cAAc,aAAa;AAC3B;AACA;AACA;AACA;AACA,mBAAM,CAAC,qBAAqB;AAC5B,SAAS,wBAAwB;AACjC;AACA;AACA,qBAAqB,qBAAqB;AAC1C;AACA,sBAAsB,sBAAsB;AAC5C;AACA;AACA;AACA;AACA,WAAW,qBAAqB;AAChC;AACA;AACA;AACA,qBAAqB,qBAAqB;AAC1C;AACA,sBAAsB,sBAAsB;AAC5C;AACA;AACA;AACA;AACA,WAAW,qBAAqB;AAChC;AACA,0BAA0B,sCAAsC;AAChE;AACA;AACA;AACA;AACA,WAAW,6BAA6B;AACxC;AACA;AACA,WAAW,6BAA6B;AACxC;AACA;AACA;AACA,mBAAM,CAAC,wBAAwB;AAC/B,SAAS,+BAA+B;AACxC;AACA,oBAAoB,kBAAkB;AACtC;AACA;AACA;AACA,kBAAkB,uBAAuB,iBAAiB,IAAI;AAC9D,mBAAmB,wBAAwB;AAC3C;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ,cAAc;AACtB;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA,mBAAM,CAAC,+BAA+B;;AAEtC;AACA,SAAS,YAAY;AACrB;AACA;AACA,mBAAM,CAAC,YAAY;AACnB,SAAS,sBAAsB;AAC/B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,sBAAsB;AAC7B,SAAS,UAAU;AACnB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,UAAU;AACjB,SAAS,iBAAiB;AAC1B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,WAAW;AACX;AACA,mBAAM,CAAC,iBAAiB;AACxB,SAAS,gBAAgB;AACzB;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,gBAAgB;AACvB,SAAS,0BAA0B;AACnC,mBAAmB,YAAY;AAC/B,6FAA6F,UAAU,gBAAgB,UAAU;AACjI;AACA;AACA,mBAAmB,iBAAiB;AACpC;AACA,MAAM,gBAAgB;AACtB;AACA;AACA;AACA,mBAAM,CAAC,0BAA0B;AACjC,SAAS,UAAU;AACnB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,iCAAiC,mBAAM;AACvC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,UAAU;AACjB,SAAS,yBAAyB;AAClC;AACA;AACA;AACA;AACA,SAAS,UAAU;AACnB;AACA,mBAAM,CAAC,yBAAyB;AAChC,SAAS,yBAAyB;AAClC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,EAAE,0BAA0B;AAC5B;AACA;AACA;AACA,MAAM,UAAU;AAChB;AACA;AACA;AACA,mBAAmB,YAAY;AAC/B;AACA;AACA;AACA;AACA;AACA,mBAAmB,sBAAsB;AACzC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAmB,iBAAiB;AACpC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,kBAAkB,uBAAuB;AACzC;AACA;AACA;AACA;AACA;AACA,MAAM;AACN;AACA;AACA;AACA;AACA;AACA,MAAM;AACN;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,IAAI,UAAU;AACd;AACA;AACA;AACA,mBAAM,CAAC,yBAAyB;;AAEhC;AACA,IAAI,IAAI;AACR,IAAI,gBAAgB;AACpB,IAAI,UAAU,GAAG,gBAAgB;AACjC,IAAI,UAAU,OAAO,UAAU,GAAG,UAAU,MAAM,UAAU,OAAO,UAAU;AAC7E,SAAS,gBAAgB;AACzB,UAAU,8BAA8B,EAAE,qBAAqB;AAC/D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,kBAAkB,yBAAyB,CAAC,uBAAuB,MAAM,IAAI,GAAG,IAAI;AACpF;AACA;AACA;AACA,YAAY,KAAK;AACjB;AACA,qBAAqB,mBAAmB,qBAAqB,IAAI;AACjE;AACA;AACA;AACA,YAAY,yDAAyD;AACrE;AACA;AACA;AACA;AACA;AACA,wBAAwB,UAAU;AAClC;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sCAAsC,IAAI,+BAA+B,IAAI;AAC7E;AACA;AACA,gBAAgB;AAChB,gBAAgB;AAChB,gBAAgB;AAChB,QAAQ;AACR;AACA;AACA;AACA,qCAAqC,IAAI,gCAAgC,IAAI;AAC7E;AACA;AACA,gBAAgB;AAChB,gBAAgB;AAChB,gBAAgB;AAChB;AACA,iCAAiC,SAAS,WAAW,IAAI;AACzD,kCAAkC,SAAS,WAAW,IAAI;AAC1D;AACA;AACA;AACA,iCAAiC,kBAAkB;AACnD;AACA;AACA,kCAAkC,kBAAkB;AACpD;AACA;AACA,uDAAuD,2BAA2B;AAClF,iBAAiB,IAAI;AACrB;AACA,OAAO;AACP,yDAAyD,2BAA2B;AACpF,iBAAiB,IAAI;AACrB;AACA,OAAO;AACP;AACA;AACA;AACA;AACA;AACA,QAAQ;AACR;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,gBAAgB;;AAEvB;AACA,SAAS,yBAAyB;AAClC;AACA;AACA;AACA,UAAU,4CAA4C,EAAE,sBAAsB;AAC9E;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,gBAAgB,uBAAuB;AACvC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,wBAAwB,mBAAmB;AAC3C,uBAAuB,iBAAiB;AACxC,0BAA0B,mBAAmB;AAC7C,yBAAyB,iBAAiB;AAC1C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,gBAAgB;AACpC;AACA;AACA;AACA;AACA;AACA;AACA;AACA,kBAAkB;AAClB,iBAAiB;AACjB,MAAM;AACN;AACA,kBAAkB;AAClB,iBAAiB;AACjB;AACA,QAAQ,kBAAkB;AAC1B;AACA;AACA,QAAQ,kBAAkB;AAC1B;AACA;AACA;AACA;AACA,4BAA4B,gBAAgB;AAC5C,qBAAqB,eAAe;AACpC;AACA;AACA;AACA,0CAA0C,mBAAM,cAAc,eAAe,GAAG,eAAe,GAAG,eAAe,GAAG,eAAe;AACnI;AACA,oBAAoB,oBAAoB;AACxC;AACA;AACA,oDAAoD,mBAAM;AAC1D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,wBAAwB,qBAAqB;AAC7C;AACA;AACA;AACA;AACA;AACA,cAAc,+BAA+B;AAC7C;AACA;AACA;AACA;AACA;AACA,KAAK;AACL;AACA;AACA;AACA;AACA;AACA;AACA,UAAU,kBAAkB;AAC5B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,0BAA0B,uBAAuB;AACjD;AACA;AACA;AACA,4BAA4B,KAAK;AACjC,2BAA2B,KAAK;AAChC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,yBAAyB;;AAEhC;AACA,IAAI,SAAS;AACb,IAAI,UAAU;AACd,IAAI,WAAW,GAAG,2BAA2B;AAC7C,IAAI,mBAAmB,mBAAmB,mBAAM,WAAW,KAAK,OAAO,SAAS,KAAK,KAAK,OAAO,SAAS;AAC1G,SAAS,mCAAmC;AAC5C;AACA;AACA,wCAAwC,mBAAM;AAC9C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,eAAe;AACf;AACA;AACA;AACA;AACA,aAAa;AACb,GAAG;AACH,0CAA0C,mBAAM;AAChD,mBAAmB,uBAAuB;AAC1C;AACA;AACA;AACA;AACA;AACA,wBAAwB,gBAAgB;AACxC;AACA;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ,mBAAmB;AAC3B;AACA;AACA,QAAQ,KAAK,oBAAoB,SAAS;AAC1C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ,KAAK,oBAAoB,SAAS;AAC1C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,iDAAiD,mBAAM;AACvD,2CAA2C,mBAAM;AACjD;AACA;AACA;AACA;AACA;AACA,kCAAkC,KAAK;AACvC;AACA;AACA,8BAA8B,KAAK;AACnC,GAAG;AACH,oDAAoD,mBAAM;AAC1D;AACA;AACA;AACA;AACA,qBAAqB,UAAU;AAC/B,GAAG;AACH,mDAAmD,mBAAM;AACzD;AACA;AACA;AACA;AACA,iBAAiB,UAAU;AAC3B;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,2CAA2C,mBAAM;AACjD,mBAAmB,uBAAuB;AAC1C;AACA;AACA;AACA,yDAAyD,iDAAiD,IAAI;AAC9G,wDAAwD,+CAA+C,IAAI;AAC3G,oDAAoD,mBAAM;AAC1D;AACA,eAAe,KAAK,iCAAiC,SAAS;AAC9D;AACA;AACA,eAAe,KAAK,iCAAiC,SAAS;AAC9D;AACA;AACA,KAAK;AACL;AACA;AACA;AACA;AACA,sDAAsD,SAAS,0BAA0B,SAAS;AAClG;AACA;AACA,mBAAmB,mBAAmB;AACtC;AACA;AACA;AACA;AACA,mDAAmD,SAAS,0CAA0C,SAAS;AAC/G;AACA;AACA,qBAAqB,mBAAmB;AACxC;AACA;AACA;AACA,GAAG;AACH,2DAA2D,mBAAM;AACjE;AACA,mBAAmB,uBAAuB;AAC1C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,WAAW,KAAK,OAAO,SAAS,mEAAmE,KAAK,OAAO,SAAS;AACxH,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,0BAA0B,eAAe;AACzC;AACA;AACA,oBAAoB,4BAA4B;AAChD,0BAA0B,4BAA4B;AACtD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,SAAS;AACT;AACA;AACA;AACA;AACA;AACA;AACA,+CAA+C,iCAAiC;AAChF;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,mCAAmC;AAC1C,SAAS,mCAAmC;AAC5C;AACA;AACA,UAAU,4CAA4C,EAAE,sBAAsB;AAC9E;AACA;AACA,0CAA0C,mBAAM;AAChD;AACA;AACA,8BAA8B,WAAW;AACzC;AACA;AACA;AACA;AACA;AACA,UAAU,kBAAkB;AAC5B;AACA;AACA,UAAU,kBAAkB;AAC5B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mCAAmC,WAAW,CAAC,uBAAuB;AACtE,cAAc,4BAA4B,yCAAyC,UAAU;AAC7F;AACA;AACA,cAAc,+BAA+B;AAC7C;AACA;AACA;AACA;AACA,YAAY,SAAS;AACrB;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,wCAAwC,mBAAM;AAC9C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mCAAmC,mBAAmB,YAAY,iBAAiB,YAAY,mBAAmB,YAAY,iBAAiB,YAAY,KAAK,SAAS,SAAS,KAAK,KAAK,SAAS,SAAS,KAAK,KAAK,SAAS,SAAS;AAC1O,qCAAqC,iBAAiB,YAAY,mBAAmB,YAAY,iBAAiB,YAAY,mBAAmB,YAAY,KAAK,SAAS,SAAS,KAAK,KAAK,SAAS,SAAS,KAAK,KAAK,SAAS,SAAS;AAC5O;AACA,aAAa,uBAAuB;AACpC;AACA;AACA;AACA;AACA;AACA,2BAA2B,iBAAiB,YAAY,mBAAmB,YAAY,iBAAiB,YAAY,mBAAmB,YAAY,iBAAiB,YAAY,KAAK,SAAS,SAAS,KAAK,KAAK,SAAS,SAAS,KAAK,KAAK,SAAS,SAAS;AAC/P,6BAA6B,mBAAmB,YAAY,iBAAiB,YAAY,mBAAmB,YAAY,iBAAiB,YAAY,mBAAmB,YAAY,KAAK,SAAS,SAAS,KAAK,KAAK,SAAS,SAAS,KAAK,KAAK,SAAS,SAAS;AACnQ;AACA;AACA;AACA,WAAW,uBAAuB;AAClC,GAAG;AACH,0BAA0B,4BAA4B;AACtD;AACA;AACA;AACA;AACA;AACA,qBAAqB,uBAAuB;AAC5C,sBAAsB,wBAAwB;AAC9C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,mCAAmC;AAC1C,SAAS,gCAAgC;AACzC;AACA;AACA;AACA,UAAU,4CAA4C,EAAE,sBAAsB;AAC9E;AACA;AACA;AACA,oCAAoC,mBAAM,yCAAyC,uBAAuB;AAC1G;AACA;AACA,8CAA8C,mBAAM;AACpD;AACA,oBAAoB,yBAAyB;AAC7C,4BAA4B,WAAW;AACvC,0BAA0B,yBAAyB;AACnD,+BAA+B,WAAW;AAC1C;AACA;AACA;AACA;AACA,gBAAgB,+BAA+B;AAC/C;AACA;AACA;AACA;AACA,cAAc,SAAS;AACvB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,qCAAqC,mBAAM;AAC3C,qBAAqB,WAAW;AAChC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,2CAA2C,mBAAM;AACjD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,yBAAyB,aAAa;AACtC,2BAA2B,UAAU;AACrC;AACA,uBAAuB,aAAa;AACpC,yBAAyB,UAAU;AACnC,KAAK;AACL,GAAG;AACH,gDAAgD,mBAAM;AACtD,+CAA+C,UAAU;AACzD;AACA;AACA;AACA,MAAM;AACN;AACA;AACA,MAAM;AACN;AACA;AACA,uBAAuB,gBAAgB,CAAC,uBAAuB;AAC/D,WAAW,WAAW;AACtB,GAAG;AACH,0CAA0C,mBAAM;AAChD;AACA;AACA;AACA,8BAA8B,WAAW;AACzC;AACA;AACA;AACA;AACA,UAAU,kBAAkB;AAC5B;AACA;AACA,UAAU,kBAAkB;AAC5B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mCAAmC,WAAW;AAC9C,cAAc,4BAA4B,yCAAyC,UAAU;AAC7F;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,0BAA0B,4BAA4B;AACtD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,gCAAgC;AACvC,SAAS,+BAA+B;AACxC;AACA,yCAAyC,mBAAM;AAC/C;AACA;AACA;AACA;AACA,aAAa;AACb,GAAG;AACH,0CAA0C,mBAAM;AAChD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,aAAa;AACb,GAAG;AACH,2DAA2D,mBAAM;AACjE;AACA;AACA;AACA;AACA,wBAAwB,SAAS,uBAAuB,SAAS;AACjE;AACA;AACA,WAAW,aAAa,qDAAqD,UAAU;AACvF,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAmB,uBAAuB;AAC1C,0BAA0B,WAAW;AACrC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,kBAAkB,SAAS;AAC3B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,+BAA+B;AACtC,SAAS,kCAAkC;AAC3C;AACA,yCAAyC,mBAAM;AAC/C;AACA;AACA;AACA;AACA,aAAa;AACb,GAAG;AACH,2CAA2C,mBAAM;AACjD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,aAAa;AACb,GAAG;AACH,yDAAyD,mBAAM;AAC/D;AACA;AACA;AACA;AACA,yBAAyB,SAAS,sBAAsB,SAAS;AACjE;AACA;AACA,WAAW,aAAa,qDAAqD,UAAU;AACvF,GAAG;AACH,2DAA2D,mBAAM;AACjE;AACA;AACA;AACA;AACA,wBAAwB,SAAS,uBAAuB,SAAS;AACjE;AACA;AACA,WAAW,aAAa,qDAAqD,UAAU;AACvF,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAmB,uBAAuB;AAC1C,0BAA0B,WAAW;AACrC;AACA;AACA;AACA,UAAU;AACV;AACA;AACA;AACA;AACA;AACA;AACA,mBAAmB,SAAS;AAC5B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,kCAAkC;AACzC,SAAS,6CAA6C;AACtD;AACA;AACA,UAAU,gBAAgB,EAAE,sBAAsB;AAClD;AACA,+CAA+C,mBAAM,qDAAqD,uBAAuB;AACjI;AACA;AACA,wCAAwC,mBAAM;AAC9C;AACA,oBAAoB,yBAAyB;AAC7C,4BAA4B,WAAW;AACvC,0BAA0B,yBAAyB;AACnD,+BAA+B,WAAW;AAC1C;AACA;AACA,gBAAgB,+BAA+B;AAC/C;AACA;AACA;AACA;AACA,cAAc,SAAS;AACvB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,qCAAqC,mBAAM;AAC3C,yBAAyB,oBAAoB;AAC7C;AACA;AACA,0CAA0C,mBAAM;AAChD;AACA;AACA;AACA;AACA;AACA;AACA,SAAS,mBAAmB,sBAAsB,SAAS,MAAM,iBAAiB,sBAAsB,SAAS;AACjH;AACA;AACA,aAAa;AACb,GAAG;AACH,uDAAuD,mBAAM;AAC7D;AACA;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ,mBAAmB,mBAAmB,SAAS;AACvD,oBAAoB;AACpB,MAAM,SAAS,iBAAiB,mBAAmB,SAAS;AAC5D,oBAAoB;AACpB,MAAM;AACN;AACA;AACA,sBAAsB,gBAAgB;AACtC,MAAM,uBAAuB;AAC7B;AACA,WAAW,WAAW;AACtB,GAAG;AACH,yCAAyC,mBAAM;AAC/C;AACA;AACA;AACA,0BAA0B,WAAW;AACrC,UAAU,kBAAkB;AAC5B;AACA;AACA;AACA;AACA,GAAG;AACH,6CAA6C,mBAAM;AACnD;AACA;AACA;AACA;AACA,qCAAqC,WAAW;AAChD,mCAAmC,WAAW;AAC9C,cAAc,4BAA4B,yCAAyC,UAAU;AAC7F;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,0CAA0C,mBAAM;AAChD,6CAA6C,mBAAM;AACnD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,0BAA0B,4BAA4B;AACtD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sBAAsB,6BAA6B;AACnD,4BAA4B,6BAA6B;AACzD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,6CAA6C;AACpD,SAAS,oCAAoC;AAC7C;AACA;AACA;AACA;AACA,UAAU,4CAA4C,EAAE,sBAAsB;AAC9E;AACA;AACA;AACA,+CAA+C,mBAAM,qDAAqD,uBAAuB;AACjI;AACA;AACA,8CAA8C,mBAAM;AACpD;AACA,oBAAoB,yBAAyB;AAC7C,4BAA4B,WAAW;AACvC,0BAA0B,yBAAyB;AACnD,+BAA+B,WAAW;AAC1C;AACA;AACA,gBAAgB,+BAA+B;AAC/C;AACA;AACA;AACA;AACA,cAAc,SAAS;AACvB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,qCAAqC,mBAAM;AAC3C,yBAAyB,oBAAoB;AAC7C;AACA;AACA,2CAA2C,mBAAM;AACjD;AACA;AACA;AACA;AACA,0BAA0B,gBAAgB;AAC1C,0BAA0B,gBAAgB;AAC1C,0BAA0B,WAAW;AACrC,GAAG;AACH,iDAAiD,mBAAM;AACvD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,uCAAuC,SAAS,iCAAiC,SAAS;AAC1F;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sCAAsC,SAAS,iCAAiC,SAAS;AACzF;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,+CAA+C,mBAAM;AACrD;AACA;AACA;AACA,4BAA4B,WAAW;AACvC;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,wCAAwC,mBAAM,0EAA0E,aAAa,gCAAgC,UAAU;AAC/K,8CAA8C,mBAAM;AACpD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,0CAA0C,mBAAM;AAChD;AACA;AACA,iEAAiE,SAAS;AAC1E;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,mDAAmD,mBAAM;AACzD;AACA;AACA;AACA;AACA;AACA;AACA,oCAAoC,mBAAM;AAC1C;AACA,8EAA8E,SAAS;AACvF;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,OAAO;AACP;AACA;AACA;AACA,oBAAoB,oBAAoB;AACxC,0BAA0B,oBAAoB;AAC9C;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,oDAAoD,mBAAM;AAC1D;AACA;AACA;AACA,iFAAiF,wBAAwB;AACzG;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA,yBAAyB,gBAAgB,CAAC,uBAAuB;AACjE,UAAU,WAAW;AACrB;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,0CAA0C,mBAAM;AAChD;AACA;AACA;AACA;AACA;AACA,4BAA4B,WAAW;AACvC,YAAY,kBAAkB;AAC9B;AACA;AACA,YAAY,kBAAkB;AAC9B;AACA;AACA;AACA;AACA,oBAAoB,yBAAyB;AAC7C;AACA;AACA,4BAA4B,WAAW;AACvC,0BAA0B,yBAAyB;AACnD;AACA;AACA;AACA;AACA,+BAA+B,WAAW;AAC1C;AACA;AACA,gBAAgB,4BAA4B,sCAAsC,UAAU;AAC5F;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,0BAA0B,4BAA4B;AACtD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,oCAAoC;AAC3C,SAAS,+BAA+B;AACxC;AACA;AACA,UAAU,4CAA4C,EAAE,sBAAsB;AAC9E;AACA;AACA;AACA,oCAAoC,mBAAM,yCAAyC,uBAAuB;AAC1G;AACA;AACA,qCAAqC,mBAAM,aAAa,WAAW;AACnE;AACA;AACA;AACA,GAAG;AACH,8CAA8C,mBAAM;AACpD;AACA,oBAAoB,yBAAyB;AAC7C,4BAA4B,WAAW;AACvC,0BAA0B,yBAAyB;AACnD,+BAA+B,WAAW;AAC1C;AACA;AACA;AACA;AACA,gBAAgB,+BAA+B;AAC/C;AACA;AACA;AACA;AACA,cAAc,SAAS;AACvB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,qDAAqD,mBAAM;AAC3D;AACA;AACA;AACA,yBAAyB,aAAa,qDAAqD,UAAU;AACrG;AACA;AACA;AACA;AACA,yBAAyB,aAAa,qDAAqD,UAAU;AACrG;AACA;AACA,GAAG;AACH,2CAA2C,mBAAM;AACjD;AACA;AACA;AACA;AACA;AACA;AACA,0BAA0B,gBAAgB;AAC1C;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,+CAA+C,mBAAM;AACrD;AACA;AACA;AACA;AACA;AACA;AACA;AACA,kBAAkB,mBAAmB,SAAS,SAAS,KAAK,iBAAiB,SAAS,SAAS,KAAK,mBAAmB,SAAS,SAAS;AACzI,kBAAkB,iBAAiB,SAAS,SAAS,KAAK,mBAAmB,SAAS,SAAS,KAAK,iBAAiB,SAAS,SAAS;AACvI;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,KAAK,SAAS,SAAS,KAAK,KAAK,SAAS,SAAS;AACvE,QAAQ,kBAAkB;AAC1B,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA,4CAA4C,gBAAgB,CAAC,uBAAuB;AACpF,UAAU,WAAW;AACrB;AACA;AACA,qCAAqC,SAAS,YAAY,SAAS;AACnE;AACA;AACA,8CAA8C,mBAAmB,GAAG,mBAAmB;AACvF;AACA;AACA;AACA;AACA;AACA,KAAK;AACL,GAAG;AACH,0CAA0C,mBAAM;AAChD;AACA;AACA;AACA;AACA,0BAA0B,WAAW;AACrC,UAAU,kBAAkB;AAC5B;AACA;AACA,UAAU,kBAAkB;AAC5B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,qCAAqC,WAAW;AAChD,mCAAmC,WAAW;AAC9C,cAAc,4BAA4B,yCAAyC,UAAU;AAC7F;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,0BAA0B,4BAA4B;AACtD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,2BAA2B,oBAAoB,gBAAgB,SAAS;AACxE;AACA,0BAA0B,mCAAmC;AAC7D;AACA,iCAAiC,oBAAoB,YAAY,SAAS;AAC1E;AACA,sIAAsI,SAAS;AAC/I;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,+BAA+B;AACtC,SAAS,kCAAkC;AAC3C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,iBAAiB,gBAAgB;AACjC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL;AACA;AACA;AACA;AACA;AACA,mDAAmD,8BAA8B;AACjF;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,+CAA+C,mBAAM;AACrD;AACA;AACA,0BAA0B,gCAAgC;AAC1D;AACA;AACA;AACA,GAAG;AACH,+CAA+C,mBAAM,qDAAqD,uBAAuB;AACjI;AACA;AACA,uDAAuD,mBAAM;AAC7D;AACA;AACA;AACA,YAAY,+BAA+B;AAC3C;AACA;AACA;AACA;AACA,UAAU,SAAS;AACnB;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,oDAAoD,mBAAM,iDAAiD,WAAW,SAAS,WAAW;AAC1I,2CAA2C,mBAAM;AACjD;AACA;AACA;AACA;AACA,oCAAoC,mBAAM;AAC1C;AACA;AACA;AACA;AACA,KAAK;AACL,oBAAoB,yBAAyB;AAC7C;AACA;AACA,0BAA0B,yBAAyB;AACnD;AACA;AACA;AACA;AACA;AACA;AACA;AACA,uBAAuB,iCAAiC;AACxD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,wDAAwD,mBAAM;AAC9D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,yBAAyB;AAC7C;AACA;AACA;AACA,0BAA0B,yBAAyB;AACnD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,6CAA6C,mBAAM;AACnD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,yCAAyC,mBAAM;AAC/C;AACA;AACA,2CAA2C,mBAAM;AACjD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,6DAA6D,mBAAM;AACnE;AACA;AACA;AACA,iDAAiD,mBAAM;AACvD;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,qCAAqC,mBAAM;AAC3C;AACA;AACA,GAAG;AACH,qCAAqC,mBAAM;AAC3C,yBAAyB,oBAAoB;AAC7C;AACA;AACA,sCAAsC,mBAAM;AAC5C;AACA;AACA;AACA,iDAAiD,mBAAM;AACvD,yBAAyB,WAAW;AACpC;AACA;AACA;AACA;AACA;AACA,mCAAmC,WAAW;AAC9C,cAAc,4BAA4B,yCAAyC,UAAU;AAC7F;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,uCAAuC,mBAAM;AAC7C;AACA;AACA;AACA,0BAA0B,WAAW;AACrC,UAAU,kBAAkB;AAC5B;AACA;AACA;AACA;AACA,GAAG;AACH,kDAAkD,mBAAM;AACxD,sBAAsB,gBAAgB,CAAC,uBAAuB;AAC9D,QAAQ,WAAW;AACnB;AACA;AACA,GAAG;AACH,2CAA2C,mBAAM;AACjD,gDAAgD,mBAAM;AACtD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,gDAAgD,mBAAM;AACtD;AACA;AACA;AACA,4BAA4B,gCAAgC;AAC5D;AACA;AACA,UAAU,sBAAsB;AAChC;AACA;AACA;AACA,GAAG;AACH,4DAA4D,mBAAM;AAClE;AACA;AACA;AACA;AACA,YAAY,qBAAqB;AACjC,YAAY,sBAAsB;AAClC,YAAY,qBAAqB;AACjC;AACA;AACA;AACA;AACA,GAAG;AACH,4DAA4D,mBAAM;AAClE;AACA;AACA;AACA;AACA,YAAY,qBAAqB;AACjC,YAAY,sBAAsB;AAClC,YAAY,qBAAqB;AACjC;AACA;AACA;AACA;AACA,GAAG;AACH,sDAAsD,mBAAM;AAC5D;AACA;AACA;AACA;AACA;AACA;AACA,cAAc,uBAAuB;AACrC,cAAc,wBAAwB;AACtC,cAAc,wBAAwB;AACtC,cAAc,uBAAuB;AACrC;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,oDAAoD,mBAAM;AAC1D;AACA;AACA;AACA;AACA;AACA;AACA,cAAc,uBAAuB;AACrC,cAAc,wBAAwB;AACtC,cAAc,wBAAwB;AACtC,cAAc,uBAAuB;AACrC;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,+CAA+C,mBAAM;AACrD;AACA,yCAAyC,uBAAuB;AAChE,8CAA8C,mBAAmB,GAAG,mBAAmB;AACvF;AACA;AACA;AACA;AACA;AACA,KAAK;AACL,GAAG;AACH,kDAAkD,mBAAM;AACxD;AACA,iBAAiB,uBAAuB,qCAAqC,SAAS;AACtF;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,MAAM;AACN;AACA,MAAM;AACN;AACA,MAAM;AACN;AACA;AACA;AACA,GAAG;AACH,mEAAmE,mBAAM;AACzE;AACA;AACA;AACA,kBAAkB,eAAe;AACjC;AACA;AACA;AACA;AACA;AACA,YAAY,0BAA0B;AACtC,YAAY,oBAAoB;AAChC;AACA;AACA;AACA;AACA;AACA;AACA,cAAc,0BAA0B;AACxC,cAAc,0BAA0B;AACxC,cAAc,0BAA0B;AACxC;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,qEAAqE,mBAAM;AAC3E;AACA;AACA;AACA,kBAAkB,eAAe;AACjC;AACA;AACA;AACA;AACA;AACA,YAAY,0BAA0B;AACtC,YAAY,oBAAoB;AAChC;AACA;AACA;AACA;AACA;AACA;AACA,cAAc,0BAA0B;AACxC,cAAc,0BAA0B;AACxC,cAAc,0BAA0B;AACxC;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,iEAAiE,mBAAM;AACvE;AACA;AACA;AACA;AACA;AACA;AACA,mBAAmB,uBAAuB;AAC1C;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ,iBAAiB,mBAAmB,SAAS;AACrD;AACA,MAAM,SAAS,mBAAmB,mBAAmB,SAAS;AAC9D;AACA;AACA;AACA,GAAG;AACH,4CAA4C,mBAAM;AAClD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sBAAsB,eAAe;AACrC;AACA;AACA,uDAAuD,eAAe;AACtE;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,gDAAgD,mBAAM,iDAAiD,WAAW;AAClH,kDAAkD,mBAAM;AACxD;AACA;AACA;AACA;AACA;AACA,uDAAuD,WAAW;AAClE;AACA;AACA,4BAA4B,4BAA4B,yCAAyC,UAAU;AAC3G;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,4CAA4C,mBAAM;AAClD;AACA,kEAAkE,gBAAgB,CAAC,uBAAuB;AAC1G;AACA;AACA;AACA,8CAA8C,mBAAmB,GAAG,mBAAmB;AACvF;AACA;AACA;AACA;AACA;AACA,KAAK;AACL,gCAAgC,WAAW;AAC3C;AACA;AACA;AACA;AACA;AACA;AACA;AACA,qCAAqC,WAAW;AAChD;AACA;AACA;AACA;AACA;AACA;AACA,eAAe,oBAAoB,YAAY,SAAS;AACxD,oBAAoB,oBAAoB;AACxC;AACA;AACA,KAAK,YAAY,WAAW;AAC5B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL,GAAG;AACH,4CAA4C,mBAAM;AAClD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,uDAAuD,WAAW;AAClE;AACA;AACA;AACA,GAAG;AACH,4CAA4C,mBAAM;AAClD;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,+CAA+C,mBAAM;AACrD;AACA,yBAAyB,4BAA4B,sCAAsC,UAAU;AACrG;AACA;AACA,sDAAsD,mBAAM;AAC5D,+CAA+C,mBAAM;AACrD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,4CAA4C,mBAAM;AAClD,iDAAiD,mBAAM;AACvD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,gDAAgD,mBAAM;AACtD;AACA;AACA;AACA;AACA;AACA,wCAAwC,oBAAoB;AAC5D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oCAAoC,kBAAkB;AACtD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sBAAsB,oBAAoB;AAC1C;AACA,4BAA4B,oBAAoB;AAChD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,0BAA0B,4BAA4B;AACtD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,+BAA+B,oBAAoB,6BAA6B,SAAS;AACzF;AACA;AACA;AACA;AACA,+BAA+B,oBAAoB,YAAY,SAAS;AACxE;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,kCAAkC;;AAEzC;AACA,IAAI,IAAI;AACR,IAAI,WAAW;AACf,SAAS,qBAAqB;AAC9B,UAAU,8BAA8B,EAAE,qBAAqB;AAC/D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,wDAAwD,mBAAM;AAC9D;AACA,iBAAiB,uBAAuB,qCAAqC,IAAI;AACjF;AACA;AACA;AACA;AACA,iBAAiB,sBAAsB;AACvC;AACA;AACA,GAAG;AACH,uCAAuC,mBAAM;AAC7C,oBAAoB,oBAAoB;AACxC;AACA;AACA,UAAU,kBAAkB;AAC5B;AACA;AACA;AACA;AACA,GAAG;AACH,4CAA4C,mBAAM;AAClD;AACA,yBAAyB,2BAA2B,OAAO,IAAI;AAC/D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mCAAmC,2BAA2B,WAAW,IAAI;AAC7E,cAAc,uBAAuB;AACrC;AACA;AACA;AACA;AACA,YAAY,IAAI;AAChB,YAAY,IAAI;AAChB;AACA;AACA;AACA;AACA,cAAc,4BAA4B,4BAA4B,IAAI,KAAK,WAAW;AAC1F;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA,4CAA4C,mBAAM;AAClD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA,uCAAuC,mBAAM;AAC7C;AACA,kBAAkB,cAAc;AAChC;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,wCAAwC,mBAAM;AAC9C;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,yBAAyB,oBAAoB,MAAM,IAAI;AACvD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sBAAsB,eAAe;AACrC;AACA;AACA;AACA;AACA,wBAAwB,eAAe;AACvC;AACA;AACA;AACA;AACA,4BAA4B,oBAAoB,OAAO,IAAI;AAC3D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,qBAAqB;;AAE5B;AACA,IAAI,IAAI;AACR,IAAI,uBAAuB;AAC3B,IAAI,2BAA2B;AAC/B,SAAS,sBAAsB;AAC/B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,YAAY,IAAI;AAChB;AACA;AACA,sBAAsB,IAAI;AAC1B,uCAAuC,uBAAuB;AAC9D;AACA;AACA;AACA,mBAAmB,2BAA2B;AAC9C,sBAAsB,2BAA2B;AACjD;AACA;AACA,sBAAsB,IAAI;AAC1B,uCAAuC,uBAAuB;AAC9D;AACA,oBAAoB,2BAA2B;AAC/C,qBAAqB,2BAA2B;AAChD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,sBAAsB;AAC7B,SAAS,aAAa;AACtB;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,aAAa;AACpB,SAAS,sBAAsB;AAC/B,qBAAqB,uBAAuB;AAC5C,sBAAsB,sBAAsB;AAC5C,oBAAoB,sBAAsB;AAC1C;AACA,0BAA0B,YAAY,OAAO,aAAa;AAC1D;AACA;AACA,mBAAM,CAAC,sBAAsB;AAC7B,SAAS,sBAAsB;AAC/B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,oBAAoB;AACxC,6BAA6B,8CAA8C;AAC3E;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,gBAAgB;AACpC;AACA;AACA;AACA;AACA,SAAS;AACT;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,iBAAiB,gBAAgB;AACjC;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL;AACA;AACA;AACA;AACA,mDAAmD,mBAAM;AACzD,qBAAqB,WAAW;AAChC,iBAAiB,mBAAmB;AACpC;AACA;AACA;AACA,UAAU,YAAY;AACtB;AACA;AACA;AACA;AACA,GAAG;AACH,mDAAmD,mBAAM;AACzD,qBAAqB,WAAW;AAChC;AACA;AACA;AACA;AACA,UAAU,wBAAwB;AAClC;AACA;AACA;AACA;AACA,GAAG;AACH,gDAAgD,mBAAM;AACtD;AACA,6CAA6C,mBAAM;AACnD,iBAAiB,qBAAqB;AACtC,UAAU,gBAAgB;AAC1B;AACA;AACA;AACA;AACA,GAAG;AACH,8CAA8C,mBAAM;AACpD,8CAA8C,YAAY;AAC1D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,oBAAoB;AACxC;AACA;AACA;AACA;AACA,eAAe,IAAI,SAAS,IAAI;AAChC;AACA;AACA,gBAAgB,IAAI,UAAU,IAAI;AAClC;AACA;AACA;AACA;AACA;AACA,2BAA2B,IAAI;AAC/B;AACA;AACA,OAAO;AACP;AACA;AACA;AACA;AACA;AACA;AACA;AACA,yCAAyC,mBAAM;AAC/C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,OAAO;AACP,KAAK;AACL;AACA;AACA;AACA,sCAAsC,mBAAM;AAC5C;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL,kCAAkC,mBAAM;AACxC,qDAAqD,mBAAM,4CAA4C,IAAI,+BAA+B,IAAI,6BAA6B,IAAI,gCAAgC,IAAI;AACnN,+CAA+C,mBAAM;AACrD,2BAA2B,kBAAkB;AAC7C;AACA;AACA,iBAAiB;AACjB;AACA,gDAAgD,MAAM;AACtD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,0BAA0B,kBAAkB;AAC5C,+DAA+D,sEAAsE;AACrI,KAAK;AACL,iDAAiD,mBAAM;AACvD,yDAAyD,mBAAM;AAC/D;AACA;AACA;AACA;AACA,uDAAuD,IAAI;AAC3D;AACA;AACA;AACA;AACA;AACA,qDAAqD,IAAI;AACzD;AACA;AACA;AACA;AACA,KAAK;AACL,oCAAoC,mBAAM;AAC1C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,cAAc,sBAAsB;AACpC;AACA;AACA;AACA;AACA;AACA;AACA,qBAAqB;AACrB;AACA;AACA;AACA;AACA,KAAK;AACL,uDAAuD,mBAAM;AAC7D;AACA;AACA,yBAAyB;AACzB;AACA;AACA;AACA;AACA,0BAA0B,sBAAsB;AAChD,mBAAmB;AACnB;AACA;AACA;AACA,KAAK;AACL;AACA;AACA;AACA;AACA;AACA,yBAAyB,kBAAkB;AAC3C;AACA;AACA,uCAAuC;AACvC,QAAQ;AACR,gCAAgC,2BAA2B;AAC3D;AACA;AACA;AACA;AACA,mBAAM,CAAC,sBAAsB;;AAE7B;AACA,IAAI,IAAI;AACR,IAAI,iBAAiB;AACrB,IAAI,WAAW,GAAG,iBAAiB;AACnC,IAAI,sBAAsB;AAC1B,SAAS,OAAO;AAChB,oBAAoB,EAAE,IAAI,EAAE,OAAO,EAAE,IAAI,EAAE;AAC3C;AACA,mBAAM,CAAC,OAAO;AACd,SAAS,iCAAiC;AAC1C,UAAU,8BAA8B,EAAE,qBAAqB;AAC/D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,OAAO;AACP;AACA;AACA;AACA,4CAA4C,mBAAM;AAClD,uBAAuB,OAAO;AAC9B;AACA,qCAAqC,mBAAM;AAC3C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,UAAU,OAAO;AACjB;AACA;AACA;AACA;AACA,mCAAmC,sBAAsB;AACzD,GAAG;AACH;AACA;AACA;AACA;AACA;AACA,SAAS,yBAAyB,MAAM,IAAI;AAC5C;AACA;AACA,qBAAqB,mBAAmB,qBAAqB,IAAI;AACjE;AACA;AACA;AACA,YAAY,yDAAyD;AACrE;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB;AACpB,oBAAoB;AACpB,MAAM;AACN;AACA,oBAAoB;AACpB,oBAAoB;AACpB;AACA,QAAQ,kBAAkB;AAC1B;AACA;AACA;AACA;AACA,wCAAwC,WAAW,gBAAgB,WAAW;AAC9E;AACA;AACA,2BAA2B;AAC3B,2BAA2B;AAC3B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,UAAU,kBAAkB;AAC5B;AACA;AACA,UAAU,2BAA2B,wCAAwC,SAAS,IAAI,EAAE;AAC5F;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,iCAAiC;;AAExC;AACA,SAAS,2BAA2B;AACpC;AACA;AACA;AACA;AACA;AACA;AACA;AACA,UAAU,4CAA4C,EAAE,sBAAsB;AAC9E;AACA;AACA,uCAAuC,mBAAM;AAC7C,WAAW,2BAA2B;AACtC;AACA;AACA;AACA,KAAK;AACL,GAAG;AACH,sCAAsC,mBAAM;AAC5C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,wCAAwC,uBAAuB;AAC/D;AACA;AACA,GAAG;AACH,kDAAkD,mBAAM;AACxD;AACA,aAAa,aAAa;AAC1B;AACA;AACA,aAAa,aAAa;AAC1B;AACA;AACA,GAAG;AACH,0CAA0C,mBAAM;AAChD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,UAAU,kBAAkB;AAC5B;AACA;AACA,UAAU,kBAAkB;AAC5B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,uDAAuD,uBAAuB;AAC9E;AACA;AACA;AACA,cAAc,+BAA+B;AAC7C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,2CAA2C,mBAAM;AACjD,mBAAmB,uBAAuB;AAC1C;AACA;AACA;AACA,2CAA2C,MAAM;AACjD;AACA;AACA;AACA,MAAM;AACN;AACA;AACA,MAAM;AACN;AACA;AACA;AACA,GAAG;AACH,qCAAqC,mBAAM;AAC3C;AACA;AACA,GAAG;AACH,iDAAiD,mBAAM;AACvD;AACA,mBAAmB,uBAAuB;AAC1C;AACA;AACA;AACA;AACA;AACA,oCAAoC,gBAAgB;AACpD,oCAAoC,gBAAgB;AACpD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,+CAA+C,mBAAM;AACrD;AACA;AACA;AACA;AACA;AACA;AACA;AACA,WAAW,uBAAuB;AAClC;AACA,UAAU,mCAAmC;AAC7C,UAAU,6BAA6B;AACvC,UAAU,oBAAoB;AAC9B,UAAU,0BAA0B;AACpC;AACA;AACA;AACA;AACA,GAAG;AACH,iDAAiD,mBAAM;AACvD;AACA;AACA;AACA;AACA;AACA;AACA;AACA,WAAW,uBAAuB;AAClC;AACA,UAAU,mCAAmC;AAC7C,UAAU,6BAA6B;AACvC,UAAU,oBAAoB;AAC9B,UAAU,0BAA0B;AACpC;AACA;AACA;AACA;AACA,GAAG;AACH,gDAAgD,mBAAM;AACtD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,0BAA0B,4BAA4B;AACtD;AACA;AACA,oBAAoB,+BAA+B;AACnD,0BAA0B,+BAA+B;AACzD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,2BAA2B;;AAElC;AACA,SAAS,iBAAiB;AAC1B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,iBAAiB;AACxB,SAAS,uBAAuB;AAChC;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,sBAAsB;AAC1C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,iCAAiC,UAAU,IAAI,QAAQ;AACvD;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sBAAsB,uBAAuB;AAC7C,YAAY,wBAAwB;AACpC;AACA;AACA;AACA;AACA,+BAA+B,GAAG,uBAAuB,YAAY;AACrE,WAAW;AACX;AACA;AACA;AACA;AACA,oBAAoB,uBAAuB;AAC3C;AACA;AACA;AACA;AACA;AACA;AACA,OAAO;AACP;AACA;AACA;AACA,kBAAkB,yBAAyB;AAC3C,wBAAwB,yBAAyB;AACjD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,UAAU,iBAAiB;AAC3B,kDAAkD,SAAS,GAAG,SAAS,OAAO,SAAS,GAAG,SAAS;AACnG;AACA;AACA;AACA;AACA;AACA;AACA,8BAA8B,SAAS,SAAS,SAAS;AACzD,WAAW;AACX;AACA;AACA;AACA;AACA;AACA;AACA;AACA,IAAI,QAAQ;AACZ,6BAA6B,eAAe,qBAAqB,UAAU,wBAAwB,WAAW;AAC9G;AACA;AACA,MAAM,QAAQ,0BAA0B,WAAW,IAAI,aAAa;AACpE;AACA;AACA;AACA;AACA,mBAAM,CAAC,uBAAuB;;AAE9B;AACA,SAAS,yBAAyB;AAClC;AACA;AACA;AACA,gFAAgF,yBAAyB;AACzG;AACA;AACA,wDAAwD,yBAAyB;AACjF;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,kBAAkB,gBAAgB;AAClC,MAAM,qBAAqB;AAC3B;AACA;AACA,EAAE,qBAAqB;AACvB,EAAE,iCAAiC;AACnC,EAAE,gBAAgB;AAClB;AACA;AACA;AACA;AACA,EAAE,sBAAsB;AACxB,EAAE,iCAAiC;AACnC,EAAE,yBAAyB;AAC3B,EAAE,2BAA2B;AAC7B,EAAE,mCAAmC;AACrC,EAAE,mCAAmC;AACrC,EAAE,gCAAgC;AAClC,EAAE,6CAA6C;AAC/C,gDAAgD,mBAAM;AACtD,IAAI,kCAAkC;AACtC,IAAI,oCAAoC;AACxC,IAAI,+BAA+B;AACnC,IAAI,sBAAsB;AAC1B,IAAI,+BAA+B;AACnC,IAAI,gCAAgC;AACpC,IAAI,sBAAsB;AAC1B,IAAI,+BAA+B;AACnC,GAAG;AACH;AACA,EAAE,2BAA2B;AAC7B;AACA,EAAE,+BAA+B;AACjC,EAAE,kCAAkC;AACpC,EAAE,+BAA+B;AACjC,EAAE,kCAAkC;AACpC;AACA,mBAAM,CAAC,yBAAyB;;AAEhC;AACA,SAAS,cAAc;AACvB;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAmB,KAAK,GAAG,MAAM,IAAI,MAAM;AAC3C;AACA;AACA;AACA;AACA;AACA;AACA;AACA,WAAW;AACX;AACA,mBAAM,CAAC,cAAc;AACrB,SAAS,QAAQ;AACjB;AACA;AACA,mBAAM,CAAC,QAAQ;AACf,SAAS,iBAAiB;AAC1B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,iBAAiB;AACxB,SAAS,uBAAuB;AAChC,gBAAgB,iBAAiB;AACjC;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,uBAAuB;AAC9B,SAAS,gBAAgB;AACzB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,gBAAgB;AACvB,SAAS,uBAAuB;AAChC;AACA;AACA,mBAAM,CAAC,uBAAuB;AAC9B,SAAS,6BAA6B;AACtC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,WAAW;AACX;AACA,mBAAM,CAAC,6BAA6B;AACpC,SAAS,oBAAoB;AAC7B;AACA;AACA;AACA;AACA;AACA;AACA;AACA,8BAA8B,qBAAqB;AACnD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,oBAAoB;AAC3B,SAAS,iBAAiB;AAC1B,gBAAgB,gBAAgB;AAChC,gBAAgB,uBAAuB;AACvC;AACA,cAAc,uBAAuB;AACrC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,iBAAiB;AACxB,SAAS,eAAe;AACxB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,eAAe;AACtB,SAAS,eAAe;AACxB;AACA,gCAAgC,mBAAM;AACtC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA,uBAAuB,WAAW;AAClC;AACA;AACA;AACA,GAAG;AACH;AACA;AACA,mBAAM,CAAC,eAAe;;AAEtB;AACA,SAAS,gBAAgB;AACzB,aAAa,cAAc;AAC3B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,8BAA8B,mBAAM;AACpC;AACA;AACA;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA,mDAAmD,KAAK,GAAG,MAAM,IAAI,MAAM;AAC3E;AACA,4BAA4B,KAAK,GAAG,MAAM,IAAI,MAAM,OAAO,iEAAiE;AAC5H;AACA;AACA;AACA;AACA;AACA;AACA;AACA,WAAW;AACX;AACA,mBAAM,CAAC,gBAAgB;;AAEvB;AACA,SAAS,eAAe;AACxB;AACA,kCAAkC,mBAAM;AACxC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,eAAe;AACtB,SAAS,qBAAqB;AAC9B,qBAAqB,eAAe;AACpC;AACA;AACA,mBAAM,CAAC,qBAAqB;AAC5B,SAAS,iBAAiB;AAC1B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,iBAAiB;AACxB,SAAS,mBAAmB;AAC5B,sBAAsB,iBAAiB;AACvC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,mBAAmB;;AAE1B;AACA,IAAI,SAAS;AACb;AACA;AACA;AACA,IAAI,QAAQ;AACZ;AACA;AACA;AACA;AACA;AACA;AACA;AACA,IAAI,WAAW;AACf;AACA;AACA;AACA;AACA;;AAEA;AACA,SAAS,gBAAgB;AACzB,YAAY,cAAc;AAC1B;AACA;AACA,UAAU,QAAQ,EAAE,6BAA6B;AACjD;AACA;AACA;AACA,oBAAoB,iBAAiB;AACrC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,qBAAqB,YAAY;AACjC;AACA;AACA;AACA,OAAO;AACP;AACA;AACA,MAAM;AACN;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,oBAAoB,cAAc;AAClC;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sBAAsB,gBAAgB;AACtC,iBAAiB,aAAa;AAC9B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA,+BAA+B,mBAAM;AACrC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,gBAAgB;AACvB,SAAS,YAAY;AACrB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA,mBAAM,CAAC,YAAY;AACnB,SAAS,cAAc;AACvB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,cAAc;AACrB,SAAS,gBAAgB;AACzB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,gBAAgB;AACvB,SAAS,aAAa;AACtB;AACA;AACA;AACA;AACA;AACA,gCAAgC,mBAAM;AACtC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sBAAsB,QAAQ;AAC9B;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,aAAa;AACpB,SAAS,QAAQ;AACjB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,WAAW;AACX;AACA,mBAAM,CAAC,QAAQ;;AAEf;AACA,SAAS,yBAAyB;AAClC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,0BAA0B,gBAAgB;AAC1C,kBAAkB,OAAO;AACzB;AACA;AACA,kBAAkB,YAAY;AAC9B,oBAAoB,OAAO;AAC3B;AACA;AACA;AACA;AACA,mCAAmC,mBAAM;AACzC;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,YAAY;AAChC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,6BAA6B,QAAQ;AACrC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,iCAAiC,WAAW;AAC5C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,yBAAyB,YAAY;AACrC;AACA;AACA;AACA;AACA,oCAAoC,mBAAM;AAC1C;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA,8BAA8B,mBAAM;AACpC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,yBAAyB;;AAEhC;AACA,SAAS,qBAAqB;AAC9B;AACA,mCAAmC,mBAAM;AACzC;AACA;AACA,mBAAmB,mBAAmB;AACtC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,qBAAqB;AAC5B,SAAS,mBAAmB;AAC5B;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,mBAAmB;AAC1B,SAAS,oBAAoB;AAC7B;AACA;AACA;AACA;AACA;AACA;AACA;AACA,8BAA8B,qBAAqB;AACnD;AACA,+BAA+B,mBAAM;AACrC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,oBAAoB;AAC3B,SAAS,iBAAiB;AAC1B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,iBAAiB;;AAExB;AACA,SAAS,kBAAkB;AAC3B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sBAAsB,kBAAkB;AACxC,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL;AACA;AACA;AACA,mBAAM,CAAC,kBAAkB;AACzB,SAAS,wBAAwB;AACjC,eAAe,gBAAgB;AAC/B;AACA;AACA,GAAG;AACH,UAAU,kBAAkB;AAC5B;AACA;AACA;AACA;AACA;AACA,sBAAsB,yBAAyB;AAC/C,sCAAsC,mBAAmB;AACzD,mBAAmB,qBAAqB;AACxC,wBAAwB,kBAAkB;AAC1C,eAAe,oBAAoB;AACnC,WAAW,iBAAiB;AAC5B;AACA;AACA,mBAAM,CAAC,wBAAwB;;AAE/B;AACA,SAAS,6BAA6B;AACtC;AACA;AACA,aAAa,eAAe;AAC5B;AACA;AACA;AACA;AACA;AACA;AACA,SAAS,eAAe;AACxB;AACA,mBAAM,CAAC,6BAA6B;AACpC,SAAS,cAAc;AACvB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,yBAAyB,YAAY;AACrC,sBAAsB,OAAO,KAAK,GAAG,EAAE,uCAAuC;AAC9E;AACA;AACA;AACA,kBAAkB,uBAAuB;AACzC,WAAW,6BAA6B;AACxC;AACA;AACA;AACA,mBAAM,CAAC,cAAc;AACrB,SAAS,wBAAwB;AACjC,mBAAmB;AACnB,UAAU,QAAQ,EAAE,6BAA6B;AACjD,iBAAiB,qBAAqB;AACtC,iBAAiB,wBAAwB;AACzC,aAAa,cAAc;AAC3B,oBAAoB,QAAQ;AAC5B,qBAAqB,kBAAkB;AACvC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,0BAA0B,wBAAwB;AAClD,oBAAoB,cAAc;AAClC;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,wBAAwB;;AAE/B;AACA,SAAS,sBAAsB;AAC/B,oBAAoB,qBAAqB;AACzC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,sBAAsB;;AAE7B;AACA,SAAS,wBAAwB;AACjC,YAAY,cAAc;AAC1B,gBAAgB,iBAAiB;AACjC;AACA,oBAAoB,qBAAqB;AACzC;AACA;AACA,mBAAmB,QAAQ;AAC3B;AACA;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL;AACA;AACA,MAAM;AACN;AACA;AACA;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA,MAAM;AACN;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,aAAa,wBAAwB;AACrC;AACA;AACA,IAAI,sBAAsB;AAC1B;AACA,iBAAiB,wBAAwB;AACzC,WAAW;AACX;AACA,mBAAM,CAAC,wBAAwB;;AAE/B;AACA,SAAS,oBAAoB;AAC7B,YAAY,cAAc;AAC1B,eAAe,wBAAwB;AACvC;AACA;AACA;AACA,GAAG;AACH,mBAAmB;AACnB,oBAAoB,qBAAqB;AACzC,UAAU,eAAe,EAAE,6BAA6B;AACxD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,gBAAgB,iBAAiB;AACjC;AACA,wCAAwC,mBAAM;AAC9C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,gBAAgB,QAAQ;AACxB,qCAAqC,mBAAM;AAC3C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,mBAAmB,YAAY;AAC/B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,iBAAiB,oBAAoB;AACrC,WAAW;AACX;AACA,mBAAM,CAAC,oBAAoB;;AAE3B;AACA,SAAS,6BAA6B;AACtC,gBAAgB,gBAAgB;AAChC,cAAc,uBAAuB;AACrC,iBAAiB,uBAAuB;AACxC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,6BAA6B;AACpC,SAAS,6BAA6B;AACtC,YAAY,cAAc;AAC1B,2CAA2C,6BAA6B,6BAA6B,iBAAiB;AACtH,oBAAoB,qBAAqB;AACzC,iCAAiC,mBAAM;AACvC;AACA;AACA,qCAAqC,mBAAM;AAC3C;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA,kBAAkB,QAAQ;AAC1B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,iBAAiB,oBAAoB,qBAAqB,kBAAkB;AAC5E,WAAW;AACX;AACA,mBAAM,CAAC,6BAA6B;;AAEpC;AACA,SAAS,kBAAkB;AAC3B,YAAY,cAAc;AAC1B,UAAU,SAAS;AACnB;AACA;AACA;AACA;AACA,qCAAqC,mBAAM;AAC3C,8BAA8B,WAAW;AACzC,iBAAiB;AACjB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,gCAAgC,QAAQ;AACxC;AACA,sBAAsB,OAAO,KAAK,GAAG,EAAE,oDAAoD;AAC3F;AACA;AACA;AACA;AACA,aAAa,KAAK,GAAG,2BAA2B;AAChD;AACA;AACA;AACA;AACA,KAAK;AACL;AACA;AACA,6BAA6B;AAC7B,WAAW,YAAY,uBAAuB;AAC9C;AACA,mBAAM,CAAC,kBAAkB;;AAEzB;AACA,SAAS,MAAM;AACf;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,MAAM;AACb,SAAS,UAAU;AACnB;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,UAAU;AACjB,SAAS,oBAAoB;AAC7B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,MAAM;AACN;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,oBAAoB;AAC3B,SAAS,oBAAoB;AAC7B;AACA;AACA;AACA;AACA,mBAAM,CAAC,oBAAoB;AAC3B,SAAS,8BAA8B;AACvC;AACA;AACA,qBAAqB,eAAe;AACpC,qBAAqB,eAAe;AACpC;AACA;AACA;AACA,mBAAmB,oDAAoD;AACvE;AACA;AACA;AACA;AACA,SAAS,eAAe;AACxB;AACA,mBAAM,CAAC,8BAA8B;AACrC,SAAS,eAAe;AACxB;AACA,eAAe,MAAM;AACrB,eAAe,MAAM;AACrB;AACA,aAAa,oBAAoB;AACjC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA,mBAAM,CAAC,eAAe;AACtB,SAAS,YAAY;AACrB,qBAAqB,eAAe;AACpC,oBAAoB,eAAe;AACnC,4BAA4B,oBAAoB;AAChD;AACA,WAAW,eAAe;AAC1B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAmB,eAAe;AAClC;AACA;AACA;AACA;AACA,mBAAmB,eAAe;AAClC;AACA,iBAAiB,UAAU;AAC3B;AACA;AACA;AACA,sBAAsB,UAAU;AAChC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,YAAY;AACnB,SAAS,gBAAgB;AACzB;AACA;AACA;AACA;AACA;AACA;AACA,wCAAwC,mBAAM;AAC9C,gBAAgB,8BAA8B;AAC9C;AACA,eAAe,8BAA8B;AAC7C;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,qBAAqB;AACzC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,gBAAgB;AACvB,SAAS,WAAW;AACpB;AACA;AACA,oBAAoB,qBAAqB;AACzC,oBAAoB,mBAAmB;AACvC,kBAAkB,OAAO;AACzB,oBAAoB,mBAAmB;AACvC,kBAAkB,YAAY;AAC9B,kBAAkB,gBAAgB;AAClC;AACA,oCAAoC,QAAQ;AAC5C,kBAAkB,YAAY;AAC9B,kBAAkB,gBAAgB;AAClC;AACA;AACA,WAAW;AACX;AACA,mBAAM,CAAC,WAAW;;AAElB;AACA,SAAS,iBAAiB;AAC1B,qCAAqC,WAAW;AAChD,mCAAmC,WAAW;AAC9C;AACA;AACA;AACA;AACA;AACA;AACA,kCAAkC,mBAAM;AACxC,mCAAmC,mBAAM;AACzC,oCAAoC,mBAAM;AAC1C,oBAAoB,qBAAqB;AACzC,0BAA0B,mBAAmB;AAC7C;AACA;AACA;AACA;AACA;AACA,oBAAoB,uBAAuB;AAC3C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,6BAA6B;AACjD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,WAAW;AACX;AACA,mBAAM,CAAC,iBAAiB;;AAExB;AACA,IAAI,gCAAgC;AACpC,SAAS,UAAU;AACnB;AACA,kBAAkB,kBAAkB;AACpC;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,UAAU;AACjB,SAAS,UAAU;AACnB;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,UAAU;AACjB,SAAS,oBAAoB;AAC7B;AACA,iBAAiB,UAAU;AAC3B,oCAAoC,OAAO;AAC3C;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,oBAAoB;AAC3B,SAAS,cAAc;AACvB;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,cAAc;AACrB,SAAS,mBAAmB;AAC5B;AACA;AACA;AACA;AACA;AACA,eAAe,eAAe;AAC9B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,mBAAmB;AAC1B,SAAS,sBAAsB;AAC/B,sBAAsB,iBAAiB;AACvC;AACA;AACA;AACA;AACA,oBAAoB,qBAAqB;AACzC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAmB,EAAE,IAAI,EAAE;AAC3B;AACA;AACA;AACA,MAAM;AACN,yBAAyB,iBAAiB;AAC1C;AACA;AACA;AACA;AACA,mBAAM,CAAC,sBAAsB;AAC7B,SAAS,YAAY;AACrB;AACA,aAAa,mBAAmB;AAChC;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,sBAAsB;AAC1C;AACA,uBAAuB,mBAAmB;AAC1C;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,cAAc;AAClC;AACA;AACA,mBAAM,CAAC,YAAY;AACnB,SAAS,iBAAiB;AAC1B;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,iBAAiB;AACxB,SAAS,cAAc;AACvB,4HAA4H,cAAc,QAAQ,EAAE,GAAG,EAAE,GAAG,OAAO;AACnK,SAAS,UAAU,IAAI,sBAAsB,GAAG,gBAAgB,GAAG,aAAa;AAChF;AACA,mBAAM,CAAC,cAAc;AACrB,SAAS,oBAAoB,aAAa;AAC1C,sBAAsB,iBAAiB;AACvC;AACA;AACA;AACA,kBAAkB,sBAAsB;AACxC;AACA;AACA;AACA;AACA,aAAa,YAAY;AACzB,gDAAgD,gCAAgC;AAChF,kBAAkB,cAAc;AAChC,iBAAiB,cAAc;AAC/B,kBAAkB,oBAAoB;AACtC,sBAAsB,YAAY;AAClC,QAAQ,iBAAiB;AACzB;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,oBAAoB;;AAE3B;AACA,SAAS,cAAc;AACvB;AACA;AACA,aAAa,cAAc;AAC3B,kDAAkD,oBAAoB,OAAO,UAAU,gCAAgC,EAAE;AACzH,mBAAmB,gBAAgB;AACnC;AACA,0CAA0C,6BAA6B;AACvE,oDAAoD,QAAQ;AAC5D;AACA;AACA,GAAG,IAAI,oBAAoB;AAC3B,oDAAoD,QAAQ;AAC5D;AACA;AACA,GAAG;AACH,UAAU,6CAA6C,EAAE,kBAAkB;AAC3E,kBAAkB,WAAW,qCAAqC,WAAW;AAC7E,sBAAsB,iBAAiB;AACvC;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,cAAc;;AAErB;AACA,IAAI,IAAI,GAAG,SAAS;AACpB,IAAI,YAAY;AAChB,IAAI,sBAAsB;AAC1B,IAAI,oBAAoB;AACxB,IAAI,cAAc;AAClB,IAAI,aAAa;AACjB,IAAI,aAAa;AACjB,SAAS,oBAAoB;AAC7B;AACA;AACA;AACA;AACA;AACA;AACA,cAAc,IAAI,YAAY,IAAI;AAClC;AACA;AACA;AACA,cAAc,IAAI;AAClB;AACA;AACA,cAAc,IAAI;AAClB;AACA;AACA;AACA;AACA,mBAAM,CAAC,oBAAoB;AAC3B,SAAS,uBAAuB;AAChC,6CAA6C,IAAI,sCAAsC,IAAI;AAC3F;AACA,mBAAM,CAAC,uBAAuB;AAC9B,SAAS,WAAW;AACpB,wCAAwC,0BAA0B,IAAI;AACtE;AACA,mBAAM,CAAC,WAAW;AAClB,SAAS,oBAAoB;AAC7B;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAmB;AACnB,uCAAuC,mBAAM;AAC7C;AACA;AACA,KAAK;AACL;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,YAAY;AAChC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,uCAAuC,mBAAM;AAC7C;AACA;AACA;AACA,oBAAoB,YAAY,GAAG,iBAAiB;AACpD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,sCAAsC,mBAAM;AAC5C;AACA;AACA;AACA;AACA;AACA;AACA,iBAAiB;AACjB;AACA,iBAAiB;AACjB;AACA,iBAAiB;AACjB;AACA,iBAAiB;AACjB;AACA,GAAG;AACH,4CAA4C,mBAAM,+CAA+C,oBAAoB;AACrH;AACA;AACA;AACA;AACA,0CAA0C,mBAAM;AAChD;AACA;AACA;AACA,mDAAmD,IAAI;AACvD,iDAAiD,IAAI;AACrD;AACA;AACA;AACA;AACA;AACA;AACA,4CAA4C,IAAI;AAChD,4CAA4C,IAAI;AAChD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,uBAAuB,IAAI,kBAAkB,IAAI;AACjD;AACA;AACA;AACA,MAAM;AACN;AACA,4CAA4C,IAAI;AAChD,4CAA4C,IAAI;AAChD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,uBAAuB,IAAI,kBAAkB,IAAI;AACjD;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA,eAAe;AACf;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,aAAa;AACb,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,2CAA2C,mBAAM;AACjD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,kCAAkC,IAAI;AACtC;AACA,QAAQ;AACR;AACA;AACA,KAAK;AACL;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,wCAAwC,mBAAM,mBAAmB,oBAAoB;AACrF;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oCAAoC,8BAA8B;AAClE,oCAAoC,8BAA8B;AAClE;AACA;AACA;AACA;AACA,KAAK;AACL;AACA,6CAA6C,mBAAM;AACnD;AACA;AACA;AACA;AACA,GAAG;AACH,wCAAwC,mBAAM;AAC9C;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA,mBAAmB,WAAW,GAAG,aAAa;AAC9C;AACA;AACA;AACA;AACA;AACA;AACA,kCAAkC,mBAAM,sBAAsB,OAAO,GAAG,KAAK;AAC7E;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL,oBAAoB,kBAAkB;AACtC;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,wCAAwC,mBAAM;AAC9C;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,YAAY,6CAA6C;AACzD;AACA;AACA,sBAAsB,MAAM,GAAG,QAAQ;AACvC;AACA;AACA;AACA;AACA,kCAAkC,qCAAqC;AACvE,sBAAsB,MAAM,GAAG,QAAQ;AACvC;AACA;AACA;AACA;AACA,kCAAkC,qCAAqC;AACvE;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,2BAA2B,gBAAgB,GAAG,KAAK;AACnD;AACA;AACA;AACA,2CAA2C,mBAAM;AACjD,2CAA2C,mBAAM;AACjD;AACA;AACA;AACA,8BAA8B,OAAO,GAAG,KAAK;AAC7C,SAAS,SAAS;AAClB,4BAA4B,OAAO,GAAG,KAAK;AAC3C,SAAS,SAAS;AAClB;AACA,GAAG;AACH,0CAA0C,mBAAM;AAChD;AACA,eAAe;AACf,MAAM;AACN,eAAe;AACf;AACA,GAAG;AACH,uCAAuC,mBAAM;AAC7C;AACA,wBAAwB;AACxB,wBAAwB;AACxB;AACA;AACA;AACA;AACA,yCAAyC,UAAU;AACnD,yCAAyC,UAAU;AACnD;AACA;AACA;AACA;AACA;AACA;AACA,aAAa;AACb,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,MAAM;AACN,yBAAyB;AACzB,yBAAyB;AACzB;AACA;AACA;AACA;AACA,6CAA6C,aAAa,gBAAgB,aAAa;AACvF,MAAM;AACN;AACA,4CAA4C,aAAa,gBAAgB,aAAa;AACtF;AACA;AACA;AACA,6CAA6C,aAAa,gBAAgB,aAAa;AACvF,MAAM;AACN;AACA,4CAA4C,aAAa,gBAAgB,aAAa;AACtF;AACA,8CAA8C,mBAAM;AACpD;AACA;AACA;AACA;AACA;AACA,mBAAmB;AACnB;AACA;AACA,eAAe;AACf,KAAK;AACL,2CAA2C,mBAAM;AACjD;AACA;AACA;AACA;AACA,kCAAkC,sBAAsB,cAAc,sBAAsB;AAC5F,8CAA8C,oBAAoB,cAAc,oBAAoB;AACpG;AACA;AACA;AACA;AACA;AACA;AACA,2CAA2C,sBAAsB,cAAc,sBAAsB;AACrG,+BAA+B,oBAAoB,cAAc,oBAAoB;AACrF;AACA;AACA,KAAK;AACL;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,qFAAqF,aAAa,wCAAwC,aAAa;AACvJ;AACA,YAAY,wBAAwB;AACpC;AACA,YAAY,sBAAsB;AAClC;AACA,YAAY;AACZ;AACA;AACA,QAAQ;AACR;AACA,qFAAqF,aAAa,wCAAwC,aAAa;AACvJ;AACA;AACA;AACA,YAAY,wBAAwB;AACpC;AACA,YAAY,sBAAsB;AAClC;AACA,YAAY;AACZ;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,YAAY,0BAA0B;AACtC;AACA,YAAY;AACZ;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA,YAAY,0BAA0B;AACtC;AACA,YAAY;AACZ;AACA;AACA;AACA;AACA;AACA,uBAAuB,sBAAsB;AAC7C;AACA;AACA,+CAA+C,EAAE,wCAAwC,EAAE;AAC3F,8CAA8C,cAAc,GAAG,YAAY,0CAA0C,cAAc,GAAG,YAAY;AAClJ,8CAA8C,YAAY,GAAG,YAAY,0CAA0C,YAAY,GAAG,YAAY;AAC9I;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,wBAAwB,aAAa,IAAI,eAAe,IAAI,eAAe,IAAI,aAAa;AAC5F;AACA;AACA;AACA;AACA;AACA;AACA,6BAA6B,oBAAoB,GAAG,yBAAyB,GAAG,EAAE;AAClF;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,WAAW;AACX;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,6CAA6C,cAAc;AAC3D,6CAA6C,cAAc;AAC3D,6CAA6C,cAAc;AAC3D,6CAA6C,cAAc;AAC3D;AACA;AACA;AACA;AACA;AACA;AACA,sBAAsB,sBAAsB;AAC5C;AACA;AACA,sBAAsB,oBAAoB;AAC1C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mCAAmC,mBAAM,cAAc,aAAa,GAAG,aAAa;AACpF;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL;AACA;AACA,gDAAgD,mBAAM;AACtD;AACA,KAAK;AACL,uBAAuB;AACvB;AACA;AACA;AACA,uBAAuB;AACvB;AACA;AACA;AACA;AACA,kDAAkD,IAAI,4CAA4C,IAAI;AACtG;AACA,QAAQ;AACR;AACA;AACA,MAAM;AACN,kDAAkD,IAAI;AACtD;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,2BAA2B,wCAAwC;AACnE;AACA;AACA,2BAA2B,wCAAwC;AACnE;AACA;AACA,2BAA2B,wCAAwC;AACnE;AACA;AACA,2BAA2B,wCAAwC;AACnE;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,cAAc;AACd;AACA;AACA,WAAW;AACX;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,6CAA6C,IAAI;AACjD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,8BAA8B,4CAA4C;AAC1E;AACA,cAAc;AACd;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,iCAAiC,kCAAkC;AACnE;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,uBAAuB,oBAAoB;AAC3C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,8CAA8C,mBAAM;AACpD;AACA;AACA;AACA;AACA;AACA,SAAS;AACT;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,uBAAuB,sBAAsB;AAC7C;AACA;AACA;AACA,8BAA8B,IAAI;AAClC;AACA;AACA,UAAU;AACV;AACA;AACA,8BAA8B,IAAI;AAClC;AACA;AACA;AACA;AACA,OAAO;AACP,qDAAqD,mBAAM;AAC3D;AACA,0BAA0B;AAC1B,0BAA0B;AAC1B,0BAA0B;AAC1B;AACA;AACA;AACA;AACA;AACA,wCAAwC,IAAI;AAC5C;AACA;AACA;AACA;AACA;AACA,OAAO;AACP;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,4CAA4C,IAAI,0BAA0B,IAAI;AAC9E,2CAA2C,IAAI,0BAA0B,IAAI;AAC7E;AACA;AACA;AACA;AACA;AACA;AACA,QAAQ;AACR;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,2BAA2B;AAC/C;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sCAAsC,IAAI,gCAAgC,IAAI;AAC9E;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sCAAsC,IAAI,gCAAgC,IAAI;AAC9E;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,2BAA2B;AAC/C;AACA;AACA,kDAAkD,IAAI;AACtD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,2BAA2B;AAC3B;AACA;AACA;AACA;AACA;AACA;AACA,OAAO;AACP;AACA;AACA,0CAA0C,mBAAM;AAChD;AACA,GAAG;AACH,sDAAsD,mBAAM;AAC5D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,yCAAyC,mBAAM;AAC/C;AACA,yBAAyB;AACzB;AACA,GAAG;AACH,6CAA6C,mBAAM;AACnD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL,GAAG;AACH,kDAAkD,mBAAM;AACxD;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,uDAAuD,mBAAM;AAC7D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,0CAA0C,mBAAM;AAChD;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,6CAA6C,mBAAM;AACnD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,2CAA2C,mBAAM;AACjD;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,+CAA+C,mBAAM;AACrD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,iDAAiD,mBAAM;AACvD;AACA,oBAAoB,oBAAoB;AACxC,0BAA0B,oBAAoB;AAC9C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA,sCAAsC,mBAAM;AAC5C;AACA;AACA;AACA;AACA;AACA,sBAAsB;AACtB;AACA;AACA;AACA;AACA;AACA;AACA,sBAAsB,sBAAsB;AAC5C;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAmB;AACnB;AACA;AACA,GAAG;AACH,mDAAmD,mBAAM;AACzD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,4CAA4C,mBAAM;AAClD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,OAAO;AACP;AACA;AACA;AACA;AACA,GAAG;AACH,mDAAmD,mBAAM;AACzD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,wCAAwC,mBAAM;AAC9C;AACA;AACA;AACA;AACA;AACA;AACA,SAAS;AACT;AACA;AACA;AACA;AACA;AACA;AACA,OAAO;AACP;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,2CAA2C,mBAAM;AACjD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,sBAAsB,yBAAyB;AAC/C,4BAA4B,yBAAyB;AACrD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,SAAS;AACT,OAAO;AACP,KAAK;AACL;AACA;AACA;AACA;AACA;AACA,sBAAsB,qBAAqB;AAC3C;AACA;AACA;AACA;AACA,UAAU;AACV;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,OAAO;AACP;AACA;AACA;AACA;AACA;AACA,0CAA0C,mBAAM;AAChD;AACA;AACA,+BAA+B,YAAY,GAAG,eAAe;AAC7D,SAAS;AACT,OAAO;AACP;AACA;AACA;AACA,yDAAyD,aAAa;AACtE;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,UAAU;AACV;AACA;AACA,iEAAiE,aAAa;AAC9E;AACA;AACA;AACA;AACA;AACA;AACA,0DAA0D,aAAa;AACvE;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,YAAY,qBAAqB;AACjC;AACA;AACA,yCAAyC,YAAY,GAAG,eAAe;AACvE;AACA;AACA;AACA;AACA;AACA;AACA,KAAK;AACL;AACA,oBAAoB,kBAAkB;AACtC;AACA;AACA;AACA;AACA;AACA;AACA,kDAAkD,IAAI;AACtD,uBAAuB,WAAW;AAClC;AACA;AACA,oDAAoD,IAAI;AACxD,0FAA0F,uBAAuB;AACjH,yBAAyB,WAAW,kBAAkB,WAAW;AACjE,UAAU;AACV,yBAAyB,WAAW,OAAO,uBAAuB;AAClE;AACA,QAAQ;AACR,uBAAuB,WAAW;AAClC,QAAQ;AACR;AACA,uBAAuB,WAAW;AAClC;AACA;AACA;AACA,wCAAwC,IAAI,oCAAoC,IAAI;AACpF;AACA;AACA;AACA;AACA;AACA;AACA,oBAAoB,sBAAsB;AAC1C;AACA;AACA,mCAAmC,IAAI,6BAA6B,IAAI;AACxE;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,4CAA4C,mBAAM;AAClD;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,eAAe;AACf;AACA;AACA,eAAe;AACf;AACA;AACA,eAAe;AACf;AACA,aAAa;AACb,GAAG;AACH;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,mBAAM,CAAC,oBAAoB;;AAE3B;AACA,SAAS,oBAAoB;AAC7B;AACA;AACA,mBAAM,CAAC,oBAAoB;AAC3B,SAAS,qBAAqB;AAC9B,YAAY,WAAW;AACvB;AACA;AACA;AACA;AACA;AACA,oBAAoB,oBAAoB;AACxC,UAAU,uBAAuB,EAAE,cAAc;AACjD;AACA;AACA;AACA;AACA;AACA;AACA,GAAG;AACH,EAAE,qBAAqB,4BAA4B,mBAAmB;AACtE;AACA;AACA;AACA,EAAE,oBAAoB;AACtB;AACA;AACA;AACA;AACA;AACA,EAAE,yBAAyB;AAC3B,EAAE,uBAAuB;AACzB;AACA;AACA,mBAAM,CAAC,qBAAqB;;AAE5B;AACA,SAAS,qBAAqB;AAC9B,EAAE,yBAAyB;AAC3B,0BAA0B,oBAAoB;AAC9C;AACA;AACA;AACA,mBAAM,CAAC,qBAAqB;AAC5B,IAAI,MAAM,GAAG,0BAA0B;AACvC,iBAAiB,qBAAqB;AACtC,iBAAiB,qBAAqB;AACtC,cAAc,sBAAsB;AACpC,CAAC;AAGC"}      °¼ Ô9ðÿ—   üèñÿ”! éñÿ               ŠG .úÿ                                                                       