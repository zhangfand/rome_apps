import node_util from "node:util";
import node_process from "node:process";
import node_os from "node:os";
import node_tty from "node:tty";
import node_path, { basename as external_node_path_basename, dirname, extname, isAbsolute, join } from "node:path";
import node_fs, { promises as external_node_fs_promises } from "node:fs";
import promises from "node:fs/promises";
import * as __rspack_external__rsbuild_core_1b356efc from "@rsbuild/core";
import { createRsbuild, defineConfig, loadConfig, loadEnv, mergeRsbuildConfig, rspack } from "@rsbuild/core";
import { createRequire } from "node:module";
import { createHash } from "node:crypto";
import { Readable } from "node:stream";
import { pipeline } from "node:stream/promises";
import { spawn } from "node:child_process";
import { createRequire as external_module_createRequire } from "module";
import { getUndoPath, ABSOLUTE_PUBLIC_PATH, isCssFile, DTS_EXTENSIONS_PATTERN, SWC_HELPERS, AUTO_PUBLIC_PATH, CSS_EXTENSIONS_PATTERN, JS_EXTENSIONS_PATTERN, isCssGlobalFile, SINGLE_DOT_PATH_SEGMENT, BASE_URI, isCssModulesFile } from "./108.js";
function checkNodeVersion() {
    const { versions } = process;
    if ("styleText" in node_util || !versions.node || versions.bun || versions.deno) return;
    throw new Error(`Unsupported Node.js version: "${process.versions.node || 'unknown'}". Expected Node.js >= 20.`);
}
checkNodeVersion();
const createStyler = (style)=>(text)=>node_util.styleText(style, String(text));
const color = {
    dim: createStyler('dim'),
    red: createStyler('red'),
    bold: createStyler('bold'),
    blue: createStyler('blue'),
    cyan: createStyler('cyan'),
    gray: createStyler('gray'),
    black: createStyler('black'),
    green: createStyler('green'),
    white: createStyler('white'),
    reset: createStyler('reset'),
    yellow: createStyler('yellow'),
    magenta: createStyler('magenta'),
    underline: createStyler('underline'),
    strikethrough: createStyler('strikethrough')
};
function hasFlag(flag, argv = globalThis.Deno ? globalThis.Deno.args : node_process.argv) {
    const prefix = flag.startsWith('-') ? '' : 1 === flag.length ? '-' : '--';
    const position = argv.indexOf(prefix + flag);
    const terminatorPosition = argv.indexOf('--');
    return -1 !== position && (-1 === terminatorPosition || position < terminatorPosition);
}
const { env: dist_env } = node_process;
let flagForceColor;
if (hasFlag('no-color') || hasFlag('no-colors') || hasFlag('color=false') || hasFlag('color=never')) flagForceColor = 0;
else if (hasFlag('color') || hasFlag('colors') || hasFlag('color=true') || hasFlag('color=always')) flagForceColor = 1;
function envForceColor() {
    if (!('FORCE_COLOR' in dist_env)) return;
    if ('true' === dist_env.FORCE_COLOR) return 1;
    if ('false' === dist_env.FORCE_COLOR) return 0;
    if (0 === dist_env.FORCE_COLOR.length) return 1;
    const level = Math.min(Number.parseInt(dist_env.FORCE_COLOR, 10), 3);
    if (![
        0,
        1,
        2,
        3
    ].includes(level)) return;
    return level;
}
function translateLevel(level) {
    if (0 === level) return false;
    return {
        level,
        hasBasic: true,
        has256: level >= 2,
        has16m: level >= 3
    };
}
function _supportsColor(haveStream, { streamIsTTY, sniffFlags = true } = {}) {
    const noFlagForceColor = envForceColor();
    if (void 0 !== noFlagForceColor) flagForceColor = noFlagForceColor;
    const forceColor = sniffFlags ? flagForceColor : noFlagForceColor;
    if (0 === forceColor) return 0;
    if (sniffFlags) {
        if (hasFlag('color=16m') || hasFlag('color=full') || hasFlag('color=truecolor')) return 3;
        if (hasFlag('color=256')) return 2;
    }
    if ('TF_BUILD' in dist_env && 'AGENT_NAME' in dist_env) return 1;
    if (haveStream && !streamIsTTY && void 0 === forceColor) return 0;
    const min = forceColor || 0;
    if ('dumb' === dist_env.TERM) return min;
    if ('win32' === node_process.platform) {
        const osRelease = node_os.release().split('.');
        if (Number(osRelease[0]) >= 10 && Number(osRelease[2]) >= 10586) return Number(osRelease[2]) >= 14931 ? 3 : 2;
        return 1;
    }
    if ('CI' in dist_env) {
        if ([
            'GITHUB_ACTIONS',
            'GITEA_ACTIONS',
            'CIRCLECI'
        ].some((key)=>key in dist_env)) return 3;
        if ([
            'TRAVIS',
            'APPVEYOR',
            'GITLAB_CI',
            'BUILDKITE',
            'DRONE'
        ].some((sign)=>sign in dist_env) || 'codeship' === dist_env.CI_NAME) return 1;
        return min;
    }
    if ('TEAMCITY_VERSION' in dist_env) return /^(9\.(0*[1-9]\d*)\.|\d{2,}\.)/.test(dist_env.TEAMCITY_VERSION) ? 1 : 0;
    if ('truecolor' === dist_env.COLORTERM) return 3;
    if ('xterm-kitty' === dist_env.TERM) return 3;
    if ('xterm-ghostty' === dist_env.TERM) return 3;
    if ('wezterm' === dist_env.TERM) return 3;
    if ('TERM_PROGRAM' in dist_env) {
        const version = Number.parseInt((dist_env.TERM_PROGRAM_VERSION || '').split('.')[0], 10);
        switch(dist_env.TERM_PROGRAM){
            case 'iTerm.app':
                return version >= 3 ? 3 : 2;
            case 'Apple_Terminal':
                return 2;
        }
    }
    if (/-256(color)?$/i.test(dist_env.TERM)) return 2;
    if (/^screen|^xterm|^vt100|^vt220|^rxvt|color|ansi|cygwin|linux/i.test(dist_env.TERM)) return 1;
    if ('COLORTERM' in dist_env) return 1;
    return min;
}
function createSupportsColor(stream, options = {}) {
    const level = _supportsColor(stream, {
        streamIsTTY: stream && stream.isTTY,
        ...options
    });
    return translateLevel(level);
}
const supportsColor = {
    stdout: createSupportsColor({
        isTTY: node_tty.isatty(1)
    }),
    stderr: createSupportsColor({
        isTTY: node_tty.isatty(2)
    })
};
const supports_color = supportsColor;
const colorLevel = supports_color.stdout ? supports_color.stdout.level : 0;
let errorStackRegExp = /at [^\r\n]{0,200}:\d+:\d+[\s\)]*$/;
let anonymousErrorStackRegExp = /at [^\r\n]{0,200}\(<anonymous>\)$/;
let indexErrorStackRegExp = /at [^\r\n]{0,200}\(index\s\d+\)$/;
let isErrorStackMessage = (message)=>errorStackRegExp.test(message) || anonymousErrorStackRegExp.test(message) || indexErrorStackRegExp.test(message);
let startColor = [
    189,
    255,
    243
];
let endColor = [
    74,
    194,
    154
];
let isWord = (char)=>!/[\s\n]/.test(char);
let gradient = (message)=>{
    if (colorLevel < 3) return 2 === colorLevel ? color.cyan(message) : message;
    let chars = [
        ...message
    ];
    let steps = chars.filter(isWord).length;
    let r = startColor[0];
    let g = startColor[1];
    let b = startColor[2];
    let rStep = (endColor[0] - r) / steps;
    let gStep = (endColor[1] - g) / steps;
    let bStep = (endColor[2] - b) / steps;
    let output = '';
    for (let char of chars){
        if (isWord(char)) {
            r += rStep;
            g += gStep;
            b += bStep;
        }
        output += `\x1b[38;2;${Math.round(r)};${Math.round(g)};${Math.round(b)}m${char}\x1b[39m`;
    }
    return color.bold(output);
};
let LOG_LEVEL = {
    silent: -1,
    error: 0,
    warn: 1,
    info: 2,
    log: 2,
    verbose: 3
};
let LOG_TYPES = {
    error: {
        label: 'error',
        level: 'error',
        color: color.red
    },
    warn: {
        label: 'warn',
        level: 'warn',
        color: color.yellow
    },
    info: {
        label: 'info',
        level: 'info',
        color: color.cyan
    },
    start: {
        label: 'start',
        level: 'info',
        color: color.cyan
    },
    ready: {
        label: 'ready',
        level: 'info',
        color: color.green
    },
    success: {
        label: 'success',
        level: 'info',
        color: color.green
    },
    log: {
        level: 'info'
    },
    debug: {
        label: 'debug',
        level: 'verbose',
        color: color.magenta
    }
};
const normalizeErrorMessage = (err)=>{
    if (err.stack) {
        let [name, ...rest] = err.stack.split('\n');
        if (name.startsWith('Error: ')) name = name.slice(7);
        return `${name}\n${color.gray(rest.join('\n'))}`;
    }
    return err.message;
};
let createLogger = (options = {})=>{
    const { level = 'info', prefix, console: console1 = globalThis.console } = options;
    let maxLevel = level;
    let log = (type, message, ...args)=>{
        let logType = LOG_TYPES[type];
        const { level } = logType;
        if (LOG_LEVEL[level] > LOG_LEVEL[maxLevel]) return;
        if (null == message) return console1.log();
        let label = '';
        let text = '';
        if ('label' in logType) {
            label = (logType.label || '').padEnd(7);
            label = color.bold(logType.color ? logType.color(label) : label);
        }
        if (message instanceof Error) {
            text += normalizeErrorMessage(message);
            const { cause } = message;
            if (cause) {
                text += color.yellow('\n  [cause]: ');
                text += cause instanceof Error ? normalizeErrorMessage(cause) : String(cause);
            }
        } else if ('error' === level && 'string' == typeof message) {
            let lines = message.split('\n');
            text = lines.map((line)=>isErrorStackMessage(line) ? color.gray(line) : line).join('\n');
        } else text = `${message}`;
        if (prefix) text = `${prefix} ${text}`;
        const method = 'error' === level || 'warn' === level ? level : 'log';
        console1[method](label.length ? `${label} ${text}` : text, ...args);
    };
    let logger = {
        greet: (message)=>log('log', gradient(message))
    };
    Object.keys(LOG_TYPES).forEach((key)=>{
        logger[key] = (...args)=>log(key, ...args);
    });
    Object.defineProperty(logger, 'level', {
        get: ()=>maxLevel,
        set (val) {
            maxLevel = val;
        }
    });
    Object.defineProperty(logger, 'options', {
        get: ()=>({
                ...options
            })
    });
    logger.override = (customLogger)=>{
        Object.assign(logger, customLogger);
    };
    return logger;
};
let src_logger = createLogger();
const isDebugKey = (keys)=>{
    if (!process.env.DEBUG) return false;
    const values = process.env.DEBUG.toLocaleLowerCase().split(',');
    return keys.some((key)=>values.includes(key));
};
const isDebug = ()=>isDebugKey([
        'rslib',
        'rsbuild',
        'rs*',
        'rstack',
        '*'
    ]);
if (isDebug()) src_logger.level = 'verbose';
function getTime() {
    const now = new Date();
    const hours = String(now.getHours()).padStart(2, '0');
    const minutes = String(now.getMinutes()).padStart(2, '0');
    const seconds = String(now.getSeconds()).padStart(2, '0');
    return `${hours}:${minutes}:${seconds}`;
}
src_logger.override({
    debug: (message, ...args)=>{
        if ('verbose' !== src_logger.level) return;
        const time = color.gray(getTime());
        console.log(`  ${color.green('rslib')} ${time} ${message}`, ...args);
    }
});
function toArr(any) {
    return null == any ? [] : Array.isArray(any) ? any : [
        any
    ];
}
function toVal(out, key, val, opts) {
    var x, old = out[key], nxt = ~opts.string.indexOf(key) ? null == val || true === val ? "" : String(val) : "boolean" == typeof val ? val : ~opts.boolean.indexOf(key) ? "false" === val ? false : "true" === val || (out._.push((x = +val, 0 * x === 0) ? x : val), !!val) : (x = +val, 0 * x === 0) ? x : val;
    out[key] = null == old ? nxt : Array.isArray(old) ? old.concat(nxt) : [
        old,
        nxt
    ];
}
function lib_default(args, opts) {
    args = args || [];
    opts = opts || {};
    var k, arr, arg, name, val, out = {
        _: []
    };
    var i = 0, j = 0, idx = 0, len = args.length;
    const alibi = void 0 !== opts.alias;
    const strict = void 0 !== opts.unknown;
    const defaults = void 0 !== opts.default;
    opts.alias = opts.alias || {};
    opts.string = toArr(opts.string);
    opts.boolean = toArr(opts.boolean);
    if (alibi) for(k in opts.alias){
        arr = opts.alias[k] = toArr(opts.alias[k]);
        for(i = 0; i < arr.length; i++)(opts.alias[arr[i]] = arr.concat(k)).splice(i, 1);
    }
    for(i = opts.boolean.length; i-- > 0;){
        arr = opts.alias[opts.boolean[i]] || [];
        for(j = arr.length; j-- > 0;)opts.boolean.push(arr[j]);
    }
    for(i = opts.string.length; i-- > 0;){
        arr = opts.alias[opts.string[i]] || [];
        for(j = arr.length; j-- > 0;)opts.string.push(arr[j]);
    }
    if (defaults) for(k in opts.default){
        name = typeof opts.default[k];
        arr = opts.alias[k] = opts.alias[k] || [];
        if (void 0 !== opts[name]) {
            opts[name].push(k);
            for(i = 0; i < arr.length; i++)opts[name].push(arr[i]);
        }
    }
    const keys = strict ? Object.keys(opts.alias) : [];
    for(i = 0; i < len; i++){
        arg = args[i];
        if ("--" === arg) {
            out._ = out._.concat(args.slice(++i));
            break;
        }
        for(j = 0; j < arg.length && 45 === arg.charCodeAt(j); j++);
        if (0 === j) out._.push(arg);
        else if ("no-" === arg.substring(j, j + 3)) {
            name = arg.substring(j + 3);
            if (strict && !~keys.indexOf(name)) return opts.unknown(arg);
            out[name] = false;
        } else {
            for(idx = j + 1; idx < arg.length && 61 !== arg.charCodeAt(idx); idx++);
            name = arg.substring(j, idx);
            val = arg.substring(++idx) || i + 1 === len || 45 === ("" + args[i + 1]).charCodeAt(0) || args[++i];
            arr = 2 === j ? [
                name
            ] : name;
            for(idx = 0; idx < arr.length; idx++){
                name = arr[idx];
                if (strict && !~keys.indexOf(name)) return opts.unknown("-".repeat(j) + name);
                toVal(out, name, idx + 1 < arr.length || val, opts);
            }
        }
    }
    if (defaults) {
        for(k in opts.default)if (void 0 === out[k]) out[k] = opts.default[k];
    }
    if (alibi) for(k in out){
        arr = opts.alias[k] || [];
        while(arr.length > 0)out[arr.shift()] = out[k];
    }
    return out;
}
function removeBrackets(v) {
    return v.replace(/[<[].+/, "").trim();
}
function findAllBrackets(v) {
    const ANGLED_BRACKET_RE_GLOBAL = /<([^>]+)>/g;
    const SQUARE_BRACKET_RE_GLOBAL = /\[([^\]]+)\]/g;
    const res = [];
    const parse = (match)=>{
        let variadic = false;
        let value = match[1];
        if (value.startsWith("...")) {
            value = value.slice(3);
            variadic = true;
        }
        return {
            required: match[0].startsWith("<"),
            value,
            variadic
        };
    };
    let angledMatch;
    while(angledMatch = ANGLED_BRACKET_RE_GLOBAL.exec(v))res.push(parse(angledMatch));
    let squareMatch;
    while(squareMatch = SQUARE_BRACKET_RE_GLOBAL.exec(v))res.push(parse(squareMatch));
    return res;
}
function getMriOptions(options) {
    const result = {
        alias: {},
        boolean: []
    };
    for (const [index, option] of options.entries()){
        if (option.names.length > 1) result.alias[option.names[0]] = option.names.slice(1);
        if (option.isBoolean) if (option.negated) {
            if (!options.some((o, i)=>i !== index && o.names.some((name)=>option.names.includes(name)) && "boolean" == typeof o.required)) result.boolean.push(option.names[0]);
        } else result.boolean.push(option.names[0]);
    }
    return result;
}
function findLongest(arr) {
    return arr.sort((a, b)=>a.length > b.length ? -1 : 1)[0];
}
function padRight(str, length) {
    return str.length >= length ? str : `${str}${" ".repeat(length - str.length)}`;
}
function camelcase(input) {
    return input.replaceAll(/([a-z])-([a-z])/g, (_, p1, p2)=>p1 + p2.toUpperCase());
}
function setDotProp(obj, keys, val) {
    let current = obj;
    for(let i = 0; i < keys.length; i++){
        const key = keys[i];
        if (i === keys.length - 1) {
            current[key] = val;
            return;
        }
        if (null == current[key]) {
            const nextKeyIsArrayIndex = +keys[i + 1] > -1;
            current[key] = nextKeyIsArrayIndex ? [] : {};
        }
        current = current[key];
    }
}
function setByType(obj, transforms) {
    for (const key of Object.keys(transforms)){
        const transform = transforms[key];
        if (transform.shouldTransform) {
            obj[key] = [
                obj[key]
            ].flat();
            if ("function" == typeof transform.transformFunction) obj[key] = obj[key].map(transform.transformFunction);
        }
    }
}
function getFileName(input) {
    const m = /([^\\/]+)$/.exec(input);
    return m ? m[1] : "";
}
function camelcaseOptionName(name) {
    return name.split(".").map((v, i)=>0 === i ? camelcase(v) : v).join(".");
}
var CACError = class extends Error {
    constructor(message){
        super(message);
        this.name = "CACError";
        if ("function" != typeof Error.captureStackTrace) this.stack = new Error(message).stack;
    }
};
var Option = class {
    rawName;
    description;
    name;
    names;
    isBoolean;
    required;
    config;
    negated;
    constructor(rawName, description, config){
        this.rawName = rawName;
        this.description = description;
        this.config = Object.assign({}, config);
        rawName = rawName.replaceAll(".*", "");
        this.negated = false;
        this.names = removeBrackets(rawName).split(",").map((v)=>{
            let name = v.trim().replace(/^-{1,2}/, "");
            if (name.startsWith("no-")) {
                this.negated = true;
                name = name.replace(/^no-/, "");
            }
            return camelcaseOptionName(name);
        }).sort((a, b)=>a.length > b.length ? 1 : -1);
        this.name = this.names.at(-1);
        if (this.negated && null == this.config.default) this.config.default = true;
        if (rawName.includes("<")) this.required = true;
        else if (rawName.includes("[")) this.required = false;
        else this.isBoolean = true;
    }
};
let runtimeProcessArgs;
let runtimeInfo;
if ("u" > typeof process) {
    let runtimeName;
    runtimeName = "u" > typeof Deno && "string" == typeof Deno.version?.deno ? "deno" : "u" > typeof Bun && "string" == typeof Bun.version ? "bun" : "node";
    runtimeInfo = `${process.platform}-${process.arch} ${runtimeName}-${process.version}`;
    runtimeProcessArgs = process.argv;
} else runtimeInfo = "u" < typeof navigator ? "unknown" : `${navigator.platform} ${navigator.userAgent}`;
var Command = class {
    rawName;
    description;
    config;
    cli;
    options;
    aliasNames;
    name;
    args;
    commandAction;
    usageText;
    versionNumber;
    examples;
    helpCallback;
    globalCommand;
    constructor(rawName, description, config = {}, cli){
        this.rawName = rawName;
        this.description = description;
        this.config = config;
        this.cli = cli;
        this.options = [];
        this.aliasNames = [];
        this.name = removeBrackets(rawName);
        this.args = findAllBrackets(rawName);
        this.examples = [];
    }
    usage(text) {
        this.usageText = text;
        return this;
    }
    allowUnknownOptions() {
        this.config.allowUnknownOptions = true;
        return this;
    }
    ignoreOptionDefaultValue() {
        this.config.ignoreOptionDefaultValue = true;
        return this;
    }
    version(version, customFlags = "-v, --version") {
        this.versionNumber = version;
        this.option(customFlags, "Display version number");
        return this;
    }
    example(example) {
        this.examples.push(example);
        return this;
    }
    option(rawName, description, config) {
        const option = new Option(rawName, description, config);
        this.options.push(option);
        return this;
    }
    alias(name) {
        this.aliasNames.push(name);
        return this;
    }
    action(callback) {
        this.commandAction = callback;
        return this;
    }
    isMatched(name) {
        return this.name === name || this.aliasNames.includes(name);
    }
    get isDefaultCommand() {
        return "" === this.name || this.aliasNames.includes("!");
    }
    get isGlobalCommand() {
        return this instanceof GlobalCommand;
    }
    hasOption(name) {
        name = name.split(".")[0];
        return this.options.find((option)=>option.names.includes(name));
    }
    outputHelp() {
        const { name, commands } = this.cli;
        const { versionNumber, options: globalOptions, helpCallback } = this.cli.globalCommand;
        let sections = [
            {
                body: `${name}${versionNumber ? `/${versionNumber}` : ""}`
            }
        ];
        sections.push({
            title: "Usage",
            body: `  $ ${name} ${this.usageText || this.rawName}`
        });
        if ((this.isGlobalCommand || this.isDefaultCommand) && commands.length > 0) {
            const longestCommandName = findLongest(commands.map((command)=>command.rawName));
            sections.push({
                title: "Commands",
                body: commands.map((command)=>`  ${padRight(command.rawName, longestCommandName.length)}  ${command.description}`).join("\n")
            }, {
                title: "For more info, run any command with the `--help` flag",
                body: commands.map((command)=>`  $ ${name}${"" === command.name ? "" : ` ${command.name}`} --help`).join("\n")
            });
        }
        let options = this.isGlobalCommand ? globalOptions : [
            ...this.options,
            ...globalOptions || []
        ];
        if (!this.isGlobalCommand && !this.isDefaultCommand) options = options.filter((option)=>"version" !== option.name);
        if (options.length > 0) {
            const longestOptionName = findLongest(options.map((option)=>option.rawName));
            sections.push({
                title: "Options",
                body: options.map((option)=>`  ${padRight(option.rawName, longestOptionName.length)}  ${option.description} ${void 0 === option.config.default ? "" : `(default: ${option.config.default})`}`).join("\n")
            });
        }
        if (this.examples.length > 0) sections.push({
            title: "Examples",
            body: this.examples.map((example)=>{
                if ("function" == typeof example) return example(name);
                return example;
            }).join("\n")
        });
        if (helpCallback) sections = helpCallback(sections) || sections;
        console.info(sections.map((section)=>section.title ? `${section.title}:\n${section.body}` : section.body).join("\n\n"));
    }
    outputVersion() {
        const { name } = this.cli;
        const { versionNumber } = this.cli.globalCommand;
        if (versionNumber) console.info(`${name}/${versionNumber} ${runtimeInfo}`);
    }
    checkRequiredArgs() {
        const minimalArgsCount = this.args.filter((arg)=>arg.required).length;
        if (this.cli.args.length < minimalArgsCount) throw new CACError(`missing required args for command \`${this.rawName}\``);
    }
    checkUnknownOptions() {
        const { options, globalCommand } = this.cli;
        if (!this.config.allowUnknownOptions) {
            for (const name of Object.keys(options))if ("--" !== name && !this.hasOption(name) && !globalCommand.hasOption(name)) throw new CACError(`Unknown option \`${name.length > 1 ? `--${name}` : `-${name}`}\``);
        }
    }
    checkOptionValue() {
        const { options: parsedOptions, globalCommand } = this.cli;
        const options = [
            ...globalCommand.options,
            ...this.options
        ];
        for (const option of options){
            const value = parsedOptions[option.name.split(".")[0]];
            if (option.required) {
                const hasNegated = options.some((o)=>o.negated && o.names.includes(option.name));
                if (true === value || false === value && !hasNegated) throw new CACError(`option \`${option.rawName}\` value is missing`);
            }
        }
    }
    checkUnusedArgs() {
        const maximumArgsCount = this.args.some((arg)=>arg.variadic) ? 1 / 0 : this.args.length;
        if (maximumArgsCount < this.cli.args.length) throw new CACError(`Unused args: ${this.cli.args.slice(maximumArgsCount).map((arg)=>`\`${arg}\``).join(", ")}`);
    }
};
var GlobalCommand = class extends Command {
    constructor(cli){
        super("@@global@@", "", {}, cli);
    }
};
var CAC = class extends EventTarget {
    name;
    commands;
    globalCommand;
    matchedCommand;
    matchedCommandName;
    rawArgs;
    args;
    options;
    showHelpOnExit;
    showVersionOnExit;
    constructor(name = ""){
        super();
        this.name = name;
        this.commands = [];
        this.rawArgs = [];
        this.args = [];
        this.options = {};
        this.globalCommand = new GlobalCommand(this);
        this.globalCommand.usage("<command> [options]");
    }
    usage(text) {
        this.globalCommand.usage(text);
        return this;
    }
    command(rawName, description, config) {
        const command = new Command(rawName, description || "", config, this);
        command.globalCommand = this.globalCommand;
        this.commands.push(command);
        return command;
    }
    option(rawName, description, config) {
        this.globalCommand.option(rawName, description, config);
        return this;
    }
    help(callback) {
        this.globalCommand.option("-h, --help", "Display this message");
        this.globalCommand.helpCallback = callback;
        this.showHelpOnExit = true;
        return this;
    }
    version(version, customFlags = "-v, --version") {
        this.globalCommand.version(version, customFlags);
        this.showVersionOnExit = true;
        return this;
    }
    example(example) {
        this.globalCommand.example(example);
        return this;
    }
    outputHelp() {
        if (this.matchedCommand) this.matchedCommand.outputHelp();
        else this.globalCommand.outputHelp();
    }
    outputVersion() {
        this.globalCommand.outputVersion();
    }
    setParsedInfo({ args, options }, matchedCommand, matchedCommandName) {
        this.args = args;
        this.options = options;
        if (matchedCommand) this.matchedCommand = matchedCommand;
        if (matchedCommandName) this.matchedCommandName = matchedCommandName;
        return this;
    }
    unsetMatchedCommand() {
        this.matchedCommand = void 0;
        this.matchedCommandName = void 0;
    }
    parse(argv, { run = true } = {}) {
        if (!argv) {
            if (!runtimeProcessArgs) throw new Error("No argv provided and runtime process argv is not available.");
            argv = runtimeProcessArgs;
        }
        this.rawArgs = argv;
        if (!this.name) this.name = argv[1] ? getFileName(argv[1]) : "cli";
        let shouldParse = true;
        for (const command of this.commands){
            const parsed = this.mri(argv.slice(2), command);
            const commandName = parsed.args[0];
            if (command.isMatched(commandName)) {
                shouldParse = false;
                const parsedInfo = {
                    ...parsed,
                    args: parsed.args.slice(1)
                };
                this.setParsedInfo(parsedInfo, command, commandName);
                this.dispatchEvent(new CustomEvent(`command:${commandName}`, {
                    detail: command
                }));
            }
        }
        if (shouldParse) {
            for (const command of this.commands)if (command.isDefaultCommand) {
                shouldParse = false;
                const parsed = this.mri(argv.slice(2), command);
                this.setParsedInfo(parsed, command);
                this.dispatchEvent(new CustomEvent("command:!", {
                    detail: command
                }));
            }
        }
        if (shouldParse) {
            const parsed = this.mri(argv.slice(2));
            this.setParsedInfo(parsed);
        }
        if (this.options.help && this.showHelpOnExit) {
            this.outputHelp();
            run = false;
            this.unsetMatchedCommand();
        }
        if (this.options.version && this.showVersionOnExit && null == this.matchedCommandName) {
            this.outputVersion();
            run = false;
            this.unsetMatchedCommand();
        }
        const parsedArgv = {
            args: this.args,
            options: this.options
        };
        if (run) this.runMatchedCommand();
        if (!this.matchedCommand && this.args[0]) this.dispatchEvent(new CustomEvent("command:*", {
            detail: this.args[0]
        }));
        return parsedArgv;
    }
    mri(argv, command) {
        const cliOptions = [
            ...this.globalCommand.options,
            ...command ? command.options : []
        ];
        const mriOptions = getMriOptions(cliOptions);
        let argsAfterDoubleDashes = [];
        const doubleDashesIndex = argv.indexOf("--");
        if (-1 !== doubleDashesIndex) {
            argsAfterDoubleDashes = argv.slice(doubleDashesIndex + 1);
            argv = argv.slice(0, doubleDashesIndex);
        }
        let parsed = lib_default(argv, mriOptions);
        parsed = Object.keys(parsed).reduce((res, name)=>({
                ...res,
                [camelcaseOptionName(name)]: parsed[name]
            }), {
            _: []
        });
        const args = parsed._;
        const options = {
            "--": argsAfterDoubleDashes
        };
        const ignoreDefault = command && command.config.ignoreOptionDefaultValue ? command.config.ignoreOptionDefaultValue : this.globalCommand.config.ignoreOptionDefaultValue;
        const transforms = Object.create(null);
        for (const cliOption of cliOptions){
            if (!ignoreDefault && void 0 !== cliOption.config.default) for (const name of cliOption.names)options[name] = cliOption.config.default;
            if (Array.isArray(cliOption.config.type) && void 0 === transforms[cliOption.name]) {
                transforms[cliOption.name] = Object.create(null);
                transforms[cliOption.name].shouldTransform = true;
                transforms[cliOption.name].transformFunction = cliOption.config.type[0];
            }
        }
        for (const key of Object.keys(parsed))if ("_" !== key) {
            setDotProp(options, key.split("."), parsed[key]);
            setByType(options, transforms);
        }
        return {
            args,
            options
        };
    }
    runMatchedCommand() {
        const { args, options, matchedCommand: command } = this;
        if (!command || !command.commandAction) return;
        command.checkUnknownOptions();
        command.checkOptionValue();
        command.checkRequiredArgs();
        command.checkUnusedArgs();
        const actionArgs = [];
        command.args.forEach((arg, index)=>{
            if (arg.variadic) actionArgs.push(args.slice(index));
            else actionArgs.push(args[index]);
        });
        actionArgs.push(options);
        return command.commandAction.apply(this, actionArgs);
    }
};
const cac = (name = "")=>new CAC(name);
const nodeBuiltInModules = [
    'assert',
    'assert/strict',
    'async_hooks',
    'buffer',
    'child_process',
    'cluster',
    'console',
    'constants',
    'crypto',
    'dgram',
    'diagnostics_channel',
    'dns',
    'dns/promises',
    'domain',
    'events',
    'fs',
    'fs/promises',
    'http',
    'http2',
    'https',
    'inspector',
    'inspector/promises',
    'module',
    'net',
    'os',
    'path',
    'path/posix',
    'path/win32',
    'perf_hooks',
    'process',
    'punycode',
    'querystring',
    'readline',
    'readline/promises',
    'repl',
    'stream',
    'stream/consumers',
    'stream/promises',
    'stream/web',
    'string_decoder',
    'sys',
    'timers',
    'timers/promises',
    'tls',
    'trace_events',
    'tty',
    'url',
    'util',
    'util/types',
    'v8',
    'vm',
    'wasi',
    'worker_threads',
    'zlib',
    /^node:/,
    'pnpapi'
];
async function calcLongestCommonPath(absPaths) {
    if (0 === absPaths.length) return null;
    const sep = node_path.posix.sep;
    const splitPaths = absPaths.map((p)=>p.split(sep));
    let lcaFragments = splitPaths[0];
    for(let i = 1; i < splitPaths.length; i++){
        const currentPath = splitPaths[i];
        const minLength = Math.min(lcaFragments.length, currentPath.length);
        let j = 0;
        while(j < minLength && lcaFragments[j] === currentPath[j])j++;
        lcaFragments = lcaFragments.slice(0, j);
    }
    let lca = lcaFragments.length > 0 ? lcaFragments.join(sep) : sep;
    const stats = await promises.stat(lca);
    if (stats?.isFile()) lca = node_path.dirname(lca);
    return lca;
}
function ensureAbsolutePath(base, filepath) {
    return isAbsolute(filepath) ? filepath : join(base, filepath);
}
const readPackageJson = (rootPath)=>{
    const pkgJsonPath = node_path.join(rootPath, './package.json');
    if (!node_fs.existsSync(pkgJsonPath)) return void src_logger.warn(`The \`package.json\` file does not exist in the ${rootPath} directory`);
    try {
        return JSON.parse(node_fs.readFileSync(pkgJsonPath, 'utf8'));
    } catch  {
        src_logger.warn(`Failed to parse ${pkgJsonPath}, it might not be valid JSON`);
        return;
    }
};
const isObject = (obj)=>'[object Object]' === Object.prototype.toString.call(obj);
const isEmptyObject = (obj)=>0 === Object.keys(obj).length;
function pick(obj, keys) {
    return keys.reduce((ret, key)=>{
        if (void 0 !== obj[key]) ret[key] = obj[key];
        return ret;
    }, {});
}
function omit(obj, keysObj) {
    return Object.keys(obj).reduce((ret, key)=>{
        if (true !== keysObj[key]) ret[key] = obj[key];
        return ret;
    }, {});
}
function isPluginIncluded(pluginName, plugins) {
    return Boolean(plugins?.some((plugin)=>{
        if (Array.isArray(plugin)) return isPluginIncluded(pluginName, plugin);
        if ('object' == typeof plugin && null !== plugin && 'name' in plugin) return plugin.name === pluginName;
        return false;
    }));
}
function checkMFPlugin(config, sharedPlugins) {
    if ('mf' !== config.format) return true;
    const added = isPluginIncluded('rsbuild:module-federation-enhanced', [
        ...sharedPlugins || [],
        ...config.plugins || []
    ]);
    if (!added) {
        src_logger.warn(`${color.green('format: "mf"')} should be used with ${color.blue('@module-federation/rsbuild-plugin')}", consider installing and adding it to plugins. Check the documentation (https://module-federation.io/guide/basic/rsbuild.html#rslib-module) to get started with "mf" output.`);
        process.exit(1);
    }
    return added;
}
function debounce(func, wait) {
    let timeoutId = null;
    return (...args)=>{
        if (null !== timeoutId) clearTimeout(timeoutId);
        timeoutId = setTimeout(()=>{
            func(...args);
        }, wait);
    };
}
function isTTY(type = 'stdout') {
    return ('stdin' === type ? process.stdin.isTTY : process.stdout.isTTY) && !process.env.CI;
}
const isIntermediateOutputFormat = (format)=>'cjs' === format || 'esm' === format;
const windowsSlashRegex = /\\/g;
function normalizeSlash(p) {
    return p.replace(windowsSlashRegex, '/');
}
async function isDirectory(filePath) {
    try {
        const stat = await promises.stat(filePath);
        return stat.isDirectory();
    } catch  {
        return false;
    }
}
const isFunction = (func)=>'function' == typeof func;
const getNodeEnv = ()=>process.env.NODE_ENV || '';
const setNodeEnv = (env)=>{
    process.env.NODE_ENV = env;
};
function getWatchFilesForRestart(rslib) {
    const meta = rslib.getRslibConfig()._privateMeta;
    if (!meta) return [];
    return [
        meta.configFilePath,
        ...meta.envFilePaths || []
    ].filter(Boolean);
}
async function watchFilesForRestart(files, restart) {
    if (!files.length) return;
    const { default: chokidar } = await import("./chokidar.js");
    const watcher = chokidar.watch(files, {
        ignoreInitial: true,
        ignorePermissionErrors: true
    });
    const callback = debounce(async (filePath)=>{
        watcher.close();
        await beforeRestart({
            filePath
        });
        await restart();
    }, 300);
    watcher.on('add', callback);
    watcher.on('change', callback);
    watcher.on('unlink', callback);
}
let cleaners = [];
const onBeforeRestart = (cleaner)=>{
    cleaners.push(cleaner);
};
const clearConsole = ()=>{
    if (isTTY() && !process.env.DEBUG) process.stdout.write('\x1B[H\x1B[2J');
};
const beforeRestart = async ({ filePath, clear = true } = {})=>{
    if (clear) clearConsole();
    if (filePath) {
        const filename = node_path.basename(filePath);
        src_logger.info(`restarting as ${color.yellow(filename)} is changed\n`);
    } else src_logger.info('restarting...\n');
    for (const cleaner of cleaners)await cleaner();
    cleaners = [];
};
const LibSvgrPatchPlugin_pluginName = 'LIB_SVGR_PATCH_PLUGIN';
const PUBLIC_PATH_PLACEHOLDER = '__RSLIB_SVGR_AUTO_PUBLIC_PATH__';
class LibSvgrPatchPlugin {
    name = LibSvgrPatchPlugin_pluginName;
    apply(compiler) {
        compiler.hooks.make.tap(this.name, (compilation)=>{
            compilation.hooks.processAssets.tap(this.name, (assets)=>{
                const isEsm = Boolean(compilation.options.output.module);
                const chunkAsset = Object.keys(assets).filter((name)=>name.endsWith('js'));
                for (const name of chunkAsset)compilation.updateAsset(name, (old)=>{
                    const oldSource = old.source().toString();
                    const newSource = new rspack.sources.ReplaceSource(old);
                    const pattern = new RegExp(`\\(?['"]${PUBLIC_PATH_PLACEHOLDER}(.*)['"]\\)?`, 'g');
                    const matches = [
                        ...oldSource.matchAll(pattern)
                    ];
                    const len = matches.length;
                    if (0 === len) return old;
                    const undoPath = getUndoPath(name, compilation.outputOptions.path, true);
                    for(let i = 0; i < len; i++){
                        const match = matches[i];
                        const filename = match[1];
                        const requirePath = `${undoPath}${filename}`;
                        let replaced = '';
                        replaced = isEsm ? `__rslib_svgr_url__${i}__` : `require("${requirePath}")`;
                        newSource.replace(match.index, match.index + match[0].length - 1, replaced);
                        if (isEsm) newSource.insert(0, `import __rslib_svgr_url__${i}__ from "${requirePath}";\n`);
                    }
                    return newSource;
                });
            });
        });
    }
}
const PLUGIN_NAME = 'rsbuild:lib-asset';
const RSBUILD_SVGR_PLUGIN_NAME = 'rsbuild:svgr';
const pluginLibAsset = ({ bundle })=>({
        name: PLUGIN_NAME,
        pre: [
            RSBUILD_SVGR_PLUGIN_NAME
        ],
        setup (api) {
            api.modifyBundlerChain((config, { CHAIN_ID })=>{
                const svgAssetRule = config.module.rules.get(CHAIN_ID.RULE.SVG).oneOfs.get(CHAIN_ID.ONE_OF.SVG_ASSET);
                const originalTypeOptions = svgAssetRule.get('type');
                const originalParserOptions = svgAssetRule.get('parser');
                const originalGeneratorOptions = svgAssetRule.get('generator');
                const isUserSetPublicPath = 'auto' !== config.output.get('publicPath');
                const generatorOptions = isUserSetPublicPath ? originalGeneratorOptions : {
                    ...originalGeneratorOptions,
                    importMode: 'preserve'
                };
                const rule = config.module.rule(CHAIN_ID.RULE.SVG);
                rule.oneOf(CHAIN_ID.ONE_OF.SVG_ASSET).generator(generatorOptions).issuer({
                    not: CSS_EXTENSIONS_PATTERN
                });
                rule.oneOf(`${CHAIN_ID.ONE_OF.SVG_ASSET}-for-css`).type(originalTypeOptions).parser(originalParserOptions).generator(originalGeneratorOptions).issuer(CSS_EXTENSIONS_PATTERN);
                const ruleIds = [
                    CHAIN_ID.RULE.FONT,
                    CHAIN_ID.RULE.MEDIA,
                    CHAIN_ID.RULE.IMAGE,
                    CHAIN_ID.RULE.ADDITIONAL_ASSETS
                ];
                for (const ruleId of ruleIds){
                    const oneOfId = `${ruleId}-asset`;
                    const assetRule = config.module.rules.get(ruleId);
                    if (!assetRule) continue;
                    const assetRuleOneOf = assetRule.oneOfs.get(oneOfId);
                    const originalTypeOptions = assetRuleOneOf.get('type');
                    const originalParserOptions = assetRuleOneOf.get('parser');
                    const originalGeneratorOptions = assetRuleOneOf.get('generator');
                    const generatorOptions = isUserSetPublicPath ? originalGeneratorOptions : {
                        ...originalGeneratorOptions,
                        importMode: 'preserve'
                    };
                    const rule = config.module.rule(ruleId);
                    rule.oneOf(oneOfId).generator(generatorOptions).issuer({
                        not: CSS_EXTENSIONS_PATTERN
                    });
                    rule.oneOf(`${oneOfId}-for-css`).type(originalTypeOptions).parser(originalParserOptions).generator(originalGeneratorOptions).issuer(CSS_EXTENSIONS_PATTERN);
                }
                const isUsingSvgr = config.module.rule(CHAIN_ID.RULE.SVG).oneOf(CHAIN_ID.RULE.SVG).uses.has(CHAIN_ID.USE.SVGR);
                const isUsingSvgrUrlLoader = config.module.rule(CHAIN_ID.RULE.SVG).oneOf(CHAIN_ID.ONE_OF.SVG).uses.has(CHAIN_ID.USE.URL);
                if (isUsingSvgr && isUsingSvgrUrlLoader) {
                    const urlLoaderRule = config.module.rule(CHAIN_ID.RULE.SVG).oneOf(CHAIN_ID.ONE_OF.SVG).use(CHAIN_ID.USE.URL);
                    const originalOptions = urlLoaderRule.get('options');
                    urlLoaderRule.options({
                        ...originalOptions,
                        publicPath: (url)=>`${PUBLIC_PATH_PLACEHOLDER}${url}`
                    });
                    config.plugin(LibSvgrPatchPlugin.name).use(LibSvgrPatchPlugin, []);
                }
                if (isUsingSvgr) if (bundle) {
                    const rule = config.module.rule(CHAIN_ID.RULE.SVG).oneOf(CHAIN_ID.ONE_OF.SVG_URL);
                    const originalGeneratorOptions = rule.get('generator');
                    const generatorOptions = isUserSetPublicPath ? originalGeneratorOptions : {
                        ...originalGeneratorOptions,
                        importMode: 'preserve'
                    };
                    rule.generator(generatorOptions);
                } else {
                    const rule = config.module.rule(CHAIN_ID.RULE.SVG).oneOf(CHAIN_ID.ONE_OF.SVG);
                    rule.issuer({
                        not: CSS_EXTENSIONS_PATTERN
                    });
                }
                if (bundle) config.plugins.get(CHAIN_ID.PLUGIN.MINI_CSS_EXTRACT)?.tap((options)=>[
                        {
                            ...options[0],
                            enforceRelative: true
                        }
                    ]);
            });
        }
    });
const composeAssetConfig = (bundle, format)=>{
    if ('esm' === format || 'cjs' === format) {
        if (bundle) return {
            output: {
                dataUriLimit: 0,
                assetPrefix: 'auto'
            },
            plugins: [
                pluginLibAsset({
                    bundle: true
                })
            ]
        };
        return {
            output: {
                dataUriLimit: 0,
                assetPrefix: 'auto'
            },
            plugins: [
                pluginLibAsset({
                    bundle: false
                })
            ]
        };
    }
    return {};
};
const LibCssExtractPlugin_pluginName = 'LIB_CSS_EXTRACT_PLUGIN';
class LibCssExtractPlugin {
    name = LibCssExtractPlugin_pluginName;
    options;
    constructor(options){
        this.options = options ?? {};
    }
    apply(compiler) {
        compiler.hooks.make.tap(LibCssExtractPlugin_pluginName, (compilation)=>{
            compilation.hooks.processAssets.tap(LibCssExtractPlugin_pluginName, (assets)=>{
                const chunkAsset = Object.keys(assets).filter((name)=>name.includes('.css'));
                for (const name of chunkAsset)compilation.updateAsset(name, (old)=>{
                    const oldSource = old.source().toString();
                    const replaceSource = new rspack.sources.ReplaceSource(old);
                    function replace(searchValue, replaceValue) {
                        let start = oldSource.indexOf(searchValue);
                        while(-1 !== start){
                            replaceSource.replace(start, start + searchValue.length - 1, replaceValue);
                            start = oldSource.indexOf(searchValue, start + 1);
                        }
                    }
                    replace(SINGLE_DOT_PATH_SEGMENT, '.');
                    const undoPath = getUndoPath(name, compilation.outputOptions.path, true);
                    replace(`${ABSOLUTE_PUBLIC_PATH}${AUTO_PUBLIC_PATH}`, undoPath);
                    replace(ABSOLUTE_PUBLIC_PATH, '');
                    replace(`${BASE_URI}/`, '');
                    return replaceSource;
                });
            });
        });
    }
}
const cssConfig_require = createRequire(import.meta.url);
const RSLIB_CSS_ENTRY_FLAG = '__rslib_css__';
async function cssExternalHandler(request, callback, jsExtension, auto, styleRedirectPath, styleRedirectExtension, redirectedPath, issuer) {
    if (request.includes('compiled/css-loader/')) return void callback();
    let resolvedRequest = request;
    if (styleRedirectPath) {
        if (void 0 === redirectedPath) return false;
        resolvedRequest = redirectedPath;
    }
    if (!isCssFile(resolvedRequest)) {
        if (isCssFile(issuer)) return void callback();
        return false;
    }
    if (styleRedirectExtension) {
        const isCssModulesRequest = isCssModulesFile(resolvedRequest, auto);
        if (isCssModulesRequest) return void callback(void 0, resolvedRequest.replace(/\.[^.]+$/, jsExtension));
        callback(void 0, resolvedRequest.replace(/\.[^.]+$/, '.css'));
        return;
    }
    callback(void 0, resolvedRequest);
}
const cssConfig_PLUGIN_NAME = 'rsbuild:lib-css';
const isPreprocessorRule = (preprocessRuleId, toMatchRuleId)=>{
    if (preprocessRuleId === toMatchRuleId) return true;
    if (new RegExp(`^${preprocessRuleId}-\\d+$`).test(toMatchRuleId)) return true;
    return false;
};
const pluginLibCss = (rootDir, auto, banner, footer)=>({
        name: cssConfig_PLUGIN_NAME,
        setup (api) {
            api.processAssets({
                stage: 'additional'
            }, ({ assets, compilation })=>{
                for (const key of Object.keys(assets))if (RegExp(RSLIB_CSS_ENTRY_FLAG).exec(key)) compilation.deleteAsset(key);
            });
            api.modifyBundlerChain((config, { CHAIN_ID })=>{
                let isUsingCssExtract = false;
                const ruleIds = Object.keys(config.module.rules.entries());
                for (const [ruleId, oneOfId] of [
                    [
                        CHAIN_ID.RULE.CSS,
                        CHAIN_ID.ONE_OF.CSS_MAIN
                    ],
                    [
                        CHAIN_ID.RULE.SASS,
                        'sass'
                    ],
                    [
                        CHAIN_ID.RULE.LESS,
                        'less'
                    ],
                    [
                        CHAIN_ID.RULE.STYLUS,
                        'stylus'
                    ]
                ]){
                    const matchedRuleIds = ruleId === CHAIN_ID.RULE.CSS ? [
                        ruleId
                    ] : ruleIds.filter((currentRuleId)=>isPreprocessorRule(ruleId, currentRuleId));
                    for (const matchedRuleId of matchedRuleIds){
                        if (!config.module.rules.has(matchedRuleId)) continue;
                        const mainRule = config.module.rule(matchedRuleId).oneOfs.get(oneOfId);
                        if (mainRule) {
                            if (mainRule.uses.has(CHAIN_ID.USE.MINI_CSS_EXTRACT)) {
                                isUsingCssExtract = true;
                                mainRule.use(CHAIN_ID.USE.MINI_CSS_EXTRACT).loader(cssConfig_require.resolve('./libCssExtractLoader.js')).options({
                                    rootDir,
                                    auto,
                                    banner,
                                    footer
                                });
                            }
                        }
                    }
                }
                if (isUsingCssExtract) {
                    const cssExtract = CHAIN_ID.PLUGIN.MINI_CSS_EXTRACT;
                    config.plugins.delete(cssExtract);
                    config.plugin(LibCssExtractPlugin.name).use(LibCssExtractPlugin);
                }
            });
        }
    });
function composeCssConfig(rootDir, auto, bundle = true, banner, footer) {
    if (bundle || null === rootDir) return {};
    return {
        plugins: [
            pluginLibCss(rootDir, auto, banner, footer)
        ],
        tools: {
            cssLoader: {
                import: false
            }
        }
    };
}
const isJavaScriptOutputFile = (file)=>/\.(cjs|mjs|js|jsx)$/.test(file);
const formatBinarySize = (size)=>{
    if (size >= 1000000) return `${(size / 1000000).toFixed(1)} MB`;
    if (size >= 1000) return `${(size / 1000).toFixed(1)} kB`;
    return `${size} B`;
};
const prettyTime = (seconds)=>{
    const format = (time)=>color.bold(time);
    if (seconds < 10) {
        const digits = seconds >= 0.01 ? 2 : 3;
        return `${format(seconds.toFixed(digits))} s`;
    }
    if (seconds < 60) return `${format(seconds.toFixed(1))} s`;
    const minutes = Math.floor(seconds / 60);
    const minutesLabel = `${format(minutes.toFixed(0))} m`;
    const remainingSeconds = seconds % 60;
    if (0 === remainingSeconds) return minutesLabel;
    const secondsLabel = `${format(remainingSeconds.toFixed(remainingSeconds % 1 === 0 ? 0 : 1))} s`;
    return `${minutesLabel} ${secondsLabel}`;
};
const getTimeCost = (start)=>prettyTime((Date.now() - start) / 1000);
const runCommand = async (command, args)=>new Promise((resolve, reject)=>{
        const child = spawn(command, args, {
            stdio: [
                'ignore',
                'pipe',
                'pipe'
            ]
        });
        let stdout = '';
        let stderr = '';
        child.stdout.on('data', (chunk)=>{
            stdout += chunk.toString();
        });
        child.stderr.on('data', (chunk)=>{
            stderr += chunk.toString();
        });
        child.on('error', (error)=>{
            reject(error);
        });
        child.on('close', (code)=>{
            if (0 === code) return void resolve({
                stdout,
                stderr
            });
            reject(new Error(`Command "${[
                command,
                ...args
            ].join(' ')}" exited with code ${code}.${stderr ? `\n${stderr.trim()}` : ''}`));
        });
    });
const ensureBinaryVersion = async (executableBinaryPath, builderBinaryPath = process.execPath)=>{
    const [resolvedVersion, builderVersion] = await Promise.all([
        readBinaryVersion(executableBinaryPath),
        readBinaryVersion(builderBinaryPath)
    ]);
    if (resolvedVersion !== builderVersion) throw new Error(`"experiments.exe" requires the target executable and builder Node.js binaries to use the same version, but received "${resolvedVersion}" from "${executableBinaryPath}" and "${builderVersion}" from "${builderBinaryPath}".`);
};
const readBinaryVersion = async (executableBinaryPath)=>{
    const { stdout } = await runCommand(executableBinaryPath, [
        '--version'
    ]);
    return stdout.trim().replace(/^v/, '');
};
const maybeSignMacBinary = async (filePath)=>{
    if ('darwin' !== process.platform) return;
    await runCommand('codesign', [
        '--sign',
        '-',
        filePath
    ]);
};
const resolveExecutableOutputPath = ({ environment, mainFile, target })=>{
    const builtEntryFileName = node_path.parse(mainFile).name;
    const baseFileName = target.fileName ?? builtEntryFileName;
    if (node_path.isAbsolute(baseFileName) || /[\\/]/.test(baseFileName)) throw new Error(`"experiments.exe.fileName" must be a file name, but received "${baseFileName}".`);
    const parsedName = node_path.parse(baseFileName);
    const suffixedBaseName = target.suffix ? `${parsedName.name}-${target.suffix}` : parsedName.name;
    const rawFileName = parsedName.ext ? `${suffixedBaseName}${parsedName.ext}` : suffixedBaseName;
    const normalizedFileName = 'win32' !== target.platform || rawFileName.toLowerCase().endsWith('.exe') ? rawFileName : `${rawFileName}.exe`;
    return node_path.join(target.outputPath ?? environment.distPath, normalizedFileName);
};
const resolveBuiltMainFile = ({ environment, entryName, stats })=>{
    const entrypoint = stats.compilation.entrypoints.get(entryName);
    if (!entrypoint) throw new Error(`Unable to find the emitted entrypoint "${entryName}" for "${environment.name}".`);
    const entryFiles = entrypoint.getFiles().filter(isJavaScriptOutputFile);
    if (1 !== entryFiles.length || !entryFiles[0]) throw new Error(`"experiments.exe" expects a single bundled JavaScript entry file, but "${environment.name}" emitted ${entryFiles.length} JavaScript files for entry "${entryName}": ${entryFiles.join(', ') || '(none)'}.`);
    return node_path.join(stats.compilation.outputOptions.path ?? environment.distPath, entryFiles[0]);
};
const MIN_EXE_NODE_VERSION = '25.7.0';
const parseVersion = (version)=>{
    const [major = 0, minor = 0, patch = 0] = version.replace(/^v/, '').split('.').map((value)=>Number.parseInt(value, 10));
    return {
        major,
        minor,
        patch
    };
};
const compareVersions = (left, right)=>{
    const leftVersion = parseVersion(left);
    const rightVersion = parseVersion(right);
    if (leftVersion.major !== rightVersion.major) return leftVersion.major - rightVersion.major;
    if (leftVersion.minor !== rightVersion.minor) return leftVersion.minor - rightVersion.minor;
    return leftVersion.patch - rightVersion.patch;
};
const isExeSupportedNodeVersion = (version = process.versions.node)=>compareVersions(version, MIN_EXE_NODE_VERSION) >= 0;
const assertSupportedExeNodeVersion = (version = process.versions.node)=>{
    if (isExeSupportedNodeVersion(version)) return;
    throw new Error(`"experiments.exe" requires Node.js >= ${MIN_EXE_NODE_VERSION}, but the current version is ${version}.`);
};
const assertSupportedExeRuntime = (versions = process.versions)=>{
    if (versions.bun) throw new Error('"experiments.exe" uses the Node.js SEA workflow and requires Node.js. For Bun, run "bun build --compile" on the JavaScript entry file generated by Rslib.');
    if (versions.deno) throw new Error('"experiments.exe" uses the Node.js SEA workflow and requires Node.js. For Deno, run "deno compile" on the JavaScript entry file generated by Rslib.');
    assertSupportedExeNodeVersion(versions.node);
};
const compareNodeVersions = (left, right)=>compareVersions(left, right);
const normalizeNodeVersion = (version = process.versions.node)=>version.startsWith('v') ? version : `v${version}`;
const getCurrentExePlatform = ()=>{
    if ('darwin' === process.platform || 'linux' === process.platform || 'win32' === process.platform) return process.platform;
    throw new Error(`"experiments.exe" does not support platform "${process.platform}".`);
};
const getCurrentExeArch = ()=>{
    if ('x64' === process.arch || 'arm64' === process.arch) return process.arch;
    throw new Error(`"experiments.exe" does not support architecture "${process.arch}".`);
};
const getExeCacheDir = ()=>{
    if ('win32' === process.platform) return node_path.join(process.env.LOCALAPPDATA ?? node_path.join(node_os.homedir(), 'AppData', 'Local'), 'rslib', 'sea');
    const xdgCacheHome = process.env.XDG_CACHE_HOME ?? node_path.join(node_os.homedir(), '.cache');
    return node_path.join(xdgCacheHome, 'rslib', 'sea');
};
const inFlightBinaryDownloads = new Map();
const shasumManifestCache = new Map();
const resolveCustomBinaryVersion = async (customBinaryPath)=>{
    try {
        return normalizeNodeVersion(await readBinaryVersion(customBinaryPath));
    } catch (error) {
        const originalError = error instanceof Error ? error.message : String(error);
        throw new Error(`"experiments.exe" could not determine the Node.js version of custom binary "${customBinaryPath}".\nOriginal error: ${originalError}`);
    }
};
const resolveArchiveInfo = ({ arch, nodeVersion, platform })=>{
    const normalizedVersion = normalizeNodeVersion(nodeVersion);
    const releasePlatform = 'win32' === platform ? 'win' : platform;
    const baseName = `node-${normalizedVersion}-${releasePlatform}-${arch}`;
    const archiveName = 'win32' === platform ? `${baseName}.zip` : `${baseName}.tar.gz`;
    const archiveDir = node_path.join(getExeCacheDir(), normalizedVersion, releasePlatform, arch);
    const archivePath = node_path.join(archiveDir, archiveName);
    const extractDir = node_path.join(archiveDir, baseName);
    const binaryPath = 'win32' === platform ? node_path.join(extractDir, 'node.exe') : node_path.join(extractDir, 'bin', 'node');
    return {
        archiveName,
        archivePath,
        binaryPath,
        extractDir,
        url: `https://nodejs.org/dist/${normalizedVersion}/${archiveName}`
    };
};
const getShasumManifest = async (nodeVersion)=>{
    const normalizedVersion = normalizeNodeVersion(nodeVersion);
    const cachedManifest = shasumManifestCache.get(normalizedVersion);
    if (cachedManifest) return cachedManifest;
    const manifestPromise = (async ()=>{
        const manifestUrl = `https://nodejs.org/dist/${normalizedVersion}/SHASUMS256.txt`;
        const response = await fetch(manifestUrl);
        if (!response.ok) throw new Error(`Failed to download "${manifestUrl}" for "experiments.exe": ${response.status} ${response.statusText}.`);
        const manifestText = await response.text();
        const checksums = new Map();
        for (const line of manifestText.split(/\r?\n/)){
            const match = line.match(/^([a-f0-9]{64})\s+\*?(.+)$/i);
            if (match?.[1] && match[2]) checksums.set(match[2], match[1].toLowerCase());
        }
        return checksums;
    })();
    shasumManifestCache.set(normalizedVersion, manifestPromise);
    try {
        return await manifestPromise;
    } catch (error) {
        shasumManifestCache.delete(normalizedVersion);
        throw error;
    }
};
const getExpectedArchiveChecksum = async ({ archiveName, nodeVersion })=>{
    const checksums = await getShasumManifest(nodeVersion);
    const checksum = checksums.get(archiveName);
    if (!checksum) throw new Error(`Failed to find checksum for "${archiveName}" in Node.js SHASUMS256.txt for "experiments.exe".`);
    return checksum;
};
const calculateFileSha256 = async (filePath)=>{
    const hash = createHash('sha256');
    const stream = node_fs.createReadStream(filePath);
    for await (const chunk of stream)hash.update(chunk);
    return hash.digest('hex');
};
const verifyArchiveChecksum = async ({ archivePath, expectedChecksum })=>{
    const actualChecksum = await calculateFileSha256(archivePath);
    if (actualChecksum !== expectedChecksum) throw new Error(`Checksum mismatch for "${archivePath}" in "experiments.exe": expected ${expectedChecksum}, received ${actualChecksum}.`);
};
const downloadArchive = async ({ archivePath, expectedChecksum, url })=>{
    const response = await fetch(url);
    if (!response.ok) throw new Error(`Failed to download "${url}" for "experiments.exe": ${response.status} ${response.statusText}.`);
    if (!response.body) throw new Error(`Failed to download "${url}" for "experiments.exe": response body is empty.`);
    await node_fs.promises.mkdir(node_path.dirname(archivePath), {
        recursive: true
    });
    const tempArchivePath = `${archivePath}.tmp-${process.pid}-${Date.now()}`;
    try {
        await pipeline(Readable.fromWeb(response.body), node_fs.createWriteStream(tempArchivePath));
        await verifyArchiveChecksum({
            archivePath: tempArchivePath,
            expectedChecksum
        });
        await node_fs.promises.rename(tempArchivePath, archivePath);
    } catch (error) {
        await node_fs.promises.rm(tempArchivePath, {
            force: true
        });
        throw error;
    }
};
const extractArchive = async ({ archivePath, extractDir, platform })=>{
    await node_fs.promises.mkdir(node_path.dirname(extractDir), {
        recursive: true
    });
    if ('win32' === platform) {
        if ('win32' === process.platform) return void await runCommand('powershell', [
            '-NoLogo',
            '-NoProfile',
            '-Command',
            `Expand-Archive -Path "${archivePath}" -DestinationPath "${node_path.dirname(extractDir)}" -Force`
        ]);
        await runCommand('unzip', [
            '-q',
            archivePath,
            '-d',
            node_path.dirname(extractDir)
        ]);
        return;
    }
    await runCommand('tar', [
        '-xzf',
        archivePath,
        '-C',
        node_path.dirname(extractDir)
    ]);
};
const ensureDownloadedBinary = async ({ arch, nodeVersion, platform })=>{
    const archiveInfo = resolveArchiveInfo({
        arch,
        nodeVersion,
        platform
    });
    const expectedChecksum = await getExpectedArchiveChecksum({
        archiveName: archiveInfo.archiveName,
        nodeVersion
    });
    const inFlightKey = archiveInfo.archivePath;
    const inFlightDownload = inFlightBinaryDownloads.get(inFlightKey);
    if (inFlightDownload) return inFlightDownload;
    const downloadPromise = (async ()=>{
        if (node_fs.existsSync(archiveInfo.binaryPath)) return archiveInfo.binaryPath;
        if (node_fs.existsSync(archiveInfo.archivePath)) try {
            await verifyArchiveChecksum({
                archivePath: archiveInfo.archivePath,
                expectedChecksum
            });
        } catch  {
            await node_fs.promises.rm(archiveInfo.archivePath, {
                force: true
            });
        }
        if (!node_fs.existsSync(archiveInfo.archivePath)) await downloadArchive({
            archivePath: archiveInfo.archivePath,
            expectedChecksum,
            url: archiveInfo.url
        });
        await extractArchive({
            archivePath: archiveInfo.archivePath,
            extractDir: archiveInfo.extractDir,
            platform
        });
        if (!node_fs.existsSync(archiveInfo.binaryPath)) throw new Error(`Failed to locate downloaded Node.js executable "${archiveInfo.binaryPath}" for "experiments.exe".`);
        if ('win32' !== platform) await node_fs.promises.chmod(archiveInfo.binaryPath, 493);
        return archiveInfo.binaryPath;
    })();
    inFlightBinaryDownloads.set(inFlightKey, downloadPromise);
    try {
        return await downloadPromise;
    } finally{
        if (inFlightBinaryDownloads.get(inFlightKey) === downloadPromise) inFlightBinaryDownloads.delete(inFlightKey);
    }
};
const resolveTargetBinaries = async (target)=>{
    const currentPlatform = getCurrentExePlatform();
    const currentArch = getCurrentExeArch();
    const currentNodeVersion = normalizeNodeVersion(process.versions.node);
    if (target.customBinaryPath) {
        const binaryVersion = await resolveCustomBinaryVersion(target.customBinaryPath);
        assertSupportedExeNodeVersion(binaryVersion);
        const builderBinaryPath = 0 === compareNodeVersions(binaryVersion, currentNodeVersion) ? process.execPath : await ensureDownloadedBinary({
            arch: currentArch,
            nodeVersion: binaryVersion,
            platform: currentPlatform
        });
        return {
            ...target,
            executableBinaryPath: target.customBinaryPath,
            builderBinaryPath,
            nodeVersion: binaryVersion,
            suffix: target.suffix ? `${target.platform}-${target.arch}-${binaryVersion}` : null
        };
    }
    assertSupportedExeNodeVersion(target.nodeVersion);
    const sameCurrentRuntime = currentPlatform === target.platform && currentArch === target.arch && currentNodeVersion === target.nodeVersion;
    const targetBinaryPath = sameCurrentRuntime ? process.execPath : await ensureDownloadedBinary({
        arch: target.arch,
        nodeVersion: target.nodeVersion,
        platform: target.platform
    });
    const builderBinaryPath = sameCurrentRuntime ? process.execPath : 0 === compareNodeVersions(target.nodeVersion, currentNodeVersion) ? process.execPath : await ensureDownloadedBinary({
        arch: currentArch,
        nodeVersion: target.nodeVersion,
        platform: currentPlatform
    });
    return {
        ...target,
        executableBinaryPath: targetBinaryPath,
        builderBinaryPath
    };
};
const buildExecutable = async ({ builderBinaryPath, executablePath, executableBinaryPath, mainFile, format, root, seaOptions, targetPlatform })=>{
    const tempDir = await node_fs.promises.mkdtemp(join(node_os.tmpdir(), 'rslib-sea-'));
    try {
        await node_fs.promises.mkdir(dirname(executablePath), {
            recursive: true
        });
        const seaConfigPath = join(tempDir, 'sea-config.json');
        const seaConfig = {
            main: mainFile,
            mainFormat: 'esm' === format ? 'module' : 'commonjs',
            output: executablePath,
            executable: executableBinaryPath,
            disableExperimentalSEAWarning: seaOptions.disableExperimentalSEAWarning ?? true,
            ...seaOptions.useSnapshot ? {
                useSnapshot: true
            } : {},
            ...seaOptions.useCodeCache ? {
                useCodeCache: true
            } : {},
            ...seaOptions.execArgv ? {
                execArgv: seaOptions.execArgv
            } : {},
            ...seaOptions.execArgvExtension ? {
                execArgvExtension: seaOptions.execArgvExtension
            } : {},
            ...seaOptions.assets ? {
                assets: Object.fromEntries(Object.entries(seaOptions.assets).map(([key, value])=>[
                        key,
                        node_path.resolve(root, value)
                    ]))
            } : {}
        };
        await node_fs.promises.writeFile(seaConfigPath, `${JSON.stringify(seaConfig, null, 2)}\n`);
        await runCommand(builderBinaryPath, [
            '--build-sea',
            seaConfigPath
        ]);
        if ('darwin' === targetPlatform) await maybeSignMacBinary(executablePath);
    } finally{
        await node_fs.promises.rm(tempDir, {
            recursive: true,
            force: true
        });
    }
};
const ExePlugin = ({ targets, format, root })=>({
        name: 'rslib:exe',
        setup (api) {
            api.onAfterEnvironmentCompile(async ({ environment, stats })=>{
                if (!stats || stats.hasErrors()) return;
                const entryNames = Array.from(stats.compilation.entrypoints.keys());
                if (1 !== entryNames.length || !entryNames[0]) throw new Error(`"experiments.exe" currently supports a single entry per library, but "${environment.name}" has ${entryNames.length} entries. Split them into multiple "lib" items to generate multiple executables.`);
                src_logger.info('generate single executable application started...');
                const entryName = entryNames[0];
                const mainFile = resolveBuiltMainFile({
                    environment,
                    entryName,
                    stats
                });
                const resolvedTargets = await Promise.all(targets.map((target)=>resolveTargetBinaries(target)));
                await Promise.all(resolvedTargets.map(async (target)=>{
                    const start = Date.now();
                    const executablePath = resolveExecutableOutputPath({
                        environment,
                        mainFile,
                        target
                    });
                    const relativeExecutablePath = node_path.relative(root, executablePath) || executablePath;
                    if (target.customBinaryPath) await ensureBinaryVersion(target.executableBinaryPath, target.builderBinaryPath);
                    await buildExecutable({
                        builderBinaryPath: target.builderBinaryPath,
                        executablePath,
                        executableBinaryPath: target.executableBinaryPath,
                        mainFile,
                        format,
                        root,
                        seaOptions: target.seaOptions,
                        targetPlatform: target.platform
                    });
                    const { size } = await node_fs.promises.stat(executablePath);
                    src_logger.info(`generated executable: ${color.cyan(relativeExecutablePath)} ${color.green(`(${formatBinarySize(size)})`)} in ${getTimeCost(start)} ${color.dim(`(${environment.name})`)}`);
                }));
            });
        }
    });
const normalizeTargetsInput = (exe)=>{
    if (true === exe) return [
        {}
    ];
    return exe.targets && exe.targets.length > 0 ? exe.targets : [
        {}
    ];
};
const resolveExeTargets = (exe, root = process.cwd())=>{
    const currentPlatform = getCurrentExePlatform();
    const currentArch = getCurrentExeArch();
    const currentNodeVersion = normalizeNodeVersion(process.versions.node);
    const targets = normalizeTargetsInput(exe);
    const sharedOptions = exe && true !== exe ? {
        fileName: exe.fileName,
        outputPath: exe.outputPath ? node_path.resolve(root, exe.outputPath) : void 0,
        seaOptions: exe.seaOptions ?? {}
    } : {
        fileName: void 0,
        outputPath: void 0,
        seaOptions: {}
    };
    const normalizedTargets = targets.map((target, index)=>{
        const targetConfig = 'string' == typeof target ? {} : target;
        const resolvedCustomBinaryPath = 'string' == typeof target ? node_path.resolve(root, target) : void 0;
        const platform = targetConfig.platform ?? currentPlatform;
        const arch = targetConfig.arch ?? currentArch;
        const nodeVersion = normalizeNodeVersion(targetConfig.nodeVersion ?? currentNodeVersion);
        const isCrossPlatform = platform !== currentPlatform || arch !== currentArch;
        const normalizedTarget = {
            arch,
            fileName: sharedOptions.fileName,
            index,
            nodeVersion,
            outputPath: sharedOptions.outputPath,
            platform,
            customBinaryPath: resolvedCustomBinaryPath,
            seaOptions: isCrossPlatform ? {
                ...sharedOptions.seaOptions,
                useCodeCache: false,
                useSnapshot: false
            } : sharedOptions.seaOptions,
            suffix: null
        };
        return normalizedTarget;
    });
    if (normalizedTargets.length <= 1) return normalizedTargets;
    return normalizedTargets.map((target)=>({
            ...target,
            suffix: `${target.platform}-${target.arch}-${target.nodeVersion}`
        }));
};
const validateExeTargets = ({ bundle, targets, format, sourceEntry, target })=>{
    if (0 === targets.length) return;
    if ('esm' !== format && 'cjs' !== format) throw new Error(`"experiments.exe" only supports "esm" and "cjs" formats, but received ${JSON.stringify(format)}.`);
    if (!bundle) throw new Error('"experiments.exe" requires "bundle" to be "true".');
    if ('node' !== target) throw new Error(`"experiments.exe" only supports "output.target = \\"node\\"", but received ${JSON.stringify(target)}.`);
    if (sourceEntry && Object.keys(sourceEntry).length > 1) throw new Error(`"experiments.exe" currently supports a single entry per library, but received ${Object.keys(sourceEntry).length} entries.`);
    assertSupportedExeRuntime();
    for (const targetItem of targets)if ('esm' === format && targetItem.seaOptions.useSnapshot) throw new Error('"experiments.exe.seaOptions.useSnapshot" cannot be used together with "esm" executables.');
};
const composeExeConfig = ({ bundle, exe, format, root, sourceEntry, target })=>{
    if (!exe) return {
        config: {}
    };
    const normalizedTargets = resolveExeTargets(exe, root);
    validateExeTargets({
        bundle,
        targets: normalizedTargets,
        format,
        sourceEntry,
        target
    });
    const exeFormat = 'esm' === format ? 'esm' : 'cjs';
    const config = {
        plugins: [
            ExePlugin({
                targets: normalizedTargets,
                format: exeFormat,
                root
            })
        ],
        tools: {
            rspack: {
                optimization: {
                    runtimeChunk: false,
                    splitChunks: false
                },
                output: {
                    asyncChunks: false
                }
            }
        }
    };
    return {
        config
    };
};
const EntryChunkPlugin_PLUGIN_NAME = 'rsbuild:lib-entry-chunk';
const IMPORT_META_URL_SHIM = `const __rslib_import_meta_url__ = /*#__PURE__*/ (function () {
  return typeof document === 'undefined'
    ? new (require('url'.replace('', '')).URL)('file:' + __filename).href
    : (document.currentScript && document.currentScript.src) ||
      new URL('main.js', document.baseURI).href;
})();
`;
class EntryChunkPlugin {
    shimsInjectedAssets = new Set();
    enabledImportMetaUrlShim;
    contextToWatch = null;
    constructor({ enabledImportMetaUrlShim = true, contextToWatch }){
        this.enabledImportMetaUrlShim = enabledImportMetaUrlShim;
        this.contextToWatch = contextToWatch;
    }
    apply(compiler) {
        compiler.hooks.afterCompile.tap(EntryChunkPlugin_PLUGIN_NAME, (compilation)=>{
            if (null === this.contextToWatch) return;
            const contextDep = compilation.contextDependencies;
            if (!contextDep.has(this.contextToWatch)) contextDep.add(this.contextToWatch);
        });
        compiler.hooks.make.tap(EntryChunkPlugin_PLUGIN_NAME, (compilation)=>{
            compilation.hooks.chunkAsset.tap(EntryChunkPlugin_PLUGIN_NAME, (_chunk, filename)=>{
                const isJs = JS_EXTENSIONS_PATTERN.test(filename);
                if (!isJs) return;
                this.shimsInjectedAssets.add(filename);
            });
        });
        compiler.hooks.make.tap(EntryChunkPlugin_PLUGIN_NAME, (compilation)=>{
            compilation.hooks.processAssets.tap(EntryChunkPlugin_PLUGIN_NAME, (assets)=>{
                if (!this.enabledImportMetaUrlShim) return;
                const chunkAsset = Object.keys(assets).filter((name)=>JS_EXTENSIONS_PATTERN.test(name) && this.shimsInjectedAssets.has(name));
                for (const name of chunkAsset)compilation.updateAsset(name, (old)=>{
                    const oldSource = old.source().toString();
                    const replaceSource = new rspack.sources.ReplaceSource(old);
                    if (oldSource.startsWith('#!')) {
                        const firstLineEnd = oldSource.indexOf('\n');
                        replaceSource.insert(firstLineEnd + 1, IMPORT_META_URL_SHIM);
                    } else if (oldSource.startsWith("'use strict'") || oldSource.startsWith('"use strict"')) replaceSource.replace(0, 11, `"use strict";\n${IMPORT_META_URL_SHIM}`);
                    else replaceSource.insert(0, IMPORT_META_URL_SHIM);
                    return replaceSource;
                });
            });
        });
    }
}
const composeEntryChunkConfig = ({ enabledImportMetaUrlShim, contextToWatch = null })=>({
        tools: {
            rspack: {
                plugins: [
                    new EntryChunkPlugin({
                        enabledImportMetaUrlShim,
                        contextToWatch
                    })
                ]
            }
        }
    });
const pluginCjsShims = (enabledShims)=>({
        name: 'rsbuild:cjs-shims',
        setup (api) {
            api.modifyEnvironmentConfig((config)=>{
                config.source.define = {
                    ...config.source.define,
                    ...enabledShims['import.meta.url'] && {
                        'import.meta.url': '__rslib_import_meta_url__'
                    },
                    ...enabledShims['import.meta.dirname'] && {
                        'import.meta.dirname': '__dirname'
                    },
                    ...enabledShims['import.meta.filename'] && {
                        'import.meta.filename': '__filename'
                    }
                };
            });
        }
    });
const requireShim = `// Rslib ESM shims
import __rslib_shim_module__ from "node:module";
const require = /*#__PURE__*/ __rslib_shim_module__.createRequire(/*#__PURE__*/ (() => import.meta.url)());
`;
const pluginEsmRequireShim = ()=>({
        name: 'rsbuild:esm-require-shim',
        setup (api) {
            api.modifyRspackConfig((config)=>{
                config.plugins ??= [];
                config.plugins.push(new rspack.BannerPlugin({
                    banner: requireShim,
                    stage: rspack.Compilation.PROCESS_ASSETS_STAGE_OPTIMIZE - 1,
                    raw: true,
                    include: /\.(js|mjs)$/
                }));
            });
        }
    });
const getDefaultExtension = (options)=>{
    const { format, pkgJson, autoExtension } = options;
    let jsExtension = '.js';
    let dtsExtension = '.d.ts';
    if (!autoExtension) return {
        jsExtension,
        dtsExtension
    };
    if (!pkgJson) {
        src_logger.warn('The `autoExtension` configuration will not be applied due to read package.json failed');
        return {
            jsExtension,
            dtsExtension
        };
    }
    const isModule = 'module' === pkgJson.type;
    if (isModule && 'cjs' === format) {
        jsExtension = '.cjs';
        dtsExtension = '.d.cts';
    }
    if (!isModule && 'esm' === format) {
        jsExtension = '.mjs';
        dtsExtension = '.d.mts';
    }
    return {
        jsExtension,
        dtsExtension,
        isModule
    };
};
const LATEST_TARGET_VERSIONS = {
    node: [
        'last 1 node versions'
    ],
    web: [
        'last 1 Chrome versions',
        'last 1 Firefox versions',
        'last 1 Edge versions',
        'last 1 Safari versions',
        'last 1 ios_saf versions',
        'not dead'
    ],
    get 'web-worker' () {
        return LATEST_TARGET_VERSIONS.web;
    }
};
const calcEsnextBrowserslistByTarget = (target)=>{
    if ('node' === target) return LATEST_TARGET_VERSIONS.node;
    return LATEST_TARGET_VERSIONS.web;
};
const RSPACK_TARGET_UNLISTED_MODERN_ECMA_VERSIONS = [
    'es2023',
    'es2024',
    'esnext'
];
const ESX_TO_BROWSERSLIST = {
    es5: {
        chrome: '13',
        edge: '12',
        firefox: '2',
        ios: '6',
        node: '0.6',
        safari: '5.1'
    },
    get es6 () {
        return ESX_TO_BROWSERSLIST.es2015;
    },
    es2015: {
        chrome: '51',
        edge: '79',
        firefox: '53',
        ios: '16.3',
        node: '6.5',
        safari: '16.3'
    },
    es2016: {
        chrome: '52',
        edge: '79',
        firefox: '53',
        ios: '16.3',
        node: '7',
        safari: '16.3'
    },
    es2017: {
        chrome: '55',
        edge: '79',
        firefox: '53',
        ios: '16.3',
        node: '7.6',
        safari: '16.3'
    },
    es2018: {
        chrome: '64',
        edge: '79',
        firefox: '78',
        ios: '16.3',
        node: '10',
        safari: '16.3'
    },
    es2019: {
        chrome: '66',
        edge: '79',
        firefox: '78',
        ios: '16.3',
        node: '10',
        safari: '16.3'
    },
    es2020: {
        chrome: '91',
        edge: '91',
        firefox: '80',
        ios: '16.3',
        node: '16.9',
        safari: '16.3'
    },
    es2021: {
        chrome: '91',
        edge: '91',
        firefox: '80',
        ios: '16.3',
        node: '16.9',
        safari: '16.3'
    },
    es2022: {
        chrome: '94',
        edge: '94',
        firefox: '93',
        ios: '16.4',
        node: '16.11',
        safari: '16.4'
    },
    get es2023 () {
        return ESX_TO_BROWSERSLIST.es2022;
    },
    es2024: calcEsnextBrowserslistByTarget,
    esnext: calcEsnextBrowserslistByTarget
};
function transformSyntaxToRspackTarget(syntax) {
    const handleSyntaxItem = (syntaxItem)=>{
        const normalizedSyntaxItem = syntaxItem.toLowerCase();
        if (normalizedSyntaxItem.startsWith('es')) {
            if (normalizedSyntaxItem in ESX_TO_BROWSERSLIST) {
                if (RSPACK_TARGET_UNLISTED_MODERN_ECMA_VERSIONS.includes(normalizedSyntaxItem)) return 'es2022';
                if ('es6' === normalizedSyntaxItem) return 'es2015';
                return normalizedSyntaxItem;
            }
            throw new Error(`Unsupported ES version: ${syntaxItem}`);
        }
        return `browserslist:${syntaxItem}`;
    };
    if (Array.isArray(syntax)) return syntax.map(handleSyntaxItem);
    return [
        handleSyntaxItem(syntax)
    ];
}
function transformSyntaxToBrowserslist(syntax, target) {
    const handleSyntaxItem = (syntaxItem)=>{
        const normalizedSyntaxItem = syntaxItem.toLowerCase();
        if (normalizedSyntaxItem.startsWith('es')) {
            if (normalizedSyntaxItem in ESX_TO_BROWSERSLIST) {
                const browserslistItem = ESX_TO_BROWSERSLIST[normalizedSyntaxItem];
                if ('function' == typeof browserslistItem) return browserslistItem(target);
                return Object.entries(browserslistItem).flatMap(([engine, version])=>{
                    if (Array.isArray(version)) return version;
                    return `${engine} >= ${version}`;
                });
            }
            throw new Error(`Unsupported ES version: ${syntaxItem}`);
        }
        return [
            syntaxItem
        ];
    };
    if (Array.isArray(syntax)) return syntax.flatMap(handleSyntaxItem);
    return handleSyntaxItem(syntax);
}
const POSIX_SEP_RE = new RegExp('\\' + node_path.posix.sep, 'g');
const NATIVE_SEP_RE = new RegExp('\\' + node_path.sep, 'g');
const PATTERN_REGEX_CACHE = new Map();
const GLOB_ALL_PATTERN = "**/*";
const TS_EXTENSIONS = [
    '.ts',
    '.tsx',
    '.mts',
    '.cts'
];
const JS_EXTENSIONS = [
    '.js',
    '.jsx',
    '.mjs',
    '.cjs'
];
const TSJS_EXTENSIONS = TS_EXTENSIONS.concat(JS_EXTENSIONS);
const TS_EXTENSIONS_RE_GROUP = `\\.(?:${TS_EXTENSIONS.map((ext)=>ext.substring(1)).join('|')})`;
const TSJS_EXTENSIONS_RE_GROUP = `\\.(?:${TSJS_EXTENSIONS.map((ext)=>ext.substring(1)).join('|')})`;
const IS_POSIX = node_path.posix.sep === node_path.sep;
function util_makePromise() {
    let resolve, reject;
    const promise = new Promise((res, rej)=>{
        resolve = res;
        reject = rej;
    });
    return {
        promise,
        resolve,
        reject
    };
}
async function util_resolveTSConfigJson(filename, cache) {
    if ('.json' !== node_path.extname(filename)) return;
    const tsconfig = node_path.resolve(filename);
    if (cache && (cache.hasParseResult(tsconfig) || cache.hasParseResult(filename))) return tsconfig;
    return external_node_fs_promises.stat(tsconfig).then((stat)=>{
        if (stat.isFile() || stat.isFIFO()) return tsconfig;
        throw new Error(`${filename} exists but is not a regular file.`);
    });
}
const util_isInNodeModules = IS_POSIX ? (dir)=>dir.includes('/node_modules/') : (dir)=>dir.match(/[/\\]node_modules[/\\]/);
const posix2native = IS_POSIX ? (filename)=>filename : (filename)=>filename.replace(POSIX_SEP_RE, node_path.sep);
const util_native2posix = IS_POSIX ? (filename)=>filename : (filename)=>filename.replace(NATIVE_SEP_RE, node_path.posix.sep);
const resolve2posix = IS_POSIX ? (dir, filename)=>dir ? node_path.resolve(dir, filename) : node_path.resolve(filename) : (dir, filename)=>util_native2posix(dir ? node_path.resolve(posix2native(dir), posix2native(filename)) : node_path.resolve(posix2native(filename)));
function util_resolveReferencedTSConfigFiles(result, options) {
    const dir = node_path.dirname(result.tsconfigFile);
    return result.tsconfig.references.map((ref)=>{
        const refPath = ref.path.endsWith('.json') ? ref.path : node_path.join(ref.path, options?.configName ?? 'tsconfig.json');
        return resolve2posix(dir, refPath);
    });
}
function util_resolveSolutionTSConfig(filename, result) {
    const allowJs = result.tsconfig.compilerOptions?.allowJs;
    const extensions = allowJs ? TSJS_EXTENSIONS : TS_EXTENSIONS;
    if (result.referenced && extensions.some((ext)=>filename.endsWith(ext)) && !util_isIncluded(filename, result)) {
        const solutionTSConfig = result.referenced.find((referenced)=>util_isIncluded(filename, referenced));
        if (solutionTSConfig) return solutionTSConfig;
    }
    return result;
}
function util_isIncluded(filename, result) {
    const dir = util_native2posix(node_path.dirname(result.tsconfigFile));
    const files = (result.tsconfig.files || []).map((file)=>resolve2posix(dir, file));
    const absoluteFilename = resolve2posix(null, filename);
    if (files.includes(filename)) return true;
    const allowJs = result.tsconfig.compilerOptions?.allowJs;
    const isIncluded = isGlobMatch(absoluteFilename, dir, result.tsconfig.include || (result.tsconfig.files ? [] : [
        GLOB_ALL_PATTERN
    ]), allowJs);
    if (isIncluded) {
        const isExcluded = isGlobMatch(absoluteFilename, dir, result.tsconfig.exclude || [], allowJs);
        return !isExcluded;
    }
    return false;
}
function isGlobMatch(filename, dir, patterns, allowJs) {
    const extensions = allowJs ? TSJS_EXTENSIONS : TS_EXTENSIONS;
    return patterns.some((pattern)=>{
        let lastWildcardIndex = pattern.length;
        let hasWildcard = false;
        let hasExtension = false;
        let hasSlash = false;
        let lastSlashIndex = -1;
        for(let i = pattern.length - 1; i > -1; i--){
            const c = pattern[i];
            if (!hasWildcard) {
                if ('*' === c || '?' === c) {
                    lastWildcardIndex = i;
                    hasWildcard = true;
                }
            }
            if (!hasSlash) {
                if ('.' === c) hasExtension = true;
                else if ('/' === c) {
                    lastSlashIndex = i;
                    hasSlash = true;
                }
            }
            if (hasWildcard && hasSlash) break;
        }
        if (!hasExtension && (!hasWildcard || lastWildcardIndex < lastSlashIndex)) {
            pattern += `${pattern.endsWith('/') ? '' : '/'}${GLOB_ALL_PATTERN}`;
            lastWildcardIndex = pattern.length - 1;
            hasWildcard = true;
        }
        if (lastWildcardIndex < pattern.length - 1 && !filename.endsWith(pattern.slice(lastWildcardIndex + 1))) return false;
        if (pattern.endsWith('*') && !extensions.some((ext)=>filename.endsWith(ext))) return false;
        if (pattern === GLOB_ALL_PATTERN) return filename.startsWith(`${dir}/`);
        const resolvedPattern = resolve2posix(dir, pattern);
        let firstWildcardIndex = -1;
        for(let i = 0; i < resolvedPattern.length; i++)if ('*' === resolvedPattern[i] || '?' === resolvedPattern[i]) {
            firstWildcardIndex = i;
            hasWildcard = true;
            break;
        }
        if (firstWildcardIndex > 1 && !filename.startsWith(resolvedPattern.slice(0, firstWildcardIndex - 1))) return false;
        if (!hasWildcard) return filename === resolvedPattern;
        if (firstWildcardIndex + GLOB_ALL_PATTERN.length === resolvedPattern.length - (pattern.length - 1 - lastWildcardIndex) && resolvedPattern.slice(firstWildcardIndex, firstWildcardIndex + GLOB_ALL_PATTERN.length) === GLOB_ALL_PATTERN) return true;
        if (PATTERN_REGEX_CACHE.has(resolvedPattern)) return PATTERN_REGEX_CACHE.get(resolvedPattern).test(filename);
        const regex = pattern2regex(resolvedPattern, allowJs);
        PATTERN_REGEX_CACHE.set(resolvedPattern, regex);
        return regex.test(filename);
    });
}
function pattern2regex(resolvedPattern, allowJs) {
    let regexStr = '^';
    for(let i = 0; i < resolvedPattern.length; i++){
        const char = resolvedPattern[i];
        if ('?' === char) {
            regexStr += '[^\\/]';
            continue;
        }
        if ('*' === char) {
            if ('*' === resolvedPattern[i + 1] && '/' === resolvedPattern[i + 2]) {
                i += 2;
                regexStr += '(?:[^\\/]*\\/)*';
                continue;
            }
            regexStr += '[^\\/]*';
            continue;
        }
        if ('/.+^${}()|[]\\'.includes(char)) regexStr += "\\";
        regexStr += char;
    }
    if (resolvedPattern.endsWith('*')) regexStr += allowJs ? TSJS_EXTENSIONS_RE_GROUP : TS_EXTENSIONS_RE_GROUP;
    regexStr += '$';
    return new RegExp(regexStr);
}
function util_replaceTokens(result) {
    if (result.tsconfig) result.tsconfig = JSON.parse(JSON.stringify(result.tsconfig).replaceAll(/"\${configDir}/g, `"${util_native2posix(node_path.dirname(result.tsconfigFile))}`));
}
async function find(filename, options) {
    let dir = node_path.dirname(node_path.resolve(filename));
    if (options?.ignoreNodeModules && util_isInNodeModules(dir)) return null;
    const cache = options?.cache;
    const configName = options?.configName ?? 'tsconfig.json';
    if (cache?.hasConfigPath(dir, configName)) return cache.getConfigPath(dir, configName);
    const { promise, resolve, reject } = util_makePromise();
    if (options?.root && !node_path.isAbsolute(options.root)) options.root = node_path.resolve(options.root);
    findUp(dir, {
        promise,
        resolve,
        reject
    }, options);
    return promise;
}
function findUp(dir, { resolve, reject, promise }, options) {
    const { cache, root, configName } = options ?? {};
    if (cache) if (cache.hasConfigPath(dir, configName)) {
        let cached;
        try {
            cached = cache.getConfigPath(dir, configName);
        } catch (e) {
            reject(e);
            return;
        }
        if (cached?.then) cached.then(resolve).catch(reject);
        else resolve(cached);
    } else cache.setConfigPath(dir, promise, configName);
    const tsconfig = node_path.join(dir, options?.configName ?? 'tsconfig.json');
    node_fs.stat(tsconfig, (err, stats)=>{
        if (stats && (stats.isFile() || stats.isFIFO())) resolve(tsconfig);
        else if (err?.code !== 'ENOENT') reject(err);
        else {
            let parent;
            if (root === dir || (parent = node_path.dirname(dir)) === dir) resolve(null);
            else findUp(parent, {
                promise,
                resolve,
                reject
            }, options);
        }
    });
}
node_path.sep;
function toJson(tsconfigJson) {
    const stripped = stripDanglingComma(stripJsonComments(stripBom(tsconfigJson)));
    if ('' === stripped.trim()) return '{}';
    return stripped;
}
function stripDanglingComma(pseudoJson) {
    let insideString = false;
    let offset = 0;
    let result = '';
    let danglingCommaPos = null;
    for(let i = 0; i < pseudoJson.length; i++){
        const currentCharacter = pseudoJson[i];
        if ('"' === currentCharacter) {
            const escaped = isEscaped(pseudoJson, i);
            if (!escaped) insideString = !insideString;
        }
        if (insideString) {
            danglingCommaPos = null;
            continue;
        }
        if (',' === currentCharacter) {
            danglingCommaPos = i;
            continue;
        }
        if (danglingCommaPos) {
            if ('}' === currentCharacter || ']' === currentCharacter) {
                result += pseudoJson.slice(offset, danglingCommaPos) + ' ';
                offset = danglingCommaPos + 1;
                danglingCommaPos = null;
            } else if (!currentCharacter.match(/\s/)) danglingCommaPos = null;
        }
    }
    return result + pseudoJson.substring(offset);
}
function isEscaped(jsonString, quotePosition) {
    let index = quotePosition - 1;
    let backslashCount = 0;
    while('\\' === jsonString[index]){
        index -= 1;
        backslashCount += 1;
    }
    return Boolean(backslashCount % 2);
}
function strip(string, start, end) {
    return string.slice(start, end).replace(/\S/g, ' ');
}
const singleComment = Symbol('singleComment');
const multiComment = Symbol('multiComment');
function stripJsonComments(jsonString) {
    let isInsideString = false;
    let isInsideComment = false;
    let offset = 0;
    let result = '';
    for(let index = 0; index < jsonString.length; index++){
        const currentCharacter = jsonString[index];
        const nextCharacter = jsonString[index + 1];
        if (!isInsideComment && '"' === currentCharacter) {
            const escaped = isEscaped(jsonString, index);
            if (!escaped) isInsideString = !isInsideString;
        }
        if (!isInsideString) if (isInsideComment || currentCharacter + nextCharacter !== '//') if (isInsideComment === singleComment && currentCharacter + nextCharacter === '\r\n') {
            index++;
            isInsideComment = false;
            result += strip(jsonString, offset, index);
            offset = index;
        } else if (isInsideComment === singleComment && '\n' === currentCharacter) {
            isInsideComment = false;
            result += strip(jsonString, offset, index);
            offset = index;
        } else if (isInsideComment || currentCharacter + nextCharacter !== '/*') {
            if (isInsideComment === multiComment && currentCharacter + nextCharacter === '*/') {
                index++;
                isInsideComment = false;
                result += strip(jsonString, offset, index + 1);
                offset = index + 1;
            }
        } else {
            result += jsonString.slice(offset, index);
            offset = index;
            isInsideComment = multiComment;
            index++;
        }
        else {
            result += jsonString.slice(offset, index);
            offset = index;
            isInsideComment = singleComment;
            index++;
        }
    }
    return result + (isInsideComment ? strip(jsonString.slice(offset)) : jsonString.slice(offset));
}
function stripBom(string) {
    if (0xfeff === string.charCodeAt(0)) return string.slice(1);
    return string;
}
const not_found_result = {
    tsconfigFile: null,
    tsconfig: {}
};
async function parse_parse(filename, options) {
    const cache = options?.cache;
    if (cache?.hasParseResult(filename)) return getParsedDeep(filename, cache, options);
    const { resolve, reject, promise } = util_makePromise();
    cache?.setParseResult(filename, promise, true);
    try {
        let tsconfigFile = await util_resolveTSConfigJson(filename, cache) || await find(filename, options);
        if (!tsconfigFile) {
            resolve(not_found_result);
            return promise;
        }
        let result;
        if (filename !== tsconfigFile && cache?.hasParseResult(tsconfigFile)) result = await getParsedDeep(tsconfigFile, cache, options);
        else {
            result = await parseFile(tsconfigFile, cache, filename === tsconfigFile);
            await Promise.all([
                parseExtends(result, cache),
                parseReferences(result, options)
            ]);
        }
        util_replaceTokens(result);
        resolve(util_resolveSolutionTSConfig(filename, result));
    } catch (e) {
        reject(e);
    }
    return promise;
}
async function getParsedDeep(filename, cache, options) {
    const result = await cache.getParseResult(filename);
    if (result.tsconfig.extends && !result.extended || result.tsconfig.references && !result.referenced) {
        const promise = Promise.all([
            parseExtends(result, cache),
            parseReferences(result, options)
        ]).then(()=>result);
        cache.setParseResult(filename, promise, true);
        return promise;
    }
    return result;
}
async function parseFile(tsconfigFile, cache, skipCache) {
    if (!skipCache && cache?.hasParseResult(tsconfigFile) && !cache.getParseResult(tsconfigFile)._isRootFile_) return cache.getParseResult(tsconfigFile);
    const promise = external_node_fs_promises.readFile(tsconfigFile, 'utf-8').then(toJson).then((json)=>{
        const parsed = JSON.parse(json);
        applyDefaults(parsed, tsconfigFile);
        return {
            tsconfigFile,
            tsconfig: normalizeTSConfig(parsed, node_path.dirname(tsconfigFile))
        };
    }).catch((e)=>{
        throw new TSConfckParseError(`parsing ${tsconfigFile} failed: ${e}`, 'PARSE_FILE', tsconfigFile, e);
    });
    if (!skipCache && (!cache?.hasParseResult(tsconfigFile) || !cache.getParseResult(tsconfigFile)._isRootFile_)) cache?.setParseResult(tsconfigFile, promise);
    return promise;
}
function normalizeTSConfig(tsconfig, dir) {
    const baseUrl = tsconfig.compilerOptions?.baseUrl;
    if (baseUrl && !baseUrl.startsWith('${') && !node_path.isAbsolute(baseUrl)) tsconfig.compilerOptions.baseUrl = resolve2posix(dir, baseUrl);
    return tsconfig;
}
async function parseReferences(result, options) {
    if (!result.tsconfig.references) return;
    const referencedFiles = util_resolveReferencedTSConfigFiles(result, options);
    const referenced = await Promise.all(referencedFiles.map((file)=>parseFile(file, options?.cache)));
    await Promise.all(referenced.map((ref)=>parseExtends(ref, options?.cache)));
    referenced.forEach((ref)=>{
        ref.solution = result;
        util_replaceTokens(ref);
    });
    result.referenced = referenced;
}
async function parseExtends(result, cache) {
    if (!result.tsconfig.extends) return;
    const extended = [
        {
            tsconfigFile: result.tsconfigFile,
            tsconfig: JSON.parse(JSON.stringify(result.tsconfig))
        }
    ];
    let pos = 0;
    const extendsPath = [];
    let currentBranchDepth = 0;
    while(pos < extended.length){
        const extending = extended[pos];
        extendsPath.push(extending.tsconfigFile);
        if (extending.tsconfig.extends) {
            currentBranchDepth += 1;
            let resolvedExtends;
            resolvedExtends = Array.isArray(extending.tsconfig.extends) ? extending.tsconfig.extends.reverse().map((ex)=>resolveExtends(ex, extending.tsconfigFile)) : [
                resolveExtends(extending.tsconfig.extends, extending.tsconfigFile)
            ];
            const circularExtends = resolvedExtends.find((tsconfigFile)=>extendsPath.includes(tsconfigFile));
            if (circularExtends) {
                const circle = extendsPath.concat([
                    circularExtends
                ]).join(' -> ');
                throw new TSConfckParseError(`Circular dependency in "extends": ${circle}`, 'EXTENDS_CIRCULAR', result.tsconfigFile);
            }
            extended.splice(pos + 1, 0, ...await Promise.all(resolvedExtends.map((file)=>parseFile(file, cache))));
        } else {
            extendsPath.splice(-currentBranchDepth);
            currentBranchDepth = 0;
        }
        pos += 1;
    }
    result.extended = extended;
    for (const ext of result.extended.slice(1))extendTSConfig(result, ext);
}
function resolveExtends(extended, from) {
    if ([
        '.',
        '..'
    ].includes(extended)) extended += '/tsconfig.json';
    const req = external_module_createRequire(from);
    let error;
    try {
        return req.resolve(extended);
    } catch (e) {
        error = e;
    }
    if ('.' !== extended[0] && !node_path.isAbsolute(extended)) try {
        return req.resolve(`${extended}/tsconfig.json`);
    } catch (e) {
        error = e;
    }
    throw new TSConfckParseError(`failed to resolve "extends":"${extended}" in ${from}`, 'EXTENDS_RESOLVE', from, error);
}
const EXTENDABLE_KEYS = [
    'compilerOptions',
    'files',
    'include',
    'exclude',
    'watchOptions',
    'compileOnSave',
    'typeAcquisition',
    'buildOptions'
];
function extendTSConfig(extending, extended) {
    const extendingConfig = extending.tsconfig;
    const extendedConfig = extended.tsconfig;
    const relativePath = util_native2posix(node_path.relative(node_path.dirname(extending.tsconfigFile), node_path.dirname(extended.tsconfigFile)));
    for (const key of Object.keys(extendedConfig).filter((key)=>EXTENDABLE_KEYS.includes(key)))if ('compilerOptions' === key) {
        if (!extendingConfig.compilerOptions) extendingConfig.compilerOptions = {};
        for (const option of Object.keys(extendedConfig.compilerOptions))if (!Object.prototype.hasOwnProperty.call(extendingConfig.compilerOptions, option)) extendingConfig.compilerOptions[option] = rebaseRelative(option, extendedConfig.compilerOptions[option], relativePath);
    } else if (void 0 === extendingConfig[key]) if ('watchOptions' === key) {
        extendingConfig.watchOptions = {};
        for (const option of Object.keys(extendedConfig.watchOptions))extendingConfig.watchOptions[option] = rebaseRelative(option, extendedConfig.watchOptions[option], relativePath);
    } else extendingConfig[key] = rebaseRelative(key, extendedConfig[key], relativePath);
}
const REBASE_KEYS = [
    'files',
    'include',
    'exclude',
    'baseUrl',
    'rootDir',
    'rootDirs',
    'typeRoots',
    'outDir',
    'outFile',
    'declarationDir',
    'excludeDirectories',
    'excludeFiles'
];
function rebaseRelative(key, value, prependPath) {
    if (!REBASE_KEYS.includes(key)) return value;
    if (Array.isArray(value)) return value.map((x)=>rebasePath(x, prependPath));
    return rebasePath(value, prependPath);
}
function rebasePath(value, prependPath) {
    if (node_path.isAbsolute(value) || value.startsWith('${configDir}')) return value;
    return node_path.posix.normalize(node_path.posix.join(prependPath, value));
}
class TSConfckParseError extends Error {
    code;
    cause;
    tsconfigFile;
    constructor(message, code, tsconfigFile, cause){
        super(message);
        Object.setPrototypeOf(this, TSConfckParseError.prototype);
        this.name = TSConfckParseError.name;
        this.code = code;
        this.cause = cause;
        this.tsconfigFile = tsconfigFile;
    }
}
function applyDefaults(tsconfig, tsconfigFile) {
    if (isJSConfig(tsconfigFile)) tsconfig.compilerOptions = {
        ...DEFAULT_JSCONFIG_COMPILER_OPTIONS,
        ...tsconfig.compilerOptions
    };
}
const DEFAULT_JSCONFIG_COMPILER_OPTIONS = {
    allowJs: true,
    maxNodeModuleJsDepth: 2,
    allowSyntheticDefaultImports: true,
    skipLibCheck: true,
    noEmit: true
};
function isJSConfig(configFileName) {
    return 'jsconfig.json' === node_path.basename(configFileName);
}
async function loadTsconfig(root, tsconfigPath = 'tsconfig.json') {
    const tsconfigFileName = await find(join(root, tsconfigPath), {
        root,
        configName: external_node_path_basename(tsconfigPath)
    });
    if (tsconfigFileName) {
        const { tsconfig } = await parse_parse(tsconfigFileName);
        return tsconfig;
    }
    return {};
}
const handleMatchedExternal = (value, request)=>{
    if ('boolean' == typeof value) return value;
    if ('string' == typeof value) {
        const [first, second] = value.split(' ');
        const hasType = !!second;
        const _request = second ? second : first;
        if (!hasType) return request === _request;
        return false;
    }
    if (Array.isArray(value)) return handleMatchedExternal(value[0] ?? '', request);
    return false;
};
const composeExternalsWarnConfig = (format, ...externalsArray)=>{
    if ('esm' !== format) return {};
    const externals = [];
    for (const e of externalsArray.filter(Boolean))if (Array.isArray(e)) externals.push(...e);
    else externals.push(e);
    const matchUserExternals = (externals, request, callback)=>{
        if ('string' == typeof externals) {
            if (handleMatchedExternal(externals, request)) return void callback(true, true);
        }
        if (Array.isArray(externals)) {
            let i = 0;
            const next = ()=>{
                let asyncFlag;
                const handleExternalsAndCallback = (matched, shouldWarn)=>{
                    if (!matched) {
                        if (asyncFlag) {
                            asyncFlag = false;
                            return;
                        }
                        next();
                        return;
                    }
                    callback(matched, shouldWarn);
                };
                do {
                    asyncFlag = true;
                    if (i >= externals.length) return void callback(false);
                    matchUserExternals(externals[i++], request, handleExternalsAndCallback);
                }while (!asyncFlag);
                asyncFlag = false;
            };
            next();
            return;
        }
        if (externals instanceof RegExp) {
            if (externals.test(request)) return void callback(true, true);
        } else if ('function' == typeof externals) ;
        else if ('object' == typeof externals) {
            if (Object.hasOwn(externals, request)) return void (handleMatchedExternal(externals[request], request) ? callback(true, true) : callback(true));
        }
        callback(false);
    };
    return {
        output: {
            externals: [
                ({ request, dependencyType, contextInfo }, callback)=>{
                    let shouldWarn = false;
                    const _callback = (_matched, _shouldWarn)=>{
                        if (_shouldWarn) shouldWarn = true;
                    };
                    if (contextInfo.issuer && 'commonjs' === dependencyType) {
                        matchUserExternals(externals, request, _callback);
                        if (shouldWarn) src_logger.warn(composeModuleImportWarn(request, contextInfo.issuer));
                    }
                    callback();
                }
            ]
        }
    };
};
const getAutoExternalDefaultValue = (format, autoExternal)=>autoExternal ?? isIntermediateOutputFormat(format);
const composeAutoExternalConfig = (options)=>{
    const { bundle, format, pkgJson, userExternals } = options;
    if (!bundle) return {};
    const autoExternal = getAutoExternalDefaultValue(format, options.autoExternal);
    if (false === autoExternal) return {};
    if (!pkgJson) {
        src_logger.warn('The `autoExternal` configuration will not be applied due to read package.json failed');
        return {};
    }
    const userExternalKeys = userExternals && isObject(userExternals) ? Object.keys(userExternals) : [];
    const externalOptions = {
        dependencies: true,
        optionalDependencies: true,
        peerDependencies: true,
        devDependencies: false,
        ...true === autoExternal ? {} : autoExternal
    };
    const externals = [
        'dependencies',
        'peerDependencies',
        'devDependencies',
        'optionalDependencies'
    ].reduce((prev, type)=>{
        if (externalOptions[type]) return pkgJson[type] ? prev.concat(Object.keys(pkgJson[type])) : prev;
        return prev;
    }, []).filter((name)=>!userExternalKeys.includes(name));
    const uniqueExternals = Array.from(new Set(externals));
    return externals.length ? {
        output: {
            externals: [
                ...uniqueExternals.map((dep)=>new RegExp(`^${dep}($|\\/|\\\\)`)),
                ...uniqueExternals
            ]
        }
    } : {};
};
function composeMinifyConfig(config) {
    const minify = config.output?.minify;
    const format = config.format;
    if (void 0 !== minify) return {};
    return {
        output: {
            minify: {
                js: true,
                css: false,
                jsOptions: {
                    test: /\.[cm]?jsx?(\?.*)?$/,
                    minimizerOptions: {
                        mangle: false,
                        minify: 'mf' === format,
                        compress: {
                            directives: false,
                            defaults: false,
                            unused: true,
                            dead_code: true,
                            toplevel: 'mf' !== format
                        },
                        format: {
                            comments: 'some',
                            preserve_annotations: true
                        }
                    }
                }
            }
        }
    };
}
function composeBannerFooterConfig(banner, footer) {
    const bannerConfig = pick(banner, [
        'js',
        'css'
    ]);
    const footerConfig = pick(footer, [
        'js',
        'css'
    ]);
    if (isEmptyObject(bannerConfig) && isEmptyObject(footerConfig)) return {};
    const plugins = [];
    if (!isEmptyObject(bannerConfig)) {
        if (bannerConfig.js) plugins.push(new rspack.BannerPlugin({
            banner: bannerConfig.js,
            stage: rspack.Compilation.PROCESS_ASSETS_STAGE_OPTIMIZE_SIZE + 1,
            raw: true,
            include: /\.(js|mjs|cjs)$/
        }));
        if (bannerConfig.css) plugins.push(new rspack.BannerPlugin({
            banner: bannerConfig.css,
            stage: rspack.Compilation.PROCESS_ASSETS_STAGE_OPTIMIZE_SIZE + 1,
            raw: true,
            include: /\.(css)$/
        }));
    }
    if (!isEmptyObject(footerConfig)) {
        if (footerConfig.js) plugins.push(new rspack.BannerPlugin({
            banner: footerConfig.js,
            stage: rspack.Compilation.PROCESS_ASSETS_STAGE_OPTIMIZE_SIZE + 1,
            raw: true,
            footer: true,
            include: /\.(js|mjs|cjs)$/
        }));
        if (footerConfig.css) plugins.push(new rspack.BannerPlugin({
            banner: footerConfig.css,
            stage: rspack.Compilation.PROCESS_ASSETS_STAGE_OPTIMIZE_SIZE + 1,
            raw: true,
            footer: true,
            include: /\.(css)$/
        }));
    }
    return {
        tools: {
            rspack: {
                plugins
            }
        }
    };
}
function composeDecoratorsConfig(compilerOptions, version) {
    if (version || !compilerOptions?.experimentalDecorators) return {};
    return {
        source: {
            decorators: {
                version: 'legacy'
            }
        }
    };
}
function composePrintFileSizeConfig(bundle, target) {
    if (bundle) return {};
    return {
        performance: {
            printFileSize: {
                total: ({ environmentName, distPath, assets, totalSize, totalGzipSize })=>{
                    let log = `${color.yellow(assets.length)} files generated in ${color.cyan(distPath)}, ${color.magenta('total:')} ${(totalSize / 1000).toFixed(1)} kB`;
                    if ('web' === target) log += ` ${color.green(`(${(totalGzipSize / 1000).toFixed(1)} kB gzipped)`)}`;
                    log += ` ${color.dim(`(${environmentName})`)}`;
                    return log;
                },
                detail: false
            }
        }
    };
}
async function createConstantRsbuildConfig() {
    return defineConfig({
        performance: {
            chunkSplit: {
                strategy: 'custom'
            },
            buildCache: true
        },
        tools: {
            htmlPlugin: false,
            rspack: {
                optimization: {
                    moduleIds: 'named',
                    nodeEnv: false
                },
                resolve: {
                    extensionAlias: {
                        '.js': [
                            '.ts',
                            '.tsx',
                            '.js',
                            '.jsx'
                        ],
                        '.jsx': [
                            '.tsx',
                            '.jsx'
                        ],
                        '.mjs': [
                            '.mts',
                            '.mjs'
                        ],
                        '.cjs': [
                            '.cts',
                            '.cjs'
                        ]
                    }
                }
            }
        },
        output: {
            target: 'node',
            distPath: {
                js: './',
                jsAsync: './',
                css: './',
                cssAsync: './'
            }
        }
    });
}
const composeFormatConfig = ({ format, target, bundle = true, umdName, pkgJson, enabledShims, multiCompilerIndex, sourceEntry })=>{
    const jsParserOptions = {
        cjs: {
            requireResolve: false,
            requireDynamic: false,
            requireAsExpression: false
        },
        esm: {
            importMeta: false,
            importDynamic: false,
            commonjs: {
                exports: 'skipInEsm'
            }
        },
        others: {
            worker: false
        }
    };
    const plugins = [
        new rspack.experiments.RslibPlugin({
            interceptApiPlugin: true,
            forceNodeShims: enabledShims.esm.__dirname || enabledShims.esm.__filename,
            autoCjsNodeBuiltin: 'esm' === format && 'node' === target
        })
    ].filter(Boolean);
    switch(format){
        case 'esm':
            return {
                output: {
                    filenameHash: false
                },
                tools: {
                    rspack: {
                        module: {
                            parser: {
                                javascript: {
                                    ...jsParserOptions.esm,
                                    ...jsParserOptions.cjs,
                                    ...jsParserOptions.others
                                }
                            }
                        },
                        optimization: {
                            concatenateModules: false,
                            sideEffects: true,
                            runtimeChunk: getRuntimeChunkConfig({
                                bundle,
                                multiCompilerIndex,
                                sourceEntry
                            }),
                            avoidEntryIife: true,
                            splitChunks: {
                                chunks: 'async'
                            }
                        },
                        output: {
                            module: true,
                            chunkFormat: false,
                            library: {
                                type: 'modern-module'
                            },
                            chunkLoading: 'import',
                            workerChunkLoading: 'import'
                        },
                        plugins
                    }
                }
            };
        case 'cjs':
            return {
                output: {
                    module: false,
                    filenameHash: false
                },
                tools: {
                    rspack: {
                        module: {
                            parser: {
                                javascript: {
                                    ...jsParserOptions.esm,
                                    ...jsParserOptions.cjs,
                                    ...jsParserOptions.others
                                }
                            }
                        },
                        optimization: {
                            splitChunks: {
                                chunks: 'async'
                            }
                        },
                        output: {
                            iife: false,
                            chunkFormat: 'commonjs',
                            library: {
                                type: 'commonjs-static'
                            },
                            chunkLoading: 'require',
                            workerChunkLoading: 'async-node'
                        },
                        plugins
                    }
                }
            };
        case 'umd':
            {
                if (!bundle) throw new Error('When using "umd" format, "bundle" must be set to "true". Since the default value for "bundle" is "true", so you can either explicitly set it to "true" or remove the field entirely.');
                const config = {
                    output: {
                        module: false,
                        filenameHash: false
                    },
                    tools: {
                        rspack: {
                            module: {
                                parser: {
                                    javascript: {
                                        importMeta: false
                                    }
                                }
                            },
                            output: {
                                asyncChunks: false,
                                library: umdName ? {
                                    type: 'umd',
                                    name: umdName
                                } : {
                                    type: 'umd'
                                }
                            },
                            optimization: {
                                nodeEnv: process.env.NODE_ENV,
                                splitChunks: false
                            },
                            plugins
                        }
                    }
                };
                return config;
            }
        case 'iife':
            {
                if (!bundle) throw new Error('When using "iife" format, "bundle" must be set to "true". Since the default value for "bundle" is "true", so you can either explicitly set it to "true" or remove the field entirely.');
                const config = {
                    output: {
                        module: false,
                        filenameHash: false,
                        minify: {
                            jsOptions: {
                                minimizerOptions: {
                                    module: true
                                }
                            }
                        }
                    },
                    tools: {
                        rspack: {
                            module: {
                                parser: {
                                    javascript: {
                                        importMeta: false
                                    }
                                }
                            },
                            output: {
                                iife: true,
                                asyncChunks: false,
                                chunkFormat: false,
                                library: {
                                    type: 'module'
                                },
                                chunkLoading: 'import'
                            },
                            optimization: {
                                nodeEnv: process.env.NODE_ENV,
                                splitChunks: false
                            },
                            plugins
                        }
                    }
                };
                return config;
            }
        case 'mf':
            if (!bundle) throw new Error('When using "mf" format, "bundle" must be set to "true". Since the default value for "bundle" is "true", so you can either explicitly set it to "true" or remove the field entirely.');
            return {
                dev: {
                    writeToDisk: true
                },
                tools: {
                    rspack: (config, { env })=>{
                        config.output = {
                            ...config.output,
                            uniqueName: pkgJson.name
                        };
                        config.optimization = {
                            ...config.optimization,
                            nodeEnv: 'development' === env ? 'development' : 'production',
                            moduleIds: 'development' === env ? 'named' : 'deterministic'
                        };
                    }
                },
                output: {
                    target: 'web'
                }
            };
        default:
            throw new Error(`Unsupported format: ${format}`);
    }
};
const disableUrlParseRsbuildPlugin = ()=>({
        name: 'rsbuild:disable-url-parse',
        setup (api) {
            api.modifyBundlerChain((config, { CHAIN_ID })=>{
                config.module.rule(CHAIN_ID.RULE.JS).oneOf(CHAIN_ID.ONE_OF.JS_MAIN).parser({
                    url: false
                });
            });
        }
    });
const fixJsModuleTypePlugin = ()=>({
        name: 'rsbuild:fix-js-module-type',
        setup (api) {
            api.modifyBundlerChain((config, { CHAIN_ID })=>{
                config.module.rule(CHAIN_ID.RULE.JS).oneOf(CHAIN_ID.ONE_OF.JS_MAIN).delete('type');
            });
        }
    });
const BundlePlugin = ()=>({
        name: 'rslib:bundle',
        setup (api) {
            api.onBeforeEnvironmentCompile({
                order: 'post',
                handler: ({ bundlerConfig })=>{
                    if (bundlerConfig?.module?.parser?.javascript?.jsx === true) {
                        src_logger.error('Bundle mode does not support preserving JSX syntax. Set "bundle" to "false" or change the JSX runtime to `automatic` or `classic`. Check out ' + color.green('https://rslib.rs/guide/solution/react#jsx-transform') + ' for more details.');
                        process.exit(1);
                    }
                }
            });
        }
    });
const RSLIB_RUNTIME_CHUNK_NAME = 'rslib-runtime';
const getMultiCompilerRuntimeChunkName = (multiCompilerIndex)=>'number' == typeof multiCompilerIndex ? `${multiCompilerIndex}~${RSLIB_RUNTIME_CHUNK_NAME}` : RSLIB_RUNTIME_CHUNK_NAME;
const getRuntimeChunkConfig = ({ bundle, multiCompilerIndex, sourceEntry })=>{
    if (!bundle) return {
        name: RSLIB_RUNTIME_CHUNK_NAME
    };
    if (!sourceEntry || Object.keys(sourceEntry).length <= 1) return;
    return {
        name: getMultiCompilerRuntimeChunkName(multiCompilerIndex)
    };
};
const composeBundleConfig = (bundle)=>{
    if (bundle) return {
        rsbuildConfig: {
            plugins: [
                BundlePlugin()
            ]
        }
    };
    return {
        rsbuildConfig: {}
    };
};
const composeShimsConfig = (format, shims)=>{
    const resolvedShims = {
        cjs: {
            'import.meta.url': shims?.cjs?.['import.meta.url'] ?? true,
            'import.meta.dirname': shims?.cjs?.['import.meta.dirname'] ?? true,
            'import.meta.filename': shims?.cjs?.['import.meta.filename'] ?? true
        },
        esm: {
            __filename: shims?.esm?.__filename ?? false,
            __dirname: shims?.esm?.__dirname ?? false,
            require: shims?.esm?.require ?? false
        }
    };
    const enabledShims = {
        cjs: 'cjs' === format ? resolvedShims.cjs : {
            'import.meta.url': false,
            'import.meta.dirname': false,
            'import.meta.filename': false
        },
        esm: 'esm' === format ? resolvedShims.esm : {
            __filename: false,
            __dirname: false,
            require: false
        }
    };
    let rsbuildConfig = {};
    switch(format){
        case 'esm':
            rsbuildConfig = {
                tools: {
                    rspack: {
                        node: {
                            __dirname: resolvedShims.esm.__dirname ? 'node-module' : false,
                            __filename: resolvedShims.esm.__filename ? 'node-module' : false
                        }
                    }
                },
                plugins: [
                    resolvedShims.esm.require && pluginEsmRequireShim(),
                    disableUrlParseRsbuildPlugin(),
                    fixJsModuleTypePlugin()
                ].filter(Boolean)
            };
            break;
        case 'cjs':
            rsbuildConfig = {
                plugins: [
                    pluginCjsShims(resolvedShims.cjs),
                    disableUrlParseRsbuildPlugin(),
                    fixJsModuleTypePlugin()
                ].filter(Boolean)
            };
            break;
        case 'umd':
        case 'iife':
        case 'mf':
            rsbuildConfig = {
                plugins: [
                    fixJsModuleTypePlugin()
                ]
            };
            break;
        default:
            throw new Error(`Unsupported format: ${format}`);
    }
    return {
        rsbuildConfig,
        enabledShims
    };
};
const composeModuleImportWarn = (request, issuer)=>`The externalized commonjs request ${color.green(`"${request}"`)} from ${color.green(issuer)} will use ${color.blue('"module"')} external type in ESM format. If you want to specify other external type, consider setting the request and type with ${color.blue('"output.externals"')}.`;
const composeExternalsConfig = (format, externals)=>{
    const externalsTypeMap = {
        esm: 'module-import',
        cjs: 'commonjs-import',
        umd: 'umd',
        mf: 'global',
        iife: 'global'
    };
    const globalObjectMap = {
        esm: void 0,
        cjs: void 0,
        umd: void 0,
        mf: void 0,
        iife: 'globalThis'
    };
    const rspackConfig = {};
    const rsbuildConfig = {};
    switch(format){
        case 'esm':
        case 'cjs':
        case 'umd':
        case 'mf':
        case 'iife':
            rsbuildConfig.output = externals ? {
                externals
            } : {};
            rspackConfig.externalsType = externalsTypeMap[format];
            if (globalObjectMap[format]) rspackConfig.output = {
                globalObject: globalObjectMap[format]
            };
            break;
        default:
            throw new Error(`Unsupported format: ${format}`);
    }
    return {
        ...rsbuildConfig,
        tools: {
            rspack: rspackConfig
        }
    };
};
const composeOutputFilenameConfig = (config, format, autoExtension, multiCompilerIndex, pkgJson)=>{
    const { jsExtension, dtsExtension } = getDefaultExtension({
        format,
        pkgJson,
        autoExtension
    });
    const filenameHash = config.output?.filenameHash ?? false;
    const getHash = ()=>{
        if ('string' == typeof filenameHash) return filenameHash ? `.[${filenameHash}]` : '';
        return filenameHash ? '.[contenthash:10]' : '';
    };
    const inferChunkFilename = (filename)=>{
        if ('function' != typeof filename) {
            const hasName = filename.includes('[name]');
            const hasId = filename.includes('[id]');
            const hasChunkHash = filename.includes('[chunkhash]');
            const hasContentHash = filename.includes('[contenthash]');
            const multiCompilerPrefix = 'number' == typeof multiCompilerIndex ? `${multiCompilerIndex}~` : '';
            if (hasChunkHash || hasContentHash || hasName || hasId) return filename.replace(/(^|\/)([^/]*(?:\?|$))/, `$1${multiCompilerPrefix}$2`);
            return filename.replace(/(^|\/)([^/]*(?:\?|$))/, `$1${multiCompilerIndex}[id].$2`);
        }
    };
    const hash = getHash();
    const defaultJsFilename = `[name]${hash}${jsExtension}`;
    const userJsFilename = config.output?.filename?.js;
    const defaultJsChunkFilename = inferChunkFilename(userJsFilename ?? defaultJsFilename);
    const finalJsExtension = 'string' == typeof userJsFilename && userJsFilename ? extname(userJsFilename) : jsExtension;
    const chunkFilename = {
        tools: {
            rspack: {
                output: {
                    chunkFilename: defaultJsChunkFilename
                }
            }
        }
    };
    const finalConfig = userJsFilename ? chunkFilename : mergeRsbuildConfig(chunkFilename, {
        output: {
            filename: {
                js: defaultJsFilename
            }
        }
    });
    return {
        config: 'mf' === format ? {} : finalConfig,
        jsExtension: finalJsExtension,
        dtsExtension
    };
};
const composeSyntaxConfig = (target, syntax)=>{
    if (syntax) return {
        tools: {
            rspack: (config)=>{
                config.target = transformSyntaxToRspackTarget(syntax);
            }
        },
        output: {
            overrideBrowserslist: transformSyntaxToBrowserslist(syntax, target)
        }
    };
    return {
        tools: {
            rspack: (config)=>{
                config.target = [
                    'es2022'
                ];
                return config;
            }
        },
        output: {
            overrideBrowserslist: ESX_TO_BROWSERSLIST.esnext(target)
        }
    };
};
const traverseEntryQuery = (entry, callback)=>{
    const newEntry = {};
    for (const [key, value] of Object.entries(entry)){
        let result = value;
        result = 'string' == typeof value ? callback(value) : Array.isArray(value) ? value.map(callback) : {
            ...value,
            import: 'string' == typeof value.import ? callback(value.import) : value.import.map(callback)
        };
        newEntry[key] = result;
    }
    return newEntry;
};
const resolveEntryPath = (entries, root)=>traverseEntryQuery(entries, (item)=>node_path.resolve(root, item));
const composeEntryConfig = async (rawEntry, bundle, root, cssModulesAuto, userOutBase)=>{
    let entries = rawEntry;
    if (!entries) {
        if (false !== bundle) return {
            entryConfig: {},
            outBase: null
        };
        entries = {
            index: 'src/**'
        };
    }
    if ('object' != typeof entries) throw new Error(`The ${color.cyan('source.entry')} configuration should be an object, but received ${typeof entries}: ${color.cyan(entries)}. Checkout ${color.green('https://rslib.rs/config/rsbuild/source#sourceentry')} for more details.`);
    if (false !== bundle) {
        const entryErrorReasons = [];
        traverseEntryQuery(entries, (entry)=>{
            const entryAbsPath = node_path.isAbsolute(entry) ? entry : node_path.resolve(root, entry);
            const isDirLike = '' === node_path.extname(entryAbsPath);
            const dirError = `Glob pattern ${color.cyan(`"${entry}"`)} is not supported when "bundle" is "true", considering "bundle" to "false" to use bundleless mode, or specify a file entry to bundle. See ${color.green('https://rslib.rs/guide/basic/output-structure')} for more details.`;
            if (node_fs.existsSync(entryAbsPath)) {
                const stats = node_fs.statSync(entryAbsPath);
                if (!stats.isFile()) entryErrorReasons.push(dirError);
            } else {
                const isGlobLike = entry.startsWith('!') || /[*?[{\]}]/.test(entry);
                if (isDirLike || isGlobLike) entryErrorReasons.push(dirError);
                else entryErrorReasons.push(`Can't resolve the entry ${color.cyan(`"${entry}"`)} at the location ${color.cyan(entryAbsPath)}. Please ensure that the file exists.`);
            }
            return entry;
        });
        if (entryErrorReasons.length) throw new AggregateError(entryErrorReasons.map((reason)=>new Error(reason)));
        return {
            entryConfig: {
                source: {
                    entry: resolveEntryPath(entries, root)
                }
            },
            outBase: null
        };
    }
    const scanGlobEntries = async (tryResolveOutBase)=>{
        const resolvedEntries = {};
        const resolveOutBase = async (resolvedEntryFiles)=>{
            if (void 0 !== userOutBase) return node_path.isAbsolute(userOutBase) ? userOutBase : node_path.resolve(root, userOutBase);
            const lcp = await calcLongestCommonPath(resolvedEntryFiles) ?? root;
            return lcp;
        };
        for (const key of Object.keys(entries)){
            const entry = entries[key];
            const entryFiles = Array.isArray(entry) ? entry : 'string' == typeof entry ? [
                entry
            ] : null;
            if (!entryFiles) throw new Error('Entry can only be a string or an array of strings for now');
            const { glob } = await import("./tinyglobby.js");
            const globEntryFiles = await glob(entryFiles, {
                cwd: root,
                absolute: true,
                dot: true,
                ignore: [
                    '**/.DS_Store',
                    '**/Thumbs.db'
                ]
            });
            const resolvedEntryFiles = globEntryFiles.filter((i)=>!DTS_EXTENSIONS_PATTERN.test(i));
            if (0 === resolvedEntryFiles.length) {
                const error = new Error(`No entry files matching ${entryFiles.map((file)=>color.cyan(file)).join(', ')}. Please ensure the entry pattern in ${color.cyan('source.entry')} is correct and points to valid source files.`);
                error.stack = '';
                throw error;
            }
            const outBase = await resolveOutBase(resolvedEntryFiles);
            function getEntryName(file) {
                const { dir, name } = node_path.parse(node_path.relative(outBase, file));
                const entryFileName = node_path.join(dir, name);
                if (isCssGlobalFile(file, cssModulesAuto)) return `${RSLIB_CSS_ENTRY_FLAG}/${entryFileName}`;
                return entryFileName;
            }
            for (const file of resolvedEntryFiles){
                const entryName = getEntryName(file);
                if (resolvedEntries[entryName]) {
                    if (tryResolveOutBase) src_logger.warn(`Duplicate entry ${color.cyan(entryName)} from ${color.cyan(node_path.relative(root, file))} and ${color.cyan(node_path.relative(root, resolvedEntries[entryName]))}, which may lead to the incorrect output, please rename the file.`);
                }
                resolvedEntries[entryName] = file;
            }
        }
        if (tryResolveOutBase) {
            const outBase = await resolveOutBase(Object.values(resolvedEntries));
            return {
                resolvedEntries,
                outBase
            };
        }
        return {
            resolvedEntries,
            outBase: null
        };
    };
    const { outBase } = await scanGlobEntries(true);
    const entryConfig = {
        tools: {
            rspack: {
                entry: async ()=>{
                    const { resolvedEntries } = await scanGlobEntries(false);
                    return resolvedEntries;
                }
            }
        }
    };
    return {
        entryConfig,
        outBase
    };
};
const composeBundlelessExternalConfig = (jsExtension, redirect, cssModulesAuto, bundle, outBase)=>{
    if (bundle) return {
        config: {}
    };
    const isRspackVueLoaderRequest = (request, suffix)=>'string' == typeof request && request.includes('rspack-vue-loader') && (suffix ? request.includes(suffix) : true);
    const isPathInOutBase = (resourcePath, outBase)=>{
        const normalizedOutBase = normalizeSlash(outBase).replace(/\/+$/, '');
        const normalizedResourcePath = normalizeSlash(resourcePath);
        return normalizedResourcePath === normalizedOutBase || normalizedResourcePath.startsWith(`${normalizedOutBase}/`);
    };
    const styleRedirectPath = redirect.style?.path ?? true;
    const styleRedirectExtension = redirect.style?.extension ?? true;
    const jsRedirectPath = redirect.js?.path ?? true;
    const jsRedirectExtension = redirect.js?.extension ?? true;
    const assetRedirectPath = redirect.asset?.path ?? true;
    const assetRedirectExtension = redirect.asset?.extension ?? true;
    let resolver;
    return {
        resolvedJsRedirect: {
            path: jsRedirectPath,
            extension: jsRedirectExtension
        },
        config: {
            output: {
                externals: [
                    async (data, callback)=>{
                        const { request, getResolve, context, contextInfo } = data;
                        if (!request || !getResolve || !context || !contextInfo) return void callback();
                        const { issuer } = contextInfo;
                        const originExtension = extname(request);
                        if (!resolver) resolver = getResolve();
                        async function redirectPath(request) {
                            try {
                                let resolvedRequest = request;
                                resolvedRequest = await resolver(context, resolvedRequest);
                                if ('string' != typeof outBase) throw new Error(`outBase expect to be a string in bundleless mode, but got ${outBase}`);
                                const isSubpath = normalizeSlash(resolvedRequest).startsWith(`${normalizeSlash(outBase)}/`);
                                if (isSubpath) {
                                    resolvedRequest = normalizeSlash(node_path.relative(node_path.dirname(issuer), resolvedRequest));
                                    if (!resolvedRequest.startsWith('./') && !resolvedRequest.startsWith('../')) resolvedRequest = `./${resolvedRequest}`;
                                    return {
                                        path: resolvedRequest,
                                        isResolved: true
                                    };
                                }
                                return {
                                    path: void 0,
                                    isResolved: true
                                };
                            } catch  {
                                src_logger.debug(`Failed to resolve module ${color.green(`"${request}"`)} from ${color.green(issuer)}. If it's an npm package, consider adding it to dependencies or peerDependencies in package.json to make it externalized.`);
                                return {
                                    path: request,
                                    isResolved: false
                                };
                            }
                        }
                        if (issuer) {
                            if ('string' == typeof outBase && !isPathInOutBase(context, outBase)) return void callback();
                            if (isRspackVueLoaderRequest(request, 'dist/exportHelper.js') || isRspackVueLoaderRequest(request, '?vue&type=')) return void callback();
                            let resolvedRequest = request;
                            const { path: redirectedPath, isResolved } = await redirectPath(resolvedRequest);
                            const cssExternal = await cssExternalHandler(resolvedRequest, callback, jsExtension, cssModulesAuto, styleRedirectPath, styleRedirectExtension, redirectedPath, issuer);
                            if (false !== cssExternal) return cssExternal;
                            if (void 0 === redirectedPath) return void callback(void 0, request);
                            if (jsRedirectPath) resolvedRequest = redirectedPath;
                            if (resolvedRequest.startsWith('.') && isResolved) {
                                const ext = extname(resolvedRequest);
                                if (ext) if (JS_EXTENSIONS_PATTERN.test(resolvedRequest)) resolvedRequest = resolvedRequest.replace(/\.[^.]+$/, jsRedirectExtension ? jsExtension : JS_EXTENSIONS_PATTERN.test(originExtension) ? originExtension : '');
                                else {
                                    resolvedRequest = assetRedirectPath ? redirectedPath : request;
                                    if (assetRedirectExtension) resolvedRequest = resolvedRequest.replace(/\.[^.]+$/, jsExtension);
                                }
                                else if (jsRedirectExtension) {
                                    if (!jsRedirectPath && await isDirectory(join(dirname(issuer), resolvedRequest))) resolvedRequest = `${resolvedRequest.replace(/\/+$/, '')}/index`;
                                    resolvedRequest = `${resolvedRequest}${jsExtension}`;
                                }
                            }
                            callback(void 0, resolvedRequest);
                            return;
                        }
                        callback();
                    }
                ]
            }
        }
    };
};
const composeDtsConfig = async (libConfig, format, dtsExtension)=>{
    const { autoExternal, banner, footer, redirect } = libConfig;
    let { dts } = libConfig;
    if (false === dts || void 0 === dts) return {};
    if (true === dts) dts = {
        bundle: false
    };
    const { pluginDts } = await import("rsbuild-plugin-dts");
    return {
        plugins: [
            pluginDts({
                bundle: dts?.bundle,
                distPath: dts?.distPath,
                build: dts?.build,
                abortOnError: dts?.abortOnError,
                dtsExtension: dts?.autoExtension ? dtsExtension : '.d.ts',
                autoExternal: getAutoExternalDefaultValue(format, autoExternal),
                alias: dts?.alias,
                banner: banner?.dts,
                footer: footer?.dts,
                redirect: redirect?.dts,
                tsgo: dts?.tsgo
            })
        ]
    };
};
const composeTargetConfig = (userTarget, format)=>{
    const target = userTarget ?? ('mf' === format ? 'web' : 'node');
    switch(target){
        case 'web':
            return {
                config: {
                    tools: {
                        rspack: {
                            target: [
                                'web'
                            ]
                        }
                    }
                },
                target: 'web',
                externalsConfig: {}
            };
        case 'node':
            return {
                config: {
                    tools: {
                        rspack: {
                            externalsPresets: {
                                node: false
                            },
                            target: [
                                'node'
                            ]
                        }
                    },
                    output: {
                        target: 'node'
                    }
                },
                target: 'node',
                externalsConfig: {
                    output: {
                        externals: nodeBuiltInModules
                    }
                }
            };
        default:
            throw new Error(`Unsupported platform: ${target}`);
    }
};
const composeExternalHelpersConfig = (externalHelpers, pkgJson)=>{
    let defaultConfig = {
        tools: {
            swc: {
                jsc: {
                    externalHelpers: false
                }
            }
        }
    };
    if (externalHelpers) {
        const deps = [
            ...Object.keys(pkgJson?.dependencies ?? []),
            ...Object.keys(pkgJson?.devDependencies ?? [])
        ];
        if (!deps.includes(SWC_HELPERS)) {
            src_logger.error(`${color.green('externalHelpers')} is enabled, but the ${color.blue(SWC_HELPERS)} dependency declaration was not found in package.json.`);
            process.exit(1);
        }
        defaultConfig = Object.assign(defaultConfig, {
            output: {
                externals: new RegExp(`^${SWC_HELPERS}($|\\/|\\\\)`)
            }
        });
        defaultConfig.tools.swc.jsc.externalHelpers = true;
    }
    return defaultConfig;
};
async function composeLibRsbuildConfig(config, multiCompilerIndex, root = process.cwd(), sharedPlugins) {
    checkMFPlugin(config, sharedPlugins);
    const pkgJson = readPackageJson(root);
    const { compilerOptions } = await loadTsconfig(root, config.source?.tsconfigPath);
    const cssModulesAuto = config.output?.cssModules?.auto ?? true;
    const { format = 'esm', shims, bundle = true, banner = {}, footer = {}, autoExtension = true, autoExternal, externalHelpers = false, redirect = {}, umdName, experiments = {} } = config;
    const hasExe = Boolean(experiments.exe);
    const { rsbuildConfig: bundleConfig } = composeBundleConfig(bundle);
    const { rsbuildConfig: shimsConfig, enabledShims } = composeShimsConfig(format, shims);
    const { config: targetConfig, externalsConfig: targetExternalsConfig, target } = composeTargetConfig(config.output?.target, format);
    const formatConfig = composeFormatConfig({
        format,
        target,
        pkgJson: pkgJson,
        bundle,
        umdName,
        enabledShims,
        multiCompilerIndex,
        sourceEntry: config.source?.entry
    });
    const userExternals = hasExe ? void 0 : config.output?.externals;
    const externalHelpersConfig = composeExternalHelpersConfig(hasExe ? false : externalHelpers, pkgJson);
    const userExternalsConfig = composeExternalsConfig(format, userExternals);
    const { config: outputFilenameConfig, jsExtension, dtsExtension } = composeOutputFilenameConfig(config, format, autoExtension, multiCompilerIndex, pkgJson);
    const { entryConfig, outBase } = await composeEntryConfig(config.source?.entry, config.bundle, root, cssModulesAuto, config.outBase);
    const { config: exeConfig } = composeExeConfig({
        bundle,
        exe: experiments.exe,
        format,
        root,
        sourceEntry: entryConfig.source?.entry,
        target
    });
    const { config: bundlelessExternalConfig } = composeBundlelessExternalConfig(jsExtension, redirect, cssModulesAuto, bundle, outBase);
    const syntaxConfig = composeSyntaxConfig(target, config?.syntax);
    const autoExternalConfig = composeAutoExternalConfig({
        bundle,
        format,
        autoExternal: hasExe ? false : autoExternal,
        pkgJson,
        userExternals
    });
    const cssConfig = composeCssConfig(outBase, cssModulesAuto, config.bundle, banner?.css, footer?.css);
    const assetConfig = composeAssetConfig(bundle, format);
    const entryChunkConfig = composeEntryChunkConfig({
        enabledImportMetaUrlShim: enabledShims.cjs['import.meta.url'],
        contextToWatch: outBase
    });
    const dtsConfig = await composeDtsConfig(config, format, dtsExtension);
    const externalsWarnConfig = composeExternalsWarnConfig(format, userExternalsConfig?.output?.externals, autoExternalConfig?.output?.externals);
    const minifyConfig = composeMinifyConfig(config);
    const bannerFooterConfig = composeBannerFooterConfig(banner, footer);
    const decoratorsConfig = composeDecoratorsConfig(compilerOptions, config.source?.decorators?.version);
    const printFileSizeConfig = composePrintFileSizeConfig(bundle, target);
    return mergeRsbuildConfig(bundleConfig, formatConfig, shimsConfig, syntaxConfig, externalHelpersConfig, outputFilenameConfig, targetConfig, externalsWarnConfig, userExternalsConfig, autoExternalConfig, targetExternalsConfig, bundlelessExternalConfig, entryConfig, cssConfig, assetConfig, entryChunkConfig, minifyConfig, dtsConfig, bannerFooterConfig, decoratorsConfig, printFileSizeConfig, exeConfig);
}
async function composeCreateRsbuildConfig(rslibConfig) {
    const constantRsbuildConfig = await createConstantRsbuildConfig();
    const { lib: libConfigsArray, mode: _mode, root, plugins: sharedPlugins, dev: _dev, server: _server, logLevel, ...sharedRsbuildConfig } = rslibConfig;
    if (logLevel && !isDebug()) src_logger.level = logLevel;
    if (!Array.isArray(libConfigsArray) || 0 === libConfigsArray.length) throw new Error(`Expect "lib" field to be a non-empty array, but got: ${color.cyan(JSON.stringify(libConfigsArray))}.`);
    const libConfigPromises = libConfigsArray.map(async (libConfig, index)=>{
        const userConfig = mergeRsbuildConfig(sharedRsbuildConfig, libConfig);
        const libRsbuildConfig = await composeLibRsbuildConfig(userConfig, libConfigsArray.length > 1 ? index : null, root, sharedPlugins);
        userConfig.source ??= {};
        userConfig.source.entry = {};
        userConfig.output ??= {};
        delete userConfig.output.externals;
        const config = {
            format: libConfig.format ?? 'esm',
            config: mergeRsbuildConfig(constantRsbuildConfig, libRsbuildConfig, omit(userConfig, {
                id: true,
                bundle: true,
                format: true,
                autoExtension: true,
                autoExternal: true,
                redirect: true,
                syntax: true,
                externalHelpers: true,
                banner: true,
                footer: true,
                dts: true,
                shims: true,
                umdName: true,
                outBase: true,
                experiments: true
            }))
        };
        if ('string' == typeof libConfig.id) config.id = libConfig.id;
        return config;
    });
    const composedRsbuildConfig = await Promise.all(libConfigPromises);
    return composedRsbuildConfig;
}
async function composeRsbuildEnvironments(rslibConfig) {
    const rsbuildConfigWithLibInfo = await composeCreateRsbuildConfig(rslibConfig);
    const environmentWithInfos = [];
    const usedIds = rsbuildConfigWithLibInfo.map(({ id })=>id).filter(Boolean);
    const environments = {};
    const formatCount = rsbuildConfigWithLibInfo.reduce((acc, { format })=>{
        acc[format] = (acc[format] ?? 0) + 1;
        return acc;
    }, {});
    const composeDefaultId = (format)=>{
        const nextDefaultId = (format, index)=>`${format}${1 === formatCount[format] && 0 === index ? '' : index}`;
        let index = 0;
        let candidateId = nextDefaultId(format, index);
        while(usedIds.includes(candidateId))candidateId = nextDefaultId(format, ++index);
        usedIds.push(candidateId);
        return candidateId;
    };
    for (const { format, id, config } of rsbuildConfigWithLibInfo){
        const libId = 'string' == typeof id ? id : composeDefaultId(format);
        environments[libId] = config;
        environmentWithInfos.push({
            id: libId,
            format,
            config
        });
    }
    const conflictIds = usedIds.filter((id, index)=>usedIds.indexOf(id) !== index);
    if (conflictIds.length) throw new Error(`The following ids are duplicated: ${conflictIds.map((id)=>`"${id}"`).join(', ')}. Please change the "lib.id" to be unique.`);
    return {
        environments,
        environmentWithInfos
    };
}
const pruneEnvironments = (environments, libs)=>{
    if (!libs || 0 === libs.length) return environments;
    const filteredEnvironments = Object.fromEntries(Object.entries(environments).filter(([name])=>libs.includes(name)));
    if (0 === Object.keys(filteredEnvironments).length) throw new Error(`The following libs are not found: ${libs.map((lib)=>`"${lib}"`).join(', ')}.`);
    return filteredEnvironments;
};
const applyDebugInspectConfigPlugin = (rsbuildInstance, config)=>{
    if (!isDebugKey([
        'rslib'
    ])) return;
    const inspect = async ()=>{
        await rsbuildInstance.inspectConfig({
            verbose: true,
            writeToDisk: true,
            extraConfigs: {
                rslib: config
            }
        });
    };
    const debugRslibPlugin = {
        name: 'rsbuild:debug-rslib',
        setup (api) {
            api.onBeforeBuild(async ({ isFirstCompile })=>{
                if (isFirstCompile) await inspect();
            });
            api.onAfterStartDevServer(()=>{
                inspect();
            });
        }
    };
    rsbuildInstance.addPlugins([
        debugRslibPlugin
    ], {
        before: 'rsbuild:asset'
    });
};
async function createRslib(options = {}) {
    const envs = options.loadEnv ? loadEnv({
        cwd: options.cwd,
        ...'boolean' == typeof options.loadEnv ? {} : options.loadEnv
    }) : null;
    const configOrFactory = options.config;
    const config = isFunction(configOrFactory) ? await configOrFactory() : configOrFactory || {};
    if (envs) {
        config.source ||= {};
        config.source.define = {
            ...envs.publicVars,
            ...config.source.define
        };
    }
    const cwd = options.cwd || process.cwd();
    config.root = config.root ? ensureAbsolutePath(cwd, config.root) : cwd;
    if (config._privateMeta) config._privateMeta.envFilePaths = envs ? envs.filePaths : [];
    const onAfterCreateRsbuildCallbacks = [];
    const onAfterCreateRsbuild = (callback)=>{
        onAfterCreateRsbuildCallbacks.push(callback);
    };
    const createRsbuildInstance = async (options, mode, environments)=>{
        const rsbuildInstance = await createRsbuild({
            cwd: options.cwd,
            callerName: 'rslib',
            config: {
                mode,
                root: config.root,
                plugins: config.plugins,
                dev: config.dev,
                server: config.server,
                logLevel: isDebug() ? 'info' : config.logLevel,
                environments
            }
        });
        if (envs) {
            rsbuildInstance.onCloseBuild(envs.cleanup);
            rsbuildInstance.onCloseDevServer(envs.cleanup);
        }
        applyDebugInspectConfigPlugin(rsbuildInstance, config);
        for (const callback of onAfterCreateRsbuildCallbacks)await callback({
            rsbuild: rsbuildInstance
        });
        return rsbuildInstance;
    };
    const build = async (buildOptions = {})=>{
        if (!getNodeEnv()) setNodeEnv('production');
        if (buildOptions.watch) {
            config.plugins = config.plugins || [];
            config.plugins.push({
                name: 'rslib:on-after-build',
                setup (api) {
                    api.onAfterBuild(({ isFirstCompile, stats })=>{
                        if (isFirstCompile) if (stats?.hasErrors()) src_logger.error('build completed with errors, watching for changes...');
                        else src_logger.success('build completed, watching for changes...');
                    });
                }
            });
        }
        const { environments } = await composeRsbuildEnvironments(config);
        const rsbuildInstance = await createRsbuildInstance(options, 'production', pruneEnvironments(environments, buildOptions.lib));
        const buildResult = await rsbuildInstance.build({
            watch: buildOptions.watch
        });
        return buildResult;
    };
    const inspectConfig = async (inspectOptions = {})=>{
        if (inspectOptions.mode) setNodeEnv(inspectOptions.mode);
        else if (!getNodeEnv()) setNodeEnv('production');
        const { environments } = await composeRsbuildEnvironments(config);
        const rsbuildInstance = await createRsbuildInstance(options, 'production', pruneEnvironments(environments, inspectOptions.lib));
        const inspectConfigResult = await rsbuildInstance.inspectConfig({
            mode: inspectOptions.mode,
            verbose: inspectOptions.verbose,
            outputPath: inspectOptions.outputPath,
            writeToDisk: inspectOptions.writeToDisk,
            extraConfigs: {
                rslib: config
            }
        });
        const rawRslibConfig = node_util.inspect(config, {
            depth: null
        });
        return {
            rslibConfig: rawRslibConfig,
            ...inspectConfigResult
        };
    };
    const startMFDevServer = async (mfOptions = {})=>{
        if (!getNodeEnv()) setNodeEnv('development');
        const { environments, environmentWithInfos } = await composeRsbuildEnvironments(config);
        const selectedEnvironmentIds = environmentWithInfos.filter((env)=>{
            const isMf = 'mf' === env.format;
            if (!mfOptions.lib || 0 === mfOptions.lib.length) return isMf;
            return env.id && mfOptions.lib.includes(env.id);
        }).map((env)=>env.id);
        if (!selectedEnvironmentIds.length) throw new Error(`No mf format found in ${mfOptions.lib && mfOptions.lib.length > 0 ? `libs ${mfOptions.lib.map((lib)=>`"${lib}"`).join(', ')}` : 'your config'}, please check your config to ensure that the mf format is enabled correctly.`);
        const selectedEnvironments = pruneEnvironments(environments, selectedEnvironmentIds);
        const rsbuildInstance = await createRsbuildInstance(options, 'development', selectedEnvironments);
        const startDevServer = await rsbuildInstance.startDevServer();
        onBeforeRestart(startDevServer.server.close);
        return startDevServer;
    };
    const getRslibConfig = ()=>config;
    const rslib = {
        getRslibConfig,
        onAfterCreateRsbuild,
        build,
        inspectConfig,
        startMFDevServer
    };
    return rslib;
}
function loadConfig_defineConfig(config) {
    return config;
}
const resolveConfigPath = (root, customConfig)=>{
    if (customConfig) {
        const customConfigPath = isAbsolute(customConfig) ? customConfig : join(root, customConfig);
        if (node_fs.existsSync(customConfigPath)) return customConfigPath;
        const error = new Error(`${color.dim('[rslib:loadConfig]')} Cannot find config file: ${color.dim(customConfigPath)}`);
        error.stack = '';
        throw error;
    }
    const CONFIG_FILES = [
        'rslib.config.mjs',
        'rslib.config.ts',
        'rslib.config.js',
        'rslib.config.cjs',
        'rslib.config.mts',
        'rslib.config.cts'
    ];
    for (const file of CONFIG_FILES){
        const configFile = join(root, file);
        if (node_fs.existsSync(configFile)) return configFile;
    }
    return null;
};
async function loadConfig_loadConfig({ cwd = process.cwd(), path, envMode, meta, loader }) {
    const configFilePath = resolveConfigPath(cwd, path);
    if (!configFilePath) {
        src_logger.debug('no config file found.');
        return {
            content: {},
            filePath: null
        };
    }
    const { content } = await loadConfig({
        cwd: dirname(configFilePath),
        path: configFilePath,
        envMode,
        meta,
        loader
    });
    return {
        content: content,
        filePath: configFilePath
    };
}
const getEnvDir = (cwd, envDir)=>{
    if (envDir) return node_path.isAbsolute(envDir) ? envDir : node_path.resolve(cwd, envDir);
    return cwd;
};
const parseEntryOption = (entries)=>{
    if (!entries?.length) return;
    const entryList = [];
    for (const rawEntry of entries){
        const value = rawEntry?.trim();
        if (!value) continue;
        const equalIndex = value.indexOf('=');
        if (equalIndex > -1) {
            const name = value.slice(0, equalIndex).trim();
            const entryPath = value.slice(equalIndex + 1).trim();
            if (name && entryPath) {
                entryList.push({
                    key: name,
                    value: entryPath,
                    explicit: true
                });
                continue;
            }
        }
        const basename = node_path.basename(value, node_path.extname(value));
        entryList.push({
            key: basename,
            value,
            explicit: false
        });
    }
    const keyCount = {};
    for (const { key, explicit } of entryList)if (!explicit) keyCount[key] = (keyCount[key] ?? 0) + 1;
    const keyIndex = {};
    const parsed = {};
    for (const { key, value, explicit } of entryList){
        const needsIndex = !explicit && (keyCount[key] ?? 0) > 1;
        const finalKey = needsIndex ? `${key}${keyIndex[key] ?? 0}` : key;
        if (needsIndex) keyIndex[key] = (keyIndex[key] ?? 0) + 1;
        parsed[finalKey] = value;
    }
    return Object.keys(parsed).length ? parsed : void 0;
};
const applyCliOptions = (config, options, root)=>{
    if (options.root) config.root = root;
    if (options.logLevel) config.logLevel = options.logLevel;
    for (const lib of config.lib){
        if (void 0 !== options.format) lib.format = options.format;
        if (void 0 !== options.bundle) lib.bundle = options.bundle;
        if (void 0 !== options.tsconfig) {
            lib.source ||= {};
            lib.source.tsconfigPath = options.tsconfig;
        }
        const entry = parseEntryOption(options.entry);
        if (void 0 !== entry) {
            lib.source ||= {};
            lib.source.entry = entry;
        }
        const syntax = options.syntax;
        if (void 0 !== syntax) lib.syntax = syntax;
        if (void 0 !== options.dts) lib.dts = options.dts;
        if (void 0 !== options.autoExtension) lib.autoExtension = options.autoExtension;
        if (void 0 !== options.autoExternal) lib.autoExternal = options.autoExternal;
        lib.output ??= {};
        if (void 0 !== options.target) lib.output.target = options.target;
        if (void 0 !== options.minify) lib.output.minify = options.minify;
        if (void 0 !== options.clean) lib.output.cleanDistPath = options.clean;
        const externals = options.externals?.filter(Boolean) ?? [];
        if (externals.length > 0) lib.output.externals = externals;
        if (options.distPath) lib.output.distPath = {
            ...'object' == typeof lib.output.distPath ? lib.output.distPath : {},
            root: options.distPath
        };
    }
};
const init_loadConfig = async (options, root)=>{
    const { content: config, filePath: configFilePath } = await loadConfig_loadConfig({
        cwd: root,
        path: options.config,
        envMode: options.envMode,
        loader: options.configLoader
    });
    if (null === configFilePath) {
        config.lib = [
            {}
        ];
        src_logger.debug('Falling back to CLI options for the default library.');
    }
    applyCliOptions(config, options, root);
    return config;
};
async function init(options) {
    const cwd = process.cwd();
    const root = options.root ? ensureAbsolutePath(cwd, options.root) : cwd;
    const rslib = await createRslib({
        cwd: root,
        config: ()=>init_loadConfig(options, root),
        loadEnv: false === options.env ? false : {
            cwd: getEnvDir(root, options.envDir),
            mode: options.envMode
        }
    });
    return rslib;
}
const RSPACK_BUILD_ERROR = 'Rspack build failed.';
const applyCommonOptions = (cli)=>{
    cli.option('-c, --config <config>', 'specify the configuration file, can be a relative or absolute path').option('--config-loader <loader>', 'Set the config file loader (auto | jiti | native)', {
        default: 'auto'
    }).option('-r, --root <root>', 'specify the project root directory, can be an absolute path or a path relative to cwd').option('--env-mode <mode>', 'specify the env mode to load the `.env.[mode]` file').option('--env-dir <dir>', 'specify the directory to load `.env` files').option('--log-level <level>', 'set the log level (info | warn | error | silent)').option('--lib <id>', 'specify the library (repeatable, e.g. --lib esm --lib cjs)', {
        type: [
            String
        ],
        default: []
    }).option('--no-env', 'Disable loading of `.env` files');
};
function setupCommands() {
    const cli = cac('rslib');
    cli.version("0.21.5");
    applyCommonOptions(cli);
    const buildDescription = `build the library for production ${color.dim('(default if no command is given)')}`;
    const buildCommand = cli.command('', buildDescription).alias('build');
    const inspectCommand = cli.command('inspect', 'inspect the Rsbuild / Rspack configs of Rslib projects');
    const mfDevCommand = cli.command('mf-dev', 'start Rsbuild dev server of Module Federation format');
    buildCommand.option('-w, --watch', 'turn on watch mode, watch for changes and rebuild').option('--entry <entry>', 'set entry file or pattern (repeatable)', {
        type: [
            String
        ],
        default: []
    }).option('--dist-path <dir>', 'set output directory').option('--bundle', 'enable bundle mode (use --no-bundle to disable)').option('--format <format>', 'specify the output format (esm | cjs | umd | mf | iife)').option('--syntax <syntax>', 'set build syntax target (repeatable)').option('--target <target>', 'set runtime target (web | node)').option('--dts', 'emit declaration files (use --no-dts to disable)').option('--externals <pkg>', 'add package to externals (repeatable)', {
        type: [
            String
        ],
        default: []
    }).option('--minify', 'minify output (use --no-minify to disable)').option('--clean', 'clean output directory before build (use --no-clean to disable)').option('--auto-extension', 'control automatic extension redirect (use --no-auto-extension to disable)').option('--auto-external', 'control automatic dependency externalization (use --no-auto-external to disable)').option('--tsconfig <path>', 'use specific tsconfig (relative to project root)').action(async (options)=>{
        try {
            const cliBuild = async ()=>{
                const rslib = await init(options);
                if (options.watch) watchFilesForRestart(getWatchFilesForRestart(rslib), async ()=>{
                    await cliBuild();
                });
                const buildInstance = await rslib.build(options);
                if (options.watch) onBeforeRestart(buildInstance.close);
                else await buildInstance.close();
            };
            await cliBuild();
        } catch (err) {
            const isRspackError = err instanceof Error && err.message === RSPACK_BUILD_ERROR;
            if (!isRspackError) src_logger.error('Failed to build.');
            if (err instanceof AggregateError) for (const error of err.errors)src_logger.error(error);
            else src_logger.error(err);
            process.exit(1);
        }
    });
    inspectCommand.option('--output <output>', 'specify inspect content output path', {
        default: '.rsbuild'
    }).option('--verbose', 'show full function definitions in output').action(async (options)=>{
        try {
            const rslib = await init(options);
            await rslib.inspectConfig({
                lib: options.lib,
                mode: options.mode,
                outputPath: options.output,
                verbose: options.verbose,
                writeToDisk: true
            });
        } catch (err) {
            src_logger.error('Failed to inspect config.');
            src_logger.error(err);
            process.exit(1);
        }
    });
    mfDevCommand.action(async (options)=>{
        try {
            const cliMfDev = async ()=>{
                const rslib = await init(options);
                await rslib.startMFDevServer({
                    lib: options.lib
                });
                watchFilesForRestart(getWatchFilesForRestart(rslib), async ()=>{
                    await cliMfDev();
                });
            };
            await cliMfDev();
        } catch (err) {
            src_logger.error('Failed to start mf-dev.');
            src_logger.error(err);
            process.exit(1);
        }
    });
    cli.help((sections)=>{
        sections.shift();
        for (const section of sections){
            if ('Usage' === section.title) section.body = section.body.replace('$ rslib', color.yellow('$ rslib [command] [options]'));
            if ('Commands' === section.title) section.body = section.body.replace(`         ${buildDescription}`, `build    ${buildDescription}`);
            if (section.title?.startsWith('For more info')) {
                section.title = color.dim('  For details on a sub-command, run');
                section.body = color.dim('  $ rslib <command> -h');
            } else if (section.title) section.title = color.cyan(section.title);
        }
    });
    cli.parse();
}
function initNodeEnv() {
    if (!process.env.NODE_ENV) {
        const command = process.argv[2] ?? 'build';
        process.env.NODE_ENV = [
            'build',
            'inspect'
        ].includes(command) ? 'production' : 'development';
    }
}
function showGreeting() {
    const { npm_execpath, npm_lifecycle_event, NODE_RUN_SCRIPT_NAME } = process.env;
    const isNpx = 'npx' === npm_lifecycle_event;
    const isBun = npm_execpath?.includes('.bun');
    const isNodeRun = Boolean(NODE_RUN_SCRIPT_NAME);
    const prefix = isNpx || isBun || isNodeRun ? '\n' : '';
    src_logger.greet(`${prefix}Rslib v0.21.5\n`);
}
function setupLogLevel() {
    const logLevelIndex = process.argv.findIndex((item)=>'--log-level' === item || '--logLevel' === item);
    if (-1 !== logLevelIndex) {
        const level = process.argv[logLevelIndex + 1];
        if (level && [
            'warn',
            'error',
            'silent'
        ].includes(level) && !isDebug()) src_logger.level = level;
    }
}
function runCLI() {
    process.title = 'rslib-node';
    initNodeEnv();
    setupLogLevel();
    showGreeting();
    try {
        setupCommands();
    } catch (err) {
        src_logger.error('Failed to start Rslib CLI.');
        src_logger.error(err);
        process.exit(1);
    }
}
function mergeLibConfigs(...libArrays) {
    const definedLibArrays = libArrays.filter((lib)=>void 0 !== lib);
    if (0 === definedLibArrays.length) return;
    const mergedById = new Map();
    const itemsWithoutId = [];
    for (const libArray of definedLibArrays)for (const libConfig of libArray)if (void 0 !== libConfig.id) {
        const existing = mergedById.get(libConfig.id);
        if (existing) mergedById.set(libConfig.id, mergeRsbuildConfig(existing, libConfig));
        else mergedById.set(libConfig.id, libConfig);
    } else itemsWithoutId.push(libConfig);
    return [
        ...mergedById.values(),
        ...itemsWithoutId
    ];
}
function mergeRslibConfig(...originalConfigs) {
    const definedConfigs = originalConfigs.filter((config)=>void 0 !== config);
    if (0 === definedConfigs.length) return {};
    const libArrays = definedConfigs.map((config)=>config.lib);
    const configsWithoutLib = definedConfigs.map((config)=>{
        const { lib: _, ...rest } = config;
        return rest;
    });
    const mergedConfig = mergeRsbuildConfig(...configsWithoutLib);
    const mergedLib = mergeLibConfigs(...libArrays);
    if (void 0 !== mergedLib) mergedConfig.lib = mergedLib;
    return mergedConfig;
}
const src_version = "0.21.5";
var rspack_0 = __rspack_external__rsbuild_core_1b356efc.rspack;
export { __rspack_external__rsbuild_core_1b356efc as rsbuild, createRslib, loadConfig_defineConfig as defineConfig, loadConfig_loadConfig as loadConfig, loadEnv, mergeRslibConfig, rspack_0 as rspack, runCLI, src_version as version };
