import { jsxs as _jsxs, jsx as _jsx } from "react/jsx-runtime";
import * as React from "react";
import * as RadioGroupPrimitive from "@radix-ui/react-radio-group";
import { cn } from "./cn.js";
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "./tooltip.js";
const sizeClasses = {
    sm: {
        track: "h-[var(--control-h-sm)] p-[2px]",
        segment: "gap-1 px-2",
    },
    md: {
        track: "h-[var(--control-h-md)] p-[3px]",
        segment: "gap-1.5 px-2.5",
    },
};
/**
 * Single-select control that switches or filters **one** view: a muted track
 * with the active segment lifted onto the canvas surface.
 *
 * Reach for `Tabs` instead when each choice reveals its own panel of content —
 * that is the tablist/tabpanel contract, and a tablist with no panels behind it
 * misreports the page to assistive tech. This is a radiogroup: Radix supplies
 * the roving focus, arrow-key navigation, and `role="radio"` semantics.
 *
 * Controlled only — `value` and `onValueChange` are both required, so there is
 * no second, uncontrolled code path to reason about.
 *
 * `T` is inferred from `options`/`value`, so a caller with a union-typed filter
 * gets that union back in `onValueChange` — an exhaustive `switch` in the
 * handler typechecks with no cast. It defaults to `string` for callers that
 * have no union to name.
 */
export function SegmentedControl({ options, value, onValueChange, disabled, size = "md", "aria-label": ariaLabel, className, }) {
    const s = sizeClasses[size];
    const descriptionId = React.useId();
    return (
    // The provider is the kit's own rather than the host's: the only tooltips
    // it governs are the per-option ones rendered below, so owning it here
    // keeps `title` working without asking every consumer to wrap the control.
    _jsx(TooltipProvider, { children: _jsx(RadioGroupPrimitive.Root, { "data-slot": "segmented-control", "aria-label": ariaLabel, value: value, 
            // Radix types its callback against the DOM's bare string; the only
            // values it can hand back are the option values we rendered, which are
            // `T` by construction.
            onValueChange: (next) => onValueChange(next), disabled: disabled, orientation: "horizontal", className: cn("inline-flex w-fit items-center rounded-[var(--control-r-md)] bg-muted", s.track, className), children: options.map((option) => {
                const optionDescriptionId = `${descriptionId}-${option.value}`;
                const item = (_jsxs(RadioGroupPrimitive.Item, { "data-slot": "segmented-control-item", value: option.value, disabled: option.disabled, "aria-describedby": option.title ? optionDescriptionId : undefined, className: cn("inline-flex h-full items-center justify-center rounded-[var(--control-r-sm)] border border-transparent text-ui whitespace-nowrap text-foreground/60 transition-colors outline-none hover:text-foreground focus-visible:border-ring focus-visible:outline-solid focus-visible:outline-1 focus-visible:outline-offset-0 focus-visible:outline-ring disabled:pointer-events-none disabled:opacity-50 data-[state=checked]:bg-background data-[state=checked]:text-foreground data-[state=checked]:shadow-sm dark:text-muted-foreground dark:hover:text-foreground dark:data-[state=checked]:text-foreground [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4", s.segment), children: [option.icon, option.label] }, option.value));
                if (!option.title)
                    return item;
                return (_jsxs(React.Fragment, { children: [_jsxs(Tooltip, { children: [_jsx(TooltipTrigger, { asChild: true, children: item }), _jsx(TooltipContent, { children: option.title })] }), _jsx("span", { id: optionDescriptionId, className: "sr-only", children: option.title })] }, option.value));
            }) }) }));
}
//# sourceMappingURL=segmented-control.js.map