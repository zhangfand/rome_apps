import { clsx } from "clsx";
import { extendTailwindMerge } from "tailwind-merge";
import { TYPOGRAPHY_ROLES } from "./typography-roles.js";
/**
 * tailwind-merge knows Tailwind's stock font-size scale and nothing else, so
 * an unregistered role reads as an unrecognized `text-*` value and lands in
 * the text-color group: `text-aux` and a caller's `text-muted-foreground` then
 * look like a conflict, and the color silently deletes the role. Registering
 * the roles puts each one back in the font-size group, where it coexists with
 * a color and still loses to a later size.
 */
const twMerge = extendTailwindMerge({
    extend: { theme: { text: TYPOGRAPHY_ROLES } },
});
/**
 * Merge conditional class names, letting later Tailwind utilities win over
 * earlier ones in the same group. Every kit component composes its variants
 * through this, and consumers use it to override those variants from the
 * outside — so it is part of the public surface, not an internal helper.
 */
export function cn(...inputs) {
    return twMerge(clsx(inputs));
}
//# sourceMappingURL=cn.js.map